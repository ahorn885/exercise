# AIDSTATION — Current State

Single rolling-state pointer. Changes on every shipped session. Long-form session narrative lives in `handoffs/`; rolling cross-session items live in `CARRY_FORWARD.md`. **Backlog / features / bugs are tracked in GitHub issues (`ahorn885/exercise`) as of 2026-05-27** — the `Project_Backlog_vN.md` chain is frozen under `archive/backlog/`.

> **Pointer hygiene (2026-05-25):** this file is the *current* pointer, not a narrative archive. The per-session predecessor chain below is a **dated index only** — open the named handoff in `handoffs/` for the full record. Don't paste session narrative back into this file; that's what `handoffs/` is for.

---

## Last shipped session

**APP — DETERMINISTIC SURFACE-SPECIFIC ROUTING WITHIN A DISCIPLINE (#624 narrowed; 2026-06-16, PR [#639](https://github.com/ahorn885/exercise/pull/639); branch `claude/v5-implementation-locations-vgk6fi`; design `designs/Layer4_SurfaceSpecificRouting_624_Design_v1.md`; handoff `handoffs/V5_Implementation_Locations_SurfaceSpecificRouting_624_2026_06_16_Closing_Handoff_v1.md`).** Next slice of the "Locations & Gear" arc. **Andy chose "deterministic intent infra"** (over a prompt-only instruction / closing as handled) via `AskUserQuestion`. **The gap:** post-#635 a multi-surface discipline (Trail Running needs groomed/technical/hill) still resolves EXACT at the nearest locale carrying ANY required surface (home hills for pv=71) and hands the synthesizer one scheduling locale, collapsing every session onto it; the nearby groomed-trail venue (Cleburne, 18 km) is in the `terrain_venues` menu but nothing **routes each session purpose to the surface that trains it**. **Key decision — no new vocab:** a surface's training purpose is **derived from the existing `layer0.terrain_types` columns** (`requires_elevation` / `technical_surface`) → elevation = hill/vert work, non-elev-technical = skill work, flat-non-technical = easy/long aerobic (elevation dominates when both set). So **NO DDL, no Trigger-#2 table.** **Slice 1 shipped:** `session_feasibility.surface_purpose()` classifier + `TerrainResolution.surface_routes` (built in `enrich_resolution_display` from the same nearest-carrier rows as `terrain_venues`, gated to ≥2 distinct purposes across ≥2 distinct locales); EXACT feasibility line renders the per-purpose routing directive ("easy / long aerobic on "Cleburne State Park" (18 km away) (Groomed Trail); hill / vert work on "509 Williams Avenue" (home) (Hill / Rolling); …"). `orchestrator._q_terrain_attributes` reader (mirrors `_q_terrain_names`) → `_FeasibilityInputs.terrain_attrs` → enrich call. `hashing.LAYER4_PROMPT_REVISION` 4→5 (cached plans cold re-synth). **Backward-compatible** (no attrs → `surface_routes` empty → existing menu unchanged). 3 substantive code files + design + tests; **tests/ green 2487 passed / 30 skipped, etl/tests 89; NO DDL** (reads existing columns) — Layer-0 gate + JS harness unaffected. **#624 stays OPEN, narrowed further** to the deferred follow-ups: **Slice 2** (grid session-typing — `DisciplineAllocation.session_types` for the per-slot binding / end-to-end determinism) + **Slice 3** (craft disciplines keep the existing cascade for now). **NEXT (Locations & Gear arc):** #624 Slice 2/3, [#623](https://github.com/ahorn885/exercise/issues/623) (retire assumed basic gear — Trigger #2), [#619](https://github.com/ahorn885/exercise/issues/619) (profile Locations tab + nav IA). **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (Rule #14). *(#622's `0007` migration was **applied to prod Neon + verified 2026-06-16** via read-only `neon-query`: 0 active vessels left in `equipment_items`, 3 new craft aliases (raft→D-019 / sup→D-032 / tt_bike→D-007) at `0A-v1.6.8`, 7 craft-terrain rows, 0 active exercises still naming a vessel — no longer owed.)*

### Predecessor — Crafts out of the equipment picker, into the craft store (#622)

**APP — CRAFTS OUT OF THE EQUIPMENT PICKER, INTO THE CRAFT STORE (#622; 2026-06-16, PR [#637](https://github.com/ahorn885/exercise/pull/637) CI-green + squash-merged to `main`; branch `claude/v5-terrain-feasibility-venue-pick-2iqsmu`; handoff `handoffs/V5_Implementation_Locations_CraftsOutOfEquipment_2026_06_16_Closing_Handoff_v1.md`).** Lead "Gear surface" slice of the "Locations & Gear" arc, investigation-first (read-only `neon-query` vs prod, Rule #14). **Root cause:** #586 added the athlete craft store but **never removed the vessels from `layer0.equipment_items`**, so the per-locale gear editor (`routes/locales._layer0_equipment`, which renders every active equipment row) kept listing bikes/boats under "Sport-Specific — Cycling/Paddle (top-level vessels)". Two mechanisms confirmed independent: discipline feasibility is **craft-store-driven** (`owned_crafts`→`craft_discipline_aliases`/`craft_terrain_compatibility`), exercise resolution is **Layer-2C equipment-pool-driven** (bike exercises named vessels in `equipment_required`). **Fix (Andy ratified via `AskUserQuestion` — Trigger #2 vocab + Trigger #3 cross-layer): layer0 migration `0007`.** **(0C)** retire the 9 vessel rows from `equipment_items` (Road/Mountain/Gravel bike, TT/triathlon bike, Kayak, Canoe, Packraft, Stand-up Paddleboard, Raft); KEEP accessories (Power meter, Helmet, Cycling trainer) — picker auto-excludes, no route/template change. **(0A)** add the 3 vessels missing from the craft store so every bike/boat has a home — `tt_bike`→D-007 Time-Trial Cycling (TRN-001/004), `raft`→D-019 Paddle Rafting (TRN-009/011/017), `sup`→**D-032 Stand-up Paddleboard** (TRN-009/010) — keyspace **confirmed live via neon-query** (corrected an early genesis-grep assumption that SUP had no discipline). **(0B)** de-drift the 27 exercises that named a vessel in `equipment_required` — drop vessel tokens (post-#586 the discipline cascade is the craft-gate; 2C double-gating a bike *exercise* on vessel-equipment is redundant), keep real accessories (Cycling trainer, Backpack, Foam pad, Treadmill); EX174's vessel substitute cleaned. **App:** `athlete.BIKE_TYPES += tt_bike`, `PADDLE_CRAFT_TYPES += sup, raft`, `CRAFT_LABELS` + `CyclingBaseline`/`PaddlingBaseline` Literals widened. **Verified locally against the v1.8.0 baseline gate** (0007 applies clean + idempotent, `validate_layer0` PASS) AND in CI (Layer 0 integrity gate green); full suite **2483 passed / 30 skipped. NO public-schema DDL.** **#622 CLOSED (completed).** **`0007` APPLIED to prod Neon + verified 2026-06-16** (read-only `neon-query`: 0 active vessels in `equipment_items`, 3 new craft aliases at `0A-v1.6.8`, 7 craft-terrain rows, 0 active exercises naming a vessel) — nothing owed. **NEXT (Locations & Gear arc):** [#623](https://github.com/ahorn885/exercise/issues/623) (retire assumed basic gear — Trigger #2 + audit the 2C cascade first), [#619](https://github.com/ahorn885/exercise/issues/619) (profile Locations tab + nav IA), [#624 follow-up](https://github.com/ahorn885/exercise/issues/624) (within-discipline surface routing — Trigger #3). **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (Rule #14).

### Predecessor — Deterministic venue pick (#624 + #618-7)

**APP — DETERMINISTIC VENUE PICK for terrain feasibility (#624 + #618 item 7; 2026-06-16, PR [#635](https://github.com/ahorn885/exercise/pull/635) CI-green + auto-merged to `main`, follow-up PR [#636](https://github.com/ahorn885/exercise/pull/636); branch `claude/planview-polish-handoff-0yiak5` → `claude/terrain-names-superseded-filter`; handoff `handoffs/V5_Implementation_TerrainFeasibility_DeterministicVenuePick_2026_06_16_Closing_Handoff_v1.md`).** Lead slice of the "Locations & Gear" arc, **investigation-first** (Rule #14) via the read-only `neon-query` workflow. **The read OVERTURNED #624's premise:** pv=71's nearby groomed-trail park (Cleburne State Park) **is** saved, **is** in-cluster (~18 km — *nearer* than the recommended Dinosaur Valley SP at ~40 km), and **is** correctly tagged `TRN-002` (Groomed Trail). No terrain-tagging gap, no radius/ranking miss, and **no public-venue discovery exists** — the candidate set is purely the athlete's saved `locale_profiles`; the "Dinosaur Valley" recommendation was **LLM-invented from world knowledge**. **Root cause = synthesis grounding:** the feasibility cascade handed the synthesizer **one locale slug** per discipline (and Trail Running, needing any of TRN-002/003/004, resolved EXACT at *home* via TRN-004 Hill/Rolling), so the LLM editorialized "no nearby groomed trail" + named a park it knew. **Fix (Andy chose "deterministic venue pick" via `AskUserQuestion` — Trigger #1 prompt + Trigger #3 contract):** `locations.cluster_locale_ids` now **haversine-sorts the cluster nearest-first** (wires the sort the resolver long flagged as a stand-in); new `cluster_locale_meta` returns each locale's **display name + distance**; `session_feasibility.enrich_resolution_display` attaches the scheduling locale's name+distance (every tier) and, for EXACT, a **per-terrain nearest-venue menu**; `feasibility_line` renders it by canonical terrain name + locale display name + distance (e.g. *"Groomed Trail at "Cleburne State Park" (18 km away); Hill / Rolling at "509 Williams Avenue" (home)"*) and **bars naming any location not in the list** — fixing **#618-7** too (cites the display name "509 Williams Avenue", never the slug "Williams"). Terrain names read **live from `layer0.terrain_types`** (`_q_terrain_names` — the DB, never the frozen xlsx authoring); `hashing.LAYER4_PROMPT_REVISION` 3→4 (cached plans cold re-synth). **Follow-up #636:** the post-merge `neon-query` verification showed `layer0.terrain_types` keeps every superseded etl-version row (70 for 5 ids) — added the standard `WHERE superseded_at IS NULL` filter to `_q_terrain_names` (matching `layer2b/builder`) so a future rename can't pick a stale name. 4 substantive files + tests; **full suite green 2483 passed / 30 skipped. NO DDL.** **#618 CLOSED (completed — all 7 items shipped via #632/#633/#635).** **#624 stays OPEN, narrowed** to the remaining Layer-2C nuance: the cascade collapses surface varieties (TRN-002/003/004 interchangeable), so a home-hills EXACT satisfies Trail Running before groomed trail is weighed as the *scheduling* locale — surface-specific within-discipline routing is the open follow-up (Trigger #3). **NEXT (Locations & Gear arc continues):** [#622](https://github.com/ahorn885/exercise/issues/622) (movable crafts still under Equipment) + [#623](https://github.com/ahorn885/exercise/issues/623) (retire assumed basic gear — Trigger #2) as the "Gear surface" slice; [#619](https://github.com/ahorn885/exercise/issues/619) (profile Locations tab + nav IA). **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (Rule #14).

### Predecessor — Plan-view pv=71 polish (#618 — 6 of 7 items)

**APP — PLAN-VIEW pv=71 POLISH (#618 — 6 of 7 items; 2026-06-15, PRs [#632](https://github.com/ahorn885/exercise/pull/632) + [#633](https://github.com/ahorn885/exercise/pull/633), both CI-green + auto-merged to `main`; branch `claude/practical-newton-lk98tt`; handoff `handoffs/V5_Implementation_PlanView_pv71_Polish_2026_06_15_Closing_Handoff_v1.md`).** Render-side cleanups from Andy's live plan-71 walkthrough (the freshest "app itself" signal after several Layer 0 / ops sessions). **PR #632 (render):** dropped the internal "Pattern A" eyebrow; replaced the raw `created_via` chip ("plan create") with a state-appropriate **lifecycle label** (Active/Upcoming/Completed/Archived — mirrors the plans-list bucketing; `_load_plan_version` now also selects `completed_at`/`archived_at`); moved the coaching/workout-type flags (e.g. "long slow distance") **up with the coach notes** instead of below the per-block detail; and render **explicit rest days** for off days (`routes/plan_create._plan_days_with_rest_gaps` gap-fills the plan span; degrades to a no-fill passthrough on the string-dated render harness). **PR #633 (plan-gen + render):** fixed the discipline-**slug leak** (`climbing_roped` was echoing into athlete-facing `coaching_intent`) by stripping the raw toggle slug from BOTH prompt feeds — the `per_phase` session-grid `SKILL-GATED` annotation **and** the `validator` skill-capability correction feedback — plus a coaching-VOICE rule barring internal ids/slugs across create + T1/T2/T3 refresh; bumped `hashing.LAYER4_PROMPT_REVISION` 2→3 so cached plans re-synthesize cold with the fix (Trigger #1, Andy-confirmed). Also dropped the redundant nutrition `load_tier` word from the day-fuel macros line (it duplicated the phase name + the per-session intensity chip; Andy-confirmed). 2 substantive files/PR + tests; **full suite green 2481 passed / 30 skipped. NO DDL.** **#618 stays OPEN for item 7** — the address inconsistency ("Williams" vs "509 Williams"), which is **locale data**, not render. **NEXT (Andy-directed): a "Locations & Gear" session** — lead with [#624](https://github.com/ahorn885/exercise/issues/624) (terrain detection — a nearby groomed-trail park not discovered/tagged) + #618-item7 (locale-label normalization), wrap in [#622](https://github.com/ahorn885/exercise/issues/622) (movable crafts still under Equipment) / [#623](https://github.com/ahorn885/exercise/issues/623) (retire assumed basic gear — Trigger #2) / [#619](https://github.com/ahorn885/exercise/issues/619) (profile Locations tab + nav IA). **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (Rule #14).

### Predecessor — Layer 0 `primary_movement` backfill + ops automation (#616 / #630)

**LAYER 0 — `primary_movement` BACKFILL (migration `0006`) + standing `validate_layer0` guard (2026-06-15, PR [#615](https://github.com/ahorn885/exercise/pull/615) — CI green, squash-merged to `main`; closes [#616](https://github.com/ahorn885/exercise/issues/616); branch `claude/intelligent-euler-hjggno`; handoff `handoffs/V5_Implementation_Layer0_PrimaryMovementBackfill_0006_2026_06_15_Closing_Handoff_v1.md`).** Closed the freshest genesis-collapse owed item. **Confirmed (against the v1.7.0 baseline) a real prod gap:** all **24 active** `layer0.disciplines` rows have `primary_movement` NULL → live nutrition is movement-blind (Layer 2E `_movement_sport_profile` defaults every discipline to `multi_sport`; the climbing protein bump never fires). **Root cause:** the column is set by a migration but a full ETL re-extraction of the disciplines dimension (`0A-v1.6.0`→`v1.6.7`) drops it — the canon normalizer stamps `endurance_profile` but not `primary_movement` (confirmed: every active row has `endurance_profile` set, `primary_movement` NULL). **The old `etl/sources/migrate_disciplines_add_primary_movement_v1.sql` was STALE** (pre-drift `D-001..D-029` keyspace; its verify block would RAISE on D-030/031/032) so the genesis handoff's "just run the owed runner" would have FAILED. **Decision (Andy via `AskUserQuestion`):** fix it via a **`0006` migration + validate guard** (over patching the etl/sources migration or also curating into `discipline_canon.py`). **Shipped:** `etl/migrations/layer0/0006_populate_disciplines_primary_movement.sql` — backfills the **24-discipline canon** (mapped to `ENUM_MOVEMENTS`) as a **serving-relevant Shape-2 edit** (supersede + re-insert at bumped `0A-v1.6.7`→`0A-v1.6.8`, so the per-table digest advances and the movement-blind plan-gen caches invalidate); idempotent (only backfills active NULL rows; 3× apply = clean no-op, no UNIQUE collision); + a new `etl/layer0/validation/primary_movement_check.py` wired into `validate_layer0` (every active discipline must carry an `ENUM_MOVEMENTS` value — fix-not-waive) so this regression can't silently recur. Old `etl/sources` migration **deleted** + de-referenced from `run_owed_layer0_migrations.sql` (now 3 includes). **Verified:** real-Postgres gate PASS end-to-end (all 10 checks incl. the new one; 24 active populated, 24 old rows kept as superseded history at `0A-v1.6.7`); full suite **2565 passed / 30 skipped**. **DONE — applied to prod (Andy, 2026-06-15) + verified via the read-only diagnostics workflow: 24 active / 0 NULL / `0A-v1.6.8`;** the genesis **baseline was re-dumped to `etl/output/layer0_etl_v1.8.0.sql`** (PR [#626](https://github.com/ahorn885/exercise/pull/626), gate-validated) so it no longer lags live.

**Ops automation shipped this session (the "decide-not-do" setup; handoff `handoffs/V5_Implementation_Layer0_PrimaryMovement_Applied_OpsAutomation_2026_06_15_Closing_Handoff_v1.md`):** cloud `workflow_dispatch` workflows triggerable by Claude — **`layer0-apply`** (applies migrations to prod, gated by the **`production` environment** required-reviewer approval), **`layer0-redump`** (PR [#625](https://github.com/ahorn885/exercise/pull/625)), **`neon-query`** read-only diagnostics (PR [#627](https://github.com/ahorn885/exercise/pull/627)), + a nightly **`layer0-validate-live`** running `validate_layer0` against live prod via the read-only role (this PR); **auto-merge + branch protection** (required checks, 0 approvals, admin-bypass off → PRs self-land on green), **CODEOWNERS** soft-nudge on `etl/migrations/` (PR [#628](https://github.com/ahorn885/exercise/pull/628)); the `.claude/settings.json` SessionStart git-pull hook (PR [#617](https://github.com/ahorn885/exercise/pull/617)); `DIAG_TOKEN` rotated (it was never actually repo-leaked — false alarm). **Secrets in use:** `NEON_DATABASE_URL` (apply/redump), `NEON_RO_DATABASE_URL` (read-only diag + validate-live). All four capabilities test-fired green — incl. `layer0-validate-live` against **live** prod → `RESULT: PASS` (read-only role covers every check; no grant gap). **The operating model is now codified durably** in `CLAUDE.md` (Environment quick-reference → *Ops automation*) + the `CARRY_FORWARD.md` *Ops automation / operating model* section; record issue [#630](https://github.com/ahorn885/exercise/issues/630) (closed completed). **Latest handoff: `handoffs/V5_Implementation_Layer0_OpsAutomation_OperatingModelCodified_2026_06_15_Closing_Handoff_v1.md`.** **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (Rule #14).

### Predecessor — Layer 0 genesis collapse to the v1.7.0 baseline (#604)

**LAYER 0 — GENESIS COLLAPSE to the v1.7.0 live baseline (single source of truth / [#604](https://github.com/ahorn885/exercise/issues/604); 2026-06-15, PR [#612](https://github.com/ahorn885/exercise/pull/612) — CI green, MERGED to `main`; branch `claude/genesis-refresh-v1.7.0`; handoff `handoffs/V5_Implementation_Layer0_GenesisCollapse_v1_7_0_2026_06_15_Closing_Handoff_v1.md`).** Closes #604. The Layer-0 gate previously loaded `schema.sql` + a frozen base snapshot + migrations `0001–0005`; it now loads a **single self-contained baseline** — `etl/output/layer0_etl_v1.7.0.sql`, a full `pg_dump` of live `layer0` (schema + data, 28 tables / 34,456 rows; Andy's hands — Neon egress blocked). **Decisions (Andy-ratified via `AskUserQuestion`):** **Option A — collapse** (over keep-layered / consolidate-without-redump): `schema.sql` + migrations `0001–0005` (effects already in live) + the `batch_a`/`K`/`K2` equipment scaffolding + the old `v1.4.0–v1.6.7` snapshots → archived under `etl/_archive/pre_v1.7.0_baseline/`; **new DDL starts at migration `0006`**. **Family-map (Trigger #3):** repointing the `TestLayer0TableFamilyMap` drift guard at the baseline surfaced 2 versioned tables absent from `_LAYER0_TABLE_FAMILY` — Andy chose **keep both out** (no cache change): `supplement_vocabulary` (own `supp_vocab.*` line, 2E reads live — intentional per the existing note) + `discipline_technique_foci` (0B-versioned, **no reader** — dead serving data), now locked by `_FAMILY_MAP_EXCEPTIONS` + a test. **Surfaced the drift #604 was for:** the genesis lag (live had `GHD`, v1.6.7 didn't) closed; `discipline_technique_foci` — a live table whose DDL lived only in retired scaffolding — now captured. **Gate (`ci.yml`):** loads only the baseline, **stripping the PG18 `\restrict`/`\unrestrict` + `transaction_timeout` dump artifacts at load** (raw `pg_dump` loads with no hand-editing); bumped `postgres:16→17` to match prod Neon; migration loop now `0006+`. **CI-found bug fixed:** the first gate run failed on a latent **`nullglob` + `ls $glob`** bug — the now-empty migrations dir made `ls` list the cwd, so the loop tried to `psql DATABASE.md`; fixed by iterating the glob directly. **K3 resolved (Andy 2026-06-15):** the K3 equipment additions (`Rope`/`Quickdraws`/`Harness`/`Climbing gym membership`/`Crash pad`) are **unwanted** — climbing gear lives in `sport_specific_gear_toggles` (`Climbing — roped` et al.), so [#613](https://github.com/ahorn885/exercise/issues/613) is closed not-planned, `populate_equipment_items_K3_additions.sql` is archived, and its `\ir` is dropped from `run_owed_layer0_migrations.sql`. (Briefly over-archived mid-session then handled correctly once Andy confirmed.) **Repoints off the archived `schema.sql`:** `test_layer2c_prep` + `db.py` (`apply_schema` retired, no callers). **Verified:** gate PASS on local PG16 (stricter — rejects the stripped PG17 GUC), full suite **2476 passed / 30 skipped**, CI green. **NEXT:** resume the WS-H Slice-5b remainder / new functionality. **OWED (surfaced this session, Andy's hands):** `run_owed_layer0_migrations.sql` is still owed for `migrate_disciplines_add_primary_movement_v1.sql` — live `disciplines.primary_movement` is **empty (0 active rows)** and Layer 2A SELECTs it (HARD prereq); the runner's other 3 includes are already applied (idempotent re-runs). **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-H Event Windows Slice 5b (onboarding event-window panel / F5)

**WS-H — EVENT WINDOWS Slice 5b BUILT (onboarding event-window panel / F5, #608 item 3 — the arc's LAST item; 2026-06-14, PR OPEN [CI pending]; branch `claude/plan-gen-review-panel-mqah8e`; arc `designs/Event_Windows_Design_v1.md` §6/§F5; **north-star** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §2 WS-H).** Continued after merging item 2 (Andy: "merge and keep going"). **Decision (Andy chose via `AskUserQuestion`):** surface F5 as a **lightweight optional card** (over a gated 7th onboarding step / over closing the item). **Shipped:** an "Optional · event windows" card on the **Locations** onboarding step (`templates/onboarding/locales.html`) — a link into the live `/profile/event-windows` editor (`return_to=onboarding.locales`), kept **out of the home-location gate** so it never blocks setup; placed with Locations since an away window reuses the locales built there. Generalized the editor's back-link label: `routes/profile._event_windows_return_label(return_to)` derives **"setup"** (from `/onboarding…`) vs **"plan generation"** (default), so the banner reads right from either origin and stays consistent across the add/delete redirects (which preserve `return_to` but not a separate label); `event_windows.html` renders `← Back to {{ return_to_label or 'plan generation' }}`. 1 substantive template + 1 route/template label tweak + 2 test edits. **NO DDL, no prompt/vocab/cache-key change** — reuses the live editor + routes. +1 test (label-from-origin) + extended the onboarding locales render test. **Full suite green: 2475 passed / 30 skipped.** **Owes nothing** (no DDL). **#608 — once this merges, all three items are done → CLOSE #608 (completed).** Item 2 (form-state) merged earlier this session as PR [#610](https://github.com/ahorn885/exercise/pull/610). **NEXT:** **#604** (still owed the live `pg_dump` + Rule #12 sign-off / K3 decision) OR new functionality (off-plan #592 race terrain/weather, #593 reduced-volume travel days). **STILL OWED (carried, unrelated):** the post-#572 live **T3 *refresh*** re-verify (diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-H Event Windows Slice 5b (2b round-trip form-state preservation)

**WS-H — EVENT WINDOWS Slice 5b BUILT + MERGED (2b round-trip form-state preservation / #608 item 2; 2026-06-14, PR [#610](https://github.com/ahorn885/exercise/pull/610) squash-merged to `main`, GH Actions CI green [unit suite / JS harness / Layer 0 gate] + Vercel preview deploy green; branch `claude/plan-gen-review-panel-mqah8e`; arc `designs/Event_Windows_Design_v1.md` §6/§F1; **north-star** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §2 WS-H).** Picked up the Slice-5b/F1 closing handoff (Andy: "let's keep going"). Ran the Rule #9 sweep — clean (the one ❌ `etl/output/layer0_etl_v1.7.0.sql` is the known-owed #604 `pg_dump`, not drift). Of the two remaining #608 items, **Andy chose item 2** (form-state) via `AskUserQuestion`, with the **server-side session stash** approach (over a CSP carve-out). **Problem:** creating a not-yet-saved away destination mid-window-capture round-trips out to `/locales/new` and back, and the half-filled window form (dates / constraint / brought-craft / notes) **reset** — strict CSP forbids the inline-JS client-side fix. **Shipped:** `routes/profile.py` — `_stash_event_window_draft` / `_pop_event_window_draft` (draft **consumed once** on next render, so no stale-draft leak) + a new `POST /profile/event-windows/new-locale` route that stashes the in-progress form then hands off to `/locales/new` with a `return_to` back here (carrying any plan-gen `return_to`); `event_windows()` GET pops the draft into the template; Rule #15 `print()` on the stash. `templates/profile/event_windows.html` — the "Add a new location" control is now a **`formaction`+`formnovalidate` submit button** (POSTs the form to the stash route, no JS) and every field **repopulates from `draft`**; a first **visually-hidden duplicate** of the primary submit keeps **"Add window" the Enter-key default** so implicit submission doesn't bounce to `/locales/new`. **Mirrors `locales._stash_return_to`'s own session-stashed round-trip.** 2 substantive files (`routes/profile.py`, `templates/profile/event_windows.html`) + 1 test file. **NO DDL, no prompt/vocab/cache-key change** — reuses the existing windows routes/repo; only the capture surface changed; `return_to` stays open-redirect-guarded. +3 tests (draft repopulation, single-consume stash w/ multi-valued brought-craft, the implicit-submission ordering); updated the existing capture test for the link→button change. **Full suite green: 2474 passed / 30 skipped.** **Owes nothing** (no DDL). **#608 REMAINS OPEN** for the last item: **(3) the onboarding event-window panel (F5)** — surface the capture during onboarding (lean toward a lightweight optional link/card on an existing step, not a gated 7th step). OR **#604** (still owed the live `pg_dump` + Rule #12 sign-off / K3 decision). **STILL OWED (carried, unrelated):** the post-#572 live **T3 *refresh*** re-verify (diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-H Event Windows Slice 5b (plan-gen review panel / F1)

**WS-H — EVENT WINDOWS Slice 5b BUILT + MERGED (plan-gen review/edit/append panel / F1; 2026-06-14, PR [#609](https://github.com/ahorn885/exercise/pull/609) squash-merged to `main`, CI green + Vercel deploy green; **Part of [#608](https://github.com/ahorn885/exercise/issues/608)** item 1 of 3 — #608 stays open for items 2–3; branch `claude/eventwindows-5a-capture-ux-2kzr5d` [harness-pinned name says 5a; scope is 5b — kept per "never push to a different branch"]; arc `designs/Event_Windows_Design_v1.md` §6/§F1; **north-star** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §2 WS-H).** The arc's last real UX piece. Picked up from the Slice-5a handoff (Andy: "5b is next"). Ran the Rule #9 sweep — clean (the one ❌ `etl/output/layer0_etl_v1.7.0.sql` is the known-owed #604 `pg_dump`, not drift). **Decision (Andy chose, in-chat):** surface the event windows for **review at plan generation** via **Option 1 — lean review on the create form + round-trip edit on the dedicated page** (over a fully-inline panel, which strict-CSP would saddle with a start-date-reset-on-every-edit wrinkle since no inline JS can preserve the field). **Shipped:** the create form (`/plans/v2/new`) now lists the athlete's **upcoming** event windows (`end_date >= today`) for review, with an "Add / edit event windows" button that round-trips to the now-nav-reachable `/profile/event-windows` editor and **returns to the create form** (a safe-local-path `return_to`, open-redirect-guarded, mirroring `locales._stash_return_to`); the editor gains a "back to plan generation" link + threads `return_to` through its add/delete forms **and** the nested inline new-location create link, so append/edit (incl. creating a brand-new away destination) all bounce back to where the athlete started. **Editing stays on the one editor page — single source of truth, no duplicated capture UI.** 4 substantive files (`routes/plan_create.py`, `routes/profile.py`, `templates/plan_create/new_form.html`, `templates/profile/event_windows.html`) + 1 test file. **NO DDL, no prompt-wording change, no vocab, no cache-key change** — reuses the existing `athlete_event_windows_repo` + routes; only the capture/review surface changed. +5 render/guard tests (panel populated + empty, the round-trip back-link + `return_to` threading, the open-redirect guard). **Full suite green: 2472 passed / 30 skipped.** **Slice 5b owes nothing** (no DDL; reuses live routes/repo). **#608 REMAINS OPEN** for the two deferred items: (2) the 2b round-trip start-date form-state preservation (strict-CSP-blocked without inline JS) + (3) the onboarding event-window panel (F5). **NEXT: #608 items 2–3** (the Slice-5b remainder), OR **#604** (still owed the live `pg_dump` + Rule #12 sign-off / K3 decision). **STILL OWED (carried, unrelated):** the post-#572 live **T3 *refresh*** re-verify (diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-H Event Windows Slice 5a (capture UX: nav + craft↔locale)

**WS-H — EVENT WINDOWS Slice 5a BUILT + MERGED (capture UX: nav reachability + craft↔locale relocation; 2026-06-14, PR [#607](https://github.com/ahorn885/exercise/pull/607) squash-merged to `main`, CI green; branch `claude/big-data-migration-604-yx7j6l` [harness-pinned for #604; kept per "never push to a different branch" — scope is Slice 5a]; handoff `handoffs/V5_Implementation_WSH_EventWindows_Slice5a_CaptureUX_NavCraftLocale_2026_06_14_Closing_Handoff_v1.md`; arc `designs/Event_Windows_Design_v1.md` §6/§F5; **north-star** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §2 WS-H).** The in-flight WS-H arc's UX tail. **Session start:** Andy nudged #604 ("the big data migration isn't done yet"); re-investigated → parts 1–2 (genesis refresh) still blocked on the live `pg_dump` (owed-Andy's-hands, Neon egress), and part 3 (scaffolding retirement) is genuinely gated (Rule #12 sign-off + a K3 entanglement: `run_owed_layer0_migrations.sql:39` still `\ir`s K3, its deploy is owed, and it carries the retired `Crash pad`; `batch_a` asserts the retired `Bench press rack`; K/K2 collide with older archive copies). Surfaced via `AskUserQuestion`; **Andy chose "move on the other slices."** So #604 stays parked (fully captured in the handoff §2 + CARRY_FORWARD) and the session pivoted to **Slice 5** (design §6 "capture UX polish"). Slice 5 is a 4-item grab-bag (nav-link / plan-gen review panel / craft↔locale relocation / 2b form-state); shipped the two clean **no-trigger** items as **Slice 5a**: **(1) nav reachability** — `/profile/event-windows` was built but had **no nav entry anywhere**; added it to the sidebar dropdown + mobile drawer (`profile.event_windows`). **(2) Relocated the (b) standing craft↔locale capture** off the event-windows page onto the **per-locale edit page** (`/locales/<locale>/edit`), where the predecessor explicitly parked it (craft kept at a place is a property of the place): new `POST /locales/<locale>/crafts → save_locale_crafts` on the **locales** blueprint (replace-all; none-checked clears) + a guarded "Craft you keep here" card in `templates/locales/form.html` (`crafts_here` drives checked state) + removed the card/routes/`craft_locales` kwarg from `routes/profile.py` + `event_windows.html` (now a one-line link to the locations page). **Repo + orchestrator union resolution byte-identical — only the capture surface moved; NO DDL, no cache-key change, no vocab.** 4 substantive files (`routes/locales.py`, `routes/profile.py`, `templates/locales/form.html`, `templates/profile/event_windows.html`) + 2 mechanical nav templates + 1 test file. +1 test (`test_form_renders_craft_kept_here`); event-windows render test updated. **Full suite green: 2468 passed / 30 skipped.** **Slice 5a owes nothing** (no DDL; reuses live routes/repo). **NEXT: Slice 5b — the plan-gen review/edit/append panel (F1)** (restores the v1 `/coaching/review` UX; larger, touches plan-create flow → its own slice) + the 2b form-state preservation + onboarding panel; OR **#604** (still owed the live `pg_dump` + Rule #12 sign-off / K3 decision). **STILL OWED (carried, unrelated):** the post-#572 live **T3 *refresh*** re-verify (diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-H Event Windows Slice 4 (away craft)

**WS-H — EVENT WINDOWS Slice 4 BUILT + MERGED (away craft / F4; 2026-06-14, PR [#605](https://github.com/ahorn885/exercise/pull/605) squash-merged to `main`, CI green; branch `claude/604-migration-xsqcb3` [harness-pinned name for the blocked #604 migration; scope is Slice 4]; spec `designs/Event_Windows_Slice4_AwayCraft_Spec_v1.md`; handoff `handoffs/V5_Implementation_WSH_EventWindows_Slice4_AwayCraft_2026_06_14_Closing_Handoff_v1.md`; arc `designs/Event_Windows_Design_v1.md` §F4; **north-star** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §2 WS-H).** **Closed [#581](https://github.com/ahorn885/exercise/issues/581) (completed)** — the literal Phase H (b)+(c). The away segment's `owned_crafts`, hard-coded `[]` since Slice 2a (the F4 placeholder), now fills from two declared surfaces fed to the EXISTING unified cascade: **(c) brought-craft on the away window** ∪ **(b) standing craft kept at a locale in the destination cluster** → `away_crafts = sorted(set(brought) | {standing at each away_cluster locale})`. A brought/kept packraft now resolves D-009 via the craft tiers instead of degrading to strength. **Session pivot:** #604 (vocab single-source-of-truth) needs a `pg_dump` of live `layer0` — blocked (Neon egress); Andy chose (`AskUserQuestion`) to pivot to Slice 4. **Forks (Andy-ratified 2026-06-14):** **both (b)+(c) in one slice** (over the 5-file ceiling, accepted — WS-B/C precedent); **CSV `TEXT brought_craft`** column for (c) + a **`athlete_craft_locale` join table** for (b); validate against the closed `BIKE_TYPES ∪ PADDLE_CRAFT_TYPES` enum (a rented/kept-there craft is legit away, not gated to the owned-home set). **Shipped:** `init_db` DDL (`brought_craft` column + `athlete_craft_locale` table) + `athlete_event_windows_repo` (brought-craft load/validate, away-only) + new `athlete_craft_locale_repo` (b store + eviction) + `session_feasibility.EventWindowOverride.brought_craft` + `orchestrator` (load craft↔locale map once in `_build_event_window_overlay`; away `owned_crafts` union; Rule #15 `(brought=… standing=…)` provenance) + `hashing` (brought_craft folds into `compute_event_windows_hash`) + `per_phase` (dropped the now-false hard-coded "no brought craft" away-label clause → "any craft you have there"; a **correctness fix to previously-approved Trigger-#1 wording**) + `routes/profile` + `event_windows.html` (brought-craft multi-select + "craft you keep at a location" section). **Cache:** (c) brought_craft in the windows hash (declared window field); (b) craft↔locale via **eviction-on-write only** (mirrors how home `owned_crafts` rides Layer-1 eviction, not a content hash). **Empty union byte-identical to the Slice-2a `owned_crafts=[]` path.** +15 tests (`TestAwayCraft` spy on `_resolve_included_feasibility` + `TestCraftLocaleRepo` + brought-craft repo/hash). **Full suite green: 2467 passed / 30 skipped.** Layer-0 gate untouched (new table is an app migration). **DDL APPLIED on Neon (Andy 2026-06-14)** → #605 merged; Slice 4 fully live. **NEXT: Slice 5 — capture UX polish** (nav-link to `/profile/event-windows`; plan-gen review panel; move the (b) craft↔locale capture to the locale page — parked on the event-windows page this slice to avoid touching `routes/locales.py`) OR **#604** (still owed the live `pg_dump`). **STILL OWED (carried, unrelated):** the post-#572 live **T3 *refresh*** re-verify (diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-H Event Windows Slice 3 (category equipment baselines)

**WS-H — EVENT WINDOWS Slice 3 BUILT + MERGED (category equipment baselines / F8; 2026-06-14, PR [#603](https://github.com/ahorn885/exercise/pull/603) squash-merged to `main`, CI green; branch `claude/v5-wsh-eventwindows-inline-create-rt69cq` [harness-pinned name; scope is Slice 3]; handoff `handoffs/V5_Implementation_WSH_EventWindows_Slice3_CategoryBaselines_2026_06_14_Closing_Handoff_v1.md`; arc `designs/Event_Windows_Design_v1.md` §F8; **north-star** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §2 WS-H).** Closes the cold-start gap that made "away" useless until logged: a not-yet-logged locale (an away destination created inline per Slice 2b, or a cold home gym) links no `gym_profile` and carries no logged terrain, so the feasibility cascade degrades every discipline to near-strength. **F8:** until the athlete logs actuals on arrival, a locale whose **category** has an authored baseline ASSUMES that baseline's equipment + terrain; logging actuals → T1/T2 refresh of that window (the arrival-regen loop). **Design (all Andy-ratified 2026-06-14):** **one authored, editable baseline per logical category** (commercial / hotel / climbing / pool — NOT a crowd aggregate); **replace** semantics (baseline applies only when zero logged equipment/terrain; any logged value wins); **universal scope** (home + away — the away env already resolves through the two cluster fns, so the away cold-start fix falls out with **no away-path change**, and a cold home locale benefits too); **contents ratified item-by-item (Trigger #2)** — commercial 28-item / hotel 7 / climbing 4 / pool terrain-only, every equipment token reconciled to its exact `layer0.equipment_items` canonical name, terrain ids TRN-001 Road / TRN-016 Indoor / TRN-014 ClimbingGym / TRN-008 Pool; **overlay mark (Trigger #1 wording ratified)** — a cold away segment carries `EventWindowSegment.assumed_baseline_category` → "log actuals on arrival to refine this window" note. **Shipped (5 substantive):** new `layer0.location_category_equipment_baseline` (migration **0005**, FK-self-checking verify block) + `locations.py` (`load_category_baselines` + `_CATEGORY_BASELINE_KEY` 5+2 slug→4 map + `locale_assumed_baseline_display` + the substitution inside `cluster_equipment_by_locale`/`cluster_terrain_by_locale`, **gated behind a non-empty baseline load** → pre-migration/baseline-unaware callers byte-identical, Rule #15 logged) + `orchestrator.py` (register table in `_LAYER0_TABLE_FAMILY` 0C + set the overlay flag on cold away segments) + `session_feasibility.EventWindowSegment.assumed_baseline_category` + `per_phase._event_window_label` (the note). **Cache:** registered in `_LAYER0_TABLE_FAMILY` (0C) so a baseline edit → 0C `etl_version_set` digest shift → plan keys change → affected plans re-synthesize. +`tests/test_layer4_location_baselines.py` + overlay-note + `_LAYER0_TABLE_FAMILY` + slug-coverage drift guards. **Full suite green: 2452 passed / 30 skipped.** **CI green** (all 4 real checks). **Post-apply token correction:** the first apply surfaced (Andy pasted the live vocab, Rule #14) that `Bench press rack`/`Climbing Wall`/`Crash pad` are NOT live canonical names (wrong/renamed in a later vocab pass) → corrected: hotel `Bench press rack`→**`Bench`**; climbing `Climbing Wall`→**`Treadwall`** + drop `Crash pad` + add **`Campus board`** (climbing feasibility rests on the TRN-014 Climbing Gym terrain, already present). All corrected tokens are in both live vocab + CI genesis, so the 0005 equipment FK-check is **hard `EXCEPTION`** (the temporary NOTICE softening reverted); gate reproduced locally → PASS. **Migration 0005 APPLIED on Neon (Andy 2026-06-14)** + the two corrective `UPDATE`s (0-row missing-tag check confirms clean) → **#603 squash-merged to `main`; Slice 3 fully live.** **FILED [#604](https://github.com/ahorn885/exercise/issues/604) (vocab single-source-of-truth):** the token mix-up was traced to sourcing the *retired* `etl/sources` xlsx scaffolding (not a live-data drift — the WS-V reconciliation held); #604 is the design-first follow-up to refresh the layer0 genesis snapshot from live + archive the stale scaffolding (Andy chose the full-genesis-refresh path; `pg_dump` is owed-Andy's-hands). **NEXT: Slice 4 — away craft** (the literal WS-H #581 (b)+(c) — craft↔locale ∪ craft↔window → populates the away env's `owned_crafts`, today hard-coded `[]`; DDL, design-first) OR **#604** genesis refresh. **STILL OWED (carried, unrelated):** the post-#572 live **T3 *refresh*** re-verify (diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-H Event Windows Slice 2b (inline-create-a-new-destination)

**WS-H — EVENT WINDOWS Slice 2b BUILT + MERGED (inline-create-a-new-destination capture; 2026-06-14, PR [#601](https://github.com/ahorn885/exercise/pull/601) squash-merged to `main`, CI green; branch `claude/eventwindows-away-windows-xon8mb`; handoff `handoffs/V5_Implementation_WSH_EventWindows_Slice2b_InlineCreate_2026_06_14_Closing_Handoff_v1.md`; spec `designs/Event_Windows_Slice2_AwayWindows_Spec_v1.md` §5 Slice-2b; arc `designs/Event_Windows_Design_v1.md`; **north-star** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §2 WS-H).** Closed the UX follow-up Slice 2a deferred: the away-window destination field on `/profile/event-windows` now links into the **existing** `/locales/new` Mapbox flow with a `return_to` back to the event-windows page, so an athlete can **create a not-yet-saved destination during window capture** and bounce straight back to finish the window — F1/F2 ("the event's location may not exist yet"). **Reuse, not re-implementation:** `new_locale` already owns geocoding-consent disclosure, place search, chain detection, the `mapbox_id` / `gym_profiles.mapbox_id UNIQUE` dedup (differentiator #8 — two athletes' "Belfast hotel" converge into shared crowd data), the manual fallback, AND the `return_to` round-trip mechanism (`locales._stash_return_to` / `_locale_flow_redirect`, session-stashed once + consumed on save). So 2b is the connecting link only — **no embedded search UI** (would duplicate the consent + dedup machinery and need inline JS, which strict-CSP `app.py:439` forbids), **no new route, no schema change, no DDL.** **Template-only:** `templates/profile/event_windows.html` gains the "Add a new location" link under the away dropdown (carrying `return_to=url_for('profile.event_windows')`) + an updated header comment; +1 render-smoke test in `tests/test_redesign_locales_form_render.py` (asserts the 2a pick-existing dropdown AND the 2b inline-create link + return_to render, CSP-clean). **Full suite green: 2433 passed / 30 skipped.** **NOTHING owed Andy's hands** (no DDL; reuses live routes). **NEXT: Slice 3** — category equipment baselines for commercial/hotel/climbing gyms (Trigger #2 — baseline *contents* need Andy's sign-off; this is what makes away useful *cold*, since today a never-logged destination degrades to near-strength). **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-H Event Windows Slice 2a (away windows + counts-follow-away)

**WS-H — EVENT WINDOWS Slice 2a BUILT + LIVE (away windows + counts-follow-away; 2026-06-14, PR [#600](https://github.com/ahorn885/exercise/pull/600) squash-merged to `main`; branch `claude/eventwindows-refresh-overlay-lbw9y6`; handoff `handoffs/V5_Implementation_WSH_EventWindows_Slice2a_AwayWindows_2026_06_14_Closing_Handoff_v1.md`; spec `designs/Event_Windows_Slice2_AwayWindows_Spec_v1.md`; arc `designs/Event_Windows_Design_v1.md`; **north-star** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §2 WS-H).** Wrote the build-ready spec then built it (the arc design §6 bullet 2): the third `override_type='away'` — a date-bounded window where the athlete trains from a *different* location whose environment **replaces** the home cluster (vs Slice 1's two *subtractive* home types). Design-first (trips Trigger #1 away wording + Trigger #3 DDL); Andy ratified all three decisions in-session. **DDL APPLIED on Neon (Andy 2026-06-14) → Slice 2a fully live.** Spec contents (**revised same-day per Andy's feedback**): **(DDL)** ONE ALTER — `away_locale` column on `athlete_event_windows` + `override_type='away'` (the cluster *anchor*). **Discriminator DROPPED:** Andy's call that the away env is the destination's **own radius cluster** (anchor + 26.2mi, *same logic as home*) dissolves the need to fence away locales out of the home cluster — a far destination self-excludes by distance, a near one is honestly reachable. **(Resolution)** generalize `locations.cluster_locale_ids(anchor_locale=None)` (None → today's preferred-home, byte-identical) and call it with `anchor_locale=away_locale` → the away cluster; terrain/equip from `cluster_terrain/equipment_by_locale(away_cluster)`, **`owned_crafts=[]`** (F4, away craft is Slice 4) — fed to the **existing, single** unified cascade (no new resolution logic; `owned_crafts` becomes env-dependent via a small optional kwarg on `_resolve_included_feasibility`, default None → byte-identical). `away` takes precedence over a subtractive override on the same dates. **Confirmed for Andy: ONE unified cascade, not two — intentional (WS-I Slice B #588), not drift;** away reuses it with empty crafts (craft tiers find nothing → INDOOR→STRENGTH→REALLOCATE, the craftless-at-home degradation). **(Render/cache)** away-aware `_event_window_label` + block intro (Trigger-#1 draft in §7) + `away_locale` folded into `compute_event_windows_hash`. **5 substantive files** (repo, `session_feasibility`, `orchestrator`, `locations`, `per_phase`) + minimal *pick-existing-locale* capture; **inline-create-new-destination flagged to defer to the UX slice** to hold the ceiling. **Decisions locked (Andy 2026-06-14):** (1) **counts-follow-away** — a fully-away week is counted against the destination; the grounding win is that the grid is *already* per-week and *already* consumes a per-week feasibility dict (WS-E2 reallocation), so this is "pick the right dict per week," **no new count machinery, no `session_grid.py` change** (spec §4.1); *whole-week threshold* (partial weeks stay home + per-day composition; Andy confirmed). (2) away wording **approved** (§7). (3) **inline-create a new destination** → re-sliced to **2a (resolution + counts + pick-existing capture) + 2b (inline-create UX, separate PR)**.

**Slice 2a (2026-06-14, PR [#600](https://github.com/ahorn885/exercise/pull/600) merged; DDL applied → LIVE).** `override_type='away'` + `away_locale` (the cluster anchor): plan-gen resolves the window's days against the **destination's own radius cluster** (`cluster_locale_ids` generalized to take `anchor_locale=None` → preferred home byte-identical; away passes the destination) with `owned_crafts=[]` (F4) fed to the **existing** unified cascade — `away` replaces the home env + **wins over** a co-active subtractive override. `EventWindowSegment` now carries the **full** away feasibility; `_format_session_grid` selects it per-week for any **fully-away** week → WS-E2 reallocates the counts (counts-follow-away). Away-aware `_event_window_label` + overlay intro (Trigger-#1, approved); `away_locale` folded into `compute_event_windows_hash`; away capture (pick-existing destination dropdown) on `/profile/event-windows`. **5 substantive core** (`session_feasibility`, `orchestrator`, `locations`, `per_phase`, `athlete_event_windows_repo`) + thin mechanical (`hashing` one-field, `routes/profile` 1-line, template, `init_db` DDL). **+9 tests; full suite green 2432 passed / 30 skipped.** **DDL APPLIED (Andy 2026-06-14):** `ALTER TABLE athlete_event_windows ADD COLUMN IF NOT EXISTS away_locale TEXT` — Slice 2a fully live. **NEXT: Slice 2b** — inline-create-a-new-destination UX (reuse `routes/locales.py new_locale` + `mapbox_id` dedup). **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify. **NEXT (post-sign-off):** BUILD Slice 2 per spec §5; DDL owed-Andy's-hands. **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-H Event Windows refresh-overlay render

**WS-H — EVENT WINDOWS refresh-overlay render (create-first follow-up; 2026-06-14, PR [#599](https://github.com/ahorn885/exercise/pull/599) squash-merged to `main`; branch `claude/eventwindows-slice-2-hn8yvq`; handoff `handoffs/V5_Implementation_WSH_EventWindows_RefreshOverlay_2026_06_14_Closing_Handoff_v1.md`; arc `designs/Event_Windows_Design_v1.md`; **north-star** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §2 WS-H).** Finished the tier-3 "partially-live" item flagged in the Slice-1 handoff §6.2: the event-window overlay now renders on **plan-REFRESH**, not just create. Slice 1 left `event_windows_hash` on `plan_refresh_key` (default None → byte-identical) but the refresh caller never fed it, so a refreshed plan correctly *evicted* caches on a window edit yet never *rendered* the overlay. **Wiring (mirrors #540→#557 terrain-feasibility create→refresh):** `orchestrate_plan_refresh` builds the overlay over `[refresh_scope_start, refresh_scope_end]` via the **same** `_build_event_window_overlay`, computes `compute_event_windows_hash`, and threads `event_window_segments`+`event_windows_hash` through `llm_layer4_plan_refresh_cached` (folds the hash into `plan_refresh_key`) → `llm_layer4_plan_refresh` → the T1/T2/T3-intra `render_user_prompt` **and** the T3 cross-phase Pattern A route (`_route_t3_cross_phase_to_pattern_a` → `synthesize_pattern_a_for_refresh` → `_run_pattern_a_engine`, which renders via `per_phase`). The three refresh tier prompts render the existing `per_phase._format_event_window_overlay` block (Slice-1 wording, already signed off) scoped to the refresh window, after the `=== Session feasibility ===` block. **Rule #15 logging inherited** (shared builder emits `event_window_overlay: …`). **NO DDL, no prompt-wording change, no vocab adds.** **4 substantive files** (`orchestrator.py` + `plan_refresh_t1/t2/t3.py`) + 3 mechanical passthroughs (`cached_wrappers.py`, `plan_refresh.py`, `plan_create.py`) — additive-only (172 insertions, 0 deletions). **Tests:** refresh-tier overlay render (overlap / out-of-scope) + `plan_refresh_key` changes-with-windows. **Full suite green: 2423 passed / 30 skipped.** **NEXT:** **Slice 2 — away windows (design-first)** — away `locale_profiles` destination + inline create + `mapbox_id` dedup, `away` override_type + `away_locale` column + away discriminator on `locale_profiles`, per-environment cascade; **trips Trigger #1 (new away wording) + Trigger #3 (schema), no build-ready spec yet → write the spec first**, DDL owed-Andy's-hands. **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (diag token + Andy pasting logs, Rule #14) — now also a chance to eyeball the refresh-overlay render on a window-overlapping scope.

### Predecessor — WS-H Event Windows Slice 1 BUILT (subtractive home windows)

**WS-H — EVENT WINDOWS Slice 1 BUILT (subtractive home windows; 2026-06-14, PR [#596](https://github.com/ahorn885/exercise/pull/596) squash-merged to `main`; branch `claude/compassionate-keller-ej5tcx`; handoff `handoffs/V5_Implementation_WSH_EventWindows_Slice1_Build_2026_06_14_Closing_Handoff_v1.md`; spec `designs/Event_Windows_Slice1_HomeWindows_Spec_v1.md`; arc `designs/Event_Windows_Design_v1.md`; **north-star** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §2 WS-H).** Built the first end-to-end vertical of the Event-Windows arc: the athlete declares date-bounded **subtractive home windows** — `indoor_only` (home cluster minus outdoor terrain) + `locale_unavailable(L)` (home cluster minus one locale) — and plan-gen resolves the affected days against the reduced environment. **Resolution model = the EXISTING cascade per date-segment** (Andy's settled §4): refactored `orchestrator._build_terrain_feasibility` into `_gather_feasibility_inputs` + `_resolve_included_feasibility` (the cascade against an injected `(locale_order, terrain, equipment)`), then `_build_event_window_overlay` date-segments the plan span (`session_feasibility.segment_window_boundaries`, atomic ranges + union-of-subtractions per spec §8), runs the same cascade per reduced env, and emits only the disciplines whose routing CHANGES. **Shipped (substantive):** new `athlete_event_windows_repo.py` (load/add/delete + app-layer validation + plan-cache eviction scoped to `plan_create`+`plan_refresh`); `session_feasibility.py` (`EventWindowOverride`/`EventWindowSegment` + pure `segment_window_boundaries`); `orchestrator.py` (the refactor + overlay builder + `orchestrate_plan_create` wiring, plan span via the same deterministic `phase_structure_from_3b`); `hashing.compute_event_windows_hash` folded into **both** `plan_create_key` + `plan_refresh_key` (appended only when windows overlap → **no-windows key byte-identical**, regression (a)); `per_phase._format_event_window_overlay` — the **date-scoped overlay block + soft placement directive (Trigger-#1 wording SIGNED OFF by Andy 2026-06-14)**, rendered only for synthesis units whose date window overlaps a segment, dates clipped to the unit; mechanical passthrough threading through `cached_wrappers` + `plan_create` (mirrors the `terrain_feasibility` param); minimal capture UI (`routes/profile` `/profile/event-windows` GET/add/delete + `templates/profile/event_windows.html`); DDL `athlete_event_windows` in `init_db._PG_MIGRATIONS`. **Rule #15:** `event_window_overlay: dates=… override=… tiers={…}` per segment. **Tests:** `tests/test_layer4_event_windows.py` (29 — segmentation, reduced-env routing for both override types + cascade-fallback "not always a downgrade", overlay glue changed-only/no-op-dropped, the render incl. full-prompt integration + date-gating, hash + byte-identical key regression, repo validation). **Full suite green: 2418 passed / 30 skipped.** **~6 substantive files (UI counted; cached_wrappers/plan_create are mechanical mirrors of the existing terrain_feasibility passthrough) — one over the soft ceiling, flagged, consistent with the WS-B/WS-C precedent + the spec's own template-split allowance.** **DDL APPLIED on Neon (Andy, 2026-06-14, verified — 8 columns match `init_db`): Slice 1 is FULLY LIVE in prod** (`/profile/event-windows` capture + the plan-gen overlay are active). **DEFERRED (flagged, create-first like #540→#557):** the overlay renders on **plan-CREATE only** — `event_windows_hash` exists on `plan_refresh_key` but the refresh caller doesn't feed it yet, so **refresh-overlay render is the follow-up** (window edits still evict both caches). Counts stay on the home env (spec §4 limitation: E2 won't pre-cap a window-induced substitution). **NEXT:** Slice 2 (away windows + destination locale + `away` override_type + per-environment cascade), or wire refresh-overlay consumption. **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-H Event-Window arc DESIGN

**WS-H — EVENT-WINDOW arc DESIGN (design-first; 2026-06-14, PRs [#591](https://github.com/ahorn885/exercise/pull/591)+[#594](https://github.com/ahorn885/exercise/pull/594) squash-merged to `main`; branch `claude/v5-strength-saturation-cap-pjscu2` [harness-pinned name; scope is WS-H]; design `designs/Event_Windows_Design_v1.md` + build-ready Slice-1 spec `designs/Event_Windows_Slice1_HomeWindows_Spec_v1.md`; handoff `handoffs/V5_Implementation_WSH_EventWindows_Design_2026_06_14_Closing_Handoff_v1.md`; **north-star plan** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §2 WS-H + §6a).** WS-E2 closed the last *code* workstream; Andy picked WS-H (away-craft) + chose **design-first**. The design surfaced that away-craft can't exist without a missing date-windowed environment surface — v2 resolves feasibility only over the **home cluster** for the whole plan (`orchestrator._build_terrain_feasibility`), so a craft↔away-location store alone would be **unconsumed** (the WS-F #578 shape). **Andy then reframed "away/travel" → "EVENT windows"**: date-bounded periods where the environment differs, covering BOTH away locations AND **home-constraint** windows (home but can't train normally — weather / kids needing supervision → indoor-only). v1 had exactly this (`plan_travel.indoor_only` via `/coaching/review`, on the now-retired `home/hotel/partner/airport` enum) and it was **left behind**. **Ratified forks:** F1 athlete-level + **plan-gen review/edit/append panel w/ inline new-location create**; F2 destination = a **reused `locale_profiles` row** (*"travel is how we build our crowd-sourced locations database"*, differentiator #8); F4 away craft **none unless declared**. **F3 REVISED → per-DAY/sub-week** (not per-week — Andy may hit 3-4 locations in one week; the grid-weekly-counts↔synthesis-per-day-placement seam is the core new machinery/main risk). **F8 NEW — category equipment baselines (Trigger #2):** a not-yet-seen hotel gym gets an **assumed** baseline, athlete logs actuals on arrival → **T1/T2 regen**; scoped to **commercial/hotel/climbing gyms** (the current `MANUAL_CATEGORIES`, NOT the retired enum — Andy corrected). **F9 NEW — subtractive home windows = Slice 1:** `indoor_only` (weather/childcare) **+ `locale_unavailable`** (cluster minus a closed gym/park; Andy); the `indoor_only`/`no_outdoor_cardio` split collapsed to one flag, `reduced_volume` deferred to #593. **F-race → SEPARATE ISSUE:** race locations are real but treated differently (LLM-infer area terrain/weather → "train for <terrain>" suggestion + plan-gen feed); filed separately. **Re-sliced into 5** (home-constraint → away locale → baselines → away craft/the literal WS-H → UX), each ≤5 files + independently shippable. **Slice-1 build spec written** (`designs/Event_Windows_Slice1_HomeWindows_Spec_v1.md`): `athlete_event_windows` (indoor_only + locale_unavailable) + per-(week,env) feasibility overlay + the grid↔synthesis seam. **Seam SETTLED (Andy 2026-06-14):** it's the existing cascade resolved **per date-segment** (a window is just different terrain/equipment inputs — "no different from normal proxies/substitutions"); the earlier good-days/overflow arithmetic was over-engineered and removed. Grid+E2 stay on the home env in Slice 1; synthesis composes date-scoped tiers. **No code/DDL/tests this slice** (design-only; spec build-ready). Filed #592 (race terrain/weather inference) + #593 (reduced-volume travel days); #581 commented. **NEXT: BUILD Slice 1** — the 5 files in spec §5 (`athlete_event_windows_repo.py`, orchestrator date-segment loop, per_phase date-scoped block, `compute_event_windows_hash`, minimal capture UI) + the `athlete_event_windows` DDL (Andy applies on Neon) + Trigger-#1 synthesis wording sign-off. **DDL owed Andy's hands** (Neon egress blocked). **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-E2: deterministic strength-saturation cap

**WS-E2 — deterministic strength-saturation cap (dose+2 + reallocate-with-variety) (2026-06-14, PR [#590](https://github.com/ahorn885/exercise/pull/590), squash-merged to `main`, CI-green; closes WS-E2 [#584](https://github.com/ahorn885/exercise/issues/584); branch `claude/next-slice-implementation-37iur2`; handoff `handoffs/V5_Implementation_WSE2_StrengthSaturationCap_2026_06_14_Closing_Handoff_v1.md`; **north-star plan** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §7).** The §7 WS-E2 saturation backstop — the last code workstream on the locale-retirement/feasibility-saturation plan. Andy picked it via `AskUserQuestion` and ratified the three design forks (Trigger #1/#5 stop-and-ask): **pre-synthesis grid pass**, **proportional-to-load_weight** reallocation, **build-now**. **Shipped:** new pure `session_grid.apply_strength_saturation_cap` runs **after** the §5.1.1 ceiling inside `build_session_grid` (gated on a new `strength_feasibility_tiers` kwarg — bare callers omit it → no cap, mirrors the `available_days`-gated ceiling). Weekly **failover** strength (terrain/craft-infeasible cardio composed as a strength substitution) is capped at `dose + 2` total — a flat **+2 failover headroom every phase** (the prompt-added dose cancels out of the arithmetic). Over-cap excess is **trimmed lowest-priority-first** and **reallocated to feasible (exact/proxy/indoor) disciplines proportional to load_weight** (d'Hondt highest-averages), with a per-discipline absorption ceiling (a discipline can at most **double** in one pass) so volume spreads instead of dumping (Andy's "3 running + 3 cycling → 6 running" failure mode). **Volume-conserving** — sessions move strength→feasible 1:1, so the upstream session ceiling holds; excess no absorber can take **stays as strength** (training time preserved, never dropped — E1/#579 still backstops the same-day collision crash). New `SessionGrid.saturation_note` carries the **Rule #15** decision line, printed by `per_phase` as `strength_saturation_cap:`. **Trigger-#1 prompt edit:** `strength_guidance.py` failover paragraph now states the cap (was "may push total higher… acceptable"). **Scope note (documented limitation):** caps terrain/craft failover only; skill-gated strength (#336, a deliberate safety substitution) is excluded from both trim and absorb (passed in via `skill_gated_ids`). **No new cache surface** (pure function of already-keyed inputs: feasibility via #556, skill-gated via #336). **NO DDL.** Tests: new `tests/test_layer4_session_grid_saturation.py` (11 cases). **Full suite green (2391 passed, 30 skipped).** **3 substantive files** (`session_grid.py`, `per_phase.py`, `strength_guidance.py`) + 1 new test — within the 5-file ceiling. **NEXT:** plan §2 remaining — WS-V vocabulary arc (durable), WS-H away-craft (needs DDL/design-first); off-plan #542 nutrition macros / #543 health-condition dropdown (Trigger #2) + compliance epics. **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (paired: diag token + Andy pasting logs, Rule #14). **Minor watch:** a capped-saturated week (dose+2 strength) trips the advisory `_rule_strength_frequency_band` warning (±1 band) by design — non-blocking; bump to ±2 if the noise bothers.

### Predecessor — WS-B/WS-C: retire legacy `LOCALES` enum + onboarding forced-home gate

**WS-B/WS-C — retire legacy `LOCALES` enum + onboarding forced-home gate (2026-06-14, PR [#589](https://github.com/ahorn885/exercise/pull/589), squash-merged to `main`, CI-green; closes WS-B [#582](https://github.com/ahorn885/exercise/issues/582) + WS-C [#583](https://github.com/ahorn885/exercise/issues/583); branch `claude/ws-bc-locale-retirement-onboarding-home`; handoff `handoffs/V5_Implementation_WSBC_LocaleRetirement_OnboardingHome_2026_06_14_Closing_Handoff_v1.md`; **north-star plan** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md` §4/§5).** Next slice off the locale-retirement north star (WS-I complete). **WS-B:** fully retired the legacy `home/hotel/partner/airport` enum — removed the `LOCALES` constant + every special case keyed off it in `routes/locales.py` (where-available choices prepend, `list_profiles` display + `legacy_locales` kwarg, the `edit_profile` legacy-auto-create bypass → all locales must now exist, `is_deletable` → always True, the `delete_locale` legacy short-circuit) and the `LEGACY_LOCALES` import in `routes/onboarding.py`. Locales are now purely athlete-created (`locale_profiles` + `gym_profiles` + overrides); home = the `preferred` flag (set by `_ensure_home` on first build). `templates/locales/list.html` drops the legacy-slot rendering; the empty hero leads with search/add to `new_locale`. **WS-C:** `onboarding.locales_continue` is now a GATE — requires ≥1 built locale AND one tagged home (`preferred`) before advancing to skills, else flashes + bounces back; without a home the plan-gen cone resolves an empty cluster → the strength-saturation failure the arc closes (plan §1/§5). `_athlete_locales_for_review` reshaped to `{slug,label,category,is_home}`; `templates/onboarding/locales.html` reworked to the athlete-created list + ★ Home flag + empty state + requirement copy. **NO DDL** (schema already supports athlete-created locales + the `preferred` flag). Tests: the three WS-C gate paths + new-shape review tests + a now-deletable former-legacy-slug delete test + the empty-hero render repointed off the retired edit shortcuts. **Full suite green (2380 passed, 30 skipped).** **7 substantive files (2 over the 5-file ceiling — flagged; Andy chose B+C as one cohesive change; the 3 test edits are mechanical).** **NEXT:** plan §2 remaining — WS-V vocabulary arc, WS-E2 saturation policy (queued lower), WS-H away-craft (needs DDL/design-first); off-plan #541/#542/#543 + compliance epics. **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (paired: diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-I Slice B: unified craft/terrain feasibility cascade (the bug fix)

**WS-I Slice B — unified craft/terrain feasibility cascade (the bug fix) (2026-06-14, PR [#588](https://github.com/ahorn885/exercise/pull/588), squash-merged to `main`, CI-green; closes WS-I [#586](https://github.com/ahorn885/exercise/issues/586); branch `claude/zealous-gauss-bl22ek`; handoff `handoffs/V5_Implementation_WSI_SliceB_UnifiedCraftTerrainCascade_2026_06_14_Closing_Handoff_v1.md`; **north-star plan** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md`).** Second + final slice of WS-I ([#586](https://github.com/ahorn885/exercise/issues/586), design `designs/CraftEquipment_Taxonomy_And_FeasibilityCascade_Design_v1.md`) — the cascade rewrite that fixes the live **craft-STRENGTH-preempts-INDOOR** bug (design §1b: a craftless athlete with a trainer used to be sent to strength because the old craft axis short-circuited ahead of INDOOR). **Shipped:** (1) **removed the two non-composing axes** (`resolve_craft_feasibility`/`CraftResolution` deleted; the `orchestrator.py:437` craft-STRENGTH `continue` gone) and replaced them with one **unified nested cascade** `resolve_craft_terrain_feasibility` (`session_feasibility.py`, design §3): `1 own-craft/required-terrain → 2 own-craft/alt-terrain → 3 proxy-craft/desired-terrain → 4 proxy-craft/own-terrain (swap) → 5 indoor → 6 strength → 7 reallocate` (**tier 3 > tier 4**, Andy-ratified). Craftless is no longer a special branch — tiers 1–4 miss and the walk lands on INDOOR (the bug fix falls out for free). Non-craft (foot/swim/climb) disciplines keep the terrain-only `resolve_terrain_feasibility` cascade untouched. (2) **new reader `_q_craft_terrain_compatibility`** + tiers 2–4 read `layer0.craft_terrain_compatibility` (explicit craft↔terrain, so a road bike ≠ a gravel bike on singletrack). (3) **registered `craft_terrain_compatibility`→0A in `_LAYER0_TABLE_FAMILY`** so grid edits invalidate plan-gen caches (the digest entry deferred from Slice A, now landing with the code that reads it — deploy-safe: `0004` is already on Neon). (4) **Rule #15 logging** rewritten per-discipline (owned/proxy crafts + which compatible terrains are in-cluster, chosen tier, machines, strength pool). (5) Test matrix: the craftless-with-trainer → INDOOR case (the bug), proxy-craft-on-desired-terrain (tier 3), all 7 tiers; the stale `cycling_trainer` synthetic fixtures (§4) cleaned. **Full suite green (2379 passed, 30 skipped).** Profile UI needed no change (the craft picker derives from the post-Slice-A enum; "Cycling trainer" is already an equipment item). **NO new app DDL** (reads the already-applied `0004`). **NEXT:** WS-I complete + merged (#586 closed); then lower-priority arc ([#582] retire `LOCALES`/WS-B, [#583] onboarding forced-home + craft capture/WS-C, [#584] saturation policy/WS-E2, WS-H away-craft needs DDL/design). **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (paired: diag token + Andy pasting logs, Rule #14).

### Predecessor — WS-I Slice A: craft/equipment taxonomy + seed

**WS-I Slice A — craft/equipment taxonomy + `craft_terrain_compatibility` seed (2026-06-14, PR [#587](https://github.com/ahorn885/exercise/pull/587), branch `claude/dreamy-wozniak-t3tifr`, open + CI-green; handoff `handoffs/V5_Implementation_WSI_SliceA_CraftEquipmentTaxonomy_2026_06_14_Closing_Handoff_v1.md`; **north-star plan** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md`).** First of a 2-slice split (Andy-ratified "Slice A first") of the WS-I unified craft/equipment feasibility cascade ([#586](https://github.com/ahorn885/exercise/issues/586), design `designs/CraftEquipment_Taxonomy_And_FeasibilityCascade_Design_v1.md`). **This slice = data + taxonomy only:** (1) **dropped `cycling_trainer` from the craft enum** (`athlete.py` `BIKE_TYPES`/`CRAFT_LABELS`) — a trainer/erg is fixed gear → equipment, not a mobile craft; the picker + closed-enum validation derive from these constants so it leaves the UI + fails validation in lockstep; (2) **new `etl/migrations/layer0/0004_*`** — creates + seeds `layer0.craft_terrain_compatibility` (the explicit craft↔terrain map, **21-row Andy-ratified grid**, design §4; spec-sourced + self-contained like `0003`) and **retires the `cycling_trainer` craft_discipline_aliases rows** (cache-neutral soft-supersede, `0001` shape); (3) updated 2 enum-pinned tests. **Design-doc correction (Andy-flagged):** §4's "seed via the `Sports_Framework` xlsx + extractor/runner" is **stale** — that path is retired (epic #488; DB is source of truth, reference data authored as SQL migrations under `etl/migrations/layer0/`). Built on the migration model. **Validated against a throwaway Postgres** (exact CI gate: schema + genesis + all 4 migrations + `validate_layer0` PASS; 21 active rows, 0 dangling terrain FKs, aliases all superseded, idempotent). **`0004` APPLIED on Neon** this session (Andy; briefly blocked by the SQL editor being in the wrong Neon *project* → `3F000`, resolved). **Cascade rewrite deferred to Slice B** (kept the `_LAYER0_TABLE_FAMILY` digest entry with the code that reads it, to avoid prod querying a not-yet-migrated table). **NEXT:** **WS-I Slice B** (the cascade — where the live craft-STRENGTH-preempts-INDOOR bug at `orchestrator.py:437` gets fixed): add `craft_terrain_compatibility`→0A to `_LAYER0_TABLE_FAMILY`; rewrite `session_feasibility.py`+`orchestrator.py` to the unified nested cascade (design §3, tier 3 > tier 4) reading the new table, with Rule #15 logging + the craftless-with-trainer + proxy-craft-terrain test matrix. On a fresh branch after #587 merges. **Owed-hands all cleared this session** (Andy 2026-06-14): `0004` applied on Neon + the per-athlete fix done (`cycling_trainer` stripped from set B + Cycling trainer ticked into home equipment) — only **merging #587** remains. **STILL OWED (carried, unrelated):** the post-#572 live **T3 *refresh*** re-verify.

### Predecessor — pv=71 gear-toggle validation + abseiling gate + WS-I design

**pv=71 gear-toggle live validation + abseiling skill-gate + WS-I craft/equipment cascade design (2026-06-13, PR [#585](https://github.com/ahorn885/exercise/pull/585), squash-merged to `main`; handoff `handoffs/V5_Implementation_pv71_GearToggleValidation_AbseilGate_WSI_CraftCascadeDesign_2026_06_13_Closing_Handoff_v1.md`; **north-star plan** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md`).** **Watched live plan-create pv=71 (Andy's gear-toggle profile test) via the diag token — it reached `ready` (41 sessions, all 5 phases; Peak:w1 = 9 sessions, the exact week that crash-looped on pv=69/70).** Confirms WS-G end-to-end: with set B populated (Andy ticked + saved his Gear after pv=70), `owned_crafts=['cycling_trainer','kayak','mountain_bike','packraft','road_bike']` and **D-008 Mountain Biking + D-009 Packrafting resolve `tier=exact`** (real rides/paddles) instead of craft-failing to STRENGTH — so the empty-set-B → 7-strength/week → `no strength+strength` collision → stall-fail mode is gone at the root (proven infeasibility-input → `ready`). **Two findings surfaced + actioned:** **(1) Abseiling (D-013) skill-gate gap** — it resolved `tier=reallocate` instead of being skill-gated because the `climbing_roped` toggle row gated only `D-012`, though the validator's own `_rule_skill_capability_gate` docstring names abseiling (pv=46). Fixed: `climbing_roped.gated_discipline_ids → ['D-012','D-013']` in `etl/sources/populate_skill_capability_toggles.sql` (Andy shares the toggle, 2026-06-13); **Andy APPLIED the UPDATE on Neon** → live on next cold cone build (row-count verify=5 unchanged; no Python test pins the seed). **(2) Craft-STRENGTH terminal preempts INDOOR** (`orchestrator.py:437-449` `continue`s to strength without calling the terrain axis, so a craftless athlete with a Cycling trainer/Paddle ergometer gets strength, not trainer/erg) **+ taxonomy split** (`cycling_trainer` is a craft in `BIKE_TYPES` *and* the `'Cycling trainer'` indoor machine; paddle erg is equipment-only). Both folded into a build-ready design: **`designs/CraftEquipment_Taxonomy_And_FeasibilityCascade_Design_v1.md`** (WS-I, issue [#586](https://github.com/ahorn885/exercise/issues/586)) — drop `cycling_trainer` from the craft enum; one **unified nested cascade** `exact → owned-craft/other-terrain → proxy-craft/desired-terrain → proxy-craft/own-terrain → indoor → strength → reallocate` (tier 3 > tier 4, Andy-ratified); explicit Layer-0 `craft_terrain_compatibility` map with the **Andy-ratified seed grid** (§4). Also added `.claude/settings.json` (`allow: mcp__Vercel`) so the diag/log MCP stops re-prompting each ephemeral web container. **Code/data + design only; NO app DDL this session** (the skill-gate UPDATE is the only Neon write, applied; WS-I migration bundles with its build). **NEXT:** **WS-I build ([#586](https://github.com/ahorn885/exercise/issues/586))** — recommend its own branch/PR (multi-file: enum + new Layer-0 table/sheet/extractor + cascade rewrite + UI + tests), build-ready now the grid's locked. **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify (this session watched a *create*, pv=71); the WS-I Neon migration (when WS-I builds).

### Predecessor — pv=69/70 feasibility-saturation arc

**pv=69/70 feasibility-saturation arc — observability, strength+strength crash-guard, craft-model correction (2026-06-13, PRs [#576](https://github.com/ahorn885/exercise/pull/576)/[#577](https://github.com/ahorn885/exercise/pull/577) observability, [#579](https://github.com/ahorn885/exercise/pull/579) crash-guard, [#578](https://github.com/ahorn885/exercise/pull/578)→[#580](https://github.com/ahorn885/exercise/pull/580) craft-from-equipment + revert)** (all squash-merged to `main`; handoff `handoffs/V5_Implementation_pv69_FeasibilitySaturation_CraftModel_2026_06_13_Closing_Handoff_v1.md`; **north-star plan** `plans/Feasibility_Saturation_And_Locale_Retirement_Plan_v1.md`). **Watched live plan-creates pv=69/70 and caught a deterministic stall-fail, then diagnosed it from a full new instrumentation pass.** Root cause (proven on pv=70): bike/paddle disciplines (MTB grid-allocated **5×**, packraft) **craft-fail to STRENGTH** because the athlete-level craft store (`bike_types_available`/`paddle_craft_types`, "set B") is **empty** while the bikes/boats are entered as **location equipment** ("set C") → ~7 strength/week → `no strength+strength on same day` hard reject → per-block budget exhausted → cron-loop → D-77 stall-fail. **Shipped:** (1) full plan-gen decision-surface logging (3B/2A/grid/cluster/terrain/equipment/skill/cache-key/API-error + per-day collision detail), readable verbatim via `/admin/logs?token=` + **CLAUDE.md Rule #15 (Instrument as you build)**; (2) deterministic `per_phase._repair_strength_collisions` crash-guard (relocate-else-drop a colliding 2nd strength pre-validation, so a collision self-heals instead of looping). **Craft model corrected (Andy's call):** craft is **athlete-owned portable gear** — canonical store = the athlete-level capture (set B), available **home-cluster-wide**, with away availability explicit via **(b)** craft↔location + **(c)** craft-on-travel-event — so the location-equipment-derived fix #578 was **reverted (#580)**. **Code-only, NO DDL** (WS-H away-availability will need DDL). **NEXT:** **[#581](https://github.com/ahorn885/exercise/issues/581) athlete-owned craft store (WS-G — the immediate fix; Andy must also tick his craft in profile → "Gear" since set B is empty)**, then [#583](https://github.com/ahorn885/exercise/issues/583)/[#582](https://github.com/ahorn885/exercise/issues/582) onboarding-forced-home + legacy-`LOCALES` retirement, [#584](https://github.com/ahorn885/exercise/issues/584) saturation policy (dose+2 cap + reallocate-with-variety). **STILL OWED (carried):** the post-#572 live **T3 *refresh*** re-verify — this session watched *create* plans (pv=69/70), not a refresh.

### Predecessor — T3 plan-refresh truncation fix (#572)

**T3 plan-refresh truncation fix — size synthesis output budget to the session ceiling (2026-06-13, PR [#572](https://github.com/ahorn885/exercise/pull/572))** (branch `claude/adoring-mccarthy-su0igw`, squash-merged to `main`; handoff `handoffs/V5_Implementation_T3RefreshOutputBudgetTruncationFix_2026_06_13_Closing_Handoff_v1.md`). **Ran the live T3 re-verify owed by #569 — #569's `plan_start_date` fix is confirmed live (pv=67 cleared `_validate_inputs`), and the watch caught a *second* shipping bug.** pv=67 (`created_via=plan_refresh_t3`) reached real synthesis but failed `schema_violation … stop_reason=max_tokens, last_attempt_output_tokens=10000/ceiling=10000`: the refresh tiers carried flat output budgets that never absorbed the create path's per-session sizing, so a T3 cross-phase block (up to 56 sessions in one `record_refresh_sessions` call) truncated on a flat 10000 ceiling (~178 tok/session vs the create path's 1400). Because the truncation is a *retryable* `schema_violation`, pv=67 stayed `generating` and the every-minute cron deterministically re-drove it (advance lock 04:12→04:24) — never `ready`. **Fix (Andy's call — reuse the create-path balancing, not decomposition):** new `per_phase.block_output_budget(max_sessions, floor)` = `sessions × 1400 + 2000`, clamped to `_MODEL_MAX_OUTPUT_TOKENS=64000` (the `claude-sonnet-4-6` per-request output ceiling); the refresh driver `llm_layer4_plan_refresh` sizes `effective_max_tokens` to each tier's session ceiling via `_TIER_MAX_SESSIONS` (T1 7,600 / T2 21,600 / **T3 80,400→64,000 clamped**); T3 `DEFAULT_EXTENDED_THINKING_BUDGET` 6500→0 (under thinking the request is `max_tokens + thinking`, so a 64K output budget needs thinking off to stay under the model ceiling — mirrors `per_phase`). The create per-phase block-mode now calls the same helper — **behavior-preserving** (its per-week blocks are ≤21,600, clamp is a no-op). +4 tests; full suite **2366 passed / 30 skipped**; CI green. **Code-only, NO DDL.** **OWED — live T3 re-verify (Andy's hands, post-#572):** re-run a T3 refresh on prod → confirm the new pv reaches `ready` with a phase-correct `total_sessions` (diag token); #572 covers realistic ~20–35-session 4-week windows (≈28–49k, under 64K). **Two optional/contingent follow-ups filed** ([#574](https://github.com/ahorn885/exercise/issues/574) decompose T3 per-week — the create machinery NOT ported, refresh is single-call; [#575](https://github.com/ahorn885/exercise/issues/575) per-pass give-up backstop so a retryable failure stops cron re-driving — adjacent to #539), both `status:deferred`/`priority:low`, build only if their trigger fires. **NEXT:** unchanged — the #541/#542/#543 plan-quality batch, then the compliance build-out (epics #353/#355/#356/#359).

### Predecessor — Layer 4 strength two-template restructure (#571)

**Layer 4 strength two-template restructure — deeper, durability-driven strength shared across plan-gen + refresh (2026-06-13, PR [#571](https://github.com/ahorn885/exercise/pull/571))** (branch `claude/vibrant-wright-4kcjku`, squash-merged to `main`; handoff `handoffs/V5_Implementation_Layer4_StrengthTwoTemplate_2026_06_13_Closing_Handoff_v1.md`). **Addresses the shallow-strength plan-quality item ([#541](https://github.com/ahorn885/exercise/issues/541)).** Deepens Layer 4 strength from the single economy-derived template (3–5 exercises, 2–3 sets) to a durability-weighted **two-template** model, extracted to a shared source of truth so the refresh prompts can't drift back to the shallow version. Driven by a second cited deep-research pass (design `Layer4_StrengthProgramming_Phase2_Design_v2` §A; 5 angles — session depth, durability/load-carriage, interference, core, periodization). **Programmed template:** layered session — heavy core (2–3 compounds, 4–6 reps) + durability layer (eccentric/unilateral knee, Nordic, calf/tib-ant) + 1 plyo + 1 trunk/carry; dose 2/wk Base-Build, 1/wk Peak-Taper, **never >3**; maintain by cutting volume, holding load. **Failover template:** a cardio slot the feasibility resolver flags `[TERRAIN-INFEASIBLE]`/`[NO CRAFT]` → composed as **muscular endurance** to *replace* the infeasible aerobic session (keep target hours), **exempt from the dose cap** so the terminal fallback is never starved (`_rule_strength_frequency_band` is advisory `warning`-only by design — a hard cap there would block the failover). **Shipped:** new `layer4/strength_guidance.py` (`STRENGTH_PROGRAMMING_GUIDANCE`) spliced into `per_phase` + `plan_refresh_t1` + `plan_refresh_t2`; `hashing.LAYER4_PROMPT_REVISION="2"` added to `plan_create_key` + `plan_refresh_key` (prompt text isn't otherwise in the key → forces one-time cold re-synth); design v2 (v1 archived). **Merge interaction:** [#557](https://github.com/ahorn885/exercise/issues/557)+[#208](https://github.com/ahorn885/exercise/issues/208) (refresh terrain-feasibility + async refresh) landed on `main` mid-session and wire the failover trigger end-to-end into refresh — so the **failover template is live on refresh too** (not the originally-planned dormant-until-PR2), and the latent "refresh prescribes/clobbers infeasible cardio" bug is closed by #557. **Tail (filed [#573](https://github.com/ahorn885/exercise/issues/573)):** `strength_substitution` marker on `PlanSession` + validator source-scoping so the advisory counts only programmed sessions — advisory-quality only, no longer blocked on refresh wiring. +`tests/test_layer4_strength_templates.py`; full suite **2370 passed / 30 skipped**; CI green. **One-time cache note:** the `LAYER4_PROMPT_REVISION` bump re-synthesizes the first post-deploy plan/refresh cold (expected). Respects Andy's wrist constraint (neutral-grip carries). **NEXT:** the rest of the plan-quality batch ([#542](https://github.com/ahorn885/exercise/issues/542) low-protein macros / [#543](https://github.com/ahorn885/exercise/issues/543) structured health conditions), the #573 marker tail, then the compliance build-out (epics #353/#355/#356/#359).

### Predecessor — T3 plan-refresh `plan_start_date` fix (#569)

**T3 plan-refresh fix — wire `plan_start_date` through the async path (2026-06-13, PR [#569](https://github.com/ahorn885/exercise/pull/569), `Closes #570`)** (branch `claude/plan-65-refresh-r8tclv`, squash-merged to `main`; handoff `handoffs/V5_Implementation_T3RefreshPlanStartDateFix_2026_06_13_Closing_Handoff_v1.md`). **Ran the live T3-refresh verify owed by #566/#208 — and it caught a real bug: T3 refresh had never actually worked.** Andy refreshed pv=65 (T3); the new row pv=66 failed with `Layer4InputError: plan_start_date_missing` — `routes/plan_refresh.py` never supplied `plan_start_date` to `orchestrate_plan_refresh` (neither `run_refresh_orchestration` nor the frozen-input `build_refresh_advance_ctx` carried it). T1/T2 ignore it; only T3 requires it (Step 4d §3.2 — `phase_structure_from_3b()` anchors phase boundaries on the plan's real start), so the gap shipped silently — `git log -S` confirms `plan_start_date` was never in that file, i.e. **not a #208 regression**. **Fix:** new `_resolve_plan_start_date` walks `refresh_parent_version_id` back to the root `plan_create` row and returns its `scope_start_date` (the plan's TRUE start, NOT the refresh row's own scope-start = the refresh *window*, which would silently mis-place phase boundaries); threaded through `build_refresh_advance_ctx` → `run_refresh_orchestration`. A `seen`-set guards cyclic pointers; unresolvable chain → `None` so the T3 validator still raises the explicit error. **`plan_start_date` (past) and `refresh_scope_start/end` (future window) are orthogonal — only future phases are re-synthesized; the past start only anchors the periodization calendar.** **The #208 async machinery itself is now confirmed sound on real traffic:** POST→302→progress→background cron/poller passes, cron solo-drove after tab-close, advance-lock guard held, and it failed *gracefully* via `_mark_plan_failed` (no 504). The failure was invisible to the runtime-log MCP (`print()` → `info` level) and only readable via the token-gated `/admin/plan/<id>/diag` (Rule #14). +7 tests; full suite **2362 passed / 30 skipped**; CI green; **#570 closed**. **OWED — live T3 re-verify (Andy's hands):** re-run a T3 refresh post-merge → confirm the new pv reaches `ready` with a phase-correct `total_sessions` (diag token). **NEXT:** unchanged — the #541/#542/#543 plan-quality batch, then the compliance build-out (epics #353/#355/#356/#359).

### Predecessor — Plan-refresh hardening (#566)

**Plan-refresh hardening — dashboard v2 sessions + Refresh button, shared injury renderer (#555), refresh terrain feasibility (#557), async/resumable refresh (#208) (2026-06-13, PR [#566](https://github.com/ahorn885/exercise/pull/566))** (branch `claude/sleepy-rubin-5nilih`, squash-merged to `main`; handoff `handoffs/V5_Implementation_PlanRefreshHardening_2026_06_13_Closing_Handoff_v1.md`). **Made plan-refresh production-shaped — it now reuses the create path's hardened generation infra instead of a parallel synchronous one, and the two synthesis prompts share their renderers.** Four parts (A→D): **(A)** the dashboard Today/Tomorrow cards read only the legacy `plan_items` model, so an active v2 plan (e.g. pv=65) showed "No session scheduled" — new `plan_sessions_repo.load_scheduled_sessions_for_window` resolves the active per-day version pointer (D-64 §6.3) and `routes/dashboard._v2_session_card` renders it through the existing cards; added the missing **Refresh plan** button to the plan view. **(B)** fixed **#555** by extracting `layer4/injury_render.py` as the single injury-accommodation renderer (params + rationale, not just the modality type name) that both create (`per_phase`) and refresh (`plan_refresh_t1`) delegate to — they can't drift again. **(C)** **#557** — `orchestrate_plan_refresh` now runs the same `_build_terrain_feasibility` cascade the create path uses (#540), folds it into the `plan_refresh` cache key, renders the shared `_format_session_feasibility` block in all 3 tier prompts, and threads it through T3 cross-phase → Pattern A. **(D)** **#208** — the refresh POST now allocates a `generating` row, **freezes** its inputs (incl. the once-parsed intent — never re-parsed per pass, the #202 determinism lesson), and hands off to the shared progress screen + cron/poller via `created_via`-dispatch in `_advance_plan_generation_locked`; create rows take the byte-for-byte identical path. **Schema (D):** six idempotent `plan_versions` `refresh_*` columns (`_PG_MIGRATIONS`) — **APPLIED on Neon by Andy 2026-06-13**, no backfill. Full suite **2355 passed / 30 skipped**; CI green; **#555/#557/#208 closed**. #540's only residual is the Track-3-gated layer0 column lift (parked). **NEXT:** the #541/#542/#543 plan-quality batch (shallow strength / low-protein macros / structured health conditions), then the compliance build-out (epics #353/#355/#356/#359 — the long pole for general availability).

### Predecessor — Track 2 slice 2b.2b session-grid ceiling fields (#564)

**Track 2 slice 2b.2b — athlete-facing session-grid ceiling fields (2026-06-12, PR [#564](https://github.com/ahorn885/exercise/pull/564))** (branch `claude/slice-2b2b-session-grid-unit-fields`, squash-merged to `main` `64bf221`; handoff `handoffs/V5_Implementation_Track2_Slice2b2b_SessionGridUnitFields_2026_06_12_Closing_Handoff_v1.md`). **Drains the last owed Track 2 item.** The deterministic session-count ceiling (slice 2b.2a / §5.1.1) was shipped, but `per_phase` read its two athlete inputs from a top-level placeholder nothing populated, so the grid always used spec defaults. This wires the two controls end to end. **Fields:** `two_a_day_preference` (`never`/`occasionally`/`regularly`) — the primary density control; `peak_sessions_max` (optional INT) — the advanced override (authoritative when set, else the grid derives the ceiling from the preference). **Shipped:** `init_db.py` migration (`athlete_profile.two_a_day_preference TEXT` + `peak_sessions_max INTEGER`, idempotent `_PG_MIGRATIONS` ALTERs, nullable/no-CHECK — **APPLIED on Neon by Andy**); the two scalars on `Layer1Availability` §G (`layer4/context.py` + `layer1/builder.py` `availability_scalars`); `per_phase.py` now reads them from the **nested `availability`** payload (was a top-level placeholder) and passes to the grid; capture on **both** surfaces (onboarding Schedule step + profile Schedule tab) via the shared `routes/onboarding._parse_schedule_form` — two-a-day radio + optional advanced Peak-cap number (flash-rejected outside `1..2×available_days`); `static/style.css` `u-mw-120` (CSP-clean width util); `athlete.py` allowlist + `TWO_A_DAY_CHOICES`. **NO `session_grid` change** — it already derives the ceiling from the preference when `peak_sessions_max` is unset. `two_a_day_preference` is intentionally distinct from `doubles_feasible` per Design_v2 §5.1.1 (desired density vs scheduling feasibility); form copy calls it out. +`tests/test_onboarding_schedule_ceiling.py` (parser) + `test_layer1_builder` round-trip. Full suite **2333 passed / 30 skipped**; CI green. **One-time cache note:** new `Layer1Availability` fields shift `layer1_hash` → harmless one-time plan-cache recompute on first run. **UX watch-item (non-blocking):** two-a-day + doubles questions sit adjacent in the schedule form — revisit if redundant-feeling. **Track 2 follow-up tail now fully drained** (only gated, non-live tails persist: #557 refresh-wiring behind #208, the Track-3-gated layer0 column lift). **NEXT:** #539 is **deprioritized** — closed `not_planned` / `priority:low` (2026-06-11; premise disconfirmed — the cron already drives tab-closed gen to `ready`, not a cron bug). #540's create-path is done (tail gated, reprioritized down). Open candidates: the #541/#542/#543 plan-quality batch + the compliance build-out (epics #353/#355/#356/#359).

### Predecessor — Track 2 slice 2c.2c craft-feasibility axis (#562)

**Track 2 slice 2c.2c — the craft-feasibility axis (2026-06-12, PR [#562](https://github.com/ahorn885/exercise/pull/562))** (branch `claude/inspiring-galileo-nvnqys`, squash-merged to `main`; handoff `handoffs/V5_Implementation_Track2_Slice2c2c_CraftAxis_2026_06_12_Closing_Handoff_v1.md`). **Completes the #540 create-path feasibility cascade** — terrain (#553/#556) + **craft**. The terrain axis answers "is there a surface for discipline D?"; the craft axis answers "does the athlete own the vehicle?" and runs **first** per discipline. Gap it closed: an athlete with trail terrain but **no MTB** was prescribed an un-equippable trail ride (advisory X1b.3b flag only) — now deterministic. **Ladder (`group_kind` bike/paddle only):** own a craft for the discipline's modality group (a gravel bike for MTB — both `bike_offroad`) → train as-is; own only a same-`group_kind` craft in another group (a road bike for MTB — `bike_pavement`) → **swap** the allocation to the owned craft's discipline (train MTB as a road ride), then re-run the terrain axis on **that** discipline's surface; own no craft of the kind → **strength** terminal. **Shipped:** pure `resolve_craft_feasibility`+`CraftResolution` (`layer4/session_feasibility.py`); `TerrainResolution` craft overlay (`craft_tier`/`owned_craft`/`craft_swap_to_name`/`craft_kind`) + render (`feasibility_line` swap/strength reasons, `grid_annotation` `[CRAFT-SWAP]`/`[NO CRAFT]` tags); two orchestrator readers (`_q_modality_group_kind`, `_q_craft_group_kind`) + craft+terrain composition in `_build_terrain_feasibility`; folds into the existing render + cache hash with no new plumbing. **`Modality_Group_Spec_v2`** (v1 archived): §3.3 records `group_kind`'s second, Layer-4 *feasibility-substitute* use as **distinct from group_id WEIGHT pooling** (additive — pooling algorithm + §2 boundary unchanged). **Render copy was Trigger-#1 approved before wiring; full-ladder-vs-strength-gate was Trigger-#5.** +new `tests/test_layer4_craft_feasibility.py` + wiring/render tests in `tests/test_layer4_terrain_feasibility_wiring.py`. Full suite **2327 passed / 30 skipped**; CI green. **No DDL, no vocab adds, NOTHING owed** (reads genesis-resident `modality_groups`/`craft_discipline_aliases` + the 2c.2b craft columns). **NEXT: #539** (tab-closed plan-gen crawl — the other go-live blocker; #540 create-path now done). #540 **tail (gated, not urgent):** #557 refresh-wiring (behind #208) + layer0 column lift (Track-3-gated) — consider closing #540 with these as residual. Then the #541/#542/#543 quality batch.

### Predecessor — Track 2 slice 2c.2b craft-capture integrity (#558/#560)

**Track 2 slice 2c.2b — craft-capture integrity (2026-06-12, PRs [#558](https://github.com/ahorn885/exercise/pull/558) profile + [#560](https://github.com/ahorn885/exercise/pull/560) onboarding)** (branch `claude/inspiring-galileo-nvnqys`, both squash-merged to `main` `a867dc6`/`f0c74c6`; handoff `handoffs/V5_Implementation_Track2_Slice2c2b_CraftCapture_2026_06_12_Closing_Handoff_v1.md`). Prereq for the #540 craft axis. The discipline-baseline craft columns (`discipline_baseline_cycling.bike_types_available`, `discipline_baseline_paddling.paddle_craft_types`) were **read by Layer 1 + consumed by the X1b.3b craft-substitution narrowing while nothing ever wrote them** — the craft axis ran on empty data. **Shipped the write path on both surfaces:** new `athlete_crafts_repo.py` (`load_craft_catalog`/`get_athlete_crafts`/`replace_athlete_crafts` — closed-enum validated, per-family replace-all, upserts only the craft column; + Layer 1 eviction; mirrors `athlete_discipline_weighting_repo`); `athlete.BIKE_TYPES`+`CRAFT_LABELS`; `CyclingBaseline.bike_types_available` tightened free-text→`Literal` (parity with paddle, no silent alias miss); shared `templates/onboarding/_crafts_form.html` partial (bike/paddle checkboxes, slugs == `craft_discipline_aliases` keys) included by both the profile Athlete tab (`/profile/crafts`) and the onboarding Skills step (`skills_save` now persists crafts too). **Scope (Andy): tight — existing snake-case slugs only, no vocab adds, NO DDL.** SUP deferred (minor); **paddle-rafting D-019 deferred → team-only, filed [#559](https://github.com/ahorn885/exercise/issues/559).** Recurrence guard: capture enums cover every live `craft_discipline_aliases` key. **Key correction:** an initial "reconcile craft vocab to canonical names" premise was wrong — `designs/X1b3b_CraftDisciplineAliases_v1.md` already settled snake-slug keying + `cycling_trainer`-kept + #477-superseded. Full suite **2310 passed / 30 skipped**; CI green both PRs.

### Predecessor — Layer 4 slice 2c.2 wiring (#556)

**Layer 4 slice 2c.2 — terrain-feasibility WIRING into plan-create (2026-06-12, PR [#556](https://github.com/ahorn885/exercise/pull/556))** (branch `claude/eloquent-gates-1jvhj7`, squash-merged to `main`; handoff `handoffs/V5_Implementation_Layer4_Slice2c2_Wiring_2026_06_12_Closing_Handoff_v1.md`). **Go-live blocker [#540](https://github.com/ahorn885/exercise/issues/540).** The pure cascade (PR #553) was imported by nothing; this wires it. `terrain_feasibility: dict[str, TerrainResolution]` threads orchestrator → cached `plan_create` engine → per-phase synthesis prompt + cache key, mirroring `training_substitution_payload` end-to-end (**create-only**; refresh = [#557](https://github.com/ahorn885/exercise/issues/557)). **New:** `orchestrator._q_terrain_gap_rules` reader + `_build_terrain_feasibility` (per-discipline strength-pool extraction, priority-ranked; **excludes skill-gated disciplines so #336/#540 partition not clobber**; drops unconstrained); `hashing.compute_terrain_feasibility_hash` folded into `plan_create_key`; `per_phase` render = a dedicated `=== Session feasibility ===` directive block + a short inline session-grid tag on the count line for the kind-changing tiers (strength/reallocate), reusing #336's two-surface pattern; pure `feasibility_line`/`grid_annotation` helpers in `session_feasibility.py`. **STRENGTH tier fixed to name the equipment-bearing cluster locale (the gym), not always `locale_order[0]`.** The render is a **Trigger #1** change — rendered text reviewed with Andy against a real cone before merge. +14 tests (new `tests/test_layer4_terrain_feasibility_wiring.py` + strength gym-locale tests); **full suite 2296 passed / 30 skipped; CI green.** No DDL. **NEXT (Andy's call): slice 2c.2b — craft-capture integrity** (`bike_types_available` free-text → enum + casing normalize; paddle/bike capture forms unwired; no foot/snow/swim/climb craft capture; extend `craft_discipline_aliases`) → then 2c.2c craft axis → layer0 column lift. **#557 refresh-wiring** is sequenced behind the refresh route going live (#208). **#539** (tab-closed plan-gen crawl) still open alongside #540 at the top of the 4-tier order.

### Predecessor — Layer 2D determinism close (#202) + injury-accommodation audit

**Layer 2D determinism close (#202) + injury-accommodation workflow audit (2026-06-11, PR [#554](https://github.com/ahorn885/exercise/pull/554))** (branch `claude/happy-davinci-hsf02q`, `Closes #202`, squash-merged to `main`; handoff `V5_Implementation_Layer2D_DeterminismClose_202_2026_06_11_Closing_Handoff_v1.md`). Investigated **[#202](https://github.com/ahorn885/exercise/issues/202)** (plan-gen cache-key non-determinism, parent #201): `plan_create_key` folds every layer hash 1→3b, so any builder timestamp reaching a payload reaches the cache key. **#202 was already substantially fixed** (Layers 1/2A/2E day-anchored+tested; 3A `now` is validation-only; 3A integration fallback day-anchored; uuid prefixes feed output ids only). **Layer 2D was the one genuine remaining instance:** `_is_recent_post_surgical` used wall-clock `datetime.utcnow().date()` → its recency bool drives the cross-education loading swap → 2D payload → `layer2d_hash` → `plan_create_key`, so it could drift the cache key across a UTC day boundary between resumable passes. **Fix:** threaded the cone's day-anchored `today` through `q_layer2d_injury_risk_profile_payload` → `_evaluate_exercise` → `_recommend_accommodations` → `_apply_phase_contraindications` → `_is_recent_post_surgical` (defensive day-anchored fallback mirrors `layer3a/integration.py`); orchestrator passes `today` at both 2D call sites; +2 determinism tests. Suite **2265 + etl 88** green; CI green. **Downstream finding:** the post-surgical recency branch is currently **unreachable** (`_severity_to_verdict` maps Post-surgical → exclude, dominating `_max_verdict`), so the fix is defensive — if ever reached, the `loading_type_change` modality renders as a bounded enum (no LLM hang) but is under-specified + unenforced. **Filed/annotated the workflow gaps:** **[#555](https://github.com/ahorn885/exercise/issues/555)** (prompt drops modality params/rationale — Trigger #1), **[#242](https://github.com/ahorn885/exercise/issues/242)** (validator skips `loading_type_change`), + a dead-branch design fork (remove vs route post-surgical-with-clearance to accommodate). NO DDL. **NEXT: go-live blockers #539 + #540** (the #540 core just merged via #553 — its prompt-wiring slice is the live next move; the injury-accommodation gaps above are tier-3 + dormant).

### Predecessor — Layer 4 slice 2c.2 core: pure terrain-feasibility cascade (#553)

**Layer 4 slice 2c.2 — deterministic terrain-feasibility cascade (CORE; 2026-06-11, PR [#553](https://github.com/ahorn885/exercise/pull/553))** (branch `claude/relaxed-newton-556i1f`, squash-merged to `main`; handoff `handoffs/V5_Implementation_Layer4_Slice2c2_TerrainFeasibility_2026_06_11_Closing_Handoff_v1.md`). **Go-live blocker [#540](https://github.com/ahorn885/exercise/issues/540)** (pv=65: Rock Climbing scheduled at a home with no climbing terrain). Long stop-and-ask session that **fully ratified a design with Andy** (handoff §2) and landed the **verified pure core**. **Reframed** from "route + flag" to: deterministic, **pre-synthesis** resolution; **make a session that works, never flag-and-prescribe**; route across the whole **locale cluster** (home + nearby); **two axes** — terrain (this slice) + craft (deferred). **Cascade per discipline:** EXACT cluster terrain → PROXY terrain (`terrain_gap_rules` @ fidelity ≥ 0.25, e.g. MTB→road ride) → INDOOR machine (treadmill/stair climber/erg/trainer, gated on a cluster locale's equipment) → STRENGTH substitute (the discipline's own mapped exercises via Layer 2C `ResolvedExercise.discipline_ids`, e.g. climbing→pull/grip) → reallocate. **All on existing layer0 data — no padding, no migration.** D-018 Mountaineering = TRN-005/007/012. **MERGED:** `layer4/session_feasibility.py` (pure `resolve_terrain_feasibility` + discipline→terrain + discipline→indoor-machine maps), `locations.cluster_terrain_by_locale`/`cluster_equipment_by_locale` (per-locale, keyed — the "not always home" fix), 19 tests; **full CI green**. Wired into plan-create by PR #556 (above).

### Predecessor — Skill-capability gating (#336): substitute strength for unclaimed-skill disciplines

**Skill-capability gating — substitute strength for unclaimed-skill disciplines (2026-06-11, PR [#551](https://github.com/ahorn885/exercise/pull/551))** (branch `claude/upbeat-fermi-7bkkde`, squash-merged to `main` `94ec454`; handoff `V5_Implementation_SkillCapabilityGating_336_2026_06_11_Closing_Handoff_v1.md`). Closes **[#336](https://github.com/ahorn885/exercise/issues/336)** (high/safety; parent #201). The `requires_skill_capability` signal was **computed in Layer 2C but gated nothing** — pv=46 prescribed roped climbing + abseiling to an athlete who never claimed the skill (correctness + safety). **Andy's call: substitute, not exclude** — the discipline stays in the plan; its skill-specific session is swapped for strength-and-conditioning (grip/upper-body for climbing) with a coach note "prescribing strength until you're cleared on `<skill>`". New `blocker` validator rule `_rule_skill_capability_gate` + shared `skill_gated_disciplines()` helper (`layer4/validator.py`) sourced from the 2C flags — **consumes the orphaned flag (#298)**; blocks `kind=='cardio'` sessions tagged to a gated discipline, strength substitutions pass. Per-phase synthesizer prompt gains a "Skill-capability gates" substitution directive + inline `[SKILL-GATED]` session-grid annotation (`layer4/per_phase.py`); 2C flag message reworded (`layer2c/builder.py`). **Key data finding:** `D-012`'s `exercise_db_sport` is "Adventure Racing", so its 2C exercises are already AR strength — the prescribed climbing was *discipline-level sport sessions*, placing the gate at the session level not the exercise pool. +10 tests; suite **2351 passed / 30 skipped**; CI green. **OWED (now load-bearing): confirm `layer0.skill_capability_toggles` is applied on Neon** — the gate is data-driven off it; an unapplied table = silent no-op. _(Directly adjacent to #540: same "substitute strength at the session level" pattern + same `per_phase.py` prompt-annotation render site — the #540 wiring should reuse it.)_

### Predecessor — Docs sweep: Layer 0 DB-authoritative references

**Docs sweep — Layer 0 DB-authoritative references (2026-06-11, PR [#549](https://github.com/ahorn885/exercise/pull/549))** (branch `claude/docs-sweep-db-migration-9ra7cc`, squash-merged to `main` `e927072`; _no separate handoff — docs-only follow-up to the #488 freeze_). First step after the §6.4 xlsx-authoring freeze: swept the **live, forward-looking docs** that still framed the xlsx/ETL pipeline as the current Layer 0 authoring loop, repointing them at the DB-authoritative model (author via SQL migrations under `etl/migrations/layer0/`, gated by `validate_layer0`; xlsx toolchain frozen under `etl/_frozen_xlsx_authoring/`). **6 files:** `etl/README.md` (rewritten from "run the ETL" to the migration model + real `etl/` layout tree), `DEV_SETUP.md` ("Layer 0 ETL" section + 2 Windows examples → `etl.layer0.validate_layer0`), `specs/Control_Spec_v8.md` (§1 dataflow note, §2 Layer 0 authoring/source/versioning block, §10 "xlsx is source of truth" rule), `specs/Layer0_ETL_Spec_v8.md` (retired-authoring-model banner; kept as the schema/validation reference), `research/AR_Exercise_Database_Documentation.md` (historical-reference banner), `etl/migrations/layer0/README.md` (post-reorg design-doc path fix). **History left untouched** — `handoffs/`, `archive/`, research audit logs, this file's predecessor chain, `CARRY_FORWARD.md` (point-in-time records, not current guidance). Two parallel read-only scans found the refs + classified stale-vs-historical; re-grepped to confirm no `etl.layer0.run` / "xlsx is source of truth" framing remains outside the frozen archive + dated records. **CI green** (Layer 0 gate + Python suite + JS harness + Vercel preview); docs-only, no code/tests/CI touched. **NEXT: go-live blockers #539 / #540** (unchanged — top of the 4-tier order now the Layer 0 epic is closed).

### Predecessor — Layer 0 §6.4 freeze: retire the xlsx authoring path (epic #488 COMPLETE)

`handoffs/V5_Implementation_Layer0_XlsxAuthoringFreeze_2026_06_11_Closing_Handoff_v1.md` — 2026-06-11 (**Layer 0 §6.4 freeze — retire the xlsx authoring path; **epic [#488](https://github.com/ahorn885/exercise/issues/488) COMPLETE.** Branch `claude/layer0-xlsx-authoring-freeze`.** Last open epic item, gated on 2–3 clean migrations (cleared by `0001`+`0002`+`0003`). Andy chose **Option 3** (full retirement), directed execute after a keep-vs-retire classification. **Key finding:** every freeze-set symbol is imported **only by tests** (live equivalents stand alone — the route keeps its own `RACE_INELIGIBLE_TERRAIN_IDS`; terrain/toggle/substitute data is in the DB), so Option 3 = **archive the ETL + retire the ETL-guard tests, no constant relocation**. **Archived → `etl/_frozen_xlsx_authoring/`** (README-marked, CI-uncollected): `extractors/` (3 parsers), `run.py`, `emit_sql.py`, the 2 workbooks (`Sports_Framework_v14`/`AR_Exercise_Database_v19`), + the 6 ETL-only parser/canon/versioning tests. **3 main-suite tests resolved:** `test_onboarding_race_events` cross-check narrowed to the live route set (`{TRN-014,015,016}`); `test_layer2c_prep` parser classes retired (kept `TestSchemaSubstrate`); `test_bucket_c_terrain_vocab_audit` frozen whole (built on `_TERRAIN_STRUCTURED_ROWS`, the frozen ETL terrain source) — its integrity coverage **rebuilt DB-side** as a new `terrain_types` `validate_layer0` check (`validation/terrain_types_check.py`, the 9th gate check: TRN-NNN format + active-set uniqueness; 7 unit tests; green on genesis). **Stayed live:** `schema.sql`, `validate_layer0`+`validation/`+waivers, `export_xlsx` (#545 hedge), `db.py`, the canon/transform modules the gate imports, `etl/migrations/layer0/`, the genesis snapshot. **Verified:** full-suite collection **2364, 0 import errors** (dependency map held); edited/remaining tests **123 passed**; gate+hedge import clean. Design §6.4/§9 marked DONE; CLAUDE.md needed no change. **Layer 0 is now DB-authoritative end-to-end** (authored via SQL migrations, gated by `validate_layer0`; xlsx path frozen as history). **NEXT: go-live blockers #539 / #540** (top of the 4-tier order now the Layer 0 epic is closed).)

### Predecessor — `aidstation-sources/` reorganization + cleanup

`handoffs/V5_Housekeeping_AidstationSources_Reorg_2026_06_11_Closing_Handoff_v1.md` — 2026-06-11 (**`aidstation-sources/` reorganization + cleanup. Branch `claude/aidstation-sources-reorg`.** After #546 merged, Andy expanded scope to a doc/repo cleanup. Foldered the flat 87-file root into **`specs/`** (14 canonical layer + architecture specs) · **`designs/`** (19 per-feature designs) · **`research/`** (8 audits/evidence) · **`plans/`** (5) — root keeps only the 5 machinery files (CLAUDE/CURRENT_STATE/CARRY_FORWARD/PR_Verification_Status/README). **Superseded `_vN` versions → `archive/superseded-specs/`** (35 files; Rule #12 reconciled to archive-not-delete, Andy's call), dead ETL scratch (`etl/ migrations/ patches/ data/ vocab_reconciliation/`) → `archive/etl-scratch/`. `prompts/`/`handoffs/`/`scripts/`/`.claude/` unchanged (prompts/ imported by live code). **`README.md` rewritten** as a plain-English index ("read these three first" + a "Where do I find…?" table + per-folder one-liners) — the non-technical entry point; spec *files* deliberately NOT renamed (would break ~63 prompts + ~49 HANDOFF + ~12 cross-refs + CLAUDE.md resolve-by-name). `CLAUDE.md` synced (`specs/Control_Spec`, `specs/Layer2C_Spec`, Rule #12 archive note). **Deletes = cruft only** (8 `ph` 3-byte stubs + `placeholder.md`); everything else is `git mv` (**121 renames, no content lost**). 4 stray dated root `HANDOFF-*.md` → `handoffs/`. **Completeness-asserted** (87/87 assigned, 0 orphans/dupes); ETL-scratch refs confirmed doc-only; **`verify-handoff.sh` green**; no app/CI touched. **NEXT: the xlsx-authoring freeze (Option 3)** — now unblocked (gate cleared + clean base); fires Trigger #6, lead with a keep-vs-retire classification. **Owed/flagged:** brand `.zip`s at repo root (0 refs, binaries) left pending Andy's call.)

### Predecessor — Layer 0 migrations 0002 + 0003 (de-orphan spec-sourced tables; freeze gate cleared)

`handoffs/V5_Implementation_Layer0_Migrations0002_0003_SpecSourcedOrphans_DeOrphan_2026_06_11_Closing_Handoff_v1.md` — 2026-06-11 (**Layer 0 migrations `0002` + `0003` — de-orphan the two spec-sourced tables; **clears the §6.4 freeze gate**. Epic [#488](https://github.com/ahorn885/exercise/issues/488), branch `claude/eloquent-albattani-6ksbxb`, **PR [#546](https://github.com/ahorn885/exercise/pull/546)**. Andy: "keep going on the next slice of the migration." Epic's only open item was the §6.4 freeze, **gated on 2–3 migrations (only `0001` had)** — surfaced the fork (gated freeze vs advance the gate) via `AskUserQuestion`; Andy chose **advance the gate**, then "keep going" → landed both migrations. Two **spec-sourced orphan** Layer 0 tables (not in `schema.sql`/genesis, lived only as ad-hoc `etl/sources/*.sql`, so the CI gate never saw them) brought into the gate-covered model, each via **one self-contained idempotent migration** (`CREATE TABLE IF NOT EXISTS` + seed): **`0002_seed_supplement_vocabulary.sql`** (2E table; subsumes the base seed + the D-21 contraindication retag — tokens already canonical §B; named candidate "retag" was a no-op vs the canonical seed) and **`0003_seed_terrain_gap_rules.sql`** (2B table; subsumes `populate_terrain_gap_rules.sql` + the `partial`→4-band severity reclassify, reproducing the live `0C-v2.0-r2` state — 16 rows). Both keep DDL **out** of `schema.sql`: `supplement_vocabulary` *must* (it's `supp_vocab.*`-versioned, outside the `{0A,0B,0C}` family the `TestLayer0TableFamilyMap` drift guard pins — a first-pass `schema.sql` fold tripped CI and was reverted); `terrain_gap_rules` *could* (0C cone table, already in the family) but stays out for consistency. **Verified against a local throwaway-Postgres replica of `layer0-gate`** (Docker down + Neon blocked → ran `postgres:16` server bins unprivileged): schema + genesis + `0001`+`0002`+`0003` apply clean — 25 supplement rows (0 legacy tokens) + 16 gap rows (banded `crit:4/high:2/med:5/low:4/unbridge:1`, 0 `partial`) — **`validate_layer0` PASS**, idempotent; drift-guard test 3-passed; **etl/tests 197 passed**; main suite untouched. **`0002` APPLIED on Neon (Andy, 2026-06-11)** → 2E contraindication screening live. **`0003` Neon re-apply OPTIONAL** (terrain_gap_rules already live from the ad-hoc apply; idempotent re-derivation). **Freeze gate now 3/3 — CLEARED.** **NEXT: the §6.4 freeze itself** — archive extractors/`run.py`/`emit_sql.py` + v14/v19 (the DB→xlsx export #545 is the bulk-review hedge) — the last open epic item; fires Trigger #6 (surface before executing). **Also still open (higher on 4-tier):** #539 (tab-closed plan-gen crawl), #540 (terrain-infeasible locale routing).**)

### Predecessor — Layer 0 phase 4: DB→xlsx bulk-review export (decision B)

`handoffs/V5_Implementation_Layer0_Phase4_DBExport_2026_06_11_Closing_Handoff_v1.md` — 2026-06-11 (**Layer 0 phase 4 — DB→xlsx bulk-review export (decision B), epic [#488](https://github.com/ahorn885/exercise/issues/488). Branch `claude/beautiful-clarke-rgsgnm`. Phase 4 is two parts; shipped the ripe one. **`etl/layer0/export_xlsx.py`** — read-only DB→xlsx projection, one sheet per `layer0.*` table discovered from `information_schema` (no maintained list → auto-covers `terrain_gap_rules`/`supplement_vocabulary`/future tables), **active rows only** (`WHERE superseded_at IS NULL`). Recovers the spreadsheet's only genuine advantage (bulk visual review of all ~36 sports / ~211 exercises at once) without re-introducing it as an authoring input — edits still arrive as `etl/migrations/layer0/` SQL. Mirrors `validate_layer0`'s shape: pure unit-tested `cell_value`/`build_workbook`, `collect(conn)` the only DB-touching step, `main()` lazy-imports `db`. **Load-bearing subtlety:** `TIMESTAMPTZ`→tz-aware datetime, which openpyxl *refuses* to write → `cell_value` flattens datetimes to ISO strings (+ `Decimal`→float, `TEXT[]`→comma-join, `JSONB`→JSON). `etl/tests/test_export_xlsx.py` (12 DB-free). **etl/tests 197 passed** (185 prior + 12); main suite untouched. Design doc §6.5/§8/§9 synced in place. **NO commit of the .xlsx** (`.gitignore` `*.xlsx`; regenerate-on-demand, not a genesis artifact). **OWED — Andy's-hands one-time live run** (`python -m etl.layer0.export_xlsx` vs Neon — container egress blocked, so the SELECT path is unexercised here; pure layer fully tested). **Phase-4 §6.4 freeze STILL OWED + STILL GATED** on 2–3 migrations going through cleanly — only `0001` has; the export now makes that freeze *safe* when the gate clears, but don't pull it forward. **Also still open (higher on the 4-tier order):** go-live blockers #539 (tab-closed plan-gen crawl) + #540 (terrain-infeasible locale routing) — recommended #539; Andy chose phase 4 this round.**)

### Predecessor — Layer 0 slice 3b: per-table version resolution

`handoffs/V5_Implementation_Layer0_Slice3b_PerTableVersionResolution_2026_06_11_Closing_Handoff_v1.md` — 2026-06-11 (**Layer 0 slice 3b — per-table version resolution (epic [#488](https://github.com/ahorn885/exercise/issues/488)). Branch `claude/determined-dirac-5mjwge`; **PR [#544](https://github.com/ahorn885/exercise/pull/544)**. "Re-architect the lookup" (Andy's call) so a serving-relevant Layer 0 migration touches only the changed table, not the whole ~14-table family. **Finding that reshaped it:** every one of the 26 serving `etl_version` filters was already paired with `superseded_at IS NULL`, and supersede-before-insert keeps a table's active set single-version — so the version predicate did ZERO row-selection work; its only live job was the cache key. **Decision (Trigger #3/#5, Andy signed off):** Option B / B2 / take-the-one-time-wipe — **decouple serving from the version**. All 26 serving sites (5 Layer 2 builders + the 2 orchestrator `_q_` helpers) drop the `etl_version` predicate + bind and read `WHERE superseded_at IS NULL`; orphaned `version_0a/0b/0c` locals + helper params removed; builders still accept/validate/echo `etl_version_set` (cache provenance). `_q_current_etl_version_set` keeps the `{0A,0B,0C}` `dict[str,str]` contract but each value is now a per-table digest (`"sports=0A-v1.6.7;disciplines=0A-v1.6.8;…"`) from a single `UNION ALL` over the new `_LAYER0_TABLE_FAMILY` (24 tables, bucketed by table not prefix; per-table max). A serving-relevant migration bumps just the changed table → digest shifts → caches invalidate, no family re-stamp, no advance-the-max foot-gun. `terrain_gap_rules` (0C, created outside `schema.sql`, read by 2B) is mapped in; `supplement_vocabulary` stays out (own `supp_vocab.*` line). Drift guard `TestLayer0TableFamilyMap`. README shape-2 recipe + design §5.3 updated. **Suite 2294 / 30 skipped; etl/tests 185.** **OWED — NO Neon migration this slice;** first prod deploy wipes plan caches once (auto, harmless) → **cold-plan post-deploy verify** (terrain non-empty + real exercise pool; may also fix 2B terrain-gap proxies that were exact-matching the wrong `0C` version). **NEXT: phase 4** (design §6) — freeze extractors + remaining workbooks, then DB→xlsx export.**)

### Predecessor — pv=65 cold-plan review + Plans/profile quick-wins (#537)

**pv=65 cold-plan review + Plans/profile quick-wins — 2026-06-11, PR [#537](https://github.com/ahorn885/exercise/pull/537)** (branch `claude/plan-65-generation-handoff-yzcou4`; _no separate handoff — see the top entry of `CARRY_FORWARD.md`_). **The discipline-mix rewrite is empirically validated:** Andy's fresh cold AR plan **pv=65** reached `ready` (00:11→01:08) with **MTB-dominant** allocation — the win-condition for the whole X1a→X3/X4 + #509 series. (Open diag read: bike `load_weight.source` == `race_override` vs `bridge` — decides whether #342 terrain-tagging is still owed.) Shipped 4 review quick-wins in PR #537: strength sessions labeled "Strength · \<sport\>" (#4), coach notes grouped under the head (#2), §B profile-card spacing (#8), dropped medication-name capture (#9). Suite **2281 passed / 30 skipped**, CI green, NO DDL. **Filed the rest of the review as issues:** [#539](https://github.com/ahorn885/exercise/issues/539) tab-closed gen crawl (go-live), [#540](https://github.com/ahorn885/exercise/issues/540) infeasible terrain sessions (slice 2c.2), [#541](https://github.com/ahorn885/exercise/issues/541) shallow strength, [#542](https://github.com/ahorn885/exercise/issues/542) low-protein macros, [#543](https://github.com/ahorn885/exercise/issues/543) structured health conditions. **NEXT: #539 (go-live) or #540 (correctness), then the #541/#542 quality batch.**

### Predecessor — Layer 0 Open item E: 2B/2C version fix + migrations convention

`handoffs/V5_Implementation_Layer0_OpenItemE_2B2C_VersionFix_MigrationsConvention_2026_06_11_Closing_Handoff_v1.md` — 2026-06-11 (**Layer 0 xlsx→DB migration (epic [#488](https://github.com/ahorn885/exercise/issues/488)), continued from the Slice 1 `validate_layer0` gate. Branch `claude/pensive-wozniak-uvw1hi`. Investigating **Open item E** (the Trigger #3 `etl_version_set`-pinning check) surfaced a **live Layer 2B/2C serving bug** → fixed (**PR [#521](https://github.com/ahorn885/exercise/pull/521)**, `32e55fd`), then established the **migrations convention + first proof migration** (**PR [#533](https://github.com/ahorn885/exercise/pull/533)**, `8bd0691`). **#521:** `layer4/orchestrator._q_current_etl_version_set` read only `layer0.sports` and broadcast its `0A-`prefixed version to the 0B/0C keys; prod stores family-prefixed versions (`0A-/0B-/0C-v1.6.7`, Andy-confirmed by query) and Layer 2B/2C exact-match `etl_version`, so terrain (2B) + the exercise pool (2C) were resolving **empty in prod** (fail-soft, so plans still reached `ready`). Fixed to read each family's own version from a representative table (sports/0A, exercises/0B, terrain_types/0C) — repairs serving AND clears Open item E (single-family migrations now observable to cache invalidation). Guard `TestQCurrentEtlVersionSet`. **#533:** `etl/migrations/layer0/` (README convention + `0001_remove_triathlon_d007_noop_rows.sql`) + `ci.yml` `layer0-gate` now applies migrations after the genesis snapshot then runs `validate_layer0` (gate green = migration proven). **0001** (#476) supersedes the zero-weight Triathlon×D-007 placeholder rows across bridge/map/phase_load — **verified D-007 is NOT junk first** (real 95–100% band under Endurance Cycling + substitute/pairing/alias/modality target stay); cache-neutral, no version bump. **#476 closed.** Suites green (2235/30 at #521; etl-gate green at #533). **OWED — Andy's-hands:** apply `0001` in Neon SQL editor (no cache impact); cold-plan verify #521 (terrain non-empty + real exercise pool — may quiet recent 2B/2C plan-quality complaints). **NEXT (slice 3b, new session):** per-table version resolution re-architecture (Andy's call) so serving-relevant migrations bump only the changed table, not the whole ~14-table family.**)

### Predecessor — Plans-page UI detour (delete / archive / mobile)

`handoffs/V5_Implementation_PlansPageUI_DeleteArchiveMobile_2026_06_10_Closing_Handoff_v1.md` — 2026-06-10 (**Plans-page UI detour (off the X-series, Andy-directed). Branch `claude/plans-page-ui-issues-wjmw3l`; PR [#531](https://github.com/ahorn885/exercise/pull/531) squash-merged to `main`. Two Plans-screen gaps surfaced now that generated plans carry real info (X-series wiring landed green): (1) AI-generated `plan_versions` had no removal path (only Mark-complete) → added a three-action lifecycle **Complete** (finished) / **Archive** (quit/superseded, no completion implied — new `archived_at` column) / **Delete** (hard). Delete nulls the non-cascading back-refs (`superseded_by_version_id` predecessor pointer + `plan_refresh_log.plan_version_id_before/_after`) before DELETE; `ON DELETE CASCADE` clears sessions/nutrition/progress. New `gen_archived` bucket + Archived section in `plans/list.html` (`gen_card` took an `archived` param); `archive`/`unarchive`/`delete` routes in `routes/plan_create.py`; `profile._load_active_plan_nutrition` skips archived. (2) New-plan/Import unreachable on mobile (desktop topbar hidden < 860px shell breakpoint) → in-page mobile-only action row in `dash-head`, CSS-gated in `static/style.css`. Tests in `test_redesign_plans_list_render.py` + `test_redesign_profile_render.py`. **Suite 2274 passed / 30 skipped.** **OWED-Andy's-hands — Neon migration: `archived_at TIMESTAMPTZ` on `plan_versions`** (idempotent ALTER in `init_db.py` after the `completed_at` ALTER). **Apply BEFORE relying on the prod Plans page** — `routes/plans.list_plans` now SELECTs `pv.archived_at`, so the list 500s until the column lands; Archive is inert until applied, Delete + mobile actions work regardless. **NEXT SESSION: resume the original track** — the X1-X4 + #509 win-condition empirical proof (CARRY_FORWARD owed item 1; gated on whether the PGE race event's terrain rows carry per-row `discipline_id` + `pct_of_race`).**)

### Predecessor — X3/X4 + #509 race-derived discipline mix into both Layer 2A axes

`handoffs/V5_Implementation_X3X4_509_InclusionWeightingPrecedence_2026_06_10_Closing_Handoff_v1.md` — 2026-06-10 (**X3/X4 + #509 — race-derived discipline mix wired into BOTH Layer 2A axes. Completes the 4-slice discipline-mix rewrite (X1a→X1b→X2→**X3/X4**) + its inclusion follow-up (**#509**). The shared seam is X3's `layer4/orchestrator._derive_race_discipline_mix(target_race_event) → {discipline_id: pct}` (groupby-sum `pct_of_race` over terrain rows carrying a `discipline_id`; drop race-wide rows per Modality_Group_Spec §10; raw sums, downstream `_normalize_load_weights` rescales). **X3/X4** (PR [#520](https://github.com/ahorn885/exercise/pull/520), `7944ff9`, branch `claude/elegant-lovelace-tmt93d`): threaded as `race_discipline_overrides` at the **cone** 2A call site → the **weighting** axis; the 3-way precedence (race>athlete>bridge) was already built+tested in `_apply_modality_group_pooling` (X1b.2), so this is pure wiring of the live seam — NOT the stale CARRY_FORWARD "merge into athlete_discipline_overrides" plan (predated X1b.2; would have dropped `race_override` provenance). Single-session site off-race → unchanged. **#509** (PR [#523](https://github.com/ahorn885/exercise/pull/523), `fa663ad`, branch `claude/509-layer2a-inclusion-precedence`): wired the same signal into the **inclusion** axis. `layer2a/builder.py` SELECT now pulls `pla.default_inclusion` (was ignored — the bug); retired the `_default_inclusion` notes heuristic (could never emit `excluded`) for `_curator_default_inclusion` (column-authoritative, NULL→included); `_resolve_conditional`→`_resolve_inclusion(row, race_overrides, athlete_overrides)` = `race ?? athlete ?? curator-default`. **Decision (Andy): athlete weighting is the COMPLETE membership list** (Option A — weighted→in, unweighted→out; consistent with X2 all-or-nothing). Pooling scoped to `inclusion=="included"` so common exclusions don't pollute group pools. `Layer2A_Spec` §5.2/5.3/5.6 rewritten. NO DDL (column exists v10, populated on prod, gate-enforced). Tests: `TestDeriveRaceDisciplineMix`(5)+`TestRaceDisciplineMixWireUp`(1)+`TestInclusionPrecedence`(6). **Suite 2238 passed / 30 skipped** + etl/tests 185; CI green both PRs. **#509 closed (completed, #523).** **OWED — empirical win-condition proof:** cold AR plan re-run showing MTB-dominant allocation + curator-`excluded` AR disciplines no longer HITL-gating. **Gating unknown: does Andy's PGE race event's terrain carry per-row `discipline_id`+`pct_of_race`?** If all race-wide, both X4 + #509 are inert (fall back to bridge/curator) — then the next move is capturing that terrain→discipline tagging (#342).**)

### Predecessor — Wellness FIT Phase C field locks

`handoffs/V5_Implementation_Wellness_FIT_PhaseC_FieldLocks_2026_06_10_Closing_Handoff_v1.md` — 2026-06-10 (**Wellness FIT Phase C — 4 PR-#504 diagnostics surfaced through `/admin/fit-inspect` (`_sleep_sub_score_slot_candidates` + `find_constant_value_fields` + `find_value_match_fields` + `_sleep_counter_derivation_candidates`); Andy dropped 5 nights + 3 running activity FITs of fresh reference data unlocking the disambiguation. **Locked:** `[346] field_5` = Light sub-score, `field_7` = REM, `field_8` = Stress (triple-confirmed across May 28 / Jun 2 / Sep 8 — Sep 8's 37-score / 72-min-awake / low-stress 3.40 was the disambiguator); `field_10` = Awake (Sep 8's field_10 = 0 Poor against 72 min awake unambiguous — retracted the prior wrong Jun 2 lock of `field_5 = Awake`). **Best-guess:** `[384] field_18` = `sleep_stress_above_resting_pct` (% of overnight stress samples above Garmin's 25-cutoff; fits 6 reference nights within rounding; beats the alternative `100 − BB_overnight_delta` formula which broke on May 28 / Mar 17). Web research confirmed no public/community decoder of msg 384 exists; Garmin's published 0-25/26-50/51-75/76-100 stress bands give the threshold meaning. **VO₂max running = 48** concluded **Connect-API-blocked**: cross-night scan across 7 metrics files + 3 running activity FITs (Apr 20 / May 5 / May 15) found no constant 48 field; Andy confirmed value didn't drop to 41 (ruling out my gid140 field_31 candidate); is a cloud-side rolling aggregate, not in FIT. **Surfaced 3 new wellness charts:** Sleep contributor sub-scores · device (Light/REM/Stress/Awake 4-line); Sleep stress fraction · device (% above resting, best-guess); Body battery · overnight recovery (per night, anchored to garmin_daily_metrics.sleep_start_ms/sleep_end_ms via window-function SQL). Branch `claude/gifted-albattani-sissi8`; PR #504 squash-merged to `main`. 79 tests pass on wellness suites. **OWED-Andy's-hands:** Neon migration for 5 new nullable INTEGER columns (`sleep_light_sub_score`, `sleep_rem_sub_score`, `sleep_stress_sub_score`, `sleep_awake_sub_score`, `sleep_stress_above_resting_pct`) — idempotent ALTERs already in `init_db.py`; live verification (upload May 28 + Jun 2 + Sep 8 dumps, expect 3 new cards populating with the pinned values). **Still open on #283:** `[346] field_12/field_13` mystery counters (no good hypothesis); `[384] field_3` = sleep_onset_latency_sec (single Connect check unblocks — ask Andy for May 29 or May 30 "Time to fall asleep"); `[384] field_16` (tracks Stress sub-score with adjustments, no clean formula).**)

### Predecessor — X2 athlete discipline-weighting end-to-end

`handoffs/V5_Implementation_X2_AthleteDisciplineWeighting_2026_06_10_Closing_Handoff_v1.md` — 2026-06-10 (**X2 — athlete discipline-weighting wired end-to-end. Branch `claude/x2-athlete-discipline-weighting`; PR [#514](https://github.com/ahorn885/exercise/pull/514) squash-merged to `main` (`a284a6d`). Third slice of the discipline-mix rewrite (X1a→X1b→**X2**→X3/X4). The read path already existed (table + `Layer1TrainingHistory.discipline_weighting` + 2A `athlete_discipline_overrides`); X2 closed the two gaps — the **write UI** and the **orchestrator unpack**. New `athlete_discipline_weighting_repo.py`: `load_discipline_catalog` (all potential disciplines — distinct set across every sport's bridge, Andy's call over sport-scoped), `get`/`replace_discipline_weighting` (all-or-nothing, sum-to-100 / 1..100 → `DisciplineWeightingError`), Layer-1 eviction. New `POST /profile/disciplines` (`save_disciplines`) + a separate weighting form in the profile Athlete tab (own form so its sum rule can't block the profile save; client-side running total; no inline `style=` per redesign §18). `_athlete_discipline_overrides(layer1_payload)` threaded into **both** 2A call sites (shared cone + single_session); inert when empty (→ system defaults). Convention: `discipline_slug` stores the canonical `discipline_id` (direct unpack, no slug→id layer). New `tests/test_athlete_discipline_weighting_repo.py` (10 tests). **Suite 2389 passed / 30 skipped**; CI green. NO Neon migration (table pre-existed). **Merge drift:** `main` advanced under the branch — `#511` nutrition block collided in `edit.html` (kept both at merge) and the dirty state silently blocked Actions until resolved. X1b.3b also shipped this session (PR #512, `5f79eca`). **OWED:** X3/X4 (race terrain → `discipline_mix` + race>athlete>bridge precedence merge — the payoff slice that fixes plan #61's TR-dominant AR allocation; X1b.2 already left the `race_discipline_overrides` kwarg seam at `layer2a/builder.py:739`; coordinate with **[#509](https://github.com/ahorn885/exercise/issues/509)** inclusion-precedence, explicitly sequenced with X3/X4). Empirical proof that the athlete split shifts allocation. Start X3/X4 fresh off `main` — this branch accumulated drift.**)

### Predecessor — Layer 0 Slice 1: validate_layer0 integrity gate

`handoffs/V5_Implementation_Layer0_Slice1_ValidateGate_2026_06_10_Closing_Handoff_v1.md` — 2026-06-10 (**Layer 0 Slice 1: the `validate_layer0` integrity gate — live + CI-enforced over `layer0.*`. Branch `claude/exciting-mayer-enbgvg`; PRs [#510](https://github.com/ahorn885/exercise/pull/510) (sign-off) + [#513](https://github.com/ahorn885/exercise/pull/513) (gate), epic [#488](https://github.com/ahorn885/exercise/issues/488). Resolved the Layer 0 authoring-model design's 3 open decisions and approved the spec in place (#510): **A** admin UI → defer · **B** DB→xlsx export → phase 4 · **C** all validators FAIL, with one `sum_to_100` waiver bucket + `vocab_alignment`/`contraindicated_conditions` fix-not-waive; `default_inclusion` FAIL (closed enum, already ERROR in the ETL — §5.2 mislabel corrected). Built Slice 1 (#513, merged `ae8ac9c`): `etl/layer0/validate_layer0.py` orchestrator+CLI (8 validator funcs → stable violation ids → waiver filter → non-zero exit; lazy `db.connect` so logic is unit-testable), `layer0_validation_waivers.json` (5 by-design sub-100 sports waived), 12 DB-free unit tests, and a `ci.yml` `layer0-gate` job (throwaway postgres + `schema.sql` + committed snapshot → gate; no Neon). **The gate's first CI run = the inventory:** all clean except 5 `sum_to_100` (waived: Canoe/Kayak Marathon Ultra, Marathon Mountain, Mountain Running/Skyrunning, Swimrun, Ultramarathon Trail) + 1 `vocab_alignment` — 18 `Fencing` exercise-mappings orphaned by the 2026-05-30 Modern Pentathlon canon removal. **Fixed at source** (`sport_canon.REMOVED_EXERCISE_DB_SPORTS` lockstep filter) → re-emit `etl/output/layer0_etl_v1.6.7.sql`, **Andy-applied to Neon** (0A/0B/0C → v1.6.7). Mid-flight: merged main's parallel **X1b.3b** (craft-aliases) advance after a dirty-merge CI stall + a v1.6.6 add/add collision (regenerated v1.6.7 from the combined tree; X1b.3b passed the gate clean). Spun out **[#509](https://github.com/ahorn885/exercise/issues/509)** — Layer 2A inclusion-precedence unification (race > athlete > curator default; surfaced from a `default_inclusion` serving bug where Layer 2A ignores the column and re-derives from notes). `etl/tests/` 185 passed. **OWED:** `etl/migrations/layer0/` convention + first proof migration (+ **open item E** — confirm no downstream assumes versions only advance via a full ETL run); phase-4 extractor/workbook freeze; #509 (sequenced with X3/X4); DB→xlsx export (phase 4). **Drift fixed:** main's pointer was stale at 06-09 — X1b.3b shipped to `main` without updating CURRENT_STATE; this session re-points it.**)

### Predecessor — Layer 0 xlsx → DB source-of-truth (design spec + xlsx prune)

`handoffs/V5_Implementation_Layer0_XlsxRetirement_DBSourceOfTruth_2026_06_09_Closing_Handoff_v1.md` — 2026-06-09 (**Layer 0 xlsx → DB source-of-truth: design spec + full xlsx prune. PR [#487](https://github.com/ahorn885/exercise/pull/487), epic [#488](https://github.com/ahorn885/exercise/issues/488). Answered "how do we stop relying on the legacy xlsx foundational docs" — they're already *not* a runtime dependency (serving reads `layer0.*` only; 77 refs / 0 xlsx reads in app code). Spec `Layer0_AuthoringModel_DBSourceOfTruth_Design_v1.md` (DRAFT, pending sign-off on decisions A/B/C) flips the *authoring* loop: DB-canonical, edits as versioned SQL migrations, ETL validators ported to a DB-side `validate_layer0` gate (Slice 1 — the prerequisite; nothing ships before it). Pruned every superseded workbook — `etl/sources/` now holds only `Sports_Framework_v14.xlsx` + `AR_Exercise_Database_v19.xlsx` + `Vocabulary_Audit_v2.md`; deleted SF v6/v10/v11/v12/v13, AR v17, the `aidstation-sources/data/` dup folder, and `migrate_discipline_ids_R6.py`. Retired the v10/v11 test pins: `test_v11_parsers.py` → `test_sports_framework_parsers.py` repointed to v14 (counts 89/3/1 held); `test_discipline_canon.py` on v14 (took `main`, 24 disciplines). Closed the CI gap — `ci.yml` now runs `etl/tests/` (was `tests/` only; the Layer 0 parser/canon suite had zero CI coverage). Reconciled `main`'s mid-flight v13→v14 advance on merge. Full `etl/tests/` 162 passed. **OWED:** spec decisions A/B/C; Slice 1 `validate_layer0` validator port (#488); `etl/migrations/layer0/` convention.**)

### Predecessor — X1b.3a Layer 2C craft_substitution_via_group flag

`handoffs/V5_Implementation_X1b3a_Layer2C_CraftSubstitution_2026_06_08_Closing_Handoff_v1.md` — 2026-06-08 (**X1b.3a Layer 2C `craft_substitution_via_group` flag shipped — Modality_Group_Spec_v1 §6 consumer side. Branch `claude/x1b3-layer2c-modality-substitution`; PR [#480](https://github.com/ahorn885/exercise/pull/480) squash-merged to `main`. `layer2c/builder.py:_load_modality_groups` reader (mirrors layer2a's) + `_emit_modality_group_substitution_flags` emitter: for each discipline whose locale coverage_pct is below 0.50 threshold, finds same-group included disciplines with coverage at/above threshold, emits one `Layer2CCoachingFlag(flag_type="craft_substitution_via_group", ...)` per (target, candidate) pair with both ids in metadata. `Layer2CCoachingFlag.flag_type` Literal extended. Layer 2C now performs 5 DB queries per call (was 4); membership SELECT placed after `_build_coverage` + spec §8 flags. Pre-v1.5.0 substrate (empty membership) → no flags, identical to pre-X1b.3 behavior. New `tests/test_layer2c_modality_groups.py` — 6 tests covering §13 scenario 7 + no-membership / both-low / cross-group / multi-candidate / target-not-in-membership variants. **Suite 2133 passed / 16 skipped (+6 net).** **Triage win this session:** plans #62 + #63 failed immediately with `psycopg2.errors.UndefinedTable: relation "layer0.discipline_modality_membership" does not exist` — root-caused: the prior X1b session's "Andy-applied 03:31 UTC" had hit a non-prod Neon branch. Reapplied `etl/output/layer0_etl_v1.5.0.sql` via Neon SQL editor on the prod-pinned branch. Plan #64 (cold AR) went `ready` in one cold run: 5 blocks / 50 sessions / 0 traceback / Engine A baseline timings. X1b.1 substrate + X1b.2 Layer 2A pooling are now actually live on prod for the first time since their merge. **OWED:** X1b.3b (next session) = `layer0.craft_discipline_aliases` substrate (schema + xlsx + extractor + Neon-editor SQL) + `resolve_training_substitution` filter wire + orchestrator unpack — blocked here because `athlete_crafts` is free-text and modality membership is keyed on discipline_id, so the filter needs a craft-name → discipline-id alias map. Andy chose Option 3 (proper layer0 table over hardcoded whitelist) at the X1b.3 stop-and-ask. Mechanically-applicable edits captured in handoff §7. Also still owed: X2 (athlete UI + orchestrator unpack); X3/X4 (race terrain → discipline_mix + precedence merge); empirical proof of X1b.3a flag firing (locale with deliberate gear gap); plan #64 `/admin/plan/64/inspect` to confirm X1b.2 empirical-proof check (`modality_group_allocations[*]` should include `foot` + `paddle_flatwater` groups; allocation should be MTB-dominant per Andy's race spec); plan #61 quality issues; #476/#477 vocabulary canon cleanup.**)

### Predecessor — X1b modality groups: Layer 0 substrate (X1b.1) + Layer 2A consumer (X1b.2)

`handoffs/V5_Implementation_X1b_ModalityGroups_Layer0_Layer2A_2026_06_08_Closing_Handoff_v1.md` — 2026-06-08 (**X1b modality groups: substrate (X1b.1) + Layer 2A consumer (X1b.2) shipped. Branches `claude/x1b-modality-groups-layer0` (PR #478) + `claude/x1b2-layer2a-modality-pooling` (PR #479) — both squash-merged to `main`. **X1b.1 substrate:** schema adds `layer0.modality_groups` + `layer0.discipline_modality_membership` (many-to-many, with ETL canon-filter on membership rows pointing at canon-removed disciplines). `Sports_Framework_v13.xlsx` adds two sheets (Modality Groups: 9 groups per spec §3.2; Discipline Modality Membership: 26 seeded rows covering all 24 surviving disciplines). New `extract_modality_groups` + `extract_discipline_modality_membership` extractors in `etl/layer0/extractors/sports_framework.py`; ETL runner bumped v12→v13. New ERROR-severity `run_modality_group_orphan` validator. Live on Neon at 0A/0B/0C-v1.5.0 (Andy-applied 03:31 UTC; 9 groups + 23 memberships inserted, validators passed expected WARNs only). **X1b.2 Layer 2A consumer:** `_load_modality_groups(db, version_0a)` reader, `_apply_modality_group_pooling(disciplines, membership, race, athlete)` implements spec §5.1 algorithm (per-discipline base from existing `_compute_load_weight`; group pool sums race/athlete/base when ≥2 included members; per-member final via precedence race→athlete→bridge; REDIRECT §5.3 routes race tags on non-included crafts to first included same-group member + `craft_substitution_via_group` flag). New `ModalityGroupAllocation` payload type + `Layer2APayload.modality_group_allocations` list. `WeightResult.source` Literal expanded with `"race_override"`. New optional `race_discipline_overrides: dict[str, float]` kwarg on `q_layer2a_discipline_classifier_payload` (X3 wires; default None preserves backwards compat). Layer 2A now performs 3 DB queries (was 2). New `tests/test_layer2a_modality_groups.py` (7 §13 scenarios) + existing test query-count assertion bumped. Suite **2127 passed / 16 skipped (+7 net)**. **OWED:** X1b.3 (Layer 2C consumer + `resolve_training_substitution` filter to same-group members); X2 (athlete UI + orchestrator unpack); X3/X4 (race terrain → discipline_mix + precedence merge — wires the kwarg X1b.2 added); cold AR plan re-run (Andy's-hands empirical validation — bridge-only allocation should already shift MTB-dominant since v1.5.0 bands ≈ Andy's race spec); Sheet 5 phase-load research pass; plan #61 quality issues; #476/#477 vocabulary canon cleanup.**)

### Predecessor — Discipline-mix architecture rewrite (X1a + X1b spec)

`handoffs/V5_Implementation_DisciplineMix_X1a_X1b_BridgeBands_ModalitySpec_2026_06_08_Closing_Handoff_v1.md` — 2026-06-08 (**Discipline-mix architecture rewrite — X1a shipped (bridge bands rewrite + ETL framework supersede-before-insert fix) + X1b specced (modality groups). Branch `claude/jolly-mayer-a0K3Q`; PR #475. Monitored plan #61 → reached `ready` but allocation was TR-dominant (~31% TR / ~16% MTB by hours despite race spec saying MTB 45% / TR 20%). Root-caused 2-fold: (a) orchestrator strips race terrain's per-row `discipline_id` + `pct_of_race` at `layer4/orchestrator.py:372-373` (only `terrain_id` flows to 3B), and (b) the AR bridge bands themselves were wrong (TR 15-25, MTB 10-20 — inverted from real expedition AR). Andy directed a 4-slice fix: X1a bridge bands rewrite (data) + X1b modality groups (deterministic OR vocabulary on Layer 2A, fulfilling the deferred `BestFitModality_Spec_v4.md` §14 escape hatch) + X2 wire dead `athlete_discipline_weighting` end-to-end (UI + orchestrator) + X3/X4 race terrain → discipline_mix derivation + precedence merge. **Precedence:** race spec > athlete weighting > bridge default. **X1a shipped (PR #475):** `Bridge_Bands_Research_v1.md` (21 sports researched, ARWS / World Triathlon / FIS / FRA / IBU / ICF / ISMF / ITRA / UIPM / ÖTILLÖ / XTERRA cited); `Sports_Framework_v12.xlsx` Sheet 3 (43 of 73 rows rewritten — AR MTB 10-20 → 35-55 the smoking-gun fix); `Bridge_Bands_Patch_Log_v1.md`; `etl/layer0/run.py` points at v12; `etl/layer0/db.py` framework fix Andy caught at first run (UniqueViolation on `equipment_items_active_ci_name_idx` because insert_versioned was inserting active rows BEFORE superseding prior versions → reordered DELETE → UPDATE supersede → INSERT); `etl/layer0/emit_sql.py` + `etl/output/layer0_etl_v1.4.0.sql` (Neon SQL editor variant for atypical environments). **X1b specced:** `Modality_Group_Spec_v1.md` — 9-group vocabulary (paddle_flatwater/whitewater, foot, bike_pavement/offroad, snow_travel/glide, climb, swim_openwater); unified `foot` group (TR+Road+Trek+Orient); packraft in both paddle groups; single climb group; §5.3 REDIRECT semantics; all-or-nothing athlete weighting UI; no orphans (ETL enforces); no race-wide rows in discipline-mix derivation. **Live on Neon (Andy-applied):** 0A-v1.4.0 / 0B-v1.4.0 / 0C-v1.4.0 — sports 36, exercises 211, equipment 121, body_parts 54; cone cache fully invalidated. **Issues filed:** #476 (D-007 row cleanup), #477 (Endurance Cycling discipline-ID variant split — Andy chose Option 2 to split D-006/D-008 into format-specific IDs).**)

### Predecessor — Wellness FIT extraction audit (Jun 2 calibration day; PR #470)

`handoffs/V5_Implementation_Wellness_FIT_ExtractionAudit_2026_06_07b_Closing_Handoff_v1.md` — 2026-06-07 (**Wellness FIT extraction audit (PR #470, `07f31e7`) — 5 fixes from Andy's Jun 2 calibration day (3rd reference). RETRACTED `[384] field_18` = sleep_avg_respiration: May 28's 13/13 match was coincidence, Jun 2's field_18=70 vs Connect breath rate 12 disproved it; 13/49/70 spread tracks INVERSELY with sleep score 96/65/58 → some "worse-sleep-is-worse" metric (onset latency / pre-sleep disturbance), not respiration. NEW LOCK: `_SLEEP_DATA.fit` `[382] field_1` = restless_moments (May 28: 28 ✓ matches Connect "28 Restless Moments"). EXTRACTED from existing files (Andy's hypothesis right — data is in unparsed `MonitoringMessage` attributes, not separate file types Andy doesn't see emit): `floors_climbed`/`floors_descended` from `MonitoringMessage.ascent`/`.descent` (MAX-per-day since cumulative; May 30 10/15, Jun 2 8/5 ✓), `intensity_minutes` = `moderate_activity_minutes` + 2×`vigorous_activity_minutes` (Garmin's published formula; May 30: 5, Jun 2: 204 ✓). SpO₂ via opportunistic `MonitoringMessage.pulse_ox`/`current_pulse_ox`/`spo2` attribute capture — stays null if Fenix 8's typed message doesn't expose any. FIXED bulk-importer chronological ordering: Jun 2 had 3 `_METRICS.fit` files with ATL 95→107→126 across the day; new `fit_file_meta()` helper returns `(kind, time_created_ms)` in single parse → pre-pass sorts ascending → main pass dispatches in chronological order; latest UPSERT wins for any (date, field). Schema: 4 new nullable cols (`restless_moments`/`floors_climbed`/`floors_descended`/`intensity_minutes`) via idempotent ALTER. Bonus `_maybe_series` swap in `_build_chart_data` so test fixtures don't have to enumerate every column. `/wellness` gains 4 new cards (restless per-night bar / floors climbed-descended bars / intensity minutes bar / SpO₂ avg-low overlay); `active_minutes` Phase-B scaffold retired (intensity_minutes ships real data). Suite 2119 passed / 16 skipped (+13 net). Neon migration applied (Andy: SQL editor, verified 33-col schema). OWED-Andy's-hands: live verification on prod (upload May 27/28/30/Jun 2 dumps, expect new cards + ATL=126 not 95/107 = chronological sort proof). Issue #283 still open — sleep stage Deep/Light/REM minute split (`[384] field_5/6/7` packed encoding still cracks no decode pattern across 3 days), sub-score contributor positions in `[346]` (need a day where Connect rates one specifically Poor), field_18 mystery (what IS it?), and training_readiness/VO2max/fitness_age (may not exist on this watch model — only 4 FIT file types ever observed across 4 days). Orthogonal track — plan-gen go-live (slice 2b.2a cold #60 re-run) unaffected.**)

### Predecessor — Session-count ceiling slice 2b.2a (PR #465, plan #60 fix)

`handoffs/V5_Implementation_Track2_SessionCountCeiling_Plan60Fix_2026_06_07_Closing_Handoff_v1.md` — 2026-06-07 (**Session-count ceiling (slice 2b.2a) — fixes the cold PGE plan #60 stall. Branch `claude/plan-60-monitoring-kCS2m`; PR #465. Monitored plan #60 → FAILED (D-77 stall backstop, only Build:w1 cached). Root cause: the slice-2b session-count grid summed per-discipline counts with no global ceiling and no available-days awareness → 14 sessions for Andy's 6-training-day week (hard max 12) → Build:w2 unschedulable under the `two_per_day` payload invariant → every synth attempt raised a `Layer4Payload` ValidationError at construction → `synthesis_budget_exhausted` → stall. NOT the validator-retry loop Track 2 retired — an upstream feasibility gap. Fix (spec bumped to `Layer4_DeterminismFirst_Synthesis_Design_v2.md`, D11/§5.1.1): `session_grid.apply_session_ceiling` / `phase_session_ceiling` / `resolve_available_days` cap the weekly total at a deterministic Peak-anchored ceiling (`peak_sessions_max` default 10, or `two_a_day_preference` density never 1.0/occasionally 1.5/regularly 1.85), near-flat phase scales (Base .90/Build 1.0/Peak 1.0/Taper .85 — sport-science: taper holds frequency, cuts volume), hard-clamped to 2×available_days; over-ceiling weeks shed lowest-`load_weight` disciplines first (trim multis, then rotate singles out); phase volume_band hours preserved (fewer, longer sessions). NO LLM. NO HITL fail-path (deterministic shed always converges). `per_phase.py` resolves available_days + prefs from the Layer 1 payload at the grid call site (prefs default until 2b.2b). §5.4 rest-detection REMOVED (Andy: "the athlete owns their rest days") — dropped `expected_rest_count`/`detect_insufficient_rest`/`InsufficientRestWarning` + the `insufficient_rest` validator warning; consecutive-hard recovery-spacing advisory stays. Suite 2106 passed / 16 skipped. OWED: cold #60 re-run (Andy's-hands, after merge → prod); slice 2b.2b (athlete fields `two_a_day_preference`+`peak_sessions_max` + Neon migration + onboarding/profile UI); #469 lb/kg unit toggle (NEW backlog — 161kg-in-kg-field mis-entry surfaced here, separate from the #60 cause).**)

### Predecessor — Wellness FIT ingestion + Inspector UX (4-PR arc: #460 / #463 / #466 / #467)

`handoffs/V5_Implementation_Wellness_FIT_Ingestion_2026_06_07_Closing_Handoff_v1.md` — 2026-06-07 (**Wellness FIT ingestion + Inspector UX — 4 PRs shipped (#460 / #463 / #466 / #467), all squash-merged to `main` (`d94d07a` tip). Re-scoped issue #283 in place (was `icebox`/`deferred`; now `type:bug`/`area:integrations`/`priority:med`) — original symptom was that wellness FIT files weren't surfacing on `/wellness`. Built end-to-end: NEW `garmin_daily_metrics` table (24 nullable columns, idempotent `ALTER…IF NOT EXISTS` migrations); three NEW FIT parsers in `garmin_fit_parser.py` (`parse_metrics_fit` / `parse_sleep_data_fit` / `parse_hrv_status_fit`) reverse-engineered against May 28 + May 30 calibration days cross-referenced to Andy's Garmin Connect screenshots; bulk importer at `routes/garmin.py:1352` dispatches on `FileIdMessage.type` (32=wellness / 44=metrics / 49=sleep_data / 68=hrv_status) and UPSERTs via `COALESCE` so the 3 new file types land in any order without clobbering. `/wellness` restructured `templates/wellness/index.html` from one-card-per-data-source to **one-card-per-metric** with self↔device overlays (sleep score, energy) and 18 chart surfaces total: sleep hours / sleep score (self+device) / energy (self+body battery min) / soreness / mood / HRV (overnight + highest-5min) / acute training load / heat acclimation % / heart rate (resting+7d-avg+avg+peak) / training load / activities-done / stress (avg+peak) / stress time-in-zone (3-min × 4 bands) / respiration / body battery (high+low+charged+drained) / resting calories (RMR) / daily activity (steps+cal+distance) / body comp. Garmin's authoritative resting HR (from `[211] field_1`) replaces `MIN(wellness_log.heart_rate)` on the HR card. **Field-mapping locks (with cross-reference values pinned in parser docstrings):** `[384] field_2`=sleep_score (96/65), `field_9`/`field_11`=bedtime/wake (01:14/09:30 IST May 28 ✓), `field_24`=awake_minutes (4/8) — FIXED latent bug where `field_17` fallback didn't track awake (May 28 = 4 coincidence, May 30 = 3 vs actual 8); `[346] field_4`=duration sub-score (100/51); `[378] field_3`=acute training load (98/59, Andy's call); `[281] field_6`=heat acclimation % (32→22 US→Ireland); `[370] field_1`=HRV overnight avg×128 (54/52 ms), `field_2`=highest 5-min×128 (77/79), `field_0`=7d avg×128 with **65535 = "No Status" sentinel**; `[371] field_0`=per-period HRV samples×128 (overnight chart); `[211] field_0`/`field_1`=Garmin's 7d-avg/today's resting HR. PR #467 fixed two Inspector UX bugs at `/connections/?tab=files`: `1.15fr 1fr` grid was being squeezed by the Inspector's `<pre>` `min-content` (long unbreakable JSON tokens) — `min-width: 0` on both tracks pins fractional allocation; NEW **"Copy all"** button concatenates every parsed dump with `── filename ──` headers, writes via `navigator.clipboard.writeText` + execCommand fallback, flashes "Copied!" — CSP-clean nonced script. Suite **2106 passed / 16 skipped** (+22 net). **Owed-Andy's-hands:** the `garmin_daily_metrics` migration on Neon (DB egress blocked from container); validate the live page populates with May 28/30 dumps. **Issue #283 follow-ups still open** (documented in §6.1 of this handoff): sleep stage Deep/Light/REM minutes (needs 3rd calibration day to decode `[384] field_5/6/7` packed values); sub-score contributor positions in `[346]` (Light vs REM ordering); separate FIT file types for SpO₂ / VO₂max / training readiness / fitness age / floors / intensity minutes (need samples from Andy if Fenix 8 emits them). Orthogonal track — plan-gen go-live (Track 2 closed, cold PGE plan run still owed) is unaffected.**)

### Predecessor — Track 2 slice 2d (rx_engine post-hoc wiring + Track 2 CLOSED)

`handoffs/V5_Implementation_Track2_DeterminismFirst_Synthesis_2d_2026_06_06_Closing_Handoff_v1.md` — 2026-06-06 (**Track 2 Determinism-First Layer 4 Synthesis — slice 2d shipped (rx_engine post-hoc wiring + Rules 2 + 7 demoted) — Track 2 CLOSED. Branch `claude/eloquent-gauss-fE3PC`; PR #462 squash-merged to `main` (`a0b7b20`); prod deploy `dpl_9KhyJjWBe3L3BesWEesFcgvVuoBk` READY on `aidstation-pro.vercel.app`. NEW `layer4/rx_wire.py` runs post-synth via `orchestrator._apply_rx_wire` AFTER `_apply_locale_assign`: for each strength exercise, `rx_engine.current_rx(db, user_id, exercise_name)` (NEW reader) hit → overwrite `load_prescription` with `"{sets} × {reps} @ {lbs} lbs"` (or `"{sets} × {dur}s"`); else first-exposure → category-keyed RPE template (compound_barbell / compound_dumbbell / accessory_dumbbell / accessory_cable / bodyweight) + append `first_exposure` coaching flag. Deterministic classifier: layer2c tier 0/3 → bodyweight; else name cue ("barbell"/"dumbbell"/"cable"/"machine") + movement_patterns ({squat, hinge, push, pull, lunge} → compound). NO LLM in this path. Identity-preserving short-circuit when no exercises touched. Degraded per-exercise try/except — rx-wire defect can never wedge plan-gen. Returns `RxWireDiagnostic` for `synthesis_metadata` (Rule #14). Cardio + rest sessions untouched. Validator Rules 2 (acwr) + 7 (injury_violation) demoted blocker → warning; Rule 7b already warning. Spec §10 slice-2d table updated + status amended "2a + 2b + 2c + 2d shipped — Track 2 closed". Suite 2084 passed / 12 skipped (+33 net). Track 2 retires the fragility/quality half of Layer 4 — remaining validator blockers are now exclusively structural (`session_multi_locale`, `session_locale_not_in_cluster`, `two_per_day`, `discipline_excluded`, `schema_violation`). No DDL owed. **Track 3 dependency** (rx lookup keyed by exercise name in v1): once Track 3 migrates `current_rx` to layer0 ids, the reader bumps to id-keyed; until then layer0-only exercises fall through to first-exposure (acceptable v1 per spec §7). 2c.2 (cardio routing) + layer0 vocab adds + Track 3 still owed; the cold PGE plan run is the combined Track 1 + Track 2 win-condition proof.**)


### Predecessor — Track 2 slice 2c (rest detection + locale assignment + small-call LLM substitute)

`handoffs/V5_Implementation_Track2_DeterminismFirst_Synthesis_2c_2026_06_06_Closing_Handoff_v1.md` — 2026-06-06 (**Track 2 Determinism-First Layer 4 Synthesis — slice 2c shipped (rest detection + STRENGTH locale assignment + small-call LLM substitute + Rules 3 + 11 demoted). Branch `claude/sharp-euler-nGoV2`. NEW `layer4/locale_assign.py` runs post-synth via `orchestrator._apply_locale_assign`: deterministic majority-fit chooses cluster locale → 3-tier substitution ladder (pattern-match → tier-3 bodyweight proxy → small-call Haiku LLM with enum-bounded `pick_substitute_exercise` tool → `substitution_no_candidate` coaching-flag tail). Returns `LocaleAssignDiagnostic` for `synthesis_metadata` (Rule #14). Cardio + rest sessions untouched. `session_grid.py` extended with `expected_rest_count` + `detect_insufficient_rest`. Validator Rules 3 + 11 demoted blocker→warning; Rule 3 now also emits per-(phase,ISO-week) insufficient_rest warnings. Spec §10 slice-2c table updated. Suite 2051 passed / 16 skipped (+27). **Cardio-session route-locale routing pulled out → 2c.2 follow-up** (needs layer0 vocab work: new TRN-017 "Off-Trail / Bush", rename TRN-007 → "Technical Rock/Scree", snow-sports semantics design call). No DDL owed. Slice 2d (rx_engine wiring + remaining validator demotions) + 2c.2 (cardio routing) + layer0 vocab adds still owed.**)

### Predecessor — Track 2 slices 2a + 2b (spec APPROVED + tool-schema enum + session grid)

`handoffs/V5_Implementation_Track2_DeterminismFirst_Synthesis_2a_2b_2026_06_06_Closing_Handoff_v1.md` — 2026-06-06 (**Track 2 Determinism-First Layer 4 Synthesis — spec APPROVED + slices 2a + 2b shipped. Branch `claude/stoic-darwin-f3h6w`; PR #433 squash-merged to `main`. Slice 2a: tool-schema enum on `exercise_id` across all 4 LLM tool paths — out-of-pool picks structurally impossible; Rule 6a `equipment_unavailable` retired. Slice 2b: new `layer4/session_grid.py` (deterministic per-week per-discipline counts + maintenance cadence for sub-0.5-sess/wk disciplines + polarized intensity mix per Seiler + race-sim long day for `continuous_multi_day`); per_phase prompt rewrite (option A — cut allocation/dose/deload language; new `=== Session grid ===` block is now authoritative); `_rule_volume_band` demoted to warning-only. Suite 2024 passed / 16 skipped. Owed = the cold PGE plan run.**)

### Predecessor — Track 1 Locations Consolidation (canonical-direct equipment, live on prod)

`handoffs/V5_Implementation_Track1_LocationsConsolidation_CanonicalEquipment_2026_06_05_Closing_Handoff_v1.md` — 2026-06-05/06 (**Track 1 Locations Consolidation built end-to-end + live on prod (design APPROVED): canonical-direct equipment model fixing the pv=59 `equipment_unavailable` vocabulary mismatch. Branch `claude/practical-wozniak-WSKk4`; PRs #426 + #431 merged to `main`. Neon DDL applied. 3-track epic #427 filed (Track 1 #428 / Track 2 #429 / Track 3 #430). Andy verified live: add Mapbox locations, save equipment on legacy + new locations, mark either Home — all work. Owed: the cold PGE plan run (the `equipment_unavailable`-gone proof)**)

**Plan-gen go-live track (Track 1 of the 3-track redesign).** Implemented the signed `Locations_Consolidation_Design_v1.md` in its 1a→1b split. **1a:** new **`locations.py`** — the authoritative resolver (`primary_locale` = `locale_profiles.preferred`; `locale_effective_tags` = `(gym_profiles.equipment ∪ adds) ∖ removes` in **layer0 canonical names**; `cluster_locale_ids`/`cluster_effective_tags` = home + saved locales within 42.2 km) — wired into `orchestrator` (`_q_primary_locale`/`_q_locale_equipment_pool` delegate; the cone passes the real cluster to 2C, un-stubbing `[primary_locale]`). Removes the snake_case-`public.equipment_items.tag` vs Title-case-canonical mismatch (the pv=59 root cause) at the source. **1b:** unified the locale equipment model — `routes/locales.py` picker reads `layer0.equipment_items` (canonical), one `_edit_locale` path (build/own-edit/peer-inherit) replaces the legacy/shared split, **home=`preferred`** (atomic `POST /locales/<l>/home` + first-locale-auto-home + `locale_profiles_one_home_idx`), residential→`private=TRUE`; `init_db.py` **DROP `locale_equipment` + DROP COLUMN `locale_profiles.equipment`** at the migration tail (seed removed). **One stop-and-ask resolved (Andy: "degrade now"):** the v1 strength-UI surfaces (`/references/exercises` availability filter, purchases owned-hint) depend on the public int-id catalog that can't be canonical-ized until **Track 3** — degraded cleanly (bucket filter + counts kept); coaching/plans/dashboard rewired to `preferred`. **Full suite 1998 passed / 16 skipped.** **Post-merge (PR #431, `7636217`):** two live save-equipment bugs fixed — (1) the Track-1 500 on `/locales/<l>/edit` (`gym_profiles.category` NOT NULL vs a categoryless locale → `_create_gym_profile` defaults to private `home_gym`); (2) pre-existing Mapbox "malformed" (Phase-6 redesign dropped `lng`/`lat`/name fields from `new.html` — restored). **Neon DDL applied (A0 clean, one-home index + DROPs verified). Andy verified live: locale add/edit/make-home all work on legacy + new locations.** **Still owed: the cold PGE plan run** (no `equipment_unavailable`, blocks → `ready`). **Next: spec + build Track 2 (#429, determinism-first Layer 4 — owns session→locale assignment); Track 3 (#430) catalog migration restores the degraded references/purchases surfaces.** Diagnostic token in the handoff ⚡ callout.

### Predecessor — Engine A fixes (#423) + pv=59 equipment root-cause + Track 1 scope/A0

`handoffs/V5_Implementation_EngineA_Fixes_LocationsConsolidation_Scope_pv59_2026_06_05_Closing_Handoff_v1.md` — 2026-06-05 (**monitored pv=59 (post-#316 grid) → FAILED at `Build:w1`; fixed Engine A (#423: latency-accounting + `extended_thinking_budget=0`); VERIFIED the pv=59 `equipment_unavailable` is a vocabulary mismatch (orchestrator feeds snake_case `public.equipment_items.tag`; 2C resolves layer0 Title-case canonical) NOT missing data; scoped a 3-track redesign + shipped slice A0 (layer0 equipment dedupe). Branch `claude/amazing-thompson-FIfxN`; PR #425 merged to `main`**)

**Plan-gen go-live track.** pv=59 failed at `Build:w1`: **Engine A** — the synthesizer's `thinking=5000` first attempt ran away 437s to `max_tokens` (no tool block), the forced retry did the real work in 116s, and the per-block budget guard UNDERCOUNTED (it saw only the winning 116s, not the wasted 437s) so it started a doomed next attempt → 800s 504. **Fixed (#423, PR #425):** `invoke_tool_call` accumulates wall time across attempts (`latency_acc`); `DEFAULT_EXTENDED_THINKING_BUDGET 5000→0` skips the thinking-first attempt (straight to the forced ~116s tool). The grid (#316) verdict: `Build:w1` showed **1** `volume_band` blocker (down from pv=58's 3) but the correction loop never ran attempt 2 (Engine A ate the budget) — the next cold plan (with #423) is the real grid test. **Separately VERIFIED:** the `equipment_unavailable` is a **vocabulary mismatch** — `orchestrator._q_locale_equipment_pool` returns snake_case `public.equipment_items.tag` (`squat_rack`) but 2C resolves `exercises.equipment_required` in layer0 Title-case canonical (`Squat rack`) → `issubset` never matches → every equipment-requiring exercise fails regardless of saved gear (bodyweight resolves → why Base banked). **Andy then directed a 3-track redesign** (`Locations_Consolidation_Design_v1.md` = Track 1 spec): **Track 1** Locations Consolidation — drop legacy locations, canonical-direct equipment store (NO alias; picker reads `layer0.equipment_items`), `home`=`locale_profiles.preferred`, `private`=crowd-source-exclusion only, multi-locale 26.2 mi cluster; **Track 2** determinism-first Layer 4 synthesis — intensity/rest/session-allocation/exercise-selection deterministic, LLM only organizes the week, feasibility validator rules demoted to warnings (not yet specced); **Track 3** = full D-52 catalog migration (equipment slice on the critical path; exercise-catalog bulk parallel, not a plan-gen blocker). **Shipped slice A0:** `etl/sources/dedupe_layer0_equipment_items.sql` (dedupe `layer0.equipment_items` — ~15 exact dupes + 3 case-variant pairs + recurrence guard). **Owed Andy's-hands:** run A0 on Neon; then (after Track-1 A1) run the Track-1 DDL, re-enter Home gear in the canonical picker, re-run a cold plan (win = no `equipment_unavailable`, no `volume_band` blockers, no Engine-A 504, blocks → `ready`). **Next: build Track 1 / A1 (canonical-direct code), file the 3-track epic.** Diagnostic token in the handoff ⚡ callout.

### Predecessor — Layer 4 per-week volume periodization grid (#316)

`handoffs/V5_Implementation_Layer4_VolumePeriodizationGrid_316_2026_06_05_Closing_Handoff_v1.md` — 2026-06-05 (**pv=58 cold plan (budget raised to 800) stalled at `Build:w2` → root-caused from pulled logs to two engines; fixed the data engine — the per-week volume periodization grid (#316, closed) — branch `claude/quirky-volta-rcjOo`; PR #422 squash-merged to `main`**)

**Plan-gen go-live track.** Andy raised `PLAN_GEN_FUNCTION_CAP_S` to **800** and re-ran a cold PGE plan (**pv=58**); it stalled at `Build:w2` (1/~6 blocks → 800s `FUNCTION_INVOCATION_TIMEOUT` → stall backstop). Untruncated logs (Rule #14) root-caused **two independent engines**: **(A)** the synthesizer's first attempt runs `extended_thinking_budget=5000` + `tool_choice:auto` and ran away to `max_tokens` (`418s`, empty output) before the forced-tool retry did the real work in `110s` — each failed attempt ≈530s, ungated; **(B)** `volume_band` graded each week against a **flat per-phase band** while the prompt told the model to ramp/deload/taper → `Build:w2` mis-split the weekly hour budget (`above_week_2_D-001` + `below_week_2_D-003`/`D-009` blockers) and a legitimate ramp would have tripped the flat band anyway. **This session fixed Engine B — #316, the per-week periodization grid** (Andy deferred Engine A → #423). **PR #422 (merged, NO migration):** new **`layer4/periodization.py`** = single source of truth for the per-`(phase, week_in_phase)` volume multiplier, consumed by the validator (`phase_week_volume_bands_hours`; `_rule_volume_band` grades each week against its own bent band; `above`/`below` symmetric; the PR #315 flat-band demotion **removed**), the synthesizer prompt (`_format_phase_load_bands` renders a **concrete per-week, per-discipline target + tolerance** — fixes the mis-allocation), and a **`recovery_week` stamper** (the §8.1 orchestrator step that was documented but never implemented). **Curve signed off 2026-06-05** (`Layer4_VolumePeriodizationGrid_Design_v1.md`): deload every 4th wk mode-dependent at ~45%, ~8%/wk loading ramp resetting each deload, **Base/Build/Peak volume-neutral**, **Taper a Bosquet-2007 41–60% descent** — cited (deloading / ACWR / Bosquet literature). Also a stale-cap comment refresh (`per_phase.py` 300s→`PLAN_GEN_FUNCTION_CAP_S`). `tests/test_layer4_*` **875 passed** (new `tests/test_layer4_periodization.py`; volume-band tests re-baselined). **v1 is athlete-agnostic — 3A coupling = #424 (committed v2).** **Expected first post-deploy cold plan: a one-time block re-synth** (prompt-text cache-key shift; cone still HITs). **Next: Andy re-runs a cold plan** — win = NO `volume_band` blockers + `recovery_week` on deload weeks; **caveat: Engine A (#423) is unfixed, so a dense week may still 418s-runaway and 504 even though validation now passes** (that's #423, not the grid). **Diagnostic token in the handoff ⚡ callout.**

### Predecessor — Layer 4 strength surface (#335) + diag observability (pv=57)

`handoffs/V5_Implementation_Layer4_StrengthSurface_335_DiagObservability_pv57_2026_06_05_Closing_Handoff_v1.md` — 2026-06-05 (**pv=57 cold plan stalled at the Peak seam → root-caused (from pulled logs) to the strength synthesizer flying blind; fixed #335 strength surface + diag observability — branch `claude/v5-layer4-handoff-9q4Vs`; PR #421 merged to `main`**)

**Plan-gen go-live track.** Andy ran a fresh cold PGE plan (**pv=57**) to exercise the seam fix; it **stalled at block 3/~6 (`Peak:w1`)**. Pulling the actual Vercel logs (Rule #14) root-caused it — after two mis-theories I corrected: the kill is the **800s `FUNCTION_INVOCATION_TIMEOUT`**, NOT a 300s gateway; the cone is a clean HIT (not re-computing). **Real cause = #335:** the per-phase synthesizer prompt rendered only `effective_pool size=N` (a count), never the resolved exercise list → the model **invented `exercise_id`s** → Rule 6a rejected each as `equipment_unavailable` (even for gear Andy owns) → that under-specified task drove a **395s empty-output thinking attempt** on `Peak:w1` → 800s timeout → stall. **PR #421 (merged):** **#335 Slice 1** (`_format_strength_exercise_pool` renders `=== Strength exercise pool ===` + the `# Strength programming` system prompt — pick real ids, never invent; per-phase dose; RM/RPE); **`movement_patterns` threaded** through 2C (`layer0.exercises.movement_patterns` → `ResolvedExercise`) powering pattern ranking + a **data-driven core/accessory** split (core = Critical/High priority + compound pattern); **Slice 2** the `strength_frequency_band` advisory validator (Base/Build 2, Peak/Taper 1, ±1, `warning`-only); **diag observability** (per-block `synthesis_metadata` + `advance_lock_until` + `server_now` + `block_timing` in the token-readable JSON); **stall reaper self-explains** (writes its measured anchor/age/window into `generation_traceback`); and a **test-fixture fix** (`_PLAN_START = date.today()` — the hardcoded `date(2026,6,1)` was failing 45 tests on the calendar; the product form already defaults to today + rejects past dates). **NO migration.** **Deferred:** #335 Slice 3 (§3 3A-strength-state dose *modulation* — base dose ships, personalization not wired) + Phase 2b rx_engine; Layer 2B cache-key drift (`l2b` re-keys blocks); advance-lock TTL (310s) < real 800s ceiling; optional `PLAN_GEN_FUNCTION_CAP_S` 300→800 env tune (faster, now safe). **Next: Andy re-runs a cold PGE plan in a new session** — expect no equipment blockers, no thinking runaway, blocks climbing to `ready`, finally exercising the seam fix. **Diagnostic token is in the handoff's ⚡ callout at the top.**

### Predecessor — Layer 4 seam-verdict coercion + advance-lock TTL (pv=55/pv=56, PRs #416 + #419)

`handoffs/V5_Implementation_Layer4_SeamCoercion_AdvanceLockTTL_pv55_pv56_2026_06_04_Closing_Handoff_v1.md` — 2026-06-04 (**plan-gen go-live: two cold PGE plans (pv=55, pv=56) validated the Phase 1 stall fix and surfaced two NEW blockers, both fixed — branch `claude/layer4-strength-phase2-U2btL`; PRs #416 + #419 merged to `main`**)

**Plan-gen go-live track.** Andy ran two fresh cold PGE plans on prod to validate the Phase 1 `sport_locale_incompatible` fix (#413); each failed differently and was root-caused from the diag endpoint + `/admin/plan/<id>/inspect` + Vercel runtime log, fixed, shipped. **Phase 1 is validated** — both plans sailed far past pv=54's stall-at-0. **(1) pv=55** synthesized **70 sessions / 6 blocks** then died at the **seam reviewer** on `seam_reviewer_invalid_verdict_combination`: a single advisory LLM mislabel (`patched`+`accept_with_observation`) NOT in the route's `_RETRYABLE_BLOCK_CODES`, so it killed a near-complete plan on the FIRST occurrence (the #335 "no new blockers" lesson, on the seam path). **PR #416 (`3ce7ed3`):** `_coerce_verdict_combination` normalizes every invalid seam (verdict, direction) to the nearest valid one and continues (a seam review is advisory — it judges a transition between two already-validated phases — so it must never discard valid synthesis); + the non-retryable `Layer4OutputError` route branch now logs detail + persists `generation_traceback` (the Rule #14 blind spot that made pv=55 read `traceback: null`). **(2) pv=56** cached only **1 block / 10 sessions** then hit the 900s stall — NOT latency (block 0 = clean 169s, 0 retries): the per-plan **advance-lock leaked** — a session `pg_advisory_lock` whose holding pass was 504/SIGKILLed before its `finally` release, surviving on Neon's pooler so every later cron no-op'd on "advance lock held elsewhere" → zero progress → stall. **PR #419:** replace it with a **TTL claim** on a new `plan_versions.advance_lock_until` column (atomic conditional `UPDATE…RETURNING id`; cleared in `finally`; a killed claim lapses after `_ADVANCE_LOCK_TTL_S` = `min(budget+280, stall−60)` and the next cron reclaims). **Issues: #417 filed+closed (seam-fatal, by #416); #418 filed (Layer 4 `notable_observations` have no reader — seam escalations go into the void; #295, deferred).** [**Superseded by pv=57:** the "~300s gateway" latency framing here was disproven — the kill is the 800s function timeout; and the real blocker was the strength surface (#335), fixed in the session above.]

### Predecessor — redesign finish-the-open: Garmin FIT import + remaining base_legacy migration (PR #414, orthogonal track)

`handoffs/V5_Implementation_Redesign_Phase6_FinishOpen_GarminFITImport_2026_06_03_Closing_Handoff_v1.md` — 2026-06-03 (**redesign finish-the-open: the manual `.FIT`-import flow (`garmin/import` · `import_preview` · `import_wellness`) migrated off `base_legacy` onto the new `.app` shell — branch `claude/great-lovelace-GuFhP`, PR open to `main`**)

**Redesign track, tier-3 finish-the-open (orthogonal to the plan-gen go-live board).** One multi-slice PR (#414, open) that finished the redesign migration. Slices, each token-classed + render-tested + CSP-clean, behaviour unchanged: **(1)** the manual-`.FIT` flow — `garmin/import`/`import_preview`/`import_wellness` (bulk `data-bulk-*` uploader + cardio/strength preview + nonced disposition script) brand-neutral per §E.4 ("body battery"→Recovery); **(2)** the `garmin/wellness_log` viewer (Chart.js vars remapped off legacy `--ink`/`--orange` to the new tokens); **(3)** the **print stylesheet** (global `@media print`: drop chrome + un-flex shell, `.no-print`/`.print-only`); **(4)** the **auth screens** login/register/forgot/reset off the old Bootstrap `auth/_shell.html` onto an `.app` standalone shell (real `username` kept; no fabricated stats/social-OAuth/remember-me); **(5)** a trail-voice **403** page (`@errorhandler(403)`→`_error.html`); **(6)** the **onboarding wizard** — Connect/Profile/Locations/Skills/Schedule/Target race + route-locales onto the new shell behind a shared progress stepper (`_onb_steps.html`), Target race keeping its Bootstrap-grid body + the 3 shared race partials. An **artboard audit** drove (4)–(6); a **second audit pass** then took **(7)** the rest of the implementable `base_legacy` set as 4 more slices — the logging **history lists** (`training`/`cardio`/`body`/`conditions`/`injuries` `list.html`), `training/form` (strength edit), `profile/feedback` (§20 provenance), `profile/race_event_edit` (race form, shares the 3 race partials with `target_race`), and `natural_log/index` (NL "log via text"). **Every designed + route-backed surface is now on the new `.app` shell.** New `tests/test_redesign_{garmin_import,garmin_wellness_log,print_styles,auth,onboarding,log_lists,log_detail,race_event_edit,natural_log}_render.py` + `_error`(403). **Redesign + auth suites green (98); CSS braces 963/963; no migration, no owed deploy.** **Remaining on `base_legacy` — final disposition (not gaps):** by decision (`garmin/auth`/`sync`/`sync_preview`, `admin/plan_inspect`), blocked (`coaching/review` ⛔§30), undesigned/no-artboard (`purchases/*`, `references/exercises`, `workouts/{build_form,suggestion_view}` — the ad-hoc LLM build flow, design later). **Next: the redesign migration is complete — remaining redesign work is net-new (e.g. the ad-hoc workout-builder screen needs a design first); or (higher priority, off-thread) the plan-gen go-live board (#316/#350 latency + §14 coherence #333).** All on PR #414.

### Predecessor — plan-gen cold pv=54 stall fix (Phase 1, advisory locale rule) + Phase 2 strength design (PR #413, plan-gen track)

**Watched cold plan pv=54 stall → root-caused two distinct Layer-4 defects, shipped Phase 1, specced Phase 2.** pv=54 (the first genuinely *cold* full-synthesis PGE plan) reached 8 week-blocks then tripped the 900s stall reaper on the `Build:seam0:w3` seam re-synthesis. **Root cause = `sport_locale_incompatible` (validator Rule 13) firing unsatisfiable BLOCKERS** on D-003/D-008/D-012 at `home` — it gated *sport/cardio* sessions on Layer 2C *strength-exercise* coverage (a category error; D-008 MTB / D-012 Climbing have **zero** Layer 0B exercises, so coverage is structurally 0 even though Andy owns the bike/packraft). **Phase 1 (PR #413, merged):** Rule 13 → advisory `warning`, **strength-scoped** (skips cardio); Rule 6a `equipment_unavailable` stays the hard per-exercise gate; `Layer4_Spec` row 917 updated; validator suite **109 + related 350 green** (the per-phase prompt is terrain-non-gating now). The deeper finding behind it: the per-phase synthesizer **renders no resolved exercises** and has **no strength-dose policy**, so strength comes out bare/empty (**#335**; pv=54 generated **0** strength sessions). **Phase 2 (specced + signed off, `aidstation-sources/Layer4_StrengthProgramming_Phase2_Design_v1.md`):** render the exercise surface + an **evidence-based dose policy** (2×/wk Base/Build → 1× Peak → taper-off; 3–5 multi-joint, 4–10RM; **RM/RPE loads**; hybrid stable-core + rotating-accessory; **unilateral/offset integrated-stability bias**; **paired day-level scheduling**). Deterministic frequency/pool/guardrails (warnings, never blockers) + LLM selection/placement/variety. A cited sport-science deep-research pass backs the policy (doc §A). **rx_engine deterministic absolute loads = Phase 2b, deferred (#335); multi-locale cluster = Phase 3, deferred (#298/#341).** **Drift reconciled:** this pointer was stale past #352 — **#396** (advance-lock leak fix + 800s cap correction), **#397–#404** (redesign phases), **#405** (per-attempt latency logging, #316 Slice 0), **#406** also landed on `main`. **Next: see CARRY_FORWARD — re-run a fresh cold plan to validate the Phase 1 stall fix empirically, then implement Phase 2.**

### Predecessor — redesign Phase 6 polish (§26–§30) + HANDOFF sync (PR #412, orthogonal track)

`handoffs/V5_Implementation_Redesign_Phase6_Polish_HandoffSync_Coaching30Blocked_2026_06_03_Closing_Handoff_v1.md` — 2026-06-03 (**redesign Phase 6 polish complete (§26 shared empty-state · §27 trail-voice error states · §28 light-mode toggle · §29 a11y roving tab-order) + HANDOFF build-status sync + §30 `coaching_bp` retirement documented BLOCKED — branch `claude/live-tracker-design-handoff-5A9HS`, PR #412 squash-merged to `main`**)

**Redesign track session (orthogonal to the plan-gen go-live board).** Brought `docs/redesign/HANDOFF.md` current with the live tracker (`BUILD_TASKS.md`), then shipped the four remaining Phase 6 polish sections — each a standalone slice on the new `.app` shell with a render test, CSP-clean, Vercel-green: **§27** Flask 404/500 trail-voice error pages via a standalone `templates/_error.html` (no shell includes → a 500 can't cascade; per-request diagnostic + pre-filled support mailto); **§28** real light-mode toggle (topbar + drawer → `app.js`, persisted in `localStorage`, FOUC-free via a nonced `<head>` pre-paint on `<html>`; fixed the dead `body.theme-light .app` selector); **§29** roving tab-order ported from `a11y-wire.js` onto the real sidebar + tab bar (`[data-roving]`, arrow/Home/End, `aria-current` initial stop) + Primary-landmark relocation; **§26** extracted the "You're at the start line." empty state to a shared `templates/_no_plan.html` partial. **§30 `coaching_bp` retirement is BLOCKED** (code-verified, Andy chose defer+document): it runs on the legacy `training_plans` model and backs the already-shipped §06 plan view (chat + AI review), so it can't be deleted until the `training_plans`↔`plan_versions` models unify (same prerequisite as the parked plan-refresh redesign track in `CARRY_FORWARD.md`). **No migration; no owed deploys.** Redesign + auth suites green (59); `app.js` syntax-checks; CSS braces 817/817. The core redesign surface map is now fully on the new shell; only optional polish remains (print stylesheets + operator `base_legacy` forms). **Next (higher priority): the plan-gen go-live board** — re-run the e2e + read the diag endpoint → per-invocation wall-clock backstop (#316/#350) → the still-owed §14 coherence read (#333).

### Predecessor — diag endpoint un-shadowed + compliance issue tree + plan #48 504 triage

`handoffs/V5_Implementation_DiagAuthGateFix_ComplianceIssueTree_Plan48_504Triage_2026_05_31_Closing_Handoff_v1.md` — 2026-05-31 (**diag endpoint un-shadowed (the #349 surface was login-walled in prod and never worked) + compliance/privacy issue tree filed (#353–#395) + plan #48 triaged to a 504 wall-clock timeout — branch `claude/keen-lovelace-eQI41`, PR #352 squash-merged to `main`**)

**Three threads off the post-#349 watch.** (1) **The #349 diag endpoint never worked in prod** — the app-wide `@app.before_request _require_login` gate redirects every non-exempt endpoint to `/auth/login` *before* the route runs, and `admin.plan_diag` was missing from `_AUTH_EXEMPT_ENDPOINTS`, so the global wall shadowed the route's own `DIAG_TOKEN` check; fetching `…/diag?token=…` returned the sign-in page, not JSON. The #349 unit test only covered the pure token helper and the §5.0 route smoke was never run against the wired app, so the gap reached prod. **Fix (PR #352):** exempt `admin.plan_diag` (in-route auth, same pattern as the cron/webhook endpoints); `_diag_authorized` stays the real gate (admin session OR constant-time token, default-deny when unset); the HTML `…/inspect` page stays admin-only. New `tests/test_app_auth_gate.py` exercises the real wired app (wrong token → 403, not a login redirect). **No migration.** (2) **Compliance issue tree:** turned `docs/redesign/00_OVERVIEW.md` (8 controls) + `Privacy_Program_Backlog_v17.md` into two epic trees under a new `area:compliance` label — **#353** (engineering build: 8 control epics #355–#362 + requirement-level stories #363–#387) and **#354** (open non-code items #388–#395). Decisions (Andy): epics+stories hybrid, open items only, single-source cross-linked. (3) **Plan #48 failed again** — Vercel logs show repeated **504 Gateway Timeout** on the generate route + cron, with an in-flight Anthropic call as the last line before each 504: a **single slow LLM call exceeding the 300s function cap**, a hard kill before any `except` runs (so no traceback persists — the diag endpoint's blind spot). This is **#350/#316** (latency / hard-kill backstop), **not** the #47 `ValidationError`. **Next: re-run the e2e and read plan status from the now-live diag endpoint** (`blocks_snapshotted` localizes the stall even with no traceback) → propose a per-invocation wall-clock backstop (#316/#350) → the still-owed §14 coherence read (#333).

### Predecessor — #47 fixed: top-level Layer4Payload invariant violations retryable + log-visibility diag endpoint

`handoffs/V5_Implementation_PlanGen_47_RetryablePayloadValidation_LogVisibility_2026_05_31_Closing_Handoff_v1.md` — 2026-05-31 (**#47 fixed: top-level Layer4Payload invariant violations are now retryable, not a fatal plan-discard; + log-visibility diag endpoint + CLAUDE.md Rule #14 — branch `claude/e2e-test-run-alnUX`, PR #349 squash-merged to `main`**)

**Watched the post-#334 PGE e2e (plan #47): the plan reached race day (3B fix confirmed live — scope 2026-05-31 → 2026-07-17) but generation crashed `failed` with 0 blocks. Root-caused a latent Layer 4 error-handling gap, fixed it, then built the log-visibility surface the triage proved we needed.** The synthesizer over-emitted 3 sessions on one day; `Layer4Payload._check_two_per_day` rejected it (`max 2 sessions per day (got 3)`) — but that pydantic `ValidationError` raised at `_build_payload_for_validation`, which sat **outside** the per-row parse `try/except` in `synthesize_phase`, so it escaped the typed-error contract → the route catch-all discarded the WHOLE plan as "failed unexpectedly" over one bad block. Latent until #334 ran the first cold cone synthesizing fresh blocks (warm-cache pv=46 replayed cached blocks, never re-built a payload from a live fumble). **Fix:** wrap the payload construction, mirroring the parse step — a `ValidationError` now retries in-loop and, after `capped_retries`, raises `Layer4OutputError('schema_violation')` (a `_RETRYABLE_BLOCK_CODES` member), so the block re-synthesizes instead of killing the plan; covers every top-level invariant uniformly. **Log-visibility (Rule #14, option 1):** the triage was blind because the Vercel runtime-log MCP truncates/groups and the admin inspect page is login-walled — so the real fault was unreadable until Andy hand-pasted the traceback. Shipped a durable `plan_versions.generation_traceback` (best-effort persist in `_mark_plan_failed`) + a token-gated `GET /admin/plan/<id>/diag` JSON endpoint readable past the login wall (admin session OR constant-time `DIAG_TOKEN`). **CLAUDE.md Rule #14** codifies the lesson: ask for the log, don't infer around an unreadable one. Suite **1855/16**. **Migration owed + DONE this session (Andy):** `generation_traceback` column applied on Neon; `DIAG_TOKEN` set in Vercel + shared. **Follow-ups: #350** (log-drain hard-kill backstop), **#351** (per-day clamp so the >2/day fumble self-heals without a retry). **Next: re-run the PGE e2e** (now both reach-race-day + resilience are live) and read the result from the diag endpoint → the §14 within-phase coherence read (still owed) → remaining #333 sub-items.

### Predecessor — #334 fixed: event-mode plans reach race day with a 2-week terminal Taper

`handoffs/V5_Implementation_Layer4_ReachRaceDay_TerminalTaper_334_2026_05_31_Closing_Handoff_v1.md` — 2026-05-31 (**#334 fixed: event-mode plans now reach race day with a 2-week terminal Taper — branch `claude/layer4-reach-race-day-334`, PR #346 merged `c3f14b3`; PR #345 (#333 sentinel guard) also merged `e29053c`**)

**Closed the #334 go-live blocker: event-mode plans were stopping ~1 week short of race day and dropping the Taper.** Two compounding bugs: (1) 3B `_time_to_event_weeks` floored (`days // 7`) → a 48-day gap resolved to 6 weeks not 7, dropping the final partial week; (2) `_allocate_weeks_standard`'s `int()` truncation floored a sub-1-week Taper to 0 → it was dropped → `scope_end_date` landed on Peak's end (which *also* silently kills the Decision-4 race-week brief + Taper `coaching_flags`, both of which only attach to an existing Taper). **Fix (spec-first, two amendments then code):** 3B horizon → race-day-inclusive `ceil((days + 1) / 7)` (the `+1` counts race day so an exact-multiple gap doesn't end a day short; PGE 48-day gap → 7 weeks, ends exactly on race day); Layer 4 event-mode terminal phase floored at **2 weeks** (taper wk + race wk; race week stays the final Taper week, no 5th phase), shortfall reclaimed one week at a time from a non-terminal phase (Base first, else largest) without starving any phase below 1 week. **Safety insulation:** the DNF-recurrence HITL trigger (`weeks < window`) was switched to a local conservative **floor** so the new ceil can't inflate the count past the window boundary and suppress a warning that should fire (a 77-day gap is genuinely inside the 12-week `quad_failure` window). PR split to its own #346 (off main); #345 restored to #333-only + merged. **Known residual → #347:** whole-week structure + no `event_date` clamp can overshoot race day by ≤6 days when race day falls mid-week (exact/no-impact for PGE); trimming needs a session-level clamp. Suite **1851/16**; **no migration**. **Cache note:** the changed `time_to_event_weeks` makes the first post-deploy event-mode gen a cold cone (expected). **Next: re-run the PGE e2e (fresh `pv`)** to confirm the plan spans through 2026-07-17 with a 2-week Taper, then the §14 within-phase coherence read (still owed) → remaining #333 sub-items.

### Predecessor — proof run pv=46 reached `ready`; #324 closed; coherence read triaged

`handoffs/V5_Implementation_PlanGen_ProofRun_pv46_Ready_CoherenceReadTriage_2026_05_30_Closing_Handoff_v1.md` — 2026-05-30 (**proof run pv=46 reached `ready` — completion SOLVED; #324 closed; coherence read triaged into the tracker — branch `claude/v5-implementation-plan-mcYEL`; no app code**)

**The six-link chain landed: pv=46 generated end-to-end and reached `ready` — the FIRST PGE 2026 plan to complete in ~15 sessions — and the read of it set the next agenda.** Confirmed #332 was already merged (19:21 UTC) + deployed (`dpl_95uU2…`/`50bd3c7`) — the predecessor's "OPEN/CI-green" was stale — so all six fixes (#325/#327/#329/#330/#331/#332) were live. Watched **pv=46**: cone a cache HIT, Layer 4 blocks mostly cache HITs replayed from pv=45 (rebound to pv=46 by #332), **clean persist, zero errors**, `ready` in ~7 min. The persist guard held; **#324 (completion blocker) is closed `completed`**, Part-B latency carried as #316. **But `ready` ≠ usable:** Andy's coherence read (design §14) is **indeterminate — gated on phase-label visibility (#333)** — and surfaced 17 notes. Research-first triage (swept all 102 open issues): **most "missing from the plan" notes are already-known *static-audit* findings** (orphaned-code epic #295 + Layer-2E nutrition #210/#300 + notes-never-read #306), now **empirically confirmed** by a real plan. **Filed: 10 new issues #333–#342** (go-live blockers #333 render gaps / #334 doesn't reach race day / #335 strength = bare labels / #336 skill-capability not gating → milestone 1), **6 evidence comments** (#316/#306/#300/#289/#298/#301), **closed #324**. The blocker has **moved from "can it complete?" to "is what it produces correct and complete?"** **Next: the go-live board — recommend #333 (unblocks the §14 coherence sign-off) → #334. No code this session; suite unchanged; no migration.**

### Predecessor — plan-gen six-link failure chain: diagnose→decompose→timeout→rebind (pv=41→45)

`handoffs/V5_Implementation_PlanGen_SixLinkFailureChain_DiagnoseDecomposeTimeoutRebind_2026_05_30_Closing_Handoff_v1.md` — 2026-05-30 (**plan-gen six-link failure chain: diagnose→decompose→timeout→rebind — branch `claude/plan-version-rebind-cache-hit`; PRs #329/#330/#331 merged, #332 since merged + deployed**)

**One live-debugging arc that took plan-gen from "fails with a generic stall" to synthesizing a complete, coherent PGE plan — by excavating FOUR distinct blockers in sequence (pv=41→45), each masking the next.** Watched prod runs live via the Vercel logs + `/admin/plan/<id>/inspect`, root-causing each from its actual traceback/`generation_error`. **(1) #329** — pv=41's opaque 15-min stall: instrument the failing attempt (`output_tokens/ceiling/thinking/kind` folded into the `schema_violation` detail; env `PLAN_GEN_LOG_FAILED_ATTEMPT=1`) + persist the real `generation_error` live + **fail fast** when a deterministic fumble repeats with 0 cached blocks (kills #325's silent stall). **(2) Diagnosis via #329** — pv=42 showed the failing call ran at **`ceiling=4000`, NOT 21,600**: the failure was in the **seam-driven re-synthesis** path (`week_range=None` → raw `DEFAULT_MAX_TOKENS`), which re-synthesized a WHOLE phase in one undersized call. THIS is why #327's bump "did nothing" (it sized block mode only) and why runs differed (whether a seam fires is stochastic). Andy's "a token bump can't be the cure" was right. **(3) #330 (D-77 Slice 3)** — decompose seam re-synth into resumable, cache-wired, budget-gated **week-blocks** (21,600 ceiling + seam constraints; disjoint `phase_idx` band `[500,1000)` so the stall counter sees them as progress — no route SQL). **(4) #331** — pv=43/44's untyped `ValueError: Streaming is required`: the Anthropic SDK refuses a non-streaming call when `max_tokens > 21,333`, and the thinking attempt sends `21,600+5,000=26,600`; a **latent landmine** #330 was first to cross. Fix: pin an explicit `timeout` (client + per-request) — disables the guard for every caller. **(5) #332** — pv=45 synthesized 5 blocks + ran the seam re-synth clean through Build w1–w4 (real coherent programming), then crashed at **persist** on a `UniqueViolation` (a pv=39-stamped session in pv=45's plan): the per-block cache is shared across plan versions but the per-block hydrate path never rebound `plan_version_id` like the §9.4 whole-payload path does. Fix: rebind on hydrate (self-heals the poisoned cache on next read) + force the payload's id at persist (safety net). Suite **1845/16**; no migration. [**Superseded next-move:** "merge #332 + proof run" — done as pv=46 (above), which reached `ready` and closed #324.]

### Predecessor — pv=40 verification + per-block `max_tokens` truncation fix (PR #327)

`handoffs/V5_Implementation_PlanGen_PerBlockMaxTokens_Pv40_TruncationFix_2026_05_30_Closing_Handoff_v1.md` — 2026-05-30 (**pv=40 verification run + per-block `max_tokens` truncation fix — branch `claude/plan-generation-handoff-PCfYG`, PR #327 merged**)

**Watched the live #325 verification run (pv=40), caught it FAIL, root-caused it from the prod logs, and shipped the truncation fix.** pv=40 cached **ZERO blocks** and tripped the 900s stall at ~15.5 min. Root cause (`detail`, identical every pass): `model did not emit a record_phase_sessions tool_use block (stop_reason=max_tokens)` — the first block truncated against its output ceiling → `schema_violation` → #325 retried the *deterministic* failure every pass → 0 blocks → stall. Block-mode sizing was `14×900+1200=13,800`, below the 10–16k tokens prod blocks emit; the forced-tool retry's ceiling is `effective_max_tokens` ALONE (no `+thinking_budget`). pv=39 dodged it via its stochastic thinking attempt; pv=40 fell through. **Fix (PR #327):** `_BLOCK_OUTPUT_TOKENS_PER_SESSION` 900→1400 + `_BLOCK_OUTPUT_TOKENS_OVERHEAD` 1200→2000 → 21,600 (~35% over 16k). Suite **1993/16**; no migration. [**Superseded next-move:** "re-verify #327" — done as pv=41→45 this session, which excavated the four further blockers above; #327's ceiling is correct but only applied to block mode, not the seam path (#330 fixed that).]

### Predecessor — canon cleanup + observability + resilience + first PGE verification (pv=39)

`handoffs/V5_Implementation_CanonCleanup_Observability_Resilience_PGEVerify_2026_05_30_Closing_Handoff_v1.md` — 2026-05-30 (**big arc: Layer-0 discipline/sport canon cleanup (#319, deployed — disciplines 25→21, Trekking fold, `endurance_profile` replaces `discipline_category`, Modern Pentathlon/Biathlon removed via a new `sport_canon`); plan-gen observability (#323 — durable `plan_progress_blocks` + admin inspect); Neon/quality merges (#314/#315); version-tag de-footgun (#322); the FIRST end-to-end PGE 2026 verification run (pv=39); and the plan-gen resilience fix (#325). Bookkeeping branch `claude/bookkeeping-2026-05-30`**)

**The discipline/sport data cleanup + observability shipped, and the first real PGE plan generated 7 clean weeks before failing on the 8th — a failure diagnosed entirely from the new observability.** Six PRs merged (#314/#315/#319/#322/#323/#325); Andy ran the one owed migration + ETL (`--version-tag 1.3.1`), so Layer 0 is live on the 21-discipline canon. The pv=39 run **validated** the cleanup/observability/injury work (7 clean weeks; clean disciplines — no Fencing/pentathlon/biathlon; wrist constraint threaded into sessions) and **precisely characterized the go-live blocker** (issue #324): plan-gen is too **fragile** (one unparseable block discarded the whole plan; two passes hit the 800s Vercel cap and 504'd, losing work) and too **slow** (156–249s/block, 10–16k output tokens). Suspected cache-key drift was **ruled out** — the cone is deterministic (every hashed input day-anchored; the repeated 3A logs were benign HITs). **PR #325 fixes the fragility** (a block-fumble — `Layer4OutputError` schema_violation / synthesis_budget_exhausted — now retries on the next pass instead of failing the plan, bounded by the 15-min stall backstop; `_INVOCATION_RESERVE_S` 255→330 so a block finishes before the 800s cap). **The latency half is #316** (the pre-compute periodization grid; Andy's call: pre-compute, not shrink blocks). **No owed deploys** (#323/#325 auto-create/deploy on cold start). [**Superseded next-move:** "verify #325" — done as pv=40, which surfaced the truncation bug fixed above by #327.] Suite **1993/16**.

### Predecessor — plan-gen poller 500 regression fix (PR #312)

`handoffs/V5_Implementation_PlanGen_D77_PollerGeneratingMapping_504GiveUpFix_2026_05_29_Closing_Handoff_v1.md` — 2026-05-29 (**plan-gen poller 500 regression fix — PR #311's two new `'generating'` outcomes were never mapped in the poller route, so every multi-block plan KeyError-500'd to "taking longer than expected"; branch `claude/ecstatic-hamilton-NoWBX`, PR #312**)

**Fixed the regression PR #311 introduced into plan-gen.** PR #311's concurrency guard + per-invocation budget added two `{"status": "generating", ...}` returns to `_advance_plan_generation`, but the poller route `generate_plan` was never taught to handle them — it fell through to `jsonify({"status": "failed", "error": outcome['error']})`, KeyError-ing on the missing `'error'` key → **HTTP 500 on every intermediate poll**. The budget makes `budget_partial_progress` the *normal* path for any plan needing >1 pass, so essentially every real multi-block plan 500'd its way to the client's give-up message ("This is taking longer than expected. Please try again.") even though the cone was healthy and caching blocks. Fix: (1) `generate_plan` now maps `'generating'` → `{"status":"generating"}` (keep-polling); (2) `templates/plan_create/progress.html` replaces the *total*-poll cap (`MAX_ATTEMPTS=20`, far too few now each pass does ~1 week-block) with a *consecutive-transport-failure* cap — a clean response resets it; the server-side 900s wall-clock stall backstop remains the real terminal guard. 2 code/template files + `tests/test_routes_plan_create.py` (+5 `TestGeneratePlanRoute`); 50 passed in that file, 160 combined. **No migration.** **This unblocks the still-owed D-77 convergence re-run** — that gating proof (CARRY_FORWARD owed #1) could never have gone green at the client before, because the poller 500'd on every budget-partial pass. The live code state otherwise matches the predecessor below (D-77 convergence, PR #294/#311, re-run owed).

### Predecessor — D-77 housekeeping (docs→GitHub-issues, 4-tier framework)

`handoffs/V5_Housekeeping_DocsToGitHubIssues_NextStepsFramework_2026_05_28_Closing_Handoff_v1.md` — 2026-05-28 (**bookkeeping / process — no app code: repointed the housekeeping docs at GitHub-issues tracking, pruned the migrated deferred-work sections, consolidated the owed Andy's-hands deploys, and recast "next moves" as the 4-tier priority framework; branch `claude/friendly-darwin-tEKUp`**)

This session changed bookkeeping docs (`CLAUDE.md`, `CURRENT_STATE.md`, `CARRY_FORWARD.md`, `README.md`, `scripts/verify-handoff.sh` + this handoff) and added `scripts/issue-cleanup.sh`; it also professionally reorganized the GitHub issue tracker (label hygiene, backfill, native sub-issue hierarchy under epics, and `Go-live: PGE 2026` / `Post-launch` milestones — see the handoff §11). No app code or specs — the live code state is unchanged from the predecessor below (D-77 convergence, PR #294, re-run owed). **With housekeeping complete, the next move returns to the live-app push: the D-77 go-live blocker (Tier 1).** Backlog / features / bugs / deferred work are tracked in **GitHub issues** (`ahorn885/exercise`); `CARRY_FORWARD.md` keeps only live operational carry-state (owed deploys + the §5.0 walkthrough ledger).

### Predecessor — D-77 cache-key determinism audit + `last_sync` drift fix (last code ship; PR #294)

`handoffs/V5_Implementation_PlanGen_D77_CacheKeyDeterminismAudit_2026_05_27_Closing_Handoff_v1.md` — 2026-05-28 (**cone cache-key determinism audit (#202) + the `last_sync` drift the prod re-run exposed & fixed; branch `claude/zealous-pasteur-LYRuG`, PR #294**)

Audited the 2 un-checked wall-clock reads named for #202 (`layer3a/integration.py:471` `as_of or datetime.now()`; `layer2d/builder.py:567` `datetime.utcnow().date()`) plus a full cone sweep, for the cache-key non-determinism class behind the D-77 convergence money-loop (c4f9160 + #199). **Both are day-granular, NOT the per-pass full-precision class that broke convergence; the three genuine bugs (`layer1.as_of`, `layer2a.generated_at`, `layer2e.computed_at`) were already fixed.** `layer3a` target: the `or datetime.now()` fallback is unreachable in the cone (sole caller `assemble_layer3a_integration_bundle` passes a midnight-anchored `as_of`; `orchestrator.py:320,631`) AND date-truncated even if hit (`anchor` only feeds `_window_cutoff().date()`) — day-anchored it anyway (footgun on a key-feeding path: the integration-bundle hash folds into the 3A cache key `layer3a_athlete_state_key`). `layer2d` target: reaches `layer2d_hash` only for a post-surgical-within-6wk athlete, `.date()`-granular → ≤1 re-key per UTC midnight, self-healing — documented, left as-is (threading the cone clock through the Layer-2D entry is an optional Trigger-#3 follow-on, not done silently). Also: `uuid4` session IDs are frozen by the per-block synthesis cache (no block key contains a session_id) → not a chain-drift source. 1 code (`layer3a/integration.py`) + 1 test file (`tests/test_layer3a_integration.py`, +2 determinism guards); suite **1801/16** (+2). **No migration** (defensive/inert — the touched fallback is unreachable in prod). **The PGE re-run (`pv=35`) then DISPROVED the audit's "no third money-loop":** it 504-looped ~6 min then `synthesis_budget_exhausted`; the `synthesize_phase` logs showed `call_cache_key` differing across passes ONLY in `l3a`+`l3b` (l1/l2a–l2e identical) → 3A/3B re-run every pass → blocks orphaned. Root: `ProviderStatus.last_sync` (raw `MAX(received_at)`, `layer4/context.py:952`) rides in the integration bundle hashed into the 3A cache key; a provider check-in mid-generation drifts it sub-day → 3A miss → temp=1 re-run → l3a drifts → cascades to l3b (3B keys on `layer3a_hash`). The c4f9160/#199 pattern in the one bundle field the audit's fixed-DB guard test never exercised. **Fixed (same PR #294):** day-anchor `last_sync` in `q_layer3A_connected_providers` + HIT/MISS + `integration_bundle_hash` diagnostics (3A/3B cached wrappers) + guard test `test_last_sync_is_day_anchored`; suite **1802/16**. **⚠ Owed (Andy's hands): redeploy + re-run** — expect `llm_layer3a_athlete_state: … HIT … ibundle=<X>` IDENTICAL across passes, per-block `HIT` on later passes, plan reaches `ready` over the cron passes. Still open (NOT convergence blockers): **(B)** `volume_band_below` STILL fires (#293 incomplete — quality flag, best-effort-accepted so it doesn't block; likely D-015 Navigation getting a spurious hours-band + primary under-prescribed — needs a modeling decision); **(C)** ~150s/block latency (`out`≈10k tokens; tolerable, lever is block thinking-budget/size); **(D)** `unknown discipline_category` for D-015/D-008 + `sport_locale_incompatible_D-008_home` data hygiene.

### Predecessor — D-77 volume-band percent→hours unit fix (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanGen_D77_VolumeBandPctToHours_UnitFix_2026_05_27_Closing_Handoff_v1.md` — 2026-05-27 (**volume-band pct→hours unit fix — per-discipline `phase_load` bands were percentages compared/labeled as hours; branch `claude/layer2e-cache-key-determinism-dlKSm`, PR #293**)

The Layer 2A `phase_load` bands are percentage shares (spreadsheet `… Low %` headers, Σ≈100 per sport), but `validator._rule_volume_band` compared the raw percent against session hours and the synthesizer prompt rendered the percent labeled `hr/wk` — firing `volume_band_below` on every discipline + driving an implied ~80h/week target (over-dense weeks + the token/latency blowout; the earlier `max_tokens` truncation was downstream of this). Fix: shared `validator.phase_volume_bands_hours(layer2a, phase, capacity_hours)` converts pct→hours (renormalize the included disciplines' shares × `min(capacity, framework weekly-total-high)`); `weekly_capacity_hours(layer1) = min(available, goal)`; surfaced `Layer2APayload.weekly_total_hours_by_phase` (2nd SELECT against `layer0.phase_load_weekly_totals`, previously 2E-only) onto the payload + `layer4/context.py`; wired into all 4 volume-rule entry points (`per_phase`, `plan_create` final pass, `plan_refresh`, `race_week_brief`). 7 code + 2 test files; suite 1799/16. **No migration** (catalog data was correct; only the code misread it). **(Recorded here this session — the volume-band session shipped its handoff + code but never bumped this pointer; a Rule #10 drift, now reconciled.)**

### Predecessor — D-77 backlog migration to GitHub issues (full record in `handoffs/`)

`handoffs/V5_BacklogMigration_DocToGitHubIssues_2026_05_27_Closing_Handoff_v1.md` — 2026-05-27 (**backlog migrated to GitHub issues — froze the `Project_Backlog_vN` chain under `archive/backlog/`, repointed CLAUDE.md/CURRENT_STATE/CARRY_FORWARD/verify-handoff.sh at issues; branch `claude/backlog-stub-discovery-RdtlW`, PR #292 merged**)

Migrated the entire doc-tracked backlog into **~90 GitHub issues across 12 epics** in `ahorn885/exercise` (every open `D-NN` → its own sub-issue, `[D-NN]` preserved in titles; labelled `layer:*`/`area:*`/`type:*`/`status:*`/`priority:*` + `v1`/`v2`/`icebox`). The 62-version `Project_Backlog_vN.md` chain is frozen under `aidstation-sources/archive/backlog/`. **Backlog / features / bugs are now tracked as issues, not docs.** No app code changed. **Next session starts on epic #201 (D-77 plan-gen convergence); the live-fire first move is #202 — the cone cache-key determinism audit (2 un-audited instances: `layer2d/builder.py:567`, the `layer3a` `as_of` fallback) + the still-owed PGE 2026 re-run verification.** The 12 epics: #201, #210, #211, #212, #225, #228, #241, #246, #259, #261, #262, #286.

### Predecessor — D-77 Layer 2E cache-key determinism + stall-backstop orphan-scoping (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanGen_D77_Layer2ECacheKeyDeterminism_StallBackstopOrphanScoping_2026_05_27_Closing_Handoff_v1.md` — 2026-05-27 (**D-77 non-convergence REAL ROOT CAUSE found in the prod logs + fixed — Layer 2E `computed_at` cache-key non-determinism (PR #199, merged `9012430`) + stall-backstop orphan-scoping (PR #200); branch `claude/review-run-logs-A4JVs`**)

**Finally read the prod Vercel logs (prior 3 sessions diagnosed code-only) and pinned the actual non-convergence root cause — a SECOND instance of the c4f9160 cache-key non-determinism bug that fix missed.** The two failed `plan_version_id=29` runs 504-looped with `max_tokens=9200` live + `schema_violation` GONE (PR #197 worked) but `accepted=True` never appearing. **Bug A (PR #199, merged):** `Layer2EPayload.computed_at` (`layer2e/builder.py:1156`) was stamped full-microsecond `datetime.now()`; the cone rebuilds every resumable pass, and `computed_at` folds into `layer2e_hash` → `plan_create_key` → EVERY `compute_block_cache_key`, so each pass minted different block keys → every cached block orphaned → re-synthesized from scratch each pass → 504-loop re-burning the full extended-thinking synthesis each minute (the ~$50). Same class as c4f9160 (which day-anchored `layer1.as_of` + `layer2a.generated_at`) but it missed 2E (2E runs after 3B, so it poisons Layer 4 keys but not 3A/3B — which is why 3A/3B cached fine and only Layer 4 looped). Fix: day-anchor `computed_at` to the cone's logical `today`. Plus diagnostics: `cached_wrappers.py` logs `call_cache_key` + each component layer-hash per pass (key drift → names the guilty layer in one read); `cache.py` logs per-block cache HIT/MISS. **Bug B (PR #200):** after PR #199 deployed, new plans `pv=30..33` failed IMMEDIATELY — the D-77 stall backstop false-tripped because `_generation_stalled` measured `NOW() - MAX(block.created_at)` scoped to the USER, and `pv=29`'s orphaned rows (>1h old) anchored the 900s window in the past. Fix: bound `_generation_stalled` + `_count_cached_blocks` to `created_at >= pv.created_at` (per-plan progress only). 2 code + 2 test files across the two PRs + `Layer4_Spec §11.5`. Suite **1796 passed / 16 skipped**. **No migration** (query/sampling-level; existing orphan rows just stop mattering). **⚠ Owed (Andy's hands): merge PR #200 + redeploy, then create ONE fresh plan** — it should now get past the gate (Bug B) and CONVERGE (Bug A). Proof in the new logs: `call_cache_key=<X>` IDENTICAL across passes (was drifting); per-block `HIT` on later passes; `<phase>:w<n> done — … accepted=True`; plan reaches `ready`. Residual watch (Trigger #5, data-gated): if the per-attempt `llm call <N>ms … max_tokens=9200` line shows a single dense-week call near 300s, shrink the block / trim thinking budget. Coherence (design §14) still UNVERIFIED until a plan reaches `ready`.

### Predecessor — D-77 block-mode `max_tokens` truncation fix (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanGen_D77_BlockMaxTokens_SchemaViolationFix_2026_05_27_Closing_Handoff_v1.md` — 2026-05-27 (**D-77 block-mode `max_tokens` truncation fix — the cone/truncation watch-item fired; branch `claude/plan-gen-schema-violation-tpu23`**)

**The cone/truncation fix (PR #195) worked — the re-run no longer 504-looped, but failed with a bounded `schema_violation` ("Plan synthesis failed (schema_violation). Adjust your inputs and try again."), exactly the predecessor's §6/§11.5 watch-item.** The per-block budget guard + empty-tool escalation stopped the loop, but the **forced-tool retry runs with thinking OFF**, so `max_tokens` is the *entire* output budget (no `+ thinking_budget` headroom). The per-phase default `max_tokens=4000` predates the dense `PlanSession` schema (per-session `cardio_blocks` + `strength_exercises` + 240-char notes ≈ 400–600 output tokens), so a full high-frequency week (up to 14 sessions, the PGE 6-discipline norm) truncated mid-`sessions` → incomplete tool call → `schema_violation`. **Fix:** block mode (`week_range` set) now sizes the output budget to the unit's session ceiling — `effective_max_tokens = max(max_tokens, max_sessions_this_unit × _BLOCK_OUTPUT_TOKENS_PER_SESSION(600) + _BLOCK_OUTPUT_TOKENS_OVERHEAD(800))` = **9200** for a 1-week block; never below the caller's value. Higher `max_tokens` is only a ceiling (billed/latency cost tracks emitted tokens; the `_PER_BLOCK_BUDGET_MS=120_000` guard still bounds time) and it also gives the *first* thinking attempt more post-thinking output room. Whole-phase seam-resynth (`week_range=None`, ≤56 sessions) is intentionally NOT scaled — that's the Slice-3 stitcher's path, not a token bump. 1 code file (`layer4/per_phase.py`) + 2 tests (`TestBlockOutputBudget`) + `Layer4_Spec §11.5` in place. Suite **1795 passed / 16 skipped** (+2). **No migration** (`max_tokens` is a sampling param, not in any cache key/schema). **⚠ Owed (Andy's hands): redeploy (merge) + re-run the PGE plan.** **NOTE: diagnosis is code-evidence-based — the prod log was not in hand this session** (Andy pasted only the athlete message). The re-run proof is the per-attempt log line `synthesize_phase: <phase>:w<n> … out=N max_tokens=9200` (`out` should land BELOW the ceiling, not pin it) + the `<phase>:w<n> done — … accepted=True` summary firing. If a `schema_violation` still surfaces, send back that line + the route's `_advance_plan_generation: Layer4 … (schema_violation) … : <detail>` (`detail` carries `stop_reason` + cause) — `stop_reason=max_tokens` near 9200 ⇒ raise further / trim thinking budget; anything else ⇒ a different fix. Coherence (design §14) still UNVERIFIED until a plan reaches `ready`.

### Predecessor — D-77 cone cache-key determinism + synthesizer truncation + per-block budget (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanGen_D77_ConeCacheKeyDeterminism_SynthTruncationFix_2026_05_27_Closing_Handoff_v1.md` — 2026-05-27 (**D-77 non-convergence ROOT CAUSE fixed — cone cache-key determinism + synthesizer truncation + per-block budget; branch `claude/plan-gen-timeout-logs-caH8Q`**)

**The instrumented re-run (`plan_version_id=27`) pinned the stall to TWO independent bugs; fixed both.** (1) **Cone cache keys were non-deterministic across function invocations** — `Layer1Payload.as_of` + `Layer2APayload.rationale_metadata.generated_at` were stamped with full-precision `datetime.utcnow()` and hashed into `layer1_hash`/`layer2a_hash`, which fold into EVERY Layer 4 cache key (3A/3B/`compute_block_cache_key`/refresh/single-session/brief). So 3A + 3B re-ran cold on **every** resumable pass (prod logs: the live-run `Layer3A/3BEvidenceBasisWarning` in 17/17 passes, ~150s/pass) and any synthesized block was orphaned next pass — D-77 monotonic convergence was **structurally impossible**, which is why decomposition never helped (this overturns the predecessor's "warm 3A/3B cone" assumption — it had no per-layer cache instrumentation). Fix: day-anchor both timestamps (matches the 3A `as_of` day-granular cache semantics; neither field is consumed — pure provenance). (2) **The synthesizer starved its own output on the thinking budget** — `attempt 1/3 llm call 142528ms (in=6896 out=9000)` then `tool_args keys=[]`: under thinking the ceiling is `max_tokens(4000)+budget(5000)=9000`, the model spent it all thinking and emitted a contentless tool call → no sessions → never cached. Fix: `invoke_tool_call` treats an empty tool call like a missing block → fires the existing forced-tool retry (thinking off → full output budget, can't decline). Plus a structural **per-block budget** (`_PER_BLOCK_BUDGET_MS=120_000`) in `synthesize_phase`: once a block spends its LLM-time budget it won't start another attempt — accepts best-effort (caches → progress) or fails terminally fast (`synthesis_budget_exhausted`), never 504-loops. 4 code files (`layer1/builder.py`, `layer2a/builder.py`, `llm_invocation.py`, `layer4/per_phase.py`) + 13 tests (escalation ×10, hash-determinism ×2, budget-guard ×1) + `Layer4_Spec §11.5` updated in place. Suite **1793 passed / 16 skipped** (+13). **No migration.** **⚠ Owed (Andy's hands): redeploy (merge), then re-run the PGE plan** — expect it to CONVERGE now (3A/3B cache after pass 1 → blocks cache → `ready`). Watch the Vercel log: `synthesize_phase: <phase>:w<n> done — …` should fire (it never has), and the cone should NOT re-run 3A/3B every pass. If the forced-tool retry still truncates a dense ~14-session week against `max_tokens=4000`, the follow-on is bumping block-mode `max_tokens` (the budget guard keeps it failing fast, not looping, meanwhile).

### Predecessor — D-77 per-block budget instrumentation (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanGen_D77_PerBlockBudget_Instrumentation_2026_05_27_Closing_Handoff_v1.md` — 2026-05-27 (**D-77 per-block synthesis-budget finding + diagnostic instrumentation; branch `claude/plan-generation-timeout-8s1Hn`**)

**Found why the re-run STILL stalled, then shipped logging-only instrumentation (Andy's gate: "add logging first").** Andy re-ran the coherence walk on the deployed wall-clock-backstop fix (#189, now under prod #190) and got the same "Plan generation stalled." The prod runtime log for `plan_version_id=25` shows: 3A/3B were warm cache hits (cone NOT the bottleneck), week-1 block synthesis was reached immediately, then **every** generate pass (poller + cron) 504'd with an `api.anthropic.com` call in flight, and the wall-clock backstop tripped at ~960s with **zero blocks cached**. **Root cause:** a single week-block's `synthesize_phase` retry loop (up to `capped_retries+1`=3 extended-thinking calls, ×a forced-tool fallback) is **in-memory / not resumable across function invocations**, and the block caches only after the loop returns — so a block that needs retries exceeds the immovable 300s cap, 504-kills mid-loop, caches nothing, and restarts from attempt 1 next pass: an infinite loop, the SAME failure as the original `plan_version_id=23` incident. D-77 Slice 1 shrank the *cache unit* (phase→week) but never bounded a single block's *synthesis cost*. The backstop is healthy; the unit still doesn't fit. At an `AskUserQuestion` gate Andy chose **"add logging first"** over committing to a fix — so this session is **instrumentation only, no behavior change**: `layer4/per_phase.py` now logs per-attempt LLM latency + the previously-silent per-block validator-rejection rule set (+ parse / no-`sessions` detail + a per-block summary) under a greppable `<phase>:w<week>` tag. `Layer4_Spec §11.5` records the open limitation. Suite **1780 passed / 16 skipped** (unchanged — pure `print`). **No migration. ⚠ Owed (Andy's hands): redeploy (merge), re-run the PGE plan (it will likely stall again — that's expected; the goal is the logs), then send back the `synthesize_phase:` log lines** so next session reads *why* blocks retry and picks the bounding fix (candidates: per-block wall-clock budget → best-effort; lower retry/thinking budget; finer decomposition — all Trigger #5).

### Predecessor — D-77 Slice 2 backstop fix (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanGen_D77_Slice2_BackstopWallClockFix_2026_05_27_Closing_Handoff_v1.md` — 2026-05-27 (**D-77 Slice 2 backstop fix — wall-clock stall gate; branch `claude/gallant-gates-DWmb6`**)

**Fixed the Slice 2 progress backstop, which was false-failing plans ~46s in.** The first real-LLM coherence walk (`plan_version_id=24`) hit "Plan generation stalled," but the Vercel log showed a single in-flight Anthropic call and **no `schema_violation`** — so the Slice 2 backstop was strangling the run, NOT the decomposition. The stall trip was a per-CALL counter (`_STALL_PASS_LIMIT=2`), and both the every-minute cron and the progress poller call `_advance_plan_generation` on the same `generating` row → poller-start ticked it to 1, the cron 43s later to 2, killing the plan before its first ~300s week-block could cache. Fix: a **wall-clock gate** (`_generation_stalled`, `_STALL_WALLCLOCK_S=900`) — trip only when no block has cached for ~3 function budgets, measured on the DB clock (`NOW()` vs. the newest block's `created_at`, else generation start); robust to a 504-killed pass and to concurrent cron/poller calls. Migration-free (existing `created_at`); `generation_stall_passes` retained-but-unused. `Layer4_Spec §11.5` + the `init_db.py` schema comment amended in place. Suite **1780 passed / 16 skipped** (+3). **⚠ Decomposition convergence is still UNVERIFIED** — the backstop never let the first block finish. **Next:** Andy redeploys (merge) + re-runs the coherence walk, which now actually exercises per-week synthesis (does it reach `ready`? do the independently-generated weeks blend within a phase? — design §14).

### Predecessor — D-77 Slices 1+2 (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanGen_PerWeekDecomposition_D77_Slices1_2_2026_05_27_Closing_Handoff_v1.md` — 2026-05-27 (**D-77 Slices 1+2 — per-week decomposition + proactive threading + progress-based backstop; branch `claude/magical-pasteur-nihYk`**)

**Shipped the convergence fix — D-77 Slices 1+2, closing the prod non-convergence incident.** The Pattern A synthesis unit is now a **week-block** (`_BLOCK_WEEKS=1`, tunable), not a whole phase, so every unit fits the immovable 300s Vercel cap and each resumable pass caches ≥1 new block → monotonic convergence for the complex multi-discipline plan (the NORM). **Slice 1:** `_run_pattern_a_engine` nests a per-`(phase, week-block)` loop; `synthesize_phase(week_range=...)` scopes/validates/filters parsed sessions to the block window (so blocks stay disjoint); a global block index `u` is the cache `phase_idx`; the §9.2 chain rolls per block via new `compute_block_cache_key`; coherence is held by proactive prior-block threading (`prior_phase_sessions` → `prior_block_sessions`) + the unchanged phase skeleton + phase-seam tail; blocks aggregate back to a per-phase result. **Slice 2:** a progress-based backstop in `_advance_plan_generation` counts cached block rows at pass start (robust to a 504-killed prior pass) and fails the row loudly after `_STALL_PASS_LIMIT=2` zero-progress passes (never abandons a long plan), backed by 2 additive `plan_versions` columns. `Layer4_Spec.md §5.2/§9.2/§11` amended in place; D-77 backlog row added (Trigger #3). Suite **1777 passed / 16 skipped** (+15: block-key + decomposition + backstop). **Slice 3 (week-seam stitcher) deferred** — its own Trigger #1 prompt pass. **⚠ Owed (Andy's hands):** `python init_db.py` on Neon (2 idempotent nullable-default cols: `generation_units_cached`, `generation_stall_passes`) + redeploy, then the real-LLM coherence walk (do the independently-generated weeks blend? — the proof is the live run, not unit tests, per the design §14 gut check).

### Predecessor — D-77 design (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanGen_PerWeekDecomposition_D77_Design_2026_05_27_Closing_Handoff_v1.md` — 2026-05-27 (**plan-gen non-convergence incident triage + D-77 design — per-week decomposition + week-seam stitcher; branch `claude/great-ride-SVQHu`; PR #187 merged**)

**Triaged a live prod incident, then designed its fix; design + bookkeeping only, no code.** A real expedition-AR plan (`plan_version_id=23`) 504-looped on every cron pass for ~30 min — a single per-phase synthesizer call (`synthesize_phase`, ≤56 sessions, extended thinking) exceeds the immovable 300s Vercel function cap, so it never caches and the resumable cron redoes it forever (the row never reaches a terminal state, so the progress screen spins indefinitely). Andy killed it via Neon SQL (`UPDATE plan_versions SET generation_status='failed' WHERE id=23`); logs confirmed the cron flipped 504→200 (fast no-op) at 01:31 UTC. **Reframe (Andy): the complex multi-discipline expedition plan IS the NORM, not the edge** — so the fix can't be a give-up circuit-breaker or a big-plan special-case; with 300s immovable the only lever is unit size. Shipped the **D-77 design** (`Layer4_PerWeekDecomposition_D77_Design_v1.md`, PR #187): per-week synthesis units (fits budget → monotonic convergence) + proactive prior-block threading + a NEW intra-phase week-seam reviewer (generalizes the phase-level seam reviewer — the "stitcher") + a progress-based backstop (zero-progress pass → fail loudly; never abandon a long plan). Scope = Pattern A (`plan_create` + `plan_refresh` T3 cross-phase). Two decisions ratified at an AskUserQuestion gate (stitch = threading + week-seam reviewer now; block = 1 week, tunable). **Implementation gated on ratifying the design** (Triggers #1/#3/#5); sliced 1) decomposition+threading → 2) progress backstop → 3) week-seam stitcher. No code/schema/tests changed this session (suite unchanged, 1760/16).

### Predecessor (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanCreate_Layer3B_SectionH2_PreviousAttempts_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**§H.2 deployed-shape gap — structured `previous_attempts` (Slice 2 of 2); branch `claude/friendly-bell-OsRrS`**)

**Closed the Layer 3B §H.2 deployed-shape gap entirely — the last starved HITL flag (`3B.dnf_recurrence_risk`) is now production-reachable.** Andy confirmed the Slice-1 scalar goal fields populate in prod ("the new goal fields DID populate"), then picked **Slice 2 — `previous_attempts`**, the pre-agreed second half of "Full §H.2." Layer 3B was *already fully built to consume* `previous_attempts` (the goal block + cache key + `3B.dnf_recurrence_risk` trigger + confidence floor in `layer3b/builder.py`, and the cached wrapper accepts/threads/cache-keys it) — but the deployed row never stored it, so the flag could never fire. **This slice is pure capture, zero consume-side change:** new `race_events.previous_attempts JSONB` column (shape mirrors `race_terrain`) → new `PreviousAttempt` model on `RaceEventPayload` (`outcome` Literal Finished/DNF/DNS + loose `dnf_cause`) → `race_events_repo` (create/update/load/get + JSONB hydrate) → a new repeating sub-form partial `_previous_attempts_editor.html` in the Goal section of both race-event forms → both route parsers (`_parse_previous_attempts`) → orchestrator threads it (as plain dicts) into `llm_layer3b_goal_timeline_viability_cached`. Edits route periodization-grade. **Folded-in fix:** `routes/onboarding._get_target_race_row`'s SELECT was missing all 4 Slice-1 goal columns (Slice 1 only added them to `get_race_event`), so the onboarding goal pre-fill rendered blank + its periodization-diff over-evicted — added them alongside `previous_attempts`. `Athlete_Onboarding_Data_Spec_v6.md §H.2` amended in place (full goal set now captured). 9 substantive code files + spec + 3 test files (same coupled capture surface as Slice 1 — pre-approved). Full suite **1760 passed / 16 skipped** (+14). **⚠ Owed (Andy's hands): `python init_db.py` on Neon (1 idempotent nullable-default column, no backfill) + redeploy via the PR merge**, then smoke-check the "Previous attempts" sub-form + confirm a DNF inside the recovery window surfaces `3B.dnf_recurrence_risk`.

### Earlier predecessor (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanCreate_Layer3B_SectionH2_GoalContextScalars_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**§H.2 deployed-shape gap — scalar goal-context capture (Slice 1 of 2); branch `claude/v5-implementation-blockers-PV5LH`; PR #184**)

**Closed the high-value half of the Layer 3B §H.2 deployed-shape gap — the athlete can now state their actual race goal, and Layer 3B reads it.** 3B's event-mode goal-viability + HITL machinery was designed to read the athlete's goal fields, but the deployed `race_events` row never stored three of them — the cached wrapper hardcoded `goal_outcome="Finish"`, so every athlete was treated as a finisher regardless of ambition (viability skewed optimistic; the `3B.first_time_competitive_goal` HITL flag could never fire). Andy picked **"Full §H.2"** then **"Slice 1 now (4 scalars)"**: captured **`goal_outcome` (Finish/Compete mid-pack/Podium), `first_time_at_distance` (tri-state), `time_goal`, `race_pack_weight_kg` (numeric kg)** end-to-end. The cached wrapper's hardcoded "Finish" now fires only for legacy/uncaptured (NULL) rows. Goal-field edits route to periodization-grade Layer 4 eviction. `Athlete_Onboarding_Data_Spec_v6.md §H.2` amended in place. ~8 substantive code files + spec + 3 test files. Full suite **1746 passed / 16 skipped** (+13). **✅ Neon migration applied + verified 2026-05-26**; **✅ deployed + verified populating in prod (this session's kickoff).** **Slice 2 shipped (above): `previous_attempts`.**

### Earlier predecessor (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanCreate_504Timeout_CronBudget_Iter1SeamCache_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**504-timeout hardening: cron wall-clock budget + iter-1 seam-review cache; branch `claude/504-timeout-investigation-PB5oM`**)

**Investigated the recurring plan-gen 504s and closed the two latent structural traps (Andy: "fix both").** The 504s were never a standing incident — production is healthy (the every-minute `generate-pending` cron returns 200 as a fast no-op when nothing is `generating`); they only surface during active, non-converging generation. The schema_violation chain (#179/#181/#182) was actually coupled to them: a row that never cached a phase kept the cron re-burning the cone every minute. With #182's missing-sessions retry making phases cache, that loop should converge. **Function Max Duration = 300s (Pro) confirmed** — that closes the "single uncached unit > budget" trap for normal plans. Two remaining traps fixed: **(trap #2) the cron advanced up to `_CRON_ADVANCE_BATCH=5` full passes back-to-back in one request with no wall-clock guard** → added `_CRON_WALL_CLOCK_BUDGET_S=240`; the cron stops starting new passes once spent (in-flight pass still commits; unstarted rows resume next fire). **(trap #3) the seam-review tier ran whole + uncached on every resumable pass** → the **iter-1 seam reviews are now cached individually** (`compute_seam_review_cache_key`; stored under the SAME `plan_create`/`plan_refresh` entry_point in a disjoint `phase_idx` namespace `_SEAM_CACHE_PHASE_IDX_BASE=1000 + seam_idx`), so a resumed pass replays them from cache; cold-run cross-seam parallelism preserved (only uncached seams fire the parallel calls); a hit contributes ZERO tokens/latency (§9.6). Iteration-2 (re-synth-driven) reviews stay uncached (rare, mutate phase state) — the remaining low-value gap. **No migration** — the seam rows satisfy the existing `layer4_cache` CHECKs (entry_point label unchanged; `phase_idx>=0` + non-null `phase_name`). 3 substantive code files (`routes/plan_create.py`, `layer4/hashing.py`, `layer4/plan_create.py`) + `Layer4_Spec.md §9.2` note + tests. Full suite **1733 passed / 16 skipped** (+4). **No owed deploy beyond a normal redeploy.**

### Earlier predecessor (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanCreate_Layer4_SchemaViolation_MissingSessions_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**Layer 4 per-phase schema_violation (missing 'sessions' array): retry + instrument + over-emit clamp; branch `claude/plan-generation-schema-error-Sn7cO`; PR #182**)

**The schema_violation moved one layer further down the cone — now `Layer4OutputError`, not 3A.** Andy ran a fresh plan and got the athlete-facing "Plan synthesis failed (schema_violation)" (`routes/plan_create.py:260`, the Layer 4 catch — distinct from the 3A "Athlete evaluation failed" that #181 fixed). Pinned via the #180 Layer4-detail log + Vercel runtime-log full-text elimination (`Layer4OutputError` ✅ / `Layer4InputError` ✗; `tool args missing` ✅ clean / `did not parse` ✗ / `no synthesizer pass` ✗ clean) to `layer4/per_phase.py:1334` — the per-phase synthesizer returned a tool_use block whose `tool_args` carried **no `sessions` array** (the model emits a thin/partial tool call under extended-thinking `tool_choice: auto`). Root cause was a **retry asymmetry**: missing-`sessions` hard-raised on the first miss while the structurally-identical parse-failure path 15 lines below retried up to `capped_retries=2`. Fix (Andy-approved "retry + instrument"): missing-`sessions` now records a `schema_violation` `RuleFailure` and retries within the cap (re-prompting with the miss), raising terminal only after the cap; logs the keys the model DID emit so a recurrence is diagnosable. **Folded in (Andy):** pre-validation `_clamp_sessions_over_emit` trims an over-emitted `sessions` array to the schema ceiling (`_MAX_SESSIONS_PER_PHASE=56`, lock-step with the tool `maxItems`) — the Layer 4 twin of the 3A/3B bounded-collection clamps. Full suite **1729 passed / 16 skipped** (+5). **No migration (code-only); the per_phase prompt body is unchanged so the content-addressed cache key is stable — start a fresh plan (stale `failed` rows stay terminal).** **Deferred to follow-on slices (Andy wants all four, each needs its own scope): #1 `Layer4ShapeInfeasibleError` (4 detection algos + an OPEN routing decision, §10.2/C3), #4 Layer 3C/3D/3.5 HITL gate (stop-and-ask #4, spec-first), #5 L3B §H.2 deployed-shape gap.** See the handoff §6 + `CARRY_FORWARD.md`.

### Earlier predecessor (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanCreate_Layer3A_SchemaViolation_ObservationText_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**Layer 3A schema_violation (notable_observations text) + spec/prompt reconciliation; branch `claude/plan-gen-schema-violation-B2ttn`; PR #181**)

**Pinned + fixed the recurrence of the live "Athlete evaluation failed (schema_violation)" — it was `Layer3AOutputError` on `notable_observations[].text` over-running `Layer3Observation.text`'s `max_length=240`, not weak_links.** Diagnosed via the new #180 Layer3-detail log + the Vercel runtime log (full-text elimination: `Layer3AOutputError` ✅ / `Layer3BOutputError` ✗; `notable_observations` / `string_too_long` / `at most 240 characters` ✅; `weak_links` ✗) — the per-string twin §6.4 of the #179 handoff flagged to watch. Fix: pre-validation `_clamp_observation_text` truncates over-length observation `text` to the 240 cap (word-boundary + ellipsis) in **both 3A and 3B** (shared `Layer3Observation`; wired into 3B's main + retry paths); only `text` is trimmed so HITL gating is unaffected. Added an LLM-facing prompt rule (3A rule 6 / 3B rule 9) to keep observation text <240 chars. **Folded in the owed doc-only reconciliation:** `Layer3_3B_Spec §8.2` + `prompts/Layer3B_v1.md` `max_items` **6→10**; `Layer3_3A_Spec §7` + `prompts/Layer3A_v1.md` rule 9 weak_links **"most-limiting first"**; specs now document the pre-validation clamps (edited **in place** — handoff §7). Full suite **1724 passed / 16 skipped** (+4). **No migration owed (code-only); the prompt-body change shifts the 3A/3B content-addressed cache key, so a fresh plan re-runs both layers.** **Next wall is data, not code:** confirm Andy's profile has `body_weight_kg` + `height_cm` (the 2E gate, a *named* failure). **Then: §6.3 email + in-app notifications.**

### Earlier predecessor (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanCreate_Layer3A_SchemaViolation_WeakLinks_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**Layer 3A schema_violation (weak_links) + bounded-collection hardening; branch `claude/awesome-cray-wiXk4`; PR #179**)

**Pinned + fixed the live "Athlete evaluation failed (schema_violation)" — it was `Layer3AOutputError` on `current_state.weak_links`, not 3B.** Diagnosed via the Vercel runtime log (full-text elimination: `Layer3AOutputError`/`schema_violation`/`current_state`/`weak_links` all match; the 3B + sibling-field tokens don't). Why now: #178 changed the 2A payload → 3A's content-addressed cache key changed → **3A re-ran fresh in prod**, and a 6-discipline athlete draws >5 weak links; the tool-schema `maxItems` is only an API hint, so it failed `max_length=5` on both retries. Fixes (Andy-directed): (1) `_clamp_weak_links` trims to 5 pre-validation + the 3A prompt now orders weak_links **most-limiting first** (principled top-5). (2) Closed the **twin** wall — pre-validation priority clamp `_clamp_notable_observations` (the post-validation `_trim` never ran on the raw over-emit path) + raised the budget cap **6→10** (`_NOTABLE_OBSERVATIONS_MAX`, lock-step with `layer4/context.py` `max_length`). (3) Full `phase_weeks` fix — tightened `_check_periodization_sanity` (custom shape with no weeks at/after `start_phase` → existing fallback-to-standard) + typed the 3 bare `ValueError`s in `phase_structure.py` to `Layer4InputError("periodization_shape_unusable")`. (4) Widened the route error taxonomy to typed-catch Layer 2A–2E / Modality input errors → named failures instead of "failed unexpectedly". Full suite **1720 passed / 16 skipped** (+6). **No migration owed (code-only).** **Owed doc-only follow-on:** reconcile `Layer3_3B_Spec §8.2` (6→10) + `Layer3_3A_Spec §7` (weak_links "most-limiting first") — handoff §6.1. **Next wall is data, not code:** confirm Andy's profile has `body_weight_kg` + `height_cm` (the 2E gate, now a *named* failure). **Then: §6.3 email + in-app notifications.**

### Earlier predecessor (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanCreate_EventModeBlockers_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**event-mode plan-gen blockers; branch `claude/vigilant-gates-uH4qI`; PR #178**)

**Cleared the chain of latent blockers the #177 migration unmasked — plan generation now runs the cone end-to-end.** With the `layer4_cache` constraint fixed, generation ran past the (previously fatal) 3A cache write in prod for the first time, exposing faults in the never-before-run stretch (`3B → 2E → Layer 4 driver → persist`). (1) Live report "Plan generation failed unexpectedly" was `Layer3BInputError("event_mode_missing_goal_outcome")` — the shared full cone calls 3B without a `goal_outcome` (the §H.2 deployed-shape gap), and it's a `ValueError` subclass that escaped the route's `*OutputError`-only catch → catch-all. Fix: back-fill `goal_outcome="Finish"` in event mode (`layer3b/cached_wrapper.py`) + catch `Layer3*InputError` gracefully + add a catch-all traceback (`routes/plan_create.py`). (2) Forward-blocker sweep found + fixed two more near-certain walls: `discipline_weights_invalid` — 2A's `load_weight` was a 0–100 `race_time_pct` midpoint summing to ~100 but Layer 4 `_validate_plan_create_inputs` wants ≈1.0, so **normalized `load_weight` to a 0–1 distribution in 2A** (`layer2a/builder.py`, same-divisor scaling preserves the override flag; 2E is ratio-based so unaffected); and `time_to_event_weeks_mismatch` for future start dates — **anchored 3B's `current_date` on `plan_start_date` for plan_create** (`layer4/orchestrator.py`, new `viability_current_date` kwarg; 3A `as_of`/2E stay on `today`). (3) Moved the success-path persist inside `_advance_plan_generation`'s `try` so a `plan_sessions` failure degrades gracefully, not a raw 500. Full suite **1714 passed / 16 skipped** (+4). **No migration owed (code-only).** **Owed doc-only follow-on:** reconcile `Layer2A_Spec §5.4` ↔ `Layer4_Spec §4.2` scale text + clear the `:90` residual below (handoff §6.1). **Next: §6.3 email + in-app notifications.**

### Earlier predecessor (full record in `handoffs/`)

`handoffs/V5_Implementation_Layer4_CacheEntryPointCheckFix_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**layer4_cache entry_point CHECK drift fix; branch `claude/ecstatic-allen-A6GC8`; PR #177**)

**Fixed a live plan-gen 500 — `layer4_cache_entry_point_check` rejected 3A/3B/NL cache writes.** A Vercel traceback showed `psycopg2.errors.CheckViolation` on the cron path: `cache.VALID_ENTRY_POINTS` (`layer4/cache.py:61`) carries 7 labels (4 Layer-4 + `llm_layer3a_athlete_state`/`llm_layer3b_goal_timeline_viability`/`nl_parser_parse_intent`) but `init_db.py`'s `layer4_cache` CHECK still allowed only the 4 Layer-4 labels — both in the live Neon table and the `CREATE TABLE` DDL. Every 3A/3B/NL cache write raised an uncaught 500, which 500-looped the every-minute `generate-pending` cron + re-rolled the 3A LLM call each pass (re-exposing the intermittent athlete-eval `schema_violation`). Tests missed it (in-memory backend, no constraint). Fix: extended the DDL + appended an idempotent `DROP/ADD CONSTRAINT` repair migration; added a defensive `except Exception` in `_advance_plan_generation` (marks the row `failed` instead of 500-looping the cron); new `TestLayer4CacheEntryPointConstraint` asserts the init_db set == `VALID_ENTRY_POINTS`. Full suite **1710 passed / 16 skipped** (+1). **⚠ Owed (Andy's hands): run `python init_db.py` against Neon (applies the repair migration), then redeploy** — the fix is inert until the live constraint is widened. **Next: §6.3 email + in-app notifications** (and, if `schema_violation` recurs post-migration, the deferred detail-capture logging — handoff §6.1).

### Earlier predecessor (full record in `handoffs/`)

`handoffs/V5_Implementation_Layer4_CronBackgroundGeneration_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**§6.2 cron-driven background plan generation; branch `claude/compassionate-davinci-DgsIp`**)

**§6.2 background generation — plans now finish with the create tab closed.** Factored the body of the progress-screen poller into a shared `routes/plan_create._advance_plan_generation(db, uid, plan_version_id)` (load → terminal short-circuit → one resumable `orchestrate_plan_create` pass → DELETE-guarded persist + flip `ready`, or a typed upstream error → flip `failed`), returning a view-agnostic outcome dict. Both the existing `POST /<id>/generate` poller (faster feedback when the tab's open) and a NEW token-gated `GET /plans/v2/cron/generate-pending` call it. The cron advances up to `_CRON_ADVANCE_BATCH=5` `generation_status='generating'` rows by one pass each, under each row's own `user_id`, committing per row; a Vercel cron (`* * * * *`) drives it as a backstop. Promoted the `CRON_SECRET` Bearer check to a shared `routes.auth.cron_authorized()` (was `_cron_authorized` in `routes/nudges.py`; both cron routes now share it). `routes/plan_create.cron_generate_pending` added to `app.py`'s auth-exempt set; `vercel.json` gains the second cron entry. Full suite **1709 passed / 16 skipped** (+12: +16 new `_advance_plan_generation`/`cron_authorized`/`cron_generate_pending` tests, −4 the now-subsumed `_terminal_status_response` tests). **⚠ Owed (Andy's hands): redeploy** so Vercel registers the new cron (`CRON_SECRET` already set, PR9 — no new env var). **Next: §6.3 email + in-app notifications** (on terminal status, guard double-send).

### Earlier predecessor (full record in `handoffs/`)

`handoffs/V5_Implementation_Layer4_CallerDedupe_SiblingRouteCatches_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**§6.1 Layer 4 caller dedupe + sibling-route Layer3 catches; branch `claude/lucid-heisenberg-8uoUh`**)

**Layer 4 caller dedupe — the REQUIRED §6.1 move for an end-to-end plan.** Migrated all 5 Layer 4 `_default_*_caller`s (`per_phase` / `seam_review` / `single_session` / `plan_refresh` / `race_week_brief`) off their byte-identical inline `messages.create` onto the shared `llm_invocation.invoke_tool_call` (same delegation pattern as 3A/3B in `layer3a/builder.py:139`), mapping `ThinkingToolCallError → Layer4OutputError`. This **distributes the PR #174 forced-tool retry to the synthesizer** — a plan that clears 3A/3B no longer `schema_violation`s at per-phase synthesis when the model declines the tool under extended thinking (it now retries thinking-off + forced tool). `grep -rn "messages.create" layer4/*.py` → none; the request shape + retry live ONLY in `invoke_tool_call`. Also extended the graceful typed-error catch to the two sibling routes that fire 3A/3B through the same cone and still 500'd on a `Layer3*OutputError`: `routes/plan_refresh.py` (broadened the `Layer4*` catch + `layer3:`/`layer4:` failure-log tag) and `routes/ad_hoc_workouts.py` (both single-session catch blocks). New parametrized `test_thinking_miss_falls_back_to_forced` (×5 callers) proves the retry reaches each caller. Full suite **1697 passed / 16 skipped** (+5). Single PR (Andy's pick — the 5 caller edits are identical).

### Older predecessor (full record in `handoffs/`)

`handoffs/V5_Implementation_PlanCreate_AsyncProgress_ToolRetry_Reliability_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**plan-create reliability: async progress screen (PR #173) + 3A/3B forced-tool retry (PR #174); branch `claude/compassionate-meitner-CovIC`**)

**Comprehensive plan-create reliability arc — two live failures fixed, roadmap decided.** Andy ran plan-create end-to-end against the real API for the first time. (1) The "internal server error after a long wait" was a **Vercel function timeout** (the POST ran the whole multi-minute cone synchronously) → fixed by **async resumable stepped generation behind a progress/thinking screen** (PR #173, merged `c91efe0`; new `plan_versions.generation_status`/`generation_error` columns; POST `/new` redirects to a progress screen that polls a resumable `/<id>/generate` worker resuming from `layer4_cache`). (2) The next live run hit `Athlete evaluation failed (schema_violation)` — under extended thinking the tool is offered via `auto` (forced tool is API-incompatible with thinking), so the model can decline it and emit no `tool_use` block → fixed by a **forced-tool retry fallback** in `invoke_tool_call` (PR #174: keep thinking; on a thinking-miss retry once with thinking off + forced tool, which can't be declined; log `stop_reason`). Full suite **1692 passed / 16 skipped**. **Owed actions ✅ done by Andy:** Neon `init_db.py` migration + Vercel Function Max Duration = **300s** (Pro). **Decided roadmap (Andy):** §6.1 **Layer 4 caller dedupe** (REQUIRED next — the 5 synthesizer callers build their request inline + still `schema_violation`; migrating them onto `invoke_tool_call` distributes the retry + does the queued dedupe + sibling-route catches), then §6.2 **cron-driven background generation** (keep generating with the tab closed), then §6.3 **email + in-app notifications**. Full record + mechanically-applicable detail in the handoff above.

### Older predecessors (full record in `handoffs/`)

`handoffs/V5_Implementation_AsyncPlanCreate_ProgressScreen_TimeoutFix_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**async plan-create + progress screen — plan-create timeout 500 fix; PR #173 merged `c91efe0`**)

**Async progress-screen plan-create — fixes the *still-live* plan-create 500 (a serverless timeout, NOT the prior thinking-400).** Andy hit "internal server error again" after a long "sending request" wait + asked for a progress/thinking screen instead of hanging on the create-plan button. Root cause: `routes/plan_create.py` ran the **entire** Layer 3A → 3B → per-phase Layer 4 cone **synchronously** in the POST — many sequential extended-thinking LLM calls (3A + 3B are the only upstream LLM calls; 2A/2B/2C/2D/2E are `q_` queries; then per-phase synthesis × N + parallel seam reviews + validator) = minutes, past Vercel's function timeout. Andy chose **resumable stepped generation** (Hobby 60s cap; scope = progress screen + 500 fix only, dedupe deferred). New flow: POST `/new` allocates the row, sets `generation_status='generating'`, commits, redirects to a **progress screen** (`templates/plan_create/progress.html`, spinner + rotating copy + elapsed timer) that polls **POST `/<id>/generate`** — a resumable worker running one `orchestrate_plan_create` pass that **resumes from `layer4_cache`** (each layer/phase cache row self-commits, so a pass cut at the timeout keeps its progress; the poller re-fires). On completion: `DELETE`-guarded persist + flip `ready` + redirect; on a typed error: flip `failed` + graceful panel. New `plan_versions.generation_status`/`generation_error` columns (+ CHECK) drive it. Full suite **1690 passed / 16 skipped** (`/tmp/venv`; +5). **⚠ TWO owed Andy-hands actions before prod:** (a) `python init_db.py` on Neon (adds the columns); (b) set Vercel **Function Max Duration = 60s** in the dashboard (Hobby default ~10s is too short for one LLM call; `vercel.json` can't set it under the legacy `builds` config). **Residual:** the seam-review tail isn't cached → re-runs on each resume; if a single unit can't fit 60s the poller caps out (needs Pro 300s or a seam-cache follow-on). The **Layer 4 caller dedupe + sibling-route catches** remain the §6.1 follow-on PR. Full record in the handoff above.

### Older predecessors (full record in `handoffs/`)

`handoffs/V5_Implementation_Layer3_ThinkingIncompatibility_500Fix_Dedupe_2026_05_26_Closing_Handoff_v1.md` — 2026-05-26 (**Layer 3A/3B thinking-incompatibility 500 fix + shared `invoke_tool_call` dedupe; PR #172 merged to `main`**)

**Layer 3A/3B extended-thinking 500 fix — the upstream plan-create blocker.** PR #170/#171 fixed the five Layer 4 callers, but `orchestrate_plan_create` → `_upstream_full_cone` fires **Layer 3A then Layer 3B** LLM calls *before* any Layer 4 call, and both `_default_llm_caller`s carried the identical incompatibility (forced `tool_choice` + `temperature != 1` + `max_tokens ≤ budget_tokens`) — **unwrapped**, so the raw `anthropic.BadRequestError` 500'd past the route. Andy chose the **dedupe** + typed-error wrap. New top-level `llm_invocation.py` holds the one authoritative request shape (`invoke_tool_call`); 3A + 3B `_default_llm_caller`s now delegate + map to `Layer3AOutputError`/`Layer3BOutputError`; `routes/plan_create.py` catches both → graceful flash. New `tests/test_layer3_thinking_request.py` (11 tests). Full suite **1685 passed / 16 skipped**. **Partial dedupe** — the 5 Layer 4 callers still hold inline copies; migrating them + extending the catch to the sibling routes is the §6.1 follow-on PR. Full record in the handoff above.

- `V5_Implementation_Layer4_ThinkingMaxTokensBudget_500Fix_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25 (**PR #171**). Layer 4 plan-gen 500 fix — the 3rd thinking incompatibility: with thinking on, `max_tokens` is the combined thinking+output budget so the API requires `max_tokens > budget_tokens`; set `max_tokens = max_tokens + extended_thinking_budget` in all 5 callers. (Flagged the dedupe as strongly indicated — now being acted on.)
- `V5_Implementation_Layer4_SynthesizerThinkingToolChoice_500Fix_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25 (**PR #170**). Layer 4 synthesizer 500 fix (extended thinking × forced tool_choice + temperature != 1): relax to `tool_choice:auto` + `temperature:1.0` when thinking on + wrap `anthropic.APIError` → `Layer4OutputError`; shipped because suites stub the caller so the real request shape was never exercised. Also reconciled the owed FormRefresh A1/A2/Slice-C + layer0 Neon deploys.

`handoffs/V5_Implementation_DisciplineCanon_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25 (**#166 `5ddc0ac` + #167 `ee0756f`, merged to `main`**)

**Discipline canon — single source of truth for discipline ids + names.** A DB audit showed one `discipline_id` carrying up to ten `discipline_name` labels across the layer0 denorm tables (the ETL extracts names *literally* from `Sports_Framework_v11.xlsx`), and the `discipline_display_names.py` overlay had itself drifted into mislabels (e.g. "Alpine Skiing" for the skimo descent leg). **#166** added `etl/layer0/discipline_canon.py` as the one authoritative map (**25 disciplines**) applied at ETL time (source xlsx untouched, same convention as `sport_name_aliases`): merge D-005+D-016→**D-004 "Swimming"**; remove **D-020** (Swimrun→a *sport*) + **D-023** (Ski Transitions); split composite keys (`D-006 + D-007`…) into atomic legs; drop orphans (Portage Running, `D-014 (Ref)`); non-discipline rows (Strength/Mobility/Weekly Total) → `discipline_id=NULL` + new `row_category`; name fixes **D-021 "Uphill Skinning" / D-022 "Alpine Descent"** (reversions kept: D-025 "Fencing", D-029 "Rifle Shooting"). `discipline_display_names.py` is now a thin **re-export** of the canon; a conformance validator guards the load; `migrate_discipline_canon_2026_05_25.sql` adds the column. **#167** fixed the composite split **double-counting `race_time_pct`/`load_weight`** (share → primary leg only; Triathlon 90.5→140.5→**90.5**) and made **Swimrun's run leg road (D-002)**, not the source's trail (D-001), via a sport-scoped override. Both merged to `main`. **✅ Loader re-run applied #167 to Neon (2026-05-25)** — confirmed via spot-checks (Triathlon D-007 Time-Trial Cycling = 0, share on primary leg D-006; Swimrun → D-004 + D-002 road; 25 non-superseded disciplines; `discipline_canon: 385/385 PASS, 0 ERROR`). Full record in the handoff above.

`handoffs/V5_Implementation_SpecNarrativeSweep_R6_PostRenumber_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25 (**#165 `f903d78`, merged**).

**Spec narrative sweep — post-R6 discipline-id prose reconciliation (doc-only).** R6 (#154) renumbered all code/SQL/workbook but left the *prose + test-scenario narrative* in several per-layer specs pre-R6. Reconciled per-occurrence against the `Discipline_ID_Renumber_R6_Design_v1.md` §2 map (name-anchored edits; files were mixed pre/post-R6 so no blanket replace). **5 spec files changed** (`Layer2D` — was entirely pre-R6, both **collapse pairs** merged as data-unions: kayak D-008a/b→D-010 with the two §13.1 risk rows merged + elevated count 5→4 + set 14→13; mtn-run D-022/D-023→D-024; plus all straight renumbers; `Layer2A` §1/§7/§10/§13.1/§13.2 incl. dropping the now-stale hard "15 AR disciplines" count; `Layer2C` toggle-gate mappings → D-012/D-013/D-017 + bike-coverage D-006/D-008; `Layer2B` D-007→D-009; `Layer0_ETL_Spec_v7` §5.2 mirror dropped the retired `navigation_required` param). `Layer3_3B_Spec` checked — no edit (lone `D-001` stable). Decision ids (`D-21`/`D-17`/`2D-6`…) left untouched. The Layer0 ETL spec's other D-008a/b refs are intentional workbook-version history. **No code/test/schema/deploy.** Full record in the handoff above.

- `V5_Implementation_Layer4_NavRetire_WeatherContingency_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25 (**#164, on `origin/main`**). **`navigation_required` retirement + climate-normals weather contingency.** The Layer4 long-session handoff named "promote `navigation_required` to a `race_events` column" as the next pivot; Andy inverted it at the gate. **(A) `navigation_required` retired end-to-end** — removed the unwired `bool | None` input from Layer 2A (`_resolve_conditional` D-015 auto-in/out + `_render_rationale`/`_emit_coaching_flags` nav branches + the public entry), Layer 3B (`builder` prompt §H.2 line + evidence-basis key + driver), and `layer3b/cached_wrapper` (kwarg + cache-key slot). D-015 stays a discipline but is now a plain `*Conditional` → `prompt_required`; the `race_rule_auto_in/out` resolutions + the `conditional_auto_resolved` flag are gone (`conditional_resolution` Literal trimmed to `athlete_opt_in | None`). `_NAV_DISCIPLINE_ID` retained (drives `sleep_deprivation_relevant`). **(B) Weather contingency is now universal + grounded** — new `weather_client.get_expected_conditions` fetches Open-Meteo climate normals (±3-day window × 5 years, no key, best-effort → None on failure) for the race lat/lng + event_date; `race_week_brief` injects an `## Expected conditions` block + the SYSTEM_PROMPT requires a weather contingency anchored to it (or intrinsic reasoning when absent); validator Rule 18 adds `weather` to all 3 `race_format` tuples and the `nav` anchor is removed. 7 code + 3 spec + 4 test files; container suite **1796 passed / 16 skipped** (fresh `/tmp/venv`). **No schema change → no migration/deploy owed.** Full record + decisions in the handoff above.

## Predecessor index (newest first — full record in `handoffs/`)

The most recent carry live state:

- `V5_Implementation_Layer4_LongSessionDay_PlanGenConsumer_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25 (**PR #163, merged**). Layer 4 plan-gen long-session-day consumer: new shared `per_phase._format_daily_windows_schedule(layer1_payload)` derives the long-session day = longest enabled window (ties → earliest listed day) and steers the **primary** discipline's weekly `long_slow_distance` cornerstone there; surfaced a `=== Schedule ===` section into `plan_refresh` T2/T3; `Layer4_Spec.md` §114 records the contract. Soft prompt-steer only; **no migration/deploy owed**.

- `V5_Implementation_FormRefresh_C_ScheduleInference_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25 (on branch `claude/v5-form-refresh-schedule-handoff-rjeik`). FormRefresh **Slice C — schedule simplification (infer long day + rest days)** (feedback item 5). **Dropped the standalone §G Long Session block (available/days/max_hr) and the Preferred Rest Day(s) picker** end-to-end (`_schedule_form.html`, both route parsers/GETs, `athlete.PROFILE_FIELDS` + `LONG_SESSION_MAX_HR_CHOICES`, `layer1/builder` denorm, `Layer1Availability`/`DailyAvailabilityWindow` typed models, `init_db.py` 4× `DROP COLUMN IF EXISTS`). The weekly **long session is now the longest enabled daily window** — primary-window duration ceiling raised **360→720 min** (migrated on deployed tables via a named-CHECK `DO $$` block) — and **rest days are the disabled days**. `doubles_feasible` is the sole surviving §G scalar. Spec: new `Athlete_Onboarding_Data_Spec_v6.md` §G rewrite + `Layer1_Spec.md` §5.4 + D-61 supersession banner. **Owed Neon deploy (Andy's hands):** `python init_db.py` drops the 4 §G columns + bumps the `daily_availability_windows` duration bound to 720 (idempotent; stacks onto the A1/A2 owed run).
- `V5_Implementation_FormRefresh_A2_DropAidStations_RouteLocalesAllTypes_Portage_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25 (on branch `claude/form-refresh-unblocked-ywib4`; not yet merged). Deleted the `aid_stations` count column end-to-end (form + `RaceEventPayload`/`Layer2ETargetEvent` + orchestrator + repo + `DROP COLUMN`) — aid logistics live on `race_route_locales` (`role='aid_station'`); its only consumer, **Layer 2E HITL gate 5 (anaphylaxis × aid), was removed**. Route locales offered on every event type (removed `single_day` gating); portage → Notes helper text. **Owed Neon deploy:** `python init_db.py` drops `aid_stations`.
- `V5_Implementation_FormRefresh_A1_RaceFormatTaxonomy_DurationAxis_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25 (**PR #159 merged; PR #160 runner + bookkeeping**). Collapsed `race_format` to a structural 3-value axis (`single_day`/`continuous_multi_day`/`stage_race`; sport on `framework_sport`) + added the magnitude axis (`estimated_duration_hr` + `primary_metric`) on both race forms. Orchestrator prefers explicit duration over the format-keyed fallback; validator rule 18 re-derived (nav/weather off the format axis). Idempotent enum-remap migration; no `framework_sport` backfill. Also shipped `etl/sources/run_owed_layer0_migrations.sql`. **Owed (Andy's hands):** `python init_db.py` + the layer0 runner + manual UI eyeball.
- `V5_Implementation_InjuryFormRefresh_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25 (**PR #158, merged**). Injury-form refresh (feedback #4+#6): dropped the duplicate Side `<select>` (side now derived from `body_part` prefix); folded `wrist extension`→`above-joint-angle` constraint; NEW `athlete.BODY_PART_CONSTRAINTS` + JS narrowing the visible constraint checkboxes. No data migration.
- `V5_Implementation_Layer2E_SpecSweep_UpstreamSourced_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25. Layer 2E spec sweep (doc-only, in-place amend) reconciling §5.3.3/§5.4.3 narrative with the PR #156 as-built upstream-sourced classification; D-26 `supplement_vocabulary` "hard blocker" claim corrected across §6.1/§12/§14.
- `V5_Implementation_Layer2E_UpstreamSourced_PrimaryMovement_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25 (**PR #156, shipped**). Sourced Layer 2E classification from upstream `layer0.disciplines` (`primary_movement TEXT` + `discipline_category`), deleting the drifted hand-maintained dicts. **⚠ OWED-DEPLOY (Andy's hands):** `etl/sources/migrate_disciplines_add_primary_movement_v1.sql` must run on Neon BEFORE deploy — Layer 2A `SELECT`s `dl.primary_movement` (HARD prerequisite). This is the architect-recommended next forward move.
- `V5_Implementation_D74_D75_Cleanup_VersionSort_PopulateIdempotency_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25. D-75 numeric ETL-version max (in-code, no deploy); D-74 DELETE-by-version idempotency added to 3 `etl/sources/` populate scripts (**owed-deploy** on next Neon run); D-76 opened + resolved (deleted the divergent `migrations/` terrain_gap_rules copy).

Older chain (dated index; topic is in the filename):

- 2026-05-24 — `V5_Implementation_D73_Phase_5_2_Walkthrough_BM1_D008b_RaceCraftAware_…`, `…_BM3_PromptBodyIntegration_…`, `…_BestFitModalityImpl_…`, `…_BestFitModalitySpec_…`, `…_SkillCaptureSurface_…`, `…_BucketC_l_SkillCapabilityToggles_…`, `…_BucketC_g_TerrainEquipmentMerge_…`, `…_BucketC_i_MapboxRequired_…`, `…_BucketE_B2_C1_…`, `…_WaterVocabExpansion_…`, `…_TerrainVocabAuditClosure_…`, `…_RouteLocalesAnchorFlags_…`, `…_ETLTerrainVocabDriftFix_…`
- 2026-05-23 — `…_RouteLocalesValidatorHotfix_PR129Validation_…`, `…_BucketE_TerrainNone_FrameworkSport_…`, `…_BucketB_500sBugfixes_…`
- 2026-05-21 — `…_V1CoachingRetire_…`, `…_RaceLocaleMapbox_…`, `…_PunchList_…`, `…_Layer3_Caching_…`, `…_Layer2C_Telemetry_…`, `…_TriggeredByAdHoc_…`, `…_FreqCaps_…`, `…_LogThis_T1Hook_…`, `…_Dashboard_CTAs_…`, `…_CallerSide_D64_Runtime_…`, `…_CallerSide_D64_NLParser_Prompt_…`, `…_CallerSide_D63_PlanCreate_Routes_…`
- 2026-05-20 — `…_CallerSide_PlanSessions_Substrate_…`, `…_PlanCreate_Orchestrator_…`, `…_PlanRefresh_Orchestrator_…`, `…_SingleSession_Orchestrator_…`, `…_FormRefresh_C_Locale_Loosen_…`, `…_FormRefresh_B_Onboarding_…`, `…_FormRefresh_A_RaceTerrain_…`, `…_Phase_5_1_Orchestrator_…`, `…_Layer4_Step7_SDK_Smoke_…`, `…_Phase_4_Layer3B_Driver_…`, `…_Phase_3_1_Driver_…`, `…_Phase_3_1_Substrate_…`, `…_Doc_Sweep_…`
- Earlier — `V5_Implementation_D73_Phase_2_4_Closing_Handoff_v1.md`, `…_Phase_2_4_Prep_…`, `…_Phase_2_5_…` (Layer 2C/2E vertical slices). Pre-D-73 PR1–PR19 + Layer 0–4 spec/design arc: see `handoffs/` + `archive/backlog/Project_Backlog_v62.md` revision header.

## Current focus

**Current code state (plan-gen COMPLETION is the live fire — the synthesis MECHANISM is now proven end-to-end).** D-77 convergence was proven at pv=39 (7 clean blocks). This session's six-link arc (pv=41→45) then excavated and fixed four further completion blockers: opaque stalls (#329 diagnostics + fast-fail), the seam-path 4000-ceiling truncation (#330 decomposed seam re-synth into resumable week-blocks — D-77 Slice 3), the SDK non-streaming 10-min guard tripping at `max_tokens>21,333` (#331 pinned timeout), and the cross-plan-version cache-hit `UniqueViolation` at persist (**#332, OPEN/CI-green** — rebind `plan_version_id` on hydrate + persist guard). **pv=45 synthesized 5 blocks and ran the seam re-synth cleanly through all four Build weeks — coherent programming — dying only at the final DB write (the #332 bug).** So the engine works; #332 removes the last plumbing gap. **The latency half is still #316** (the pre-compute periodization grid; pv=45 blocks took the full 120s budget). The instrumentation that made all this diagnosable is #323 observability (`/admin/plan/<id>/inspect` + durable `plan_progress_blocks`) plus #329's failed-attempt detail capture.

**Next moves — 4-tier priority order** (finish in-flight → go-live blockers → finish partial → new):

1. **Finish the in-flight task — merge #332 + the PROOF RUN (#324).** Merge #332, redeploy, generate ONE fresh PGE 2026 plan, confirm it reaches `ready` (watch via `/admin/plan/<id>/inspect`). Expect: primary blocks cache (idx 0..N), a seam fires → seam blocks cache in `[500,1000)`, no `ValueError`, no `UniqueViolation`, plan → `ready`. If it stalls on the 120s per-block budget (blocks cache but never finishes) → that's #316 latency, NOT a crash. If a NEW failure surfaces it's now diagnosable (#329 persists the real `generation_error`; the catch-all logs a full traceback) — send back the inspect `generation_error` + traceback. This is the #1 go-live blocker; the proof run decides whether the convergence/completion arc is finally closed.
2. **Resolve go-live / live-functionality blockers.** (a) **#316 — plan-gen latency** (the pre-compute periodization grid; Andy: pre-compute, not shrink). Only urgent if 20–30 min generation is too slow for the timeline; not a "does it work" blocker. (b) **#303 (food_allergies / anaphylaxis → nutrition, SAFETY)** is **PARKED** per Andy (2026-05-30) — de-prioritized. (c) D-77 quality flags: item **(D)** `unknown discipline_category` is **fixed** by #319 (the column is gone; `endurance_profile` curated); item **(B)** `volume_band_below` cap_hit thrash is subsumed by #316; item **(C)** latency = #316.
3. **Finish open-but-not-fully-live functions.** #205 per-block budget (in-progress — verify); #206 real-LLM smoke parity ("Step 7" — the missing safety net); #203 week-seam stitcher (Slice 3 — only meaningful once convergence holds; its own Trigger #1 prompt pass); #210 Layer 2E de-stubs (in-progress) + the orphaned-output wiring under #295 (#296–#302 / #306 / #307 — builders emit, nothing consumes); the plan-comparison feature (completes D-64 Decision #9 — badges built, per-session old-vs-new discarded); #259 plan-ready/failed notifications (poller built; notify + dashboard badge not wired); #208 sibling-route async/resumable treatment; the parked plan-refresh surface redesign (sequences after D-77 — shares the T3 engine; per-concept decisions in the 2026-05-27 handoff §7.2).
4. **New functionality.** #211 Layer 3 HITL gate (3C / 3D / 3.5; children #213 + #214 `Layer4ShapeInfeasibleError`); #246 / #251 OAuth-first onboarding; #228 / #235 catalog `public.*`→`layer0.*` migration + upstream arc; #241 provider integrations & OAuth; #225 testing & tooling; #261 / #262 / #286 icebox (low).

---

*Deferred work, parallel tracks, and the §5.0 walkthrough ledger live in GitHub issues (`ahorn885/exercise`, browse `label:epic`) and `CARRY_FORWARD.md`. Earlier closed arcs — best-fit training re-model; R6 discipline-ID renumber + the two craft collapses (PR #154); D-74/D-75 cleanup; injury-form refresh (PR #158) — have no remaining build slices; full records in `handoffs/`.*

## Layer status

| Layer | Status |
|---|---|
| **0** | DEPLOYED — discipline canon **#167 applied to Neon 2026-05-25** (composite-share fix + Swimrun road leg; confirmed via spot-checks, `discipline_canon: 385/385 PASS`) |
| **1** | 🟢 v1 spec + typed payload + runtime builder shipped 2026-05-19 (D-51 design wave + Phase 1.2 schema arc + Phase 1.3 builder); 🟢 injury_log §B.1/§B.1.1/§B.3 extensions shipped 2026-05-19 (Phase 2.2 paired) |
| **2** | 🟢 All 5 of 5 runtimes shipped — 2A + 2D + 2B + 2E (Phase 2.1 + 2.2 + 2.3 + 2.5, 2026-05-19) + 2C (Phase 2.4-Prep substrate 2026-05-19 + Phase 2.4 builder 2026-05-20); 2E ships vertical-slice (§5.5 supplements + §5.8 heat acclim stubbed pending §I.1 refresh + Plan Management spec); 🟢 **2B per-discipline terrain gaps shipped 2026-05-25** (best-fit re-model Slice 4 — additive `Layer2BPayload.terrain_by_discipline` consuming `RaceTerrainEntry.discipline_id`; flat aggregate unchanged); 🟢 **training-substitution resolver is the sole best-fit node as of 2026-05-25** (re-model Slice 5 shipped `resolve_training_substitution` in `layer2_modality/substitution.py` consuming the 2B blocks → `TrainingSubstitutionPayload`, db-less; **Slice 6 retired the v2 `Layer2ModalityPayload` resolver entirely** — `layer2_modality/resolver.py` + `_MODALITY_OPTIONS_PER_DISCIPLINE` deleted) |
| **3** | 🟢 3A + 3B complete + cached wrappers wired at orchestrator (3A substrate + driver shipped 2026-05-20; 3B driver shipped same-day Phase 4 2026-05-20; 3A + 3B cached wrappers wired into orchestrator at 4 entry points + extended `_EVICTION_POLICY` 2026-05-21 Phase 5.2 Layer 3 caching slice). 3C + 3D + 3.5 designed, not yet implemented |
| **3.5** | Designed; not yet implemented |
| **4** | SPEC COMPLETE §§1-14; Implementation Steps 2 + 3 + 4a-4f + Step 7 SDK smoke + Phase 5.1 (race_week_brief) + Phase 5.2 slices 1–3 (single_session + plan_refresh T1/T2/T3 + plan_create Pattern A) + caller-side substrate (`plan_sessions` + repo) + caller-side D-63/plan-create/D-64 Flask routes (`routes/ad_hoc_workouts.py`, `routes/plan_create.py`, `routes/plan_refresh.py` + NL parser runtime/prompt) + dashboard CTAs + log-this/T1 hook + refresh frequency caps. **All 4 of 4 entry points wired; all 3 of 3 caller-side routes E2E-reachable, dashboard-surfaced, and cost-gated by per-tier caps. Best-fit re-model COMPLETE (Slice 6, 2026-05-25)** — `training_substitution_payload` threaded into race_week_brief + plan_create + plan_refresh (T1/T2/T3) with a `training_substitution_hash` cache slot per key; single_session intentionally carries no best-fit section (no Layer 2B in its cone) |
| **5** | Not yet specced |

## D-73 upstream implementation arc

Multi-session plan in `Upstream_Implementation_Plan_v1.md`. **Arc complete through Phase 5.2** — Phase 1 (Layer 1 design + schema + builder) → Phase 2 (all 5 Layer 2 runtimes) → Phase 3/4 (Layer 3A + 3B substrate + drivers) → Phase 5.1 (race_week_brief orchestrator + form-refresh A/B/C closing Layer2B_Spec §12 Open Items 2B-2/2B-3) → Phase 5.2 (single_session + plan_refresh + plan_create orchestrators; all 4 Layer 4 entry points wired; caller-side routes + dashboard CTAs + log-this/T1 hook + frequency caps). Per-phase detail lives in the dated predecessor index above. Remaining D-73 follow-ons: Phase 2.5 de-stubs (§I.1 structured-supplement form refresh + Plan Management spec). The Phase 4 L3B-P-2 §H.2 deployed-shape gap is **fully closed 2026-05-26** — Slice 1 (scalars: `goal_outcome` / `first_time_at_distance` / `time_goal` / `race_pack_weight_kg`) + Slice 2 (`previous_attempts`, structured JSONB) both captured + threaded into 3B; **`3B.dnf_recurrence_risk` is now production-reachable** (the last starved HITL flag). L3B-P-3 (evidence_basis mode-discriminator warn→hard) is now unblocked.

**Forcing function:** Andy's PGE 2026 (2026-07-17). `race_week_brief` auto-fires 2026-07-03 (days_to_event = 14). ~7 weeks of runway from 2026-05-25.

## Tests

**Green + 16 skipped.** The §H.2 `previous_attempts` capture (Slice 2, 2026-05-26) measured **1760 passed / 16 skipped** in a fresh `/tmp/venv` (+14 over the 1746 Slice-1 baseline: +7 `TestSectionH2PreviousAttempts` in `test_race_events_repo.py`, +7 `TestParsePreviousAttempts` in `test_routes_race_events.py`; the orchestrator threading test was extended in place, not added). The §H.2 scalar goal-context capture (Slice 1, 2026-05-26) measured **1746 passed / 16 skipped** (+13). The §6.2 cron-driven background generation (2026-05-26) measured **1709 passed / 16 skipped** in a fresh `/tmp/venv` (+12 over 1697 — added `TestAdvancePlanGeneration` (8), `TestCronAuthorized` (5), `TestCronGeneratePending` (3) in `test_routes_plan_create.py`; removed the 4 `TestTerminalStatusResponse` cases, now subsumed by the shared `_advance_plan_generation` terminal short-circuit). The Layer 4 caller dedupe (2026-05-26, §6.1) measured **1697 passed / 16 skipped** in a fresh `/tmp/venv` (+5 over 1692 — the new `test_thinking_miss_falls_back_to_forced` ×5 callers in `test_layer4_thinking_request.py`; nothing removed; the 3 pre-existing parametrized shape tests still pass through delegation). The 3A/3B forced-tool retry (2026-05-26, PR #174) measured **1692 passed / 16 skipped**. The async progress-screen plan-create (2026-05-26) measured **1690 passed / 16 skipped** in a fresh `/tmp/venv` (+5 over 1685 — the new `TestTerminalStatusResponse` + `TestMarkPlanFailed` cases in `test_routes_plan_create.py`; nothing removed). The Layer 3A/3B thinking 500-fix dedupe (2026-05-26, PR #172) measured **1685 passed / 16 skipped** (+11). The Layer 4 plan-gen 500 fix (`max_tokens > budget_tokens`; 2026-05-25, PR #171) measured **1674 passed / 16 skipped** in a fresh `/tmp/venv` (no count change — two new assertions added to the existing `test_layer4_thinking_request.py` cases, no new tests). The predecessor synthesizer 500 fix (2026-05-25, PR #170) measured the same **1674 passed / 16 skipped** in a fresh `/tmp/venv` (net **+15** — the new `tests/test_layer4_thinking_request.py` parametrized guard, 3 invariants × 5 callers; the absolute count differs from prior fresh-container runs by environment/version, not lost coverage). `navigation_required` retirement + climate-normals weather contingency (2026-05-25) measured **1796 passed / 16 skipped** in a fresh web container (net delta **+10** over 1786 — new `test_weather_client.py` suite + 2 brief-render cases + 1 validator weather case; the Layer 2A nav tests were rewritten in place, not removed). Layer4 long-session-day consumer (2026-05-25) measured **1786 passed / 16 skipped** in a fresh web container (net test delta **+5** — the new `TestDailyWindowsSchedule` cases in `test_layer4_plan_create.py`; nothing removed). FormRefresh Slice C (2026-05-25) measured **1641 passed / 16 skipped** in a fresh web container (net test delta 0 — one denormalize test renamed + re-pointed at inferred-rest semantics, no add/remove). FormRefresh A2 (2026-05-25) measured the same **1641 passed / 16 skipped** in a fresh web container (throwaway `/tmp/venv`; system python had no pytest/pydantic — `pip install -r requirements.txt pytest` into a venv, system pip hits a `blinker` debian conflict). All 45 suites collect with no errors. **Note the absolute count differs from the prior `1785`** reported after A1 — that number did not reproduce in this fresh-venv container (newer pydantic/flask/pytest); it's a measurement/environment difference, not lost coverage. A2's net change was −4 tests (5 aid_stations/gate-5 tests removed, 1 regression added). **Confirm the canonical-env count on next deploy.** 12 NL parser smoke + 4 Layer 3 SDK smoke = 16 skipped when `ANTHROPIC_API_KEY` is unset. Per-session test deltas live in each handoff's §4. The discipline-canon work (2026-05-25, #166/#167) added `etl/tests/test_discipline_canon.py` — **32 passed** with `tests/test_discipline_display_names.py` (validated against the real `Sports_Framework_v11.xlsx`, no DB); these live in the `etl/tests/` suite, separate from the main app suite (which was not re-run for that work — its only app-side touch is the `discipline_display_names.py` re-export + the `layer2d` doc constant).

---
