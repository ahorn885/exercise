# AIDSTATION — Current State

Single rolling-state pointer. Changes on every shipped session. Long-form session narrative lives in `handoffs/`; rolling cross-session items live in `CARRY_FORWARD.md`.

---

## Last shipped session

`handoffs/V5_Implementation_Layer2E_SpecSweep_UpstreamSourced_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25

Layer 2E spec sweep (own session, doc-only — picked because PR #156's `primary_movement` migration is parked awaiting a Neon run, so this is the unblocked follow-through). Amended `Layer2E_Spec.md` IN PLACE (Layer2B/2C precedent — no version bump) to reconcile narrative with the PR #156 as-built: **§5.3.3** endurance band + protein band and **§5.4.3** sport profile now describe deriving the classification from upstream `discipline_category` / `primary_movement` (plumbed via 2A from `layer0.disciplines`) instead of the deleted hand-maintained `_ENDURANCE_PROFILE` / `_DISCIPLINE_PROFILE_VOTE` dicts; **§3** `IncludedDiscipline` gains the two upstream fields + a param-table note; the §6.2 promotion row's stale "sport profile that itself is hand-coded" clause fixed. Separately corrected the **D-26 `supplement_vocabulary` "hard blocker"** claim across §6.1 / §12 (2E-6) / §14 (×2) — the table shipped FC-1 2026-05-11; the residual §5.5 supplement-integration stub roots in an input-shape gap (free-text `supplement_protocol_notes` vs structured `AthleteSupplementRecord`), closed by the §I.1 form refresh. Deliberately **left** the §3 `TargetEvent` / `Layer1*` aspirational-shape rewrites untouched (CARRY_FORWARD gates those on the §B / §I.1 onboarding form refresh landing). 1 substantive file (`Layer2E_Spec.md`); no code/test change; **no deploy owed.** Rule #9 sweep on entry: clean — all PR #156 code verified on-disk (`_ENDURANCE_PROFILE`/`_DISCIPLINE_PROFILE_VOTE`/`_STRENGTH_DOMINANT_IDS` gone; `_endurance_profile`/`_movement_sport_profile`/`_CATEGORY_ENDURANCE`/`_MOVEMENT_SPORT_PROFILE` present; 2A join + `layer4/context.py` fields + migration file all present).

**Predecessor (PR #156 — shipped; its CURRENT_STATE pointer bump was owed, folded in here):** `handoffs/V5_Implementation_Layer2E_UpstreamSourced_PrimaryMovement_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25. Sourced Layer 2E's §5.3.3 endurance + §5.4.3 sport-profile classification from upstream `layer0.disciplines` (new `primary_movement TEXT` column + `discipline_category`), deleting the drifted hand-maintained dicts. Files: `layer2e/builder.py`, `layer2a/builder.py`, `layer4/context.py`, `etl/layer0/schema.sql`, `etl/layer0/run.py`, `etl/layer0/extractors/sports_framework.py`; SQL `etl/sources/migrate_disciplines_add_primary_movement_v1.sql` (**owed-deploy on Neon — HARD prerequisite: the Layer 2A query SELECTs `dl.primary_movement`, so the migration must run BEFORE merging/deploying #156**). Tests 51 in touched suites / 1340 importable-suite green.

**Predecessor:** `handoffs/V5_Implementation_D74_D75_Cleanup_VersionSort_PopulateIdempotency_2026_05_25_Closing_Handoff_v1.md` — 2026-05-25

D-74 + D-75 cleanup shipped 2026-05-25 (own session). **D-75** — `_q_current_etl_version_set`'s lexical string-`MAX` (which mis-ranks `0A-v11.0` below `0A-v9.0` at a digit-width boundary) replaced by a numeric-component-tuple max (`_max_etl_version` in `layer4/orchestrator.py`); `-rN` revisions order correctly; 6 new `TestMaxEtlVersion` cases; suite **1630 → 1636**; no schema change. **D-74** — a `DELETE FROM layer0.<table> WHERE etl_version='<ver>'` prefix added (inside the existing `BEGIN`) to the 3 versioned-INSERT populate scripts under `etl/sources/` (`populate_terrain_gap_rules.sql`, `populate_skill_capability_toggles.sql`, `populate_discipline_technique_foci.sql`) so re-runs rebuild each version's rows from the file and removed rows can't linger as active orphans; `populate_stimulus_components.sql` excluded (column UPDATEs, no orphan accumulation); **owed-deploy** on the next Neon run (no DATABASE_URL in the build container). New **D-76** opened: the `aidstation-sources/migrations/` terrain_gap_rules copy is a divergent 12-row snapshot sharing etl_version `0C-v2.0-r2` with the live 16-row `etl/sources/` copy — the DELETE prefix was withheld from it (would wipe the live rows). **D-76 RESOLVED same session** — Andy chose delete-the-duplicate; `git rm`'d the stale copy after verifying nothing in the repo applies the `migrations/` dir (`etl/sources/` is the sole canonical path).

D-73 Phase 5.2 — **R6 discipline-ID renumber + two collapses shipped (the deferred dedicated id-change session).** Clean sequential renumber to **D-001..D-029** (suffixes D-004b/D-005a absorbed, the D-027 gap closed) **plus** the two craft collapses — kayak D-008a/b → **D-010 "Kayaking"**, mountain-running D-022/D-023 → **D-024 "Mountain Running"**. Ratified at two AskUserQuestion gates: **scope** = full R6 (over mtn-run-only/hold-kayak, collapse-no-renumber, hold-both/re-scope) and **scheme** = clean-sequential (over minimal-safe in-place, collapses-only). **Key finding put to Andy first:** a surface map proved the Slice-6 handoff's "terrain axis consumed → safe to collapse" premise *incomplete* — both collapse pairs carry disjoint **injury** (`body_parts_at_risk`), **stimulus**, and **skill/conditional-inclusion** (whitewater) data that 2D/L3/2A never derive from the terrain axis; the collapse flattens it with no recovery path. Andy accepted (clean-sequential changes ~24 of 29 ids' meaning; the **whitewater conditional retirement** in `layer2a/builder.py` is the accepted regression). **Mechanism:** authoritative old→new map (`Discipline_ID_Renumber_R6_Design_v1.md` §2) drives a collision-safe single-pass `re.sub` (551 rewrites / 37 `.py`+`.sql` files) + a committed reviewable openpyxl transform (`etl/sources/migrate_discipline_ids_R6.py`) producing **`Sports_Framework_v11.xlsx`** (409 cell rewrites + Discipline Library row collapse) + ETL retarget to v11 / `0A-v11.0`. **Collapse hand-surgery:** stimulus + body_parts_at_risk + technique_foci unioned for the D-010/D-024 survivors; body_parts verification block fixed (count 31→29, baseline deduped); `whitewater_handling` skill toggle retargets to D-010; Layer 2A `_WHITEWATER_DISCIPLINE_ID` + the duration≥20h gate removed (nav conditional D-015 retained); `DISCIPLINE_DISPLAY_NAMES` pruned to 29 survivors; `test_layer2a.py` whitewater tests reworked/deleted; `test_v10_parsers.py`→`test_v11_parsers.py`. **Tests: 1630 passed / 16 skipped** (−1 = deleted whitewater auto-out test; etl/tests 140; zero other regressions; suffix-id grep clean). **DEPLOYED to Neon (PR #154):** v11 ETL re-extracted → `0A-v11.0` (sole active), all 5 populate scripts applied, runtime auto-discovers v11 via `_q_current_etl_version_set` (no manual re-pin — `race_events.etl_version_set` is provenance, not the Layer-0 pin). Validated: active disciplines = 29 ✅, no dup active ids ✅, `discipline_pairing` 325 → 273 (−52 fully collapse-explained: 18→16 header ids + dedup) ✅. **Deploy surfaced + fixed 3 latent bugs** (all on PR #154): version-naive body_parts verify block (scoped to `superseded_at IS NULL`), pairing + substitutes collapse dedup in the extractors (`extract_discipline_pairing_matrix` + `extract_discipline_substitutes` — first-seen-wins + skip self-pairs), and a wrong "re-pin" instruction in the design doc (corrected — runtime auto-discovers). Two Cleanup follow-ons logged: backlog **D-74** (populate-script DELETE-by-version idempotency — a `(TRN-013,NULL)` terrain orphan had to be superseded by hand) + **D-75** (`_q_current_etl_version_set` string-MAX sort). _Predecessor:_ D-73 Phase 5.2 caller-side — **Best-fit re-model Slice 6 shipped: renderer migration onto `TrainingSubstitution` across plan_create / plan_refresh + full v2 retirement — the re-model is COMPLETE**. Slice 6 migrates the plan-gen prompt renderer off the v2 `Layer2ModalityPayload` and retires the entire v2 path, leaving `resolve_training_substitution` as the sole best-fit node. Ratified at a **Trigger #1 gate** (2 AskUserQuestion decisions): **(1)** single_session **drops its modality section entirely** (full retirement) — single_session's cone has no Layer 2B so it structurally cannot produce a `TrainingSubstitution`; its locale `effective_pool` + discipline coverage already in the prompt carry the raw availability signal, and keeping v2 alive for one entry point would preserve the hardcoded `_MODALITY_OPTIONS_PER_DISCIPLINE` dict the re-model set out to kill; **(2)** **full retirement in one session** (ratified ceiling break — ~12 code files + tests). **Concrete delta**: `layer4/per_phase.py` (`_format_modality_recommendations_per_phase` → `_format_training_substitution_per_phase`, compact `=== … ===` idiom; `render_user_prompt` + `synthesize_phase` take `training_substitution_payload`); `layer4/plan_create.py` (param renamed through `_run_pattern_a_engine` → `synthesize_phase`); `layer4/plan_refresh.py` + `_t1`/`_t2`/`_t3` (new `training_substitution_payload` kwarg on all three tier `render_user_prompt`s + the dispatch, rendering via the shared per_phase helper); `layer4/orchestrator.py` (dropped both `resolve_best_fit_modality` calls + the `layer2_modality_payload` cone field + single_session's resolver block; threads `cone.training_substitution_payload` into plan_create + plan_refresh; dropped the v2 thread from race_week_brief); `layer4/hashing.py` (`training_substitution_hash` on `plan_create_key` [swapped for v2 slot] + `plan_refresh_key` [net-new]; removed `layer2_modality_hash` from plan_create/race_week + `layer2_modality_locale_hash` from single_session); `layer4/cached_wrappers.py` (plan_create swap, plan_refresh add, single_session + race_week v2 removal); `layer4/race_week_brief.py` + `layer4/single_session.py` (deleted their `_render_modality_section_*` renderers + params); `layer4/context.py` (deleted `Layer2ModalityPayload` + `ModalityOption`/`ModalityRecommendation`/`ModalityCoachingFlag`); `layer2_modality/` (deleted `resolver.py` entirely — `resolve_best_fit_modality` + `_MODALITY_OPTIONS_PER_DISCIPLINE` + `ClusterLocaleInput` + `ModalityOptionDef`; the surviving `Layer2ModalityInputError`, reused by the substitution resolver, moved into `substitution.py`; `__init__.py` rewritten). **Tests**: deleted `tests/test_layer2_modality.py` (the v2 resolver suite); migrated `tests/test_layer4_hashing.py` (v2 hash-slot tests → `training_substitution_hash`; added plan_refresh slot tests). **Tests**: full suite **1631 passed / 16 skipped** (the −49 vs Slice 5's 1680 = the deleted v2 test file + hashing migration; zero regressions; v2 grep clean repo-wide). Three cache-key shape changes → one-time invalidation per entry point on first deploy (Spec v4 §9 Slice-6 as-built). **Re-model complete** — no parallel v2 path; the only remaining best-fit follow-ons are the deferred craft-family table escape hatch (§14, add only if the LLM over-substitutes) + skill-gating (no skill→terrain map). _Predecessor:_ D-73 Phase 5.2 caller-side — **Best-fit re-model Slice 5 shipped: training-substitution resolver + Layer 4 craft reasoning (race_week_brief entry point)**. Slice 5 is the resolver *re-model* (Spec v4 §5): a NEW `resolve_training_substitution` (`layer2_modality/substitution.py`) that consumes the Slice-4 `Layer2BPayload.terrain_by_discipline` blocks and produces a per-discipline `TrainingSubstitutionPayload` — closest trainable terrain emphasis (ranked `pct × fidelity`) + untrainable-terrain gaps + the craft candidate set the Layer 4 LLM picks from. Ratified at a **Trigger #1 gate** (3 AskUserQuestion decisions): **(1)** full Slice 5 with a ratified ceiling break (over a 5a/5b split or prompt-deferral); **(2)** craft candidates = **all the athlete's owned crafts handed to the LLM**, no family-grouping vocab table (honors R1 — craft similarity is LLM-side — and §14's escape hatch: add a deterministic family table later only if the LLM over-substitutes); **(3)** output **ADDITIVE** alongside the v2 `Layer2ModalityPayload` (Slice 6 migrates renderers + retires the v2 path). **Concrete delta**: `layer4/context.py` (+`TerrainEmphasis`, `TerrainGapRef`, `TrainingSubstitution`, `TrainingSubstitutionFlag`, `TrainingSubstitutionPayload`); NEW `layer2_modality/substitution.py` (`resolve_training_substitution` — **db-less, no extra SQL**, consumes the 2B blocks directly; emphasis ranking, untrainable routing at fidelity floor 0.25, `terrain_low_fidelity` < 0.60, `craft_unavailable` when zero crafts logged) + `__init__.py` export; `layer4/orchestrator.py` (`_collect_athlete_crafts` flattens paddle+bike owned crafts; resolver runs in `_upstream_full_cone`; new `training_substitution_payload` field on `_UpstreamFullCone`; threaded into the brief wrapper); `layer4/hashing.py` (new `training_substitution_hash` slot on `race_week_brief_key`); `layer4/cached_wrappers.py` (brief wrapper accepts+hashes+threads); `layer4/race_week_brief.py` (`_render_training_substitution_section` additive prompt section + driver/prompt threading); tests (NEW `tests/test_layer2_substitution.py` 20 + hashing/orchestrator/brief updates). **Scope**: wired for **race_week_brief only** — the full-cone entry point that already threads the v2 modality payload AND the PGE forcing function. single_session is N/A (no Layer 2B in its cone → no race-terrain breakdown). plan_create / plan_refresh threading folds into Slice 6 (the payload is already on `_UpstreamFullCone`; matches their pre-existing v2-modality threading deferral). **As-built deviations from Spec v4** (documented in §7/§9/§12): db-less signature consuming 2B blocks (not §3's `db` + `race_legs` + `athlete_crafts: dict`); dedicated `TrainingSubstitutionFlag` (not the v2 `ModalityCoachingFlag`, whose Literal is disjoint from §8); `craft_substitution` surfaced LLM-side via the candidate set (no deterministic `discipline_id→craft-token` map under decision 2); §6 skill-gating not deterministically applied (no skill→terrain map). **Tests**: full suite **1680 passed / 16 skipped** (+31 over Slice 4's 1649; zero regressions). Additive `training_substitution_hash` slot → one-time race_week_brief cache invalidation on first deploy (Spec v4 §9; no new eviction helper — derives from the Layer 1 + 2B cone already covered). **Forward move: Slice 6** — migrate the plan-gen renderers onto `TrainingSubstitution`, thread the payload into plan_create + plan_refresh (cache slots on `plan_create_key`/`plan_refresh_key` + the `per_phase` renderer), then retire the v2 `Layer2ModalityPayload` resolver + its 3 renderers. _Predecessor:_ D-73 Phase 5.2 caller-side — **Best-fit re-model Slice 4 shipped: Layer 2B per-discipline terrain gaps**. Slice 4 makes Layer 2B the first node to *consume* the captured `RaceTerrainEntry.discipline_id` (Slice 3 / Bucket E captured it; nothing consumed it until now). Ratified at a **Trigger #3 gate** (3 AskUserQuestion decisions, all picked the recommended option): **(1)** Layer 2B emits per-discipline blocks (the real "consume the field" work) over thread-only; **(2)** the per-row discipline tag stays **OPTIONAL** — `None` = race-wide, folds into every included discipline — NOT required (single-discipline races' race-wide == their one discipline; existing rows are all `None`; this resolves the optional-vs-required question Spec v4 §12 deferred to Slice 4); **(3)** the output is **ADDITIVE** — flat `race_terrain`/`terrain_gaps`/`summary` unchanged (backward-compat; Slice 6 migrates renderers). **Concrete delta**: `layer4/context.py` (+`Layer2BDisciplineBlock`, +`discipline_id` on `RaceTerrainOutput`, +`terrain_by_discipline: list[Layer2BDisciplineBlock] = []` on `Layer2BPayload`; +export in `layer4/__init__.py`); `layer2b/builder.py` (`_build_discipline_blocks` — one block per `included_discipline_id`, subset = tagged ∪ race-wide with tagged-wins dedup, per-block coverage/gaps/summary recomputed reusing the already-computed `gaps_by_target` so **no extra SQL**; flat aggregate untouched); `tests/test_layer2b.py` (+10 in `TestPerDisciplineBlocks` — race-wide fold-in, tagged routing, the same-terrain-two-disciplines pct-collapse fix, tagged-wins, single-discipline-matches-aggregate, orphan-tag exclusion, no-terrain-no-block, empty-terrain, per-block gap, flat-aggregate-unchanged); `Layer2B_Spec.md` (§3 + new §5.6 + §7 + §9 + §10 amended in place, form-refresh-C marker style). **Tests**: full suite **1649 passed / 16 skipped** (+10; this session installed `flask`+`pydantic`+`requirements.txt` in-sandbox so all route-test modules collect — the prior `1616` headline was a partial-collection measurement, clean full-suite baseline 1639 → 1649; zero regressions). Payload is read by the Layer 3B/4 cone; additive `[]` default keeps old cached payloads valid + a one-time downstream invalidation lands on first deploy (Spec v4 §9; no new eviction helper). **Forward move: Slice 5 — resolver re-model + Layer 4 craft reasoning** (consume `terrain_by_discipline`, `pct × fidelity` emphasis ranking, craft candidate set into the Layer 4 prompt, cache-key swap; Trigger #1). _Predecessor:_ D-73 Phase 5.2 caller-side — **Best-fit re-model Slice 2 (vocab) shipped: pure-craft display-name overlay**. Continued from the Spec-v3/Slice-1 predecessor. After the Rule #9 sweep (all predecessor §8 claims verified; one minor drift fixed — the stale `Current focus` block, commit `884f161`), Andy picked the drift fix + Slice 2. Slice 2 ran as a Trigger #2 gate: investigated the real `Sports_Framework_v10.xlsx` (UI label = `sport_discipline_bridge.discipline_name` sourced from Sheet-3 sport-variant labels, e.g. AR's D-005 surfaces as the mislabel "XC Cycling (Road/Gravel)"). Scope evolved across **6 AskUserQuestion gates** to: pure-craft **labels** for all 31 disciplines, **IDs stable**, mechanism = **runtime overlay** (NOT xlsx — code review found *silent* name-coupling in the pairing machinery: Sheet-2 names ↔ Sheet-3 B2B free-text ↔ `extract_pairing_b2b_fallback` name→id, where a rename silently drops pairings; overlay is also immediate + unit-testable + clean-diff). **Concrete delta**: NEW `discipline_display_names.py` (`DISCIPLINE_DISPLAY_NAMES` 31-entry map + `discipline_display_name()` helper, terrain qualifiers stripped, fallback to bridge name) wired into `routes/race_events.py:_disciplines_for_framework_sport` (the shared label choke point for the race-event picker + onboarding via `routes/onboarding.py:973`). Two ID **collapses deferred to Slice 3** (kayak D-008a/b → "Kayaking"; mountain-running D-022/D-023 → "Mountain Running" per Andy "no events do only one or the other") — both flatten differentiated fidelity/skill/stimulus data that the Slice-3 terrain axis must first absorb. **Clean ID renumber deferred** to its own session (IDs are invisible keys; renumber = max churn/min benefit + silent-mismap risk). **Tests**: 1604 → **1616** (+12; NEW `tests/test_discipline_display_names.py` 11 + 1 route fallback test); zero regressions; overlay is runtime so **no Neon re-extract owed**. Forward move: **Slice 3 — data model** (`race_terrain` → discipline-keyed dict + discipline-centric form + the two collapses). _Predecessor:_ `BestFitRemodel_SpecV3_Slice1` — **Best-fit training RE-MODEL opened (`BestFitModality_Spec_v3.md`) + Slice 1 (race-craft-aware scoring REMOVED) + UI quick-wins shipped**. Feedback-driven session. Andy reviewed the 3 accidental BM-1/Impl/Spec handoffs (confirmed a legitimate Spec→Impl→BM-3→BM-1 chain, not duplicates) and delivered a large batch of form feedback. The keystone: the discipline/terrain/race-craft re-think reworks the race-craft mechanism that shipped the day before. **4 decisions ratified at an AskUserQuestion gate**: **R1 = hybrid best-fit** (deterministic terrain proxy reusing Layer 2B `terrain_gap_rules` + LLM-side craft-similarity reasoning in Layer 4 — reverses v1's A2 for the craft axis only, since craft similarity "kayak ≈ packraft" is fuzzy + low-cardinality while terrain proximity is already tabular + tested); **R2 = `race_terrain` → discipline-keyed dict** `{discipline_id: [{terrain_id, pct}]}` (a terrain % only means something within a discipline); **R3 = pure-craft disciplines** + separate terrain (un-bundles D-008a/b; removes the discipline/terrain/race-craft triple-entry); **R4 = remove race-craft-aware scoring** (the race craft is assumed present on race day — nothing to bump). Andy then chose **spec + first impl slice**, with Slice 1 = the removal. **Concrete delta**: NEW `aidstation-sources/BestFitModality_Spec_v3.md` (14-section depth — pure-craft model, discipline-keyed terrain, hybrid algorithm reusing Layer 2B terrain fidelity + Layer-4 craft reasoning weighted by per-discipline race %, coaching flags craft_unavailable/craft_substitution/terrain_untrainable/terrain_low_fidelity, slice sequence §12); **Slice 1 removal (18 files)** stripped the `race_modality_hints` mechanism back to the pre-race-craft resolver — `layer2_modality/resolver.py` (dropped the kwarg + `race_craft_equipment` + the `*1.2` bump + `ModalityOption.race_craft_match`; KEPT the D-008b vocab), `layer4/context.py` + `hashing.py` + `cached_wrappers.py` + `orchestrator.py` + 3 drivers (dropped the field + the `race_modality_hints_hash` cache slot + all threading + the single-session-only target-event fetch), `race_events_repo.py` + `init_db.py` (dropped column read/write; `ADD COLUMN` → `ALTER TABLE race_events DROP COLUMN IF EXISTS race_modality_hints`), `race_events_invalidation.py` (deleted the eviction helper) + `routes/race_events.py` (deleted the hints parser + hints-only `_equipment_choices` wiring), DELETED `templates/_race_modality_hints_editor.html` + its include, and removed the 26 race-craft tests across 4 test files; **KEPT** the D-008b paddling vocab, the K3 equipment ETL, and the `a5c33db` loader/rollback hardening. **UI quick-wins (3 templates)**: race-event form (dropped the `D-XXX` discipline prefix, "Route locales" → "Route details", removed the aid-station equipment Quantity input), log-injury (removed the injury-type + severity explainers), profile (removed the "API access" tab). **Tests**: full suite **1604 passed / 16 skipped** (was 1630; 26 race-craft tests removed; zero regressions); residual `race_modality_hints`/`race_craft` grep clean except the intended DROP COLUMN migration string; Jinja parse OK. **Forward-pointers**: re-model Slices 2-6 (Slice 2 vocab = pure-craft disciplines + curated display names fixing the Sheet-3 "Hiking (weighted)"/"XC Cycling" labels + AR bridge coverage → Slice 3 discipline-keyed terrain + discipline-centric form → Slice 4 Layer 2B per-discipline gaps → Slice 5 resolver + Layer 4 craft reasoning → Slice 6 renderers); K3 equipment ETL run owed on Neon; deferred form feedback (format vs framework_sport + distance-or-duration; drop top Aid-stations count + derive cadence from route locales; mandatory-gear → pack-weight/portage; injury Side-field drop + "wrist extension"→"extension" → fold into #6+#4 injury refresh; schedule simplification). **Diagnosis logged**: the discipline-name issue is that `sport_discipline_bridge.discipline_name` is pulled verbatim from Sheet 3 (sport-specific variant names) of `Sports_Framework_v10.xlsx` with no curated `display_name` column — Slice 2 owns the fix.

**Predecessor:** `handoffs/V5_Implementation_D73_Phase_5_2_Walkthrough_BM1_D008b_RaceCraftAware_2026_05_24_Closing_Handoff_v1.md` — 2026-05-24

D-73 Phase 5.2 caller-side — **BM-1 D-008b Outdoor Paddling vocab populated + race-craft-aware scoring shipped (Path A; resolver new `race_modality_hints` kwarg + *1.2 multiplicative bump + new race_events JSONB column + add-row builder form-edit UI + new full-cone-ish cache invalidation policy + K3 ETL canonical equipment additions)**. Andy picked BM-1 D-008b at the recommendations gate over Path D static-scoring-with-gap; sharp design catch surfaced that static `base_preference_score` is the wrong abstraction for race-craft-specific disciplines (packraft-vs-kayak depends on what the target race specifies; D-006 cycling has the same gap latently). Per Trigger #3 (cross-layer surface — new race_events column + resolver kwarg) + Trigger #5 (architectural alternatives) plan-mode gate, 3 nested decisions ratified at AskUserQuestion gates: **Score math = multiplicative *1.2** (over additive +15 / tier-promotion to 95+) — preserves intra-discipline relative ordering across base-score bands without additive blow-past; cap at 100. **Form UX = add-row builder** (over per-discipline checkboxes / no-form-this-slice) — mirrors `_race_terrain_editor.html` pattern. **Lint scope = add missing names to canonical 0B** (over static-set-in-test-file / skip-lint) — half of BM-5's canonicalisation work done inline. Sequencing 3 ratified at ceiling-break gate (~18 files vs ~12 precedent). **Concrete delta**: NEW `aidstation-sources/BestFitModality_Spec_v2.md` (~350 lines, 15 sections — §A function signature delta with `race_modality_hints: dict[str, list[str]] | None = None` kwarg; §B scoring math `min(100, int(round(base * 1.2)))` with set-intersection match condition; §C race_events.race_modality_hints JSONB source-of-truth + add-row builder form-edit; §D RaceEventPayload + orchestrator wire across 3 entry points; §E cache-key extension with `race_modality_hints_hash` slot + None → '' forward-compat collapse mirroring BM-3 pattern; §F ModalityOption.race_craft_match=False default for v1 payload-shape compat; §G eviction policy = 3 entry points (plan_create + single_session + race_week_brief; plan_refresh excluded per BM-3 G1=A3); §H D-008b 6-option vocab S2 pick spec'd verbatim; §I equipment canonicalisation closing BM-5 partially with 10 new K3 entries; §J 5 new test scenarios §13.7-§13.11; §K perf budget unchanged; §L 8 edge cases; §M 8 open items v2; §N backward-compat note; §O gut check); MOD `layer2_modality/resolver.py` (+~100 lines: D-008b 6-option vocab block — outdoor_paddle_packraft/kayak/sup @ TRN-009/010 + outdoor_whitewater_packraft/kayak @ TRN-011/017 gated by whitewater_handling + pool_paddle_drill @ TRN-008; `_resolve_menu_for_pair` extended with `race_craft_equipment: set[str]` kwarg + multiplicative scoring + race_craft_match field; `resolve_best_fit_modality` extended with `race_modality_hints: dict[str, list[str]] | None = None` kwarg + per-discipline hint dict access); MOD `layer4/context.py` (+13 lines: ModalityOption gains race_craft_match: bool = False; RaceEventPayload gains race_modality_hints: dict[str, list[str]] = Field(default_factory=dict)); MOD `layer4/hashing.py` (+18 lines: 3 cache-key helpers — plan_create_key + single_session_synthesize_key + race_week_brief_key — gained optional `race_modality_hints_hash: str | None = None` kwarg with None → '' forward-compat collapse mirroring the BM-3 modality-hash pattern); MOD `layer4/cached_wrappers.py` (+45 lines: 3 cached wrappers — single_session + plan_create + race_week_brief — gained `race_modality_hints: dict[str, list[str]] | None = None` kwarg + `compute_payload_hash(race_modality_hints) if race_modality_hints else None` + key wire + thread to driver via `_synthesize()` closure or kwargs dict); MOD `layer4/orchestrator.py` (+~25 lines: `_upstream_full_cone` pulls `target_race_event.race_modality_hints` and threads to `resolve_best_fit_modality` + the cached_wrappers in orchestrate_race_week_brief + orchestrate_plan_create; orchestrate_single_session_synthesize newly loads `load_target_race_event_payload` for hint access + threads to single-session resolver call + cached wrapper); MOD `layer4/single_session.py` + `layer4/plan_create.py` + `layer4/race_week_brief.py` (signature-only delta: each driver gained `race_modality_hints: dict[str, list[str]] | None = None` kwarg for signature symmetry with cached wrapper's `_synthesize` closure — resolver-side scoring already applied upstream of these drivers, so they accept-but-don't-consume the dict; ~5 lines per file); MOD `race_events_repo.py` (+~25 lines: load_race_event_payload SELECT extended to include `re.race_modality_hints` + JSONB tolerance for dual psycopg2/sqlite-shim shape + validation+coercion to `dict[str, list[str]]` shape with silent-drop on malformed entries; update_race_event new optional `race_modality_hints: dict[str, list[str]] | None = None` kwarg + UPDATE SET clause extended with `race_modality_hints = ?::jsonb`); MOD `init_db.py` (+9 lines: `_PG_MIGRATIONS` extended with `ALTER TABLE race_events ADD COLUMN IF NOT EXISTS race_modality_hints JSONB NOT NULL DEFAULT '{}'::jsonb` idempotent migration); MOD `race_events_invalidation.py` (+33 lines: NEW `evict_on_target_event_modality_hints_change(db, user_id, *, cache=None)` helper firing field-level eviction across 3 entry points — plan_create + single_session_synthesize + race_week_brief — via 3 `cache.invalidate_entry_point(layer='race_events_modality_hints')` calls; plan_refresh intentionally excluded per BM-3 G1=A3 deferral); MOD `routes/race_events.py` (+~90 lines: NEW `_parse_race_modality_hints(form) -> dict[str, list[str]]` parser following `_parse_race_terrain` shape with `race_modality_hints[N][discipline_id]` + `race_modality_hints[N][equipment_name]` form-key regex + duplicate dedup + per-discipline equipment-list collapse + silent-drop on invalid discipline-id pattern or empty cells; NEW `_equipment_choices(db) -> list[str]` returning canonical_name set from layer0.equipment_items for the per-row equipment <select> with graceful empty-list fallback when layer0 schema absent; `_DISCIPLINE_ID_PATTERN = re.compile(r'^D-\\d{3}[a-z]?$')` regex added; edit_race GET extended to thread `equipment_choices` + flattened `existing_modality_hints` list-of-dicts into the template render; update_race POST parses + threads to update_race_event(race_modality_hints=...) + adds modality_hints_changed check to the field-change eviction chain rung between periodization and brief-only — fires `evict_on_target_event_modality_hints_change` when hints flip; new_race GET passes empty modality_hints + equipment_choices); NEW `templates/_race_modality_hints_editor.html` (~120 lines: add-row builder partial mirroring `_race_terrain_editor.html` pattern — per-row discipline_id <select> + equipment_name <select> + Remove button + Add craft button + hidden template row for JS clone-on-click + nonce-scoped IIFE for CSP compliance); MOD `templates/profile/race_event_edit.html` (+5 lines: include `_race_modality_hints_editor.html` between the terrain editor and notes textarea); NEW `etl/sources/populate_equipment_items_K3_additions.sql` (~110 lines: 10 new canonical 0B entries — Treadmill / Road bike / Rope / Quickdraws / Harness / Crash pad / Hangboard / Climbing gym membership / Kayak / Canoe — with idempotent ON CONFLICT guard + DO $$ verification block; closes 10 of ~12 equipment-naming gaps in `_MODALITY_OPTIONS_PER_DISCIPLINE` per spec v2 §I); MOD `tests/test_layer2_modality.py` (+~280 lines: NEW `_KNOWN_EQUIPMENT` set covering canonical 0B K2 + K3 names; NEW `TestStaticLint.test_every_required_equipment_is_canonical` extending L2 lint to equipment names; `test_spec_representative_disciplines_present` extended to lock D-008b alongside D-001/D-006/D-010; NEW 6 test classes covering spec v2 §J — `TestScenario13_7_RaceCraftBumpAtAndysPGE` 3 tests / `TestScenario13_8_HintWithUnknownEquipment` 1 / `TestScenario13_9_HintForAbsentDiscipline` 1 / `TestScenario13_10_WhitewaterHintWithToggleOn` 1 / `TestScenarioD008b_BaseShape` 2 / `TestScenarioD008bScoreCap` 1 covering the 84+ × 1.2 → 100 cap edge case via D-010 lead climb at 90 base / `TestRaceCraftHintBackwardCompat` 4 covering None / empty-dict / empty-list / no-equipment-option-exempt cases); MOD `tests/test_layer4_hashing.py` (+~70 lines: 6 new tests covering race_modality_hints_hash None-collapse + populated-hash distinguish for all 3 keys + parametrized mutation coverage extended with the new slot). **18 substantive files** (ceiling break ratified at Sequencing 3 gate; precedent BucketC_l=12, BucketC_i=12, SkillCaptureSurface=12, BM-3=9, BestFitModalityImpl=7 — this slice intentionally larger because Path A is a Trigger #3 cross-layer surface change + Trigger #5 architectural alternative requiring spec + plumbing + form + ETL in one batched ship). **Bookkeeping (3 files; do not count against ceiling per CLAUDE.md)**: MOD `aidstation-sources/CURRENT_STATE.md` (this entry); MOD `aidstation-sources/CARRY_FORWARD.md` (BM-1 D-008b forward-pointer flipped to ✅ Shipped + NEW BM-7-D006Backfill / BM-7-Render / BM-8-Inferred forward-pointers added; §5.0 walkthrough scenario count flipped 110 → 115 with NEW BM-1+race-craft §5.0 added); NEW `aidstation-sources/handoffs/V5_Implementation_D73_Phase_5_2_Walkthrough_BM1_D008b_RaceCraftAware_2026_05_24_Closing_Handoff_v1.md` (this handoff). **Code compiles**: all 14 modified Python files pass `python3 -m py_compile`; the new SQL + HTML files are inert at compile time. **D-decisions ratified**: Path A (resolver kwarg + race_events JSONB column) over Path B LLM-side / Path C defer-D-008b / Path D static-scores-flag-gap; Score math multiplicative *1.2; Form UX add-row builder; Lint scope add canonical 0B; Sequencing 3 (single batched ship). **Forward-pointers**: (a) **NEW M-1 D-006 cycling scoring backfill** — same gap latent for D-006; backfill lowers static base scores to neutral + relies on race_modality_hints to differentiate per-race (pairs naturally with D-006 follow-on); (b) **NEW M-2 renderer transparency** — 3 prompt renderers cite `[race-craft match]` tag on `ModalityOption.race_craft_match=True` options; trivial 3-file slice deferred to keep v2 batched ship at ~18 files; (c) **NEW M-3 equipment-name fuzzy match** carried — race_modality_hints today does exact-string-match (canonical 0B dropdown mitigates user-side drift; BM-5 still owns full resolver-vs-canonical sweep); (d) **NEW M-4 multi-discipline hint inference** — orchestrator could surface hints from framework_sport + included_discipline_ids automatically; today 100% athlete-edited; (e) **M-5 carried** BM-1 vocab for D-005/D-007/D-008a/D-011/D-014/D-015/D-016/D-020 still queued; (f) **M-6 carried** BM-2 phase-aware ranking actionable post-BM-3; (g) **M-7 carried** multi-locale cluster ingestion; (h) **M-8 carried** plan_refresh tier modality wire; (i) #8 locales→locations rename remains lowest-risk mechanical; (j) NEW BM-1+race-craft Manual §5.0 walkthrough added (5 steps: navigate race-event edit form → add `(D-008b, Packraft)` row → save → confirm `race_events.race_modality_hints` JSONB landed + the 3 entry-point caches evicted; rerun race-week brief → confirm D-008b menu shows packraft top at score=96 with race_craft_match=True + kayak/SUP at base 75/65; rerun single-session at home locale with sport=Adventure Racing → same bump; change hint to `(D-008b, Kayak)` → confirm cache miss → confirm kayak option now bumps above packraft; backward-compat — pre-v2 cache entries collapse None→''); §5.0 scenario count 110 → 115; (k) Backward-compat for next deploy: all changes additive at runtime contract layer; resolver `race_modality_hints` defaults None → v1-identical behavior; ModalityOption.race_craft_match defaults False → v1 payload shape; race_events.race_modality_hints column defaults '{}'::jsonb → pre-v2 rows behave as no hints; cache keys forward-compatible via None → '' collapse; first POST-v2 deploy sees fresh keys for orchestrator calls that supply non-empty hint dicts + one round of re-synthesis settling on new keys; resolver-side scoring is deterministic so cache miss → identical result for identical (hints, terrain, equipment, skills).

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_BM3_PromptBodyIntegration_2026_05_24_Closing_Handoff_v1.md` — 2026-05-24

D-73 Phase 5.2 caller-side — **BM-3 Layer 4 prompt-body integration shipped (full wire: single_session + per_phase + race_week_brief; per-renderer-native copy; all 3 cache keys extended with modality hash)**. Andy picked BM-3 at the recommendations gate over per-discipline vocab population (BM-1) + BM-5 equipment canonicalisation + smaller mechanical slices because without BM-3 the impl slice was dormant — `Layer2ModalityPayload` was flowing through the cone but no prompt was reading it. Per Trigger #1 (LLM prompt design) + Trigger #5 (architectural alternatives) plan-mode gate, 4 nested decisions ratified at AskUserQuestion gates: **G1 = A3 full wire (single_session + per_phase + race_week_brief)** (over A1 single_session only + A2 + per_phase + A4 no wire) — activates all 3 cone-consuming entry points; plan_refresh tier renderers (T1/T2/T3-intra-phase) explicitly excluded since they consume bundled payloads without 2C and adding modality there would be a divergent contract; seam_review N/A since it doesn't see locale. **G2 (R) = R2 top-pick + ranked menu + coaching flags + guidance line** (over R1 top-pick only + R3 flags only) — preserves the spec's menu-not-decision pattern; LLM sees full option space and applies phase/race context; guidance line tells the LLM not to cite `modality_id` strings in athlete-facing copy. **G2 (F) = F2 per-renderer-native copy** (over F1 shared render helper) — each renderer gets its own conversion + formatting matching its convention (`#` markdown for single_session; `=== Section ===` for per_phase; `#` + `**bold:**` for race_week_brief). **G3 = C2 all 3 cache keys** (over C1 single_session only) — safe across BM-1 vocab follow-ons: when `_MODALITY_OPTIONS_PER_DISCIPLINE` changes in a code deploy, PostgresCacheBackend entries for all 3 entry points invalidate cleanly; closes DI4 from the impl slice + closes the vocab-addition staleness gap before it ships. **G4 = ~8-10 file ceiling break ratified** (over Tighten-to-5 cuts that would have contradicted G1/G2/G3) — final 9 substantive files; well below 12-file precedents. **Concrete delta**: MOD `layer4/hashing.py` (+30 lines: 3 cache-key helpers gained optional `layer2_modality_hash` / `layer2_modality_locale_hash` kwargs with None → '' forward-compat collapse so pre-BM-3 callers + default-None callers produce stable keys identical to pre-this-slice); MOD `layer4/cached_wrappers.py` (+35 lines: single_session computes `compute_payload_hash(layer2_modality_payload_for_locale)` from the existing impl-slice kwarg and wires into single_session_synthesize_key; plan_create_cached + race_week_brief_cached gain NEW `layer2_modality_payload: Layer2ModalityPayload | None = None` kwarg + hash + key wire + thread to driver via `_synthesize()` closure; impl-slice "intentionally NOT extended" comments replaced with BM-3 activation comments); MOD `layer4/orchestrator.py` (+4 lines: 2 cached-wrapper call sites in orchestrate_race_week_brief + orchestrate_plan_create pass `layer2_modality_payload=cone.layer2_modality_payload`); MOD `layer4/single_session.py` (+75 lines: NEW `_render_modality_section_single_session(payload)` helper with `#` / `##` markdown convention + `### D-XXX Discipline Name at Locale` per-recommendation subsection blocks + dedicated `## Coaching flags` subsection + guidance line on modality_id citation discipline + empty-payload placeholder line; `_render_user_prompt` signature gains `layer2_modality_payload_for_locale: Layer2ModalityPayload | None = None` kwarg; section splices after `# Equipment` / before `# Recent training context`; `llm_layer4_single_session_synthesize` driver threads the existing kwarg into the renderer call — DI4 follow-on complete); MOD `layer4/per_phase.py` (+70 lines: NEW `_format_modality_recommendations_per_phase(payload)` helper with `=== Best-fit modality menu (per discipline, per locale) ===` header + tight one-line-per-recommendation format `D-001 Trail Running @ home: top=outdoor_trail_run (90); alts=outdoor_road_run (60); rationale=...` + flag sub-list + phase-aware guidance line — Peak / Taper bias hint lives in per_phase only per DI2 since this is the surface where phase context is structurally available; `render_user_prompt` + `synthesize_phase` signatures gain matching kwarg with default None; section splices after `=== Race + locale + equipment ===` / before `=== Schedule ===`); MOD `layer4/race_week_brief.py` (+75 lines: NEW `_render_modality_section_race_week_brief(payload)` helper with `# Heading` + `**Purpose:** / **Recommendations:** / **Modality coaching flags:**` bold sub-labels matching brief's mixed markdown idiom; per-recommendation one-line entries `- D-001 ... @ home — top: outdoor_trail_run (rationale: ...); alts: outdoor_road_run, treadmill_run`; `_render_user_prompt` + `llm_layer4_race_week_brief` signatures gain matching kwarg with default None; section splices before `# Race-day fueling tier (2E)`); MOD `layer4/plan_create.py` (+5 lines: `_run_pattern_a_engine` + `llm_layer4_plan_create` signatures gain new kwarg; 2 `synthesize_phase` call sites — main loop at line ~489 + seam-driven re-synth at line ~794 — both forward the kwarg); MOD `tests/test_layer4_hashing.py` (+50 lines: 6 new tests covering modality-hash None-collapse + populated-hash distinguish for all 3 keys + parametrized mutation coverage extended); MOD `tests/test_layer2_modality.py` (+300 lines: 12 new BM-3 render-helper tests across 4 classes — `TestSingleSessionRenderer` 3 / `TestPerPhaseRenderer` 4 / `TestRaceWeekBriefRenderer` 3 / `TestRendererSpliceIntoFullPrompts` 2 — covering all 3 render helpers + splice site at single_session; module-level `_populated_payload()` + `_empty_payload()` helpers; `model_construct` + duck-typed attribute holders for Layer3APayload per DI5 since full pydantic fixture would have been ~30 lines for no semantic gain). **9 substantive files** (ceiling break ratified at G4 gate; well below 12-file precedents BucketC_l/BucketC_i/SkillCaptureSurface). **Bookkeeping (3 files; do not count against ceiling per CLAUDE.md)**: MOD `aidstation-sources/CURRENT_STATE.md` (this entry); MOD `aidstation-sources/CARRY_FORWARD.md` (BM-3 forward-pointer flipped to ✅ Shipped; §5.0 walkthrough scenario count 101 → 102 with NEW BM-3 §5.0 added); NEW `aidstation-sources/handoffs/V5_Implementation_D73_Phase_5_2_Walkthrough_BM3_PromptBodyIntegration_2026_05_24_Closing_Handoff_v1.md` (this handoff). **Tests**: predecessor reproducer subset 1580 → 1601 + 16 skipped (+21 net new: 12 render tests + 9 hashing tests); test_layer1_builder.py standalone unchanged at 22; ETL etl/tests/ unchanged at 139; 302 Layer 4 entry-point tests across single_session/race_week_brief/plan_create/plan_refresh/orchestrator all green; no regressions. All 7 edited Python files pass `python3 -m py_compile`. **D-decisions ratified**: G1=A3 full wire (over A1/A2/A4); G2(R)=R2 top-pick + ranked menu + flags + guidance (over R1/R3); G2(F)=F2 per-renderer-native (over F1 shared helper); G3=C2 all 3 cache keys (over C1 single only); G4=~8-10 file ceiling break; DI1=None → '' forward-compat collapse for all 3 new key kwargs preserves pre-BM-3 cache entry hits; DI2=phase-awareness guidance line lives in per_phase only (single_session has no phase concept; race_week_brief is exclusively Taper-window so phase-bias hint would be noise); DI3=modality_id citation discipline guidance line mirrored across all 3 renderers despite F2 tradeoff (same risk in all 3 surfaces); DI4=empty-payload renders placeholder line not omission (avoids LLM inferring silent section drop + hallucinating modality reasoning); DI5=splice tests use model_construct + duck-typed attribute holders for Layer3APayload (renderer reads ~6 attributes; full pydantic fixture would have been ~30 lines for no gain). **Forward-pointers**: (a) **NEW per-discipline VOCAB POPULATION — D-008b Outdoor Paddling slice** as the natural first follow-on (BM-1 9 deferred AR disciplines now have observable LLM-facing effect since BM-3 renders the menu in 3 prompts + cache invalidates cleanly on dict shape change); (b) **NEW Phase-aware ranking inside Layer 4 (BM-2) more actionable** — per_phase already has a Peak / Taper bias hint guidance line; BM-2 is structured prompt copy operationalizing the bias per phase; (c) **BM-5 equipment-name canonicalisation** carries forward unchanged (renderer prints modality_id verbatim so vocab additions + canonicalisation both propagate without renderer changes); (d) **NEW plan_refresh tier renderers (T1/T2/T3-intra-phase) modality wire forward-pointer** — explicitly excluded from BM-3 per G1=A3; would either extend Layer2Bundle or re-derive resolver on refresh inputs; (e) **multi-locale cluster ingestion** carries forward unchanged; (f) #8 locales→locations rename remains lowest-risk mechanical (now also touches 3 NEW renderer copies + their tests — all use "at locale" / "@ locale" phrasing); (g) bounce-back UX still queued; (h) Layer 2B per-discipline gap reasoning still queued; (i) Phase-aware ranking inside Layer 4 (BM-2) more actionable post-BM-3; (j) NEW BM-3 Manual §5.0 walkthrough added (5 steps: single_session prompt inspection / per_phase prompt inspection / race_week_brief prompt inspection / cache invalidation on `_MODALITY_OPTIONS_PER_DISCIPLINE` change / forward-compat None → '' cache-key collapse); §5.0 scenario count 101 → 102; (k) Backward-compat for next deploy: all changes additive at runtime contract layer; render helpers only fire when driver receives non-None modality payload (only the orchestrator's full-cone path supplies); cache keys forward-compatible — pre-BM-3 entries written without modality slot continue to hit on default-None calls (the `or ""` collapse makes components string identical to pre-this-slice); first POST-BM-3 deploy sees fresh keys for orchestrator-driven calls that supply payload + one round of re-synthesis settling on new keys.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_BestFitModalityImpl_2026_05_24_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Best-fit modality resolver IMPLEMENTATION shipped (V1 infra-only; W2 full orchestrator wire)**. Andy picked best-fit modality IMPLEMENTATION at the AskUserQuestion first-gate over #8 locales→locations rename + bounce-back UX follow-on + Layer 2B per-discipline gap reasoning. Per Trigger #5 (architectural alternatives) + Trigger #2 (vocab padding scrutiny for 9 deferred AR disciplines) plan-mode gate, 3 nested decisions ratified at AskUserQuestion gates: **G1 = V1 infra-only** (over V2 AR-relevant subset + V3 full 9-discipline vocab) — keeps the slice tight; per-discipline vocab populates become narrow follow-on slices (1 AskUserQuestion gate each) instead of 9 nested gates in one session; honors spec-first sequencing + "push to production as we go." **G2 = W2 full wire (also single-session)** (over W1 _upstream_full_cone only + W3 no wiring) — threads optional kwarg through single-session driver + cached wrapper; cache key intentionally NOT extended (driver doesn't consume payload yet; BM-3 prompt-body integration adds the hash component in 1-line follow-on). **G3 = 7-file ceiling break ratified** (over Tighten-to-5 cuts that would have contradicted G2) — well below 12-file precedents (BucketC_l / BucketC_i / SkillCaptureSurface). **Concrete delta**: NEW `layer2_modality/__init__.py` (public re-exports: ClusterLocaleInput + Layer2ModalityInputError + ModalityOptionDef + resolve_best_fit_modality); NEW `layer2_modality/resolver.py` (~470 lines: ClusterLocaleInput + ModalityOptionDef frozen dataclasses, _MODALITY_OPTIONS_PER_DISCIPLINE module-level dict seeded with spec §5.1 verbatim 3 disciplines D-001 Trail Running 3 options / D-006 Outdoor Road Cycling 3 options / D-010 Outdoor Rock Climbing 7 options, _validate_inputs per spec §4 with discipline_id shape relaxed to "non-empty string" per DI3 to avoid cascading ~37 orchestrator test fixture updates with zero behavioral gain since disciplines absent from the dict already silently pass through, _load_discipline_info single-SELECT against sport_discipline_bridge mirroring layer2c precedent, _resolve_menu_for_pair set-arithmetic per spec §5.2 with stable `(-preference_score, modality_id)` ordering, _emit_coaching_flags emitting all 3 spec §8 triggers — `no_modality_recommendation` cluster-wide when every option fails at every locale + `only_generic_modality_available` per-discipline when no `is_specific=True` option resolves + `skill_capability_blocks_specific_modality` per (discipline, locale, blocking_skill) triplet when a skill-gated specific option would otherwise be top-pick — plus public resolve_best_fit_modality entry point returning Layer2ModalityPayload); MOD `layer4/context.py` (+52 lines: 4 NEW pydantic _Base models per spec §7 — ModalityOption + ModalityRecommendation + ModalityCoachingFlag with flag_type Literal pinning the 3 triggers + Layer2ModalityPayload as the resolver output contract, alongside Layer2CPayload per the existing payload-contract convention per DI1; DI2 ratifies pydantic _Base over spec-literal frozen dataclass since codebase convention enforces extra='forbid' validation at construction); MOD `layer4/orchestrator.py` (+54 lines: imports extended with Layer2ModalityPayload + ClusterLocaleInput + resolve_best_fit_modality, _UpstreamFullCone dataclass gains layer2_modality_payload: Layer2ModalityPayload field between layer2e_payload and layer3a_payload with inline comment citing BM-3 deferral, _upstream_full_cone resolver call after Layer 2C consuming its effective_pool with single-locale cluster matching the existing primary-locale-only 2C call shape, orchestrate_single_session_synthesize mirror wire firing only when request.locale_slug is not None matching the existing 2C-only-when-locale branch + calls _q_locale_terrain_ids for the request locale + passes through to llm_layer4_single_session_synthesize_cached via the new layer2_modality_payload_for_locale kwarg per DI7); MOD `layer4/cached_wrappers.py` (+8 lines: optional layer2_modality_payload_for_locale: Layer2ModalityPayload | None = None kwarg added to llm_layer4_single_session_synthesize_cached after the cache: Layer4Cache keyword-only marker, threaded into _synthesize closure's call to llm_layer4_single_session_synthesize; cache key intentionally NOT extended per DI4 — driver doesn't consume payload in prompt yet so adding hash now would force unnecessary cache invalidation for a payload that nothing reads); MOD `layer4/single_session.py` (+14 lines: driver signature gains optional layer2_modality_payload_for_locale: Layer2ModalityPayload | None = None kwarg after llm_caller, driver receives but doesn't consume — prompt-body integration is BM-3 follow-on territory per spec §12, imports extended to include Layer2ModalityPayload, default None preserves all ~25 existing call sites in tests/test_layer4_single_session.py without fixture cascading); NEW `tests/test_layer2_modality.py` (~520 lines, 30 tests across 8 classes: TestInputValidation 6 spec §4 preconditions / TestScenario13_1_AndyAtHome 4 spec §13.1 default-OFF baseline / TestScenario13_2_ClimbingGymPlusToggle 3 spec §13.2 climbing_roped=True + gym locale / TestScenario13_3_DisableRopedToggle 2 spec §13.3 toggle OFF triggers skill_capability_blocks_specific_modality / TestScenario13_4_EmptyCluster 1 spec §13.4 degenerate / TestScenario13_5_HotelIndoorOnly 3 spec §13.5 indoor-only fires only_generic_modality_available / TestStaticLint 5 spec §13.6 alignment scoped to terrain + skill-toggle only per DI5 since spec's exemplar D-010 vocab references granular climbing gear not in canonical 0B equipment_items per spec §10 + §14 BM-5 acknowledgement / TestEdgeCases 6 spec §10 cases including discipline absent from dict silent pass-through + skill_toggle_states=None treated as default-OFF + deterministic menu ordering + payload pydantic-validation + locale_name None falls back to locale_id + discipline_name fallback to discipline_id on SDB miss; pre-load of `from layer4 import InMemoryCacheBackend # noqa: F401` per the tests/test_layer2a.py:26 + tests/test_layer2b.py:26 precedent breaks the layer4.orchestrator → layer2_modality → layer4.context circular import that otherwise blocks single-module collection). **7 substantive files** (ceiling break ratified at G3 gate; precedent BucketC_l=12, BucketC_i=12, SkillCaptureSurface=12). **Bookkeeping (3 files; do not count against ceiling per CLAUDE.md)**: MOD `aidstation-sources/CURRENT_STATE.md` (this entry); MOD `aidstation-sources/CARRY_FORWARD.md` (best-fit modality IMPLEMENTATION forward-pointer flipped to ✅ Shipped + NEW per-discipline vocab-population + BM-3 + BM-5 + multi-locale-cluster forward-pointers added; §5.0 walkthrough scenario count flipped 100 → 101 with NEW resolver §5.0); NEW `aidstation-sources/handoffs/V5_Implementation_D73_Phase_5_2_Walkthrough_BestFitModalityImpl_2026_05_24_Closing_Handoff_v1.md` (this handoff). **Tests**: predecessor reproducer subset (whole tests/ excluding test_layer1_builder.py) 1550 → 1580 + 16 skipped (+30 net new — exactly matches test_layer2_modality.py count, 12 NL parser smoke + 4 Layer 3 SDK smoke skips when ANTHROPIC_API_KEY unset preserved); test_layer1_builder.py standalone unchanged at 22; ETL etl/tests/ unchanged at 139. No regressions on Layer 4 / orchestrator / single_session / cached_wrappers / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin / layer3 cached-wrappers / layer2a / layer2b / layer2c / layer1 builder / bucket_c_terrain_vocab_audit surfaces. All 6 edited Python files pass `python3 -m py_compile`. **D-decisions ratified**: G1=V1 infra-only over V2/V3; G2=W2 full wire over W1/W3; G3=7-file ceiling break ratified explicitly at gate; DI1=output schemas live in layer4.context (mirrors Layer2CPayload convention); DI2=pydantic _Base over spec-literal frozen dataclass (matches existing convention with extra='forbid'); DI3=discipline_id shape validation relaxed from spec-literal `^D-\d{3}[a-z]?$` to "non-empty string" (avoids ~37 fixture cascade with zero behavioral gain — disciplines absent from dict already silently pass through per spec §5.1); DI4=single-session cache key NOT extended with modality hash this slice (BM-3 adds in 1-line follow-on); DI5=static lint scoped to terrain + skill-toggle only (equipment alignment deferred per BM-5); DI6=resolver issues exactly one DB SELECT per call (mirrors layer2c._load_discipline_info); DI7=W2 honored at code-time with resolver wiring inside both _upstream_full_cone AND single-session orchestrator; single-session resolver only fires when request.locale_slug is not None matching the existing 2C-only-when-locale gate. **Forward-pointers**: (a) **per-discipline VOCAB POPULATION follow-on slices** — 9 deferred AR disciplines (D-005 / D-007 / D-008a / D-008b / D-011 / D-014 / D-015 / D-016 / D-020) each carry a Trigger #2 padding scrutiny gate; ~2 files per discipline (resolver.py + test extension); D-011 carries the spec BM-4 also_satisfies chain complexity flag and likely needs its own narrow slice. (b) **BM-3 Layer 4 prompt-body integration** — where do ModalityRecommendation payloads land in plan-gen prompts? Trigger #1; ~3-5 files. Pairs with vocab population. (c) **BM-5 equipment-name canonicalisation** — extend static lint to query canonical 0B equipment vocab OR ratify resolver equipment name set as canonical surface. (d) **NEW multi-locale cluster ingestion** — orchestrator currently feeds primary locale only; resolver handles multi-locale internally; wire-side aggregation is a downstream slice pairing with Layer 2C's broader cluster expansion. (e) #8 locales→locations rename remains lowest-risk mechanical slice (now also touches resolver's locale_id terminology + SkillCaptureSurface's Step 4 indicator). (f) Bounce-back UX follow-on still queued (~1-2 files; SkillCaptureSurface D6). (g) Layer 2B per-discipline gap reasoning still queued. (h) Phase-aware ranking inside Layer 4 (BM-2) queued. (i) NEW resolver Manual §5.0 walkthrough added (5 steps: orchestrator firing path / default-OFF nuisance flag check / toggle ON observation / single-session firing path / static lint observation); §5.0 scenario count 100 → 101. (j) Backward-compat for next deploy: all changes additive at runtime contract layer; resolver runs in production on first deploy; default state for Andy's PGE 2026 (no athlete_skill_toggles rows enabled) produces no_modality_recommendation for D-010 — same nuisance shape as existing BucketC_l default-OFF requires_skill_capability flags; single-session driver receives new kwarg but doesn't consume in prompt — no observable driver-output change; cache keys unchanged for all entry points.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_BestFitModalitySpec_2026_05_24_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Best-fit modality resolver spec shipped (spec-only slice)**. Andy picked best-fit modality at the AskUserQuestion first-gate over #8 locales→locations rename + bounce-back-from-/locales/new UX follow-on + Layer 2B per-discipline gap reasoning. Per Trigger #5 (architectural alternatives) + Trigger #1 (would be prompt-side under A3) / Trigger #3 (would be schema-side under A1) plan-mode gate, 2 nested decisions ratified at AskUserQuestion gates: **G1 = A2 algorithmic Python resolver** (over A1 static `layer0.modality_recommendations` mapping table + A3 LLM prompt-side reasoning + A4 hybrid table+LLM) — mirrors the gear-toggle / skill-toggle Python-rule precedent (`_OUTDOOR_TERRAIN_TAG_TO_TRN_IDS` BucketC_g + `_TOGGLE_ALSO_SATISFIES` Phase 2.4-Prep + `_load_skill_capability_toggle_defs` BucketC_l); no new schema burden (Trigger #3 dodged); no LLM cost per session (Trigger #1 dodged); resolver outputs a menu + top-pick + rationale-hint per `(discipline, locale)` and Layer 4 picks the per-session modality with full phase + race context (menu-not-decision pattern). **G2 = S1 spec-only this session** (over S2 spec + thin runtime stub + S3 spec + full implementation) — keeps the slice tight; lets Andy review the spec before the implementation slice opens (per CLAUDE.md spec-first sequencing rule "Architecture → prompts → implementation. Resist shortcuts. Resist producing testable output before the spec is correct and complete"). **Concrete delta**: NEW `aidstation-sources/BestFitModality_Spec_v1.md` (~430 lines, 14 sections matching Layer2C_Spec.md depth standard) — §1 Purpose (resolver consumes Layer 1 skill_toggle_states + Layer 2B/2C-adjacent locale_terrain_ids + Layer 2C effective_pool + Layer 2A/Layer 1 included_discipline_ids; produces per-`(discipline, locale)` menu of viable modalities ranked by preference, with top-pick + rationale-hint, consumed by Layer 4 plan-gen); §2 Boundaries (does NOT pick per-session modality, weight by phase, look up exercise prescriptions, handle multi-cluster scheduling, consume race_terrain, emit schedule, handle injury accommodations, write to DB); §3 Function signature `resolve_best_fit_modality(db, *, cluster_locale_inputs: list[ClusterLocaleInput], included_discipline_ids: list[str], skill_toggle_states: dict[str, bool] | None = None, etl_version_set: dict[str, str]) -> Layer2ModalityPayload` + `ClusterLocaleInput` dataclass; §4 Input validation (fail-loud `Layer2ModalityInputError`; 7 preconditions); §5 Algorithm — §5.1 module-level `_MODALITY_OPTIONS_PER_DISCIPLINE: dict[str, list[ModalityOptionDef]]` vocab (3 disciplines spec'd as representative: D-001 Trail Running 3 options / D-006 Outdoor Road Cycling 3 options / D-010 Outdoor Rock Climbing 7 options; remaining D-005/D-007/D-008a/D-008b/D-011/D-014/D-015/D-016/D-020 deferred to implementation slice with explicit Trigger #2 padding scrutiny carry — D-013 Wilderness Navigation intentionally absent for being 1:1 modality), §5.2 resolution loop (set arithmetic against requires_terrain_any_of / requires_equipment_all_of / requires_skill_toggle), §5.3 preference scoring conventions (0-100 base score; outdoor>indoor + specific>generic + available-skill>requires-coached-introduction; phase awareness explicitly Layer 4's job), §5.4 menu pruning (no dedup; all satisfied options surface ranked); §6 Skill-toggle gating (default-OFF firing pattern mirroring BucketC_l + SkillCaptureSurface; resolver consumes the same `Layer1Lifestyle.skill_toggle_states` dict that orchestrator already threads end-to-end); §7 Payload schema (`Layer2ModalityPayload` + `ModalityRecommendation` + `ModalityOption` + `ModalityCoachingFlag` dataclasses, all frozen); §8 Coaching flag rules (3 triggers: `no_modality_recommendation` cluster-wide when every option fails at every locale; `only_generic_modality_available` per discipline when no specific-fit option resolves; `skill_capability_blocks_specific_modality` per (discipline, locale, blocking_skill) triplet — coordinates with existing 2C `requires_skill_capability` flag for reinforcing coach voice); §9 Caching & determinism (deterministic; cache key includes athlete_id + sha256 of all inputs; **no new cache invalidation surface required** — existing `evict_layer2b_on_terrain_change` + `evict_layer2c_on_equipment_change` + `evict_layer1_on_skill_toggle_change` SkillCaptureSurface + `evict_on_target_event_included_discipline_ids_change` BucketE_B2_C1 all transitively cover the resolver; latency <50ms per pair); §10 Edge cases (8 cases: empty locale_terrain_ids / empty effective_pool / discipline absent from dict / skill_toggle_states=None / mixed indoor+outdoor locale / two locales tie on top-pick / missing-vocab typos caught by static lint not runtime / all disciplines absent from dict); §11 Performance budget (~50-100ms typical 3-locale × 12-discipline cluster incl. one DB roundtrip for discipline-name resolution; cache hit <5ms); §12 Open items (BM-1 full vocab population in impl slice; BM-2 phase-aware ranking inside Layer 4; BM-3 Layer 4 prompt-body integration; BM-4 D-011 Rappelling also_satisfies chain handling; BM-5 equipment-name canonicalisation; BM-6 cross-discipline session bundling); §13 Test scenarios (6 integration scenarios: Andy at home with default-OFF / enables climbing_roped + adds gym locale / disables climbing_roped tripping the blocks_specific flag / empty cluster degenerate / hotel locale indoor-only / static lint test for vocab drift between resolver dict and ETL vocab); §14 Gut check (risks: module-level vocab drift accepted as precedent; equipment-name string matching with static lint mitigation; static preference scores oversimplify but narrow not decide; partial vocab coverage in v1. Missing: time-of-day / weather biases Layer 4's job; race-specific preference partially encoded; cross-discipline modality bundling flagged BM-6; equipment OR-groups deferred. Best argument against: vocab fragmentation across Python module + 3 Layer 0 tables is cognitive load but compact compared to A1's multiplicative table rows). **1 substantive file** (well under 5-file ceiling): NEW `BestFitModality_Spec_v1.md`. **Bookkeeping (3 files; do not count against ceiling per CLAUDE.md)**: MOD `aidstation-sources/CURRENT_STATE.md` (this entry); MOD `aidstation-sources/CARRY_FORWARD.md` (best-fit modality forward-pointer status flipped from "design slice when picked" to "spec'd; implementation queued"); NEW `aidstation-sources/handoffs/V5_Implementation_D73_Phase_5_2_Walkthrough_BestFitModalitySpec_2026_05_24_Closing_Handoff_v1.md` (this handoff). **Tests**: no test changes (spec-only slice). Reproducer subset unchanged at 969 passed + 12 skipped; `test_layer1_builder.py` unchanged at 22; ETL `etl/tests/` unchanged at 139. No code edits → nothing to py_compile. **D-decisions ratified**: D1=A2 algorithmic Python resolver (over A1/A3/A4); D2=S1 spec-only this session (over S2/S3); D3=module name `layer2_modality/` (descriptive; doesn't claim a 2F slot since Layer 2A-2E is an LLM-stack naming convention and this is a pure-Python rule resolver); D4=module-level `_MODALITY_OPTIONS_PER_DISCIPLINE` dict for vocab (matches `_OUTDOOR_TERRAIN_TAG_TO_TRN_IDS` BucketC_g + `_TOGGLE_ALSO_SATISFIES` Phase 2.4-Prep precedent over Layer 0 table); D5=menu-not-decision pattern (resolver narrows option space; Layer 4 picks per-session with phase context); D6=skill_toggle gating mirrors default-OFF semantic (consistent with BucketC_l + SkillCaptureSurface); D7=no new cache invalidation surface (existing eviction policies transitively cover the resolver via Layer 1 + 2B + 2C surfaces); D8=preference scoring is static 0-100 weights encoding outdoor>indoor + specific>generic + available-skill biases (phase-aware ranking is explicitly Layer 4's responsibility); D9=Trigger #2 padding scrutiny applies to vocab population (disciplines without meaningful modality split stay absent from dict — silent pass-through, not a coaching flag); D10=spec includes 3 disciplines representative (D-001/D-006/D-010) with explicit BM-1 open item for impl slice to populate the remaining 9 AR disciplines with careful per-discipline modality analysis. **Forward-pointers**: (a) **Implementation slice next** — Trigger #5 + Trigger #2 plan-mode gate to ratify per-discipline modality vocab population (9 disciplines) + full Python implementation + Layer 4 orchestrator wiring + tests + static-lint test for vocab-drift between resolver dict and ETL vocab. ~6-10 files; ceiling break likely (precedent BucketC_l=12 for ratified complexity). Open from this spec's §12 BM-1. (b) **Layer 4 prompt-body integration** (BM-3) — where in the plan-gen prompt do `ModalityRecommendation` payloads land + how does the LLM cite them when synthesising sessions? Pairs with the implementation slice or as a follow-on. (c) **Phase-aware ranking** (BM-2) — Layer 4 needs to encode "Peak bias → specific outdoor over indoor; Taper bias → lower-stimulus options" given the resolver's static menu. Out-of-scope for resolver; Layer 4 design follow-on. (d) #8 locales→locations rename remains lowest-risk mechanical slice carrying. (e) Bounce-back-from-/locales/new UX follow-on (D6 from SkillCaptureSurface) still queued. (f) Layer 2B per-discipline gap reasoning (consume C1's `discipline_id`) still queued.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_SkillCaptureSurface_2026_05_24_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Bucket C (l) capture-surface follow-on shipped end-to-end + athlete-locales onboarding step ALSO inserted (gap closed mid-gate)**. Andy picked the capture-surface follow-on at the AskUserQuestion first-gate over #8 locales→locations rename + best-fit modality cross-reference + Layer 2B per-discipline gap reasoning. Per Trigger #5 plan-mode gate, 3 nested decisions ratified at AskUserQuestion gates: **G1=onboarding step placement between prefill (3) and schedule (4-was, 6-now)** + athlete-locales onboarding step ALSO inserted at G1's gap-observation moment (the existing `/locales` blueprint is currently never linked from the onboarding chain — Andy named this gap when picking the placement option, so this slice inserts BOTH Step 4 Locations + Step 5 Skills); **G2=cache eviction via `evict_on_layer_change(cache, uid, 'layer1')`** (honest categorization since skill_toggle_states lives in Layer1Lifestyle; covers all 4 entry points + both Layer 3 wrappers via the existing Layer 1 policy; matches predecessor §6.1 stated intent; cache stays clean + emits metric); **G3=shared `_skills_form.html` partial** mirroring `_schedule_form.html` precedent (single source of truth for checkbox layout across profile-tab + onboarding surfaces). **Concrete delta**: NEW `athlete_skill_toggles_repo.py` with 5 helpers — `load_active_skill_capability_toggle_vocab(db)` reads `layer0.skill_capability_toggles WHERE superseded_at IS NULL` ordered by toggle_name (returns list of dicts with toggle_name + display_label + description); `get_athlete_skill_toggles(db, user_id)` reads `athlete_skill_toggles` returning dict keyed by toggle_name (public mirror of `layer1.builder._load_skill_toggle_states`); `upsert_athlete_skill_toggles(db, user_id, states)` UPSERTs one row per (user_id, toggle_name) using the `ON CONFLICT (user_id, toggle_name) DO UPDATE SET enabled = EXCLUDED.enabled` shape against the UNIQUE constraint shipped in `init_db._PG_MIGRATIONS`; `evict_layer1_on_skill_toggle_change(db, user_id)` builds transient `Layer4Cache(PostgresCacheBackend(lambda: db))` + fires `evict_on_layer_change(cache, user_id, 'layer1')` mirroring `routes.locales._evict_layer2b_on_terrain_change` precedent (Vercel stateless model); `parse_skill_form(form, vocab)` coerces `skill__<toggle_name>` checkbox keys to bool dict — returns ALL vocab toggles (presence True / absence explicit False) so explicit-OFF rows persist instead of collapsing to absent at the boundary. NEW `/onboarding/locales` Step 4 GET + Continue POST in `routes/onboarding.py` — read-only summary of `locale_profiles` rows + the 4 legacy slots (home/hotel/partner/airport always present, configured flag reflects locale_profiles row presence) + N custom slots (athlete-created rows where slug not in LEGACY_LOCALES); edit links pointing at the existing `/locales` blueprint which owns the Mapbox picker / terrain grid / equipment editor; Continue advances to `/onboarding/skills` regardless of locale count (educational step, not a gate). NEW `_athlete_locales_for_review(db, uid)` helper does the single SELECT against `locale_profiles WHERE user_id = ?` + assembly. NEW `/onboarding/skills` Step 5 GET + POST in `routes/onboarding.py` — GET loads vocab + current states and renders the 5 toggle checkboxes (alphabetical: climbing_roped, mountaineering, swim_open_water, via_ferrata, whitewater_handling) with display_label as bold label + description as help text + Skip-for-now link → `/onboarding/schedule` + Save & continue button; POST upserts (one row per vocab toggle via `parse_skill_form` returning all-vocab dict) + commits + fires Layer 1 eviction + flashes + redirects to `/onboarding/schedule`. NEW Skills tab on `/profile?tab=skills` with `save_skills` POST handler in `routes/profile.py` mirroring `save_schedule` precedent (Skills tab in nav between Schedule and Race events; tab-pane includes shared partial; POST upserts + commits + evicts + flashes + redirects back to `/profile?tab=skills`). NEW shared `templates/onboarding/_skills_form.html` partial included by both onboarding `skills.html` and `profile/edit.html` tab=skills; empty-vocab path renders an "ETL populate script not yet applied" warning. **Constants**: `_POST_STEP3_TARGET` flipped from `/onboarding/schedule` to `/onboarding/locales` (the prefill template's `post_step3_target` Continue button auto-flows through); NEW `_POST_STEP_LOCALES_TARGET = '/onboarding/skills'` + NEW `_POST_STEP_SKILLS_TARGET = '/onboarding/schedule'`. **Step indicator renumbering**: schedule.html (4 → 6 — inserted Step 4 Locations + Step 5 Skills as ✓ entries before the current-step badge), target_race.html (5 → 7), route_locales.html (6 → 8); surgical edits to the hardcoded `<ol>` step indicator chrome in each downstream template (no shared component to abstract; would require a bigger refactor). **12 substantive files** (5 NEW + 5 MOD + 2 NEW test; ceiling break ratified at gate; precedents BucketC_i=12, BucketC_l=12, BucketE_B2_C1=11+5, RaceLocaleMapbox=13): (1) NEW `athlete_skill_toggles_repo.py`; (2) MOD `routes/onboarding.py` (4 new routes + helper + 2 new constants + 1 constant flipped + imports extended for `LOCALES as LEGACY_LOCALES` + 5 athlete_skill_toggles_repo names); (3) MOD `routes/profile.py` (Skills-tab data loaded in `edit()` GET, NEW `save_skills` POST handler, imports extended); (4) MOD `templates/profile/edit.html` (Skills tab button in nav + tab-pane with intro copy + shared partial include); (5) NEW `templates/onboarding/_skills_form.html` (shared 5-checkbox form partial; empty-vocab warning path); (6) NEW `templates/onboarding/skills.html` (Step 5 onboarding wrapper with 5-step indicator chrome + intro copy + Skip-for-now link + Save & continue button); (7) NEW `templates/onboarding/locales.html` (Step 4 onboarding wrapper with 4-step indicator + Your custom locations card conditional + Legacy slots card always-shown with `(not configured)` small print + Add or manage locations CTA + Continue POST button); (8) MOD `templates/onboarding/schedule.html` (indicator 4 → 6); (9) MOD `templates/onboarding/target_race.html` (indicator 5 → 7); (10) MOD `templates/onboarding/route_locales.html` (indicator 6 → 8); (11) NEW `tests/test_onboarding_skills.py` (18 tests: 2 TestLoadActiveSkillCapabilityToggleVocab + 2 TestGetAthleteSkillToggles + 2 TestUpsertAthleteSkillToggles + 3 TestParseSkillForm + 1 TestEvictLayer1OnSkillToggleChange + 3 TestAthleteLocalesForReview + 2 TestLocalesRoute + 3 TestSkillsRoute); (12) NEW `tests/test_routes_profile_skills.py` (2 tests: save_skills POST upserts + commits + evicts + redirects to tab=skills; empty-vocab POST no-ops the upsert/commit/eviction but still redirects). **Tests**: predecessor reproducer subset 949 → 969 (+20 net new); test_layer1_builder.py standalone unchanged at 22; ETL `etl/tests/` unchanged at 139; whole tests/ dir 1550 passed + 16 skipped (12 NL parser smoke + 4 Layer 3 SDK smoke when `ANTHROPIC_API_KEY` unset). No regressions across any prior surface. All edited Python files pass `python3 -m py_compile`. **D-decisions ratified**: D1=onboarding step placement between prefill and schedule (over post-route-locales + profile-tab-only — keeps conceptual flow: connect → review prefilled values → set athletic context locations/skills → set training capacity schedule → pick race → map route); D2=athlete-locales onboarding step ALSO inserted (gap closure named by Andy at gate); D3=Layer 1 cache eviction policy (over no-eviction-natural-miss; honest categorization + emits eviction metric); D4=shared form partial (over two separate templates); D5=12-file ceiling break ratified; D6=bounce-back-from-/locales/new deferred to follow-on (~1-2 files); D7=locales onboarding step is read-only with link-out to existing `/locales` blueprint (not embedded Mapbox picker — avoids duplicating locale-add UX); D8=form key prefix `skill__<toggle_name>` distinguishes skill fields in mixed POST payloads; D9=parse_skill_form returns ALL vocab toggles (explicit False for unchecked) preserving "deliberately set OFF" signal at boundary; D10=step indicator chrome renumbered in 3 downstream templates (no shared component to abstract). **Forward-pointers**: (a) NEW 5-step Manual §5.0 walkthrough scenario added — `/onboarding/locales` renders the 4 legacy slots + custom locations cards + Continue advances to skills; `/onboarding/skills` renders 5 alphabetical toggles with display_label + description; check + Save & continue persists 5 athlete_skill_toggles rows + redirects to schedule + rerun race-week brief shows D-010 + D-008b flags gone (D-004 + D-016 + D-020 still fire); `/profile?tab=skills` round-trip shows tab present + state matches + Save skills flashes "Skills saved." + redirects back; cache eviction fires (0 cached rows for Andy post-save + evict_layer1 event tagged `layer=layer1` in metrics). (b) **Best-fit modality cross-reference design** is the natural next slice — open from BucketC_g + BucketC_l; design slice will reason `{locale_terrain_ids + cluster_equipment + included_disciplines + skill_toggle_states} → recommended modality per session`; plan-mode gate required (Trigger #1 if prompt-side; Trigger #3 if schema-side; Trigger #5 either way). (c) **NEW "bounce back from /locales/new to /onboarding/locales"** forward-pointer — small UX follow-on; ~1-2 files (add `return_to` param to /locales/new + /locales/<slug>/edit POST handlers; current behavior: athletes who click "Add or manage locations" from `/onboarding/locales` navigate to `/locales` and must return to onboarding via URL bar). (d) #8 locales→locations rename remains lowest-risk mechanical slice; will now also touch the new templates (Step 4 indicator says "Locations" but URL is `/onboarding/locales` — the rename would unify these). (e) Bucket D legacy hardcoded locales still unblocked (depends on Bucket C terrain-vocab decisions which all closed). (f) Layer 2B per-discipline gap reasoning still queued. (g) Backward-compat for next deploy: all changes additive; existing athletes who never re-enter onboarding keep current state (no athlete_skill_toggles rows = every toggle treated as default-OFF, identical to today). Production rollout still requires the `populate_skill_capability_toggles.sql` ETL populate (carried from BucketC_l) — until applied, both surfaces show the "ETL populate script not yet applied" warning + the POST no-ops the upsert but still redirects (defensive against early-deploy timing).

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_BucketC_l_SkillCapabilityToggles_2026_05_24_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Bucket C sub-item (l) skill-capability toggles shipped end-to-end (capture surface deferred to follow-on, mirrors gear-toggle status quo)**. Andy picked Bucket C (l) at the AskUserQuestion first-gate over #8 locales→locations rename + best-fit modality cross-reference + Layer 2B per-discipline gap reasoning. Per Trigger #3 (cross-layer schema: NEW `layer0.skill_capability_toggles` + NEW `athlete_skill_toggles` + `Layer1Lifestyle.skill_toggle_states` extension + Layer 2B/2C kwarg threading) + Trigger #5 (architectural alternatives) plan-mode gate, scope ratified across nested gates: **G1=rip + extend to Layer 2C** (delete `_COACHED_INTRO_KEYWORDS` keyword-substring match + add parallel `requires_skill_capability` flag on BOTH 2B race-terrain side AND 2C included-discipline side — coaching guidance becomes uniform); **G2=5 toggles** (climbing_roped, via_ferrata, whitewater_handling, swim_open_water, mountaineering — vocab broader than the WaterVocab pre-pin's "narrow 3" but Andy accepted the Trigger #2 vocab-padding scrutiny since these all have genuine athlete-side capability semantics not derivable from gear); **G3=mappings tightened** (climbing_roped drops D-011 Rappelling — separate skill from lead-climbing despite gear-toggle precedent treating them as one; swim_open_water drops D-014 + D-018 — gates only D-004 dedicated open-water swimming, reduces noise for AR athletes whose river-crossing swims don't require dedicated OW skill); **G4=capture surface DEFERRED** (mirror gear-toggle status quo where orchestrator passes `cluster_gear_toggle_states={}` until UI ships — Andy's PGE 2026 AR brief now surfaces ~3-4 skill-capability flags default-OFF for D-010/D-008b/D-004/D-016/D-020, same shape as existing gear-toggle nuisance flags); **G5=ceiling break ratified** (12 substantive files vs. 5-file default; precedents BucketC_i=12 + BucketE_B2_C1=11+5 + RaceLocaleMapbox=13). **Architectural principle pinned (D-decision)**: coach-need / skill-prerequisite is an athlete-capability property, not a derived terrain property. The v1 §8.2 `requires_coached_introduction` flag firing off `prescription_note` keyword-match was the wrong architectural home (fragile substring sniffing on prose authored by ETL); the new scheme stores capabilities as explicit toggle rows joining `gated_terrain_ids` + `gated_discipline_ids` arrays — Layer 2B reads the terrain side, Layer 2C reads the discipline side, identical default-OFF semantics. **Concrete delta**: NEW `layer0.skill_capability_toggles` table in `etl/layer0/schema.sql` mirroring `sport_specific_gear_toggles` minus equipment-specific cols + plus `gated_terrain_ids TEXT[]`; NEW `etl/sources/populate_skill_capability_toggles.sql` seeding 5 vocab rows at canonical 0C version `0C-v2.0-r2` with explicit display_label + description + the (tightened) gated_terrain_ids / gated_discipline_ids per Andy's mappings; NEW `athlete_skill_toggles (user_id, toggle_name, enabled BOOLEAN DEFAULT FALSE)` table via `_PG_MIGRATIONS` (UNIQUE (user_id, toggle_name); ON DELETE CASCADE on user_id FK; ast_user_idx index); `Layer1Lifestyle` gains `skill_toggle_states: dict[str, bool] = Field(default_factory=dict)`; `Layer2CCoachingFlag.flag_type` Literal extended with `"requires_skill_capability"`; `layer1/builder.py::_load_skill_toggle_states` 1-line query reading athlete_skill_toggles, threaded into the lifestyle dict at the existing `Layer1Lifestyle(...)` constructor call; `layer2b/builder.py` rips `_COACHED_INTRO_KEYWORDS` + `_COACHED_INTRO_FIDELITY_MIN` + `_mentions_coached_intro` + the keyword-match branch in `_emit_coaching_flags`, adds NEW `_load_skill_capability_toggle_defs` SQL loader + new `skill_toggle_states: dict[str, bool] | None = None` kwarg on public function + new emission block that fires `requires_skill_capability` for every race_terrain entry sitting in any toggle's `gated_terrain_ids` AND state-not-True (default-OFF fires per the gear-toggle precedent); `layer2c/builder.py` adds parallel `_load_skill_capability_toggle_defs` (own copy since 2B + 2C are independent modules; same SQL) + `skill_toggle_states` kwarg + parallel emission block right after the existing `toggle_off_for_discipline` block (fires `requires_skill_capability` for every included discipline sitting in any toggle's `gated_discipline_ids` AND state-not-True); `layer4/orchestrator.py` threads `skill_toggle_states=layer1_payload.lifestyle.skill_toggle_states` into all 3 call sites (2B in `_upstream_full_cone` + 2C in `_upstream_full_cone` + 2C in single-session orchestrator). **TRN-011 prescription_note text** in `etl/sources/populate_terrain_gap_rules.sql` rewritten to drop the now-orphaned keyword-match triggers (`supervised instruction` / `requires coached introduction`) — replaced with neutral phrasing crediting the new `whitewater_handling` skill toggle as the capability-gating surface. The TRN-011 → TRN-009 gap rule itself stays at fidelity 0.30 unchanged; the prescription_note change is documentation, not behavior. **12 substantive files** (9 code + 3 test, ceiling break ratified; precedents BucketC_i=12, BucketE_B2_C1=11+5, RaceLocaleMapbox=13): (1) `etl/layer0/schema.sql` — NEW `CREATE TABLE IF NOT EXISTS layer0.skill_capability_toggles` (mirror of sport_specific_gear_toggles minus paired_equipment_categories + also_satisfies, plus gated_terrain_ids TEXT[] column) with explanatory comment block citing Bucket C (l) ratification + the SURFACE / SKILL split principle. (2) NEW `etl/sources/populate_skill_capability_toggles.sql` — 5 INSERT rows with `ON CONFLICT (toggle_name, etl_version) DO NOTHING` for idempotency; verify block asserts COUNT(*)=5 active rows at `0C-v2.0-r2` (RAISES on shortfall); naming convention is snake_case identifiers (not the gear-toggle em-dash display strings — comment explicitly cites that as a fragility trap dodge). (3) `init_db.py` — NEW `athlete_skill_toggles` table CREATE wired as new `_PG_MIGRATIONS` entry after `_retire_outdoor_terrain_equipment_tags` callable; index `ast_user_idx` on (user_id) appended. (4) `layer4/context.py` — `Layer1Lifestyle.skill_toggle_states: dict[str, bool] = Field(default_factory=dict)` field; `Layer2CCoachingFlag.flag_type` Literal extended (`"low_coverage" | "critical_dropped" | "toggle_off_for_discipline" | "requires_skill_capability"`). (5) `layer1/builder.py` — NEW `_load_skill_toggle_states(db, user_id)` helper (1-SELECT, returns dict comp); call site in `build_layer1_payload` between `_load_disclosures` and the `Layer1Lifestyle(...)` constructor; constructor passes `skill_toggle_states=skill_toggle_states`. Adds 1 SELECT to Layer 1's count (24 → 25). (6) `layer2b/builder.py` — rip block (`_COACHED_INTRO_KEYWORDS` tuple + `_COACHED_INTRO_FIDELITY_MIN` float + `_mentions_coached_intro` helper + the fidelity-gated keyword-match branch in `_emit_coaching_flags`); replaced with explanatory comment block + NEW `_load_skill_capability_toggle_defs(db, version_0c)` SQL loader; public function signature gains `skill_toggle_states: dict[str, bool] | None = None` kwarg with `or {}` normalization; new emission block at the tail of `_emit_coaching_flags` iterates skill_toggle_defs × race_terrain entries; the load gate `if race_terrain` avoids the SQL roundtrip on the empty-race-terrain short-circuit path (which is handled by the existing `race_terrain_unset` branch). (7) `layer2c/builder.py` — NEW `_load_skill_capability_toggle_defs(db, version_0c)` (own copy of the same SQL — 2B and 2C are independent modules so each owns its loader matching the existing `_load_toggle_defs` precedent); `_emit_coaching_flags` signature gains two new params (`skill_toggle_defs` + `skill_toggle_states`); new emission block right after `toggle_off_for_discipline` mirrors the gear-toggle DP2 semantic; public function signature gains `skill_toggle_states: dict[str, bool] | None = None` kwarg with `or {}` normalization; loader call site is unconditional (every 2C call hits the table once, parallel to gear-toggle loader). (8) `layer4/orchestrator.py` — 3 call site additions: `_upstream_full_cone` 2B call adds `skill_toggle_states=layer1_payload.lifestyle.skill_toggle_states`; same `_upstream_full_cone` 2C call adds the same; the single-session orchestrator's `q_layer2c_equipment_mapper_payload` call inside the `if request.locale_slug is not None:` branch adds the same. Each site has an inline comment citing Bucket C (l). (9) `etl/sources/populate_terrain_gap_rules.sql` — TRN-011 whitewater rule's `prescription_note` text rewritten to drop the keyword-match triggers; new text credits the `whitewater_handling` skill toggle as the capability-gating surface + retains the substantive guidance about flat-water-fitness-only / pool-rolling-foundation. (10) `tests/test_layer2b.py` — `TestCoachedIntroFlag` class removed entirely (2 tests deleted: `test_whitewater_coached_intro_flag` + `test_coached_intro_does_not_fire_below_fidelity_threshold` — both asserted on the ripped behavior); NEW `TestSkillCapabilityFlag` class (5 tests: TRN-011 + toggle OFF fires `requires_skill_capability` flag with toggle name + pct in metadata; toggle ON suppresses flag; ungated terrain TRN-001 doesn't fire; empty race_terrain short-circuits without SQL roundtrip for toggle defs; multiple gated terrains emit one flag each — TRN-011+TRN-017 both gated by whitewater_handling → 2 flags with correct pct_of_race per entry). (11) `tests/test_layer2c.py` — `_FakeConn` extended to recognize `skill_capability_toggles` in the SQL string and return empty cursor WITHOUT consuming a queued batch (so the existing 35 tests stay untouched as the new SELECT slots into the call sequence between gear-toggle defs and discipline info); NEW `queue_skill_capability_toggles(*rows)` method on `_FakeConn` for tests that DO want to queue capability vocab rows; NEW top-level `_skill_cap_row(toggle_name, ...)` helper; NEW `TestSkillCapabilityFlag` class (5 tests parallel to gear-toggle precedent: toggle OFF + included gated discipline fires flag; toggle ON suppresses; non-included discipline doesn't fire even when toggle OFF; mountaineering toggle gating D-016 + D-020 fires two flags when both included; default empty skill_toggle_states kwarg fires the flag — mirrors gear-toggle default-OFF semantics). (12) `tests/test_layer1_builder.py` — pre-existing circular import worked around via `from layer4 import InMemoryCacheBackend # noqa: F401` (mirrors `tests/test_layer2a.py:26` + `tests/test_layer2b.py:26` precedent — unblocks single-module collection of this file for the first time); `_queue_empty_athlete` bumped 24 → 25 responses for the new SELECT; `test_24_selects_issued` renamed to `test_25_selects_issued` with updated assertion; `TestFullyPopulated._queue_andy` extended with 25th queue_response carrying climbing_roped=True + whitewater_handling=True (Andy is an AR athlete with real climbing + paddle experience); NEW `TestSkillToggleStates` class (3 tests: empty athlete yields empty dict; Andy's `_queue_andy` populated path yields {climbing_roped: True, whitewater_handling: True}; explicit OFF rows preserved as `{toggle: False}` not absent). **Tests**: predecessor reproducer subset (24 test files, excludes test_layer1_builder.py per predecessor convention) 941 → 949 (+8 net: 5 in 2C TestSkillCapabilityFlag + 3 net in 2B TestSkillCapabilityFlag = 5 added - 2 removed = +3); test_layer1_builder.py separately 19 → 22 (+3 in TestSkillToggleStates; the import-order workaround unblocks the 19 existing tests for single-module collection but they were always passing when collected alongside other layer4-importing modules); ETL `etl/tests/` 139 → 139 unchanged. No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin / layer3 cached-wrappers / layer2a / layer2b / layer2c / layer1 builder / bucket_c_terrain_vocab_audit surfaces; 12 NL parser smoke + 4 Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. All 6 edited Python files pass `python3 -m py_compile`. **D-decisions ratified at AskUserQuestion gates**: D1=rip + extend to Layer 2C (over rip-replace-2B-only + layer-on-top-keep-fallback — both 2B race-terrain side and 2C included-discipline side now consult skill state with parallel `requires_skill_capability` flags, semantically honest replacement of the keyword-match path that was effectively dead code today anyway because TRN-011 fidelity 0.30 < 0.5 gate filtered it out); D2=5 toggles including via_ferrata + mountaineering (over the 3-toggle pre-pin from WaterVocab gate — Andy accepted Trigger #2 padding scrutiny since these are genuine capability semantics not gear-derivable); D3=mappings tightened — drop D-011 from climbing_roped + drop D-014/D-018 from swim_open_water (less noise for AR athletes; tradeoff is a future `rappelling_skill` toggle if AR rappel sections need separate gating); D4=capture surface DEFERRED (mirrors gear-toggle status quo; capture UI shipping as a focused follow-on slice that touches profile-edit + invalidation policy without re-disturbing the runtime contract); D5=12-file ceiling break ratified explicitly at gate. **Forward-pointers**: (a) NEW 5-step Manual §5.0 walkthrough scenario added — Neon schema spot-check `\d athlete_skill_toggles` shows 5-col shape + UNIQUE + index; `SELECT toggle_name, COUNT(*) FROM layer0.skill_capability_toggles GROUP BY toggle_name` returns 5 rows at `0C-v2.0-r2`; orchestrate race-week brief for Andy's PGE 2026 → `coaching_flags` for 2B includes 0 `requires_skill_capability` rows for race-terrain (Andy's PGE 2026 terrain set TRN-002/003/004/009/016 has no skill-gated members) AND `coaching_flags` for 2C includes 4 `requires_skill_capability` rows (D-010 climbing_roped + D-008b whitewater_handling + D-004 if swim included + D-016/D-020 if mountaineering included; default-OFF since no athlete_skill_toggles rows for Andy yet); manually INSERT `athlete_skill_toggles (user_id, toggle_name, enabled) VALUES (<andy>, 'climbing_roped', TRUE), (<andy>, 'whitewater_handling', TRUE)` + re-orchestrate → those 2 flags disappear, remaining 2 still fire; `python3 -m py_compile init_db.py` clean. (b) **NEW capture surface follow-on** — profile-edit tab `?tab=skills` with 5-checkbox grid + POST handler + cache invalidation policy (skill-toggle edits should evict Layer 2B+2C entry points = `_NON_SINGLE_SESSION` policy mirror, plus the single-session orchestrator since 2C is in its cone too). ~3-4 files; could pair naturally with the future onboarding-step integration as a single onboarding+profile-edit slice. (c) **NEW best-fit modality cross-reference forward-pointer** (still open from BucketC_g) — design slice that lets a planner reason `{locale_terrain_ids + cluster_equipment + included_disciplines + skill_toggle_states} → recommended modality per session`. Skill toggle state is now an additional input to this reasoning; pairs naturally with the modality design slice when Andy picks it. (d) Bucket C now FULLY CLOSED — all 11 sub-items (a/b/c/d/e/f/g/h/i/j/k/l) shipped end-to-end. (e) #8 locales→locations rename remains the lowest-risk next-slice candidate per the architect-recommended-since-RaceLocaleMapbox pointer (~9 templates, mechanical, no /plan triggers). (f) Layer 2B per-discipline gap reasoning (consume C1's `discipline_id`) still queued. (g) The deferred capture surface means Andy's PGE 2026 brief will now surface ~3-4 nuisance `requires_skill_capability` flags until the UI lands and Andy enables the climbing_roped + whitewater_handling toggles (which he should — both are real for him). Same nuisance shape as the existing gear-toggle default-OFF flags; not blocking, but documents an athlete-UX regression that the capture-surface follow-on resolves.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_BucketC_g_TerrainEquipmentMerge_2026_05_24_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Bucket C sub-item (g) terrain↔equipment merge shipped end-to-end**. Andy picked Bucket C (g) at the AskUserQuestion first-gate over #8 locales→locations rename + Bucket C (l) skill toggles + Layer 2B per-discipline gap reasoning. Per Trigger #3 (cross-layer schema) + Trigger #5 (architectural alternatives) plan-mode gate, scope ratified across three nested gates: (1) merge direction = B-prime (expand terrain vocab + retire equipment fieldset) over D-narrow + C-cross-reference; (2) scope shape = S2 (add ONE TRN row + sunset 9 equipment tags) over S1 (zero new rows; accept the gravel gap) + S3 (3 cycling-category rows + ceiling-break larger); (3) judgment-call ratification on conservative open_water_paddle mapping {TRN-009 only} + hard DELETE of the 9 equipment_items rows + 6-file ceiling break. **Architectural principle pinned (D-decision)**: terrain rows describe SURFACE only; modality (foot / bike / paddle) is captured discipline-side + equipment-side via a future best-fit cross-reference (forward-pointer) that lets a planner reason `{locale_terrain_ids + cluster_equipment + included_disciplines} → recommended modality per session` (e.g. athlete with gravel_bike equipment + TRN-020 Gravel terrain + outdoor_gravel_cycling discipline → recommend gravel ride over road ride). This validates a 1-row-only vocab addition rather than a separate cycling-vocab category — Trigger #2 strict-no-padding scrutiny holds because adding cycling-modality rows for already-surface-equivalent terrain (TRN-001 paved already serves both running and cycling at the surface level; modality is the discipline's job) would have padded vocab where the surface-vs-modality split semantically belongs elsewhere. **Concrete delta**: NEW TRN-020 Gravel row in `etl/layer0/extractors/vocabulary.py::_TERRAIN_STRUCTURED_ROWS` (Foot category to match existing surface-typing convention, simulatable=partial, simulation_note explicitly covers both treadmill-for-running and trainer-for-cycling substitutes with gravel-specific surface gap; TRN-018/TRN-019 intentionally reserved for future cycling-vocab expansion if Andy ratifies S3 later — sequence gap is locked in by the audit test); 2 NEW gap rules in `etl/sources/populate_terrain_gap_rules.sql` (TRN-020 → TRN-002 singletrack proxy at fidelity 0.70 `low` band [singletrack covers unpaved-surface gait at high fidelity; 1-2 gravel-specific sessions close the gap] + TRN-020 → TRN-001 paved proxy at fidelity 0.65 `medium` band [paved covers gait + aerobic at full fidelity; 2-4 gravel-specific sessions for surface adaptation]); 9 retired equipment tags removed from `init_db.EQUIPMENT_CATEGORIES` ("Outdoor & Terrain" category entry deleted entirely + replaced with explanatory comment; the 9 tags were display-only venue markers with ZERO downstream consumers — `validator.py:139,143`'s `trail_running` / `outdoor_road_cycling` hits are DISCIPLINE names not equipment tags; Garmin/FIT files use `trail_running` as a sport-type string, not as an equipment_items.tag); NEW `_OUTDOOR_TERRAIN_TAG_TO_TRN_IDS` module-level mapping dict + NEW `_retire_outdoor_terrain_equipment_tags(cur)` migration callable as final `_PG_MIGRATIONS` entry (5-step idempotent: translate `locale_equipment` private-locale picks via JOIN-LATERAL-unnest with VALUES-mapping CTE → UNION dedupe-sort into `locale_profiles.locale_terrain_ids` + same translation for `locale_equipment_overrides` action='add' shared-profile picks + DELETE locale_equipment rows for retired tags + DELETE locale_equipment_overrides rows for retired tags + DELETE the 9 equipment_items rows themselves — no exercise_equipment FK risk verified at scope; no EXERCISE_EQUIPMENT seed entry references any of the 9 retired tags). **Conservative open_water_paddle mapping** ratified at gate: translates to `{TRN-009 only}` (Flat Water) — over-granting Ocean/Moving/Whitewater would fake water-skill access the athlete may not have; athlete can add the other water rows explicitly on next edit. **hills mapping** is conservative too: `{TRN-004 Hill / Rolling}` only, not TRN-005 Mountain/Alpine (which represents much more demanding sustained vertical). **Locale form template UNCHANGED**: the existing `{% for category, items in equipment_categories %}` loop in `templates/locales/form.html:63-88` auto-skips the now-missing "Outdoor & Terrain" category since `EQUIPMENT_CATEGORIES` no longer contains the entry; the locale-terrain canonical grid (lines 91-116) was already wired as the single "what's accessible from this location" surface in the Phase 5.1 form-refresh C slice 2026-05-20. **6 substantive files** (ceiling break ratified at gate; precedents BucketC_i=12, BucketE_B2_C1=11+5, RaceLocaleMapbox=13): (1) `etl/layer0/extractors/vocabulary.py` — TRN-020 row inserted after TRN-007 in the Foot section (Foot rows stay contiguous) + `_parse_terrain` docstring count 16 → 18 (also corrected pre-existing drift from WaterVocab slice that left docstring saying 16 when actual was 17); (2) `etl/sources/populate_terrain_gap_rules.sql` — 2 NEW gap rules in NEW "Foot: Gravel gaps" section between Fell/Moorland and Ocean/Tidal sections; ALSO fixed pre-existing WaterVocab drift (file header count + §2 comment + verify-block row-count `<>12` → `<>16` + RAISE NOTICE all bumped to 16 = 12 original + 2 WaterVocab + 2 mine); (3) `init_db.py` — module-level `_OUTDOOR_TERRAIN_TAG_TO_TRN_IDS` mapping dict (with module-level comment documenting the SURFACE-only principle + the conservative open_water_paddle pick + the future best-fit cross-reference forward-pointer) + `_retire_outdoor_terrain_equipment_tags(cur)` migration callable (~85 LOC, uses parameterised SQL via psycopg2 `cur.execute(sql, (retired_tags,))` for the DELETEs and embedded VALUES literal for the mapping CTE since the keys are fixed compile-time constants) + 9-tag removal from EQUIPMENT_CATEGORIES (the category entry replaced with a multi-line comment block citing Bucket C (g) ratification + retained Cycling Equipment + Paddling Equipment categories which are real-gear, not venue-markers) + migration callable wired as final `_PG_MIGRATIONS` entry referenced by name; (4) `tests/test_bucket_c_terrain_vocab_audit.py` — TestCanonicalTerrainVocab class docstring updated to reflect 18-row shape + TRN-018/019 reserved-gap rationale; `test_canonical_row_count_is_17` renamed to `test_canonical_row_count_is_18` + assertion flipped; `test_canonical_ids_are_sequential_TRN_001_through_TRN_017` renamed to `test_canonical_ids_are_TRN_001_through_017_plus_020` + expected list adjusted for the reserved gap; NEW `TestGravelTerrainAdd` (5 tests: row exists / Foot category surface-only + correct env/simulatable/flags / notes mention "compacted/unpaved" + "gravel-running" + "gravel-cycling" / simulation_note no coached-intro language for Bucket C (l) forward-compat / "Outdoor & Terrain" + all 9 retired tags absent from `init_db.EQUIPMENT_CATEGORIES`); module docstring updated with NEW "Sub-item closure (added by the BucketC_g_TerrainEquipmentMerge slice 2026-05-24)" block + "Sub-items recently closed (forward-pointer audit trail)" block citing the BucketC_i closure; (5) `etl/tests/test_vocabulary_md.py` — `test_terrain_count` 17 → 18 + comment updated with bump audit trail; `test_terrain_ids_unique_and_sequential` expected-list rewritten as `[TRN-001..TRN-017] + [TRN-020]` with comment documenting the reserved-gap intent; `test_terrain_known_canonical_names_present` adds "Gravel" to the presence list; (6) `tests/test_locales.py` — UNCHANGED but verified zero regressions (27 → 27 passing; the `TestEvictLayer2cOnEquipmentChange` class is keyed on the parent equipment-edit eviction surface, not on the specific retired tags; the template-render path is auto-skip via the existing for-loop). **Tests**: container-runnable subset 901 → 906 (+5 net new: 5 in TestGravelTerrainAdd; 0 regressions); ETL `etl/tests/` 139 → 139 unchanged. No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin / layer3 cached-wrappers / layer2a / layer2b surfaces; 12 NL parser smoke + 4 Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. `init_db.py` passes `python3 -m py_compile`. **Forward-pointers**: (a) NEW 4-step Manual §5.0 walkthrough scenario added — locale-edit form fieldset count + "Outdoor & Terrain" gone from both legacy private and shared-profile branches + 18-row terrain grid with TRN-020 Gravel / post-migration data spot-check (translated locale_terrain_ids unions in / 0 equipment_items rows for retired tags / no FK violations) / TRN-020 selectable from race-event terrain editor + onboarding mirror / Layer 2B gap-rule round-trip with TRN-020 race terrain (test row, NOT PGE 2026); (b) **NEW best-fit modality cross-reference forward-pointer** — design slice (concrete shape TBD: mapping table vs derived join vs Layer 4 prompt-side reasoning) that lets a planner read `{locale_terrain_ids + cluster_equipment + included_disciplines}` and return per-session "recommended modality" (e.g. gravel_bike + TRN-020 → gravel ride; only road bike + TRN-001 → road ride; etc.); not blocking any current open Bucket items but unlocks athlete-facing reasoning that's currently latent; (c) Bucket C now: 10 of 11 sub-items closed (a/b/c/d/e/f/g/h/i/j/k); 1 still open — (l) skill toggles (Trigger #3 + #5 plan-mode gate; spec pre-pinned at WaterVocabExpansion AskUserQuestion gate); (d) #8 locales→locations rename remains the lowest-risk next-slice candidate per the architect-recommended pointer (~9 templates, mechanical, no /plan triggers); (e) Layer 2B per-discipline gap reasoning (consume C1's `discipline_id`) still queued; (f) **WaterVocabExpansion-era gap-rule SQL verify-block drift fixed in passing** — the file's row-count assertion (`<>12`) and header comment ("12 canonical gap rows") were stale at 12 after WaterVocab landed 2 new rows (would have RAISED on a clean re-deploy); this slice bumped to 16 to reflect 12 original + 2 WaterVocab + 2 BucketC_g.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_BucketC_i_MapboxRequired_2026_05_24_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Bucket C sub-item (i) Mapbox-anchored race-location REQUIRED shipped end-to-end**. Andy picked Bucket C (i) at the AskUserQuestion gate over #8 locales→locations rename + Bucket C (l) skill toggles + Layer 2B per-discipline gap reasoning. Per Trigger #5 architectural-alternatives plan-mode gate, 3 decisions ratified: D1=ship strict "all races, hard block" (over target-races-only + create-only-hard-update-soft — keeps the requirement uniform across the table + every form surface); D2=pydantic validator + route flash + test-fixture wave (over route-validation-only — defense-in-depth against any non-route writer; backstops the load_race_event_payload path so legacy un-anchored DB rows fail loudly at orchestrator load rather than silently propagating; ceiling break ratified, ~25 fixture updates mechanical); D3=keep onboarding `[Skip]` allowed (over remove-skip — preserves escape valve for athletes who can't find their race in Mapbox; submit path requires Mapbox, skip path is the escape valve). **Contract**: NEW `_check_event_locale_mapbox_id_required` model_validator on `RaceEventPayload` raises ValueError when `event_locale_mapbox_id is None`; route-side flash + redirect added to 4 POST handlers: `routes/race_events.py::new_race` (checks `locale_fields['event_locale_mapbox_id']` from extracted hidden inputs before `create_race_event`), `routes/race_events.py::update_race` (checks `race.get('event_locale_mapbox_id')` from the loaded row — the race-details form doesn't carry Mapbox fields since the standalone `set_locale` POST owns those; legacy un-anchored rows get forced through the picker before any other edits can land), `routes/race_events.py::set_locale` (replaced loose name-OR-mapbox_id check with strict mapbox_id required), `routes/onboarding.py::target_race_save` (checks form-submitted `event_locale_mapbox_id` before create_race_event/update_race_event branch; `target_race_skip` POST untouched per D3). **Backward-compat for legacy un-anchored rows**: at LOAD time, `load_race_event_payload` raises ValueError via the new validator for any pre-walkthrough row with NULL mapbox_id; this is loud-by-design — the route layer prevents new un-anchored writes, and existing rows must be re-anchored via the picker before they can be edited or load into the orchestrator. Andy's PGE 2026 row is already Mapbox-anchored (per RaceLocaleMapbox slice 2026-05-21) so no migration needed for his data. **3 substantive code files**: (1) `layer4/context.py` — NEW `_check_event_locale_mapbox_id_required` model_validator on `RaceEventPayload` (comment block cites Bucket C (i) ratification + the PR #131 precedent distinction — athlete-input requirement, not external-data-quality); (2) `routes/race_events.py` — Mapbox-required check in `new_race` POST (before `create_race_event`); in `update_race` POST (checks loaded race row, not form); in `set_locale` (tightened loose `OR` to strict `mapbox_id required`); (3) `routes/onboarding.py` — Mapbox-required check in `target_race_save` POST. **9 substantive test files** (ceiling break ratified at gate; precedent BucketE_B2_C1=11+5, RaceLocaleMapbox=13): (4) `tests/test_layer4_race_week_brief.py` — NEW `TestEventLocaleMapboxIdRequired` (5 tests: missing-mapbox-id raises / explicit-None raises / present-mapbox-id accepted / validator-fires-even-when-legacy-slug-present / validator-applies-to-non-target-races-too); `_race_event_payload` helper extended with `event_locale_mapbox_id="poi.test_anchor"`; 2 existing test sites updated; (5) `tests/test_routes_race_events.py` — 3 NEW classes (TestNewRaceMapboxRequired 2 / TestUpdateRaceMapboxRequired 2 / TestSetLocaleMapboxRequired 1) using new `_make_app` + `_RouteFakeConn` substrate (mirrors `tests/test_locales.py::_FakeConn` precedent); (6) `tests/test_onboarding_race_events.py` — NEW `TestTargetRaceSaveMapboxRequired` (2 tests: POST-without-mapbox-id rejected / skip-path-still-works); new `_make_onboarding_app` helper; (7) `tests/test_race_events_repo.py` — `_race_row` fixture default flipped (event_locale_mapbox_id `None → "poi.test_anchor"` + event_locale_name/place_name to non-None placeholders); 1 existing test rewritten in-place (test_load_payload_defaults_mapbox_columns_to_none → test_load_payload_rejects_unanchored_row, pinning the LOAD-side backstop for legacy rows); (8) `tests/test_layer4_orchestrator.py` — `_queue_target_race_event` helper updated (3 keys flipped); 16 inline `RaceEventPayload(...)` constructors updated to add `event_locale_mapbox_id="poi.test_anchor"`; (9) `tests/test_layer4_plan_create.py` — 2 inline updates; (10) `tests/test_layer4_validator.py` — 1 inline update; (11) `tests/test_layer3b_builder.py` — 1 inline update; (12) `tests/test_layer3_cached_wrappers.py` — 1 inline update. **Tests**: container-runnable subset 889 → 901 (+12 net new: 5 validator + 5 routes + 2 onboarding = 12; 0 regressions; 1 rewritten-in-place not counted); ETL `etl/tests/` 139 → 139 unchanged. No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin / layer3 cached-wrappers / layer2a / layer2b surfaces; 12 NL parser smoke + 4 Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. All 3 edited Python files pass `python3 -m py_compile`. **Forward-pointers**: (a) NEW Manual §5.0 walkthrough scenario — 4-step coverage: confirm `/profile/race-events/new` POST without Mapbox pick gets flash + redirect; confirm `/onboarding/target-race` POST without Mapbox pick gets flash + redirect; confirm `[Skip]` on onboarding still works; confirm legacy un-anchored race row at `/profile/race-events/<id>/edit` blocks the race-details Save until athlete picks a Mapbox location via the picker above; (b) Bucket C sub-items remaining open: (g) locale-terrain vs equipment merge (Trigger #3 plan-mode), (l) skill toggles (Trigger #3 + #5 plan-mode); (i) now ✅ closed; (c) #8 locales→locations rename remains the lowest-risk next-slice candidate per the architect-recommended pointer; (d) #2b race-URL LLM site-parse pre-fill remains queued; (e) Bucket D legacy hardcoded locales (depends on Bucket C terrain-vocab decisions); (f) Layer 2B per-discipline gap reasoning (consume C1's `discipline_id`) still queued; (g) `routes/race_events.py::set_locale` previously accepted name-without-mapbox_id as a loose fallback — that path is now closed; if athletes have un-Mapbox-pickable locations (very rare in practice given Mapbox's coverage), the only escape valve is the onboarding `[Skip]` (no equivalent for `/profile/race-events/<id>`); if this surfaces as a real friction point, the future fix is a Mapbox `[Manual entry]` button that writes a synthetic `event_locale_mapbox_id=manual.<slug>` token, but no current evidence justifies it.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_BucketE_B2_C1_2026_05_24_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Bucket E.(b)-B2 (included_discipline_ids race-level filter) + E.(c)-C1 (per-row terrain discipline coupling) shipped, closing Bucket E end-to-end**. Andy picked the deferred-from-BucketE_TerrainNone_FrameworkSport-R3-split follow-on at the AskUserQuestion gate; ratified the ~12-file ceiling break + Bootstrap checkbox grid for B2 + auto-clear-with-flash on framework_sport change + client-side JS fetch on framework_sport input change (via new `/profile/race-events/disciplines/search` JSON endpoint). **B2 contract**: NEW `race_events.included_discipline_ids TEXT[] NULL` column; `RaceEventPayload.included_discipline_ids: list[str] | None` field; NEW `discipline_id_filter: list[str] | None = None` kwarg on `q_layer2a_discipline_classifier_payload` (when supplied, post-filters the bridge-derived discipline list to just the explicit IDs while preserving rationale + phase_load + training_gap on surviving rows); orchestrator threads `target_race_event.included_discipline_ids` → `discipline_id_filter` in `_upstream_full_cone`. **C1 contract**: NEW `RaceTerrainEntry.discipline_id: str | None = None` field; per-row form parser captures it via `race_terrain[N][discipline_id]`; backward-compat (existing rows have `discipline_id=None` meaning "race-wide"); Layer 2B currently passes the field through without behavior change (per-discipline gap reasoning is a Trigger #1 prompt-body update for a future slice). **Form UX**: B2 renders as a Bootstrap checkbox grid (`col-md-6 col-lg-4`) keyed on the bridge query for the effective framework_sport (race override → athlete primary_sport fallback per `_resolve_effective_framework_sport`); C1 renders as a per-row `<select data-discipline-select="1">` in the shared `_race_terrain_editor.html` partial with a "Race-wide" empty option as default. Inline CSP-nonced JS picker on both surfaces (race-event-edit + onboarding/target-race) debounces the `framework_sport` input by 350ms and rebinds BOTH the discipline checkbox grid AND every `[data-discipline-select]` per-row terrain select via the new JSON endpoint, preserving currently-checked/selected IDs that survive the new bridge set. **Auto-clear policy**: when `prior_framework_sport != new_framework_sport` AND `prior_included_discipline_ids` is non-NULL, the route layer clears the column to None + flashes "Sport override changed — your discipline picks were cleared. Re-select them for the new sport." Avoids the UI/runtime mismatch silently-drop alternative. **Cache eviction**: NEW `evict_on_target_event_included_discipline_ids_change` helper routes through `layer2a` policy (same as framework_sport — both reshape Layer 2A's discipline output); fires standalone when athlete edits the discipline grid without touching framework_sport, subsumed by the framework_sport branch when that's what drives the change. **11 substantive code/template files + 5 test files** (ceiling break ratified at AskUserQuestion gate; precedent BucketE_TerrainNone_FrameworkSport=10, RaceLocaleMapbox=13): (1) `init_db.py` — ALTER TABLE adds `included_discipline_ids TEXT[] NULL`; (2) `layer4/context.py` — `RaceTerrainEntry.discipline_id` + `RaceEventPayload.included_discipline_ids`; (3) `race_events_repo.py` — load_race_event_payload SELECT extended + raw_disc_filter hydration with list-coerce; get_race_event SELECT + result coerce; create_race_event kwarg + INSERT column + `?::text[]` cast + params threading; update_race_event kwarg + UPDATE SET + params; (4) `race_events_invalidation.py` — `evict_on_target_event_included_discipline_ids_change` helper (mirrors framework_sport precedent; same `layer2a` policy); (5) `layer2a/builder.py` — `q_layer2a_discipline_classifier_payload` gains `discipline_id_filter` kwarg + post-filter loop on raw_rows; (6) `layer4/orchestrator.py` — `_upstream_full_cone` threads `target_race_event.included_discipline_ids` → `discipline_id_filter`; (7) `routes/race_events.py` — NEW `_parse_discipline_id_filter(form)` helper using `form.getlist`; NEW `_disciplines_for_framework_sport(db, framework_sport)` bridge-keyed lookup; NEW `_resolve_effective_framework_sport(db, uid, race)` (race override → athlete primary_sport); NEW `disciplines_search` JSON endpoint (`/profile/race-events/disciplines/search?framework_sport=...`); `_parse_race_terrain` extended for per-row `discipline_id`; new_race POST + update_race POST + edit_race GET + new_race GET all thread the new field; auto-clear logic + eviction wiring in update_race; (8) `routes/onboarding.py` — mirror (`_parse_race_terrain` C1 extension + `_parse_discipline_id_filter` mirror; `_get_target_race_row` SELECT extended; `target_race` GET threads discipline_choices + initial_framework_sport via imported helpers; `target_race_save` POST adds auto-clear + eviction); (9) `templates/_race_terrain_editor.html` — restructured row layout `col-md-5 / col-md-2 / col-md-3 / col-md-2` for terrain/pct/discipline/remove; per-row `<select data-discipline-select="1">` with "Race-wide" empty option; the existing add/remove JS handles new column unchanged via `__IDX__` template; (10) `templates/profile/race_event_edit.html` — B2 checkbox grid block between framework_sport input + race_rules_summary textarea (3-col grid); inline CSP-nonced JS at end of content block rebinds both the discipline grid AND all per-row terrain discipline selects on framework_sport input change via the JSON endpoint with 350ms debounce; (11) `templates/onboarding/target_race.html` — mirror; same B2 checkbox grid + identical inline JS picker. **5 test files extended**: `tests/test_race_events_repo.py` +8 in NEW `TestIncludedDisciplineIdsOverride`; `tests/test_race_events_invalidation.py` +3 in NEW `TestIncludedDisciplineIdsChange`; `tests/test_layer4_orchestrator.py` +2 in NEW `TestIncludedDisciplineIdsOverride`; `tests/test_layer2a.py` +5 in NEW `TestDisciplineIdFilter`; `tests/test_routes_race_events.py` +13 across 4 new classes (TestParseDisciplineIdFilter 4 / TestParseRaceTerrainDisciplineId 3 / TestDisciplinesForFrameworkSport 2 / TestResolveEffectiveFrameworkSport 4); `tests/test_onboarding_race_events.py` 3 existing tests updated-in-place to include `discipline_id: None` in expected output dicts. **Tests**: container-runnable subset 859 → 889 (+30 net new); ETL `etl/tests/` 139 → 139 unchanged. No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin / layer3 cached-wrappers / layer2a / layer2b surfaces; 12 NL parser smoke + 4 Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. All edited Python files pass `python3 -m py_compile`; all 3 edited Jinja templates parse cleanly via `Environment(loader=FileSystemLoader('templates'))`. **D-decisions ratified at AskUserQuestion gate**: D1=ship as one slice (~12-file ceiling break) over split-into-two-slices + B2-only-defer-C1; D2=Bootstrap checkbox grid for B2 (over `<select multiple>` native + bootstrap-select 3rd-party JS lib); D3=auto-clear with flash on framework_sport change (over silent-drop or hard-block-with-validation-error); D4=client-side JS fetch on framework_sport input change (Andy's pick over server-side save-and-reload — better UX justifies the inline JS surface + JSON endpoint with CSP nonce per PR #126 lessons). **Forward-pointers**: (a) Layer 2B per-discipline gap reasoning is the Trigger #1 prompt-body update that consumes C1's `discipline_id` — currently passes through unused; future slice; (b) the `discipline_id_filter` post-filter is the minimal contract — future could add filter-state awareness (e.g. surface a coaching flag when the athlete's pick excludes a bridge-included discipline that the race format would normally auto-include); (c) NEW 3-step Manual §5.0 walkthrough scenario added — B2 checkbox grid render on race-edit + onboarding step-3c + JS rebind on framework_sport change + Andy's PGE 2026 row picks + Layer 2A discipline_id_filter threading + C1 per-row discipline coupling; (d) Bucket E now FULLY CLOSED: (a) ✅, (b)-B1 ✅, (b)-B2 ✅, (c)-C1 ✅; (e) Bucket C still open: (g) terrain↔equipment merge (Trigger #3 plan-mode), (i) Mapbox-required (Trigger #5), (l) skill toggles (Trigger #3 + #5 plan-mode); (f) #8 locales→locations rename remains the lowest-risk next-slice candidate per the architect-recommended-since-RaceLocaleMapbox pointer.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_WaterVocabExpansion_2026_05_24_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Bucket C sub-item (f) water-vocab 5-row expansion shipped**. Andy picked "Bucket C (f) water-type design" at the AskUserQuestion gate after the predecessor TerrainVocabAuditClosure slice's §6.2 alt-pivot menu. The plan-mode design pass per Trigger #2 (vocab padding refusal) + Trigger #5 (architectural alternatives) traced 4 candidate splits against the existing 4-row shape (Pool / Flat Water collapsed-with-slow-river / Open Water-Ocean collapsed-with-large-lake / Whitewater) and Andy ratified a 5-row split: Pool (TRN-008, unchanged) + Flat Water (TRN-009, retightened to still-water-only — lake/reservoir/pond, drops "or slow river") + **NEW Moving Water (TRN-017)** for river-current handling below Class II (ferry angles, eddy reads, current navigation) + Ocean / Tidal (TRN-010, **renamed from "Open Water / Ocean"**, retightened to saltwater/tidal water only — drops "large lake" which now belongs to TRN-009 + adds explicit salt/cold/swell framing) + Whitewater (TRN-011, unchanged). Closes Andy's PGE 2026 misclassification problem (packraft on the Cannon River was previously classified as TRN-009 → Pool covers 75%, which obscured the real river-current skill demand). **Coached-intro pushback ratified separately**: Andy flagged that capability gating should NOT be a derived terrain property; the v1 §8.2 `requires_coached_introduction` flag firing off `prescription_note` keyword-match is the wrong architectural home. The correct home is athlete-side skill toggles mirroring the `sport_specific_gear_toggles` pattern: default OFF (opt-in like equipment availability), captured during onboarding, narrow scope (climbing-related + whitewater + swim ability). Carried as NEW Bucket C sub-item (l) with both Trigger #3 (cross-layer schema: NEW `layer0.skill_toggles` table + `Layer2BInput.skill_toggle_states` extension + new athlete-side capture surface) + Trigger #5 (architectural alternatives: assume-not-skilled-opt-in confirmed; vocab scope narrow not vocab-wide) pre-pinned for the next session's plan-mode gate. **This slice preserves existing TRN-011 whitewater coached-intro semantics exactly as-is** — no regression to the §8.2 flag-firing surface; the skill-toggle slice will rework the flag semantic across the whole vocab once it lands. **4 substantive files (well under 5-file ceiling)**: (1) `etl/layer0/extractors/vocabulary.py` — `_TERRAIN_STRUCTURED_ROWS` gains NEW TRN-017 row (canonical_name=Moving Water, category=Water, requires_elevation=False, technical_surface=False, environment=Outdoor, simulatable=partial, simulation_note flags flat-water as best substitute with current-reading/ferry/eddy gap + whitewater as over-cover, notes=Rivers/current-driven channels/tidal flats below Class II); TRN-009 row retightened in place (notes drops "or slow river", adds "Still water — lake, reservoir, pond. No perceptible current."; simulation_note drops "mild current navigation" since TRN-009 is now still-water-only); TRN-010 row renamed `canonical_name="Open Water / Ocean"` → `"Ocean / Tidal"`, notes drops "large lake" reference (which migrates semantically to TRN-009 per the new still-water framing) and adds explicit salt/cold-shock/swell/horizon-sighting framing, simulation_note explicit salt + cold + swell; (2) `etl/sources/populate_terrain_gap_rules.sql` — 2 NEW gap rules (TRN-017 → TRN-009 proxy fidelity 0.65 `medium` band [flat-water aerobic + stroke covers; current/ferry/eddy uncovered; 2-4 supervised river sessions before race]; TRN-017 → TRN-011 proxy fidelity 0.85 `low` band [whitewater over-covers current handling; small aerobic-volume shortfall only]); existing TRN-010 rule's target_terrain_name renamed 'Open Water / Ocean' → 'Ocean / Tidal' to match canonical; file-level enum doc comment refreshed (was stale 3-band {bridgeable/partial/unbridgeable}, now reflects post-Phase-2.3 5-band {low/medium/high/critical/unbridgeable} with band thresholds documented + audit-trail note that existing 'partial' rows are reclassified at deploy time by `_PG_MIGRATIONS` UPDATE and new rows post-2026-05-19 should use the post-reclassification value directly); existing TRN-011 whitewater rule untouched per the coached-intro preservation policy; (3) `etl/sources/populate_discipline_technique_foci.sql` — TF-015 (Moving-water paddle technique: brace/ferry/eddy turn) row's `terrains_addressed` extended `['TRN-011']` → `['TRN-011','TRN-017']` (the row's literal description names ferry angles + eddy turns which are exactly the TRN-017 skill demands; was previously only attached to whitewater); the other 7 water-keyed foci rows (TF-014 flatwater paddle stroke / TF-016 Eskimo roll / TF-018 whitewater line reading / TF-019 multi-person raft / TF-020 surf zone entry / TF-023 OW swim sighting / TF-024 SwimRun water entry) all checked + correctly stay as-is — each row's semantic literal matches the specific water-row(s) it currently targets; (4) `tests/test_bucket_c_terrain_vocab_audit.py` — `TestCanonicalTerrainVocab` row-count assertion 16 → 17 + sequence assertion `range(1,17)` → `range(1,18)`; class docstring updated to reflect 17-row shape and reason for bump; NEW `TestWaterRowExpansion` class (6 tests: water category has 5 rows / TRN-017 row exists with expected attributes / TRN-010 renamed to "Ocean / Tidal" + old name absent from canonical / TRN-009 notes no longer mention "slow river" + now mention "still water" / TRN-017 simulation_note + notes don't leak coached-intro language (forward-compat with Bucket C (l) skill-toggle pivot) / water environment split unchanged = TRN-008 Indoor + 4 Outdoor rows); module docstring forward-pointer list updated (mark (f) ✅ closed; add (l) skill-capability toggles with Trigger #3 + #5 pinned). **Bonus file** (substantive but trivially mechanical, count would be 5 under ceiling): `etl/tests/test_vocabulary_md.py` — `test_terrain_count` 16 → 17 + `test_terrain_ids_unique_and_sequential` `range(1,17)` → `range(1,18)` + sorted-before-comparison defensive fix (the existing assertion required input-order = sorted order, which is fragile against future appends; my TRN-017 placement put it between 009 and 010 in source order to keep the Water-group reading naturally) + `test_terrain_known_canonical_names_present` adds "Flat Water" + "Moving Water" + "Ocean / Tidal" to the required-presence list so the rename + new row are mechanically locked in. **Tests**: `tests/test_bucket_c_terrain_vocab_audit.py` 20 → 26 (+6 net new in TestWaterRowExpansion); container-runnable subset 853 → 859 (+6 net: 6 new water-expansion + 0 regressions). ETL `etl/tests/` 139 → 139 unchanged (`test_vocabulary_md.py` 18 tests stay 18; 2 modified-in-place + 1 modified-in-place). No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin / layer3 cached-wrappers / layer2a / layer2b surfaces; 12 NL parser smoke + 4 Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. All edited Python files pass `python3 -m py_compile`. **D-decisions ratified across the design pass**: D1=5-row shape over 4-row-defer + 6-row-with-Ocean-split + Surf-Cold-water-add (Andy's pick — "differentiates enough without going too crazy on options"; Cand 1 Moving-Water split passes Trigger #2 cleanly because pool-proxy for packraft-on-river was actively misleading; Cand 2 Ocean/Tidal rename surfaces salt + cold + tide stimuli explicitly without forcing a new TRN-xxx row; Cand 3 Surf and Cand 4 Cold-Water deferred — Surf hits Trigger #2 hard with no athlete corpus driving it; Cold-Water collides with Layer 2E heat/cold-acclim category); D2=TRN-010 rename to "Ocean / Tidal" is athlete-visible across `_terrain_choices` dropdowns + race-event view text — Andy confirmed; D3=`requires_coached_introduction` flag stays only on TRN-011 today + the new TRN-017 row's prescription_note explicitly does NOT leak coached-intro keywords (forward-compat with skill-toggle pivot); D4=2 new gap rules for TRN-017 (lake + whitewater proxies) with `medium`/`low` enum bands respectively; D5=migration approach for Andy's existing PGE 2026 race_terrain row = manual form re-edit during §5.0 walkthrough (over one-shot UPDATE) — keeps the slice purely additive and lets Andy verify the dropdown rename at the same time; D6=Andy's skill-toggle architectural pushback gets a separate slice as NEW Bucket C sub-item (l) with both Trigger #3 + Trigger #5 pre-pinned; default-state ratified = assume-not-skilled (opt-in mirror of equipment-availability pattern) + vocab-scope ratified = narrow (climbing + whitewater + swim ability only). **Forward-pointers**: (a) Manual §5.0 walkthrough step owed — confirm `/profile/race-events/<andy_pge_id>/edit` terrain dropdown shows 17 options including "Moving Water" and "Ocean / Tidal" (rename), no "Open Water / Ocean" label remains; Andy re-edits PGE 2026 to split the 15% paddle terrain into the right TRN-009/TRN-017 mix (likely all TRN-017 since Cannon River); confirm Layer 2B reclassifies with TRN-017→TRN-009 proxy fidelity 0.65 (vs the previous flat-blur 0.75) and the prescription note now flags river-current practice; (b) NEW Bucket C sub-item (l) skill-capability toggles — replace `requires_coached_introduction` derivation with athlete-side opt-in toggles; specs pre-pinned (default OFF; narrow scope climbing + whitewater + swim ability; mirror `sport_specific_gear_toggles` pattern); next plan-mode-gate session needs to ratify ~6-8 file slice across NEW `layer0.skill_toggles` table + Layer 2B input extension + Layer 1 / onboarding capture surface + Layer 2B classifier rewire; (c) Bucket C now: 8 of 11 sub-items closed (a/b/c/d/e/f/h/j + k via predecessor); 3 still open — (g) terrain↔equipment merge (Trigger #3 plan-mode), (i) Mapbox-required (Trigger #5), (l) skill toggles (Trigger #3 + #5 plan-mode); (d) `aidstation-sources/migrations/populate_terrain_gap_rules.sql` + `aidstation-sources/migrations/migrate_terrain_types.sql` are design-side duplicate copies still on the pre-Phase-2.3 enum shape + pre-rename TRN-010 name — flagged as a doc-sweep nit for the next sweep that touches the migrations dir (not blocking; the canonical running files are `etl/sources/*`); (e) #8 "locales" → "locations" rename (~9 templates, mechanical, lowest-risk next-slice candidate per the architect-recommended-since-RaceLocaleMapbox pointer); (f) Bucket E.(b)-B2 + E.(c)-C1 follow-on slice (specs pinned); (g) Andy's PGE 2026 RouteLocalesAnchorFlags + ETL terrain-vocab-drift Manual §5.0 walkthrough steps still owed.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_TerrainVocabAuditClosure_2026_05_24_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Bucket C terrain-vocab audit closure shipped — sub-items (a)/(b)/(c)/(d)/(e)/(h)/(j) locked-in via audit tests; (f)/(g)/(i) carried as open forward-pointers**. Andy picked "Audit + close (a-e,h) + ship (j)" at the AskUserQuestion gate after the predecessor RouteLocalesAnchorFlags slice. The investigation traced each Bucket C sub-item against on-disk state and confirmed 7 of 10 are already mitigated defensively (5 in `etl/layer0/vocabulary_transforms.py` + 1 in `etl/layer0/extractors/vocabulary.py` canonical vocab shape + 1 in `etl/sources/parse_substitutes.py` aliasing): (a) `Darkness` lives in `SITUATIONAL_TOKENS:97` (discarded during ETL transform), no canonical TRN-xxx row; (b) `Group Riding Environment` lives in `SITUATIONAL_TOKENS:98`, no canonical row; (c) `Partner or Visual Cue` / `Tandem Partner` / `Team` all in `SITUATIONAL_TOKENS:99`, no canonical row mentions partner/tandem/team; (d) `Varied Terrain` recognized by `TERRAIN_TOKENS:90` so it doesn't leak into equipment during ETL transform, but maps to no canonical TRN-xxx (silently dropped at terrain-name lookup); (e) climbing split already shipped with the canonical 16-row vocab — TRN-013 Rock Wall (environment=Outdoor) + TRN-014 Climbing Gym (environment=Indoor) are distinct rows; (h) Trainer/Bike Trainer/Cycling Trainer/Indoor Trainer all aliased to canonical `Bike trainer` equipment via `etl/sources/parse_substitutes.py:58`, no canonical terrain row mentions "trainer" or "bike"; (j) Layer 2B classifier vocab boundary audited end-to-end — every canonical TRN-xxx row passes `layer2b.builder._validate_inputs` without raising `Layer2BInputError`. **2 substantive files (well under 5-file ceiling)**: (1) NEW `tests/test_bucket_c_terrain_vocab_audit.py` — 20 tests across 5 classes locking in the no-drift guarantees (TestCanonicalTerrainVocab 5 — row count=16 / every row TRN-NNN pattern / unique terrain_ids / unique canonical_names / sequential TRN-001..TRN-016; TestSituationalTokensNotTerrain 7 — 5 parametrized membership tests + no overlap with canonical + no partner/tandem/team keyword in canonical; TestLegacyTokensNotCanonical 4 — Varied Terrain in TERRAIN_TOKENS but not canonical / no canonical row mentions "trainer" / no canonical row named "bike"; TestClimbingSplit 3 — outdoor row present at TRN-013 / indoor row present at TRN-014 / Climbing category has both environments; TestLayer2BClassifierVocabBoundary 1 — every canonical terrain_id passes `_validate_inputs`); (2) MODIFIED `tests/test_layer2b.py` — added the `from layer4 import InMemoryCacheBackend` import-order workaround mirroring `tests/test_layer2a.py:26` precedent. **Bonus side-effect**: 18 previously-uncollected Layer 2B classifier behavior tests (TestInputValidation 6 + TestPGEBaseline 1 + TestUnbridgeableAlpine 1 + TestEmptyLocale 1 + TestMultipleProxyRules 1 + TestUnknownTerrainId 1 + TestCoachedIntroFlag 2 + TestCleanBaseline 1 + TestEmptyRaceTerrainLoosen 4) are now collected + passing — the pre-existing layer4/layer2b circular import was blocking them. **Tests**: NEW `tests/test_bucket_c_terrain_vocab_audit.py` 20 passed in 0.32s; container-runnable subset 815 → 853 (+38 net: 20 new audit + 18 newly-collected Layer 2B). ETL `etl/tests/` 139 → 139 unchanged. No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin / layer3 cached-wrappers / layer2a surfaces; 12 NL parser smoke + 4 Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. **D-decisions ratified at AskUserQuestion gate**: D1=Audit + close (a-e,h) + ship (j) (Andy's pick over Mapbox-required-only + Mapbox-required-plus-audit + water-type-expansion-design; lowest-risk scope; closes 7 of 10 sub-items by documenting on-disk closure with mechanically-locked-in tests rather than re-implementing what's already correct); D2=NEW file `tests/test_bucket_c_terrain_vocab_audit.py` rather than extending `tests/test_layer2b.py` (cross-cuts layer2b + etl.layer0 extractors + vocabulary_transforms — the file name documents the closure intent, easy to grep for "Bucket C" in future sessions); D3=add the import-order workaround to `tests/test_layer2b.py` while we're here (unblocks 18 previously-uncollected tests; mechanical 4-line add; same precedent as `test_layer2a.py:26`). **Forward-pointers**: (a) Bucket C sub-item (f) water-type expansion — open, design call. Trigger #2 + #5; current vocab has TRN-008 Pool / TRN-009 Flat Water / TRN-010 Open Water-Ocean / TRN-011 Whitewater; possible expansion candidates Surf / Cold-water / Tidal but each needs vocab-padding-refusal scrutiny (Trigger #2) + simulation_note authoring + terrain_gap_rules row authoring against the 16-row canonical set; (b) Bucket C sub-item (g) locale-terrain vs Outdoor-Terrain equipment-fieldset merge — open, Trigger #3 cross-layer schema (crosses `layer0.terrain_types` vs `layer0.equipment_items`); needs plan-mode gate before implementation; (c) Bucket C sub-item (i) Mapbox-anchored race-location required — open, Trigger #5; `event_locale_name` is still accepted as a free-text fallback at `routes/race_events.py:161`, would need to enforce `event_locale_mapbox_id` non-null at the form-validation boundary across 2 forms + 2 routes (race_events new/edit + onboarding); (d) Bucket E.(b)-B2 + E.(c)-C1 follow-on slice (specs pinned); (e) #8 "locales" → "locations" rename (~9 templates, mechanical, lowest-risk next-slice candidate); (f) Andy's PGE 2026 RouteLocalesAnchorFlags + ETL terrain-vocab-drift Manual §5.0 walkthrough steps still owed.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_RouteLocalesAnchorFlags_2026_05_24_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Coaching-flag emission for missing route_locales start/finish anchors shipped** closing the §6.1 architect-recommended forward-pointer from the predecessor ETLTerrainVocabDriftFix slice (which itself was the §6.1 of the RouteLocalesValidatorHotfix PR #131 hot-fix). PR #131 had loosened `RaceEventPayload._check_route_locales_invariants` to silently accept missing start/finish role anchors (Andy's PGE 2026 captured route_locales with a `transition_area` at sequence_idx=1, no explicit `start` row); the loosen note explicitly flagged "moved to a coaching-flag emission downstream (forward-pointer; not yet wired)." This slice wires it. **Implementation (no /plan triggers — architect-recommended ~1-2 file slice)**: NEW `_emit_route_locales_anchor_observations(race_event_payload) -> list[Observation]` helper in `layer4/race_week_brief.py` next to `_emit_data_gap_observations`. Returns 0-2 `Observation(category='data_gap')` rows when `race_event_payload.route_locales` is non-empty but no entry has `role='start'` and/or `role='finish'`. Anchor presence is by-role-anywhere (not by first/last position) so a list like `[transition_area, start, ..., finish]` correctly satisfies. Wired into the existing `notable_observations.extend(...)` chain in `llm_layer4_race_week_brief` after the `_emit_data_gap_observations` call. Empty `route_locales` short-circuits — that case is already covered by the `kit_manifest_inputs_incomplete_no_route_locales` validator rule + corresponding orchestrator-emitted data_gap observation. **Companion prompt augmentation**: `_render_user_prompt` now appends an explicit `**Note:** no entry has role='start' or role='finish'. Treat the corresponding anchor(s) as unknown — do not infer from first/last sequence_idx position.` line under the Route locales section when anchors are missing. The note only renders when anchors are missing (no rendering noise when anchors are present), and reflects whichever subset is missing (start-only / finish-only / both). Observations are `data_gap` per Layer4_Spec.md §8.7 convention, `elevates_to_hitl=False` (content-quality data gap, not HITL-blocking), evidence_basis cites `Race_Events_D66_Design_v1.md §4.2` + `PR #131 validator loosen 2026-05-23`. **2 substantive files (well under 5-file ceiling)**: (1) `layer4/race_week_brief.py` — NEW `_emit_route_locales_anchor_observations` helper (~35 LOC) + `notable_observations.extend(...)` wire-in (+5 LOC); `_render_user_prompt` route-locales section augmented with missing-anchor `**Note:**` line (+12 LOC); (2) `tests/test_layer4_race_week_brief.py` — NEW `TestRouteLocalesAnchorObservations` class (7 tests: empty list short-circuit / both-anchors-present / start-missing-finish-present / finish-missing-start-present / both-missing / start-anywhere-counts (by-role-anywhere semantic confirmed) / observation text ≤240 chars to honor Observation.text Field constraint); 1 NEW test in `TestObservationEmission` (`test_route_locales_missing_start_anchor_emits_observation` — end-to-end via `llm_layer4_race_week_brief` confirming the helper fires through the orchestrator-side observation chain on Andy's PGE 2026-shaped input); 2 NEW tests in `TestPromptRendering` (`test_route_locales_missing_anchors_note_rendered` — both-missing path; `test_route_locales_missing_start_only_note_rendered` — asymmetric path); 1 existing test (`test_route_locales_rendered_structured`) extended with `assert "no entry has role=" not in prompt` to lock in the "no note when anchors present" guarantee. **Tests**: `tests/test_layer4_race_week_brief.py` 56 → 65 (+9 net new: 7 in NEW TestRouteLocalesAnchorObservations + 1 in TestObservationEmission + 2 in TestPromptRendering; 1 existing extended); container-runnable subset 805 → 815 in ~1.3s (no regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin / layer3 cached-wrappers / layer2a surfaces); ETL `etl/tests/` 139 → 139 unchanged. 12 NL parser smoke tests + 4 prior Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. `layer4/race_week_brief.py` passes `python3 -m py_compile`. **D-decisions ratified informally** (no AskUserQuestion gate — architect-recommended slice with explicit spec): D1=anchor presence is by-role-anywhere not first-position (matches Andy's PGE 2026 case where no `start` row exists anywhere; reflects what the LLM needs to know — "no start was marked" vs "first position isn't start"); D2=observation surface is `notable_observations` via the existing data_gap pattern, not a new `coaching_flags` field on RaceWeekBrief (mirrors `kit_manifest_inputs_incomplete_*` precedent; minimal new surface area); D3=skip when `route_locales` is empty (already covered by the kit_manifest_inputs_incomplete_no_route_locales validator rule — emitting both would be redundant); D4=prompt augmentation lives in the Route locales section already gated on `if race_event_payload.route_locales` (no rendering when no route_locales — matches the empty-list skip semantic); D5=2-file slice (race_week_brief.py + test file; no orchestrator changes since the existing `notable_observations.extend(...)` chain composes everything). **Forward-pointers**: (a) ETL terrain-vocab drift fix manual §5.0 walkthrough still owed — confirm first post-merge prod Neon `etl.layer0.run` writes 16 structured rows + supersedes Andy's manual-fix `0C-v2.0-r2` cleanly; (b) Bucket C still has sub-items (a)-(j) — terrain/locale vocab cleanup design conversation deferred (Trigger #5 + Trigger #2 + Trigger #3); (c) Bucket E.(b)-B2 + E.(c)-C1 follow-on slice (specs already pinned); (d) #8 "locales" → "locations" rename (~9 templates, mechanical, lowest-risk next-slice candidate); (e) `route_locales_missing_*_anchor` Manual §5.0 walkthrough step added to CARRY_FORWARD — verify on next race-week-brief run against Andy's PGE 2026 context that the observation surfaces in `notable_observations` and the prompt note renders.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_ETLTerrainVocabDriftFix_2026_05_24_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **ETL terrain-vocab drift fix shipped (Bucket C sub-item k)** closing the architecture debt surfaced by the predecessor RouteLocalesValidatorHotfix slice's prod walkthrough. **Root cause**: two competing sources of truth for `layer0.terrain_types` — `etl.layer0.run` parsed 15 minimal-shape `(canonical_name, notes)` rows from `Vocabulary_Audit_v2.md` and wrote them tagged `0C-vN`, while `etl/sources/migrate_terrain_types.sql` was a one-shot script that added 7 enrichment columns + 16 structured TRN-001..TRN-016 rows tagged `0C-v2.0-r2`. `insert_versioned`'s supersede sweep matched `LIKE '0C-v%'` (`etl/layer0/db.py:122-130`), so every ETL re-run superseded the migrate script's structured rows. **Fix (Path A1, ratified at AskUserQuestion gate per Trigger #3 cross-layer schema + Trigger #5 architectural alternatives)**: the 16 TRN-xxx structured rows now live code-side in `etl/layer0/extractors/vocabulary.py::_TERRAIN_STRUCTURED_ROWS` (mirroring the Phase 2.4-Prep `_TOGGLE_ALSO_SATISFIES` precedent at vocabulary.py:270-278). `_parse_terrain(text)` returns those 16 rows directly (text arg unused, kept for parser-signature parity); `etl/layer0/run.py` extends the `layer0.terrain_types` insert columns from `["canonical_name", "notes"]` to the full 9-column structured shape; `etl/layer0/schema.sql` bakes the 7 enrichment columns + secondary `UNIQUE (terrain_id, etl_version)` as additive idempotent migrations next to the existing v10 ALTER TABLE block so `apply_schema()` becomes canonical setup. `etl/sources/migrate_terrain_types.sql` tombstoned with a RETIRED header pointing at the code-side replacement (file kept for audit trail). **Re-runs of `python -m etl.layer0.run` are now idempotent for terrain_types**; the predecessor's "do NOT run etl.layer0.run against production Neon" warning is lifted. **4 substantive files (under 5-file ceiling)**: (1) `etl/layer0/extractors/vocabulary.py` — `_TERRAIN_STRUCTURED_ROWS` constant (16 dicts × 9 keys, lifted verbatim from migrate_terrain_types.sql) + `_parse_terrain` body replaced; (2) `etl/layer0/run.py` — terrain insert call extended to 9-column shape; (3) `etl/layer0/schema.sql` — 7 `ADD COLUMN IF NOT EXISTS` + `DO $$` block for `UNIQUE (terrain_id, etl_version)` constraint; (4) `etl/tests/test_vocabulary_md.py` — 1 deleted Section-K test (audit terrain block no longer the source); 3 new tests (`test_terrain_ids_unique_and_sequential` for TRN-001..TRN-016 sequence; `test_terrain_known_canonical_names_present` for 9 expected names; `test_terrain_structured_fields_populated` for shape + value-set assertions); `test_terrain_count` count flipped 15 → 16. **Tests**: `etl/tests/test_vocabulary_md.py` 15 → 18 in 0.07s; container-runnable subset 805 → 805 in ~1.5s (no regressions on non-etl Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin / layer3 cached-wrappers / layer2a surfaces). 12 NL parser smoke tests + 4 prior Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. **D-decisions ratified**: D1=Path A1 code-side constants (Andy's pick at AskUserQuestion gate over A2-promote-to-markdown-table + A3-run-migration-SQL-as-post-step + B-skip-list — A1 mirrors existing Phase 2.4-Prep precedent, single source of truth, terrain attributes like `simulatable='partial'` + 2-sentence `simulation_note` are closer to code than narrative markdown). **Forward-pointers**: (a) on first post-merge prod Neon `etl.layer0.run`, watch the `layer0.terrain_types` row count + verify `SELECT COUNT(*) FROM layer0.terrain_types WHERE superseded_at IS NULL AND terrain_id IS NOT NULL` returns 16 + `WHERE terrain_id IS NULL AND superseded_at IS NULL` returns 0; (b) Bucket C still has sub-items (a)-(j) — terrain/locale vocab cleanup design conversation deferred (Trigger #5 + Trigger #2 + Trigger #3); (c) coaching-flag emission for missing start/finish anchors (companion to PR #131); (d) Bucket E.(b)-B2 + E.(c)-C1 follow-on slice (specs already pinned); (e) #8 "locales" → "locations" rename (~9 templates, mechanical).

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_RouteLocalesValidatorHotfix_PR129Validation_2026_05_23_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **production walkthrough surfaced a NEW 500 on `/plans/v2/new` blocking plan-create entirely; route-locales validator hot-fix shipped via PR #131** (`da8905f`) loosening `RaceEventPayload._check_route_locales_invariants` to silently accept missing start/finish role anchors. Bug 2's original `IndexError: tuple index out of range` from PR #129 no longer fires (the `%%WEEKLY TOTAL%%` escape worked); the new failure mode is `pydantic_core.ValidationError: RaceEventPayload.route_locales first entry must have role=='start' when route_locales non-empty` — Andy's PGE 2026 target race has route_locales captured with a `transition_area` row at the lowest `sequence_idx`, not an explicit `start` anchor. **D1 ratified at AskUserQuestion gate**: loosen the validator (silent accept missing anchors) rather than fix Andy's data in Neon — content-quality concerns about start/finish coverage belong on coaching-flag emission downstream, not at the payload-construction boundary; data fix is brittle + pushes the failure mode onto every new athlete. **Bucket B (PR #129) production-walkthrough closed end-to-end**: Bug 1 (locale-delete FK cascade) ✅ confirmed in prod; Bug 2 ✅ first 500 fixed by PR #129, new 500 fixed by this slice's PR #131; Bug 3 ✅ confirmed in prod on BOTH shared-profile path (chisenhall_mtb_trailhead) AND legacy private-residence path (1206_buck_avenue) — root cause CONFIRMED as psycopg2 array-adapter drift, defensive `?::text[]` cast is the right fix. **Manual Neon data fix required for Bug 3 retest**: Andy's terrain_types vocab was in a broken state — `migrate_terrain_types.sql` had run May 9 (inserting 16 structured TRN-xxx rows at v2.0-r2) but subsequent `python -m etl.layer0.run` invocations on May 10 + May 20 re-introduced the workbook-shape legacy rows (no terrain_id) at v1.3.1 + v2.4-prep and superseded the structured ones. Recovered via surgical `UPDATE layer0.terrain_types SET superseded_at = NULL WHERE etl_version = '0C-v2.0-r2' AND terrain_id IS NOT NULL` + `UPDATE ... SET superseded_at = NOW() WHERE terrain_id IS NULL AND superseded_at IS NULL`. **This surfaces real Layer 0 / ETL architecture debt**: the migrate script is a one-shot corrective that subsequent ETL runs undo. Folded into Bucket C as new sub-item (k); needs Trigger #3 + #5 design pass. **2 substantive files (well under ceiling — hot-fix slice)**: (1) `layer4/context.py` — `_check_route_locales_invariants` validator: kept structural checks (sequence_idx uniqueness + ascending sort); removed start/finish role-anchor raises; comment block documents the loosen + flags the downstream coaching-flag emission follow-on; (2) `tests/test_layer4_race_week_brief.py` — 2 existing tests renamed + flipped from `..._rejected` (`with pytest.raises(ValueError, ...)`) to `..._accepted` (plain construction + assertion). **Tests 1458 → 1458 (net-zero — 2 tests renamed/flipped); container-runnable subset 805 → 805 in ~2.2s.** No regressions. **D-decisions ratified**: D1=loosen validator (Andy's pick over data fix). **Forward-pointers**: (a) coaching-flag emission downstream for missing start/finish anchors — ~1-2 file slice; (b) ETL terrain vocab drift fix — Bucket C sub-item (k) needs Trigger #3 + #5; (c) **do NOT run `python -m etl.layer0.run` against production Neon until the ETL drift fix lands** — Andy's manual data fix would be undone.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_BucketE_TerrainNone_FrameworkSport_2026_05_23_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Bucket E.(a) terrain-dropdown defensive filter + E.(b)-B1 per-race framework_sport override shipped**. Andy's second-pass walkthrough Bucket E carried 3 sub-items (a "None" prepending on terrain dropdown, b disciplines on race event creation, c terrain↔discipline coupling); this slice closes (a) end-to-end + (b)-B1 (framework_sport override; B2 included_discipline_ids deferred to follow-on slice paired with C1 per R3 scope-split at the AskUserQuestion gate). **(a) Defensive `_terrain_choices` filter** — added `AND terrain_id IS NOT NULL` to the 3 mirrored `SELECT FROM layer0.terrain_types` queries (`routes/race_events.py`, `routes/onboarding.py`, `routes/locales.py`). Root cause hypothesis: `migrate_terrain_types.sql` (standalone ETL script that supersedes pre-r2 terrain rows + inserts the 16 TRN-xxx-keyed rows) hasn't run on production Neon; the SELECT returned pre-r2 rows where `terrain_id IS NULL`, which the template renders as `None — <name>`. Defensive filter keeps the dropdown clean regardless of migration state — §5.0 walkthrough step 1 verifies via `SELECT COUNT(*) FROM layer0.terrain_types WHERE terrain_id IS NULL AND superseded_at IS NULL = 0` whether the underlying migration still needs to run. **(b)-B1 framework_sport override** — `race_events.framework_sport TEXT NULL` migration; `RaceEventPayload.framework_sport: str | None`; `race_events_repo.create_race_event` + `update_race_event` + `get_race_event` + `load_race_event_payload` threaded through; orchestrator `_upstream_full_cone` falls back `target_race.framework_sport → layer1.identity.primary_sport → framework_sport_missing`. Free-text input (mirrors the existing `primary_sport` profile-edit precedent; Layer 2A surfaces `framework_sport_missing` / unresolved if the typed value doesn't match `layer0.sport_discipline_bridge`); placeholder reads `e.g. Adventure Racing, Trail Running, Triathlon`. Cache eviction via new `evict_on_target_event_framework_sport_change` helper in `race_events_invalidation.py` routing through `layer2a` policy — widest cut (`_ALL_ENTRY_POINTS + _LAYER3_BOTH`, supersets periodization + brief-only). Route POST flow (both `routes/race_events.py:update_race` + `routes/onboarding.py:target_race_save`) emits eviction on actual change only (`prior_framework_sport != new`); the eviction chain is `framework_sport > periodization > brief-only` so the framework_sport branch is fired first as a no-op gate (the wider eviction subsumes the narrower ones if both would fire). **10 substantive files** (R3 scope, ratified ceiling break at AskUserQuestion gate; precedents RaceLocaleMapbox=13, Phase 5.2 caller-side D63+PlanCreate=9, LogThis+T1Hook=7): (1) `init_db.py` — `ALTER TABLE race_events ADD COLUMN IF NOT EXISTS framework_sport TEXT NULL`; (2) `race_events_repo.py` — `create_race_event` + `update_race_event` kwarg threading + INSERT column / VALUES placeholder added at position 19 (between race_url and etl_version_set, preserves positional stability of all earlier params per the `_FakeConn` test precedent); `load_race_event_payload` SELECT extended + `RaceEventPayload(framework_sport=race_row['framework_sport'])` passthrough; `get_race_event` SELECT extended; `update_race_event` UPDATE clause extended; (3) `race_events_invalidation.py` — NEW `evict_on_target_event_framework_sport_change(db, user_id, *, cache=None) -> int` helper routing through `evict_on_layer_change(cache, uid, 'layer2a')`; (4) `layer4/context.py` — `RaceEventPayload.framework_sport: str | None = Field(default=None, max_length=100)`; (5) `layer4/orchestrator.py` — `_upstream_full_cone` fallback chain (`target_race.framework_sport → primary_sport → framework_sport_missing`); (6) `routes/race_events.py` — defensive `terrain_id IS NOT NULL` filter on `_terrain_choices` + `_parse_str(request.form, 'framework_sport')` in `new_race` + `update_race` + `update_race_event(..., framework_sport=...)` threading + `framework_sport_changed` diff + eviction call; (7) `routes/onboarding.py` — same `_terrain_choices` filter + `_parse_str_field(request.form, 'framework_sport')` + threading into both `update_race_event` (returning-athlete branch) + `create_race_event` (fresh-target branch) + diff + eviction call + `_get_target_race_row` SELECT extended; (8) `routes/locales.py` — `_terrain_choices` filter only (locale-terrain checkbox grid was the third surface affected by the same "None"-prepending bug); (9) `templates/profile/race_event_edit.html` — `<input type="text" id="framework_sport" name="framework_sport" maxlength="100" placeholder="...">` between race_url and race_rules_summary with helper text "Leave blank to inherit your profile's primary sport"; (10) `templates/onboarding/target_race.html` — mirror input on the onboarding step 3c form. **Tests 1446 → 1458 (+12 net new across 3 extended test files):** `tests/test_race_events_repo.py` +6 in NEW `TestFrameworkSportOverride` class (load_payload populates / defaults to None / create kwarg threaded into INSERT SQL+params / create defaults to None / update kwarg threaded into UPDATE SQL+params / update can clear to None); `tests/test_race_events_invalidation.py` +3 in NEW `TestFrameworkSportChange` class (evicts all 4 entry points + scoped to user + metrics tagged `layer2a`); `tests/test_layer4_orchestrator.py` +3 in NEW `TestFrameworkSportOverride` class (target race override wins / falls back to primary_sport when override unset / override resolves when primary_sport missing — no `framework_sport_missing` raised). Container-runnable subset: 793 → 805 in ~1.8s. **4 D-decisions ratified at AskUserQuestion gate per Trigger #5 (architectural alternatives):** D1=`E.(a) defensive filter + also confirm migration` (Andy's pick over just-confirm-migration + defensive-only — belt-and-suspenders since the dropdown rendering is athlete-facing and the migration state on production Neon is unconfirmed; defensive filter is harmless when migration has run); D2=`E.(b) B3 both B1 + B2` (Andy's pick over B1-only + B2-only + B4-readonly; full override surface); D3=`E.(c) C1 flat extended` (Andy's pick over C2-hierarchical + C3-parallel-column + C4-defer; smallest contract change preserves backward-compat); D4=`R3 split — ship a+B1 now, defer B2+C1 to follow-on slice` (Andy's pick over 14-file ceiling break + R1 11-file + R2 12-file; cleanest single-arc shape — closes the immediately-visible "None"-prepending bug + the simpler framework_sport override in one cohesive slice, defers the multi-select + terrain coupling to a paired follow-on where C1's discipline choices can pull from B2's chosen-discipline-set). No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin telemetry surfaces. 12 NL parser smoke tests + 4 prior Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. All edited Python files pass `python3 -m py_compile`. **Bucket E.(a) + (b)-B1 done; B2 + C1 carried** as the next slice's punch list under `CARRY_FORWARD.md` "Bucket E.(b)-B2 + E.(c)-C1 follow-on slice" entry.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_BucketB_500sBugfixes_2026_05_23_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Bucket B from Andy's second-pass manual walkthrough shipped: 3 production 500s + persistence bugs fixed against Vercel runtime logs + Neon SELECT diagnostics**. Andy paste-shared two function tracebacks + the diagnostic query result (`locale_terrain_ids='{}'` post-save on `chisenhall_mtb_trailhead`). **(a)** plan-gen 500 on POST `/plans/v2/new` root-caused to `layer2a/builder.py:170` `pla.discipline_name NOT LIKE '%WEEKLY TOTAL%'` — bare `%` chars collide with psycopg2's parameter-substitution parser (`IndexError: tuple index out of range`); fixed by escaping to `%%WEEKLY TOTAL%%`. Production-only failure because the test substrate mocks `db.execute` and never hits the psycopg2 parser. **(b)** `/locales/<slug>/delete` 500 — confirmed `psycopg2.errors.ForeignKeyViolation: locale_equipment_user_id_locale_fkey`; `locale_equipment` FK on `(user_id, locale)` has no `ON DELETE CASCADE` (override tables do; equipment doesn't); fixed by clearing `locale_equipment` rows before the parent `DELETE FROM locale_profiles`. **(c)** locale terrain-checkbox persistence — Andy's Neon SELECT confirmed write-side bug (`locale_terrain_ids='{}'` after save). New route-flow integration tests verify the route emits the correct UPDATE statement with the terrain list at the right param index — bug is psycopg2-side rather than route-flow. Defensive fix: explicit `?::text[]` cast on all 3 `locale_terrain_ids` placeholders (1 INSERT in `_edit_legacy_locale`, 2 UPDATEs in `_edit_shared_locale` build-new + inherit branches). Root cause unconfirmed — psycopg2's default list adapter emits `ARRAY[...]` for non-empty lists + `'{}'` for empty lists under direct experiment, both of which should work for a `TEXT[]` column; production-only quirk. The explicit cast forces typing regardless of adapter behavior. **4 substantive files (under ceiling):** (1) `routes/locales.py` — `delete_locale` adds `DELETE FROM locale_equipment` before parent `DELETE FROM locale_profiles` per Bug 1 + `locale_terrain_ids = ?::text[]` cast at all 3 sites per Bug 3 defensive fix; (2) `layer2a/builder.py` — `NOT LIKE '%%WEEKLY TOTAL%%'` `%%` escape per Bug 2; (3) `tests/test_locales.py` — +4 tests across 3 new classes (TestDeleteLocaleClearsEquipmentFirst 2 — confirms `locale_equipment` cleared before `locale_profiles` + legacy enum short-circuits without DELETEs; TestEditLegacyLocaleTerrainPersists 1 — asserts INSERT-ON-CONFLICT receives terrain list at param[4] + `::text[]` cast asserted in SQL; TestEditSharedLocaleTerrainPersists 1 — asserts inherit-path UPDATE receives terrain list at param[1] + `::text[]` cast asserted); (4) `tests/test_layer2a.py` — +1 test TestLoadDisciplinesPercentEscape (asserts `_load_disciplines` SQL contains `%%WEEKLY TOTAL%%` and not bare `%WEEKLY TOTAL%`) + import-order workaround `from layer4 import InMemoryCacheBackend` at top of file mirroring `tests/test_layer3_cached_wrappers.py:30` precedent — unblocks 14 previously-uncollected layer2a tests. 1 existing test in `TestARBaseline` updated to expect `%%` not `%` (the only pre-existing assertion that referenced the literal). 2 D-decisions ratified informally at scope gate: **D1**=ship all 3 bugs in one slice (Andy's pick at AskUserQuestion gate, over wait-for-Bug3-diagnostic + ship-1+2-only — the Bug 3 diagnostic query result landed mid-session); **D2**=defensive `::text[]` cast for Bug 3 rather than waiting on root-cause (cast is harmless when the adapter behaves correctly, decisive if adapter behavior is drifting; root cause may need further Neon-side diagnostic via `pg_typeof + array_length` to confirm). **Tests 1441 → 1446 (+5 net new); container-runnable subset 774 → 793 (+19: 4 new test_locales + 1 new test_layer2a + 14 newly-collected test_layer2a tests).** No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin telemetry surfaces. 12 NL parser smoke tests + 4 prior Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. All edited Python files pass `python3 -m py_compile`. **Bucket B done with defensive caveat on Bug 3 root cause**; Buckets C-E continue as the next punch-list cohort (C = terrain vocab cleanup; D = legacy hardcoded locale wipe; E = race event creation surface fixes). Open follow-on: Bug 3 root cause unconfirmed — if production walkthrough confirms terrain now persists with the `::text[]` cast, root cause is psycopg2 array-adapter drift (production version vs local 2.9.12); if terrain still doesn't persist, deeper investigation needed (column type drift, connection-pooling, request-scoping).

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_V1CoachingRetire_2026_05_21_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Bucket A from Andy's second-pass manual walkthrough shipped: v1 `/coaching/generate` hard-retired + `data-confirm` double-prompt fixed**. Andy's second-pass walkthrough against the freshly-shipped RaceLocaleMapbox surface produced a ~20-item punch list across 5 buckets (plan-gen / location vocab / race-event creation / delete-double-confirm / 500s). Investigation pinned 4 of 6 plan-gen complaints (`Goal finish time` + `Race Day Philosophy` repetitive, hardcoded location dropdown, `Remote control (headless API)` block, no race picker) to the v1 `/coaching/generate` page reachable from `templates/base.html:74`'s stale `Generate with AI` nav link — NOT the v2 `/plans/v2/new` surface shipped earlier same-day. CLAUDE.md strangler-fig sequencing flags v1 coaching as the replace-bucket; the nav link was the active leak. This slice closes Bucket A end-to-end and defers Buckets B-E to follow-on sessions (B = 500s pending Andy's Vercel logs; C/D/E = Trigger #5 design conversations). **5 substantive files (at ceiling; 3 deletions + 2 surgical edits; net -655 LOC):** (1) `templates/base.html` — nav line 74 re-pointed from `coaching.generate` to `plan_create.new_plan` so athletes land on the v2 surface that reads the target race from `race_events` directly, has no hardcoded `TRIP_LOCALE_TYPES`, no headless-API disclosure, no repetitive Goal-Finish-Time / Race-Day-Philosophy boxes; (2) `routes/coaching.py` — `generate()` (135 lines) + `api_generate()` (48 lines) deleted; KEPT `TRIP_LOCALE_TYPES` (still used by /review POST), `athlete_locale_choices` import (still used by /review), `_csrf_exempt` decorator (still applied to /api/review), `_check_api_key` helper (still used by /api/review + /clarify), `/context`, `/review/<plan_id>`, `/api/review`, `/clarify`, `/chat/<plan_id>`, `/preferences`, `/preferences/<pref_id>/delete` routes; module shrinks from 660 to 473 lines; (3) `templates/coaching/generate.html` — DELETED (471 lines including the v1 Multi-sport types lookup, target-event form with text-input race_name/race_date/race_type/race_location/race_disciplines, Event-Day-Goals card with goal_finish_time + goal_splits + goal_checkpoints, Plan-Block card with hardcoded `home`/`hotel`/`partner`/`airport` locale dropdown, Training-Parameters card, the `<summary>Remote control (headless API)</summary>` disclosure block + `<pre><code>POST /coaching/api/generate ...</code></pre>` example, the `/coaching/clarify` `fetch()` pre-flight invocation); (4) `templates/profile/edit.html` — bearer-token wording on the API tab collapsed from "`/coaching/api/generate` and `/coaching/api/review`" to just "`/coaching/api/review`" (sole surviving endpoint); (5) `static/app.js` — added `if (el.tagName === 'FORM') return;` at the top of the click-handler walker loop. Eliminates the double-prompt root cause on `<form data-confirm="..."><button type="submit">` patterns: pre-fix, the click handler walked up from the submit-button to the form, fired `confirm()` once; then the form's submit event handler read the SAME `data-confirm` and fired `confirm()` again. Post-fix the click walker bails at the form boundary and cedes form-level `data-confirm` ownership exclusively to the submit handler. Affected ~20 delete-button sites across plans/list, plans/view (×2), locales/list, locales/form, cardio/list, training/list, body/form, conditions/list, injuries/list, dashboard.html (×4 complete/skip), profile/edit (×3 disconnect/delete-pref/revoke-token), profile/_race_events_tab (×2), profile/race_event_edit (×3), rx/list, onboarding/route_locales, admin/dashboard. 2 D-decisions ratified at AskUserQuestion gate: D1=Hard retire (Andy's pick over Soft-retire-with-302 + Just-nav-repoint — killing the route + template eliminates the surface entirely with no bookmark/direct-URL re-discovery risk; `/coaching/review`, `/coaching/clarify`, `/coaching/api/review`, `/coaching/chat`, `/coaching/preferences`, `/coaching/context` all kept since they're still wired into `plans/view.html` AI-Review buttons + shared by /review's template); D2=JS click walker bails at `<form>` boundary (over removing the click handler entirely — single-line fix preserves `<a data-confirm>` / `<button data-confirm>` patterns while ceding form-level data-confirm to the submit handler exclusively). **Tests 1441 unchanged (no test files directly covered `/coaching/generate` or `/api/generate`; deletion didn't strand any tests); container-runnable subset 774 passed + 12 skipped in ~1.7s — identical to predecessor baseline.** No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin telemetry surfaces. 12 NL parser smoke tests + 4 prior Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. All edited templates parse cleanly via Jinja2 `Environment(loader=FileSystemLoader('templates')).get_template(...)`. `routes/coaching.py` passes `python3 -m py_compile`. Inline-script nonce sweep returns empty (deletion of `templates/coaching/generate.html` removed 3 inline scripts, all already nonce-protected). **Bucket A done end-to-end; Buckets B-E deferred: B = 500s (plan-gen orchestration on `/plans/v2/new` POST, `/locales/<slug>/delete` 500, locale terrain-checkbox persistence bug) pending Andy's Vercel logs; C = terrain vocab cleanup (Time-of-Day not terrain / Social not terrain / Partner-presence not terrain / Generic / Climbing gym-vs-outdoor split / water-type expansion / locale-terrain vs Outdoor-Terrain merge / Cycling Trainer dedup) needs Trigger #5 design pass; D = legacy hardcoded `home`/`hotel`/`partner`/`airport` slot wipe + free-text location field removal (depends on C); E = race event creation surface fixes ("None"-prepending on terrain dropdown + disciplines surfaced on creation + terrain↔discipline coupling) needs Trigger #5.** Open follow-on for the JS fix: no jsdom/playwright harness in this repo, so the double-confirm fix is verified only via manual §5.0 step 5+6.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_RaceLocaleMapbox_2026_05_21_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Race "Event locale" → Mapbox-anchored picker + race_url column shipped** (closes punch-list items #1 + #2a from the 2026-05-21 walkthrough; bundles 2 design moves per the predecessor's §6.1 architect-recommended next slice). The legacy `event_locale_id BIGINT FK to locale_profiles(id)` semantic — saved-locale dropdown — was wrong for race events: a race finish lives at a real-world place (city/state/POI), not at one of the athlete's home/hotel/partner training locales. **13 substantive files** (ceiling break to 9 ratified at AskUserQuestion gate per Trigger #5 architectural alternatives + Trigger #3 cross-layer schema; +4 over the ratified 9 from the picker partial + layer4 brief prompt thread-through + new route-helpers test file + orchestrator-test fixture extension): (1) `init_db.py` — 6 new `ALTER TABLE race_events ADD COLUMN IF NOT EXISTS …` migrations: `event_locale_name TEXT` + `event_locale_mapbox_id TEXT` + `event_locale_place_name TEXT` + `event_locale_lat NUMERIC(9,6)` + `event_locale_lng NUMERIC(9,6)` + `race_url TEXT`; legacy `event_locale_id BIGINT FK` kept nullable for backward-compat with pre-walkthrough rows; (2) `race_events_repo.py` — `create_race_event` + `update_race_event` signatures gain 6 new kwargs; SQL INSERT/UPDATE extended; NEW `update_race_event_locale(db, user_id, race_event_id, *, event_locale_name, event_locale_mapbox_id, event_locale_place_name, event_locale_lat, event_locale_lng)` standalone helper for the inline picker flow on the edit page (clears legacy `event_locale_id` FK on update so the slug-joined fallback in `load_race_event_payload` stops surfacing the stale slug); `get_race_event` + `load_race_event_payload` + `list_athlete_race_events` SELECTs extended; NUMERIC(9,6) lat/lng coerced to `float` on hydration for template arithmetic; (3) `layer4/context.py` — `RaceEventPayload` gains 5 Mapbox fields + `race_url` field with `Field(max_length=...)` + lat/lng `ge/le` bounds; `event_locale_id` semantic comment updated to note the resolution check accepts EITHER legacy slug OR new `event_locale_name`; (4) `layer4/race_week_brief.py` — `_render_user_prompt` adds a `**Race location:** <name>` line sourced from `event_locale_place_name` ?? `event_locale_name` ?? `event_locale_id` (legacy fallback). Without this thread, the LLM had no race-location signal at all and inferred from the event name; (5) `routes/race_events.py` — drops the saved-locale dropdown; NEW helpers `_run_mapbox_search(query) -> (results, error)` (mirrors `/locales/new` GET-side error translation for the 3 mapbox_client failure modes), `_extract_mapbox_locale_from_form(form) -> dict` (defensive blank/non-numeric collapse on 5 Mapbox hidden inputs), `_parse_race_url(form) -> str | None` (trim + 1000-char cap + empty-collapse). NEW route `POST /profile/race-events/<id>/locale/update` (`set_locale`) calls `update_race_event_locale` + fires `evict_on_target_event_brief_field_change` on target rows (NO `evict_on_target_event_locale_change` Layer 2C eviction — race finish anchor doesn't drive athlete equipment resolution). NEW route `GET /profile/race-events/locale/search` (`locale_search`) returns Mapbox results as JSON for the inline picker `fetch()` ({results: [...]} or {error: "..."} or 400 {error: "disclosure_required"}). NEW route `POST /profile/race-events/locale/acknowledge` (`acknowledge_mapbox_disclosure`) shares the disclosure ack with `/locales/new`. `new_race` POST extracts Mapbox hidden inputs via `_extract_mapbox_locale_from_form` + threads through `create_race_event`; `update_race` POST stops accepting locale-related form fields (the standalone `set_locale` endpoint owns that surface on the edit page); (6) `routes/onboarding.py` — drops the saved-locale dropdown helper `_athlete_locale_choices`; `target_race` GET passes `mapbox_acked` + `mapbox_disclosure_version` to the template; `target_race_save` POST imports `_extract_mapbox_locale_from_form` + `_parse_race_url` from `routes.race_events` + threads through `create_race_event` / `update_race_event`; cache-invalidation `locale_changed` branch removed (Mapbox-anchored picker no longer fires Layer 2C eviction), `mapbox_id_changed` rolled into `brief_only_changed` diff; (7) `templates/profile/race_event_edit.html` — drops the saved-locale `<select>`; adds `[race_url]` `<input type="url">`. For `is_new=False`: NEW standalone "Race location" section above race-details form, wrapping the `_race_locale_picker.html` partial in a POST form to `set_locale`. For `is_new=True`: the picker partial renders INSIDE the race-details form so the create POST carries Mapbox hidden inputs through atomically; (8) `templates/onboarding/target_race.html` — drops the saved-locale `<select>`; adds `[race_url]` `<input>`; embeds the `_race_locale_picker.html` partial INSIDE the race-details form so the single-POST onboarding flow carries Mapbox hidden inputs through; (9) NEW `templates/_race_locale_picker.html` (~190 LOC; shared partial). Renders 5 hidden inputs + search UI (Mapbox disclosure card if `not mapbox_acked`; else search box + result-list pane); CSP-nonced inline `<script>` ~80 LOC implements `fetch()` to the JSON endpoint + result-click handler that fills the 5 hidden inputs + shows "Selected: <name>" display + hides search box. Includes a "[Change]" button that clears the UI state (without clearing the saved DB row); (10) `tests/test_race_events_repo.py` — `_race_row(**overrides)` fixture extended with 6 new column defaults; 7 NEW tests in `TestMapboxRaceLocationColumns` class (load_payload populates / defaults Mapbox cols / create + update pass new kwargs / `update_race_event_locale` clears legacy FK + only touches Mapbox cols / get_race_event coerces lat/lng to float); 2 existing tests updated (param-position-shift due to 6 new columns appended to INSERT); (11) `tests/test_onboarding_race_events.py` — drops the `TestAthleteLocaleChoices` class (helper was deleted with the dropdown); replaces with an audit-trail comment; (12) NEW `tests/test_routes_race_events.py` (~210 LOC; 17 tests across 3 classes — `TestExtractMapboxLocaleFromForm` 7 (empty form → all None / extracts all 5 / whitespace trim / blank text→None / non-numeric lat-lng→None / empty lat-lng→None / negative coords pass), `TestParseRaceUrl` 5 (missing / empty / whitespace / trim / 1000-char cap), `TestRunMapboxSearch` 5 (empty short-circuits no Mapbox call / 3 error modes translate to human-readable / happy path); `mapbox_client.search_places` monkeypatched throughout — no real HTTP); (13) `tests/test_layer4_orchestrator.py` — `_queue_target_race_event` fixture extended with 6 new column defaults (all None — pre-walkthrough shape). 6 D-decisions ratified at AskUserQuestion gate before implementation per Trigger #5 (architectural alternatives) + Trigger #3 (cross-layer schema): D1=Option A "full Mapbox mirror" (Andy's pick over text-only + inline-lat-lng-no-search; mirrors `/locales/new` flow); D2=combine #1 + #2a in one slice (Andy's pick over split; tightly-coupled file deltas); D3=ceiling break to 9 substantive files ratified (consistent with recent precedents 5.1.A=8, 5.1.C=8, 5.2.D63+PlanCreate=9, 5.2.LogThis+T1Hook=7, 5.2.D64=8, Walkthrough_PunchList=8); D4=full Mapbox at onboarding too (Andy's pick over simple-text-only-at-onboarding); D5 [implementation]=keep legacy `event_locale_id BIGINT FK` nullable for backward compat (pre-walkthrough rows still load via JOIN); `update_race_event_locale` clears it explicitly on Mapbox-anchored update so stale slug doesn't leak downstream; D6 [implementation]=AJAX `fetch()` from inline nonce-protected `<script>` for the search (over GET round-trip with form-state preservation in query string — URL length is 8KB and `mandatory_gear_text` can be 8000 chars). **Tests 1419 → 1441 (+22 net new across 2 NEW + 2 extended test files): NEW `tests/test_routes_race_events.py` 17 + 7 NEW in `tests/test_race_events_repo.py::TestMapboxRaceLocationColumns` + 2 deleted from `tests/test_onboarding_race_events.py::TestAthleteLocaleChoices` (helper gone); container-runnable subset 752 → 774 in ~1.3s.** No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin telemetry surfaces. 12 NL parser smoke tests + 4 prior Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. All 3 touched templates parse cleanly via Jinja2 `Environment(loader=FileSystemLoader('templates')).get_template(...)`. **#1 race-locale dropdown → Mapbox-anchored picker + #2a race_url column shipped end-to-end across 3 surfaces** (new race / edit race / onboarding step 3c); 3 punch-list items remain deferred (#4 body_part vocab, #6 dynamic movement-constraints, #8 locales→locations rename). Open follow-on: the new Mapbox columns are captured but not yet consumed by Layer 2E heat-acclim (gated on Plan Management spec) or Layer 4 travel-distance reasoning — captured here as forward-pointers in §6.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Walkthrough_PunchList_2026_05_21_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side polish — **Manual walkthrough punch-list fixes shipped** (template-only slice). Andy's manual walkthrough against the freshly-shipped Phase 5.2 caller-side surface produced a 10-item punch list (1 CRITICAL plan-gen 500, 2 HIGH errors, 7 medium/low UX). This slice closes 5 of them plus 2 silently-broken modal flows discovered while diagnosing #3. **8 substantive template files** (over the 5-file ceiling per Andy's explicit "do a few more" override; mechanical bug-fix slice, very small per-file deltas): (1) `templates/_race_terrain_editor.html` — added `nonce="{{ csp_nonce() }}"` to the inline `<script>` that registers the `[+ Add terrain]` click handler. The app's CSP enforces `script-src 'self' 'nonce-...'` with no `'unsafe-inline'`, so the browser was silently blocking the IIFE and the button did nothing. Closes #3; (2) `templates/plans/v2/refresh.html` — same nonce fix on the inline script that auto-opens `#capExceededModal`. Pre-fix the cap-exceeded modal never auto-opened on over-cap re-render (bonus find while diagnosing #3); (3) `templates/workouts/suggestion_view.html` — same nonce fix on the inline script that auto-opens `#t1HookModal` on `?just_logged=1`. Pre-fix the post-log T1 plan-check hook modal never auto-fired (bonus find); (4) `templates/plans/list.html` — replaced the single `[+ Import Plan]` CTA in header + empty-state with a primary `[+ Create with AI]` (linking to `plan_create.new_plan` = `/plans/v2/new`) + Import demoted to `btn-outline-secondary`. Closes #9; (5) `templates/injuries/list.html` — replaced integer-threshold severity badge logic (`>= 4`, `== 3`, `/5`) with enum-aware mapping for the 6-value TEXT enum shipped in D-73 Phase 2.2 (Acute / Post-surgical → `bg-danger`; Recovering / Chronic-Managed / Structural-Permanent → `bg-warning text-dark`; Resolved → `bg-secondary`). Removed `/5` suffix. Closes #5 (`/injuries` 500 on save → list re-render); (6) `templates/coaching/generate.html` — lifted `{% set MULTISPORT_TYPES = [...] %}` from inside `{% block content %}` to template scope (between `{% block title %}` and `{% block content %}`). Pre-fix, the variable was `Undefined` at `{{ MULTISPORT_TYPES | tojson }}` in `{% block scripts %}` due to Jinja block-scoping. Closes #10 (`/coaching/generate` 500); (7) `templates/locales/list.html` — added a `[Delete]` button next to `[Edit]` on each non-legacy locale card in the card header, using the canonical `data-confirm="..."` pattern from `static/app.js`. Closes the surfacing half of #7 (delete UI was previously buried at the bottom of the edit form); (8) `templates/locales/form.html` — converted the existing bottom-of-form delete from `onsubmit="return confirm(...)"` to `data-confirm="..."` so the confirm dialog actually fires through CSP (previously silently blocked, so the form submitted immediately on click); also relabeled button text + dialog copy from "locale" to "location" (small step toward #8). 4 D-decisions ratified informally / by Andy at the punch-list scope gate: D1=bundle 2 silent-fail nonce fixes into the #3 commit (same root cause); D2=slice scope = 5 of 10 punch-list items + 2 bonuses (remaining 5 each need bigger scope or design); D3=`templates/locales/form.html` copy = "location" not "locale" (early move toward #8); D4=list-page Delete button gated `if not is_legacy and p` (mirrors existing route guard). **No Python changes; tests 1419 unchanged; container-runnable subset 752 passed + 12 skipped — identical to predecessor baseline.** All 8 templates parse cleanly via Jinja2 `Environment(loader=FileSystemLoader('templates')).get_template(...)`. **5 punch-list items deferred** as doc-sweep nits with concrete next-slice guidance: #1 race locale → Mapbox lookup (~5-6 files; Trigger #5), #2 race URL + LLM site-parse pre-fill (Trigger #2 prompt design + ~4-6 files runtime), #4 body_part Left/Right vocab cleanup (folded into §B onboarding refresh), #6 dynamic movement-constraints (~3-4 files; Trigger #5 on mapping design), #8 "locales" → "locations" rename (~9 templates; mechanical; no Triggers).

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Layer3_Caching_2026_05_21_Closing_Handoff_v1.md` — D-73 Phase 5.2 — **Layer 3A + 3B caching deployed at the orchestrator level + Layer 3 invalidation policy extended** (closes the §6.2 #3 architect-recommended pivot from the Layer2C_Telemetry handoff). The cached wrappers (`layer3a.cached_wrapper.llm_layer3a_athlete_state_cached` + `layer3b.cached_wrapper.llm_layer3b_goal_timeline_viability_cached`) were shipped per spec §9 at Phase 3.1-Driver / Phase 4 but never wired — the orchestrator called the raw `llm_layer3a_athlete_state` / `llm_layer3b_goal_timeline_viability` drivers uncached at 3 call sites (2 in `_upstream_full_cone`; 1 in `orchestrate_single_session_synthesize`). This slice swaps in the cached wrappers across all 3 sites + extends `_EVICTION_POLICY` in `cache_invalidation.py` so 3A/3B caches participate in upstream-driven eviction explicitly (layer1 + layer2a + layer3a + etl_version_set all evict 3A/3B alongside Layer 4; layer2b/2c/2d/2e/3b leave 3A/3B alone since 3A/3B don't depend on those layers per their §9.1 cache-key formulas). NL parser cache rows now explicitly preserved on Layer 1 / 2A / 3A / ETL eviction (was previously over-evicted by the None-optimization on `_ALL_ENTRY_POINTS` matches; the new 6-tuple policy bypasses the optimization and uses an explicit IN-clause filter). **5 substantive files** (at ceiling): (1) `layer4/orchestrator.py` — import swap (`llm_layer3a_athlete_state` → `llm_layer3a_athlete_state_cached`, same for 3B); `_upstream_full_cone` signature gains `cache: Layer4Cache` kwarg; 2 call sites inside the helper threaded with `cache_backend=cache.backend`; 3 callers (race_week_brief / plan_refresh / plan_create) updated to pass `cache=cache`; `orchestrate_single_session_synthesize` 3A call site swapped to cached wrapper (3B not consumed by single_session per D-63); module docstring + `orchestrate_single_session_synthesize` step-6 docstring updated to reflect cached-wrapper deployment; (2) `layer4/cache_invalidation.py` — new constants `_LAYER3_BOTH` (3A + 3B tuple) + `_LAYER3_B_ONLY` (3B-only tuple); `_EVICTION_POLICY` extended: `layer1 / layer2a / etl_version_set` now `_ALL_ENTRY_POINTS + _LAYER3_BOTH` (6-tuple); `layer3a` now `_ALL_ENTRY_POINTS + _LAYER3_B_ONLY` (5-tuple, since 3B depends on 3A); `layer2b/2c/2d/2e/3b` unchanged (3A/3B don't depend on those upstreams per cache-key §9.1); module docstring rewritten with the extended matrix + a `no*` callout for the layer2c/layer2d over-eviction edge (those policies still match `_ALL_ENTRY_POINTS` so the None-optimization collapses to a user-scoped wipe that incidentally clears 3A/3B/NL_parser — acceptable noise since equipment + injury edits are infrequent and the caches re-warm); (3) NEW `tests/test_layer3_cached_wrappers.py` (~340 LOC; 13 tests across 2 classes — `TestLayer3ACachedWrapper` 6 — miss-then-hit / day-granular as_of intra-day collapse / different user_id distinct keys / different layer1_hash distinct keys / serialization round-trip preserves payload / entry_point label stored as `llm_layer3a_athlete_state`; `TestLayer3BCachedWrapper` 7 — miss-then-hit / event-vs-no-event distinct keys / different race_event_id distinct keys / `section_h2_kwargs` slot distinct keys / `current_date` distinct keys / serialization round-trip preserves payload / entry_point label stored as `llm_layer3b_goal_timeline_viability`). Test file header documents the import-order workaround for the pre-existing layer1/layer4 circular import (force `from layer4 import InMemoryCacheBackend` before touching layer3a/cached_wrapper) + reuses `_fake_layer*_payload()` builders from `tests.test_layer4_orchestrator` rather than duplicating ~200 LOC of pydantic constructors; (4) `tests/test_layer4_orchestrator.py` — mock targets updated at all 10 patch sites: `layer4.orchestrator.llm_layer3a_athlete_state` → `_cached` (4 sites); `layer4.orchestrator.llm_layer3b_goal_timeline_viability` → `_cached` (6 sites); no other test changes needed (existing kwarg assertions like `m_l3b.call_args.kwargs["race_event_payload"]` still hold — cached wrapper takes the same kwargs plus an additional `cache_backend=`); (5) `tests/test_layer4_cache.py` — 4 policy tests updated to reflect layer3 inclusion (`test_policy_layer1/layer2a/layer3a/etl_version_set_all_entry_points` → `_covers_layer4_and_layer3` / `_includes_layer3b`); 1 layer3b test extended to assert disjointness from layer3 set; `test_layer1_evicts_all_user_rows` rewritten as `test_layer1_evicts_layer4_and_layer3_preserves_nl_parser` (count was `len(VALID_ENTRY_POINTS)` = 7; now 6 with NL parser preserved); NEW `test_layer3a_change_evicts_layer3b_preserves_layer3a` (3B evicted on 3A change; 3A's own row left for natural orphan via key change). 2 D-decisions ratified at AskUserQuestion gate before implementation per Trigger #5 (architectural alternatives): D1=scope = all 4 entry points (3A+3B in `_upstream_full_cone`; 3A only in `orchestrate_single_session_synthesize`); D2=extend invalidation policy now (Andy picked over deferring to a follow-on slice — closes the policy-vs-runtime gap end-to-end where 3A/3B's cache rows had no explicit eviction entry despite the runtime-shipped wrappers). **Tests 1405 → 1419 (+14 net new across 1 NEW + 1 extended test file: 13 in NEW `tests/test_layer3_cached_wrappers.py` + 1 net in `tests/test_layer4_cache.py` after rename+rewrite of 4 policy tests + 1 layer3b extension + 1 rewrite of layer1-evicts-all-user-rows + 1 NEW layer3a-evicts-layer3b); container-runnable subset 738 → 752 in ~1.2s.** No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard / admin telemetry surfaces. 12 NL parser smoke tests + 4 prior Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. **3A/3B LLM calls now serve from the shared `layer4_cache` table within a calendar day across multiple Layer 4 entry points; cache participation in upstream eviction is now explicit per the extended §9.3 matrix.** Pre-existing tests in `tests/test_layer3a_builder.py::TestCacheWrapper` (7 tests) + `tests/test_layer3b_builder.py::TestCacheWrapper` (7 tests) remain pre-existing-circular-import-blocked from collection (same as before; the new `tests/test_layer3_cached_wrappers.py` is the canonical living coverage path).

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Layer2C_Telemetry_2026_05_21_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Layer 2C cache-invalidation gap fix + refresh-flow telemetry surface shipped** (closes the longstanding §6.2 #2 + §6.2 #7 forward moves from the TriggeredByAdHoc handoff in one combined slice). (a) `routes/locales.py` Layer 2C invalidation gap: equipment edits at any locale now fire `evict_on_layer_change(cache, uid, 'layer2c')` on actual change (no-op gated) mirroring the existing `_evict_layer2b_on_terrain_change` precedent — Layer 2C policy is `_ALL_ENTRY_POINTS` so the eviction is broader than 2B's `_NON_SINGLE_SESSION` (equipment changes also invalidate cached single-session synthesis). Both legacy + shared (build-new + inherit) branches snapshot the prior equipment set before mutation and compare to the new set after commit. (b) NEW `/admin/telemetry/refresh` aggregate-only view behind `_require_admin()` covering the full D-63 → D-64 funnel across last 30d: ad_hoc_workout_suggestions per-status + logged_rate / t1_hook_telemetry [No, thanks] dismissal count + dismissal-to-log ratio / plan_refresh_log per-tier (T1/T2/T3) count + success + cap_override + parser_degraded + t1_hook_attribution counts/rates + p50/p95 success-only duration_ms. **5 substantive files** (at ceiling): (1) `routes/locales.py` — NEW `_evict_layer2c_on_equipment_change(db, user_id)` helper next to the terrain helper; `_edit_legacy_locale` snapshots `prior_equipment_tags` before the DELETE + fires helper after commit when `set(selected_tags) != prior_equipment_tags`; `_edit_shared_locale` computes `prior_effective_equipment` via `_load_overrides + _effective_equipment` (or empty set when no `shared`) + fires helper on `submitted != prior_effective_equipment` in both build-new and inherit POST paths; (2) `tests/test_locales.py` — extended import + NEW `TestEvictLayer2cOnEquipmentChange` class (2 tests: all 4 entry points evicted per `_ALL_ENTRY_POINTS` policy + cross-user isolation); (3) `routes/admin.py` — NEW `TELEMETRY_WINDOW_DAYS = 30` constant + 4 private aggregate helpers (`_telemetry_window_threshold` with monkeypatch-friendly `now` kwarg; `_percentile` nearest-rank with None-on-empty + clamp; `_aggregate_ad_hoc_suggestions` GROUP BY status; `_aggregate_t1_hook_dismissals` COUNT(*); `_aggregate_plan_refresh_log` per-tier aggregation with p50/p95 on success-only) + NEW `@bp.route('/telemetry/refresh') telemetry_refresh()` handler; (4) NEW `templates/admin/telemetry_refresh.html` — 3 sections (D-63 generation / T1 hook funnel / D-64 per-tier table); (5) NEW `tests/test_routes_admin.py` (~290 LOC; 19 tests across 5 classes — `TestTelemetryWindowThreshold` 2 + `TestPercentile` 6 + `TestAggregateAdHocSuggestions` 4 + `TestAggregateT1HookDismissals` 3 + `TestAggregatePlanRefreshLog` 4). 4 D-decisions ratified at AskUserQuestion gate before implementation per Trigger #5 (telemetry surface architectural alternatives): D1=admin-only at `/admin/telemetry/refresh` behind `_require_admin()` (Andy's pick over per-user /profile/telemetry + both-routes alternatives; no multi-user audience to speculate against); D2=full D-63→D-64 funnel scope across 3 tables (Andy flipped from architect's "plan_refresh_log only" recommendation; broader picture useful even at low traffic); D3=aggregates only — no row-level drill-down (Andy flipped from architect's "aggregate block + recent 50 rows" recommendation; psql is the drill-down path); D4=fixed last 30 days, no URL param (Andy's pick over configurable 7/30/90/all; premature at current traffic). Branch rename per CLAUDE.md branch-naming rule: harness-pinned `claude/v5-phase-5-2-handoff-sK0sI` → `claude/layer2c-invalidation-telemetry` (the harness name didn't reflect scope; the prior session arc already shipped via PR #123). **Tests 1384 → 1405 (+21 net new across 2 extended/new test files: 2 in `tests/test_locales.py` + 19 in NEW `tests/test_routes_admin.py`); container-runnable subset 717 → 738 in ~2.0s.** No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard surfaces. 12 NL parser smoke tests + 4 prior Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. **Layer 2C cache-invalidation doc-sweep nit carried since form-refresh C is now closed end-to-end + the freshly-shipped `cap_overridden` + `triggered_by_ad_hoc_id` telemetry columns + the `t1_hook_telemetry` table + the `ad_hoc_workout_suggestions` status column all now feed an aggregate observability surface ready to receive Andy's Neon walkthrough data.**

**Predecessor:** `V5_Implementation_D73_Phase_5_2_TriggeredByAdHoc_2026_05_21_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **`triggered_by_ad_hoc_id` plumbing shipped** (closes the §6.1 architect-recommended forward move from the FreqCaps handoff — also the longstanding carry-forward doc-sweep nit since LogThis+T1Hook). D-63 §5.4 shipped the `triggered_by_ad_hoc_id BIGINT REFERENCES ad_hoc_workout_suggestions(id)` column on `plan_refresh_log` 2026-05-21 but no caller threaded the FK. The T1-hook anchor on `/workouts/suggestions/<id>` now passes `triggered_by_ad_hoc_id=<suggestion_id>` alongside `nl_context` + `tier`; `/plans/v2/refresh` validates ownership at both GET (prefill) and POST (pre-log) per D1=(b), silently collapsing cross-user/stale/non-numeric values to NULL per D2=(a); the cap-exceeded override mini-form preserves the FK per D3=(a) and all 3 `_write_refresh_log` call sites (success + 2 failure paths) thread it per D4=(a). **4 substantive files** (well under 5-file ceiling): (1) `routes/ad_hoc_workouts.py` — `view_suggestion` adds `triggered_by_ad_hoc_id: suggestion['id']` to the `urlencode({...})` dict producing `t1_hook_refresh_query`; (2) `routes/plan_refresh.py` — `_resolve_prefill` extended to 3-tuple return `(nl_context, tier, triggered_by_ad_hoc_id)` with int-coercion + blank/non-numeric collapse (kept pure for test isolation per D1 separation); NEW `_validate_ad_hoc_id_for_user(db, user_id, ad_hoc_id) -> int | None` (single `SELECT 1 AS ok FROM ad_hoc_workout_suggestions WHERE id = ? AND user_id = ?` — returns ID on match, None on miss/None-input); GET branch validates and threads `prefill_triggered_by_ad_hoc_id` template kwarg; POST branch reads form field with defensive int-coerce + re-validates against current user + threads through cap-exceeded re-render kwargs AND all 3 `_write_refresh_log` calls; `_write_refresh_log` signature gains `triggered_by_ad_hoc_id: int | None = None` kwarg appended after `cap_overridden`; INSERT extended to 15 columns (FK at position 14, after cap_overridden — preserves existing positional test indexes for cap_overridden at 13 + success at 11 etc.); module docstring extended with D-63 §5.4 attribution paragraph documenting GET+POST validation + best-effort collapse + override + failure-path FK threading semantics; (3) `templates/plans/v2/refresh.html` — NEW conditional `{% if prefill_triggered_by_ad_hoc_id %}<input type="hidden" name="triggered_by_ad_hoc_id" value="{{ prefill_triggered_by_ad_hoc_id }}">{% endif %}` block inside both the main form (after csrf_token) and the cap-exceeded mini-form (after the existing `cap_override=1` hidden input); (4) `tests/test_routes_plan_refresh.py` — +8 tests across 2 extended classes + 1 new class: existing 7 `TestResolvePrefill` tests updated to unpack 3-tuple; +3 added (`test_triggered_by_ad_hoc_id_int_coerced` + `test_triggered_by_ad_hoc_id_blank_collapses_to_none` + `test_triggered_by_ad_hoc_id_non_numeric_collapses_to_none`); existing `test_t1_hook_full_pattern` extended to pass FK string and assert int coercion; NEW `TestValidateAdHocIdForUser` (3 tests — None input no-DB-roundtrip + happy path with queued row + None on miss); `TestWriteRefreshLog` extended with `test_triggered_by_ad_hoc_id_defaults_to_none` (asserts column name in SQL + params[14] is None) + `test_triggered_by_ad_hoc_id_passed_through` (params[14] == 987). 4 D-decisions ratified at AskUserQuestion gate before implementation per Trigger #5 (per-flow plumbing approach): D1=user-scope validation at both GET (prefill) and POST (pre-log) (Andy's pick over POST-only + skip-validation; defense-in-depth — form hidden field never carries a bad value into the cap-exceeded mini-form re-render or the underlying `_write_refresh_log` INSERT); D2=silent drop to None on validation failure (Andy's pick over reject-POST-with-flash; telemetry is best-effort and the refresh itself is legitimate); D3=cap-exceeded override mini-form carries the FK through (Andy's pick over drop-on-override; attribution survives the override since the override is athlete-driven on the same underlying ad-hoc trigger); D4=both failure-path `_write_refresh_log` calls thread the FK (Andy's pick over success-only; attribution is independent of success — a failed refresh was still triggered by this ad-hoc log). Branch rename per CLAUDE.md branch-naming rule: harness-pinned `claude/implement-freq-caps-phase-5-ma6n9` → `claude/triggered-by-ad-hoc-id` (freq caps already shipped to main; mid-arc-same-surface justification doesn't apply). **Tests 1376 → 1384 (+8 net new in 1 extended test file; container-runnable subset 709 → 717 in ~1.6s).** No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard surfaces. 12 NL parser smoke tests + 4 prior Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. **D-63 §5.4 attribution column now populated end-to-end from the T1 hook click-through; the longstanding caller-doesn't-thread-the-FK doc-sweep nit is closed.**

**Predecessor:** `V5_Implementation_D73_Phase_5_2_FreqCaps_2026_05_21_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Refresh frequency caps per D-64 §8 shipped** (closes the §6.1 architect-recommended forward move from the LogThis+T1Hook handoff). T1 ≤3/24h, T2 ≤1/48h, T3 ≤1/7d enforced server-side on `/plans/v2/refresh` POST via `_check_frequency_cap`, counting `success=TRUE` rows in `plan_refresh_log` per tier window. Over-cap submissions re-render the form with an auto-opening Bootstrap `#capExceededModal` (mirroring the predecessor session's `#t1HookModal` pattern); modal body reads "You've already refreshed N times in the last X hours. Each refresh re-runs the cascade and costs compute. Continue?" with [Cancel] (`data-bs-dismiss`) + [Refresh anyway] (separate mini-form posting hidden `cap_override=1` + `submit_t<n>=1` + preserved `nl_context`). Override is **single-use** per D3 — each subsequent submit re-checks the cap independently. **4 substantive files** (well under 5-file ceiling): (1) `init_db.py` — `ALTER TABLE plan_refresh_log ADD COLUMN IF NOT EXISTS cap_overridden BOOLEAN NOT NULL DEFAULT FALSE` per D-64 §7.1; (2) `routes/plan_refresh.py` — new `_TIER_CAP_LIMITS: dict[str, tuple[int,int]]` constant (T1=(3,24), T2=(1,48), T3=(1,168)) + 2 new helpers (`_count_recent_refreshes(db, user_id, tier, *, window_hours, now=None) -> int` issues `SELECT COUNT(*) … WHERE user_id + tier + success=TRUE + triggered_at >= now-window`; `_check_frequency_cap(db, user_id, tier, *, now=None) -> tuple[bool, int]` returns (count >= limit, count)); POST flow gains cap-check after `_parse_tier`, re-renders template with `cap_exceeded={tier, count, window_hours}` kwarg when exceeded + no override; `cap_overridden = exceeded AND cap_override` resolved per D4 (stale forms / direct-curls with `cap_override=1` against under-cap window land `cap_overridden=FALSE`); `_write_refresh_log` signature extended with `cap_overridden: bool = False` kwarg threaded into the INSERT (failure-path rows keep default FALSE — failures never count as a successful override of the gate); module docstring rewritten — deferred-caps paragraph replaced with the enforcement summary; (3) `templates/plans/v2/refresh.html` — NEW conditional `#capExceededModal` markup at end of plan-exists branch with 3-line inline `<script>` auto-open block (mirrors `#t1HookModal` precedent); (4) `tests/test_routes_plan_refresh.py` — +13 tests across 3 new classes + 2 added to existing `TestWriteRefreshLog`: `TestTierCapLimits` (3 — pins T1/T2/T3 tuple values) + `TestCountRecentRefreshes` (4 — returns count + zero on no-row + filters user/tier/success in SQL + default now uses UTC) + `TestCheckFrequencyCap` (4 — under cap + at-cap-is-exceeded + T2-at-cap + T3-window-hours-threaded); `TestWriteRefreshLog` extended with `test_cap_overridden_defaults_to_false` + `test_cap_overridden_true_passed_through`. 4 D-decisions ratified at AskUserQuestion gate before implementation per Trigger #3 (column add on existing telemetry table) + Trigger #5 (cap-exceeded UX shape): D1=cap-exceeded UX = auto-open modal on re-render (Andy's pick over 302-redirect-with-query-params + inline-alert-banner; matches `#t1HookModal` predecessor pattern); D2=cap counting = `success=TRUE` rows only (Andy flipped from architect's "count-all-rows" recommendation; fault-tolerance over conservative cost-protection — failed cascades shouldn't penalize the athlete retrying through a broken orchestrator); D3=override scope = single-use per refresh (Andy's pick over window-clearing override; each subsequent submit re-checks the cap independently, matching D-64 §8 per-refresh cost-signal framing); D4=`cap_overridden=TRUE` lands only when route's own cap-check returned exceeded AND form arrived with `cap_override=1` (Andy's pick over trust-the-form-field; column reflects "athlete confirmed the cost gate," not "request contained an override field"). **Tests 1363 → 1376 (+13 net new in 1 extended test file; container-runnable subset 696 → 709 in ~1.7s).** No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard surfaces. 12 NL parser smoke tests + 4 prior Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. **NL parser refresh traffic from the T1 hook is now cost-gated end-to-end; the §6.1 architect-recommended next forward move from the predecessor handoff lands cleanly.** Forward-pointer: `_write_refresh_log` still does NOT populate `triggered_by_ad_hoc_id` — same carry-forward doc-sweep nit as the predecessor (the column ships but its caller doesn't thread the FK).

**Predecessor:** `V5_Implementation_D73_Phase_5_2_LogThis_T1Hook_2026_05_21_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Log-this slice + D-63 T1 plan-check hook shipped** (closes the §6.1 architect-recommended forward move from the Dashboard CTAs handoff). [Log this workout] on `/workouts/suggestions/<id>` now persists the generated session into `cardio_log` (kind=cardio) or `training_log` (kind=strength, one row per StrengthExercise) tagged `is_ad_hoc=TRUE` + `ad_hoc_suggestion_id=<id>` + `ad_hoc_request_payload=<json>`; on success the route 302s to `/workouts/suggestions/<id>?just_logged=1` and the page auto-opens a Bootstrap modal offering a T1 refresh with NL context pre-filled (template `f"Did an unscheduled {duration_min}min {sport} ({intensity}) at {locale}"`) per D-63 §3.5. **7 substantive files** (ceiling break ratified at AskUserQuestion gate; precedented by 5.1.A=8 / 5.1.C=8 / 5.2.Caller-D63+PlanCreate=9 / 5.2.Caller-D64=8): (1) `init_db.py` — 4 new migrations: 3-column extensions to `cardio_log` + `training_log` per D-63 §5.1/§5.2 (`is_ad_hoc BOOLEAN NOT NULL DEFAULT FALSE` + `ad_hoc_request_payload JSONB` + `ad_hoc_suggestion_id BIGINT REFERENCES ad_hoc_workout_suggestions(id)` + partial index `(user_id, is_ad_hoc) WHERE is_ad_hoc = TRUE`) + `triggered_by_ad_hoc_id BIGINT` column on `plan_refresh_log` per §5.4 + NEW `t1_hook_telemetry` table (BIGSERIAL PK + user_id FK + suggestion_id FK + dismissed_at TIMESTAMPTZ + created_at + index on `(user_id, dismissed_at DESC)`) per §3.5; (2) `routes/ad_hoc_workouts.py` — 5 new helpers (`_render_nl_context` + `_log_cardio_session` + `_log_strength_session` + `_mark_logged` + `_record_t1_dismiss`) + 2 new routes (POST `/workouts/suggestions/<id>/log` + POST `/.../dismiss_t1_hook`); `_get_suggestion` extended to SELECT `logged_into_table` + `logged_into_id`; `view_suggestion` extended to thread `just_logged` + `t1_hook_nl_context` + `t1_hook_refresh_query` (urlencoded) kwargs; atomic per D-64 §6.2 (`db.commit()` fires only after INSERT into log table + UPDATE on suggestions both succeed; on (ValueError, RuntimeError) calls `db.rollback()` + flash); (3) `templates/workouts/suggestion_view.html` — [Log this workout] primary button leftmost on status='suggested'; status='logged' renders `Logged ✓ — Saved to your cardio/training log.`; NEW `#t1HookModal` Bootstrap modal at end of content block with title `Logged ✓` + auto-fill preview body + form-wrapped [No, thanks] (POSTs to dismiss_t1_hook) + anchor [Yes — refresh] linking to `/plans/v2/refresh?{t1_hook_refresh_query}`; 3-line inline `<script>` block on `{% if just_logged %}` auto-opens the modal via `bootstrap.Modal.getOrCreateInstance(modalEl).show()`; (4) `routes/plan_refresh.py` — NEW `_resolve_prefill(args) -> tuple[str, str | None]` helper (reads `nl_context` + caps at `_NL_TEXT_SOFT_CAP_CHARS=500` + uppercases tier + validates against `VALID_TIERS`); GET branch threads `prefill_nl_context` + `prefill_tier` kwargs; (5) `templates/plans/v2/refresh.html` — textarea body `{{ prefill_nl_context or '' }}`; 3 tier buttons conditional class `{% if prefill_tier is none or prefill_tier == 'TX' %}btn-primary{% else %}btn-outline-primary{% endif %}` (default state preserved when no prefill); (6) `tests/test_routes_ad_hoc_workouts.py` — +22 tests across 6 new classes (`TestRenderNlContext` 5 + `TestLogCardioSession` 5 + `TestLogStrengthSession` 5 + `TestMarkLogged` 2 + `TestRecordT1Dismiss` 2 + `TestGetSuggestionLoggedFields` 3); (7) `tests/test_routes_plan_refresh.py` — +7 tests in `TestResolvePrefill` (empty args + nl_context passthrough + tier uppercase + unknown tier → None + blank tier → None + soft-cap truncation + full T1-hook pattern). 4 D-decisions ratified at AskUserQuestion gate before implementation per Trigger #5 (architectural alternatives) + Trigger #1 (form copy) + Trigger #3 (cross-layer schema): D1=T1 hook UX shape = auto-fired Bootstrap modal (Andy's pick over inline-banner + direct-redirect + dashboard-flash; modal kept JS-light via pre-rendered conditional markup + 3-line `bootstrap.Modal.getOrCreateInstance().show()` on `?just_logged=1`; no AJAX adapter); D2=t1_hook_dismissed telemetry placement = NEW `t1_hook_telemetry` table (Andy's pick over columns-on-suggestions + skip-telemetry; extensible for future hook event types without schema churn on suggestions); D4=NL context auto-fill template = verbatim intensity word (`easy`/`moderate`/`hard`/`race pace`; underscore → space); D5=refresh form prefill mechanism = query-param (Andy's pick over flash + session + auto-submit; stateless; preserves editability per D-63 §3.5 "athlete sees the D-64 modal with NL pre-filled (editable)"). **Tests 1334 → 1363 (+29 net new across 2 extended test files; container-runnable subset 667 → 696 in ~1.7s).** No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser / dashboard surfaces. 12 NL parser smoke tests + 4 prior Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. **D-63 → D-64 cross-route flow now closed end-to-end via the post-log T1 plan-check hook with auto-filled NL context.** Next caller-side surface: NL parser frequency caps per D-64 §8 (now near-load-bearing with real T1 traffic from the modal) + `triggered_by_ad_hoc_id` plumbing wiring at `_write_refresh_log`.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_Dashboard_CTAs_2026_05_21_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side — **Dashboard CTAs for the 3 Layer 4 v2 caller-side routes shipped** (closes the §6.1 architect-recommended forward move from the D-64 runtime handoff). All 3 caller-side routes (single_session via `/workouts/build` + plan_create via `/plans/v2/new` + plan_refresh via `/plans/v2/refresh`) are now reachable from the dashboard, not just direct URLs. **3 substantive files** (well under 5-file ceiling): (1) `templates/dashboard.html` — NEW "Quick actions" row inserted between the brand header and Today's Workouts; 3 equal-width Bootstrap cards (`col-md-4`) for Build/Create/Refresh; each card has fw-semibold title + text-muted small body + primary btn-sm link. Refresh card renders with `opacity-50` + alternate copy ("Available once you have an active plan.") + disabled-styled button (`btn-secondary disabled` + `aria-disabled="true"` + `tabindex="-1"`) when `has_plan_version=False` per D3; (2) `routes/dashboard.py` — NEW inline helper `_has_plan_version(db, user_id) -> bool` (cheap `SELECT 1 FROM plan_versions WHERE user_id = ? LIMIT 1`); threaded into `render_template` context as `has_plan_version`; (3) NEW `tests/test_routes_dashboard.py` (~60 LOC) — 3 tests in `TestHasPlanVersion` class (returns True when row present + returns False when row absent + SQL is user_id-scoped); first test file for `routes/dashboard.py`; mirrors `tests/test_routes_plan_refresh.py` `_FakeConn` substrate. 4 D-decisions ratified at AskUserQuestion gate before implementation per Trigger #5 (architectural alternatives): D1=branch rename to `claude/dashboard-ctas-UOFaP` per CLAUDE.md branch-naming rule (harness-pinned name matched prior session scope); D2=placement = top of dashboard, directly under brand header, above Today's Workouts (Andy's pick over after-stats-row + split-refresh-up-top; v2 routes become the first interactive surface athletes see); D3=conditional = always show all 3 cards + gray out Refresh + swap copy when athlete has no `plan_versions` row (Andy's pick over always-show-no-conditional + hide-Refresh; preserves stable layout while signaling state); D4=card copy = "Build a workout" / "Create a training plan" / "Refresh your plan" with single-sentence body per spec D-63 §3.1 + D-64 §4.1 (Trigger #1 iterative; not blocking). **Tests 1331 → 1334 (+3 net new in `tests/test_routes_dashboard.py`; container-runnable subset 664 → 667 in ~1.8s).** No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts / plan_refresh / nl_parser surfaces. 12 NL parser smoke tests + 4 prior Layer 3 SDK smoke tests still skip cleanly when `ANTHROPIC_API_KEY` unset. **All 3 of 3 Layer 4 v2 caller-side routes now dashboard-reachable**, completing the caller-side v2 surfacing arc. Only D-63 T1 plan-check hook (log-this slice) + NL parser frequency caps per D-64 §8 remain as caller-side surface work; Phase 5 closed (orchestrator + caller-side + dashboard CTAs).

**Predecessor:** `V5_Implementation_D73_Phase_5_2_CallerSide_D64_Runtime_2026_05_21_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side D-64 — **NL parser runtime + plan_refresh route shipped** (Phase 5.2 caller-side arc complete). Composes the NL parser prompt body shipped earlier same-day with the runtime + Flask route, closing the D-64 caller-side surface end-to-end. **8 substantive files** (ceiling break ratified at scope-selection AskUserQuestion gate; precedented by 5.1.A=8 / 5.1.C=8 / 5.2.Caller-D63+PlanCreate=9): (1) NEW `nl_parser.py` (~530 LOC) — `parse_intent(IntentParserInput, *, user_id, cache_backend=None, llm_caller=None) -> ParsedIntent`; `_default_llm_caller` Anthropic SDK adapter (forced tool-use single tool `record_parsed_intent`, no extended thinking per NLParser_v1.md D2+D3); `build_record_parsed_intent_tool()` mirrors `ParsedIntent` MINUS `raw_text` (driver-stamped per D4+D7); `_short_circuit_empty` per §1.3 (whitespace-only bypasses LLM call); single capped retry on schema violation with the validation error injected into the user prompt per §5.3 step 1; on second fail OR network error raises `NLParserError("schema_violation"/"network"/"input_validation")`; `_enforce_closed_locale_vocab` post-LLM strip per §8.2 (unknown slugs stripped + appended to `ambiguity_notes` honoring 240-char cap); athlete-scoped cache via `CacheBackend` with `nl_parser_parse_intent` entry-point label registered in `VALID_ENTRY_POINTS` superset (NOT `LAYER4_ENTRY_POINTS` per NLParser_v1.md §10.2 — parser cache stays out of Layer-4-scoped invalidation cascades); `nl_parser_cache_key(user_id, nl_text)` per §10.1 = `sha256(user_id || sha256(normalize(nl_text)) || NL_PARSER_PROMPT_VERSION)` (tier intentionally omitted per spec — tracked as v2 NL-2 candidate); `NL_PARSER_PROMPT_VERSION = 1` constant lives in this module per D12; (2) NEW `routes/plan_refresh.py` (~360 LOC) Blueprint `/plans/v2/refresh/*` — `refresh` (GET tier-picker form with empty-state CTA when athlete has no prior plan_versions row per D1; POST resolves tier from 3 named submit buttons per D2, runs parser with degraded fallback per D-64 §5.4 on `NLParserError`, allocates new plan_versions row via `allocate_plan_version_row(created_via='plan_refresh_t1/t2/t3', pattern='B')`, loads prior window via `load_prior_plan_session_window(tier=tier)`, invokes `orchestrate_plan_refresh`, persists sessions, writes 1 `plan_refresh_log` row, commits atomically per D-64 §6.2; failure path rolls back + writes a failure-row log INSERT in a fresh sub-transaction + commits the failure-row so telemetry still lands) + `view_refresh` (GET diff view at `/plans/v2/refresh/<id>` per D3 — `_diff_sessions_against_parent` excludes rebound `plan_version_id` + `session_id` identity fields so structurally identical sessions across versions compare as 'unchanged'; badges = 'updated' / 'new' / 'unchanged'; parent plan_version_id resolved from `plan_refresh_log` row, falling back to immediately-prior plan_versions row); (3) `init_db.py` — NEW migration in `_PG_MIGRATIONS` for `plan_refresh_log` per D-64 §7.1 (BIGSERIAL PK + user_id FK + tier CHECK in (T1, T2, T3) + nl_text TEXT + parsed_intent JSONB + layers_run TEXT[] NOT NULL DEFAULT '{}' + scope_start_date/scope_end_date DATE + plan_version_id_before/after BIGINT FK to plan_versions + duration_ms + sessions_changed + success BOOLEAN NOT NULL + failure_reason TEXT + cap_overridden deferred per D4) + index on `(user_id, triggered_at DESC)`; (4) NEW `templates/plans/v2/refresh.html` (3-button picker per D-64 §4.1 — "Refresh next 2 days" / "Refresh week" / "Refresh next 4 weeks" + nl_context textarea ~500 char soft cap + empty-state alert "You don't have a plan yet" linking to `/plans/v2/new` when athlete has no prior plan_versions row); (5) NEW `templates/plans/v2/refresh_view.html` (diff view; updated/new/unchanged badges; per-card border highlighting per badge); (6) NEW `tests/test_nl_parser.py` (~600 LOC; 60 tests across 11 classes — `TestParseIntentFixtures` 16 (TS1..TS15 + whitespace short-circuit) / `TestClosedLocaleVocabViolation` 1 / `TestParseIntentRetry` 3 (first-attempt-invalid-second-valid + second-failure-raises + zero-retries-on-first-failure) / `TestParseIntentNetworkError` 1 / `TestParseIntentCache` 3 (miss-then-hit + short-circuit-skips-cache + cache-round-trip-preserves) / `TestShortCircuitEmpty` 3 / `TestNormalizeNLText` 4 / `TestNLParserCacheKey` 5 / `TestRenderUserPrompt` 10 / `TestEnforceClosedLocaleVocab` 5 / `TestBuildRecordParsedIntentTool` 7 / `TestRawTextStamping` 2); (7) NEW `tests/test_nl_parser_smoke.py` (~150 LOC; 12 env-gated `@requires_anthropic_api_key` real-LLM tests against Sonnet 4.6 — clean signals 4 / injury disambig 3 / locale vocab 2 / upstream triggers 2 / empty short-circuit 1; ~$0.04 per smoke run); (8) NEW `tests/test_routes_plan_refresh.py` (~450 LOC; 36 tests across 9 classes — `TestParseTier` 6 / `TestResolveScopeDates` 3 / `TestOrchestrationErrorMessage` 2 / `TestLatestPlanVersion` 3 / `TestAthleteLocaleSlugs` 3 / `TestAthleteActiveInjurySummary` 5 / `TestRunParser` 2 / `TestWriteRefreshLog` 3 / `TestDiffSessionsAgainstParent` 6 / `TestLatestParentForRefresh` 3). Bookkeeping: `app.py` registers `plan_refresh_bp`; `layer4/cache.py` `VALID_ENTRY_POINTS` extended with `"nl_parser_parse_intent"`. 4 D-decisions ratified at AskUserQuestion gate before implementation per Trigger #5 (architectural alternatives): D1=no-prior-plan policy = GET hides tier buttons + renders empty-state CTA to `/plans/v2/new`, POST defensive-redirect when parent_plan is None (Andy's pick over T3-no-plan-auto-routes-to-plan_create + block-all-tiers); D2=tier UX = 3 named submit buttons (one-click flow; Andy's pick over radio + dropdown); D3=redirect = NEW `/plans/v2/refresh/<id>` diff view with updated/new/unchanged badges per D-64 §9 (Andy's pick over redirect-to-plan_create-view + stay-on-form); D4=plan_refresh_log telemetry shipped + caps per D-64 §8 deferred (Andy's pick over both-shipped + telemetry-only-no-table). **Tests 1235 → 1331 (+96 net new across 3 NEW test files; container-runnable subset 568 → 664 in ~1.0s).** 12 new env-gated smoke tests skip cleanly when `ANTHROPIC_API_KEY` unset; default `pytest tests/` reads "1331 passed, 16 skipped" (4 prior Layer 3 SDK smoke + 12 new NL parser smoke). No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding / plan_create / ad_hoc_workouts surfaces. **D-64 caller-side arc complete; all 3 of 3 Layer 4 entry-point caller-side routes now E2E-reachable from v1 UI** (single_session via `/workouts/build` + plan_create via `/plans/v2/new` + plan_refresh via `/plans/v2/refresh`).

**Predecessor:** `V5_Implementation_D73_Phase_5_2_CallerSide_D64_NLParser_Prompt_2026_05_21_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side D-64 — **NL parser prompt body shipped** (Trigger #2 LLM-prompt-design gate cleared for the D-64 caller-side route arc). Pure prompt-design session per `Plan_Refresh_D64_Design_v1.md` Decision #12 deferral; **1 substantive file** (well under ceiling): NEW `aidstation-sources/prompts/NLParser_v1.md` (~580 LOC; 13-section structure mirroring Layer3A_v1.md / Layer4_SingleSession_v2.md). 14 D-decisions ratified at AskUserQuestion gate before drafting: D1=Sonnet 4.6 (Haiku 4.5 migration as §12 candidate gated on smoke-eval harness); D2=no extended thinking (classification not synthesis); D3=forced tool-use single tool `record_parsed_intent`; D4=full `ParsedIntent` mirror minus driver-stamped `raw_text` (L3A D5 precedent); D5=middle-path injury disambiguation — new-injury keywords ("tweaked"/"hurt"/"strained"/"sharp"/"sudden"/"twisted") → TRUE / update-on-existing ("feels better"/"healing"/"less pain") → FALSE / ambiguous → TRUE + `ambiguity_notes` populated; D6=strict closed-vocab locale matching — `triggers_2c_equipment` may contain ONLY slugs in `athlete_locales` input, out-of-vocab locations surface via `ambiguity_notes`; D7=driver-stamped `raw_text` post-hoc (L3A precedent); D8=single retry on schema violation + `NLParserError` → route substitutes `_default_parsed_intent()` per D-64 §5.4 (already implemented at `layer4/plan_refresh.py:1207`); D9=classification-only voice — no CLAUDE.md coaching voice inheritance (parser doesn't write athlete-facing copy); D10=`temperature=0` (deterministic + cache contract per D-64 §5.3); D11=13-section document structure (CLAUDE.md depth standard); D12=`NL_PARSER_PROMPT_VERSION = 1` constant lives in `nl_parser.py` runtime module (NOT in markdown); D13=~150-300 input tokens + ~100-200 output tokens + ~$0.003-$0.005/cold call; D14=smoke-eval harness (~10-15 hand-labeled fixtures from Andy's PGE 2026 + AR vocab) lands in paired runtime session, NOT this one. Document covers §4 tool schema (full `ParsedIntent` mirror sans `raw_text` with `additionalProperties: false` at every nesting level) + §5 system prompt (10 hard rules: output mechanism / conservative-bias on triggers / 5 trigger-flag rules / 3 soft-signal rules per closed enum / `parser_confidence` calibration / `ambiguity_notes` guidance + 240-char cap / tier-mismatch policy / empty-input short-circuit / forbidden output / classification-only voice for `ambiguity_notes`) + §6 user prompt template (4 input variables — nl_text + tier_label + athlete_locales_block + athlete_active_injuries_block — with retry augmentation block) + §7 sampling config (model=`claude-sonnet-4-6` / temp=0 / max_tokens=1024 / no extended thinking / capped_retries=1 / forced tool_choice) + §8 post-LLM transforms (`_enforce_closed_locale_vocab` strips unknown slugs + appends to `ambiguity_notes`; no confidence-floor clamp; no evidence-basis cross-check — parser doesn't emit `evidence_basis`) + §9 performance budget + §10 caching per D-64 §5.3 (cache key = `(user_id, sha256(normalize(nl_text)), NL_PARSER_PROMPT_VERSION)`; `_normalize_nl_text` = lowercase + whitespace-collapse; v1 does NOT invalidate on `athlete_locales` / `athlete_active_injuries` changes — tracked as §12 NL-2 tuning candidate) + §11 test scenarios (15 stub-LLM fixtures across `TS1`-`TS15` covering: clean single-signal / new-injury keyword / re-aggravation ambiguity / update-on-existing / locale slug match / out-of-vocab locale / extreme fatigue / sickness signal / new-discipline mention / nutrition shift / tier mismatch / motivation low / empty-input short-circuit / ambiguous body-part / travel-without-slug + closed-vocab violation post-LLM transform test + env-gated `@requires_anthropic_api_key` real-LLM smoke harness with ~10-15 hand-labeled fixtures planned for paired runtime session) + §12 8 open items (NL-1 Haiku 4.5 migration eval gated on smoke harness / NL-2 athlete-level cache invalidation on locales/injuries change / NL-3 out-of-vocab location auto-add CTA / NL-4 multi-signal density telemetry / NL-5 tier-mismatch policy v2 escalation / NL-6 soft-signal granularity expansion / NL-7 prompt-version constant convention + dev-override env var / NL-8 streaming-mode infeasibility note) + §13 gut check. Pure design session — no code, no schema, no routes, no test count delta. **D-64 caller-side route now unblocked structurally:** runtime session ships NEW `nl_parser.py` (LLM-backed Sonnet, schema-violation single retry, `_enforce_closed_locale_vocab` post-LLM strip, deterministic cache, `NL_PARSER_PROMPT_VERSION = 1`) + NEW `routes/plan_refresh.py` (allocates plan_versions row + queries prior window via `load_prior_plan_session_window(tier=tier)` + runs NL parser → ParsedIntent + on `NLParserError` substitutes `_default_parsed_intent()` per D-64 §5.4 + invokes `orchestrate_plan_refresh` + persists via `persist_layer4_sessions` + commits atomically per D-64 §6.2) + tier-picker template + `tests/test_nl_parser.py` (stub-LLM unit tests for the 15 fixtures in §11.1 + closed-vocab violation test in §11.2) + `tests/test_nl_parser_smoke.py` (env-gated real-LLM smoke harness with ~10-15 hand-labeled fixtures) + `tests/test_routes_plan_refresh.py`. ~5-7 substantive files. **`/plan` gate per Trigger #1 (form copy on the refresh-trigger card) + Trigger #5 (route shape — dashboard vs plan-view entry points + redirect-after-refresh UX); Trigger #2 (NL parser prompt design) CLEARED this session.**

**Predecessor:** `V5_Implementation_D73_Phase_5_2_CallerSide_D63_PlanCreate_Routes_2026_05_21_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side routes — **D-63 on-demand workout route + plan-create caller-side route shipped** atop the same-day Phase 5.2 caller-side substrate. First end-to-end Flask route wiring of the Layer 4 orchestrator surface. Both arcs compose `allocate_*` → `orchestrate_*` → `persist_*` → `db.commit()` atomically per D-64 §6.2; on any failure no commit fires + auto-rollback keeps half-allocated rows off the table. **9 substantive files** (ceiling break ratified at AskUserQuestion gate per Trigger #1 form copy + Trigger #3 new `ad_hoc_workout_suggestions` table + Trigger #5 route shape; precedented by 5.1.A=8 + 5.1.C=8): (1) `init_db.py` — 1 new migration in `_PG_MIGRATIONS` for `ad_hoc_workout_suggestions` table per D-63 §5.3 (BIGSERIAL PK + user_id FK + request_payload JSONB + generated_session JSONB nullable until orchestrate succeeds + status TEXT CHECK in (suggested, logged, discarded, regenerated) + logged_into_table/id columns reserved for log-this slice + regenerated_into_id self-FK chains regenerations + token_cost_estimate + created_at) + 1 index on `(user_id, status, requested_at DESC)`; (2) NEW `routes/ad_hoc_workouts.py` (~330 LOC) — Blueprint `/workouts/*` with 7 inline helpers (`_athlete_sport_choices` broad framework_sport list from `layer0.sports`, `_allocate_suggestion`, `_persist_generated_session`, `_get_suggestion`, `_mark_status` for discarded/regenerated, `_decode_jsonb` JSONB dual-path, `_parse_request_form` SingleSessionRequest factory + ValidationError→flash translation) + `build_workout` (GET form, POST orchestrate), `view_suggestion` (GET single-session result card), `discard_suggestion` (POST status flip), `regenerate_suggestion` (POST re-orchestrate + link prior via regenerated_into_id); (3) NEW `templates/workouts/build_form.html` — Bootstrap form (sport dropdown / duration number 30-360/15 / intensity dropdown {easy, moderate, hard, race_pace} / locale dropdown / optional notes textarea); (4) NEW `templates/workouts/suggestion_view.html` — renders the generated PlanSession card (cardio_blocks loop / strength_exercises loop / coaching_intent + session_notes) + [Regenerate]/[Discard] action buttons on suggested-status rows + supersession link when regenerated; (5) NEW `tests/test_routes_ad_hoc_workouts.py` (~330 LOC) — 29 tests across 8 classes covering all 7 helpers + `_orchestration_error_message` translation table + `VALID_INTENSITIES` constant; (6) NEW `routes/plan_create.py` (~190 LOC) — Blueprint `/plans/v2/*` with 4 inline helpers (`_parse_plan_start_date` ISO-format guard, `_load_plan_version` SELECT scoped to user_id + dict shape, `_resolve_plan_scope_end_date` race-date-or-24-week-fallback, `_orchestration_error_message`) + `new_plan` (GET form, POST allocate_plan_version_row + orchestrate_plan_create + persist_layer4_sessions + commit atomically, redirect-to-view), `view_plan` (GET sessions-by-date list); (7) NEW `templates/plan_create/new_form.html` — date input + read-only target-race summary block (or "No target race set" alert) + create button; (8) NEW `templates/plan_create/view.html` — plan header (pattern badge + scope dates + session count) + per-date sessions list (discipline + duration + intensity badge + coaching_intent + session_notes); (9) NEW `tests/test_routes_plan_create.py` (~190 LOC) — 16 tests across 4 classes (`TestParsePlanStartDate` 6: happy + whitespace + empty + missing-key + invalid-format + out-of-range / `TestLoadPlanVersion` 3: dict-on-hit + None-on-miss + user_id scoping defense / `TestResolvePlanScopeEndDate` 5: race-date used / 24w fallback no race / 24w fallback past race / 24w fallback missing event_date / same-day race uses race date / `TestOrchestrationErrorMessage` 2: known codes + unknown fallback). 14 D-decisions ratified at AskUserQuestion gate before implementation (D1=D-63 schema scope = `ad_hoc_workout_suggestions` only, NOT `cardio_log`/`training_log` `is_ad_hoc` extensions — log-this slice defers to D-64 caller-side pairing; D2=URL prefixes `/workouts/*` + `/plans/v2/*` avoid v1 `routes/plans.py` legacy collision; D3=broad framework_sport list `SELECT DISTINCT framework_sport FROM layer0.sports WHERE superseded_at IS NULL`, athlete-set + cross-train filtering deferred to v2; D4=quick-equipment "Somewhere else" path defer — locale-only form for v1; D5=caller owns transaction per substrate D-64 §6.2; D6=`PostgresCacheBackend(lambda: get_db())` cache factory in production; D7=no target-race picker on plan-create form — orchestrator already reads `is_target_event=TRUE` row via `load_target_race_event_payload`; D8=plan_versions notes=None for v1, phase_synthesis_notes population deferred; D9=inline helpers in route module, NOT new `ad_hoc_workouts_repo.py` — single caller, premature abstraction; D10=result-view actions = [Regenerate] + [Discard] only, [Log this workout] deferred with T1 hook; D11=ceiling break ratified — 9 substantive files matches 5.1.A/5.1.C 8-file precedent; D12=helper-level pytest density, end-to-end Flask test_client deferred to manual §5.0 walkthrough per `routes/race_events.py` + `routes/onboarding.py` test precedent; D13=batch (a)+(b) per substrate handoff §6.1 recommendation; D14=broad sport vocabulary on D-63 form). Bookkeeping: `app.py` registers 2 new blueprints. No new specs, prompt bodies, or Layer 4 internal changes. **Tests 1190 → 1235 (+45 net new route tests across `tests/test_routes_ad_hoc_workouts.py` 29 + `tests/test_routes_plan_create.py` 16; container-runnable subset 523 → 568 passing in ~1.0s).** No regressions on Layer 4 / orchestrator / repo / race_events / locales / onboarding surfaces. **Phase 5.2 caller-side routes (D-63 + plan-create) complete; both Layer 4 entry-point orchestrators now E2E-reachable from v1 UI.** Remaining caller-side surface: D-64 caller-side route + NL parser glue + NEW `prompts/NLParser_v1.md` prompt body (~5-7 files; gated on `/plan` Trigger #2 LLM prompt-design session before route work). Log-this slice + D-63 T1 plan-check hook pair with D-64 caller-side closure.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_CallerSide_PlanSessions_Substrate_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.2 caller-side substrate — **`plan_sessions` table + `plan_sessions_repo.py` shipped** atop the same-day Phase 5.2 slices 1 + 2 + 3 closure. Foundation for the D-63 + D-64 + plan-create caller-side route arc; lifts the Layer 4 §7.11 + §7.12 spec contract (PlanSession natural key `(plan_version_id, date, session_index_in_day)` + per-day version pointer per D-64 §6.3) into deployed schema + repo helpers. **3 substantive files** (well under 5-file ceiling): (1) `init_db.py` — 1 new migration in `_PG_MIGRATIONS` for `plan_sessions` table (BIGSERIAL PK + plan_version_id FK with ON DELETE CASCADE + user_id denormalization + session_id + date + session_index_in_day SMALLINT CHECK 0/1 + payload_json JSONB carrying full `PlanSession.model_dump(mode='json')` + UNIQUE (plan_version_id, date, session_index_in_day) for the natural-key invariant) + 2 indexes (`(user_id, date)` for fast dashboard queries + `(user_id, plan_version_id)` for plan-view queries); (2) NEW `plan_sessions_repo.py` (~210 LOC) — `allocate_plan_version_row(db, user_id, *, created_via, scope_start_date, scope_end_date, pattern, notes=None) -> int` (INSERT ... RETURNING id; validates `created_via` ∈ {plan_create, plan_refresh_t1/t2/t3, single_session_synthesize} + `pattern` ∈ {A, B} + scope_end >= scope_start; caller owns transaction per D-64 §6.2; notes JSONB-serialized) + `persist_layer4_sessions(db, payload: Layer4Payload) -> None` (INSERT each `PlanSession` from `payload.sessions` under its `plan_version_id` + denormalized user_id from payload; full payload_json round-trip; empty-list no-op) + `load_plan_sessions_by_version(db, plan_version_id) -> list[PlanSession]` (ordered by (date, session_index_in_day); dual-path JSONB hydration tolerating psycopg2 dict + SQLite-shim JSON-string) + `load_prior_plan_session_window(db, user_id, *, today, tier=None, days=None) -> list[PlanSession]` (per-day version pointer resolver via `DISTINCT ON (date, session_index_in_day) ORDER BY plan_version_id DESC` per D-64 §6.3; tier-tied default mapping `_PRIOR_WINDOW_DAYS_BY_TIER = {T1: 2, T2: 7, T3: 28}` matching refresh forward-scope shape; `days` kwarg overrides; raises ValueError when both None or days<=0; window is `[today - days, today - 1]` inclusive, strictly pre-today so it doesn't overlap with the refresh/create scope starting at today); private `_decode_payload` helper covers JSONB dual-path; (3) NEW `tests/test_plan_sessions_repo.py` (+~430 LOC) — 22 tests across 5 classes (`TestAllocatePlanVersionRow` 6 — happy path RETURNING id round-trip + notes JSONB serialization + 3 input validation rejections + RuntimeError when RETURNING returns no row; `TestPersistLayer4Sessions` 3 — inserts each session with natural-key columns + payload_json model dump + empty sessions no-op + user_id from payload threads to every row; `TestLoadPlanSessionsByVersion` 3 — payload_json → PlanSession round-trip + JSONB string-path hydration + empty rows returns []; `TestLoadPriorPlanSessionWindow` 9 — tier-tied defaults for T1/T2/T3 + days kwarg override + ValueError when both None + ValueError when days<=0 + DISTINCT ON resolver + ORDER BY clause + empty result for first-plan athletes + PlanSession hydration round-trip; `TestTierDefaultMapping` 1 — sanity-pin on `_PRIOR_WINDOW_DAYS_BY_TIER`). 8 D-decisions ratified at design surface before implementation (D1=plan_sessions schema with JSONB payload column not denormalized 17+ columns; D2=repo at repo root mirroring race_events_repo precedent not inside layer4/; D3=4 helpers + private JSONB hydration; D4=tier-tied default lookback `{T1: 2, T2: 7, T3: 28}` days matching refresh-scope shape, with `days` kwarg override and ValueError when neither supplied; D5=caller owns transaction per D-64 §6.2 — persist_layer4_sessions does NOT call db.commit(); D6=test density 22 across 5 classes; D7=user_id denormalized into plan_sessions for fast (user_id, date) lookups bypassing plan_versions join; D8=natural-key UNIQUE constraint enforces Layer 4 §7.12 invariant at DB layer). **Tests 1168 → 1190 (+22 net new repo tests; container-runnable subset 501 → 523 passing in ~1.6s).** No `/plan` Triggers #1 / #3 fired — the schema is mechanical implementation of an existing spec contract (Layer 4 §7.11 + §7.12 already define the natural key + resolver semantics); D4 lookback policy was the only real design call, ratified via AskUserQuestion gate. **`plan_versions` table already on disk** from prior Layer 4 §7.11 migration (lines 1088-1101 in `_PG_MIGRATIONS`); this session adds the companion `plan_sessions` table + repo helpers, no changes to existing tables. **Substrate complete; D-63 + D-64 + plan-create caller-side route arcs now unblocked** — next sessions can compose `allocate_plan_version_row` + `orchestrate_*` + `persist_layer4_sessions` inside Flask route handlers under a single transaction.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_PlanCreate_Orchestrator_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.2 slice 3 — **`orchestrate_plan_create` shipped** atop the same-day Phase 5.2 slices 1 + 2 (single_session + plan_refresh). Fourth and final Layer 4 entry-point orchestrator (after race_week_brief 5.1 + single_session 5.2.S1 + plan_refresh 5.2.S2). Composes the full upstream cone (Layer 1 → 2A/B/D/C → 3A → 3B → 2E) → `llm_layer4_plan_create_cached`; Pattern A heaviest entry point — driver runs per-phase synthesis loop + seam reviews + final cross-phase validator pass internally per `Layer4_Spec.md` §5.2; orchestrator does not see per-phase internals. **Reuses the slice 2 `_upstream_full_cone` helper as-is** (no changes — slice 2's forecast confirmed: "slice 3 reuses the helper as-is"). **2 substantive files** (well under 5-file ceiling): (1) `layer4/orchestrator.py` — new public `orchestrate_plan_create(db, user_id, *, plan_start_date, plan_version_id, cache, today=None) -> Layer4Payload`; added `llm_layer4_plan_create_cached` to the `layer4.cached_wrappers` import block; module docstring rewritten from "three-entry-point" → "four-entry-point" shape + plan_create algorithm + Pattern A heaviness note + no-event-mode first-class call-out; `__all__` extended; (2) `tests/test_layer4_orchestrator.py` — 10 new tests across 5 `TestOrchestratePlanCreate*` classes (`TestOrchestratePlanCreateHappyPath` 2: event-mode + no-event-mode; `TestOrchestratePlanCreatePreflightGates` 3: etl_version_set_undiscoverable + primary_locale_missing + framework_sport_missing; `TestOrchestratePlanCreateDefaults` 2: today defaults to `date.today()` + layer2c packed as primary-locale dict; `TestOrchestratePlanCreatePassThrough` 2: plan_start_date threads verbatim + plan_version_id threads verbatim; `TestOrchestratePlanCreateReturnValue` 1: cached wrapper output passed through verbatim); new `_plan_create_patches(*, layer4_return)` patches the same 10 import sites as race_week_brief/plan_refresh except the cached wrapper is `llm_layer4_plan_create_cached`; new `_fake_plan_create_layer4_payload(plan_version_id)` factory constructs a valid Pattern A plan_create `Layer4Payload` (mode invariants per `payload.py:548-552` = `mode='plan_create'` requires `phase_structure` non-None + `seam_reviews` non-None; empty `phases` list + empty `seam_reviews` list satisfy "non-None" without forcing the orchestrator test surface to construct full PhaseSpec/SeamReview rows — those belong to `tests/test_layer4_plan_create.py`); `PhaseStructure` added to imports. 10 D-decisions ratified at plan-mode gate per Trigger #5 (architectural alternatives): D1=reuse `_upstream_full_cone` as-is (no helper changes — slice 2's forecast confirmed); D2=`plan_start_date: date` required kwarg (orchestrator does NOT default to today — route handler resolves "athlete picks future start" vs "today" per D-64 caller-side surface); D3=`plan_version_id: int` caller-supplied (matches slice 1 D4 + slice 2 D3 precedent for D-63/D-64 caller-side deferral; `plan_versions` table allocation belongs at route layer pending D-64 caller-side); D4=3 pre-flight gates only (shared via helper — `etl_version_set_undiscoverable` / `primary_locale_missing` / `framework_sport_missing`); NO `no_target_event` (Pattern A supports open-ended plans — `race_event_payload=None` flows cleanly through the cone; Layer 3B's `mode='no-event'` branch handles downstream); NO `race_week_brief_too_early` (plan_create fires on demand); NO orchestrator-side `plan_start_date_in_past` / `plan_version_id_unset` / `time_to_event_weeks_mismatch` / `discipline_weights_invalid` (all covered by driver's `_validate_plan_create_inputs` per `Layer4_Spec.md` §4.2; `Layer4InputError` propagates verbatim — matches slice 1/2 + race_week_brief precedent of not wrapping driver-level errors); D5=target-race lookup conditional, not gated (same as slice 2 D5); D6=NO `parsed_intent` kwarg (only plan_refresh consumes ParsedIntent for refresh-trigger context per D-64 §5.4); D7=`today: date | None = None` kwarg mirroring race_week_brief + single_session + plan_refresh signatures for deterministic test injection; D8=orchestrator calls `llm_layer4_plan_create_cached` (not the raw driver) — matches race_week_brief D9 + slice 1 D9 + slice 2 D8; D9=layer2c packed as `{primary_locale: payload}` dict (race_week_brief shape, NOT plan_refresh's Layer2Bundle); driver signature requires `dict[str, Layer2CPayload]`; D10=10 tests across 5 classes (lower than plan_refresh's 13 since no `tier` dispatch dimension; matches single_session test density). **Tests 1158 → 1168 (+10 net new orchestrator tests; container-runnable subset 491 → 501 passing in ~1.3s).** Bookkeeping: `layer4/__init__.py` re-exports `orchestrate_plan_create` alongside the existing 3 entry points + `OrchestrationError`. No new spec edits (Layer4_Spec.md §3.1 already documents the driver entry-point contract; orchestrator-side composition follows the slice 1 + slice 2 + race_week_brief precedent of not being spec'd separately). No new schema, route, template, or prompt-body work. No `/plan` Triggers #1 / #3 fired. **Phase 5.2 slice 3 complete; orchestrator now exposes all 4 of 4 Layer 4 entry points** (race_week_brief 5.1 + single_session_synthesize 5.2.S1 + plan_refresh 5.2.S2 + plan_create 5.2.S3). **Phase 5.2 complete, closing Phase 5.** All Layer 4 entry points wired end-to-end from upstream pipeline; D-63 + D-64 caller-side routes + `ad_hoc_workout_suggestions` + `plan_versions` tables remain as the next E2E-reachability arc.

**Predecessor:** `V5_Implementation_D73_Phase_5_2_PlanRefresh_Orchestrator_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.2 slice 2 — `orchestrate_plan_refresh` shipped atop the same-day Phase 5.2 slice 1 (single_session). Third Layer 4 entry-point orchestrator (after race_week_brief 5.1 + single_session 5.2.S1). Composes the full upstream cone (Layer 1 → 2A/B/D/C → 3A → 3B → 2E) → `llm_layer4_plan_refresh_cached`; the driver dispatches T1/T2/T3 internally and routes T3 cross-phase to Pattern A via `_route_t3_cross_phase_to_pattern_a` (shipped at Step 4f 2026-05-18). **2 substantive files** (well under 5-file ceiling): (1) `layer4/orchestrator.py` — new public `orchestrate_plan_refresh(db, user_id, *, tier: Literal["T1","T2","T3"], refresh_scope_start, refresh_scope_end, plan_version_id, plan_version_id_parent, prior_plan_session_window, cache, parsed_intent=None, plan_start_date=None, today=None) -> Layer4Payload`; new private `@dataclass(frozen=True) _UpstreamFullCone` (11 fields: etl_version_set + framework_sport + primary_locale + all 8 payloads); new private `_upstream_full_cone(db, user_id, today, *, target_race_event)` helper that emits the 3 shared pre-flight gates (`etl_version_set_undiscoverable` / `framework_sport_missing` / `primary_locale_missing`) and composes the Layer 1 → 2A → 2B → 2D → 2C → 3A → 3B → 2E pipeline; `orchestrate_race_week_brief` REFACTORED to consume the helper (behavior-preserving — all 17 existing race_week_brief tests pass unchanged); single_session NOT refactored (opted out per cone-shape divergence — narrower cone uses `request.sport` not `primary_sport`, `request.locale_slug` not `'home'`, optional 2C); module docstring rewritten from "two-entry-point" → "three-entry-point" shape + helper-extract decision documented; new imports (`dataclass`, `Literal`, payload types + `Layer2Bundle` + `ParsedIntent` + `RaceEventPayload` from `layer4.context`, `PlanSession` from `layer4.payload`, `llm_layer4_plan_refresh_cached` from `layer4.cached_wrappers`); `__all__` extended; (2) `tests/test_layer4_orchestrator.py` — 13 new tests across 6 `TestOrchestratePlanRefresh*` classes (`TestOrchestratePlanRefreshHappyPath` 4: parametrized [T1/T2/T3] tier dispatch + parsed_intent threading; `TestOrchestratePlanRefreshPreflightGates` 3: etl_version_set_undiscoverable + primary_locale_missing + framework_sport_missing; `TestOrchestratePlanRefreshNoEventMode` 1: race_event_payload=None when no target race + Layer 2B/2E/3B receive empty/None inputs; `TestOrchestratePlanRefreshDefaults` 2: today defaults to date.today() + parsed_intent defaults to None; `TestOrchestratePlanRefreshTierPassThrough` 2: T3 plan_start_date threads to wrapper + prior_plan_session_window passes verbatim; `TestOrchestratePlanRefreshReturnValue` 1: cached wrapper output passed through verbatim); new `_plan_refresh_patches(*, layer4_return)` patches the same 10 import sites as race_week_brief (build_layer1_payload + q_layer2a + q_layer2b + q_layer2c + q_layer2d + q_layer2e + assemble_layer3a_bundle + llm_layer3a + llm_layer3b + llm_layer4_plan_refresh_cached); new `_fake_plan_refresh_layer4_payload(plan_version_id)` factory constructs valid Pattern B plan_refresh Layer4Payload (mode invariants per payload.py:553-565 = `mode='plan_refresh'` + `pattern='B'` requires `phase_structure=None` + `seam_reviews=None`); new `_default_prior_plan_session_window()` helper constructs a single non-ad-hoc PlanSession for the prior-window kwarg. 10 D-decisions ratified at plan-mode gate per Trigger #5 (architectural alternatives): D1=**extract `_upstream_full_cone` helper now + refactor race_week_brief to consume** (Andy flipped from architect's defer-to-slice-3 recommendation; full-cone helper covers 2/3 entry points immediately, single_session opts out per cone-shape divergence); D2=tier as required kwarg from caller (D-64 dispatch happens at route layer, not orchestrator); D3=plan_version_id + plan_version_id_parent + prior_plan_session_window + parsed_intent + plan_start_date all caller-supplied (matches slice 1 D4 for D-63's `suggestion_id`); D4=3 pre-flight gates only (shared `_q_*` discovery gates; no `no_target_event` since no-event refresh supported; no auto-fire window since refresh fires on demand); D5=target-race lookup conditional, not gated; D6=`parsed_intent: ParsedIntent | None` pass-through (D-64 §5.4 graceful degradation); D7=`today` mirrors race_week_brief + single_session signatures; D8=call `llm_layer4_plan_refresh_cached`; D9=single function `orchestrate_plan_refresh(tier=...)` not 3 per-tier functions (matches driver's internal dispatch); D10=13 tests across 6 classes at parity with single_session test density. **Tests 1145 → 1158 (+13 net new orchestrator tests; container-runnable subset 478 → 491 passing in ~1.0s).** Bookkeeping: `layer4/__init__.py` re-exports `orchestrate_plan_refresh` alongside the existing 2 entry points + `OrchestrationError` + `ParsedIntent`. No new spec edits (Layer4_Spec.md §3.2 already documents the driver entry-point contract; D-64 design doc already documents tier semantics + dispatch rules; orchestrator-side composition follows the slice 1 + race_week_brief precedent of not being spec'd separately). No new schema, route, template, or prompt-body work. No `/plan` Triggers #1 / #3 fired. **Phase 5.2 slice 2 complete; orchestrator now exposes 3 of 4 Layer 4 entry points (race_week_brief 5.1 + single_session_synthesize 5.2.S1 + plan_refresh 5.2.S2). Remaining: `plan_create` Pattern A (slice 3).**

**Predecessor:** `V5_Implementation_D73_Phase_5_2_SingleSession_Orchestrator_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.2 slice 1 — **`orchestrate_single_session_synthesize` shipped** atop the same-day Phase 5.1 form-refresh trilogy completion. Second Layer 4 entry-point orchestrator (after race_week_brief 5.1). Composes upstream Layer 1 → 2A → 2D → 2C (locale-only) → 3A → `llm_layer4_single_session_synthesize_cached` for D-63 on-demand workouts. Materially narrower cone than race_week_brief (no 2B / 2E / 3B / 3B-cached). **2 substantive files** (well under 5-file ceiling): (1) `layer4/orchestrator.py` — new public `orchestrate_single_session_synthesize(db, user_id, request, suggestion_id, *, cache, today=None) -> Layer4Payload`; new `_q_locale_by_slug(db, uid, locale) -> bool` helper (distinct from `_q_primary_locale` which hard-codes `'home'` — single-session athletes pick any configured locale); existing `_q_current_etl_version_set` + `_q_locale_equipment_pool` shared verbatim with race_week_brief; module docstring + `__all__` updated to reflect two-entry-point shape. `OrchestrationError` codes: `request_sport_unavailable` (Layer 2A's `Layer2AInputError` on unknown framework_sport caught + re-raised with detail); `locale_unknown` (slug not present in `locale_profiles`); `etl_version_set_undiscoverable` (shared). NO `no_target_event` / `race_week_brief_too_early` gates — single-session is off-plan, off-race, athlete-driven by definition; the driver's own `_validate_inputs` (`layer4/single_session.py` §4.4) covers locale-XOR-quick + 2C-payload-presence rules; (2) `tests/test_layer4_orchestrator.py` — 10 new tests across 6 `TestOrchestrateSingleSessionSynthesize*` classes (`TestOrchestrateSingleSessionSynthesizeHappyPath` 2: locale-path + quick_equipment-path; `TestOrchestrateSingleSessionSynthesizePreflightGates` 2: request_sport_unavailable + locale_unknown; `TestOrchestrateSingleSessionSynthesizeDiscoveryFailures` 1: etl_version_set_undiscoverable; `TestOrchestrateSingleSessionSynthesizeDefaults` 2: today defaults + 2D-payload threaded into 2C; `TestOrchestrateSingleSessionSynthesizeSportSemantics` 2: request.sport overrides primary_sport (D2) + quick_equipment path skips locale_by_slug SELECT; `TestOrchestrateSingleSessionSynthesizeReturnValue` 1: cached-wrapper output passed through verbatim); each uses `_single_session_patches()` patch-stack (narrower than race_week_brief's `_patches()` — patches 7 import sites: build_layer1_payload + q_layer2a + q_layer2c + q_layer2d + assemble_layer3a_bundle + llm_layer3a + llm_layer4_single_session_synthesize_cached — no 2B / 2E / 3B / race_week_brief driver) + the existing `_FakeConn` fixture pattern. 9 D-decisions ratified at plan-mode gate (Trigger #5 — architectural alternatives) before implementation: D1=defer shared `_upstream_pipeline` extract until 3rd entry point lands (single_session's cone narrower than race_week_brief's; Rule of Three is the right discipline; refactoring race_week_brief now risks regression on a tested surface; per `plan_refresh` arrival the full overlap surfaces); D2=`request.sport` as `framework_sport` for Layer 2A (athlete-overriding per D-63 §6.1; e.g., Andy picks Rowing for cross-training even though primary is AR); D3=skip Layer 2C entirely when `request.locale_slug` is None (quick_equipment path); driver handles the None case per spec §3.3 row 4; D4=`suggestion_id: int` as a kwarg from the caller, not orchestrator-allocated (`ad_hoc_workout_suggestions` table not yet deployed per D-63 §5.3); D5=3 pre-flight gates only (request_sport_unavailable + locale_unknown + etl_version_set_undiscoverable); D6=validate slug via new `_q_locale_by_slug` (cleaner error contract than trust-and-let-empty-pool-emerge); D7=`today: date | None = None` kwarg mirroring race_week_brief signature for deterministic test injection; D8=10 tests at parity with race_week_brief precedent; D9=orchestrator calls `llm_layer4_single_session_synthesize_cached` (not the raw driver) — matches race_week_brief precedent. **Tests 1135 → 1145 (+10 net new orchestrator tests; container-runnable subset 468 → 478 passing in ~0.9s).** Bookkeeping: `layer4/__init__.py` re-exports `orchestrate_single_session_synthesize` alongside `orchestrate_race_week_brief` + `OrchestrationError`. No new spec edits (Layer4_Spec.md §3.3 already documents the driver entry-point contract; orchestrator-side composition follows the race_week_brief precedent of not being spec'd separately). No new schema, route, template, or prompt-body work. No `/plan` Triggers #1 / #3 fired. **Phase 5.2 slice 1 complete; orchestrator now exposes 2 of 4 Layer 4 entry points (race_week_brief 5.1 + single_session_synthesize 5.2.S1). Remaining: `plan_refresh` T1/T2/T3 + `plan_create`.**

**Predecessor:** `V5_Implementation_D73_Phase_5_1_FormRefresh_C_Locale_Loosen_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.1 form-refresh C — **§J locale-terrain capture + Layer 2B `_validate_inputs` empty-race_terrain loosen shipped** atop the same-day form-refresh A + B. **Fully closes Layer2B_Spec.md §12 Open Item 2B-2** (canonical TRN-xxx vocab on `§J Locale terrain access`); flips the orchestrator's last `locale_terrain_ids=[]` forward-pointer; paired loosen makes athletes who skip §H.2 capture surface `race_terrain_unset` as a coaching flag instead of failing on `Layer2BInputError`. **8 substantive files** (ceiling break ratified at plan-mode gate per Triggers #1 + #3, precedented by 5.1.A 8 files): (1) `init_db.py` — 1 ALTER TABLE migration `locale_profiles.locale_terrain_ids TEXT[] NOT NULL DEFAULT '{}'` (D9 `_PG_MIGRATIONS` entry, no standalone SQL file — same precedent as 5.1.A); (2) `routes/locales.py` — module-local `_TRN_PATTERN` + `_terrain_choices(db)` + `_parse_locale_terrain(form)` + `_hydrate_locale_terrain_ids(profile_row)` + `_evict_layer2b_on_terrain_change(db, uid)` per D1 v1 route-local duplicate-with-cross-ref strategy (the helpers mirror `routes/race_events.py` + `routes/onboarding.py` from form-refresh A/B; ~30 LOC duplication across 3 sites keeps the 8-file ceiling intact); threaded into both `_edit_legacy_locale` + `_edit_shared_locale` GET render kwargs + POST INSERT/UPDATE branches per D4 (terrain lives on `locale_profiles` directly, not the shared `gym_profiles` table — geographic access, not commercial inventory); D10 invalidation fires on actual terrain-set change (`sorted(new) != sorted(prior)`) so no-op saves stay silent; (3) `templates/locales/form.html` — new "Terrain accessible from this location" multi-checkbox grid (D2 — checkbox grid mirroring equipment pattern, not the race-terrain pct-row editor partial; pct doesn't apply to locale terrain) inserted between equipment fieldsets + city/notes inputs with form copy per D8 (athletes select terrain reachable from the locale even if not on-property — disambiguates property-vs-reachable); D3 scope = terrain editor only (extracting a partial for a 16-row checkbox grid isn't worth it); (4) `layer2b/builder.py` — `_validate_inputs` loosen (D6): accepts `race_terrain == []` (skip per-entry + pct_sum + TRN-pattern checks when empty); still validates non-list/non-RaceTerrainEntry/invalid-pct/locale-id/disciplines/etl shape when non-empty; `_emit_coaching_flags` signature extended with `race_terrain` parameter + new short-circuit branch emitting a single `race_terrain_unset` `Layer2BCoachingFlag(flag_type='race_terrain_unset', target_terrain_id=None, message=...)` when input is empty (D7 — message + metadata=`{}`); (5) `layer4/orchestrator.py` — hoist `_q_primary_locale` up before Layer 2B (was at the 2C boundary); new `_q_locale_terrain_ids(db, uid, locale)` helper (mirrors `_q_locale_equipment_pool` shape — SELECT against `locale_profiles WHERE user_id, locale`; returns Python list from psycopg2 native TEXT[] or `[]` for NULL / missing row / SQLite-shim JSON-string path); flip `locale_terrain_ids=[]` to `locale_terrain_ids=_q_locale_terrain_ids(db, user_id, primary_locale)` per D5 (v1 home-locale only — matches existing `_q_locale_equipment_pool` precedent; cluster-union spec §3 deferred to follow-on); module docstring forward-pointer block updated to reflect Open Item 2B-2 closure + the loosen pair landing; (6) `tests/test_layer2b.py` — existing `TestInputValidation::test_empty_race_terrain_raises` removed; new `TestEmptyRaceTerrainLoosen` class with 4 tests (`test_empty_race_terrain_returns_payload_with_race_terrain_unset_flag` — payload shape on empty: empty race_terrain[] + empty terrain_gaps[] + summary all-zeros + worst_fidelity=1.0 + single race_terrain_unset flag with target_terrain_id=None + metadata={}; `test_empty_race_terrain_with_empty_locale_still_emits_unset_flag` — both-empty case; `test_non_list_race_terrain_still_raises` — type guard remains; `test_other_validation_still_fires_on_empty_terrain` — included_discipline_ids + etl_version_set still validated when terrain empty); (7) NEW `tests/test_locales.py` — 21 tests across 5 classes: `TestTrnPattern` (2: accepts canonical / rejects non-canonical), `TestParseLocaleTerrain` (8: happy multi-select, empty form, empty field, drops blanks, drops bad TRN, dedupes, sorted output, strips whitespace), `TestTerrainChoices` (2: returns id+label dicts in SELECT order, empty rows), `TestHydrateLocaleTerrainIds` (8: None / column-missing / NULL / native list / list-with-invalid-drops / JSON-string / empty-array-literal / malformed-JSON), `TestEvictLayer2bOnTerrainChange` (1: monkeypatch Layer4Cache + PostgresCacheBackend constructors; assert layer2b policy = _NON_SINGLE_SESSION evicts plan_create + plan_refresh + race_week_brief and preserves single_session); (8) `tests/test_layer4_orchestrator.py` — new `TestLocaleTerrainIdsWireUp` class with 4 tests (`test_locale_terrain_ids_thread_into_layer2b_call` — `_q_locale_terrain_ids` SELECT response threads into Layer 2B kwargs; `test_locale_terrain_ids_empty_when_column_null` — NULL column path; `test_locale_terrain_ids_empty_when_row_missing` — defensive against pre-migration / race-condition row absence; `test_locale_terrain_ids_tolerates_json_string_path` — SQLite shim JSON-string representation); existing `TestRaceTerrainAndAidStandsWireUp::test_empty_race_terrain_still_passed_through_unchanged` docstring updated to reference the loosen pair landing in C. **Tests 1110 → 1135 (+25 in container-runnable subset: 21 new locales tests + 4 new orchestrator wire-up tests).** Container-runnable subset (layer4 + race_events + onboarding race_events + locales) 443 → 468 passing in 1.00s. The +4 new `tests/test_layer2b.py` tests + the orchestrator-side flag emission remain blocked by the pre-existing `layer1/layer4` circular-import same as the rest of `test_layer2b.py` (per CURRENT_STATE.md historical note + Phase 5.1.B handoff §4); verified by `git stash` round-trip that the circular import is not introduced by this slice. 4 SDK smoke tests still skip cleanly when ANTHROPIC_API_KEY unset → default `pytest tests/` reads "1135 passed, 4 skipped" once `test_layer2b.py` collection is reachable; in the container subset it reads "468 passed". 11 D-decisions ratified at plan-mode gate per Trigger #1 (user-facing form copy) + Trigger #3 (cross-layer schema change on `locale_profiles`): D1=route-local helper duplicate per form-refresh A/B precedent; D2=checkbox grid widget (not race-terrain partial); D3=terrain editor scope only (no partial extraction for the 16-row grid); D4=both legacy + shared edit branches; D5=orchestrator v1 home-only via existing `_q_primary_locale`; D6=`_validate_inputs` accepts empty + flag-emit; D7=`race_terrain_unset` flag message + empty metadata; D8=form copy "Terrain accessible from this location" + reachable-vs-on-property disambiguation; D9=`_PG_MIGRATIONS` entry only (no standalone SQL file); D10=field-change-gated invalidation fires on any locale edit for cluster-union forward-compat; D11=8-file ceiling break ratified (precedented by 5.1.A's 8 files). Layer 2B `_validate_inputs` loosen folds into the same slice — empty-default produces a working brief, not a fail. **Phase 5.1 form-refresh trilogy A + B + C complete; orchestrator has zero remaining Layer 2B input forward-pointers; Layer2B_Spec.md §12 Open Items 2B-2 + 2B-3 both ✅ Resolved.**

**Predecessor:** `V5_Implementation_D73_Phase_5_1_FormRefresh_B_Onboarding_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.1 form-refresh B — **§H.2 onboarding step-3c terrain capture shipped** atop the same-day form-refresh A. Mirrors A's terrain editor on the onboarding side so newly-onboarding athletes capture race terrain breakdown during initial onboarding rather than via post-onboarding edit. **Fully closes Layer2B_Spec.md §12 Open Item 2B-3** (race-event edit surface from A + onboarding step-3c surface from B both capture TRN-xxx + percent rows persisted to `race_events.race_terrain JSONB`; orchestrator's `race_terrain=race_event.race_terrain` flow now reaches the Layer 2B input from both surfaces). **5 substantive files** (at ceiling): (1) `routes/onboarding.py` — adds module-local `_TRN_PATTERN` + `_parse_race_terrain(form)` + `_terrain_choices(db)` mirroring `routes/race_events.py` (D1 v1 route-local duplicate-with-cross-ref strategy ratified at plan-mode gate; the ~12 LOC duplication keeps the 5-file ceiling intact and avoids forcing `race_events_repo.py` to grow form-parsing concerns); extends `_get_target_race_row()` SELECT with `race_terrain` + `aid_stations` columns + JSONB list-or-string hydration (mirrors `race_events_repo.get_race_event` adapter tolerance — psycopg2 native JSONB list vs sqlite shim JSON string vs NULL); extends `target_race()` GET to pass `terrain_choices=_terrain_choices(db)` to the template; `target_race_save()` POST parses `race_terrain` + `aid_stations` and threads into both `create_race_event` (new-target branch) + `update_race_event` (existing-target branch); brief-only cache invalidation diff extended with `prior_terrain != new_race_terrain` + `prior_aid != new_aid_stations` comparisons (same `evict_on_target_event_brief_field_change` routing as `routes/race_events.py:update_race`); (2) NEW `templates/_race_terrain_editor.html` — extracted shared partial at root templates level (D2 location pick; sibling to `base.html`; resolved from both `onboarding/` and `profile/` subdirs without `../`); contains the terrain-rows container + add button + hidden `<template>` for JS cloning + the inline vanilla-JS row-management hooks (nextIdx scan, click-to-clone, click-delegated remove); inputs from context: `existing_terrain` list of dicts + `terrain_choices` list of `{id, label}` dicts; single-render-per-page assumption documented in partial header comment; (3) `templates/onboarding/target_race.html` — distance + total_elevation_gain_m columns shrunk from `col-md-6 × 2` to `col-md-4 × 3` to fit a new `aid_stations` integer input (`col-md-4` mirroring `templates/profile/race_event_edit.html` layout exactly per D4); `{% set existing_terrain = (target.race_terrain if target and target.race_terrain else []) %}` + `{% include '_race_terrain_editor.html' %}` inserted between mandatory_gear textarea and notes textarea; aid_stations help text matches the post-onboarding edit surface verbatim ("Total count along the course. Expedition AR + self-supported events are usually 0; aid-supported ultras typically 4–12.") per D7; (4) `templates/profile/race_event_edit.html` — behavior-neutral refactor replacing the inline ~65-line terrain section (the `<div class="col-12">` block) + the ~35-line bottom `<script>` block with `{% set existing_terrain = ... %}` + `{% include '_race_terrain_editor.html' %}`; net -103 LOC at this template, +98 LOC at the partial; (5) `tests/test_onboarding_race_events.py` — `+16` new tests: 3 added to `TestGetTargetRaceRow` (`test_hydrates_race_terrain_from_jsonb_string`, `test_hydrates_race_terrain_from_native_list`, `test_hydrates_none_race_terrain_to_empty_list`); new `TestParseRaceTerrain` class with 8 tests (happy multi-row parse, empty form, drop-on-empty-terrain_id, drop-on-empty-pct, drop-on-invalid-TRN-pattern, drop-on-non-numeric-pct, drop-on-out-of-range-pct, sorted-order-across-sparse-indices); new `TestTerrainChoices` class with 2 tests (SELECT shape + ORDER BY + dict mapping, empty-rows degenerate); existing `TestGetTargetRaceRow::test_returns_dict_on_hit` extended with new race_terrain + aid_stations column assertions in SELECT + dict pass-through. **Tests 1094 → 1110 (+16).** Container-runnable subset (layer4 + race_events + onboarding race_events) 408 → 443 passing in 1.44s. 8 D-decisions ratified at plan-mode gate per Trigger #1 (user-facing form copy): D1=route-local helper duplicate over repo extraction (v1 simplicity over cross-blueprint coupling); D2=partial at `templates/_race_terrain_editor.html` (root) not `templates/onboarding/_race_terrain_editor.html` (the partial serves both onboarding/ and profile/ subdirs); D3=partial scope = terrain editor only (aid_stations stays inline in each parent template); D4=aid_stations layout = `col-md-4 × 3` mirroring race_event_edit.html; D5=cache invalidation extended in same shape as routes/race_events.py update_race; D6=no `target_race_terrain_missing` account_nudge (out of scope; Layer 2B `_validate_inputs` loosen is the paired follow-on); D7=identical form copy to post-onboarding edit surface (coaching voice consistency); D8=section heading "Race terrain breakdown" matches edit surface label.

**Predecessor:** `V5_Implementation_D73_Phase_5_1_FormRefresh_A_RaceTerrain_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.1 form-refresh A — **race_terrain + aid_stations end-to-end shipped** atop the same-day Phase 5.1 orchestrator vertical slice. First slice of the §H.2 / §J / §I.1 form-refresh PR closes the highest-leverage orchestrator forward-pointer per the predecessor handoff §6.1 ("the only forward-pointer that affects coaching quality on multi-day events"). Closes Layer2B_Spec.md §12 Open Item 2B-3 for the race-event edit path (onboarding §H.2 step-3c terrain capture still owed in a follow-on). **8 substantive files** (3-file ceiling break, ratified at the plan-mode gate before implementation): (1) `init_db.py` — 2 ALTER TABLE migrations adding `race_events.race_terrain JSONB NOT NULL DEFAULT '[]'::jsonb` + `race_events.aid_stations INTEGER NULL CHECK (aid_stations IS NULL OR aid_stations >= 0)`; (2) `layer4/context.py` — `RaceEventPayload` gains `race_terrain: list[RaceTerrainEntry] = Field(default_factory=list)` + `aid_stations: int | None = Field(default=None, ge=0)` + a new `model_validator` enforcing `TRN-\d{3}` pattern on each entry's `terrain_id` at the payload boundary (no sum check — Layer 2B owns `[80, 120]` tolerance; partial-edit rows round-trip cleanly); (3) `race_events_repo.py` — threads the 2 new fields through `list_athlete_race_events` + `load_race_event_payload` + `create_race_event` + `get_race_event` + `update_race_event` with JSONB-list-or-string hydration tolerance for the psycopg2-default vs sqlite-shim adapter paths; (4) `routes/race_events.py` — new `_terrain_choices(db)` request-time vocabulary helper (against `layer0.terrain_types`) + `_parse_race_terrain(form)` flat-form parser (`race_terrain[N][terrain_id]` + `race_terrain[N][pct_of_race]` repeating rows; empty/malformed rows silently dropped) + `aid_stations` `_parse_int` threading on both `new_race()` + `update_race()` + brief-only cache invalidation extended to fire on race_terrain or aid_stations changes (both feed Layer 2B + 2E which are uncached at orchestrator level → Layer 4 brief is the cache-load-bearing artifact); (5) `templates/profile/race_event_edit.html` — terrain-breakdown editor with per-row TRN-xxx select + percent input + remove button + "Add terrain" button backed by a `<template>` block and a small inline JS hook (vanilla DOM API; no Alpine/HTMX); aid_stations integer input next to distance/elevation; (6) `layer4/orchestrator.py` — 2 forward-pointer flips (line 151 `race_terrain=[]` → `race_terrain=race_event.race_terrain`; line 207 `aid_stations=None` → `aid_stations=race_event.aid_stations`) + module docstring forward-pointer block updated to reflect the slice's closure (race_terrain now flows; locale_terrain_ids still empty pending §J Open Item 2B-2; `prior_plan_session_window` + `plan_version_id` + uncached 3A/3B remain); (7) `tests/test_race_events_repo.py` — `TestRaceTerrainAndAidStations` class with 9 new tests covering CRUD round-trip (load via list + via JSONB string + None-defaults-empty), terrain-id pattern rejection at the payload model_validator, create + update serialization, get_race_event hydration tolerance, list_athlete_race_events column inclusion; (8) `tests/test_layer4_orchestrator.py` — `TestRaceTerrainAndAidStationsWireUp` class with 3 new tests asserting race_terrain threads into `q_layer2b_terrain_classifier_payload` kwargs, aid_stations threads into the `Layer2ETargetEvent.aid_stations` for the Layer 2E call, and the empty-terrain pass-through case (slice intentionally does not loosen Layer 2B's `_validate_inputs` rejection of empty — that's a separate spec change). Layer 4 + race_events suite: 348 → 408 passing in the container-runnable subset (60 net new); production count delta: 1082 → 1094 (+12 tests). 9 D-decisions ratified at the plan-mode gate (D1-D9 pinned in §7 of the closing handoff). **Phase 5.1 form-refresh A complete.** Remaining §H.2/§J/§I.1 split: §H.2 onboarding step-3c capture surface (so athletes capture terrain during initial onboarding rather than via race-event edit) + §J locale-terrain capture (Open Item 2B-2) + §I.1 structured supplements (Layer 2E §5.5 de-stub) all queued. Layer 3B None-tolerant kwargs (`goal_outcome`, `first_time_at_distance`, `previous_attempts`, etc.) remain a separate L3B-P-2 surface.

**Predecessor:** `V5_Implementation_D73_Phase_5_1_Orchestrator_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.1 orchestrator vertical slice (race_week_brief) shipped (PR #109 merged into main as `8854fa4`). First end-to-end composition of the v2 upstream pipeline. New `layer4/orchestrator.py:orchestrate_race_week_brief(db, user_id, *, cache: Layer4Cache, today: date | None = None) -> Layer4Payload` threads Layer 1 → 2A/2B/2D/2C → 3A → 3B → 2E → `llm_layer4_race_week_brief_cached`. Layer 2E runs AFTER 3B per the 2E ← 3B.start_phase dependency. Four substantive files (under 5-file ceiling): (1) NEW `layer4/orchestrator.py` (266 LOC) — `orchestrate_race_week_brief` + `OrchestrationError(code, detail)` + 3 private DB helpers (`_q_current_etl_version_set` querying `MAX(etl_version) FROM layer0.sports WHERE superseded_at IS NULL` and applying the same version to all 3 sub-arc keys; `_q_primary_locale` reading `locale='home'` per v1 `routes/dashboard.py:31` convention; `_q_locale_equipment_pool` joining `locale_equipment × equipment_items`). Pre-flight gates: `OrchestrationError('no_target_event')` / `OrchestrationError('race_week_brief_too_early')` (cheap optimization saving ~$0.30 of 3A+3B LLM cost on out-of-window invocations; `_validate_inputs` in `llm_layer4_race_week_brief` is the load-bearing check) / `OrchestrationError('framework_sport_missing')` / `OrchestrationError('primary_locale_missing')` / `OrchestrationError('etl_version_set_undiscoverable')`; (2) NEW `tests/test_layer4_orchestrator.py` (~700 LOC) — 10 smoke tests across 5 test classes (`TestHappyPath` 1 / `TestPreflightGates` 2 / `TestDiscoveryFailures` 3 / `TestDefaults` 2 / `TestOrchestrationError` 2) using `unittest.mock.patch` to stub upstream builders + LLM drivers at the orchestrator's module-level imports + `_FakeConn` for the orchestrator's direct DB queries; (3) MODIFIED `aidstation-sources/Layer4_Spec.md` §4.5 — surgical 2-line D-72 audit-trail fix: source-pointer amendment header bumped to `2026-05-18 (D-66 paired implementation; D-72 slug-FK follow-on 2026-05-19)` + Row 5 `event_locale_unresolved` wording updated to reflect that `race_events_repo.load_race_event_payload` itself joins `locale_profiles` and surfaces the slug on `RaceEventPayload.event_locale_id` per D-72 (closes the CARRY_FORWARD doc-nit); (4) MODIFIED `layer4/__init__.py` — re-export block + `__all__` entries for `orchestrate_race_week_brief` + `OrchestrationError`. Bookkeeping: `Upstream_Implementation_Plan_v1.md` §4 row 5.1 flipped to ✅ Shipped 2026-05-20 (PR #109). 19 D-decisions ratified at the `/plan-mode` gate before implementation (Trigger #5 — architectural alternatives); D1-D19 pinned in §7 of the closing handoff. Vertical-slice forward-pointers carried for Phase 5.2: `prior_plan_session_window=[]` (no v2 plan-gen wired yet); `plan_version_id=1` hardcode with `# TODO(plan-versioning)`; Layer 2B `race_terrain=[]` + `locale_terrain_ids=[]` (§H.2 form-refresh gap; 2B surfaces a coaching flag); Layer 3A + Layer 3B uncached at orchestrator level (load-bearing only when multiple Layer 4 entry points share user-scoped outputs — Phase 5.2 will revisit). Layer4 suite 338 → 348 (+10 new orchestrator tests + 338 pre-existing); total tests 1072 → 1082 green + 4 skipped (SDK smoke). **Phase 5.1 complete.** First real end-to-end v2 LLM pipeline composer landed; Phase 5.2 (remaining 3 Layer 4 entry points) inherits the substrate.

**Predecessor:** `V5_Implementation_D73_Layer4_Step7_SDK_Smoke_2026_05_20_Closing_Handoff_v1.md` — D-73 Layer 4 Step 7 — **env-gated `ANTHROPIC_API_KEY` SDK smoke scaffolding shipped** atop the same-day Phase 4 Layer 3B driver. First REAL Anthropic SDK invocation harness against both 3A + 3B drivers shipped earlier in the day; closes L3A-P-1 + L3B-P-1 deferred real-LLM regression scaffolding. Three substantive files (well under ceiling): (1) NEW `tests/conftest.py` (~25 LOC) — shared `requires_anthropic_api_key = pytest.mark.skipif(not os.environ.get("ANTHROPIC_API_KEY"))` decorator; module-level marker keeps default `pytest tests/` runs at $0 and side-effect-free; tests execute only when the key is intentionally set (local dev / CI smoke job); (2) NEW `tests/test_layer3a_smoke.py` (~300 LOC) — 2 real-LLM round-trip tests against actual Sonnet 4.6 via `llm_layer3a_athlete_state(...)` with `llm_caller=None` (defaults to `_default_llm_caller` → production Anthropic SDK adapter): `test_dense_data_fit_athlete` (§13.1 shape — rich integration bundle + 5y AR athlete + dense workouts/sleep/HRV → expects `aerobic_capacity.level ∈ {good, strong}` + high/medium trajectory confidence + sweet_spot ACWR preserved); `test_sparse_data_returning_athlete` (§13.4 shape — empty bundle + experienced athlete → expects `recent_trajectory.confidence == low` + detrained-or-insufficient_data short_term + §6.3 `confidence_clamped_by_data_density` observation appended by floor-clamp); (3) NEW `tests/test_layer3b_smoke.py` (~315 LOC) — 2 real-LLM round-trip tests against actual Sonnet 4.6 via `llm_layer3b_goal_timeline_viability(...)` with `llm_caller=None`: `test_ts1_ar_finisher_compressed_event_mode` (TS-1 — Andy's PGE 2026 baseline: 9 weeks out, Finish goal, prior AR finish → expects mode=event + viability achievable-or-with-adjustment + compressed-or-custom periodization + D14 event-metadata populated from RaceEventPayload); `test_ts4_no_event_endurance_standard` (TS-4 — 24w endurance block → expects mode=no-event + viability achievable + standard-or-extended periodization + all D14 fields None). Assertions are mixed structural (payload validates, metadata stamped with `input_tokens > 0` / `output_tokens > 0` / `latency_ms > 0` / `prompt_hash` length 64 / `etl_version_set` preserved / D14 fields populated-per-mode) + loose enum (allowlist sets — e.g., `aerobic_capacity.level in {"good","strong"}`) to absorb minor classification variance across Sonnet 4.6 minor versions. Tests 1072 → 1076 (+4 smoke; all 4 skip cleanly when ANTHROPIC_API_KEY unset → default `pytest tests/` reads "1072 passed, 4 skipped"). Wiring verified end-to-end via fake-key dry-run (smoke test reaches `anthropic.Anthropic.messages.create()` → 401 AuthenticationError as expected; not a contract bug). **Layer 4 Step 7 SDK smoke scaffolding complete** for 3A + 3B; closes L3A-P-1 + L3B-P-1. Layer 4 `single_session.py` + remaining 5 entry points' smoke parity deferred (architect-recommended scope was 3A + 3B per same-day-shipped driver focus). Real-LLM cost per smoke run when ANTHROPIC_API_KEY set: ~4 × $0.03/call = ~$0.12; intentionally not part of default CI.

**Predecessor:** `V5_Implementation_D73_Phase_4_Layer3B_Driver_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 4 — Layer 3B LLM driver shipped atop the same-day Phase 3.1-Driver. Closes the upstream LLM-driver pair (3A + 3B). First Layer 3 node-pair complete; Phase 5.1 orchestrator vertical slice now unblocked structurally (LLM SDK scaffolding via Layer 4 Step 7 the remaining prerequisite). Five substantive files: (1) NEW `layer3b/builder.py` (~950 LOC) — `llm_layer3b_goal_timeline_viability(...)` entry point + Anthropic SDK adapter with extended-thinking (D2 = 3000 tokens) + forced tool-use (D1) + 4 §5.1 prep helpers + full `Layer3BPayload` tool-schema mirror per D5 + single capped retry on schema violation per §5.5 step 1 + mode-discriminator post-LLM enforcement per §5.5 step 2 + name-existence + mode-prefix `evidence_basis` cross-check per D9 + HITL auto-emit validator per §5.5 step 3 + D12 (4 §6.1 items) + §6.5 floor-rule clamp (4 rules) + `confidence_clamped_by_data_signal` observation per D8 + §5.5 step 4 periodization-sanity loop per D13 (custom-mode phase_weeks sum ±1 of timeline; single retry; fallback-to-standard with `periodization_shape_fallback` observation) + D14 D-66 event-metadata field population from `race_event_payload` + inlined `Layer3BInputError` / `Layer3BOutputError` / `Layer3BEvidenceBasisWarning` (Step 4a + Layer 2C + Layer 3A precedent); (2) NEW `layer3b/cached_wrapper.py` (~200 LOC) — `llm_layer3b_goal_timeline_viability_cached(...)` reusing generic `CacheBackend` from `layer4/cache.py` with 3B-specific `Layer3BPayload` serialize/hydrate + day-granular `current_date: date` cache key per spec §9.1 (`sha256(user_id || layer1_hash || layer3a_hash || layer2a_hash || race_event_id || current_date.isoformat() || non_event_goal_type || etl_set || section_h2_kwargs_json || model || temp || max_tokens || thinking_budget)`); (3) NEW `aidstation-sources/prompts/Layer3B_v1.md` (~700 lines) — prompt body design doc with D1-D14 source decisions + system prompt (CLAUDE.md voice + spec §6.1 HITL triggers + §6.2 periodization vocabulary + §6.5 floor rules + §6.6 no-event heuristics + §8.1 required observation triggers + §8.2 observation budget) + user prompt template (4 blocks + retry/sanity augmentations) + tool schema §4 with `additionalProperties: false` at every nesting level + post-LLM transforms §8 + caching §10 + gut check §13; (4) NEW `tests/test_layer3b_builder.py` (~1100 lines) — 77 tests across 11 test classes (`TestInputValidation` 14 / `TestToolSchema` 10 / `TestEntryPointHappyPath` 3 / `TestModeDiscriminator` 2 / `TestConfidenceFloors` 7 / `TestHITLAutoEmit` 7 / `TestPeriodizationSanity` 6 / `TestSchemaViolation` 3 / `TestEvidenceBasisCheck` 3 / `TestS13Scenarios` 8 (covering TS-1..TS-8) / `TestPrepDict` 7 / `TestCacheWrapper` 7) using stub `llm_caller` per Step 4a + Layer 3A precedent — no real Anthropic SDK invocation; (5) MODIFIED `aidstation-sources/Race_Events_D66_Design_v1.md` §8.3 — paired doc-sweep fix `mode='open_ended'` → `mode='no-event'` per canonical `Layer3BPayload.mode: Literal["event","no-event"]` (CARRY_FORWARD doc-nit + Upstream Plan §4 row 4.2 paired fix list closed). Bookkeeping: `layer3b/__init__.py` re-exports `llm_layer3b_goal_timeline_viability` + `llm_layer3b_goal_timeline_viability_cached` + `Layer3BInputError` + `Layer3BOutputError` + `Layer3BEvidenceBasisWarning` + `build_emit_layer3b_payload_tool` + `layer3b_goal_timeline_viability_key`; `layer4/cache.py` `VALID_ENTRY_POINTS` superset extended with `"llm_layer3b_goal_timeline_viability"` (LAYER4_ENTRY_POINTS frozenset untouched — preserves Layer 4-scoped invalidation invariant per Phase 3.1-Driver precedent). Tests 995 → 1072 (+77). **Phase 4 complete.** Phase 4.1 caller-side rewire helper folded into the driver via D14 internal population — `_assemble_payload_candidate` populates the 4 D-66 event-metadata fields directly from the passed `RaceEventPayload`; no standalone tuple-returning helper added (deferred per Upstream Plan row 4.1). All upstream LLM drivers (3A + 3B) now operational; Phase 5.1 orchestrator vertical slice unblocked.

**Predecessor:** `V5_Implementation_D73_Phase_3_1_Driver_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 3.1-Driver — **Layer 3A LLM driver shipped** atop the substrate landed earlier same day. First upstream LLM driver (predecessors were query-node-only runtimes). Five substantive files: (1) NEW `layer3a/builder.py` (~900 LOC) — `llm_layer3a_athlete_state(...)` entry point + Anthropic SDK adapter with extended-thinking + forced tool-use (D1+D2) + 8 §5.1 prep helpers + full `Layer3APayload` tool-schema mirror per D5 + single capped retry on schema violation per §5.3 step 1 + name-existence `evidence_basis` cross-check per D9 + §6.2 floor-rule clamp (4 rules) + §6.2 high-gate criteria (5 conditions) + §6.3 auto-appended `confidence_clamped_by_data_density` observation + inlined `Layer3AInputError` / `Layer3AOutputError` / `Layer3AEvidenceBasisWarning` (Step 4a + Layer 2C precedent); (2) NEW `layer3a/cached_wrapper.py` (~150 LOC) — `llm_layer3a_athlete_state_cached(...)` reusing generic `CacheBackend` from `layer4/cache.py` with 3A-specific `Layer3APayload` serialize/hydrate via `model_dump_json` / `model_validate_json` + day-granular cache key per spec §9.1 (`sha256(user_id || layer1_hash || layer2a_hash || bundle_hash || day_anchor || etl_set || model || temp || max_tokens || thinking_budget)`); (3) NEW `aidstation-sources/prompts/Layer3A_v1.md` (~530 lines) — prompt body design doc with D1-D10 source decisions + system prompt (CLAUDE.md voice + spec §6.1 weighting + §6.2 calibration + §8.1 required observations + §8.2 forbidden observations + §8.3 ordering) + user prompt template (8 prep blocks + 2A phase context + §B health note + as_of) + tool schema §4 with `additionalProperties: false` at every nesting level + post-LLM transforms §8 + caching §10 + gut check §13; (4) NEW `tests/test_layer3a_builder.py` (~970 lines) — 54 tests across 8 test classes (`TestInputValidation` 10 / `TestToolSchema` 8 / `TestEntryPointHappyPath` 2 / `TestConfidenceFloors` 11 / `TestSchemaViolation` 3 / `TestEvidenceBasisCheck` 1 / `TestS13Scenarios` 6 (covering §13.2/3/4/6/7/10) / `TestPrepDict` 5 / `TestCacheWrapper` 7) using stub `llm_caller` per Step 4a precedent — no real Anthropic SDK invocation; (5) MODIFIED `Layer3_3A_Spec.md` — §3.3 model literal corrected from stale `claude-sonnet-4-5` → `claude-sonnet-4-6` per D7 + Phase 3.1-Driver D7 audit pointer. Bookkeeping: `layer4/cache.py` split `LAYER4_ENTRY_POINTS` (4 originals) + extended `VALID_ENTRY_POINTS` superset (adds `"llm_layer3a_athlete_state"` for the 3A wrapper's `put()` validation); `layer4/__init__.py` re-exports `LAYER4_ENTRY_POINTS`; `tests/test_layer4_cache.py` 6 assertion swaps from `VALID_ENTRY_POINTS` → `LAYER4_ENTRY_POINTS` to preserve the Layer 4-scoped invalidation invariant; `layer3a/__init__.py` re-exports `llm_layer3a_athlete_state` + `llm_layer3a_athlete_state_cached` + `Layer3AInputError` + `Layer3AOutputError` + `build_record_athlete_state_tool` + `layer3a_athlete_state_key`. Tests 941 → 995 (+54). **Phase 3 complete for Layer 3A** (substrate + driver shipped same day). The first real Anthropic SDK call site lands here — production callers need `ANTHROPIC_API_KEY` env (Layer 4 Step 7 scaffolding will land env-gating + smoke test harness when picked).

**Predecessor:** `V5_Implementation_D73_Phase_3_1_Substrate_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 3.1-Substrate — **Layer 3A integration substrate shipped** following the split decided at session start (Phase 2.4-Prep / 2.4 precedent applied to Phase 3.1). Five `q_layer3A_*` query-node accessors per `Athlete_Data_Integration_Spec_v6.md` §10 + a `Layer3AIntegrationBundle` aggregator compose the input the Phase 3.1-Driver session will pass to `llm_layer3a_athlete_state(...)`. Pure SQL — no LLM, no `/plan-mode` triggers, under ceiling. Five substantive files: (1) `layer4/context.py` adds 7 dataclasses (`WorkoutRecord` + `SleepRecord` + `HRVRecord` + `PolarCardioLoadCrossRef` + `CombinedLoadReport` + `ProviderStatus` + `Layer3AIntegrationBundle`) with source-tag Literals (`WorkoutSource` / `SleepSource` / `HRVSource`) supporting the §6.1 weighting rules at the LLM seam; (2) NEW `layer3a/__init__.py` re-exporting the 5 accessors + the aggregator; (3) NEW `layer3a/integration.py` (~480 LOC) implementing the 5 accessors against `cardio_log` (workouts, source-detected via foreign-id columns) / `wellness_self_report` + `polar_sleep` + `coros_daily_summary` (sleep, 3-source merge) / `polar_nightly_recharge` + `coros_daily_summary.ppg_hrv` (HRV) / `cardio_log` durations in hours (ACWR per-discipline + combined; `polar_cardio_load` as cross-reference only per Integration Spec §10) / `provider_auth` + per-data-type coverage queries (provider status); (4) NEW `tests/test_layer3a_integration.py` 40 tests across 7 classes/groups using the `_FakeConn` pattern from `tests/test_layer2c.py` — covers source detection, multi-source merge ordering, ms-to-hours conversion, ACWR zone classification (parametrized 11-point sweep covering all 5 zone boundaries), no-base sentinel, moving_time preference, polar cross-ref population, provider coverage matrix; (5) `Upstream_Implementation_Plan_v1.md` §4 row 3.1 rewritten to reflect the 2-session split (Substrate ✅ + Driver). Tests 901 → 941 (+40); ACWR window cutoff math fixed during the session (inclusive `[cutoff, as_of]` containing exactly N calendar dates). `layer4/__init__.py` re-exports updated (bookkeeping). Phase 3.1-Driver (the LLM call + prompt body + Anthropic SDK adapter + confidence-floor validator) opens the next session with a `/plan-mode` gate per Triggers #2 + #5.

**Predecessor:** `V5_Implementation_D73_Doc_Sweep_2026_05_20_Closing_Handoff_v1.md` — D-73 Doc-Sweep — **5-file spec-touchpoint cleanup batch shipped** following the Phase 2.4 closing handoff. Doc-only session; zero code touched; 901 tests baseline unchanged. Two commits: (a) `226d0dd` — `Layer2C_Spec.md` §5.1 pseudo-code rewritten in place to match §6 one-hop `also_satisfies` semantics + reconciliation note shrunk from compensating-text framing to one-paragraph audit trail (closes the follow-up clarity item flagged from Phase 2.4 §2); (b) `81946b6` — 4-nit doc-sweep batch: `Layer2A_Spec.md` §5.2 drops `pla.default_inclusion` + adds Key-points note on `notes_conditions`-based code-side derivation per §5.3; `Layer2A_Spec.md` §12 Open Item 2A-1 flipped to 🟡 partial-close with Phase 5.1 forward-pointer; `Layer2D_Spec.md` §3 + §4 enum count "9-value" → "11-value"; `Layer2B_Spec.md` §13.1 TRN-008/TRN-009 swap to canonical deployed vocab; `Upstream_Implementation_Plan_v1.md` §4 row 2.2 read-tables list rewritten.

**Predecessor:** `V5_Implementation_D73_Phase_2_4_Closing_Handoff_v1.md` — D-73 Phase 2.4 — **Layer 2C equipment mapper builder shipped** atop the Phase 2.4-Prep substrate. Pure query node per `Layer2C_Spec.md` §3-§10. Three substantive files: NEW `layer2c/__init__.py` re-exports `q_layer2c_equipment_mapper_payload` + `Layer2CInputError`; NEW `layer2c/builder.py` (~650 LOC) implements the full §5 algorithm (§5.1 effective-pool construction with §6 one-hop `also_satisfies` expansion + DP1=(A) runtime toggle lookup; §5.2 sdb⨝sxm⨝e discipline-bridged exercise enumeration; §5.3 Tier 1; §5.4 Tier 2 CNF-structured substitute resolution including improvised auto-resolve; §5.5 Tier 3 proxy resolution with no-cascade rule; §5.6 accommodation modality pass-through from optional `Layer2DPayload`; §5.7 per-discipline coverage aggregation) + §8 coaching flags (8.1 low_coverage, 8.2 critical_dropped, 8.3 toggle_off_for_discipline reading `gated_discipline_ids` directly per DP2=(b)) + §10 edge cases (empty pool, zero-exercise discipline, missing-from-sdb skip, multi-discipline dedup); NEW `tests/test_layer2c.py` 35 tests across 7 classes (`TestInputValidation` 9 / `TestEffectivePool` 4 / `TestTierResolution` 7 / `TestCoverageAndDedup` 2 / `TestCoachingFlags` 5 / `TestAccommodationPassThrough` 2 / `TestEdgeCases` 4 + 2 free-standing smoke tests) using the `_FakeConn` pattern mirroring `tests/test_layer2b.py`. Two paired spec touchpoints folded into the same session: `Layer2C_Spec.md` §5.1 + §8.3 Decision Points annotated ✅ Resolved with full rationale; §10 edge-case row added for missing `parsed_substitutes.json` entries; §12 Open Items 2C-1 + 2C-2 flipped to ✅ Resolved. `Upstream_Implementation_Plan_v1.md` §4 row 2.4 + §5.4 ceiling-break list + §6 gate list updated to reflect the Split + DP resolution. Tests 866 → 901 (+35). **Phase 2 complete — all 5 of 5 Layer 2 runtimes shipped.** §5.1 vs §6 reconciliation note: §5.1's pseudo-code adds `toggle_def.also_satisfies` directly to the equipment pool, but the deployed data shape (per `migrate_toggles_v3_columns.sql`) treats `also_satisfies` as TOGGLE NAMES; builder implements §6 one-hop semantics (expand referenced toggle's `paired_equipment_categories` only, no cascade). Hard prerequisite for live-data verification: Andy operationally applies the 3 SQL migrations + re-runs ETL on Neon per Phase 2.4-Prep handoff §5 before the §5.0 walkthrough scenarios run.

**Predecessor:** `V5_Implementation_D73_Phase_2_4_Prep_Closing_Handoff_v1.md` — D-73 Phase 2.4-Prep — Layer 2C data substrate (schema + ETL extractor work landing the 4 columns Layer 2C reads; Phase 2.4 builder queued for next session per Andy 2026-05-19 split-scope pick). Drift inventory between `Layer2C_Spec.md` §3-§8 and deployed `layer0.*` surfaced four substantive gaps: (a) `layer0.exercises.equipment_substitutes_structured JSONB` (Layer 2C §5.4 CNF Tier 2) — migration on-disk but unapplied + unpopulated; (b) `layer0.exercises.terrain_required TEXT[]` (§5.2 pass-through) — migration on-disk; ETL extractor already produces the field via `vocabulary_transforms.transform_equipment_string`, `run.py` already wires it into the INSERT column list — only the column-add was missing; (c) `layer0.sport_specific_gear_toggles.also_satisfies TEXT[]` (§5.1 + §6) — no migration; (d) `layer0.sport_specific_gear_toggles.gated_discipline_ids TEXT[]` (§8.3) — no migration. **Decision Points resolved** (3-question AskUserQuestion gate; `Layer2C_Spec.md` §5 + §8.3 + Open Items 2C-1/2C-2): **DP1 (§5.1 Toggle definition lookup) = (A) Runtime lookup in 2C** (spec recommendation); **DP2 (§8.3 Discipline-to-toggle mapping) = (b) Structured column on `sport_specific_gear_toggles`** (Andy diverged from spec's hard-code-for-v1 recommendation; structured carries traceability + survives spec evolution). **Scope = Split: Prep first, then 2C** (over Vertical-slice / Migrations-plus-full-2C / Pivot-to-Step-4f-or-Step-7). **6 substantive files** (over ceiling per Andy's "Add exercise_db.py too" pick on the 6-vs-5 gate; deletes operational drift between schema migration + populate run by routing structured substitutes through the ETL extractor): (1) `etl/layer0/schema.sql` adds the 4 columns + spec-section anchor comments; (2) NEW `aidstation-sources/migrations/migrate_toggles_v3_columns.sql` combined-migration (ADD COLUMN IF NOT EXISTS + UPDATE the 3 known cases — `Climbing — roped` also_satisfies `Rappelling / abseiling` + gates `D-010`; `Rappelling / abseiling` gates `D-011`; `Snowshoeing setup` gates `D-015` — + DO-block verification with NOTICE-fallback when no active row exists so safe to run pre-ETL); (3) `etl/layer0/extractors/vocabulary.py` new code-side constants `_TOGGLE_ALSO_SATISFIES` + `_TOGGLE_GATED_DISCIPLINES` (matches Layer 2D `_HIGH_CARDIAC_LOAD_DISCIPLINES` precedent; `_parse_gear_toggles` emits the 2 new fields per row so next ETL re-run preserves the data); (4) `etl/layer0/extractors/exercise_db.py` new `load_parsed_substitutes_structured()` function reading the shipped `etl/sources/parsed_substitutes.json` (154 exercises × 510 entries from prior K-parser work) + `extract_exercises` attaches per-row; loud-fallback empty dict when file missing; (5) `etl/layer0/run.py` extends both INSERTs (4 new columns total — 2 on `exercises` via `to_jsonb` for `equipment_substitutes_structured` + already-wired `terrain_required`; 2 on `sport_specific_gear_toggles`); (6) NEW `tests/test_layer2c_prep.py` 16 tests across 3 test classes (`TestGearToggleParser` 6 / `TestParsedSubstitutesLoader` 5 / `TestSchemaSubstrate` 5 — including `Layer2CPayload` regression smoke covering shipped 5 sub-types + coaching-flag enum guard). Tests 850 → 866 (+16). **Operational sequence for Andy on Neon** (closing handoff §5): (1) `migrate_exercises_substitutes_structured.sql` (existing pre-shipped); (2) `migrate_exercises_terrain_required.sql` (existing); (3) `migrate_toggles_v3_columns.sql` (new); (4) Re-run `python -m etl.layer0.run` to populate the 4 new columns on a new etl_version. Once those 4 steps complete on Neon, Phase 2.4 Layer 2C builder session proceeds.

**Predecessor:** `V5_Implementation_D73_Phase_2_5_Closing_Handoff_v1.md` (Layer 2E nutrition baseline vertical slice — fourth upstream Layer 2 runtime).

## Current focus

**D-74 + D-75 cleanup shipped 2026-05-25** (own session) — fixed the `_q_current_etl_version_set` lexical-MAX version landmine in-code (numeric-tuple max + 6 tests; suite 1636) and added DELETE-by-version idempotency to 3 `etl/sources/` populate scripts (owed-deploy); opened + resolved **D-76** (deleted the divergent terrain_gap_rules migrations copy — `etl/sources/` is the sole canonical path). **R6 discipline-ID renumber + the two craft collapses shipped & deployed earlier the same day (PR #154).** The best-fit re-model arc + R6 are both COMPLETE — no remaining build slices. **Open next moves:** spec narrative sweep (§6.2 — per-layer specs still cite old discipline ids in prose), K3 equipment ETL deploy on Neon (owed), BM-5 equipment-canon tail, D-76, or a manual §5.0 real-LLM walk. _(NOTE: the predecessor focus preserved below predates R6 + this cleanup and lists R6 as a future next move — that line is now stale; R6 is done.)_ Andy's pick. **Best-fit training re-model — Slice 6 (renderer migration onto `TrainingSubstitution` across plan_create / plan_refresh + full v2 retirement) shipped 2026-05-25 — the re-model is COMPLETE** (full delta in "Last shipped session" above). The plan-gen per-phase renderer now consumes `TrainingSubstitution` (the v2 `Layer2ModalityPayload` resolver + its 3 renderers + `_MODALITY_OPTIONS_PER_DISCIPLINE` are deleted; `layer2_modality/resolver.py` removed); the substitution payload is threaded into plan_create + plan_refresh (all 3 tiers) + race_week_brief, with cache slots on each key. Ratified at a **Trigger #1 gate** (single_session drops its modality section — it has no Layer 2B; full retirement in one session with a ceiling break). **`resolve_training_substitution` is the sole best-fit node.** **Architect-recommended next move:** the re-model has no remaining build slices. Natural next moves: the deferred **R6 discipline-ID renumber + the two collapses** (kayak D-008a/b → "Kayaking"; mtn-running D-022/D-023 → "Mountain Running" — gating rationale "terrain axis consumed" is now fully satisfied by Slice 6); the **K3 equipment ETL deploy on Neon** (owed); deferred form feedback; or a **manual §5.0 real-LLM brief/plan walk** to see the substitution section in a live prompt. _Slice-4 context (still accurate, preserved):_ Slice 4 made Layer 2B the first node to consume `RaceTerrainEntry.discipline_id` via the additive `terrain_by_discipline` block list (optional tag, additive output, Trigger #3 gate). _Slice-2/3 context (still accurate, preserved):_ **Slice 2 (vocab) shipped 2026-05-25** (pure-craft display-name overlay; Slice 1 race-craft-aware-scoring removal shipped the same day). **Slice 3 (data model) needed no build** — a 2026-05-25 session-start Rule #9 + on-disk sweep found it already satisfied under the ratified flat-list encoding (**R5**): per-row `discipline_id` terrain capture shipped in Bucket E.(c)-C1 (`e38c7ca`) on both the race-event edit form and onboarding step-3c (shared `templates/_race_terrain_editor.html`), parsed by both routes' `_parse_race_terrain`, round-tripped through `race_events_repo.py`, and unit-tested; existing rows carry `discipline_id=None` (race-wide), so no migration is owed. The literal R2 nested-dict encoding was declined (would break flat-list consumers + force a messy migration). The two ID collapses (kayak D-008a/b, mountain-running D-022/D-023) were ratified **deferred** to a dedicated id-change session after Slice 4 (**R6** — renumber-class ~40-file + `Sports_Framework_v10.xlsx` + Neon re-extract blast radius, and premature before the terrain axis is *consumed*). _(Slice 4 — the Layer 2B per-discipline consume of `discipline_id` named as the next move here — shipped 2026-05-25; see the top of this block. Next is now Slice 5.)_ **Spec bumped v3 → v4** (slice-status reconciliation; the design in §§1-11 + §13-14 is unchanged — only §0 +R5/R6 and §12 slice-status were edited). Remaining slices 5-6 sequenced in Spec v4 §12. _Predecessor focus chain below was not refreshed across the LogThis → FreqCaps → TriggeredByAdHoc → Layer2C-Telemetry → Layer3-caching → re-model sessions; preserved for history:_ **Phase 5.2 caller-side refresh frequency caps per D-64 §8 shipped 2026-05-21** — closes the §6.1 architect-recommended forward move from the LogThis+T1Hook handoff. T1 ≤3/24h, T2 ≤1/48h, T3 ≤1/7d server-side caps enforced via `_check_frequency_cap` counting `success=TRUE` rows in `plan_refresh_log`; over-cap submissions re-render the form with an auto-opening Bootstrap `#capExceededModal` mirroring the predecessor `#t1HookModal` pattern; [Refresh anyway] submits hidden `cap_override=1` → `cap_overridden=TRUE` lands on the resulting log row (per D4: only when route's own check returned exceeded AND form arrived with override). 4 substantive files (well under ceiling): `init_db.py` (cap_overridden column), `routes/plan_refresh.py` (`_TIER_CAP_LIMITS` + `_count_recent_refreshes` + `_check_frequency_cap` + cap check in POST + `_write_refresh_log` cap_overridden kwarg), `templates/plans/v2/refresh.html` (conditional modal markup + auto-open script), `tests/test_routes_plan_refresh.py` (+13 tests across 3 new classes + 2 added to TestWriteRefreshLog). 4 D-decisions ratified at AskUserQuestion gate (D1=auto-open modal on re-render; D2=count success=TRUE rows only — Andy flipped from architect's count-all-rows recommendation for fault-tolerance; D3=single-use override per refresh; D4=cap_overridden=TRUE only on real-exceeded). Tests 1363 → 1376 (+13); container subset 696 → 709 in ~1.7s. **NL parser refresh traffic from the T1 hook is now cost-gated end-to-end.** **Architect-recommended next move:** **`triggered_by_ad_hoc_id` plumbing wiring at `_write_refresh_log`** — the doc-sweep nit carried since the LogThis+T1Hook session. D-63 §5.4 column ships but its caller doesn't thread the FK; the T1 hook's [Yes — refresh] anchor encodes `suggestion_id` only implicitly via `nl_context`, so the refresh log row should carry `triggered_by_ad_hoc_id=<suggestion_id>` for downstream attribution. ~1-2 files (`routes/plan_refresh.py` + tests; possibly thread the suggestion_id through the `/plans/v2/refresh?...&triggered_by_ad_hoc_id=<id>` query param or via a server-side resolve from `nl_context` heuristic). Trigger #5: per-flow plumbing approach. Alternatives queued: **NL parser smoke-eval harness expansion + Haiku 4.5 migration eval per NL-1** (~5-6 files; need ~20-30 hand-labeled fixtures + a Haiku-vs-Sonnet agreement comparison harness); **Form-refresh D — §I.1 structured supplements** (Layer 2E §5.5 de-stub; ~6-8 files; `/plan` Triggers #1+#3+#5); **Layer 3A + 3B caching policy at orchestrator level** (~4-6 files; near-load-bearing with 4 entry points sharing user-scoped outputs); **routes/locales.py equipment-edit Layer 2C invalidation gap** (~1-2 files; doc-sweep nit from form-refresh C investigation); **Manual §5.0 walkthrough** of D-64 + D-63 + plan_create + dashboard CTAs + log-this/T1 hook + freq caps E2E on Neon (real-LLM ~$1.50 per pass across the 3 routes + T1 hook follow-on; freq-cap surface itself is no-cost beyond the modal interaction); **Plan Management spec authorship** to land 2E-2/3/4 contracts; **Real-LLM Layer 4 regression** parity to plan_refresh (~$0.30-$0.50 per cold synthesis on Pattern B + ~$0.50-$1.00 on Pattern A). Predecessor: **Phase 5.2 caller-side D-64 NL parser runtime + plan_refresh route shipped 2026-05-21** — closing the D-64 caller-side arc end-to-end. 8 substantive files (ceiling break ratified at scope-selection AskUserQuestion gate; precedented by 5.1.A=8 / 5.1.C=8 / 5.2.Caller-D63+PlanCreate=9): NEW `nl_parser.py` runtime + NEW `routes/plan_refresh.py` Blueprint `/plans/v2/refresh/*` + NEW `init_db.py` `plan_refresh_log` migration per D-64 §7.1 + NEW `templates/plans/v2/refresh.html` 3-button tier picker + NEW `templates/plans/v2/refresh_view.html` diff view + NEW `tests/test_nl_parser.py` (60 stub-LLM tests) + NEW `tests/test_nl_parser_smoke.py` (12 env-gated real-LLM tests) + NEW `tests/test_routes_plan_refresh.py` (36 helper tests). 4 D-decisions ratified at AskUserQuestion gate (D1=no-prior-plan empty-state CTA, D2=3-button tier UX, D3=NEW diff view, D4=telemetry shipped + caps deferred). Atomic write per D-64 §6.2; degraded-fallback per D-64 §5.4 on parser failure. Tests 1235 → 1331 (+96); container subset 568 → 664. **All 3 of 3 caller-side routes now E2E-reachable** (single_session + plan_create + plan_refresh). **Architect-recommended next move:** **Dashboard CTAs for `/workouts/build` + `/plans/v2/new` + `/plans/v2/refresh`** — all 3 caller-side routes are now reachable only via direct URL; D-63 §3.1 + D-64 §4.1 spec dashboard CTA cards. ~1-2 files (`templates/dashboard.html` edit + maybe a small helper). Pair with the manual §5.0 walkthrough so Andy can E2E-verify all 3 routes from the dashboard. Alternatives queued: **Log-this slice + D-63 T1 plan-check hook** (pairs with D-64 caller-side — already shipped; ~5-7 files); **Form-refresh D — §I.1 structured supplements** (Layer 2E §5.5 de-stub; ~6-8 files; `/plan` Triggers #1+#3+#5); **Layer 3A + 3B caching policy at orchestrator level** (~4-6 files; near-load-bearing with 4 entry points sharing user-scoped outputs); **Manual §5.0 walkthrough** of D-64 + D-63 + plan_create E2E on Neon (real-LLM ~$1.00 per pass across the 3 routes); **NL parser frequency caps per D-64 §8** (deferred this session; ~3-4 files including the `cap_overridden` column + modal-confirm intermediate page); **NL parser smoke-eval harness expansion + Haiku 4.5 migration eval per NL-1** (~5-6 files; need ~20-30 hand-labeled fixtures + a Haiku-vs-Sonnet agreement comparison harness). Predecessor: **Phase 5.2 caller-side D-64 NL parser prompt body shipped 2026-05-21** — `/plan` Trigger #2 LLM-prompt-design gate cleared per `Plan_Refresh_D64_Design_v1.md` Decision #12 deferral. NEW `aidstation-sources/prompts/NLParser_v1.md` (~580 LOC; 13-section structure mirroring Layer3A_v1.md / Layer4_SingleSession_v2.md); 14 D-decisions ratified at AskUserQuestion gate (D1=Sonnet 4.6 with Haiku migration as §12 candidate; D2=no extended thinking; D3=forced tool-use; D4=full ParsedIntent mirror minus driver-stamped raw_text; D5=middle-path injury disambiguation; D6=strict closed-vocab locale matching; D7=driver-stamped raw_text; D8=single retry on schema violation + degrade per D-64 §5.4; D9=classification-only voice; D10=temperature 0; D11=13-section structure; D12=NL_PARSER_PROMPT_VERSION constant lives in runtime module; D13=perf budget ~$0.003-$0.005/cold call; D14=smoke-eval harness in paired session). Pure design session — no code, no schema, no routes, no test count delta. **D-64 caller-side route now structurally unblocked** — next session ships `nl_parser.py` runtime + `routes/plan_refresh.py` + tier-picker template + stub-LLM tests + env-gated smoke tests (~5-7 substantive files; `/plan` gate per Trigger #1 form copy + Trigger #5 route shape — Trigger #2 cleared this session). **Architect-recommended next move:** **D-64 caller-side runtime session** — composes the prompt body shipped this session with `nl_parser.py` + `routes/plan_refresh.py` E2E. Builds the smoke-eval harness (~10-15 hand-labeled NL→ParsedIntent fixtures from Andy's PGE 2026 + AR vocab per §11.3 of prompt body); Sonnet vs Haiku eval signal lands here. Alternatives queued: **Log-this slice + D-63 T1 plan-check hook** (pairs with D-64 caller-side; ~5-7 files); **Dashboard CTAs for `/workouts/build` + `/plans/v2/new`** (~1-2 files; pair with §5.0 walkthrough); **Form-refresh D — §I.1 structured supplements** (Layer 2E §5.5 de-stub; ~6-8 files; `/plan` Triggers #1+#3+#5); **Layer 3A + 3B caching policy at orchestrator level** (~4-6 files; near-load-bearing); **Manual §5.0 walkthrough** of shipped D-63 + plan-create routes E2E on Neon. Predecessor: **Phase 5.2 caller-side routes (D-63 + plan-create) shipped 2026-05-21** atop the same-day-prior Phase 5.2 caller-side substrate. First end-to-end Flask route wiring of the Layer 4 orchestrator surface: both arcs compose `allocate_*` → `orchestrate_*` → `persist_*` → `db.commit()` atomically per D-64 §6.2. `routes/ad_hoc_workouts.py` exposes `/workouts/build` (GET form + POST orchestrate-and-persist) + `/workouts/suggestions/<id>` (GET result card) + discard + regenerate; `routes/plan_create.py` exposes `/plans/v2/new` (GET form + POST allocate-orchestrate-persist) + `/plans/v2/<plan_version_id>` (GET plan view grouped by date). 14 D-decisions ratified at AskUserQuestion gate before implementation (D1=`ad_hoc_workout_suggestions` table only, `cardio_log`/`training_log` `is_ad_hoc` extensions defer; D2=URL prefixes `/workouts/*` + `/plans/v2/*`; D3=broad framework_sport list; D4=quick-equipment "Somewhere else" path defer; D5=caller owns transaction; D6=`PostgresCacheBackend(lambda: get_db())` cache factory; D7=no target-race picker on plan-create form; D8=plan_versions notes=None for v1; D9=inline helpers in route modules, no new `ad_hoc_workouts_repo.py`; D10=[Regenerate]+[Discard] only on result view, [Log this workout] defers with T1 hook; D11=9-file ceiling break ratified — precedented by 5.1.A=8 / 5.1.C=8; D12=helper-level pytest density, end-to-end Flask test_client deferred to manual §5.0 walkthrough; D13=batch (a)+(b); D14=broad sport vocabulary). **Architect-recommended next move:** **D-64 caller-side route + NL parser glue** — slice 2's plan_refresh orchestrator is structurally complete + tested in isolation + persistence substrate available, but not E2E-reachable from v1 UI. Largest of the 3 caller-side arcs because it requires a separate `/plan` Trigger #2 LLM prompt-design session for the NEW `aidstation-sources/prompts/NLParser_v1.md` (D-64 Decision #12 deferred) BEFORE route work; then `nl_parser.py` LLM-backed implementation + `routes/plan_refresh.py` + tier-picker template + tests. **`/plan` gate per Trigger #2 (NL parser LLM prompt design)** + Trigger #1 (form copy) + Trigger #5 (route shape — dashboard vs plan-view entry points + redirect-after-refresh UX). OR **Log-this slice + D-63 T1 plan-check hook** — pairs with D-64 caller-side; adds `is_ad_hoc` extensions to `cardio_log`/`training_log`, wires [Log this workout] button on the suggestion view, fires the T1 plan check via D-64 route. ~5-7 files. Other alternatives: **Form-refresh D — §I.1 structured supplements** (Layer 2E §5.5 de-stub; ~6-8 files; Triggers #1+#3+#5). **Layer 3A + 3B caching policy** at orchestrator level (~4-6 files; near-load-bearing). **routes/locales.py equipment-edit Layer 2C invalidation gap** (~1-2 files; doc-sweep nit). **Plan Management spec authorship**. **Manual §5.0 walkthrough** of D-63 + plan-create routes E2E on Neon (the schema migration must apply + the dashboard CTAs to /workouts/build + /plans/v2/new need to be added before athletes can find the new surfaces — pair with §5.0). **Real-LLM Layer 4 regression** parity (~$0.50-$1.00 per cold synthesis on Pattern A's per-phase loop). Substrate for the D-63 + D-64 + plan-create caller-side route arcs — lifts the Layer 4 §7.11 + §7.12 spec contract (PlanSession natural key + per-day version pointer per D-64 §6.3) into deployed schema + repo helpers. Phase 5.2 slice 3 (`plan_create` Pattern A orchestrator) shipped earlier same-day atop the same-day Phase 5.2 slices 1 + 2 (single_session + plan_refresh); **orchestrator now exposes all 4 of 4 Layer 4 entry points** (race_week_brief 5.1 + single_session 5.2.S1 + plan_refresh 5.2.S2 + plan_create 5.2.S3); **Phase 5.2 complete, closing Phase 5**. **Architect-recommended next moves shift to caller-side route handlers:** (a) **D-64 caller-side route + NL parser glue** — slice 2's plan_refresh orchestrator is structurally complete + tested in isolation but not E2E-reachable from v1 UI; ~3-5 files (new `routes/plan_refresh.py` + tier-picker template + NL parser implementation + tests). **`/plan` gate per Trigger #2 (LLM prompt design — D-64 §5 NL parser prompt body is deferred)** + Trigger #1 (form copy) + Trigger #5 (route shape). OR (b) **D-63 caller-side route** (`ad_hoc_workout_suggestions` table + `routes/ad_hoc_workouts.py` — slice 1 E2E-reachability; ~3-4 files; Trigger #1 only). OR (c) **plan-create caller-side route** (~3-5 files; shares `plan_versions` table with D-64). OR (d) **Form-refresh D — §I.1 structured supplements** (Layer 2E §5.5 de-stub; ~6-8 files; Triggers #1+#3+#5). Other alternatives: **Layer 3A + 3B caching policy** at orchestrator level (~4-6 files; near-load-bearing with 4 entry points); **3B None-tolerant kwargs L3B-P-2** consumer migration; **routes/locales.py equipment-edit Layer 2C invalidation gap** (~1-2 files; doc-sweep nit); **Plan Management spec authorship**; **manual §5.0 walkthrough** of all 4 orchestrator entry points + persistence E2E (once caller-side lands); **real-LLM Layer 4 regression** parity (~$0.50-$1.00 per cold synthesis on Pattern A's per-phase loop). Shared `_upstream_full_cone` helper reused as-is at slice 3 (no changes — slice 2's "slice 3 reuses the helper as-is" forecast confirmed). Architect-recommended next moves shift to E2E-reachability: **(a) D-64 caller-side route + `plan_versions` table + NL parser glue** — slice 2's plan_refresh orchestrator is structurally complete + tested in isolation but not E2E-reachable from the v1 UI; ~4-6 files (init_db.py migration + new `routes/plan_refresh.py` + templates + tests + D-64 NL parser glue); `/plan` gate per Triggers #1 + #3 + #5 (form copy + schema + NL parser design). OR **(b) D-63 caller-side route + `ad_hoc_workout_suggestions` table** — slice 1's single_session orchestrator E2E-reachability surface; ~3-4 files (init_db.py migration + new `routes/ad_hoc_workouts.py` + template + tests); `/plan` gate per Trigger #1 (form copy). OR **(c) Plan-create caller-side route + `plan_versions` table** — slice 3's plan_create orchestrator E2E-reachability surface; can share the `plan_versions` table with the D-64 caller-side (a); ~3-5 files. OR **(d) Form-refresh D — §I.1 structured supplements** (Layer 2E §5.5 de-stub; ~6-8 files; `/plan` gate per Triggers #1 + #3 + #5 — table vs JSONB schema choice). Other alternatives: **Layer 3B None-tolerant kwargs L3B-P-2** consumer migration; **`routes/locales.py` equipment-edit Layer 2C invalidation gap** (1-2 files; surfaced as nit during form-refresh C); **Plan Management spec authorship** to land 2E-2/3/4 contracts; **3A/3B caching policy modules at orchestrator level** — slice 3 added the 3rd full-cone consumer making this near-load-bearing; **manual §5.0 walkthrough** of the accumulated scenarios + all 4 orchestrator entry points end-to-end (once caller-side routes land; real-LLM ~$0.50-$1.00 pass against Andy's PGE 2026 row); **real-LLM Layer 4 regression** parity to plan_create (~$0.30-$0.50 per cold synthesis on Pattern A's per-phase loop).

Orthogonal alternatives tracked in `CARRY_FORWARD.md`.

## Layer status

| Layer | Status |
|---|---|
| **0** | DEPLOYED |
| **1** | 🟢 v1 spec + typed payload + runtime builder shipped 2026-05-19 (D-51 design wave + Phase 1.2 schema arc + Phase 1.3 builder); 🟢 injury_log §B.1/§B.1.1/§B.3 extensions shipped 2026-05-19 (Phase 2.2 paired) |
| **2** | 🟢 All 5 of 5 runtimes shipped — 2A + 2D + 2B + 2E (Phase 2.1 + 2.2 + 2.3 + 2.5, 2026-05-19) + 2C (Phase 2.4-Prep substrate 2026-05-19 + Phase 2.4 builder 2026-05-20); 2E ships vertical-slice (§5.5 supplements + §5.8 heat acclim stubbed pending §I.1 refresh + Plan Management spec); 🟢 **2B per-discipline terrain gaps shipped 2026-05-25** (best-fit re-model Slice 4 — additive `Layer2BPayload.terrain_by_discipline` consuming `RaceTerrainEntry.discipline_id`; flat aggregate unchanged); 🟢 **training-substitution resolver is the sole best-fit node as of 2026-05-25** (re-model Slice 5 shipped `resolve_training_substitution` in `layer2_modality/substitution.py` consuming the 2B blocks → `TrainingSubstitutionPayload`, db-less; **Slice 6 retired the v2 `Layer2ModalityPayload` resolver entirely** — `layer2_modality/resolver.py` + `_MODALITY_OPTIONS_PER_DISCIPLINE` deleted) |
| **3** | 🟢 3A + 3B complete + cached wrappers wired at orchestrator (3A substrate + driver shipped 2026-05-20; 3B driver shipped same-day Phase 4 2026-05-20; 3A + 3B cached wrappers wired into orchestrator at 4 entry points + extended `_EVICTION_POLICY` 2026-05-21 Phase 5.2 Layer 3 caching slice). 3C + 3D + 3.5 designed, not yet implemented |
| **3.5** | Designed; not yet implemented |
| **4** | SPEC COMPLETE §§1-14; Implementation Steps 2 + 3 + 4a-4f + Step 7 SDK smoke scaffolding (3A + 3B subset) + Phase 5.1 orchestrator vertical slice (race_week_brief) + Phase 5.2 slice 1 (single_session_synthesize orchestrator) + Phase 5.2 slice 2 (plan_refresh T1/T2/T3 orchestrator + `_upstream_full_cone` shared helper) + Phase 5.2 slice 3 (plan_create Pattern A orchestrator) + Phase 5.2 caller-side substrate (`plan_sessions` + repo) + Phase 5.2 caller-side D-63 + plan-create Flask routes + Phase 5.2 caller-side D-64 NL parser prompt body (`NLParser_v1.md`) + Phase 5.2 caller-side D-64 NL parser runtime (`nl_parser.py`) + plan_refresh route (`routes/plan_refresh.py` + `plan_refresh_log` migration + diff view) + Phase 5.2 dashboard CTAs (3 cards on `templates/dashboard.html` for `/workouts/build` + `/plans/v2/new` + `/plans/v2/refresh` with `_has_plan_version` conditional disable on Refresh) + Phase 5.2 log-this slice + D-63 T1 plan-check hook (cardio_log/training_log `is_ad_hoc` extensions + `t1_hook_telemetry` table + `#t1HookModal` post-log modal + `/plans/v2/refresh` query-param prefill) + Phase 5.2 refresh frequency caps per D-64 §8 (cap_overridden column on `plan_refresh_log` + `_TIER_CAP_LIMITS` + `_check_frequency_cap` + `#capExceededModal` modal-confirm) of 8 COMPLETE; **all 4 of 4 entry points wired; all 3 of 3 caller-side routes E2E-reachable AND dashboard-surfaced AND cost-gated by per-tier frequency caps** (single_session + plan_create + plan_refresh); caller-side v2 surfacing arc complete; **best-fit re-model COMPLETE (Slice 6, 2026-05-25)** — `training_substitution_payload` threaded into all three race-prep entry points (race_week_brief + plan_create + plan_refresh T1/T2/T3) with a `training_substitution_hash` cache slot on each key; the per-phase renderer consumes `TrainingSubstitution`; the v2 `Layer2ModalityPayload` resolver + its 3 renderers are deleted (single_session intentionally carries no best-fit section — no Layer 2B in its cone) |
| **5** | Not yet specced |

## D-73 upstream implementation arc

Multi-session plan in `Upstream_Implementation_Plan_v1.md`. Phase 1.1 (D-51 design wave) + Phase 1.2A/B/C (schema arc) + Phase 1.3 (Layer 1 typed payload + builder + spec) + Phase 2.1 (Layer 2A discipline classifier) + Phase 2.2 (Layer 2D injury risk + paired injury_log §B schema evolution + §B form UI evolution) + Phase 2.3 (Layer 2B terrain classifier + paired Layer 0 terrain_gap_rules severity reclassification + paired pydantic widening) + Phase 2.5 (Layer 2E nutrition baseline vertical slice — daily targets + race-day fueling + dietary patterns + simplified sleep-dep; §5.5 + §5.8 stubbed) + Phase 2.4-Prep (Layer 2C data substrate — 4 column adds on `layer0.exercises` + `layer0.sport_specific_gear_toggles`; new toggle migration; ETL extractor wiring; structured substitutes routed via `etl/sources/parsed_substitutes.json`) shipped 2026-05-19; Phase 2.4 builder (Layer 2C equipment mapper — §5 tier-cascade algorithm + §8 coaching flags + §6 one-hop `also_satisfies` semantics + §5.6 accommodation pass-through from `Layer2DPayload`) shipped 2026-05-20 closing Phase 2; Phase 3.1-Substrate + Phase 3.1-Driver (Layer 3A integration accessors + LLM driver) shipped 2026-05-20 closing Layer 3A; Phase 4 (Layer 3B LLM driver + paired §8.3 doc-sweep) shipped 2026-05-20 closing Layer 3B; **Phase 5.1 (Layer 4 orchestrator vertical slice — race_week_brief end-to-end) shipped 2026-05-20** atop the same-day Step 7 SDK smoke scaffolding (PR #109 merged into main as `8854fa4`; `layer4/orchestrator.py:orchestrate_race_week_brief` + paired Layer4_Spec.md §4.5 D-72 audit-trail wording fix). **Phase 5.1 form-refresh A (race_terrain + aid_stations end-to-end on race-event edit path) shipped 2026-05-20**, closing the highest-leverage orchestrator forward-pointer + partial-closing Layer2B_Spec.md §12 Open Item 2B-3 for the race-event edit surface. **Phase 5.1 form-refresh B (§H.2 onboarding step-3c terrain capture) shipped 2026-05-20**, mirroring A's editor on the onboarding side via a shared `templates/_race_terrain_editor.html` partial and **fully closing Layer2B_Spec.md §12 Open Item 2B-3** (both onboarding + post-onboarding edit surfaces now capture TRN-xxx + percent rows). **Phase 5.1 form-refresh C (§J locale-terrain capture + paired Layer 2B `_validate_inputs` loosen for empty race_terrain) shipped 2026-05-20**, adding `locale_profiles.locale_terrain_ids TEXT[]` + multi-checkbox widget on both edit branches of the locale-edit form + orchestrator `_q_locale_terrain_ids(db, uid, primary_locale)` helper + Layer 2B `race_terrain_unset` coaching flag for athletes who skipped §H.2 capture; **fully closes Layer2B_Spec.md §12 Open Item 2B-2** (`§J Locale terrain access` controlled vocab on canonical TRN-xxx). **Phase 5.2 slice 1 (`orchestrate_single_session_synthesize`) shipped 2026-05-20** — second Layer 4 entry-point orchestrator atop the same-day Phase 5.1 trilogy completion; composes Layer 1 → 2A → 2D → 2C (locale-only) → 3A → `llm_layer4_single_session_synthesize_cached` for D-63 on-demand workouts; new `_q_locale_by_slug(db, uid, locale)` helper validates athlete-picked locale; `OrchestrationError` codes `request_sport_unavailable` + `locale_unknown` + `etl_version_set_undiscoverable`; 9 D-decisions ratified at plan-mode gate per Trigger #5. **Phase 5.2 slice 2 (`orchestrate_plan_refresh` T1/T2/T3) shipped 2026-05-20** — third Layer 4 entry-point orchestrator; composes the full upstream cone (Layer 1 → 2A/B/D/C → 3A → 3B → 2E) → `llm_layer4_plan_refresh_cached`; driver dispatches T1/T2/T3 internally (T3 cross-phase routes to Pattern A via the Step 4f-shipped `_route_t3_cross_phase_to_pattern_a`). **Shared `_upstream_full_cone` helper extracted per D1** — race_week_brief refactored to consume (behavior-preserving; 17 existing tests pass unchanged); single_session opts out per cone-shape divergence. 10 D-decisions ratified at plan-mode gate per Trigger #5 (D1 flip = extract-now; D3 = caller-supplied kwargs for plan_version_id/plan_version_id_parent/prior_plan_session_window/parsed_intent/plan_start_date; D4 = 3 shared pre-flight gates only; D9 = single function with tier kwarg). **Phase 5.2 slice 3 (`orchestrate_plan_create` Pattern A) shipped 2026-05-20** — fourth and final Layer 4 entry-point orchestrator closing Phase 5.2; reuses the slice 2 `_upstream_full_cone` helper as-is (no changes — slice 2's "slice 3 reuses the helper as-is" forecast confirmed); driver runs per-phase synthesis loop + seam reviews + final cross-phase validator pass internally per §5.2 (Pattern A heaviest). 10 D-decisions ratified at plan-mode gate per Trigger #5: D1=reuse helper as-is; D2=`plan_start_date` required kwarg (no orchestrator default); D3=`plan_version_id` caller-supplied (matches slice 1/2 precedent); D4=3 pre-flight gates only (shared via helper; NO `no_target_event` since open-ended plans are first-class; NO orchestrator-side `plan_start_date_in_past`/`plan_version_id_unset`/`time_to_event_weeks_mismatch`/`discipline_weights_invalid` — driver covers); D9=layer2c packed as `{primary_locale: payload}` dict (race_week_brief shape, NOT plan_refresh's Layer2Bundle). **Phase 1 + Phase 2 + Phase 3 + Phase 4 + Phase 5.1 + Phase 5.1 form-refresh A + B + C + Phase 5.2 slices 1 + 2 + 3 complete; orchestrator now exposes all 4 of 4 Layer 4 entry points; Phase 5.2 complete, closing Phase 5.** Phase 2.5 de-stubs (§I.1 supplement form refresh + Plan Management spec) + Phase 4 L3B-P-2 §H.2 deployed-shape gap remain as the next form-refresh follow-on (D structured supplements). E2E-reachability arc opens next: D-63 caller-side route (`ad_hoc_workout_suggestions` table + `routes/ad_hoc_workouts.py`) + D-64 caller-side route (`plan_versions` table + `routes/plan_refresh.py` + NL parser glue) + plan-create caller-side route.

**Forcing function:** Andy's PGE 2026 (2026-07-17). `race_week_brief` auto-fires 2026-07-03 (days_to_event = 14). ~10 weeks of runway from 2026-05-19.

## Tests

**1636 green + 16 skipped** (last measured 2026-05-25 after D-74/D-75 cleanup — 1630 post-R6 baseline + 6 new `TestMaxEtlVersion` cases. Earlier lineage: Slice 6 measured 1631; R6 took it to 1630 (−1 deleted whitewater test) — full suite with `flask`+`pydantic`+`requirements.txt`+`pytest` installed in-sandbox so all route-test modules collect). The −49 vs Slice 5's 1680 is the deleted v2 resolver test file (`tests/test_layer2_modality.py`) + the `tests/test_layer4_hashing.py` migration (v2 hash-slot tests → `training_substitution_hash` + new plan_refresh slot tests); zero regressions; v2-surface grep clean repo-wide. Run with `python -m pytest tests/` (the deps live under `/usr/local/bin/python`, not the `~/.local/bin/pytest` shim). **NOTE:** the prior `1649` headline (Slice 4) + `1616` (Slice 2 — partial-collection, route-test modules import-blocked) are the prior baselines; the clean full-suite count is higher. The Slice-2 +12 / Slice-1 1604 / pre-re-model 1630 deltas below remain valid as session-relative numbers. The 1419 figure and its chain below are pre-re-model history, preserved per the rolling-pointer convention.

1419 green + 16 skipped (last measured 2026-05-21 after Phase 5.2 Layer 3 caching slice — NEW `tests/test_layer3_cached_wrappers.py` 13 tests across 2 classes (`TestLayer3ACachedWrapper` 6 + `TestLayer3BCachedWrapper` 7) covering hit/miss flow + cache key composition + serialization round-trip + entry_point label preservation; `tests/test_layer4_cache.py` 4 policy tests updated (`_includes_layer3` rename) + 1 layer3b disjointness assertion extension + `test_layer1_evicts_all_user_rows` rewritten as `test_layer1_evicts_layer4_and_layer3_preserves_nl_parser` + NEW `test_layer3a_change_evicts_layer3b_preserves_layer3a` = +1 net in cache.py; `tests/test_layer4_orchestrator.py` mock targets updated at 10 patch sites (no test count delta). **Net 1405 → 1419 (+14 net new; container subset 738 → 752 in ~1.2s).** 12 NL parser smoke + 4 prior Layer 3 SDK smoke = 16 total skipped when key unset. **Previous baseline chain (stale below — successive sessions LogThis / FreqCaps / TriggeredByAdHoc / Layer2C-Telemetry did not refresh this block; current count 1419 derives from each handoff's own §4 test-delta narrative).** Phase 5.2 dashboard CTAs measured 1334 green; intermediate sessions added LogThis +29 (→1363) → FreqCaps +13 (→1376) → TriggeredByAdHoc +8 (→1384) → Layer2C-Telemetry +21 (→1405) → Layer 3 caching +14 (→1419). Below is the verbatim pre-LogThis chain preserved for reference: 1334 green + 16 skipped (last measured 2026-05-21 after Phase 5.2 dashboard CTAs — 1 NEW test file: `tests/test_routes_dashboard.py` 3 tests in `TestHasPlanVersion` (returns True when row present + returns False when row absent + SQL is user_id-scoped). **Net 1331 → 1334 (+3 net new; container subset 664 → 667 in ~1.8s).** 12 NL parser smoke + 4 prior Layer 3 SDK smoke = 16 total skipped when key unset. **Previous baseline (Phase 5.2 caller-side D-64 runtime + plan_refresh route):** 1331 green + 16 skipped — 3 NEW test files: `tests/test_nl_parser.py` 60 + `tests/test_nl_parser_smoke.py` 12 (env-gated, skip when `ANTHROPIC_API_KEY` unset; 4 prior Layer 3 SDK smoke remain at parity) + `tests/test_routes_plan_refresh.py` 36. Net 1235 → 1331 (+96 net new; container subset 568 → 664 in ~1.0s). Pre-existing 4 Layer 3 SDK smoke + 12 new NL parser smoke = 16 total skipped when key unset. **Previous baseline (Phase 5.2 caller-side D-63 + plan-create routes):** 1235 green + 4 skipped (last measured 2026-05-21 after Phase 5.2 caller-side D-63 + plan-create routes — `routes/ad_hoc_workouts.py` + `routes/plan_create.py`: 1190 → 1235 (+45 net new tests across 2 NEW test files: `tests/test_routes_ad_hoc_workouts.py` 29 tests across 8 classes — `TestAthleteSportChoices` 3 / `TestAllocateSuggestion` 3 / `TestPersistGeneratedSession` 1 / `TestGetSuggestion` 4 (hit + miss + JSONB string path + None-generated-session-pre-orchestrate) / `TestMarkStatus` 3 (discarded sets discarded_at + regenerated sets link + rejects unsupported status) / `TestDecodeJsonb` 5 (None + dict + list + JSON-string + TypeError on unexpected) / `TestParseRequestForm` 8 (happy + notes preserved + 5 rejection paths + VALID_INTENSITIES constant) / `TestOrchestrationErrorMessage` 2 (known codes + unknown fallback); `tests/test_routes_plan_create.py` 16 tests across 4 classes — `TestParsePlanStartDate` 6 (happy + whitespace + empty + missing-key + invalid-format + out-of-range) / `TestLoadPlanVersion` 3 (dict-on-hit + None-on-miss + user_id scoping defense) / `TestResolvePlanScopeEndDate` 5 (race-date used / 24w fallback no race / 24w fallback past race / 24w fallback missing event_date / same-day race uses race date) / `TestOrchestrationErrorMessage` 2). Container-runnable subset 523 → 568 passing in ~1.0s. **Previous baseline (Phase 5.2 caller-side substrate):** 1168 → 1190 (+22 net new tests in NEW `tests/test_plan_sessions_repo.py` across 5 classes — `plan_sessions` table + repo: 1168 → 1190 (+22 net new tests in NEW `tests/test_plan_sessions_repo.py` across 5 classes — `TestAllocatePlanVersionRow` 6 (happy path RETURNING id round-trip + notes JSONB serialization + created_via/pattern/scope-date input validation rejections + RuntimeError on no-RETURNING-row) / `TestPersistLayer4Sessions` 3 (inserts each session with natural-key columns + empty-list no-op + user_id from payload threads to every row) / `TestLoadPlanSessionsByVersion` 3 (JSONB → PlanSession round-trip + JSONB string-path hydration + empty rows returns []) / `TestLoadPriorPlanSessionWindow` 9 (tier-tied defaults T1=2/T2=7/T3=28 days + days kwarg override + ValueError when both None + ValueError when days<=0 + DISTINCT ON resolver SQL + ORDER BY clause + empty result + PlanSession hydration) / `TestTierDefaultMapping` 1 (sanity-pin on `_PRIOR_WINDOW_DAYS_BY_TIER`). Container-runnable subset 501 → 523 passing in ~1.6s. **Previous baseline (Phase 5.2 slice 3):** 1158 → 1168 (+10 net new tests in `tests/test_layer4_orchestrator.py` across 5 `TestOrchestratePlanCreate*` classes — `TestOrchestratePlanCreateHappyPath` 2 (event-mode + no-event-mode) / `TestOrchestratePlanCreatePreflightGates` 3 (etl_version_set_undiscoverable + primary_locale_missing + framework_sport_missing) / `TestOrchestratePlanCreateDefaults` 2 (today defaults + layer2c packed as primary-locale dict) / `TestOrchestratePlanCreatePassThrough` 2 (plan_start_date + plan_version_id thread verbatim) / `TestOrchestratePlanCreateReturnValue` 1 (cached-wrapper output passes through verbatim). Container-runnable subset 491 → 501 passing in ~1.3s. Race_week_brief + single_session + plan_refresh existing tests all pass unchanged (no regression — slice 3 only added the new function, no refactor). **Previous baseline (Phase 5.2 slice 2):** 1145 → 1158 (+13 net new tests in `tests/test_layer4_orchestrator.py` across 6 `TestOrchestratePlanRefresh*` classes). Container-runnable subset 478 → 491 passing in ~1.0s. **Previous baseline (Phase 5.2 slice 1):** 1135 → 1145 (+10 net new tests in `tests/test_layer4_orchestrator.py` across 6 `TestOrchestrateSingleSessionSynthesize*` classes). Container-runnable subset 468 → 478 passing in ~0.9s. **Previous baseline (Phase 5.1 form-refresh C):** 1110 → 1135 (+25 net new: 1110 → 1135 (+25 net new: +21 in NEW `tests/test_locales.py` across 5 classes — `TestTrnPattern` 2 / `TestParseLocaleTerrain` 8 / `TestTerrainChoices` 2 / `TestHydrateLocaleTerrainIds` 8 / `TestEvictLayer2bOnTerrainChange` 1; +4 in `tests/test_layer4_orchestrator.py::TestLocaleTerrainIdsWireUp` covering `_q_locale_terrain_ids` SELECT response threading into Layer 2B kwargs + NULL column path + missing row path + SQLite-shim JSON-string path; +4 in `tests/test_layer2b.py::TestEmptyRaceTerrainLoosen` for the loosen pair (1 existing `test_empty_race_terrain_raises` removed, net +3 here) — the 4 new layer2b tests remain pre-existing-circular-import-blocked from collection at the container subset, same as the rest of `tests/test_layer2b.py`; counted toward the 1135 production total at the spec/regression run; 4 real-LLM smoke tests in `tests/test_layer3a_smoke.py` + `tests/test_layer3b_smoke.py` skip cleanly when `ANTHROPIC_API_KEY` unset → default `pytest tests/` reads "1135 passed, 4 skipped" once the circular-import is resolved). Container-runnable subset measured during this session: 468 passed (layer4 + race_events + onboarding race_events + locales) in 1.00s. When key is set, smoke tests exercise actual Sonnet 4.6 (~$0.12/run for the 4 3A/3B tests; Layer 4 real-LLM smoke not yet wired — Phase 5.2 alternative).

---
