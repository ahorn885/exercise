# AIDSTATION — Current State

Single rolling-state pointer. Changes on every shipped session. Long-form session narrative lives in `handoffs/`; rolling cross-session items live in `CARRY_FORWARD.md`.

---

## Last shipped session

`handoffs/V5_Implementation_D73_Phase_5_2_PlanRefresh_Orchestrator_2026_05_20_Closing_Handoff_v1.md` — 2026-05-20

D-73 Phase 5.2 slice 2 — **`orchestrate_plan_refresh` shipped** atop the same-day Phase 5.2 slice 1 (single_session). Third Layer 4 entry-point orchestrator (after race_week_brief 5.1 + single_session 5.2.S1). Composes the full upstream cone (Layer 1 → 2A/B/D/C → 3A → 3B → 2E) → `llm_layer4_plan_refresh_cached`; the driver dispatches T1/T2/T3 internally and routes T3 cross-phase to Pattern A via `_route_t3_cross_phase_to_pattern_a` (shipped at Step 4f 2026-05-18). **2 substantive files** (well under 5-file ceiling): (1) `layer4/orchestrator.py` — new public `orchestrate_plan_refresh(db, user_id, *, tier: Literal["T1","T2","T3"], refresh_scope_start, refresh_scope_end, plan_version_id, plan_version_id_parent, prior_plan_session_window, cache, parsed_intent=None, plan_start_date=None, today=None) -> Layer4Payload`; new private `@dataclass(frozen=True) _UpstreamFullCone` (11 fields: etl_version_set + framework_sport + primary_locale + all 8 payloads); new private `_upstream_full_cone(db, user_id, today, *, target_race_event)` helper that emits the 3 shared pre-flight gates (`etl_version_set_undiscoverable` / `framework_sport_missing` / `primary_locale_missing`) and composes the Layer 1 → 2A → 2B → 2D → 2C → 3A → 3B → 2E pipeline; `orchestrate_race_week_brief` REFACTORED to consume the helper (behavior-preserving — all 17 existing race_week_brief tests pass unchanged); single_session NOT refactored (opted out per cone-shape divergence — narrower cone uses `request.sport` not `primary_sport`, `request.locale_slug` not `'home'`, optional 2C); module docstring rewritten from "two-entry-point" → "three-entry-point" shape + helper-extract decision documented; new imports (`dataclass`, `Literal`, payload types + `Layer2Bundle` + `ParsedIntent` + `RaceEventPayload` from `layer4.context`, `PlanSession` from `layer4.payload`, `llm_layer4_plan_refresh_cached` from `layer4.cached_wrappers`); `__all__` extended; (2) `tests/test_layer4_orchestrator.py` — 13 new tests across 6 `TestOrchestratePlanRefresh*` classes (`TestOrchestratePlanRefreshHappyPath` 4: parametrized [T1/T2/T3] tier dispatch + parsed_intent threading; `TestOrchestratePlanRefreshPreflightGates` 3: etl_version_set_undiscoverable + primary_locale_missing + framework_sport_missing; `TestOrchestratePlanRefreshNoEventMode` 1: race_event_payload=None when no target race + Layer 2B/2E/3B receive empty/None inputs; `TestOrchestratePlanRefreshDefaults` 2: today defaults to date.today() + parsed_intent defaults to None; `TestOrchestratePlanRefreshTierPassThrough` 2: T3 plan_start_date threads to wrapper + prior_plan_session_window passes verbatim; `TestOrchestratePlanRefreshReturnValue` 1: cached wrapper output passed through verbatim); new `_plan_refresh_patches(*, layer4_return)` patches the same 10 import sites as race_week_brief (build_layer1_payload + q_layer2a + q_layer2b + q_layer2c + q_layer2d + q_layer2e + assemble_layer3a_bundle + llm_layer3a + llm_layer3b + llm_layer4_plan_refresh_cached); new `_fake_plan_refresh_layer4_payload(plan_version_id)` factory constructs valid Pattern B plan_refresh Layer4Payload (mode invariants per payload.py:553-565 = `mode='plan_refresh'` + `pattern='B'` requires `phase_structure=None` + `seam_reviews=None`); new `_default_prior_plan_session_window()` helper constructs a single non-ad-hoc PlanSession for the prior-window kwarg. 10 D-decisions ratified at plan-mode gate per Trigger #5 (architectural alternatives): D1=**extract `_upstream_full_cone` helper now + refactor race_week_brief to consume** (Andy flipped from architect's defer-to-slice-3 recommendation; full-cone helper covers 2/3 entry points immediately, single_session opts out per cone-shape divergence); D2=tier as required kwarg from caller (D-64 dispatch happens at route layer, not orchestrator); D3=plan_version_id + plan_version_id_parent + prior_plan_session_window + parsed_intent + plan_start_date all caller-supplied (matches slice 1 D4 for D-63's `suggestion_id`); D4=3 pre-flight gates only (shared `_q_*` discovery gates; no `no_target_event` since no-event refresh supported; no auto-fire window since refresh fires on demand); D5=target-race lookup conditional, not gated; D6=`parsed_intent: ParsedIntent | None` pass-through (D-64 §5.4 graceful degradation); D7=`today` mirrors race_week_brief + single_session signatures; D8=call `llm_layer4_plan_refresh_cached`; D9=single function `orchestrate_plan_refresh(tier=...)` not 3 per-tier functions (matches driver's internal dispatch); D10=13 tests across 6 classes at parity with single_session test density. **Tests 1145 → 1158 (+13 net new orchestrator tests; container-runnable subset 478 → 491 passing in ~1.0s).** Bookkeeping: `layer4/__init__.py` re-exports `orchestrate_plan_refresh` alongside the existing 2 entry points + `OrchestrationError` + `ParsedIntent`. No new spec edits (Layer4_Spec.md §3.2 already documents the driver entry-point contract; D-64 design doc already documents tier semantics + dispatch rules; orchestrator-side composition follows the slice 1 + race_week_brief precedent of not being spec'd separately). No new schema, route, template, or prompt-body work. No `/plan` Triggers #1 / #3 fired. **Phase 5.2 slice 2 complete; orchestrator now exposes 3 of 4 Layer 4 entry points (race_week_brief 5.1 + single_session_synthesize 5.2.S1 + plan_refresh 5.2.S2). Remaining: `plan_create` Pattern A (slice 3).**

**Predecessor:** `V5_Implementation_D73_Phase_5_2_SingleSession_Orchestrator_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.2 slice 1 — **`orchestrate_single_session_synthesize` shipped** atop the same-day Phase 5.1 form-refresh trilogy completion. Second Layer 4 entry-point orchestrator (after race_week_brief 5.1). Composes upstream Layer 1 → 2A → 2D → 2C (locale-only) → 3A → `llm_layer4_single_session_synthesize_cached` for D-63 on-demand workouts. Materially narrower cone than race_week_brief (no 2B / 2E / 3B / 3B-cached). **2 substantive files** (well under 5-file ceiling): (1) `layer4/orchestrator.py` — new public `orchestrate_single_session_synthesize(db, user_id, request, suggestion_id, *, cache, today=None) -> Layer4Payload`; new `_q_locale_by_slug(db, uid, locale) -> bool` helper (distinct from `_q_primary_locale` which hard-codes `'home'` — single-session athletes pick any configured locale); existing `_q_current_etl_version_set` + `_q_locale_equipment_pool` shared verbatim with race_week_brief; module docstring + `__all__` updated to reflect two-entry-point shape. `OrchestrationError` codes: `request_sport_unavailable` (Layer 2A's `Layer2AInputError` on unknown framework_sport caught + re-raised with detail); `locale_unknown` (slug not present in `locale_profiles`); `etl_version_set_undiscoverable` (shared). NO `no_target_event` / `race_week_brief_too_early` gates — single-session is off-plan, off-race, athlete-driven by definition; the driver's own `_validate_inputs` (`layer4/single_session.py` §4.4) covers locale-XOR-quick + 2C-payload-presence rules; (2) `tests/test_layer4_orchestrator.py` — 10 new tests across 6 `TestOrchestrateSingleSessionSynthesize*` classes (`TestOrchestrateSingleSessionSynthesizeHappyPath` 2: locale-path + quick_equipment-path; `TestOrchestrateSingleSessionSynthesizePreflightGates` 2: request_sport_unavailable + locale_unknown; `TestOrchestrateSingleSessionSynthesizeDiscoveryFailures` 1: etl_version_set_undiscoverable; `TestOrchestrateSingleSessionSynthesizeDefaults` 2: today defaults + 2D-payload threaded into 2C; `TestOrchestrateSingleSessionSynthesizeSportSemantics` 2: request.sport overrides primary_sport (D2) + quick_equipment path skips locale_by_slug SELECT; `TestOrchestrateSingleSessionSynthesizeReturnValue` 1: cached-wrapper output passed through verbatim); each uses `_single_session_patches()` patch-stack (narrower than race_week_brief's `_patches()` — patches 7 import sites: build_layer1_payload + q_layer2a + q_layer2c + q_layer2d + assemble_layer3a_bundle + llm_layer3a + llm_layer4_single_session_synthesize_cached — no 2B / 2E / 3B / race_week_brief driver) + the existing `_FakeConn` fixture pattern. 9 D-decisions ratified at plan-mode gate (Trigger #5 — architectural alternatives) before implementation: D1=defer shared `_upstream_pipeline` extract until 3rd entry point lands (single_session's cone narrower than race_week_brief's; Rule of Three is the right discipline; refactoring race_week_brief now risks regression on a tested surface; per `plan_refresh` arrival the full overlap surfaces); D2=`request.sport` as `framework_sport` for Layer 2A (athlete-overriding per D-63 §6.1; e.g., Andy picks Rowing for cross-training even though primary is AR); D3=skip Layer 2C entirely when `request.locale_slug` is None (quick_equipment path); driver handles the None case per spec §3.3 row 4; D4=`suggestion_id: int` as a kwarg from the caller, not orchestrator-allocated (`ad_hoc_workout_suggestions` table not yet deployed per D-63 §5.3); D5=3 pre-flight gates only (request_sport_unavailable + locale_unknown + etl_version_set_undiscoverable); D6=validate slug via new `_q_locale_by_slug` (cleaner error contract than trust-and-let-empty-pool-emerge); D7=`today: date | None = None` kwarg mirroring race_week_brief signature for deterministic test injection; D8=10 tests at parity with race_week_brief precedent; D9=orchestrator calls `llm_layer4_single_session_synthesize_cached` (not the raw driver) — matches race_week_brief precedent. **Tests 1135 → 1145 (+10 net new orchestrator tests; container-runnable subset 468 → 478 passing in ~0.9s).** Bookkeeping: `layer4/__init__.py` re-exports `orchestrate_single_session_synthesize` alongside `orchestrate_race_week_brief` + `OrchestrationError`. No new spec edits (Layer4_Spec.md §3.3 already documents the driver entry-point contract; orchestrator-side composition follows the race_week_brief precedent of not being spec'd separately). No new schema, route, template, or prompt-body work. No `/plan` Triggers #1 / #3 fired. **Phase 5.2 slice 1 complete; orchestrator now exposes 2 of 4 Layer 4 entry points (race_week_brief 5.1 + single_session_synthesize 5.2.S1). Remaining: `plan_refresh` T1/T2/T3 + `plan_create`.**

**Predecessor:** `V5_Implementation_D73_Phase_5_1_FormRefresh_C_Locale_Loosen_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.1 form-refresh C — **§J locale-terrain capture + Layer 2B `_validate_inputs` empty-race_terrain loosen shipped** atop the same-day form-refresh A + B. **Fully closes Layer2B_Spec.md §12 Open Item 2B-2** (canonical TRN-xxx vocab on `§J Locale terrain access`); flips the orchestrator's last `locale_terrain_ids=[]` forward-pointer; paired loosen makes athletes who skip §H.2 capture surface `race_terrain_unset` as a coaching flag instead of failing on `Layer2BInputError`. **8 substantive files** (ceiling break ratified at plan-mode gate per Triggers #1 + #3, precedented by 5.1.A 8 files): (1) `init_db.py` — 1 ALTER TABLE migration `locale_profiles.locale_terrain_ids TEXT[] NOT NULL DEFAULT '{}'` (D9 `_PG_MIGRATIONS` entry, no standalone SQL file — same precedent as 5.1.A); (2) `routes/locales.py` — module-local `_TRN_PATTERN` + `_terrain_choices(db)` + `_parse_locale_terrain(form)` + `_hydrate_locale_terrain_ids(profile_row)` + `_evict_layer2b_on_terrain_change(db, uid)` per D1 v1 route-local duplicate-with-cross-ref strategy (the helpers mirror `routes/race_events.py` + `routes/onboarding.py` from form-refresh A/B; ~30 LOC duplication across 3 sites keeps the 8-file ceiling intact); threaded into both `_edit_legacy_locale` + `_edit_shared_locale` GET render kwargs + POST INSERT/UPDATE branches per D4 (terrain lives on `locale_profiles` directly, not the shared `gym_profiles` table — geographic access, not commercial inventory); D10 invalidation fires on actual terrain-set change (`sorted(new) != sorted(prior)`) so no-op saves stay silent; (3) `templates/locales/form.html` — new "Terrain accessible from this location" multi-checkbox grid (D2 — checkbox grid mirroring equipment pattern, not the race-terrain pct-row editor partial; pct doesn't apply to locale terrain) inserted between equipment fieldsets + city/notes inputs with form copy per D8 (athletes select terrain reachable from the locale even if not on-property — disambiguates property-vs-reachable); D3 scope = terrain editor only (extracting a partial for a 16-row checkbox grid isn't worth it); (4) `layer2b/builder.py` — `_validate_inputs` loosen (D6): accepts `race_terrain == []` (skip per-entry + pct_sum + TRN-pattern checks when empty); still validates non-list/non-RaceTerrainEntry/invalid-pct/locale-id/disciplines/etl shape when non-empty; `_emit_coaching_flags` signature extended with `race_terrain` parameter + new short-circuit branch emitting a single `race_terrain_unset` `Layer2BCoachingFlag(flag_type='race_terrain_unset', target_terrain_id=None, message=...)` when input is empty (D7 — message + metadata=`{}`); (5) `layer4/orchestrator.py` — hoist `_q_primary_locale` up before Layer 2B (was at the 2C boundary); new `_q_locale_terrain_ids(db, uid, locale)` helper (mirrors `_q_locale_equipment_pool` shape — SELECT against `locale_profiles WHERE user_id, locale`; returns Python list from psycopg2 native TEXT[] or `[]` for NULL / missing row / SQLite-shim JSON-string path); flip `locale_terrain_ids=[]` to `locale_terrain_ids=_q_locale_terrain_ids(db, user_id, primary_locale)` per D5 (v1 home-locale only — matches existing `_q_locale_equipment_pool` precedent; cluster-union spec §3 deferred to follow-on); module docstring forward-pointer block updated to reflect Open Item 2B-2 closure + the loosen pair landing; (6) `tests/test_layer2b.py` — existing `TestInputValidation::test_empty_race_terrain_raises` removed; new `TestEmptyRaceTerrainLoosen` class with 4 tests (`test_empty_race_terrain_returns_payload_with_race_terrain_unset_flag` — payload shape on empty: empty race_terrain[] + empty terrain_gaps[] + summary all-zeros + worst_fidelity=1.0 + single race_terrain_unset flag with target_terrain_id=None + metadata={}; `test_empty_race_terrain_with_empty_locale_still_emits_unset_flag` — both-empty case; `test_non_list_race_terrain_still_raises` — type guard remains; `test_other_validation_still_fires_on_empty_terrain` — included_discipline_ids + etl_version_set still validated when terrain empty); (7) NEW `tests/test_locales.py` — 21 tests across 5 classes: `TestTrnPattern` (2: accepts canonical / rejects non-canonical), `TestParseLocaleTerrain` (8: happy multi-select, empty form, empty field, drops blanks, drops bad TRN, dedupes, sorted output, strips whitespace), `TestTerrainChoices` (2: returns id+label dicts in SELECT order, empty rows), `TestHydrateLocaleTerrainIds` (8: None / column-missing / NULL / native list / list-with-invalid-drops / JSON-string / empty-array-literal / malformed-JSON), `TestEvictLayer2bOnTerrainChange` (1: monkeypatch Layer4Cache + PostgresCacheBackend constructors; assert layer2b policy = _NON_SINGLE_SESSION evicts plan_create + plan_refresh + race_week_brief and preserves single_session); (8) `tests/test_layer4_orchestrator.py` — new `TestLocaleTerrainIdsWireUp` class with 4 tests (`test_locale_terrain_ids_thread_into_layer2b_call` — `_q_locale_terrain_ids` SELECT response threads into Layer 2B kwargs; `test_locale_terrain_ids_empty_when_column_null` — NULL column path; `test_locale_terrain_ids_empty_when_row_missing` — defensive against pre-migration / race-condition row absence; `test_locale_terrain_ids_tolerates_json_string_path` — SQLite shim JSON-string representation); existing `TestRaceTerrainAndAidStandsWireUp::test_empty_race_terrain_still_passed_through_unchanged` docstring updated to reference the loosen pair landing in C. **Tests 1110 → 1135 (+25 in container-runnable subset: 21 new locales tests + 4 new orchestrator wire-up tests).** Container-runnable subset (layer4 + race_events + onboarding race_events + locales) 443 → 468 passing in 1.00s. The +4 new `tests/test_layer2b.py` tests + the orchestrator-side flag emission remain blocked by the pre-existing `layer1/layer4` circular-import same as the rest of `test_layer2b.py` (per CURRENT_STATE.md historical note + Phase 5.1.B handoff §4); verified by `git stash` round-trip that the circular import is not introduced by this slice. 4 SDK smoke tests still skip cleanly when ANTHROPIC_API_KEY unset → default `pytest tests/` reads "1135 passed, 4 skipped" once `test_layer2b.py` collection is reachable; in the container subset it reads "468 passed". 11 D-decisions ratified at plan-mode gate per Trigger #1 (user-facing form copy) + Trigger #3 (cross-layer schema change on `locale_profiles`): D1=route-local helper duplicate per form-refresh A/B precedent; D2=checkbox grid widget (not race-terrain partial); D3=terrain editor scope only (no partial extraction for the 16-row grid); D4=both legacy + shared edit branches; D5=orchestrator v1 home-only via existing `_q_primary_locale`; D6=`_validate_inputs` accepts empty + flag-emit; D7=`race_terrain_unset` flag message + empty metadata; D8=form copy "Terrain accessible from this location" + reachable-vs-on-property disambiguation; D9=`_PG_MIGRATIONS` entry only (no standalone SQL file); D10=field-change-gated invalidation fires on any locale edit for cluster-union forward-compat; D11=8-file ceiling break ratified (precedented by 5.1.A's 8 files). Layer 2B `_validate_inputs` loosen folds into the same slice — empty-default produces a working brief, not a fail. **Phase 5.1 form-refresh trilogy A + B + C complete; orchestrator has zero remaining Layer 2B input forward-pointers; Layer2B_Spec.md §12 Open Items 2B-2 + 2B-3 both ✅ Resolved.**

**Predecessor:** `V5_Implementation_D73_Phase_5_1_FormRefresh_B_Onboarding_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.1 form-refresh B — **§H.2 onboarding step-3c terrain capture shipped** atop the same-day form-refresh A. Mirrors A's terrain editor on the onboarding side so newly-onboarding athletes capture race terrain breakdown during initial onboarding rather than via post-onboarding edit. **Fully closes Layer2B_Spec.md §12 Open Item 2B-3** (race-event edit surface from A + onboarding step-3c surface from B both capture TRN-xxx + percent rows persisted to `race_events.race_terrain JSONB`; orchestrator's `race_terrain=race_event.race_terrain` flow now reaches the Layer 2B input from both surfaces). **5 substantive files** (at ceiling): (1) `routes/onboarding.py` — adds module-local `_TRN_PATTERN` + `_parse_race_terrain(form)` + `_terrain_choices(db)` mirroring `routes/race_events.py` (D1 v1 route-local duplicate-with-cross-ref strategy ratified at plan-mode gate; the ~12 LOC duplication keeps the 5-file ceiling intact and avoids forcing `race_events_repo.py` to grow form-parsing concerns); extends `_get_target_race_row()` SELECT with `race_terrain` + `aid_stations` columns + JSONB list-or-string hydration (mirrors `race_events_repo.get_race_event` adapter tolerance — psycopg2 native JSONB list vs sqlite shim JSON string vs NULL); extends `target_race()` GET to pass `terrain_choices=_terrain_choices(db)` to the template; `target_race_save()` POST parses `race_terrain` + `aid_stations` and threads into both `create_race_event` (new-target branch) + `update_race_event` (existing-target branch); brief-only cache invalidation diff extended with `prior_terrain != new_race_terrain` + `prior_aid != new_aid_stations` comparisons (same `evict_on_target_event_brief_field_change` routing as `routes/race_events.py:update_race`); (2) NEW `templates/_race_terrain_editor.html` — extracted shared partial at root templates level (D2 location pick; sibling to `base.html`; resolved from both `onboarding/` and `profile/` subdirs without `../`); contains the terrain-rows container + add button + hidden `<template>` for JS cloning + the inline vanilla-JS row-management hooks (nextIdx scan, click-to-clone, click-delegated remove); inputs from context: `existing_terrain` list of dicts + `terrain_choices` list of `{id, label}` dicts; single-render-per-page assumption documented in partial header comment; (3) `templates/onboarding/target_race.html` — distance + total_elevation_gain_m columns shrunk from `col-md-6 × 2` to `col-md-4 × 3` to fit a new `aid_stations` integer input (`col-md-4` mirroring `templates/profile/race_event_edit.html` layout exactly per D4); `{% set existing_terrain = (target.race_terrain if target and target.race_terrain else []) %}` + `{% include '_race_terrain_editor.html' %}` inserted between mandatory_gear textarea and notes textarea; aid_stations help text matches the post-onboarding edit surface verbatim ("Total count along the course. Expedition AR + self-supported events are usually 0; aid-supported ultras typically 4–12.") per D7; (4) `templates/profile/race_event_edit.html` — behavior-neutral refactor replacing the inline ~65-line terrain section (the `<div class="col-12">` block) + the ~35-line bottom `<script>` block with `{% set existing_terrain = ... %}` + `{% include '_race_terrain_editor.html' %}`; net -103 LOC at this template, +98 LOC at the partial; (5) `tests/test_onboarding_race_events.py` — `+16` new tests: 3 added to `TestGetTargetRaceRow` (`test_hydrates_race_terrain_from_jsonb_string`, `test_hydrates_race_terrain_from_native_list`, `test_hydrates_none_race_terrain_to_empty_list`); new `TestParseRaceTerrain` class with 8 tests (happy multi-row parse, empty form, drop-on-empty-terrain_id, drop-on-empty-pct, drop-on-invalid-TRN-pattern, drop-on-non-numeric-pct, drop-on-out-of-range-pct, sorted-order-across-sparse-indices); new `TestTerrainChoices` class with 2 tests (SELECT shape + ORDER BY + dict mapping, empty-rows degenerate); existing `TestGetTargetRaceRow::test_returns_dict_on_hit` extended with new race_terrain + aid_stations column assertions in SELECT + dict pass-through. **Tests 1094 → 1110 (+16).** Container-runnable subset (layer4 + race_events + onboarding race_events) 408 → 443 passing in 1.44s. 8 D-decisions ratified at plan-mode gate per Trigger #1 (user-facing form copy): D1=route-local helper duplicate over repo extraction (v1 simplicity over cross-blueprint coupling); D2=partial at `templates/_race_terrain_editor.html` (root) not `templates/onboarding/_race_terrain_editor.html` (the partial serves both onboarding/ and profile/ subdirs); D3=partial scope = terrain editor only (aid_stations stays inline in each parent template); D4=aid_stations layout = `col-md-4 × 3` mirroring race_event_edit.html; D5=cache invalidation extended in same shape as routes/race_events.py update_race; D6=no `target_race_terrain_missing` account_nudge (out of scope; Layer 2B `_validate_inputs` loosen is the paired follow-on); D7=identical form copy to post-onboarding edit surface (coaching voice consistency); D8=section heading "Race terrain breakdown" matches edit surface label.

**Predecessor:** `V5_Implementation_D73_Phase_5_1_FormRefresh_A_RaceTerrain_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.1 form-refresh A — **race_terrain + aid_stations end-to-end shipped** atop the same-day Phase 5.1 orchestrator vertical slice. First slice of the §H.2 / §J / §I.1 form-refresh PR closes the highest-leverage orchestrator forward-pointer per the predecessor handoff §6.1 ("the only forward-pointer that affects coaching quality on multi-day events"). Closes Layer2B_Spec.md §12 Open Item 2B-3 for the race-event edit path (onboarding §H.2 step-3c terrain capture still owed in a follow-on). **8 substantive files** (3-file ceiling break, ratified at the plan-mode gate before implementation): (1) `init_db.py` — 2 ALTER TABLE migrations adding `race_events.race_terrain JSONB NOT NULL DEFAULT '[]'::jsonb` + `race_events.aid_stations INTEGER NULL CHECK (aid_stations IS NULL OR aid_stations >= 0)`; (2) `layer4/context.py` — `RaceEventPayload` gains `race_terrain: list[RaceTerrainEntry] = Field(default_factory=list)` + `aid_stations: int | None = Field(default=None, ge=0)` + a new `model_validator` enforcing `TRN-\d{3}` pattern on each entry's `terrain_id` at the payload boundary (no sum check — Layer 2B owns `[80, 120]` tolerance; partial-edit rows round-trip cleanly); (3) `race_events_repo.py` — threads the 2 new fields through `list_athlete_race_events` + `load_race_event_payload` + `create_race_event` + `get_race_event` + `update_race_event` with JSONB-list-or-string hydration tolerance for the psycopg2-default vs sqlite-shim adapter paths; (4) `routes/race_events.py` — new `_terrain_choices(db)` request-time vocabulary helper (against `layer0.terrain_types`) + `_parse_race_terrain(form)` flat-form parser (`race_terrain[N][terrain_id]` + `race_terrain[N][pct_of_race]` repeating rows; empty/malformed rows silently dropped) + `aid_stations` `_parse_int` threading on both `new_race()` + `update_race()` + brief-only cache invalidation extended to fire on race_terrain or aid_stations changes (both feed Layer 2B + 2E which are uncached at orchestrator level → Layer 4 brief is the cache-load-bearing artifact); (5) `templates/profile/race_event_edit.html` — terrain-breakdown editor with per-row TRN-xxx select + percent input + remove button + "Add terrain" button backed by a `<template>` block and a small inline JS hook (vanilla DOM API; no Alpine/HTMX); aid_stations integer input next to distance/elevation; (6) `layer4/orchestrator.py` — 2 forward-pointer flips (line 151 `race_terrain=[]` → `race_terrain=race_event.race_terrain`; line 207 `aid_stations=None` → `aid_stations=race_event.aid_stations`) + module docstring forward-pointer block updated to reflect the slice's closure (race_terrain now flows; locale_terrain_ids still empty pending §J Open Item 2B-2; `prior_plan_session_window` + `plan_version_id` + uncached 3A/3B remain); (7) `tests/test_race_events_repo.py` — `TestRaceTerrainAndAidStations` class with 9 new tests covering CRUD round-trip (load via list + via JSONB string + None-defaults-empty), terrain-id pattern rejection at the payload model_validator, create + update serialization, get_race_event hydration tolerance, list_athlete_race_events column inclusion; (8) `tests/test_layer4_orchestrator.py` — `TestRaceTerrainAndAidStationsWireUp` class with 3 new tests asserting race_terrain threads into `q_layer2b_terrain_classifier_payload` kwargs, aid_stations threads into the `Layer2ETargetEvent.aid_stations` for the Layer 2E call, and the empty-terrain pass-through case (slice intentionally does not loosen Layer 2B's `_validate_inputs` rejection of empty — that's a separate spec change). Layer 4 + race_events suite: 348 → 408 passing in the container-runnable subset (60 net new); production count delta: 1082 → 1094 (+12 tests). 9 D-decisions ratified at the plan-mode gate (D1-D9 pinned in §7 of the closing handoff). **Phase 5.1 form-refresh A complete.** Remaining §H.2/§J/§I.1 split: §H.2 onboarding step-3c capture surface (so athletes capture terrain during initial onboarding rather than via race-event edit) + §J locale-terrain capture (Open Item 2B-2) + §I.1 structured supplements (Layer 2E §5.5 de-stub) all queued. Layer 3B None-tolerant kwargs (`goal_outcome`, `first_time_at_distance`, `previous_attempts`, etc.) remain a separate L3B-P-2 surface.

**Predecessor:** `V5_Implementation_D73_Phase_5_1_Orchestrator_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 5.1 orchestrator vertical slice (race_week_brief) shipped (PR #109 merged into main as `8854fa4`). First end-to-end composition of the v2 upstream pipeline. New `layer4/orchestrator.py:orchestrate_race_week_brief(db, user_id, *, cache: Layer4Cache, today: date | None = None) -> Layer4Payload` threads Layer 1 → 2A/2B/2D/2C → 3A → 3B → 2E → `llm_layer4_race_week_brief_cached`. Layer 2E runs AFTER 3B per the 2E ← 3B.start_phase dependency. Four substantive files (under 5-file ceiling): (1) NEW `layer4/orchestrator.py` (266 LOC) — `orchestrate_race_week_brief` + `OrchestrationError(code, detail)` + 3 private DB helpers (`_q_current_etl_version_set` querying `MAX(etl_version) FROM layer0.sports WHERE superseded_at IS NULL` and applying the same version to all 3 sub-arc keys; `_q_primary_locale` reading `locale='home'` per v1 `routes/dashboard.py:31` convention; `_q_locale_equipment_pool` joining `locale_equipment × equipment_items`). Pre-flight gates: `OrchestrationError('no_target_event')` / `OrchestrationError('race_week_brief_too_early')` (cheap optimization saving ~$0.30 of 3A+3B LLM cost on out-of-window invocations; `_validate_inputs` in `llm_layer4_race_week_brief` is the load-bearing check) / `OrchestrationError('framework_sport_missing')` / `OrchestrationError('primary_locale_missing')` / `OrchestrationError('etl_version_set_undiscoverable')`; (2) NEW `tests/test_layer4_orchestrator.py` (~700 LOC) — 10 smoke tests across 5 test classes (`TestHappyPath` 1 / `TestPreflightGates` 2 / `TestDiscoveryFailures` 3 / `TestDefaults` 2 / `TestOrchestrationError` 2) using `unittest.mock.patch` to stub upstream builders + LLM drivers at the orchestrator's module-level imports + `_FakeConn` for the orchestrator's direct DB queries; (3) MODIFIED `aidstation-sources/Layer4_Spec.md` §4.5 — surgical 2-line D-72 audit-trail fix: source-pointer amendment header bumped to `2026-05-18 (D-66 paired implementation; D-72 slug-FK follow-on 2026-05-19)` + Row 5 `event_locale_unresolved` wording updated to reflect that `race_events_repo.load_race_event_payload` itself joins `locale_profiles` and surfaces the slug on `RaceEventPayload.event_locale_id` per D-72 (closes the CARRY_FORWARD doc-nit); (4) MODIFIED `layer4/__init__.py` — re-export block + `__all__` entries for `orchestrate_race_week_brief` + `OrchestrationError`. Bookkeeping: `Upstream_Implementation_Plan_v1.md` §4 row 5.1 flipped to ✅ Shipped 2026-05-20 (PR #109). 19 D-decisions ratified at the `/plan-mode` gate before implementation (Trigger #5 — architectural alternatives); D1-D19 pinned in §7 of the closing handoff. Vertical-slice forward-pointers carried for Phase 5.2: `prior_plan_session_window=[]` (no v2 plan-gen wired yet); `plan_version_id=1` hardcode with `# TODO(plan-versioning)`; Layer 2B `race_terrain=[]` + `locale_terrain_ids=[]` (§H.2 form-refresh gap; 2B surfaces a coaching flag); Layer 3A + Layer 3B uncached at orchestrator level (load-bearing only when multiple Layer 4 entry points share user-scoped outputs — Phase 5.2 will revisit). Layer4 suite 338 → 348 (+10 new orchestrator tests + 338 pre-existing); total tests 1072 → 1082 green + 4 skipped (SDK smoke). **Phase 5.1 complete.** First real end-to-end v2 LLM pipeline composer landed; Phase 5.2 (remaining 3 Layer 4 entry points) inherits the substrate.

**Predecessor:** `V5_Implementation_D73_Layer4_Step7_SDK_Smoke_2026_05_20_Closing_Handoff_v1.md` — D-73 Layer 4 Step 7 — **env-gated `ANTHROPIC_API_KEY` SDK smoke scaffolding shipped** atop the same-day Phase 4 Layer 3B driver. First REAL Anthropic SDK invocation harness against both 3A + 3B drivers shipped earlier in the day; closes L3A-P-1 + L3B-P-1 deferred real-LLM regression scaffolding. Three substantive files (well under ceiling): (1) NEW `tests/conftest.py` (~25 LOC) — shared `requires_anthropic_api_key = pytest.mark.skipif(not os.environ.get("ANTHROPIC_API_KEY"))` decorator; module-level marker keeps default `pytest tests/` runs at $0 and side-effect-free; tests execute only when the key is intentionally set (local dev / CI smoke job); (2) NEW `tests/test_layer3a_smoke.py` (~300 LOC) — 2 real-LLM round-trip tests against actual Sonnet 4.6 via `llm_layer3a_athlete_state(...)` with `llm_caller=None` (defaults to `_default_llm_caller` → production Anthropic SDK adapter): `test_dense_data_fit_athlete` (§13.1 shape — rich integration bundle + 5y AR athlete + dense workouts/sleep/HRV → expects `aerobic_capacity.level ∈ {good, strong}` + high/medium trajectory confidence + sweet_spot ACWR preserved); `test_sparse_data_returning_athlete` (§13.4 shape — empty bundle + experienced athlete → expects `recent_trajectory.confidence == low` + detrained-or-insufficient_data short_term + §6.3 `confidence_clamped_by_data_density` observation appended by floor-clamp); (3) NEW `tests/test_layer3b_smoke.py` (~315 LOC) — 2 real-LLM round-trip tests against actual Sonnet 4.6 via `llm_layer3b_goal_timeline_viability(...)` with `llm_caller=None`: `test_ts1_ar_finisher_compressed_event_mode` (TS-1 — Andy's PGE 2026 baseline: 9 weeks out, Finish goal, prior AR finish → expects mode=event + viability achievable-or-with-adjustment + compressed-or-custom periodization + D14 event-metadata populated from RaceEventPayload); `test_ts4_no_event_endurance_standard` (TS-4 — 24w endurance block → expects mode=no-event + viability achievable + standard-or-extended periodization + all D14 fields None). Assertions are mixed structural (payload validates, metadata stamped with `input_tokens > 0` / `output_tokens > 0` / `latency_ms > 0` / `prompt_hash` length 64 / `etl_version_set` preserved / D14 fields populated-per-mode) + loose enum (allowlist sets — e.g., `aerobic_capacity.level in {"good","strong"}`) to absorb minor classification variance across Sonnet 4.6 minor versions. Tests 1072 → 1076 (+4 smoke; all 4 skip cleanly when ANTHROPIC_API_KEY unset → default `pytest tests/` reads "1072 passed, 4 skipped"). Wiring verified end-to-end via fake-key dry-run (smoke test reaches `anthropic.Anthropic.messages.create()` → 401 AuthenticationError as expected; not a contract bug). **Layer 4 Step 7 SDK smoke scaffolding complete** for 3A + 3B; closes L3A-P-1 + L3B-P-1. Layer 4 `single_session.py` + remaining 5 entry points' smoke parity deferred (architect-recommended scope was 3A + 3B per same-day-shipped driver focus). Real-LLM cost per smoke run when ANTHROPIC_API_KEY set: ~4 × $0.03/call = ~$0.12; intentionally not part of default CI.

**Predecessor:** `V5_Implementation_D73_Phase_4_Layer3B_Driver_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 4 — Layer 3B LLM driver shipped atop the same-day Phase 3.1-Driver. Closes the upstream LLM-driver pair (3A + 3B). First Layer 3 node-pair complete; Phase 5.1 orchestrator vertical slice now unblocked structurally (LLM SDK scaffolding via Layer 4 Step 7 the remaining prerequisite). Five substantive files: (1) NEW `layer3b/builder.py` (~950 LOC) — `llm_layer3b_goal_timeline_viability(...)` entry point + Anthropic SDK adapter with extended-thinking (D2 = 3000 tokens) + forced tool-use (D1) + 4 §5.1 prep helpers + full `Layer3BPayload` tool-schema mirror per D5 + single capped retry on schema violation per §5.5 step 1 + mode-discriminator post-LLM enforcement per §5.5 step 2 + name-existence + mode-prefix `evidence_basis` cross-check per D9 + HITL auto-emit validator per §5.5 step 3 + D12 (4 §6.1 items) + §6.5 floor-rule clamp (4 rules) + `confidence_clamped_by_data_signal` observation per D8 + §5.5 step 4 periodization-sanity loop per D13 (custom-mode phase_weeks sum ±1 of timeline; single retry; fallback-to-standard with `periodization_shape_fallback` observation) + D14 D-66 event-metadata field population from `race_event_payload` + inlined `Layer3BInputError` / `Layer3BOutputError` / `Layer3BEvidenceBasisWarning` (Step 4a + Layer 2C + Layer 3A precedent); (2) NEW `layer3b/cached_wrapper.py` (~200 LOC) — `llm_layer3b_goal_timeline_viability_cached(...)` reusing generic `CacheBackend` from `layer4/cache.py` with 3B-specific `Layer3BPayload` serialize/hydrate + day-granular `current_date: date` cache key per spec §9.1 (`sha256(user_id || layer1_hash || layer3a_hash || layer2a_hash || race_event_id || current_date.isoformat() || non_event_goal_type || etl_set || section_h2_kwargs_json || model || temp || max_tokens || thinking_budget)`); (3) NEW `aidstation-sources/prompts/Layer3B_v1.md` (~700 lines) — prompt body design doc with D1-D14 source decisions + system prompt (CLAUDE.md voice + spec §6.1 HITL triggers + §6.2 periodization vocabulary + §6.5 floor rules + §6.6 no-event heuristics + §8.1 required observation triggers + §8.2 observation budget) + user prompt template (4 blocks + retry/sanity augmentations) + tool schema §4 with `additionalProperties: false` at every nesting level + post-LLM transforms §8 + caching §10 + gut check §13; (4) NEW `tests/test_layer3b_builder.py` (~1100 lines) — 77 tests across 11 test classes (`TestInputValidation` 14 / `TestToolSchema` 10 / `TestEntryPointHappyPath` 3 / `TestModeDiscriminator` 2 / `TestConfidenceFloors` 7 / `TestHITLAutoEmit` 7 / `TestPeriodizationSanity` 6 / `TestSchemaViolation` 3 / `TestEvidenceBasisCheck` 3 / `TestS13Scenarios` 8 (covering TS-1..TS-8) / `TestPrepDict` 7 / `TestCacheWrapper` 7) using stub `llm_caller` per Step 4a + Layer 3A precedent — no real Anthropic SDK invocation; (5) MODIFIED `aidstation-sources/Race_Events_D66_Design_v1.md` §8.3 — paired doc-sweep fix `mode='open_ended'` → `mode='no-event'` per canonical `Layer3BPayload.mode: Literal["event","no-event"]` (CARRY_FORWARD doc-nit + Upstream Plan §4 row 4.2 paired fix list closed). Bookkeeping: `layer3b/__init__.py` re-exports `llm_layer3b_goal_timeline_viability` + `llm_layer3b_goal_timeline_viability_cached` + `Layer3BInputError` + `Layer3BOutputError` + `Layer3BEvidenceBasisWarning` + `build_emit_layer3b_payload_tool` + `layer3b_goal_timeline_viability_key`; `layer4/cache.py` `VALID_ENTRY_POINTS` superset extended with `"llm_layer3b_goal_timeline_viability"` (LAYER4_ENTRY_POINTS frozenset untouched — preserves Layer 4-scoped invalidation invariant per Phase 3.1-Driver precedent). Tests 995 → 1072 (+77). **Phase 4 complete.** Phase 4.1 caller-side rewire helper folded into the driver via D14 internal population — `_assemble_payload_candidate` populates the 4 D-66 event-metadata fields directly from the passed `RaceEventPayload`; no standalone tuple-returning helper added (deferred per Upstream Plan row 4.1). All upstream LLM drivers (3A + 3B) now operational; Phase 5.1 orchestrator vertical slice unblocked.

**Predecessor:** `V5_Implementation_D73_Phase_3_1_Driver_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 3.1-Driver — **Layer 3A LLM driver shipped** atop the substrate landed earlier same day. First upstream LLM driver (predecessors were query-node-only runtimes). Five substantive files: (1) NEW `layer3a/builder.py` (~900 LOC) — `llm_layer3a_athlete_state(...)` entry point + Anthropic SDK adapter with extended-thinking + forced tool-use (D1+D2) + 8 §5.1 prep helpers + full `Layer3APayload` tool-schema mirror per D5 + single capped retry on schema violation per §5.3 step 1 + name-existence `evidence_basis` cross-check per D9 + §6.2 floor-rule clamp (4 rules) + §6.2 high-gate criteria (5 conditions) + §6.3 auto-appended `confidence_clamped_by_data_density` observation + inlined `Layer3AInputError` / `Layer3AOutputError` / `Layer3AEvidenceBasisWarning` (Step 4a + Layer 2C precedent); (2) NEW `layer3a/cached_wrapper.py` (~150 LOC) — `llm_layer3a_athlete_state_cached(...)` reusing generic `CacheBackend` from `layer4/cache.py` with 3A-specific `Layer3APayload` serialize/hydrate via `model_dump_json` / `model_validate_json` + day-granular cache key per spec §9.1 (`sha256(user_id || layer1_hash || layer2a_hash || bundle_hash || day_anchor || etl_set || model || temp || max_tokens || thinking_budget)`); (3) NEW `aidstation-sources/prompts/Layer3A_v1.md` (~530 lines) — prompt body design doc with D1-D10 source decisions + system prompt (CLAUDE.md voice + spec §6.1 weighting + §6.2 calibration + §8.1 required observations + §8.2 forbidden observations + §8.3 ordering) + user prompt template (8 prep blocks + 2A phase context + §B health note + as_of) + tool schema §4 with `additionalProperties: false` at every nesting level + post-LLM transforms §8 + caching §10 + gut check §13; (4) NEW `tests/test_layer3a_builder.py` (~970 lines) — 54 tests across 8 test classes (`TestInputValidation` 10 / `TestToolSchema` 8 / `TestEntryPointHappyPath` 2 / `TestConfidenceFloors` 11 / `TestSchemaViolation` 3 / `TestEvidenceBasisCheck` 1 / `TestS13Scenarios` 6 (covering §13.2/3/4/6/7/10) / `TestPrepDict` 5 / `TestCacheWrapper` 7) using stub `llm_caller` per Step 4a precedent — no real Anthropic SDK invocation; (5) MODIFIED `Layer3_3A_Spec.md` — §3.3 model literal corrected from stale `claude-sonnet-4-5` → `claude-sonnet-4-6` per D7 + Phase 3.1-Driver D7 audit pointer. Bookkeeping: `layer4/cache.py` split `LAYER4_ENTRY_POINTS` (4 originals) + extended `VALID_ENTRY_POINTS` superset (adds `"llm_layer3a_athlete_state"` for the 3A wrapper's `put()` validation); `layer4/__init__.py` re-exports `LAYER4_ENTRY_POINTS`; `tests/test_layer4_cache.py` 6 assertion swaps from `VALID_ENTRY_POINTS` → `LAYER4_ENTRY_POINTS` to preserve the Layer 4-scoped invalidation invariant; `layer3a/__init__.py` re-exports `llm_layer3a_athlete_state` + `llm_layer3a_athlete_state_cached` + `Layer3AInputError` + `Layer3AOutputError` + `build_record_athlete_state_tool` + `layer3a_athlete_state_key`. Tests 941 → 995 (+54). **Phase 3 complete for Layer 3A** (substrate + driver shipped same day). The first real Anthropic SDK call site lands here — production callers need `ANTHROPIC_API_KEY` env (Layer 4 Step 7 scaffolding will land env-gating + smoke test harness when picked).

**Predecessor:** `V5_Implementation_D73_Phase_3_1_Substrate_2026_05_20_Closing_Handoff_v1.md` — D-73 Phase 3.1-Substrate — **Layer 3A integration substrate shipped** following the split decided at session start (Phase 2.4-Prep / 2.4 precedent applied to Phase 3.1). Five `q_layer3A_*` query-node accessors per `Athlete_Data_Integration_Spec_v6.md` §10 + a `Layer3AIntegrationBundle` aggregator compose the input the Phase 3.1-Driver session will pass to `llm_layer3a_athlete_state(...)`. Pure SQL — no LLM, no `/plan-mode` triggers, under ceiling. Five substantive files: (1) `layer4/context.py` adds 7 dataclasses (`WorkoutRecord` + `SleepRecord` + `HRVRecord` + `PolarCardioLoadCrossRef` + `CombinedLoadReport` + `ProviderStatus` + `Layer3AIntegrationBundle`) with source-tag Literals (`WorkoutSource` / `SleepSource` / `HRVSource`) supporting the §6.1 weighting rules at the LLM seam; (2) NEW `layer3a/__init__.py` re-exporting the 5 accessors + the aggregator; (3) NEW `layer3a/integration.py` (~480 LOC) implementing the 5 accessors against `cardio_log` (workouts, source-detected via foreign-id columns) / `wellness_self_report` + `polar_sleep` + `coros_daily_summary` (sleep, 3-source merge) / `polar_nightly_recharge` + `coros_daily_summary.ppg_hrv` (HRV) / `cardio_log` durations in hours (ACWR per-discipline + combined; `polar_cardio_load` as cross-reference only per Integration Spec §10) / `provider_auth` + per-data-type coverage queries (provider status); (4) NEW `tests/test_layer3a_integration.py` 40 tests across 7 classes/groups using the `_FakeConn` pattern from `tests/test_layer2c.py` — covers source detection, multi-source merge ordering, ms-to-hours conversion, ACWR zone classification (parametrized 11-point sweep covering all 5 zone boundaries), no-base sentinel, moving_time preference, polar cross-ref population, provider coverage matrix; (5) `Upstream_Implementation_Plan_v1.md` §4 row 3.1 rewritten to reflect the 2-session split (Substrate ✅ + Driver). Tests 901 → 941 (+40); ACWR window cutoff math fixed during the session (inclusive `[cutoff, as_of]` containing exactly N calendar dates). `layer4/__init__.py` re-exports updated (bookkeeping). Phase 3.1-Driver (the LLM call + prompt body + Anthropic SDK adapter + confidence-floor validator) opens the next session with a `/plan-mode` gate per Triggers #2 + #5.

**Predecessor:** `V5_Implementation_D73_Doc_Sweep_2026_05_20_Closing_Handoff_v1.md` — D-73 Doc-Sweep — **5-file spec-touchpoint cleanup batch shipped** following the Phase 2.4 closing handoff. Doc-only session; zero code touched; 901 tests baseline unchanged. Two commits: (a) `226d0dd` — `Layer2C_Spec.md` §5.1 pseudo-code rewritten in place to match §6 one-hop `also_satisfies` semantics + reconciliation note shrunk from compensating-text framing to one-paragraph audit trail (closes the follow-up clarity item flagged from Phase 2.4 §2); (b) `81946b6` — 4-nit doc-sweep batch: `Layer2A_Spec.md` §5.2 drops `pla.default_inclusion` + adds Key-points note on `notes_conditions`-based code-side derivation per §5.3; `Layer2A_Spec.md` §12 Open Item 2A-1 flipped to 🟡 partial-close with Phase 5.1 forward-pointer; `Layer2D_Spec.md` §3 + §4 enum count "9-value" → "11-value"; `Layer2B_Spec.md` §13.1 TRN-008/TRN-009 swap to canonical deployed vocab; `Upstream_Implementation_Plan_v1.md` §4 row 2.2 read-tables list rewritten.

**Predecessor:** `V5_Implementation_D73_Phase_2_4_Closing_Handoff_v1.md` — D-73 Phase 2.4 — **Layer 2C equipment mapper builder shipped** atop the Phase 2.4-Prep substrate. Pure query node per `Layer2C_Spec.md` §3-§10. Three substantive files: NEW `layer2c/__init__.py` re-exports `q_layer2c_equipment_mapper_payload` + `Layer2CInputError`; NEW `layer2c/builder.py` (~650 LOC) implements the full §5 algorithm (§5.1 effective-pool construction with §6 one-hop `also_satisfies` expansion + DP1=(A) runtime toggle lookup; §5.2 sdb⨝sxm⨝e discipline-bridged exercise enumeration; §5.3 Tier 1; §5.4 Tier 2 CNF-structured substitute resolution including improvised auto-resolve; §5.5 Tier 3 proxy resolution with no-cascade rule; §5.6 accommodation modality pass-through from optional `Layer2DPayload`; §5.7 per-discipline coverage aggregation) + §8 coaching flags (8.1 low_coverage, 8.2 critical_dropped, 8.3 toggle_off_for_discipline reading `gated_discipline_ids` directly per DP2=(b)) + §10 edge cases (empty pool, zero-exercise discipline, missing-from-sdb skip, multi-discipline dedup); NEW `tests/test_layer2c.py` 35 tests across 7 classes (`TestInputValidation` 9 / `TestEffectivePool` 4 / `TestTierResolution` 7 / `TestCoverageAndDedup` 2 / `TestCoachingFlags` 5 / `TestAccommodationPassThrough` 2 / `TestEdgeCases` 4 + 2 free-standing smoke tests) using the `_FakeConn` pattern mirroring `tests/test_layer2b.py`. Two paired spec touchpoints folded into the same session: `Layer2C_Spec.md` §5.1 + §8.3 Decision Points annotated ✅ Resolved with full rationale; §10 edge-case row added for missing `parsed_substitutes.json` entries; §12 Open Items 2C-1 + 2C-2 flipped to ✅ Resolved. `Upstream_Implementation_Plan_v1.md` §4 row 2.4 + §5.4 ceiling-break list + §6 gate list updated to reflect the Split + DP resolution. Tests 866 → 901 (+35). **Phase 2 complete — all 5 of 5 Layer 2 runtimes shipped.** §5.1 vs §6 reconciliation note: §5.1's pseudo-code adds `toggle_def.also_satisfies` directly to the equipment pool, but the deployed data shape (per `migrate_toggles_v3_columns.sql`) treats `also_satisfies` as TOGGLE NAMES; builder implements §6 one-hop semantics (expand referenced toggle's `paired_equipment_categories` only, no cascade). Hard prerequisite for live-data verification: Andy operationally applies the 3 SQL migrations + re-runs ETL on Neon per Phase 2.4-Prep handoff §5 before the §5.0 walkthrough scenarios run.

**Predecessor:** `V5_Implementation_D73_Phase_2_4_Prep_Closing_Handoff_v1.md` — D-73 Phase 2.4-Prep — Layer 2C data substrate (schema + ETL extractor work landing the 4 columns Layer 2C reads; Phase 2.4 builder queued for next session per Andy 2026-05-19 split-scope pick). Drift inventory between `Layer2C_Spec.md` §3-§8 and deployed `layer0.*` surfaced four substantive gaps: (a) `layer0.exercises.equipment_substitutes_structured JSONB` (Layer 2C §5.4 CNF Tier 2) — migration on-disk but unapplied + unpopulated; (b) `layer0.exercises.terrain_required TEXT[]` (§5.2 pass-through) — migration on-disk; ETL extractor already produces the field via `vocabulary_transforms.transform_equipment_string`, `run.py` already wires it into the INSERT column list — only the column-add was missing; (c) `layer0.sport_specific_gear_toggles.also_satisfies TEXT[]` (§5.1 + §6) — no migration; (d) `layer0.sport_specific_gear_toggles.gated_discipline_ids TEXT[]` (§8.3) — no migration. **Decision Points resolved** (3-question AskUserQuestion gate; `Layer2C_Spec.md` §5 + §8.3 + Open Items 2C-1/2C-2): **DP1 (§5.1 Toggle definition lookup) = (A) Runtime lookup in 2C** (spec recommendation); **DP2 (§8.3 Discipline-to-toggle mapping) = (b) Structured column on `sport_specific_gear_toggles`** (Andy diverged from spec's hard-code-for-v1 recommendation; structured carries traceability + survives spec evolution). **Scope = Split: Prep first, then 2C** (over Vertical-slice / Migrations-plus-full-2C / Pivot-to-Step-4f-or-Step-7). **6 substantive files** (over ceiling per Andy's "Add exercise_db.py too" pick on the 6-vs-5 gate; deletes operational drift between schema migration + populate run by routing structured substitutes through the ETL extractor): (1) `etl/layer0/schema.sql` adds the 4 columns + spec-section anchor comments; (2) NEW `aidstation-sources/migrations/migrate_toggles_v3_columns.sql` combined-migration (ADD COLUMN IF NOT EXISTS + UPDATE the 3 known cases — `Climbing — roped` also_satisfies `Rappelling / abseiling` + gates `D-010`; `Rappelling / abseiling` gates `D-011`; `Snowshoeing setup` gates `D-015` — + DO-block verification with NOTICE-fallback when no active row exists so safe to run pre-ETL); (3) `etl/layer0/extractors/vocabulary.py` new code-side constants `_TOGGLE_ALSO_SATISFIES` + `_TOGGLE_GATED_DISCIPLINES` (matches Layer 2D `_HIGH_CARDIAC_LOAD_DISCIPLINES` precedent; `_parse_gear_toggles` emits the 2 new fields per row so next ETL re-run preserves the data); (4) `etl/layer0/extractors/exercise_db.py` new `load_parsed_substitutes_structured()` function reading the shipped `etl/sources/parsed_substitutes.json` (154 exercises × 510 entries from prior K-parser work) + `extract_exercises` attaches per-row; loud-fallback empty dict when file missing; (5) `etl/layer0/run.py` extends both INSERTs (4 new columns total — 2 on `exercises` via `to_jsonb` for `equipment_substitutes_structured` + already-wired `terrain_required`; 2 on `sport_specific_gear_toggles`); (6) NEW `tests/test_layer2c_prep.py` 16 tests across 3 test classes (`TestGearToggleParser` 6 / `TestParsedSubstitutesLoader` 5 / `TestSchemaSubstrate` 5 — including `Layer2CPayload` regression smoke covering shipped 5 sub-types + coaching-flag enum guard). Tests 850 → 866 (+16). **Operational sequence for Andy on Neon** (closing handoff §5): (1) `migrate_exercises_substitutes_structured.sql` (existing pre-shipped); (2) `migrate_exercises_terrain_required.sql` (existing); (3) `migrate_toggles_v3_columns.sql` (new); (4) Re-run `python -m etl.layer0.run` to populate the 4 new columns on a new etl_version. Once those 4 steps complete on Neon, Phase 2.4 Layer 2C builder session proceeds.

**Predecessor:** `V5_Implementation_D73_Phase_2_5_Closing_Handoff_v1.md` (Layer 2E nutrition baseline vertical slice — fourth upstream Layer 2 runtime).

## Current focus

Andy's pick. Phase 5.2 slice 2 (`plan_refresh` T1/T2/T3 orchestrator) shipped 2026-05-20 atop the same-day Phase 5.2 slice 1 (single_session). Orchestrator now exposes 3 of 4 Layer 4 entry points (race_week_brief 5.1 + single_session 5.2.S1 + plan_refresh 5.2.S2). Shared `_upstream_full_cone` helper extracted at slice 2 per D1 (covers race_week_brief + plan_refresh; single_session opts out per cone-shape divergence). **Architect-recommended next move:** **(a) Phase 5.2 slice 3 — `plan_create` Pattern A orchestrator** — final orchestrator entry point, closes Phase 5.2. Pattern A heaviest because of per-phase synthesis loop + seam reviews + cross-phase reconciliation; `plan_version_id` allocation moves from caller-supplied kwarg to a real D-64 versioning surface (or stays caller-supplied per slice 2 D3 precedent). The shared `_upstream_full_cone` helper already exists and can be reused or extended; ~3-5 files; `/plan` gate per Trigger #5 on per-phase loop + cross-phase reconciliation design. OR **(b) Form-refresh D — §I.1 structured supplements** (Layer 2E §5.5 de-stub; ~6-8 files; `/plan` gate per Triggers #1 + #3 + #5 — table vs JSONB schema choice). OR **(c) D-64 caller-side route + `plan_versions` table** — slice 2's orchestrator is structurally complete + tested in isolation but not E2E-reachable from the v1 UI until D-64 caller-side lands. ~4-6 files (init_db.py migration + new `routes/plan_refresh.py` + templates + tests + D-64 NL parser glue). Other alternatives: **D-63 caller-side route + `ad_hoc_workout_suggestions` table** (slice 1 caller-side wiring; ~3-4 files); **Layer 3B None-tolerant kwargs L3B-P-2** consumer migration; **`routes/locales.py` equipment-edit Layer 2C invalidation gap** (1-2 files; surfaced as nit during form-refresh C); **Plan Management spec authorship** to land 2E-2/3/4 contracts; **manual §5.0 walkthrough** of the accumulated scenarios + new orchestrator end-to-end (real-LLM ~$0.50 pass against Andy's PGE 2026 race row + locale terrain via the new surfaces); **real-LLM Layer 4 regression** parity (~$2/full smoke pass).

Orthogonal alternatives tracked in `CARRY_FORWARD.md`.

## Layer status

| Layer | Status |
|---|---|
| **0** | DEPLOYED |
| **1** | 🟢 v1 spec + typed payload + runtime builder shipped 2026-05-19 (D-51 design wave + Phase 1.2 schema arc + Phase 1.3 builder); 🟢 injury_log §B.1/§B.1.1/§B.3 extensions shipped 2026-05-19 (Phase 2.2 paired) |
| **2** | 🟢 All 5 of 5 runtimes shipped — 2A + 2D + 2B + 2E (Phase 2.1 + 2.2 + 2.3 + 2.5, 2026-05-19) + 2C (Phase 2.4-Prep substrate 2026-05-19 + Phase 2.4 builder 2026-05-20); 2E ships vertical-slice (§5.5 supplements + §5.8 heat acclim stubbed pending §I.1 refresh + Plan Management spec) |
| **3** | 🟢 3A + 3B complete (3A substrate + driver shipped 2026-05-20; 3B driver shipped same-day Phase 4 2026-05-20: `llm_layer3b_goal_timeline_viability` + cached wrapper + Layer3B_v1.md prompt body). 3C + 3D + 3.5 designed, not yet implemented |
| **3.5** | Designed; not yet implemented |
| **4** | SPEC COMPLETE §§1-14; Implementation Steps 2 + 3 + 4a-4f + Step 7 SDK smoke scaffolding (3A + 3B subset) + Phase 5.1 orchestrator vertical slice (race_week_brief) + Phase 5.2 slice 1 (single_session_synthesize orchestrator) + Phase 5.2 slice 2 (plan_refresh T1/T2/T3 orchestrator + `_upstream_full_cone` shared helper) of 8 COMPLETE; 3 of 4 entry points wired (plan_create slice 3 remains) |
| **5** | Not yet specced |

## D-73 upstream implementation arc

Multi-session plan in `Upstream_Implementation_Plan_v1.md`. Phase 1.1 (D-51 design wave) + Phase 1.2A/B/C (schema arc) + Phase 1.3 (Layer 1 typed payload + builder + spec) + Phase 2.1 (Layer 2A discipline classifier) + Phase 2.2 (Layer 2D injury risk + paired injury_log §B schema evolution + §B form UI evolution) + Phase 2.3 (Layer 2B terrain classifier + paired Layer 0 terrain_gap_rules severity reclassification + paired pydantic widening) + Phase 2.5 (Layer 2E nutrition baseline vertical slice — daily targets + race-day fueling + dietary patterns + simplified sleep-dep; §5.5 + §5.8 stubbed) + Phase 2.4-Prep (Layer 2C data substrate — 4 column adds on `layer0.exercises` + `layer0.sport_specific_gear_toggles`; new toggle migration; ETL extractor wiring; structured substitutes routed via `etl/sources/parsed_substitutes.json`) shipped 2026-05-19; Phase 2.4 builder (Layer 2C equipment mapper — §5 tier-cascade algorithm + §8 coaching flags + §6 one-hop `also_satisfies` semantics + §5.6 accommodation pass-through from `Layer2DPayload`) shipped 2026-05-20 closing Phase 2; Phase 3.1-Substrate + Phase 3.1-Driver (Layer 3A integration accessors + LLM driver) shipped 2026-05-20 closing Layer 3A; Phase 4 (Layer 3B LLM driver + paired §8.3 doc-sweep) shipped 2026-05-20 closing Layer 3B; **Phase 5.1 (Layer 4 orchestrator vertical slice — race_week_brief end-to-end) shipped 2026-05-20** atop the same-day Step 7 SDK smoke scaffolding (PR #109 merged into main as `8854fa4`; `layer4/orchestrator.py:orchestrate_race_week_brief` + paired Layer4_Spec.md §4.5 D-72 audit-trail wording fix). **Phase 5.1 form-refresh A (race_terrain + aid_stations end-to-end on race-event edit path) shipped 2026-05-20**, closing the highest-leverage orchestrator forward-pointer + partial-closing Layer2B_Spec.md §12 Open Item 2B-3 for the race-event edit surface. **Phase 5.1 form-refresh B (§H.2 onboarding step-3c terrain capture) shipped 2026-05-20**, mirroring A's editor on the onboarding side via a shared `templates/_race_terrain_editor.html` partial and **fully closing Layer2B_Spec.md §12 Open Item 2B-3** (both onboarding + post-onboarding edit surfaces now capture TRN-xxx + percent rows). **Phase 5.1 form-refresh C (§J locale-terrain capture + paired Layer 2B `_validate_inputs` loosen for empty race_terrain) shipped 2026-05-20**, adding `locale_profiles.locale_terrain_ids TEXT[]` + multi-checkbox widget on both edit branches of the locale-edit form + orchestrator `_q_locale_terrain_ids(db, uid, primary_locale)` helper + Layer 2B `race_terrain_unset` coaching flag for athletes who skipped §H.2 capture; **fully closes Layer2B_Spec.md §12 Open Item 2B-2** (`§J Locale terrain access` controlled vocab on canonical TRN-xxx). **Phase 5.2 slice 1 (`orchestrate_single_session_synthesize`) shipped 2026-05-20** — second Layer 4 entry-point orchestrator atop the same-day Phase 5.1 trilogy completion; composes Layer 1 → 2A → 2D → 2C (locale-only) → 3A → `llm_layer4_single_session_synthesize_cached` for D-63 on-demand workouts; new `_q_locale_by_slug(db, uid, locale)` helper validates athlete-picked locale; `OrchestrationError` codes `request_sport_unavailable` + `locale_unknown` + `etl_version_set_undiscoverable`; 9 D-decisions ratified at plan-mode gate per Trigger #5. **Phase 5.2 slice 2 (`orchestrate_plan_refresh` T1/T2/T3) shipped 2026-05-20** — third Layer 4 entry-point orchestrator; composes the full upstream cone (Layer 1 → 2A/B/D/C → 3A → 3B → 2E) → `llm_layer4_plan_refresh_cached`; driver dispatches T1/T2/T3 internally (T3 cross-phase routes to Pattern A via the Step 4f-shipped `_route_t3_cross_phase_to_pattern_a`). **Shared `_upstream_full_cone` helper extracted per D1** — race_week_brief refactored to consume (behavior-preserving; 17 existing tests pass unchanged); single_session opts out per cone-shape divergence. 10 D-decisions ratified at plan-mode gate per Trigger #5 (D1 flip = extract-now; D3 = caller-supplied kwargs for plan_version_id/plan_version_id_parent/prior_plan_session_window/parsed_intent/plan_start_date; D4 = 3 shared pre-flight gates only; D9 = single function with tier kwarg). **Phase 1 + Phase 2 + Phase 3 + Phase 4 + Phase 5.1 + Phase 5.1 form-refresh A + B + C + Phase 5.2 slices 1 + 2 complete; orchestrator now exposes 3 of 4 Layer 4 entry points.** Phase 2.5 de-stubs (§I.1 supplement form refresh + Plan Management spec) + Phase 4 L3B-P-2 §H.2 deployed-shape gap remain as the next form-refresh follow-on (D structured supplements). Phase 5.2 slice 3 (`plan_create` Pattern A) inherits the orchestrator + helper substrate.

**Forcing function:** Andy's PGE 2026 (2026-07-17). `race_week_brief` auto-fires 2026-07-03 (days_to_event = 14). ~10 weeks of runway from 2026-05-19.

## Tests

1158 green + 4 skipped (last measured 2026-05-20 after Phase 5.2 slice 2 — orchestrator's `plan_refresh` entry point: 1145 → 1158 (+13 net new tests in `tests/test_layer4_orchestrator.py` across 6 `TestOrchestratePlanRefresh*` classes — `TestOrchestratePlanRefreshHappyPath` 4 (parametrized [T1/T2/T3] + parsed_intent threading) / `TestOrchestratePlanRefreshPreflightGates` 3 / `TestOrchestratePlanRefreshNoEventMode` 1 / `TestOrchestratePlanRefreshDefaults` 2 / `TestOrchestratePlanRefreshTierPassThrough` 2 / `TestOrchestratePlanRefreshReturnValue` 1). Container-runnable subset 478 → 491 passing in ~1.0s. Race_week_brief's 17 existing tests pass unchanged after the `_upstream_full_cone` helper-extract refactor (behavior-preserving). **Previous baseline (Phase 5.2 slice 1):** 1135 → 1145 (+10 net new tests in `tests/test_layer4_orchestrator.py` across 6 `TestOrchestrateSingleSessionSynthesize*` classes). Container-runnable subset 468 → 478 passing in ~0.9s. **Previous baseline (Phase 5.1 form-refresh C):** 1110 → 1135 (+25 net new: 1110 → 1135 (+25 net new: +21 in NEW `tests/test_locales.py` across 5 classes — `TestTrnPattern` 2 / `TestParseLocaleTerrain` 8 / `TestTerrainChoices` 2 / `TestHydrateLocaleTerrainIds` 8 / `TestEvictLayer2bOnTerrainChange` 1; +4 in `tests/test_layer4_orchestrator.py::TestLocaleTerrainIdsWireUp` covering `_q_locale_terrain_ids` SELECT response threading into Layer 2B kwargs + NULL column path + missing row path + SQLite-shim JSON-string path; +4 in `tests/test_layer2b.py::TestEmptyRaceTerrainLoosen` for the loosen pair (1 existing `test_empty_race_terrain_raises` removed, net +3 here) — the 4 new layer2b tests remain pre-existing-circular-import-blocked from collection at the container subset, same as the rest of `tests/test_layer2b.py`; counted toward the 1135 production total at the spec/regression run; 4 real-LLM smoke tests in `tests/test_layer3a_smoke.py` + `tests/test_layer3b_smoke.py` skip cleanly when `ANTHROPIC_API_KEY` unset → default `pytest tests/` reads "1135 passed, 4 skipped" once the circular-import is resolved). Container-runnable subset measured during this session: 468 passed (layer4 + race_events + onboarding race_events + locales) in 1.00s. When key is set, smoke tests exercise actual Sonnet 4.6 (~$0.12/run for the 4 3A/3B tests; Layer 4 real-LLM smoke not yet wired — Phase 5.2 alternative).

---
