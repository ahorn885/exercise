# Project Backlog

**Renamed from:** `Layer0_Drift_Backlog.md` (was scoped to Layer 0 drift; now broader)
**Started:** 2026-05-10
**File revision:** v46 — 2026-05-17 (**Layer 4 implementation Step 4b/c shipped — `llm_layer4_plan_refresh` T1 + T2 (D-64 caller integration) per `Layer4_Spec.md` §3.2 + §5.3 Pattern B + §5.5 capped retry + §8.6/§8.7 broadened observations.** New `layer4/plan_refresh.py` (~750 lines; thin tier-dispatcher + shared driver: input validation per §4.3, tool schema builder `build_record_refresh_sessions_tool(tier)` mirroring the full PlanSession contract per Step 4a precedent including the 9-shape `IntensityTarget` `oneOf` + closed 7-flag refresh `coaching_flags` enum, `_default_llm_caller` Anthropic SDK adapter, `_build_plan_session` parser, `_build_layer4_payload` Pattern B composition with `mode=plan_refresh` + `pattern=B` + `phase_structure=None` + `seam_reviews=None` + per-session `phase_metadata=None` + `is_ad_hoc=False`, `_emit_intensity_modulated_observation` per-multi-session bubble per §8.6/§8.7, retry loop with schema-violation special case + cap-hit best-effort acceptance demoting blockers→warnings per §5.5). T3 raises `tier_t3_not_yet_implemented` until Step 4d lands. New `layer4/plan_refresh_t1.py` (~280 lines; `SYSTEM_PROMPT` matching `Layer4_RefreshT1_v1.md` §5, `DEFAULT_MAX_TOKENS=2000`, `DEFAULT_EXTENDED_THINKING_BUDGET=3000`, `render_user_prompt()` inline Python rendering of §6 template + shared `_format_active_injuries` / `_format_prior_window_summary` / `_format_window_verbatim` helpers). New `layer4/plan_refresh_t2.py` (~280 lines; same shape with `DEFAULT_MAX_TOKENS=4000`, `DEFAULT_EXTENDED_THINKING_BUDGET=4500`, T2-specific weekly-aggregate summary line + 3B periodization shape context block + deload-cadence reminder; reuses T1's formatting helpers). **`layer4/context.py` additions:** `Layer2Bundle` pydantic v2 model (5 optional Layer2*Payload fields a/b/c/d/e; `c` is `dict[str, Layer2CPayload]` keyed by locale_id) + `ParsedIntent` pydantic v2 model (5 trigger flags + 3 soft signals + raw_text + parser_confidence + ambiguity_notes; degraded defaults for parser-unavailable path per `Plan_Refresh_D64_Design_v1.md` §5.4). **`layer4/__init__.py`** adds 4 new re-exports: `Layer2Bundle`, `ParsedIntent`, `build_record_refresh_sessions_tool`, `llm_layer4_plan_refresh`. **`Layer4_Spec.md` §3.2 surgical amendment (paired with Andy 2026-05-17 §4.3-wins pick):** `layer3b_payload: Layer3BPayload | None` → `layer3b_payload: Layer3BPayload`; parameter table notes column for 3B rewritten — 3B required on every tier; T1's prior-narrative of "falls back to phase_metadata" retired. Trigger #5 fired; routed via two-round AskUserQuestion gate per the precedent. **No new D-rows.** **58 new tests** in `tests/test_layer4_plan_refresh.py` covering: Layer2Bundle × 4 + ParsedIntent × 3 + Tool schema × 6 (T1 vs T2 maxItems + name + required-fields + closed 7-flag enum + 9-shape IntensityTarget oneOf + intensity_zone enum) + §4.3 input validation × 11 + entry-point happy path × 7 (T1 cardio single + T1 two-day + T1 empty + T2 full week + T2 mixed + telemetry + scope) + observation emission × 3 + capped retry × 4 + schema violation × 3 + Layer4Payload composition × 9 + prompt rendering × 7. Combined `tests/` count 354 → 412, all green in 0.56s. **Carry-forward to next session (mechanically-spec'd in closing handoff §6.3):** (a) `Plan_Refresh_D64_Design_v1.md` §3 — T1 default cascade now includes 3B re-run; (b) `Layer4_RefreshT1_v1.md` → `_v2.md` per Rule #12 — D3 source-decision row + §3.5 + §3.8 updates reflecting 3B-always-included; T2 prompt body already says "3B re-run as part of T2 cascade" — no T2 amendment needed. **Architectural choices on the record:** (a) Two-file tier split (`_t1.py` + `_t2.py`) per Andy's pick (mirrors prompt-body D7 two-files pick); thin `plan_refresh.py` driver dispatches on tier; (b) `Layer2Bundle` + `ParsedIntent` in `layer4/context.py` (Andy's pick) — reusable across all refresh entry points + T3 when Step 4d lands; (c) Tool schema = full PlanSession contract mirror (Step 4a precedent — Option 2); (d) `parsed_intent=None` accepted at the entry-point boundary; driver substitutes a degraded ParsedIntent with `parser_confidence='low'` per `Plan_Refresh_D64_Design_v1.md` §5.4 (parser-unavailable graceful degrade). **Branch:** `claude/implement-layer4-closing-YPmyc` (harness-pinned for this session — name carried over per precedent). **10 files this session (6 substantive code + 1 spec + 3 bookkeeping):** new `layer4/plan_refresh.py`, new `layer4/plan_refresh_t1.py`, new `layer4/plan_refresh_t2.py`, `layer4/context.py` (Layer2Bundle + ParsedIntent additions), `layer4/__init__.py` (4 new re-exports), new `tests/test_layer4_plan_refresh.py`, `aidstation-sources/Layer4_Spec.md` (§3.2 surgical amendment), this backlog v45 → v46, `CLAUDE.md`, new closing handoff. **Over the 5-file ceiling intentionally — 10 files** — precedent from PR-A 8 + PR-B 7 + PR-C-followon 6 + PR-D 6 + PR-E 6 + Step 4a 8. Per `V5_Implementation_Layer4_Step4b_4c_PlanRefresh_Closing_Handoff_v1.md`.) Preceded by v45 — **Layer 4 implementation Step 4a shipped — `llm_layer4_single_session_synthesize` (D-63 caller integration) per `Layer4_Spec.md` §3.3 + §5.3 Pattern B + §5.5 capped retry + §8.7 observations. New `layer4/single_session.py` (~720 lines) defines `SingleSessionRequest` pydantic v2 model + `build_record_single_session_tool()` returning a strict JSON schema mirroring the full `Layer4Payload.PlanSession` contract (Andy 2026-05-17 Option 2 pick — full fidelity over LLM-compliance-burden) including the 9-shape `IntensityTarget` `oneOf` union + cardio block `intensity_zone` enum + strength `exercise_name`/`rest_between_sets_sec`/`tempo` + `_validate_inputs()` (§4.4 preconditions; fail-fast) + `_render_user_prompt()` (inline Python rendering of `Layer4_SingleSession_v2.md` §6 user-prompt template) + `_default_llm_caller` (Anthropic SDK adapter; dependency-injectable for tests) + `_build_plan_session()` (parses tool args → typed PlanSession via pydantic smart-union dispatch on 9 IntensityTarget shapes; fills `is_ad_hoc=True` + `ad_hoc_request_payload` + `phase_metadata=None`) + main `llm_layer4_single_session_synthesize()` driver (validates inputs → renders prompt → invokes synthesizer → builds payload → runs `validate_layer4_payload()` mode-gated to single-session-relevant rules → retries up to `capped_retries=2` with `RuleFailure` context merged → on cap-hit demotes outstanding blockers to warnings + emits `Observation(category=best_effort_plan, elevates_to_hitl=True)` per §5.5 + appends final accepted ValidatorResult so the Layer4Payload `validator_results[-1].accepted=True` invariant holds; emits `Observation(category=intensity_modulated, elevates_to_hitl=False)` when synthesizer emits the matching session flag per §8.7). New `layer4/errors.py` defines `Layer4Error` base + `Layer4InputError(code, detail)` + `Layer4OutputError(code, detail)` per §3.5. Schema-violation special case (§5.5): one schema-only retry on tool-output parse failure that does not consume the per-call budget; on second failure raises `Layer4OutputError(schema_violation)`. Paired prompt body amendment `Layer4_SingleSession_v1.md` → `_v2.md` per Rule #12 (v1 retained as in-project history); v2 amends only file header + adds D13 source-decision row (tool-schema fidelity = full payload-contract mirror) + replaces §4.1 (tool schema sketch → reference to `layer4.single_session.build_record_single_session_tool()` + field-by-field spec listing the full PlanSession contract). v1 §§1–3 + §§5–14 (system prompt, user prompt template, intensity-modulation tiers, injury exclusions, coaching voice, edge cases, test scenarios, gut check) carry over unchanged — no coaching-policy change.** 5 substantive code/spec files (`layer4/single_session.py` new + `layer4/errors.py` new + `layer4/__init__.py` 6 new re-exports + `tests/test_layer4_single_session.py` 49 tests + `aidstation-sources/prompts/Layer4_SingleSession_v2.md` new) + 3 bookkeeping (this backlog v44 → v45 + CLAUDE.md + closing handoff). Combined `tests/` count 305 → 354, all green in 0.64s. **Step 4a opens Layer 4 Step 4 (per-entry-point LLM call sites — first of 4 entry points).** Step 4b/c (`llm_layer4_plan_refresh` T1+T2 D-64 caller integration) is next per architect recommendation. **No new D-rows.** **Architectural choices on the record:** (a) `Layer1Payload` as opaque `dict[str, Any]` pass-through per PR-D precedent (Layer 1 typed schema out of v1 scope); the user-prompt template reads `experience_level` + `coaching_voice_preferences` keys; missing keys render as "unknown" / omit silently; (b) Tool-schema = full payload contract mirror (Andy 2026-05-17 Option 2); (c) `_default_llm_caller` dependency injection via `LLMCaller` type alias — tests stub; production reads `ANTHROPIC_API_KEY`; (d) `plan_version_id=0` v1 sentinel for D-63 ad-hoc outputs (spec §3.3 calls for None but Layer4Payload requires non-None int; 0 documented as "ad-hoc, no plan pinning"; orchestrator can override); (e) `sport_not_in_inclusion` §4.4 row 6 not checked at Layer 4 (no `layer2a_payload` in §3.3 signature; caller pre-checks per D-63 §6.3); (f) Best-effort fallback synthesizes a final accepted ValidatorResult with demoted warnings so the `validator_results[-1].accepted=True` Layer4Payload invariant is satisfied. **Stop-and-ask triggers retrospective:** Trigger #8 (architectural alternatives with real tradeoffs) — fired and routed properly via AskUserQuestion (tool-schema fidelity + bookkeeping bundle); Andy picked Option 2 (full fidelity) + bundled 8 files. Trigger #2 (modifying a prompt body) — fired indirectly via Option 2 pick; v2 amendment scoped to §4.1 + D13 row only; no coaching-policy / §5 system prompt / §6 user prompt changes. Trigger #5 (schema/inter-layer-contract amendments) — did NOT fire; `Layer4_Spec.md` untouched; v1 prompt body's §4.1 was explicitly v1-scoped (line 134 references the canonical `PlanSession` shape; reconciling to the on-disk pydantic contract is implementation-of-spec, not contract change). **Tests:** 49 new tests in `tests/test_layer4_single_session.py` (~700 lines) — SingleSessionRequest validation × 9 + tool schema basics × 5 + §4.4 input validation × 4 + entry-point happy path × 8 + observation emission × 2 + capped retry × 4 + schema violation × 3 + Layer4Payload composition invariants × 9 + prompt rendering × 5. **Branch:** `claude/implement-v5-validator-WelCn` (harness-pinned for this session — name carried over from a prior validator-scoped branch even though this PR is Step 4a single-session integration). **Files touched this revision (5 substantive code/spec + 3 bookkeeping = 8 total): new `layer4/single_session.py`, new `layer4/errors.py`, `layer4/__init__.py` (6 new re-exports), new `tests/test_layer4_single_session.py`, new `aidstation-sources/prompts/Layer4_SingleSession_v2.md`, this backlog v44 → v45, `CLAUDE.md`, new `V5_Implementation_Layer4_Step4a_SingleSession_Closing_Handoff_v1.md`. Over the 5-file ceiling intentionally (8 files) — Andy confirmed at session start; precedent from PR-A 8 + PR-B 7 + PR-C-followon 6 + PR-D 6 + PR-E 6.** Per `V5_Implementation_Layer4_Step4a_SingleSession_Closing_Handoff_v1.md`. Preceded by v44 — Layer 4 implementation Step 3 PR-E of E (deterministic validator harness; 21 rules; 96 tests; combined 305).)

Predecessor revisions (most recent first):

- v43 — 2026-05-17 (**Layer 4 implementation Step 3 PR-D of E shipped — new `layer4/context.py` (~570 lines, ~55 types) typed pydantic v2 mirrors of the 7 upstream-layer §7 contracts (Layer 2A + 2B + 2C + 2D + 2E + 3A + 3B) + onboarding §G.1 daily-availability-window + 2 forward-pointer placeholders (RaceEventStub pending D-66; PerDateRestriction pending D-67). Mirrors the AMENDED 2D + 2C specs from PR-C-followon: `AccommodationModality` discriminated-union (6 variants — VolumeReductionModality, IntensityReductionModality, TempoModificationModality, LoadingTypeChangeModality, FrequencyReductionModality, ExerciseSubstitutionModality) with tagged-union dispatch on `modality_type: Literal[...]` discriminator; `ExerciseRisk.accommodations` + structural invariant (non-empty iff verdict='accommodate'); `ResolvedExercise.accommodations` pass-through.** 3 substantive code files (`layer4/context.py` new + `layer4/__init__.py` 55 re-exports added + `tests/test_layer4_context.py` 50 tests) + 3 bookkeeping (this backlog v42 → v43 + CLAUDE.md + closing handoff). Combined `tests/` count 159 → 209, all green in 0.32s. PR-D closes the typed-context surface for Layer 4's §5.4 validator harness (Step 3 PR-E next). **No new D-rows** surfaced; PR-D's only minor scoping discovery was a `Layer2EHitlItem` field-set guess corrected on a late-pass cross-reference to Layer2E_Spec.md line 810 (implementation-fidelity, not contract amendment; spec line 810 contains the canonical 9-field shape). **`layer4/context.py` sections:** (1) AccommodationModality discriminated union with 6 typed variants + bounds (factor ranges 0.3-1.0 / 0.4-1.0 / 0.0-1.0 per variant; hold_s 15-60; intensity_pct_mvc 30-100) + cross-field invariants (isometric_only requires hold_s + sets + intensity_pct_mvc; eccentric_focus / heavy_slow_resistance require ≥1 tempo-tuple component; FrequencyReductionModality requires factor OR sessions_per_week_cap); (2) Layer 2A — Layer2APayload + Layer2ADiscipline + WeightResult + PhaseLoadBands + TrainingGap + TrainingGapsSummary + UnresolvedFlag + Layer2ACoachingFlag + RationaleMetadata; (3) Layer 2B — Layer2BPayload + RaceTerrainOutput + TerrainGap + Layer2BSummaryBlock + Layer2BCoachingFlag; (4) Layer 2C — Layer2CPayload + DisciplineCoverage + ResolvedExercise (with accommodations field per §5.6 amendment) + ResolutionDetail + Layer2CCoachingFlag; (5) Layer 2D — Layer2DPayload + ExerciseRisk (with verdict-to-accommodations invariant per §5.3.6 — accommodations non-empty iff verdict='accommodate'; empty for exclude / clean) + Evidence + DisciplineRisk + MatchedBodyPart + SubstituteRecommendation + Layer2DCoachingFlag + Layer2DHitlItem (InjuryRecord + HealthConditionRecord typed `dict[str, Any]` to avoid pulling Layer 1 onboarding types into Layer 4's typing surface; opaque pass-through; v2 may type explicitly if Layer 4 starts consuming structured form) + payload-level defensive partition (`_check_excluded_verdict` ensures excluded_exercises entries carry verdict='exclude' and accommodated_exercises entries carry verdict='accommodate'); (6) Layer 2E — Layer2EPayload + DailyNutritionBaseline + DailyPhaseTargets + MacroTargets + RaceDayFueling + CaffeineRacedayPlan + SupplementIntegrationPayload + IntegratedSupplement + RaceDaySupplementSuggestion + DietaryPatternFlag + SleepDepFuelingOverlay + HeatAcclimEventAdjustment + Layer2ECoachingFlag + Layer2EHitlItem (9-field shape per spec line 810: item_id + gate_number 1-5 + block_level + affected_supplement_id + affected_event_id + affected_condition_category + rationale_for_athlete + rationale_for_layer3 + resolution_options); (7) shared Layer3Observation (3A + 3B identical 4-element category enum); (8) Layer 3A — Layer3APayload + CurrentState + Assessment + RecentTrajectory + TrajectoryWindow + ACWRStatus + ACWREntry + DataDensity. **No `active_injuries` field** — that field never existed in 3A per the PR-C-followon spec audit; canonical injury source is 2D; (9) Layer 3B — Layer3BPayload + GoalViability (with achievable ↔ adjustments-empty invariant per §7 schema rule) + PeriodizationShape (with custom ↔ phase_weeks invariant) + Layer3BHITLItem (with blocker ↔ acknowledge_option=None invariant) + Layer3BPayload-level HITL-labels-unique invariant; (10) DailyAvailabilityWindow with enabled-vs-window-presence + second-window pairing + second-window-requires-doubles-feasible invariants per §G.1; (11) RaceEventStub (minimal v1 — full schema pending D-66) + PerDateRestriction (placeholder — always-empty in v1; full schema pending D-67). **Architectural choices on the record:** (a) `AccommodationModality` as tagged-union (discriminator on `modality_type`) — modality variants carry the literal discriminator; tagged-union is idiomatic + faster. Smart-union also works but is slower. The IntensityTarget union in `payload.py` uses smart-union because shapes have no discriminator field. (b) `Layer2DHitlItem.injury` + `.condition` typed `dict[str, Any]` (opaque pass-through) — Layer 1 onboarding contracts (InjuryRecord + HealthConditionRecord) aren't consumed by Layer 4's validator. (c) `Layer3Observation` shared between 3A + 3B (identical shape per spec). Layer 4 output `Observation` in `payload.py` has broader category enum (10 vs 4) — kept separate. (d) Bounds enforced at leaf models (physical-sanity not domain-policy; domain rules live in §5.4 validator harness). **Stop-and-ask triggers — none fired this session:** Trigger #5 (schema/inter-layer-contract amendments) — Layer2EHitlItem fix was implementation-fidelity (misread §7), not contract amendment; canonical shape lives at `Layer2E_Spec.md` line 810. No trigger fired. Other triggers — none applicable (no new vocabulary, no exercise db rows, no HITL trigger, no partial-update invalidation rule, no Control_Spec change, no new D-row). **Tests:** 50 new tests covering AccommodationModality dispatch × 12 (6 variants + bounds + invariants + unknown-type-rejected) + ExerciseRisk accommodation invariants × 6 (verdict × accommodations interactions) + ResolvedExercise pass-through × 6 (tier × accommodations) + happy-path × 8 payloads + extra='forbid' × 8 + JSON round-trip × 4 (incl. tagged-union survival across model_dump_json round-trips for multi-modality cases) + cross-field validation × 6 (Layer2DPayload defensive partition × 2 + GoalViability adjustments invariant × 2 + PeriodizationShape phase_weeks invariant + Layer3BPayload HITL-labels-unique). **Branch:** `claude/injury-accommodation-modality-BPYOC` (harness-pinned for this session; name carried over from PR-C-followon's modality theme; precedent: PR-A → PR-B → PR-C → PR-C-followon all harness-pinned). **Files touched this revision (3 substantive code + 3 bookkeeping = 6 total):** `layer4/context.py` (new), `layer4/__init__.py` (55 new re-exports), `tests/test_layer4_context.py` (new), this backlog file revision bump (v42 → v43), `CLAUDE.md` (last-shipped narrative bump → PR-D; PR-C-followon demoted to predecessor; Layer 4 row updated; Backlog ref v42 → v43; Next-forward-move recommends PR-E validator harness), new `V5_Implementation_Layer4_Step3_PR_D_Context_Schemas_Closing_Handoff_v1.md`. **Over the 5-file ceiling intentionally (6 files)** — precedent from PR-A 8 + PR-B 7 + PR-C 4 + PR-C-followon 6. Per `V5_Implementation_Layer4_Step3_PR_D_Context_Schemas_Closing_Handoff_v1.md`.)

- v42 — 2026-05-17 (**Injury-accommodation modality foundation amendment — `Layer2D_Spec.md` §5.3 + §7 (new §5.3.6 + AccommodationModality typed union + ExerciseRisk extension + verdict rename DOWNGRADE→ACCOMMODATE + downgraded_exercises field rename) + `Layer2C_Spec.md` §5 + §7 (new §5.6 modality pass-through + ResolvedExercise.accommodations) + `Layer4_Spec.md` §3.2 + §4.1 + §5.4 (injury_violation_* rule wording fix + NEW injury_accommodation_violation_* rule + accommodation modality framework summary block) + 3 new D-rows (D-69 closed this session; D-70 + D-71 deferred to v2).** Architect-recommended PR-D context schemas (Layer 4 implementation Step 3) was preempted mid-session when Andy surfaced the broader injury-accommodation lifecycle question: "the injury is logged → injury is interpreted (movements restricted, muscle groups impacted) → exercises are interpreted (swap or volume change or other) → accommodations are made. we should have the foundational data specced to support it." Audit revealed Layer 4 spec referenced `Layer3APayload.current_state.active_injuries` field that never existed in 3A §7 contract (2D was the canonical source per §3.2 line 29 but lines 753, 898, 1360 referenced 3A in error) AND that the binary EXCLUDE/DOWNGRADE/CLEAN verdict model in 2D captured only 1-2 of ~10 evidence-based accommodation modalities. Spec-only amendment session executes per Andy's `/plan`-mode-ratified path (Path II — single research-grounded session, evidence-based v1 closed enum of 6 modalities, defer ROM + phase-sequencing to v2). **Stop-and-ask triggers #5 (inter-layer schema contract amendments — Layer 2D + 2C + Layer 4 schemas + cross-layer modality propagation) + #11 (new cross-layer D-rows: D-69 spans Layer 2D + 2C + Layer 4 + onboarding-side InjuryRecord consumer chain) both fire** — routed through `/plan` mode discussion before any spec edit applied. **Path II research source basis** (Claude WebSearch + WebFetch literature scan; citations preserved in modality definitions): Soligard et al. 2016 IOC consensus "How much is too much (Part 1)" (BJSM 50:1030-1041); Gabbett 2016 ACWR systematic review; Cook & Purdam 2014 (BJSM) load management in tendinopathy + staged isometric→isotonic→energy-storage progression; Alfredson 1998 + Stevens & Tan 2014 (JOSPT 44:59-67) eccentric heel-drop protocol; Beyer et al. 2015 (AJSM) HSR vs Alfredson equivalent outcomes + better compliance; Rio et al. 2015 (BJSM) isometric analgesia 5×45s @ 70% MVC for patellar tendinopathy; Silbernagel et al. 2007 (AJSM 35:6) pain-monitoring model VAS ≤5/10; Manca et al. 2017 (Sports Med) Delphi consensus on cross-education for orthopedic conditions; Hendy & Lamon 2017 systematic review on cross-education 12-22% strength gain in untrained limb; Farthing & Zehr 2014 (BJSM) cross-education and immobilisation; ACSM Guidelines for Exercise Testing and Prescription 11th ed. FITT-VP framework. **`Layer2D_Spec.md` §5.3 amendments (4 surgical edits + 1 new sub-section):** (1) §5.3.4 severity_to_verdict mapping renamed `Verdict.DOWNGRADE` → `Verdict.ACCOMMODATE`; verdict enum widened from {EXCLUDE, DOWNGRADE, CLEAN} to {EXCLUDE, ACCOMMODATE, CLEAN}; "Accommodate" semantic — exercise can be prescribed with one or more accommodation modalities applied — replaces the previous binary "reduce somehow" semantic; (2) §5.3.3 movement_constraint_verdict updated to ACCOMMODATE; (3) §5.3.2 condition_verdict comment updated; (4) §5.3.5 evaluate_exercise extended to call new `recommend_accommodations()` function when verdict resolves to ACCOMMODATE; populates `ExerciseRisk.accommodations` field. (5) **NEW §5.3.6 Accommodation modality recommendation** — ~150 lines spanning 6 sub-sections: §5.3.6.1 v1 closed modality set (6 variants with parameter shapes + evidence basis citations in markdown table); §5.3.6.2 v1 default modality table mapping (injury_type × severity) → recommended modalities (covers Tendinopathy/overuse × {Acute, Recovering, Chronic-Managed}, Muscle/tendon strain × {Acute, Recovering}, Stress fracture/bone stress × {Acute, Recovering}, Joint sprain × {Acute, Recovering}, Post-surgical × Post-surgical); §5.3.6.3 fallback recommendation (conservative volume_reduction 0.7 + intensity_reduction 0.7 per IOC consensus when (injury_type, severity) not in table); §5.3.6.4 phase-dependent contraindications (acute reactive tendinopathy MUST use isometric_only — eccentric/energy-storage CONTRAINDICATED; bone stress — intensity_reduction alone INSUFFICIENT, must pair with frequency_reduction-to-zero OR route through EXCLUDE; post-surgical first 6wk — prefer loading_type_change to unilateral_contralateral over reduced-load bilateral); §5.3.6.5 v1 lookup function (`recommend_accommodations()` picks driving injury from evidence, looks up in V1_DEFAULT_ACCOMMODATIONS, applies phase contraindications); §5.3.6.6 what v1 explicitly does NOT do (ROM modality deferred per D-70; phase sequencing deferred per D-71; per-exercise tailoring beyond (injury_type, severity) keying deferred to v2; muscle-group-level interpretation NOT in v1 — body_parts vocab sufficient for v1 use cases; discipline-specific tailoring deferred). **`Layer2D_Spec.md` §7 amendments:** `Layer2DPayload.downgraded_exercises` renamed to `accommodated_exercises` (clean rename — 2D unimplemented in code); `ExerciseRisk.verdict` doc widened to {exclude, accommodate, clean}; `ExerciseRisk.accommodations: list[AccommodationModality]` field added (non-empty iff verdict='accommodate'); new `AccommodationModality` discriminated union (~85 lines) with 6 typed variants — `VolumeReductionModality` (factor 0.3-1.0 + applies_to ∈ {sets, reps, duration}), `IntensityReductionModality` (factor 0.4-1.0 + target_metric ∈ {percent_1rm, rpe, pace, power, hr_zone}), `TempoModificationModality` (tempo_pattern ∈ {eccentric_focus, isometric_only, heavy_slow_resistance} + tempo tuple OR isometric specifics), `LoadingTypeChangeModality` (from_type + to_type enum), `FrequencyReductionModality` (factor OR sessions_per_week_cap + optional discipline_id), `ExerciseSubstitutionModality` (no parameters — included for unified-enum completeness but NOT emitted directly by 2D; flows through verdict='exclude' + 2C Tier 2/3 substitution); each variant carries `rationale: str` + `evidence_basis: list[str]` (free-text citation IDs). **`Layer2C_Spec.md` §5 + §7 amendments:** new §5.6 "Accommodation modality pass-through" (~15 lines — copies 2D's per-exercise accommodations list onto ResolvedExercise.accommodations via mechanical mapping keyed on exercise_id; 2C does NOT interpret modalities, only passes through); §5.7 = original §5.6 per-discipline coverage aggregation (renumbered); `ResolvedExercise.accommodations: list[AccommodationModality]` field added (pass-through from 2D). **`Layer4_Spec.md` amendments:** §3.2 line 753 input-validation rule rewritten — `active_injury_data_gap` → `injury_data_inconsistent`, references corrected from non-existent `layer3a_payload.current_state.active_injuries` to canonical `layer2d_payload.excluded_exercises + accommodated_exercises`; §5.4 `injury_violation_*` rule (line 898) rewritten — drops 3A active_injuries body-part-restriction-text framing, replaced with clean set-membership check "every prescribed exercise_id NOT IN layer2d_payload.excluded_exercises[].exercise_id"; **NEW §5.4 rule `injury_accommodation_violation_*`** added after `injury_violation_*` — per-modality validator checks (volume_reduction: prescribed volume ≤ baseline × factor + 10% tolerance; intensity_reduction: prescribed intensity ≤ baseline × factor + 10% tolerance; tempo_modification: StrengthExercise.tempo matches modality tuple within ±1s per component; loading_type_change: implement/laterality matches to_type per 2C ResolvedExercise metadata; frequency_reduction: weekly session count ≤ sessions_per_week_cap or baseline × factor); severity = warning (not blocker) in v1 to avoid over-triggering on LLM baseline-volume fuzziness; bubbles to plan-diff observations for athlete review; v2 may promote to blocker once measured retry rates inform calibration; §13.4 wrist-injury-only-strength-day edge-case (line 1360) rewritten to reference 2D excluded_exercises as canonical source; §5.4 summary block extended with new "Injury accommodation modality framework" subsection enumerating the 6-modality closed set + per-modality validator checks + deferred items (ROM = D-70; phase sequencing = D-71). **Three new D-rows (cross-layer scope, stop-and-ask trigger #11 retrospective):** **D-69** ✅ Resolved-this-session — Injury-accommodation modality foundation. Spec the AccommodationModality typed union + per-injury-type-and-severity decision matrix + Layer 2D §5.3.6 logic + Layer 2C pass-through + Layer 4 validator rule. Closes the "we should have the foundational data specced to support [the 4-step injury-accommodation lifecycle]" Andy directive. **D-70** 🟡 Deferred (v2) — Range-of-motion restriction modality. Add `range_of_motion_restriction` variant to AccommodationModality typed union with mode ∈ {pain_free, partial_specified} + max_flexion_deg + max_extension_deg + rom_fraction parameters. Defer trigger: first real ROM-restriction case surfaces (likely post-op or shoulder-impingement athlete). Excluded from v1 because parameterization is condition-specific (e.g., "0-90° flexion post-ACL" vs "pain-free range" for shoulder impingement); no clean cross-condition shape per literature scan. **D-71** 🟡 Deferred (v2) — Phase-sequencing for tendinopathy progression. Per Cook & Purdam staged progression (isometric → HSR → energy-storage), extend 2D to emit modality SEQUENCE (e.g., {phase: isometric, weeks: 2-4} → {phase: HSR, weeks: 4-12} → {phase: energy-storage, weeks: 12-16}) rather than current-time only. Defer trigger: first chronic tendinopathy case with multi-stage progression needs. v1 captures progression implicitly via T1 refresh + 3A re-assessment re-running 2D against the updated injury severity (no phase-sequencing infrastructure needed in 2D itself). **No code changes this revision** — spec-only amendment; PR-D context schemas will follow in next session against the amended specs. **Architectural design wave gap acknowledged:** The original PR-C closing handoff §5 forward-pointer projected PR-D as "Stop-and-ask risk: None expected. Context schemas are derived directly from already-shipped upstream specs — no new design calls." Reality differed — the upstream-spec audit during PR-D scoping surfaced the 3A active_injuries field-reference drift + the broader insufficiency of binary EXCLUDE/DOWNGRADE for the evidence-based accommodation modality space. Both gaps existed in the spec since the original Layer 4 spec arc shipped. Add to gut-check learnings: implementation-track PR scoping should explicitly audit upstream spec field references (the PR-C handoff §5 sketch papered over the contract gaps; the PR-D scoping discovery surfaced them). **Branch:** `claude/spec-amendments-implementation-tTUn7` (harness-pinned for this session — Andy confirmed leaving the harness-pinned name; precedent: PR-A → PR-B → PR-C all harness-pinned). **Files touched this revision (3 substantive spec + 3 bookkeeping = 6 total):** `Layer2D_Spec.md` (§5.3.2 + §5.3.3 + §5.3.4 + §5.3.5 verdict-rename + new §5.3.6 accommodation modality recommendation section + §6 D-2D-4 drift item update + §7 ExerciseRisk + Layer2DPayload + new AccommodationModality typed union + §10 example narratives), `Layer2C_Spec.md` (new §5.6 modality pass-through + §5.7 renumbered coverage aggregation + §7 ResolvedExercise.accommodations field), `Layer4_Spec.md` (§3.2 / §4.1 row 753 rewrite + §5.4 injury_violation_* rewrite + §5.4 new injury_accommodation_violation_* rule + §5.4 summary block extension + §13.4 wrist-injury edge case rewrite), this backlog file revision bump (v41 → v42) with D-69/D-70/D-71 rows appended after D-68, `CLAUDE.md` (last-shipped-narrative bump → injury-accommodation amendment; PR-C demoted to predecessor; Layer 4 row updated; Backlog ref v41 → v42; Next-forward-move points at PR-D against amended specs), new `V5_Implementation_Layer4_PR_C_Followon_Injury_Accommodation_Modality_Foundation_Closing_Handoff_v1.md`. **Over the 5-file ceiling intentionally (6 files)** — Andy explicit direction at session start; precedented by PR-A 8 files + PR-B 7 files + PR-C 4 files + PR17 7 files. **Stop-and-ask trigger #5 + #11 retrospective:** Both fired and routed properly through `/plan` mode iteration — Andy expanded scope from quick volume_reduction_factor addition to full evidence-based 6-modality closed set; corrected my Step 3 framing to include intensity/tempo/loading/frequency/ROM (not just swap+volume); chose Path II (research-grounded single session over Layer 0 full design wave); ratified the 6-modality picks including substitution as unified-enum representation; ratified current-time-only over modality-sequence; muscle-group deferred. Per `V5_Implementation_Layer4_PR_C_Followon_Injury_Accommodation_Modality_Foundation_Closing_Handoff_v1.md`.)

- v41 — 2026-05-17 (**Layer 4 implementation Step 3 PR-C of E shipped — `Layer4_Spec.md` §5.4 amendments (volume thresholds, ACWR chronic window, locale-cluster splits, indoor-only rule, D-66/D-67/D-68 forward-pointers) + 3 new D-rows.** PR-C is the spec-only prerequisite for the Step 3 deterministic validator harness; routed through `/plan` mode per Andy 2026-05-17 amendment-authoring directive. **Stop-and-ask trigger #5 (schema/inter-layer-contract amendments) + trigger #11 (new D-rows with cross-layer scope) both fire** — three D-rows added (D-66 + D-67 + D-68), all cross-layer. **`Layer4_Spec.md` §5.4 amendments (6 surgical edits + 1 new rule row):** (1) `volume_band_*` thresholds widened ±15/±5 → **±20/±10** per Andy 2026-05-17 (weekly endurance volume naturally fluctuates ±10% from weather / schedule / recovery; tighter thresholds produced too many false-positive blockers); (2) `acwr_*` chronic denominator clarified — **trailing 28-day window** per Gabbett 2016, acute = trailing 7d; (3) `equipment_unavailable_*` (rule 6) split into three sub-rules: **6a equipment resolves at session locale** (existing semantics, D-68 fallback annotation), **6b single-locale per session** (NEW — all blocks within a PlanSession share one locale_id; no mid-session location swaps; blocker), **6c session locale in athlete cluster** (NEW — locale_id must be in saved cluster; D-67 locale-lock annotation; blocker); (4) `discipline_excluded_*` (rule 9) extended with D-67 discipline-exclusions branch (athlete-set per-date "no MTB" / "no outdoors" filter); (5) `kit_manifest_inputs_incomplete` (rule 12) D-66 always-warn annotation (race-event data model not yet built; rule decorative until D-66 ships); (6) `daily_window_fit_*` extended with D-67 `max_total_minutes` branch (per-date time cap); (7) NEW row `indoor_only_violation_*` — when D-67 `indoor_only=True` set by athlete, session's locale_category + discipline_id must both be in indoor sets (blocker; pre-D-67 always passes). Closing tolerance-defaults paragraph updated to reflect new thresholds + 28-day ACWR window. New summary block after the rule table enumerates the 4 D-67-dependent rule branches that ship in v1 but no-op until D-67 lands (defensive forward-compatibility — none block PR-E validator harness landing). **Three new D-rows (cross-layer scope, stop-and-ask trigger #11 retrospective):** **D-66** race-event data model — `race_events` + `race_route_locales` tables + onboarding/profile UI to enter target race details (race_format, distance, elevation, rules summary, route locales with start/transition/aid_station/drop_bag/finish roles, sequence_idx, mile_markers); blocks rule `kit_manifest_inputs_incomplete` from being non-trivially evaluable on multi-day events; defer design until Step 4e race-week-brief implementation approaches per §14.3.4. **D-67** per-date athlete restrictions — `daily_restrictions` table + onboarding/profile UI surface (one row per athlete-date when restrictions are set); 4 v1 restriction types: locale_lock (single locale_id), discipline_exclusions (set of discipline_ids — covers "no MTB today" + "no outdoors today"), indoor_only (bool — derived shortcut: locale + discipline filter combined), max_total_minutes (per-date time cap — covers "≤30min only today"); UX surface lives in the schedule tab on /profile + a per-session-card override modal; defer design until D-66 sibling design wave or earlier as needed. **D-68** default equipment profile per locale category — Layer 0 reference data (curated category-defaults per `MANUAL_CATEGORIES` bucket: hotel_gym, commercial_chain_gym, home_gym, etc.; ~8 categories × N canonical equipment items each) + Layer 2C fallback resolution (locale_equipment_overrides if present → else category default → else empty pool with `data_gap` observation) + orchestrator nudge prompt on default-fallback usage ("Plan assumed your hotel has treadmill + dumbbells + cable. Tap to confirm or edit" fires T1 refresh on edit). **Defaults are transient resolution-only — they NEVER write to `locale_equipment_overrides` or persist on the locale profile.** Athlete confirmation via the nudge prompt is the only mechanism by which defaults become real profile data. Defer design until v5 onboarding implementation PR approaches or before, depending on athlete-2's hotel-week timing. **Architectural design wave gaps** — these three D-rows ARE the §14 retrospective's missing finding M4 ("the locale-system seams the retro missed"). The retrospective surfaced "joint sessions + Layer 5 + D-57 + post-race surface + brief-diff renderer" as what-might-be-missing but didn't catch the per-date-restriction or unknown-equipment seams. **No code changes this revision; PR-C is spec-only** — PR-D (Step 2.5 context schemas: `Layer2APayload`, `Layer2CPayload`, `Layer2EPayload`, `Layer3APayload`, `Layer3BPayload`, `DailyAvailabilityWindow`, `RaceEventStub`, `PerDateRestriction` placeholder types per pydantic v2 `BaseModel` convention) + PR-E (Step 3 validator harness with 18 rules — 17 from §5.4 post-amendment + indoor_only_violation) follow in subsequent sessions. **Branch:** `claude/layer4-migration-hashing-XYE4x` (harness-pinned for this session — deviates from a hypothetical recommended `claude/layer4-spec-amendments-NEWSUFFIX` per harness override; same precedent as PR-B). **Files touched this revision (3 substantive — `Layer4_Spec.md` §5.4 + this file revision + closing handoff; plus `CLAUDE.md` bookkeeping = 4 total):** `Layer4_Spec.md` (§5.4 6 surgical edits + 1 new row + tolerance paragraph + new D-66/67/68 forward-pointer summary block), this backlog file revision bump (v40 → v41) with D-66/67/68 rows appended after D-65, `CLAUDE.md` (last-shipped-narrative bump → PR-C; PR-B demoted to predecessor; Layer 4 row updated to "Step 3 PR-C spec amendments shipped 2026-05-17"; Backlog ref v40 → v41; Next-forward-move points at PR-D context schemas as the next session), new `V5_Implementation_Layer4_Step3_PR_C_Spec_Amendments_Closing_Handoff_v1.md`. **Under the 5-file ceiling (4 files).** Per `V5_Implementation_Layer4_Step3_PR_C_Spec_Amendments_Closing_Handoff_v1.md`.)

Predecessor revisions (most recent first):

- v40 — 2026-05-17 (**Layer 4 implementation Step 2 (PR-B of 2) shipped — `plan_versions` SQL migration per `Layer4_Spec.md` §7.11 verbatim DDL (lifts D-64 §7.2 stub) + `layer4/hashing.py` canonical-JSON encoder + 4 per-entry-point SHA-256 cache-key helpers per §9.1.** Closes Layer 4 §14.3.4 Step 2 (Step 1 was the v38 retro-bundle's C1+C2 amendments; Step 2 PR-A was the v39 payload schema scaffolding; this revision is Step 2 PR-B closing the foundational pre-LLM-integration code shared by all 4 entry points). **No spec amendments this session** — DDL is verbatim §7.11 (implementation-of-spec, not contract change), cache-key formulas land as-spec'd per §9.1; Stop-and-ask trigger #5 did NOT fire (continuing the precedent established by the prompt-body arc's no-amendment sessions). **`init_db.py` `_PG_MIGRATIONS` append** — `plan_versions` table verbatim §7.11 DDL (BIGSERIAL PK, TIMESTAMPTZ created_at, CHECK constraint on created_via ∈ {'plan_create','plan_refresh_t1','plan_refresh_t2','plan_refresh_t3','single_session_synthesize'}, CHECK constraint on pattern ∈ {'A','B'}, denormalized superseded_at + superseded_by_version_id BIGINT REFERENCES plan_versions(id) for revert UX, JSONB notes for model/temperature snapshot + seam_reviews summary) + 2 indexes (plan_versions_user_created_idx ON (user_id, created_at DESC) for recent-plan lookups; plan_versions_user_scope_idx ON (user_id, scope_start_date, scope_end_date) for date-range lookups). Wrapped with IF NOT EXISTS for migration idempotence per existing `_PG_MIGRATIONS` convention. **`layer4/hashing.py` — new pure-function module (~250 lines, 0 I/O, 0 state).** Three layers: (1) `canonical_json(obj)` encoder — recursive `_to_jsonable` walker handles pydantic `BaseModel.model_dump(mode='json')` round-trip + dict + list/tuple (tuple → list) + set/frozenset → sorted list + date/datetime → ISO 8601 + Decimal → str; `json.dumps` with `sort_keys=True, separators=(',', ':'), ensure_ascii=False` for deterministic byte-stable output; (2) bundle-hash helpers — `compute_payload_hash(payload)` (sha256 of canonical_json of any pydantic model or dict; the building block for the per-layer hashes), `compute_layer2c_bundle_hash(locale_to_hash)` (sha256 of canonical_json of {locale_id: layer2c_hash}; used by plan_create_key + race_week_brief_key), `compute_layer2_bundle_canonical_hash(layer2_hashes)` (sha256 of canonical_json of {'a','b','c','d','e' → layer2x_hash or None}; STRICT key validation raises ValueError if the dict lacks exactly the 5 expected keys; null entries preserved so the cache differentiates 'T1 cascade with 2A re-run' from 'T1 cascade with no Layer 2 re-run' per §9.1 narrative), `compute_prior_plan_session_window_hash(sessions)` (sha256 of canonical_json of PlanSession list sorted by (date, session_index_in_day, plan_version_id) — spec calls for (date, session_index_in_day) but I extended to include plan_version_id for safety against the ±7-day context window per §3.2 spanning plan_version_id boundaries (e.g., T3 refresh window crossing a prior plan-revision boundary; two (date, idx) pairs from different plan_version_ids would otherwise rely on Python's stable-sort + input-order determinism)); (3) per-entry-point cache-key builders — `plan_create_key`, `plan_refresh_key`, `single_session_synthesize_key`, `race_week_brief_key` — formulas verbatim from §9.1 with components concatenated via `||` and sha256-hashed. **§9.4 rebinding semantics honored:** `plan_version_id` and `suggestion_id` intentionally absent from ALL key signatures (allocated per call by orchestrator; rebinding on hit). **§9.1 null-handling per spec:** `plan_refresh_key`'s `model_seam_reviewer` (None on Pattern B refresh — T1, T2, T3 intra-phase) + `parsed_intent_hash` (None when no NL intent) both default-collapse None → '' to prevent gratuitous cache misses on Pattern B refreshes; `single_session_synthesize_key`'s `layer2c_locale_hash` (None on quick_equipment mode) collapses the same way. **`layer4/__init__.py`** gains 9 hashing re-exports alongside the 32 payload re-exports — `canonical_json`, `compute_payload_hash`, `compute_layer2c_bundle_hash`, `compute_layer2_bundle_canonical_hash`, `compute_prior_plan_session_window_hash`, `plan_create_key`, `plan_refresh_key`, `race_week_brief_key`, `single_session_synthesize_key`. **`tests/test_layer4_hashing.py` — new, 94 tests, 100% pass (0.07s subset; 0.26s combined with PR-A's 65).** Coverage organized into 7 groups: (1) canonical_json basics + type handling × 10 (sorted keys; no whitespace; date ISO; datetime ISO with tz; Decimal as str; set sorted; tuple to list; nested dict sorted; pydantic model dump; nested pydantic IntensityTarget union); (2) compute_payload_hash × 5 (determinism; field-level differentiation; IntensityTarget union shape differentiation — HRTarget vs PowerTarget at same numeric values flip the hash; dict-form input equivalent to model dump; pace target round-trip); (3) compute_layer2c_bundle_hash × 3 (dict insertion order irrelevant; content differentiation; membership differentiation); (4) compute_layer2_bundle_canonical_hash × 4 (strict {a,b,c,d,e} validation rejects subset + superset; null preservation (T1 with 2A re-run ≠ T1 with no re-run); dict order irrelevant); (5) compute_prior_plan_session_window_hash × 4 (sort-order irrelevance for forward vs reverse input; content differentiation; empty list determinism; same-day different session_index handling); (6) plan_create_key × 18 (determinism; etl_version_set dict order irrelevant; parametrized dependency-on-each-component for all 16 inputs — mutating any input flips the hash); (7) plan_refresh_key × 17 (determinism; None ≡ '' for model_seam_reviewer (Pattern B refresh ↔ '') + parsed_intent_hash; Pattern A seam-reviewer model differentiation; parsed_intent set differentiation; parametrized dependency for 14 inputs); (8) single_session_synthesize_key × 14 (determinism; request dict order irrelevant; None ≡ '' for layer2c_locale_hash (quick_equipment mode); parametrized dependency for 11 inputs); (9) race_week_brief_key × 16 (determinism; parametrized dependency for 15 inputs); (10) cross-helper sanity × 1 (plan_create ≠ race_week_brief at overlapping inputs because the component lists differ structurally). **Architectural deviations on the record:** sort-key extension for `compute_prior_plan_session_window_hash` (added `plan_version_id` as tertiary sort key beyond spec's stated `(date, session_index_in_day)` — Andy notified mid-session; spec-equivalent in common case; safer in cross-version edge); strict key validation in `compute_layer2_bundle_canonical_hash` ({a,b,c,d,e} required, raises on subset/superset — Andy notified; loud over silent stance). **Stop-and-ask trigger #5 retrospective:** did NOT fire — §7.11 DDL is verbatim spec implementation (no contract change); §9.1 cache-key formulas land as-spec'd. The handoff §5 forward-pointer note ("if any §9.1 cache-key formula surfaces a contract gap, route through `/plan` mode per the Andy 2026-05-17 directive") didn't activate. **Branch:** `claude/implement-payload-closing-JfYd7` (harness-pinned for this session — deviates from the handoff §5 recommended `claude/layer4-hashing-5NYk8` per harness override; Andy confirmed leaving the harness-pinned name as-is). **One code commit on the branch + bookkeeping bundled into PR-B per PR-A precedent:** `2a5d221` (substantive PR-B — 4 files: init_db.py + layer4/hashing.py + layer4/__init__.py + tests/test_layer4_hashing.py) + this bookkeeping commit (3 files: this Project_Backlog v39 → v40, CLAUDE.md, closing handoff). **Next forward move (architect-recommended):** Layer 4 implementation Step 3 — deterministic validator harness per `Layer4_Spec.md` §5.4 (now complete including the C1 amendments shipped in v38 — 7 new validator rule rows). Pure-function rule set; per-rule pytest fixtures; cleanly buildable now. Depends on Step 2 (which now closes here). **D-row status flips this revision:** None — D-63 + D-64 still 🟡 implementation pending; Step 2 PR-B is foundational pre-LLM-integration code (the migration + hashing) shared by all 4 entry points; LLM-call integration comes at Step 4. **No new D-rows added** — Layer 4 implementation track entries fold into the existing `§14.3.4` step sequencing without separate D-row scaffolding. **Files touched this revision (4 substantive code + 3 bookkeeping = 7 total):** `init_db.py` (plan_versions migration appended to `_PG_MIGRATIONS` — 19 lines), new `layer4/hashing.py` (~250 lines), `layer4/__init__.py` (9 hashing re-exports + alphabetized in __all__), new `tests/test_layer4_hashing.py` (94 tests), this backlog file revision bump (v39 → v40), `CLAUDE.md` (last-shipped-narrative bump → Step 2 PR-B narrative; PR-A demoted to predecessor; Layer 4 pipeline row updated to "Step 2 PR-A+PR-B landed 2026-05-17"; Backlog ref v39 → v40; authoritative-current-files adds `layer4/hashing.py`; Next-forward-move points at Step 3 validator harness as primary), new `V5_Implementation_Layer4_Step2_PR_B_Migration_Hashing_Closing_Handoff_v1.md`. **Over the 5-file ceiling intentionally** (7 files total — 4 substantive code in 1 commit + 3 bookkeeping in 1 commit; precedented by PR-A's 8-file bundle + PR17's 7-file bundle; Andy explicitly directed the bookkeeping bundle into this PR at end-of-session). Per `V5_Implementation_Layer4_Step2_PR_B_Migration_Hashing_Closing_Handoff_v1.md`.)
- v39 — 2026-05-17 (**Layer 4 implementation Step 2 (PR-A of 2) shipped — payload schema scaffolding per `Layer4_Spec.md` §7 + D1 amendment (typed `IntensityTarget` union + race-week-brief `phase_metadata` strict invariant).** First code PR of the Layer 4 implementation arc, executing §14.3.4 Step 2 (Step 1 closed in v38 via the retro-bundle's C1+C2 amendments). New `layer4/` package + new top-level `tests/` directory. **Architectural choice: pydantic v2 `BaseModel` over stdlib `@dataclass`** — chosen because Layer 4's input boundary is untrusted-JSON parsing from LLM tool-use output + the §7.12 cross-field invariants need cross-model enforcement that's hand-written under stdlib. Spec literal `@dataclass` syntax preserved at field-level — the schema is what's load-bearing, not the decorator. `extra='forbid'` set on every model so synthesizer drift rejects with path-precise error at construction rather than silently dropping fields. **Three judgment calls reviewed mid-session (Andy 2026-05-17 picks all-tight):** (1) **race-week-brief phase_metadata enforcement = strict** ("we don't want our plan falling apart in the final days") — every PlanSession in a `mode == 'race_week_brief'` payload must carry phase_metadata non-None (the override-pass-through value preserved from prior Pattern-A plan); v2 will amend this clause when race-week-brief begins producing new non-override sessions; (2) **session_index_in_day {0, 1} uniqueness = strict** — paired same-day sessions must hold distinct indices to prevent broken plan-view ordering + broken session-logging downstream; consistent with §7.12 line 546 natural-key invariant; (3) **intensity_target / pacing_target = tight typed union now, all v1 disciplines covered** ("future disciplines" framing rejected — coverage for all v1 sports now, not deferred) — original "free-shape per-discipline" dict narrowed to closed v1 set of 9 typed shapes: HRTarget / PowerTarget / PaceTarget / SwimPaceTarget / RPETarget / VerticalRateTarget / StrokeRateTarget / CadenceTarget / ClimbingGradeTarget. **`Layer4_Spec.md` D1 amendment (§7.3 + §7.12 + §7.14):** §7.3 narrowed `intensity_target` from free-shape dict to typed `IntensityTarget` union (9 shapes); new §7.3.1 subsection enumerates each shape with field definitions + value bounds + the disciplines each shape covers (HR = universal endurance; Power = bike/run/skimo/row; Pace = run/hike/paddle/ski; SwimPace = swim; RPE = universal fallback; VerticalRate = skimo/hiking; StrokeRate = swim/paddle/row; Cadence = cycling; ClimbingGrade = outdoor rock with Yosemite Decimal + French Sport + UIAA grade systems per Andy 2026-05-17 climbing-systems pick — V-grade deferred to v2). Range-based convention (low + high; scalar prescriptions = low == high). Single-shape-per-block (multi-metric prescription routed to `instructions: str` in v1; closing the seam tracked in §12.4 tuning candidates). §7.12 race-week-brief phase_metadata C2-amendment clause gains "D1 amendment 2026-05-17 (v1 strict invariant)" addendum naming the strict enforcement. §7.14 `RaceSegment.pacing_target` typed against same `IntensityTarget` union. **Architectural deviations on the record:** pydantic v2 `BaseModel` (not stdlib `@dataclass` or `pydantic.dataclasses.dataclass`); branch off origin/main (`claude/layer4-payload-5NYk8`, deviating from session-pinned `claude/review-design-spec-5NYk8` per Andy explicit OK); `extra='forbid'` model-config default. **Coverage:** `layer4/payload.py` (~700 lines, 20 §7 types + 9 IntensityTarget types + 4 cross-field invariant `@model_validator`s on Layer4Payload + per-class validators on PlanSession (kind XOR, ad-hoc), CardioBlock (interval-set), StrengthExercise (resolution_tier), RacePlan (segments-chronological), and the 8 IntensityTarget range checks); `layer4/__init__.py` (public re-exports — 32 names); `tests/test_layer4_payload.py` (65 tests, 100% pass: happy-path per mode × 5 + PlanSession kind invariants × 8 + CardioBlock interval rules × 2 + StrengthExercise tier rules × 3 + Layer4Payload mode rules × 8 + two-per-day rules × 3 + ValidatorResult + ShapeOverride invariants × 4 + phase_metadata per-mode × 4 [including new race_week_brief strict] + extra='forbid' × 2 + JSON round-trip × 3 + RacePlan chronological × 1 + IntensityTarget happy-path × 11 [9 shapes + 2 climbing-grade variants] + IntensityTarget rejection × 9 + dict-form round-trip × 3 [smart union dispatch verification]); `requirements.txt` (pydantic≥2.5 appended). **§7.12 (structural invariants only) coverage — complete:** PlanSession kind XOR sub-blocks; PlanSession.is_ad_hoc requires ad_hoc_request_payload; CardioBlock.block_kind interval-fields; StrengthExercise.resolution_tier 1/2/3; Layer4Payload.mode per-mode invariants; two-sessions-per-day max-2 + no-strength-strength + no-two-hards + at-least-one-cardio + session_index_in_day {0,1} distinctness; validator_results non-empty + last.accepted; shape_override observation requirement; phase_metadata per-mode (including race_week_brief strict via D1); RacePlan.segments chronological ordering. **Domain rules out of scope here** — §5.4 validator harness (volume bands / ACWR forward projection / injury exclusions / intensity-distribution drift / hard-session-pair recovery / weekly aggregates) is Step 3 implementation; landing in PR-C per §14.3.4 sequencing. **Two code commits on the branch:** `8545820` (initial PR-A scaffolding — 4 files) + `8b32ab7` (D1 amendment tightening — 4 files modified) + this bookkeeping commit (3 files). **Stop-and-ask trigger #5 retrospective (Andy directive 2026-05-17):** the D1 §7.3 + §7.12 + §7.14 spec amendment IS a schema change to an inter-layer contract; should have routed through formal `/plan` mode before the amendment text was drafted (substantive design call was settled via `AskUserQuestion` for the climbing-grade pick + 9-shape list ratification, but the amendment authoring itself wasn't formally `/plan`-gated). Going forward: trigger #5 applies to amendment authoring, not just the substantive design call — even when the design pick is settled, the spec edit goes through `/plan` mode. **Next forward move (Andy-confirmed):** Layer 4 implementation Step 2 (PR-B of 2) — `plan_versions` table SQL migration per §7.11 (lifts D-64 §7.2 stub) appended to `init_db.py` `_PG_MIGRATIONS` + `layer4/hashing.py` canonical-JSON encoder + SHA-256 helpers per §9.1 cache-key formulas (~3 files; well under ceiling). Then Step 3 deterministic validator harness per §5.4 (now-complete C1 rule set). **D-row status flips this revision:** None — D-63 + D-64 still 🟡 implementation pending; Step 2 is foundational pre-LLM-integration code shared by all entry points. **No new D-rows added** — Layer 4 implementation track entries fold into the existing "Recommended next move" sequencing without separate D-row scaffolding (the §14.3.4 step list IS the tracker). **Files touched this revision (3 bookkeeping; PR-A code commits already pushed):** this backlog file revision bump (v38 → v39), `CLAUDE.md` (last-shipped-narrative bump + Layer 4 row updated to "Step 2 PR-A landed; PR-B queued" + Backlog ref v38 → v39 + Next-forward-move points at PR-B), `V5_Implementation_Layer4_Step2_PR_A_Payload_Closing_Handoff_v1.md` (new). **Over the 5-file ceiling intentionally** (PR-A totals 8 files — 5 substantive code/spec edits across 2 commits + 3 bookkeeping; precedented by PR17's 7-file bookkeeping bundle; Andy explicitly directed the bookkeeping bundle into this PR at end-of-session). Per `V5_Implementation_Layer4_Step2_PR_A_Payload_Closing_Handoff_v1.md`.)
- v38 — 2026-05-17 (**Layer 4 spec §14 retrospective shipped — fresh-eyes audit + gut check + implementation readiness gate per `Layer4_Spec.md` §12.6 deferral.** Closes the Layer 4 spec arc: §§1–14 now complete. Per Andy 2026-05-17 architectural call (3 picks): (1) retro shape = (a) **combined audit + gut-check + readiness gate** (rejected pure-gut-check + two-artifact-split options — audit gives the retro teeth; gut-check gives directional read; readiness gate hands off to the now-unblocked implementation track); (2) findings policy = (a) **catalog inline; classify; defer fixes** (rejected mechanically-amend-everything + catalog-only-no-classification — retro WRITES findings + classifies (cosmetic / load-bearing / contract-gap); Andy decides which warrant in-session amendment vs follow-on); (3) implementation readiness gate = (a) **include as §14.3** (rejected defer-to-separate-document — handoff §5.1 promised the implementation track gets "the benefit of the second-mind read"; readiness gate IS that handoff artifact). **`Layer4_Spec.md` §14 added — ~190 lines replacing the 4-paragraph "gut check deferred" stub.** Three subsections: **§14.1 audit findings** (5 cosmetic + 10 load-bearing + 3 contract-gap items catalogued; amendment-round ripple effects traced — D-63 sport-unavailable clean ✅; `intensity_modulated` broadening §13 ripple partial ⚠️ on §13.3 TS-23/TS-24 not mentioning the flag despite broadened trigger applying; race-week-brief 0-amendment session confirmed ✅ but surfaced 2 pre-existing contract gaps the session didn't introduce); **§14.2 gut check** (what's right: discriminated-union PlanSession + per-phase+seam-reviewer + 4-entry-points + closed-set flag taxonomy + shape_override narrow + capped-retry-with-best-effort + cache/determinism contract + 3 amendment rounds shipped cleanly without ripple; risks: sequential per-phase forecloses parallelism + seam-reviewer authority calibration unmeasured + cost projections unmeasured + intensity-distribution defaults are policy not data + §5.4 validator rule list incomplete + intensity_modulated under-emission failure mode + closed-set flag phase-applicability is prompt-only + spec approaching 2100 lines maintenance edge; what might be missing: joint sessions / Layer 5 contract / D-57 / post-race surface / brief-diff renderer / §7.12 phase_metadata override-pass-through gap / §5.4 rule rows / opportunity expansion / seam-reviewer concurrency; best argument against: 4-entry-point complexity multiplier — counter: empirically validated by the prompt-body arc — the 3 different file-scope picks per prompt body (unified single-session; two-file T1/T2; unified race-week-brief) would collapse into one mega-prompt rats-nest under a unified-with-mode-discriminator architecture); **§14.3 implementation readiness gate** (subsections: stable contracts ready to build; ambiguous-but-tolerable items; what implementation should validate early; recommended 8-step sequencing with §5.4+§7.12 amendment session as Step 1; observability requirements; pre-implementation gate checklist). **3 contract gaps catalogued** (stop-and-ask trigger #5 candidates): **C1** §5.4 validator rule table missing ~7 rules referenced by prompt bodies (5 from race-week-brief: `taper_phase_intent_violation_*`, `kit_manifest_inputs_incomplete`, `race_plan_segments_unordered_*`, `fueling_strategy_2e_tier_mismatch_*`, `contingency_anchor_category_missing_*`; 2 from per-phase: `phase_date_out_of_range_*`, daily-window-fit) — blocks implementation Step 3 validator harness; **C2** §7.12 `phase_metadata` override-pass-through semantics undefined (race-week-brief literally contradicts §7.12 by preserving pre-existing Pattern-A-produced `phase_metadata` on its Pattern-B output; prompt's interpretation is reasonable but spec doesn't sanction it) — blocks Step 4e race-week-brief integration; **C3** `Layer4ShapeInfeasibleError` orchestrator routing (3D HITL vs inline error) — explicitly orchestrator-side per §12.3 + §10.2; pick at implementation-track kickoff, not a Layer 4 spec gap. **Retro-bundled in-session amendments (Andy mid-session pick to reverse Pick 2 defer-fixes posture for the two contract gaps + three load-bearing findings):** **C1** — `Layer4_Spec.md` §5.4 validator rule table gains 7 new rows: `taper_phase_intent_violation_*` (race-week-brief Taper-session overrides; blocker), `kit_manifest_inputs_incomplete` (race-week-brief multi-day soft warning), `race_plan_segments_unordered_*` (race-week-brief multi-day blocker), `fueling_strategy_2e_tier_mismatch_*` (race-week-brief multi-day blocker), `contingency_anchor_category_missing_*` (race-week-brief warning), `phase_date_out_of_range_*` (plan_create + Pattern-A T3 blocker; promoted from `Layer4_PerPhase_v1.md` §11 row 14 prompt-only enforcement), `daily_window_fit_*` (all entry points emitting PlanSession blocker; promoted from `Layer4_PerPhase_v1.md` §11 row 12). **C2** — §7.12 `PlanSession.phase_metadata` schema rule amended with race-week-brief override-pass-through clause: when race-week-brief modifies pre-existing Taper-phase sessions from `prior_plan_session_window`, the modified session's `phase_metadata` is preserved verbatim from the prior-plan session (which carries the original `plan_create` or Pattern-A `plan_refresh` metadata); race-week-brief does NOT produce new PlanSession rows in v1; if it did, those new rows would follow the Pattern-B default of `phase_metadata=None`. **B2** — §13.3 plan_refresh TS-23 / TS-24 / TS-25 expected-output language updated to reference `intensity_modulated` emission per the broadened §8.6 trigger covering refresh-path modulation against `parsed_intent` direction or 3A signals. TS-22 + TS-32 already referenced the flag pre-broadening; TS-26 / TS-31 are 3B-shape-shift / no-intent cases where the flag doesn't naturally apply. **B9** — §11.5 cumulative ceilings gains race-event cumulative line item: race_week_brief daily regenerations across the race-week (midnight-UTC cache invalidation per §9.3) add a per-race-event burst pattern (~$2.50/race-event for ~14 daily fires; ~$0.18/day well under the $0.50 daily soft cap but ~30% of the $8 monthly soft cap from race-week-brief alone). **B10** — §11.2 token budget table gains extended_thinking budget per-prompt-body table: SeamReviewer ~2000; SingleSession ~3500; RefreshT1 ~3000; RefreshT2 ~4500; PerPhase ~5000 per phase; RaceWeekBrief ~5500 (highest in pipeline); plus the note that Sonnet 4.6 bills extended thinking against output cost (a 4-phase plan_create no-retry case carries ~26000 extended-thinking output tokens on top of the ~14400 structured output ≈ ~40400 total output-billed tokens). **All retro-flagged contract gaps now resolved.** **Recommended next session:** Layer 4 implementation Step 1 + 2 — `plan_versions` table migration per `Layer4_Spec.md` §7.11 + payload schema scaffolding per §7. No remaining spec amendments block implementation. **D-row status flips this revision:** None. **No new D-rows added** — §14 retro + retro-bundled amendments are spec-track close-out, not backlog D-row work. **Files touched (1 substantive spec edit covering §14 + 5 inline amendment edits + 2 bookkeeping + handoff = 4 total):** `Layer4_Spec.md` (§14 replaced ~190 net lines + C1 7-row §5.4 addition + C2 §7.12 schema rule amendment + B2 3 TS-row expected-output edits + B9 §11.5 paragraph add + B10 §11.2 extended-thinking table add ≈ ~270 net new lines total across all in-session edits), `CLAUDE.md` (last-shipped-narrative bump → "§14 retro + retro-bundled C1+C2+B2+B9+B10 amendments shipped — all retro-flagged contract gaps resolved" + Layer 4 row updated to reflect 4 amendment rounds total + Project_Backlog v37 → v38 + Next-forward-move rewritten — Layer 4 implementation Steps 1+2 now the architect-recommended next move since no remaining spec amendments block implementation), this backlog file revision bump (v37 → v38), §14 retrospective closing handoff (covers both the retro authoring AND the in-session bundle application). **Under the 5-file ceiling** (4 files total — same envelope as the race-week-brief session). **The Layer 4 spec arc is now COMPLETE §§1–14 with all retro-flagged contract gaps resolved.** Spec line count: ~2000 (1928 after §14 + ~70 from retro-bundled C1+C2+B2+B9+B10 surgical edits). Per `V5_Design_Layer4_Spec_Section14_Retrospective_Closing_Handoff_v1.md`.)
**Predecessor revisions:**
- v37 — 2026-05-17 (**Layer 4 race-week-brief synthesizer prompt body shipped — fifth and last of 5 in the Layer 4 prompt-body arc; THE LAYER 4 PROMPT-BODY ARC IS NOW COMPLETE.** Per Andy 2026-05-17 architectural call: (1) file scope = (a) **unified file** (`aidstation-sources/prompts/Layer4_RaceWeekBrief_v1.md`) with a Mustache-style multi-day branch at the RacePlan emission slot in §6, NOT a two-file split — coaching surface is shared across single-day + multi-day; multi-day extension is structurally additive (segment + transition + night-section reasoning) not conceptually different; carries the single-session unified-prompt precedent (rejected T1/T2's two-file precedent); (2) pacing strategy depth = (a) **hybrid** — hard numeric targets (HR/pace/power) where 3A `data_density ∈ {rich, moderate}` AND discipline supports it natively; RPE + qualitative heuristic where data is thin OR conditions degrade hard targets (expedition AR night sections, technical terrain, multi-day fatigue past hour N); per-segment `pacing_target` uses whichever measure the discipline supports; (3) contingency depth = (a) **mixed** — short anchor table of must-have categories per race_format (any race: GI / hydration / mechanical-or-gear-failure; AR + ultra: nav / sleep-dep / weather; stage races: between-stage recovery; multi-day: cumulative fatigue + crew-pacing-mismatch) + LLM expands within each + adds athlete-specific (2D injury re-aggravation per Andy's wrist; 3A `weak_links`); carries the anchor-table precedent from per-phase (deload cadence per mode + Taper-length per race format). **`aidstation-sources/prompts/Layer4_RaceWeekBrief_v1.md` — 955 lines, 14 H2 sections + Source-decisions header.** Source decisions D1–D13 in file header: D1 tool-use (`record_race_week_brief` with three arguments — `taper_session_overrides` + `race_week_brief` + optional `race_plan`); D2 extended thinking ~5500 tokens (highest in pipeline — broadest reasoning surface: multi-locale × multi-segment × multi-hour × athlete-specific; sits above per-phase's 5000); D3 full payloads verbatim for all 5 Layer 2 payloads + 3A + 3B + Taper window + event metadata; D4 full verbatim Taper window (≤21 days × ≤2 sessions/day ≈ ≤42 sessions; ~1500–2500 tokens; Taper is load-bearing context); D5 hybrid pacing depth (Andy's Pick 2); D6 mixed contingency depth (Andy's Pick 3); D7 unified file with multi-day branch (Andy's Pick 1); D8 closed 2-flag LLM-emittable enum (`intensity_modulated` per the §8.6 broadened trigger + `discipline_specific_intensity`) + 5 spec-auto Taper flags stamped orchestrator-side per `Layer4_Spec.md` §8.5 (`race_rehearsal`, `fueling_practice`, `kit_check`, `pacing_lock`, `pre_race_taper`) — the prompt is the upstream half (produces session content; orchestrator stamps); D9 hybrid kit_manifest resolution (layer0.equipment_items canonical preferred + free-text fallback flagged with `data_gap` observation per `Layer4_Spec.md` §4.5 row 7); D10 hybrid RuleFailure retry context; D11 tight per-field length caps matched to max_tokens=6000 budget; D12 direct/no-platitudes coaching voice with explicit forbidden-phrasing list for `mental_prep_cues` (no "you've got this", "crush it", "trust the process" generic, "race day magic", "mind over matter", emoji); D13 file location `aidstation-sources/prompts/Layer4_RaceWeekBrief_v1.md` per the `prompts/` subdir convention. **No spec amendments this session** — race-week-brief consumes the existing v1 spec contract without surfacing any contract gap (broke the spec-amendment precedent set by single-session §10.4 + T1/T2 §8.6/§8.7). Companion contract sections honored (`Layer4_Spec.md`): §3.4 (call signature — full payload set; `max_tokens=6000`), §3.5 (errors — `race_week_brief_requires_event_mode`), §4.5 (input validation rules — event mode required, event within 14d window, event date in future, 2E payload required, event locale resolves, race_format set, kit data prerequisites soft-warning per row 7), §5.1 (pattern routing — B always for both single-day + multi-day; Pattern A's per-phase machinery unnecessary for the ≤ 1-phase Taper window), §5.3 (Pattern B algorithm), §5.4 (deterministic validator — race-week-brief scope: Taper-session overrides against Taper phase intent; kit_manifest items against layer0 equipment registry; RacePlan.segments chronological ordering when multi-day; fueling-strategy macro ranges against 2E tier; contingency anchor-category coverage per D6), §5.5 (capped retry, cap=2), §7.2/§7.3/§7.4 (PlanSession discriminated union + sub-blocks — Taper session overrides), §7.5 (SessionPhaseMetadata — Taper-phase metadata since the brief operates within an existing Taper phase; differs from single-session + T1/T2 which set phase_metadata=None per §7.12), §7.13 (RaceWeekBrief schema — always emitted), §7.14 (RacePlan schema — multi-day events only: expedition_ar / stage_race / multi_day_ultra), §8.5 (Taper-phase spec-auto coaching flags — orchestrator stamps), §8.6 (`intensity_modulated` + `discipline_specific_intensity` LLM-emittable), §8.7 (call-level observations — `opportunity` LLM-emitted exception; `data_gap` orchestrator-emitted on §4.5 soft-fail), §9.1 (cache key — full payload set + Taper window hash; midnight-UTC invalidation per §9.3 since output is `days_to_event`-anchored — daily regenerations are expected), §11.1 (latency p50 ~8s single-day / p50 ~12s multi-day; p95 ~12s / ~18s respectively), §11.2 (token budget — ~4500–6500 input + ~1500–5000 output depending on race_format), §11.3 (cost — ~$0.08–0.22 typical depending on race_format; ~$0.35 worst-case multi-day with cap-hit retries; cumulative ~$2.50 per race-event for 14 daily regenerations). **20 PSS-RWB-prefix v1 test scenarios** in §12 covering: Andy's Pocket Gopher Extreme 2026 expedition AR baseline; single-day road marathon; single-day trail 50K; multi-day 3-stage race; 24h multi-day ultra; 3A overtrained state pullback; active illness pullback; missing 2C equipment view; 4 validator retry scenarios (taper-intent-violation, segments-unordered, 2E-fueling-mismatch, anchor-category-missing); cap-hit best-effort; forecast unavailable; day-before manual fire; goal_viability=unrealistic; missing mandatory race-rules gear; thin prior Taper window; 13-segments schema-cap; 2D wrist injury contingency; very-sparse data density RPE fallback. **D-row status flips this revision:** None. **No new D-rows added** — prompt-body sessions are spec-track follow-on. **Files touched (1 substantive prompt body + 2 bookkeeping + handoff = 4 total):** new `aidstation-sources/prompts/Layer4_RaceWeekBrief_v1.md` (race-week-brief prompt body), `CLAUDE.md` (last-shipped-narrative bump from "4 of 5 prompts shipped" → "5 of 5 prompts shipped — ARC COMPLETE" + authoritative-current-files adds the RaceWeekBrief file + Project_Backlog v36 → v37 + Next-forward-move rewritten to drop "race-week-brief" candidate and reframe with arc-complete framing), this backlog file revision bump (v36 → v37), race-week-brief closing handoff. **Under the 5-file ceiling** (4 files total). **The Layer 4 prompt-body arc is now COMPLETE** — 5 of 5 prompt bodies shipped (1: SeamReviewer 2026-05-16; 2: PerPhase 2026-05-16; 3: SingleSession 2026-05-16; 4+5: RefreshT1 + RefreshT2 2026-05-17; 6 [final]: RaceWeekBrief 2026-05-17). **All four Layer 4 entry points now have prompt bodies ready for implementation:** `plan_create` (Pattern A — PerPhase + SeamReviewer); `plan_refresh` T1/T2 (Pattern B — RefreshT1 + RefreshT2); `single_session_synthesize` (Pattern B — SingleSession); `race_week_brief` (Pattern B — RaceWeekBrief). T3 cross-phase Pattern A is composed from per-phase + seam-reviewer (no separate prompt); T3-intra-phase Pattern B routing decision (own prompt vs subsume T2 vs defer) is implementation-time per `Layer4_Spec.md` §5.1 + `Layer4_RefreshT2_v1.md` §14.1 open item. Per Andy 2026-05-17: (a) unified file (file-scope pick), (a) hybrid pacing depth (pacing pick), (a) mixed contingency depth (contingency pick). Per `V5_Design_Layer4_Prompts_RaceWeekBrief_Closing_Handoff_v1.md`.)
- v36 — 2026-05-17 (**Layer 4 per-tier T1/T2 refresh synthesizer prompt bodies shipped — fourth + fourth-and-a-half of 5 in the Layer 4 prompt-body arc; coupled `Layer4_Spec.md` §8.6 + §8.7 amendment broadening the `intensity_modulated` flag trigger from D-63-only to also cover `plan_refresh` paths.** Per Andy 2026-05-17 architectural call: (b) two separate prompt files per tier — `Layer4_RefreshT1_v1.md` + `Layer4_RefreshT2_v1.md`, NOT a unified file (rejected option (a) which was the architect's recommendation; rationale: two prompts drift independently as they tune; T2's longer scope has distinct §9 guidance that doesn't fit cleanly under conditional logic with T1); (c) strong-bias toward `parsed_intent` (the athlete's NL signals dominate 3A objective signals on prescription direction; 3A grounds the magnitude; hard-safety constraints — active injuries, equipment, schedule — never overridden); (a) match-surrounding-shape continuity unless refresh trigger justifies a shift (refresh is minimally disruptive by default; the refresh-window-after sessions are the load-bearing continuity constraint that the refresh-window output must hand off to cleanly). **`aidstation-sources/prompts/Layer4_RefreshT1_v1.md` — ~700 lines, 14 H2 sections.** Source decisions D1–D11 in file header: D1 tool-use (`record_refresh_sessions`); D2 extended thinking ~3000 tokens (sits between single-session's 3500 and per-phase's 5000 — T1 has fewer combinatorics than per-phase but more continuity-checking than single-session); D3 full payloads verbatim for 3A + 2A + 2D + 1 + request + `parsed_intent`; D4 hybrid prior-plan-window rendering (refresh-window-prior as summary table; refresh-window-after verbatim — the latter is the continuity hand-off constraint); D5 strong-bias-toward-intent (Andy's Pick 2); D6 full per-phase + cross-phase LLM-emittable coaching-flag set (7 flags: `technique_emphasis`, `long_slow_distance`, `weak_link_targeted`, `overreach_test`, `discipline_specific_intensity`, `race_pace_specific`, `intensity_modulated`); D7 match-surrounding-shape continuity (Andy's Pick 3); D8 hybrid RuleFailure retry context; D9 tight schema length caps (240/200/120/240 chars matching single-session); D10 NL `raw_text` always rendered as primary signal even when `parser_confidence='low'`; D11 file location per `prompts/` subdir convention. **`aidstation-sources/prompts/Layer4_RefreshT2_v1.md` — ~750 lines, 14 H2 sections.** Same decision-table structure (D1–D11). Differences from T1: D2 extended thinking ~4500 tokens (higher — T2's full-week scope has combinatorial weekly placement decisions; sits closer to per-phase); D3 adds 3B + (optionally via intent triggers) 2B/2C/2E to the input set; D4 same hybrid rendering at 7-day scope; D9 same caps but `max_tokens=4000` (up from T1's 2000) for up to 14 sessions output. T2-specific guidance in §9: weekly volume band + intensity distribution become load-bearing validator constraints (the natural granularity for `volume_band_*` + `intensity_dist_*` rules); LSD anchor as the load-bearing weekly decision in Base/Build; deload week recognition via 3B's `deload_cadence_anchor`; overreach week handling with athlete-pullback path. Cross-phase week handling (refresh spans Build→Peak boundary mid-week) uses per-day phase mapping + weighted-blend distribution targets; Pattern A is NOT activated (T2 stays Pattern B per §5.1 routing regardless of phase boundary). **Paired spec amendment this session (`Layer4_Spec.md`):** §8.6 cross-phase flags table — `intensity_modulated` trigger row broadened from "Synthesizer modulated athlete's picked D-63 intensity per §6.2 of D-63" to "Synthesizer modulated session intensity from what natural periodization-shape + adjacent-session continuity would call for, due to athlete signal — covers (a) D-63 single-session modulation per §6.2 of D-63, AND (b) `plan_refresh` T1/T2 modulation against `parsed_intent` direction or 3A signals per `Layer4_RefreshT1_v1.md` §8 / `Layer4_RefreshT2_v1.md` §8" — flag now fires on whole-week modulations (e.g., T2 pulled back due to sickness, every session in the refresh window emits the flag); §8.7 spec-auto observation row updated to reference the broadened §8.6 trigger. Companion contract sections honored (`Layer4_Spec.md`): §3.2 (call signature — `tier` + `refresh_scope_*` + `prior_plan_session_window` + `parsed_intent`), §4.3 (input validation — `tier_scope_mismatch` T1 ≤ 3 days / T2 ≤ 9 days, `plan_version_id_parent` exists), §5.1 (pattern routing — T1 → B always; T2 → B always per §5.1 even on cross-phase weeks; cross-phase Pattern A is reserved for T3), §5.3 (Pattern B algorithm — context build, single LLM call, parse, validator, capped retry, payload compose), §5.4 (deterministic validator — full rule set on refresh; weekly-aggregate rules are load-bearing at T2 scope), §5.5 (capped retry semantics, cap=2 default for Pattern B), §7.2/§7.3/§7.4 (`PlanSession` discriminated union + `CardioBlock` + `StrengthExercise`), §7.5 (`SessionPhaseMetadata` — None on Pattern B refresh per §7.12 schema rule), §8.2–8.6 (full coaching flag taxonomy — LLM-emitted vs spec-auto split honored), §8.6 broadened this session, §8.7 (call-level observations; `opportunity` LLM-emitted exception), §11.1 / §11.2 / §11.3 (latency / token / cost — T1: ~5s / ~4500 input + ~1500 output / ~$0.05 typical; T2: ~7s / ~6500 input + ~3000 output / ~$0.12 typical). Token + cost: T1 ~$0.05 per call typical, ~$0.10 worst-case with cap-hit retries; T2 ~$0.12 per call typical, ~$0.22 worst-case. **15 PSS-T1-prefix + 18 PSS-T2-prefix v1 test scenarios** in each file's §12 covering: empty NL note baseline; fatigue/sickness/motivation signal cases; injury parser-miss case (raw_text wins over structured flags); travel locale shift; refresh-window-after long-ride continuity protection; deload + overreach week cadence handling; cross-phase week (T2 only); validator retry pass + cap-hit; end-of-plan edge case; periodization mode shift (T2 only); empty refresh-window-after; missing phase metadata fallback; race-rehearsal-imminent Taper modulation. **D-row status flips this revision:** None. **No new D-rows added** — prompt-body sessions are spec-track follow-on, not backlog D-rows. **Files touched (2 substantive prompt bodies + 1 substantive spec amendment + 2 bookkeeping + handoff = 6 total):** new `aidstation-sources/prompts/Layer4_RefreshT1_v1.md` (T1 refresh prompt body), new `aidstation-sources/prompts/Layer4_RefreshT2_v1.md` (T2 refresh prompt body), `Layer4_Spec.md` (§8.6 + §8.7 surgical broaden), `CLAUDE.md` (last-shipped-narrative bump from "3 of 5 prompts shipped" → "4 of 5 prompts shipped — per-tier T1 + T2; coupled §8.6 spec amendment broadening `intensity_modulated`" + authoritative-current-files adds RefreshT1 + RefreshT2 files + Project_Backlog v35 → v36 + Next-forward-move rewritten to flag 1 remaining prompt body queued — race-week-brief — and update Layer 4 implementation track note that D-64 LLM-call layer for T1/T2 can now land), this backlog file revision bump (v35 → v36), refresh-T1T2 closing handoff. 5-file ceiling broken intentionally (6 files; precedented — single-session was also 6). Per Andy 2026-05-17: (b) two files (file-scope pick), (c) strong-bias toward intent (NL weighting pick), (a) match-surrounding-shape (continuity pick). Per `V5_Design_Layer4_Prompts_RefreshT1T2_Closing_Handoff_v1.md`.)
- v35 — 2026-05-16 (**PR19 — retire legacy `LOCALES` external consumers (PR18 follow-on Item E).** Ships the PR18 closing handoff §5.1 deferred item — drops the hardcoded 4-bucket `LOCALES = ['home','hotel','partner','airport']` literal from `routes/coaching.py` + the `from routes.locales import LOCALES` import from `routes/references.py`; both now consume a shared `athlete_locale_choices(db, uid)` helper that returns ordered `{slug, label, bucket}` dicts representing the athlete's full locale list (4 legacy slots always-present + athlete-created rows alpha by slug). **`routes/locales.py`** gains the helper + a `CATEGORY_TO_WHERE_AVAILABLE_BUCKET` dict mapping the 10 manual categories → Layer 0 `exercise_inventory.where_available` buckets so the /references filter can match athlete locales against the gym-centric 4-bucket Layer 0 taxonomy: `home_gym` + `other_residence` → `home`; `hotel_gym` → `hotel`; `commercial_chain_gym` + `independent_gym` + `climbing_gym_chain` + `climbing_gym_indie` + `pool_indoor` + `pool_outdoor` → `partner`; `outdoor_park` → `''` (no Layer 0 analog — parks match no exercises until the park-tags follow-up lands per PR18 §5.2). Internal `LOCALES` literal in `routes/locales.py` stays — used heavily for legacy-card auto-render, deletability gate, dup-check helper. **`routes/coaching.py`** drops the local `LOCALES` literal; new `TRIP_LOCALE_TYPES = ('home','hotel','partner','airport')` (a separate constant: trip-environment-type taxonomy used by `plan_travel.locale` + Claude prompt construction, NOT the athlete-saved-locale list); both `generate()` + `review(plan_id)` GET handlers pass `locales=athlete_locale_choices(db, uid)` + `trip_locale_types=TRIP_LOCALE_TYPES` to templates; both POST handlers replace `if locale not in LOCALES: locale = 'home'` silent rewrite with strict validation against the athlete's actual locale slug set — on miss, flash "Select a valid current location." + redirect to GET (per Andy's form-level guard pick: no silent default fallback). `chat()` JSON endpoint at line 537 unchanged (`data.get('locale', 'home')` — API contract preserved; not a form). **`routes/references.py`** drops the `LOCALES` import; `exercises()` route queries `athlete_locale_choices` once at top + sanitizes URL `?locale=` param against valid slug set; filter logic at line 79 changed from raw-slug intersection against the exercise `where_available` CSV to bucket-mapped intersection — each selected athlete-slug maps to its `bucket` value via the choice dict, the union of mapped buckets intersects against `where_available`. Custom locales with `bucket=''` (e.g., outdoor_park) contribute nothing to the filter and correctly match 0 exercises. **Templates touched:** `templates/coaching/generate.html` (primary "Current Location" dropdown iterates `locales` as dicts `{c.slug, c.label}`; trip-locale dropdown iterates the new `trip_locale_types` 4-bucket constant); `templates/coaching/review.html` (same — primary + locale-row trip dropdown); `templates/references/exercises.html` (checkbox loop iterates dicts; badge-color logic at line 56 unchanged — it correctly colors Layer 0 `where_available` bucket values which remain the 4 legacy slugs). **Form-level guard decision (Andy 2026-05-16):** picked option c — no implicit `'home'` fallback on missing/invalid POST locale; flash + redirect to GET surfaces the error to the athlete instead of silently rewriting. The "zero locales" hard-fail branch doesn't fire in practice since legacy slots auto-render in the choice list. **References filter decision (Andy 2026-05-16):** picked option b — show athlete's locales in the filter UI, map custom slugs → Layer 0 buckets via category. **D-row status flips this revision:** None. **No new D-rows added** — PR19 is a v5 implementation follow-on cleanup, not a backlog D-row of its own. **Files touched (6 substantive code + 4 bookkeeping = 10):** `routes/locales.py` (additive helper + mapping dict, ~60 lines), `routes/coaching.py` (LOCALES drop + TRIP_LOCALE_TYPES + 4 surgical edits across generate + review handlers), `routes/references.py` (import swap + filter rewrite, ~10 lines net), `templates/coaching/generate.html` (2 dropdown updates), `templates/coaching/review.html` (2 dropdown updates), `templates/references/exercises.html` (checkbox-loop update), `CLAUDE.md` (last-shipped narrative bumped to PR19 + Backlog ref v34 → v35 + Next-forward-move drops Item E candidate), this backlog file revision bump (v34 → v35), `PR_Verification_Status.md` (new PR19 section with 11 testable §5.0 steps + aggregate refresh), `V5_Implementation_PR19_LOCALES_Retire_Closing_Handoff_v1.md` (new). **5-file ceiling status:** 6 substantive code files (1 over ceiling; 3 of them are 1–6-line template surgical edits — primary surface area is `routes/coaching.py` refactor + `routes/locales.py` additive helper). Per Andy 2026-05-16: form-level guard for default-fallback + athlete-locale-aware references filter with category→bucket mapping. Per `V5_Implementation_PR19_LOCALES_Retire_Closing_Handoff_v1.md`.)
- v34 — 2026-05-16 (**PR18 — locale-CRUD bundle shipped (PR11 follow-on): items A address-display + B 🔴 refresh-broken-on-rename fix + C duplicate-mapbox_id detection + D delete with privacy split rule + outdoor_park reclassification into `SHARED_PROFILE_CATEGORIES` + `home_gym`/`other_residence` label tweaks.** Ships the four bundle items captured during the PR11 verification walk per the PR11-follow-on handoff §3 + §4 mechanical scope. **`mapbox_client.py`** gains `retrieve(mapbox_id, session_token=None)` + `_request_retrieve` helper hitting `GET /search/searchbox/v1/retrieve/{mapbox_id}` (one-shot UUID session token generated per refresh; same `MapboxNoResults` / `MapboxAPIError` / `MapboxTokenMissing` hierarchy as the forward endpoint; `_normalize_feature` reused — `mapbox_id` opaque, `text` from `name_preferred`/`name`, `place_name` from `full_address`/`place_formatted`, `category` from `poi_category` list joined). **`routes/locales.py`** gets: `_display_address(profile_row)` helper (JSON-parses `place_payload` → `properties.full_address` / `properties.place_formatted` / empty; defensive on absent + malformed + non-dict); `_existing_locale_by_mapbox_id(db, uid, mapbox_id, exclude_slug=None)` dup helper for item C; new `delete_locale` route at `POST /locales/<locale>/delete` (FK CASCADE clears `locale_equipment` + `locale_equipment_overrides` + `locale_toggle_overrides`; residential split rule additionally drops the linked `gym_profiles` row when `category IN ('home_gym','other_residence')` AND `gym_profile_id IS NOT NULL` AND `created_by_user_id = uid` — defensive since residential categories under the current taxonomy don't enter the shared-profile flow at all; legacy enum slugs guarded with a warning flash); `_save_mapbox_anchored` dup-check on both INSERT + upgrade paths (warning flash "You already have a locale at this address (X). Edit it instead." + redirect to `edit_profile` on the existing dup; upgrade path uses `exclude_slug=upgrade_slug` so it doesn't self-trigger); `nearby_instances` POST filters selections against existing-mapbox_id rows for this user and flashes "Skipped N already-saved location(s)" info; `refresh_from_mapbox` Phase 1 rewritten to call `mapbox_client.retrieve(stored_mapbox_id, session_token=uuid.uuid4().hex)` instead of `search_nearby(locale_name, lng, lat)`+match-by-id — eliminates the renamed-locale failure mode entirely (item B 🔴 fix); the now-unused `stored_lng` / `stored_lat` reads dropped; `MapboxNoResults` from `/retrieve` keeps the existing "Mapbox no longer returns this exact place. Edit the locale to relink." warning + edit-screen redirect; `SHARED_PROFILE_CATEGORIES` gains `'outdoor_park'` (8 total — privacy boundary becomes residence-vs-public, cleanly; only `home_gym` + `other_residence` stay private); `MANUAL_CATEGORIES` label swaps `home_gym` → `Home (primary residence)` and `other_residence` → `Other residence (in-laws / friend / AirBnB)` (enum values unchanged); `_is_shared_profile_locale` docstring updated. **`templates/locales/list.html`** renders the address as a muted small-text caption under each non-legacy card title (gated on non-empty `display_addresses[locale]`). **`templates/locales/form.html`** renders the address as a muted small-text caption under the header (gated on non-empty `display_address`) + adds a bottom Delete button (browser `confirm()` per Andy 2026-05-16 pick) gated on `is_deletable` (legacy enums hidden — `locale not in LOCALES`). **PR_Verification_Status** gains a new PR18 section with 12 testable §5.0 steps for the post-merge walk: A address visible across 3+ locales; B refresh a renamed locale; C dup-check on `_save_mapbox_anchored` INSERT + upgrade + nearby_instances; D delete a chain locale (`gym_profiles` stays) + delete a home_gym locale (`gym_profiles` goes); outdoor_park inherit/override UI; label rendering; legacy-enum delete-button absence; FK CASCADE verification on delete; legacy-enum delete-route rejection; cross-user dup-detection scoping (N/A at N=1). **D-row status flips this revision:** None. **No new D-rows added** — PR18 is a v5 implementation follow-on bundle, not a backlog D-row of its own. **Files touched (4 substantive code + 4 bookkeeping = 8):** `mapbox_client.py` (retrieve + helper), `routes/locales.py` (substantive multi-edit per above), `templates/locales/list.html` (address render), `templates/locales/form.html` (address + delete), `CLAUDE.md` (new last-shipped narrative + Backlog ref v33 → v34 + Next-forward-move adds Item E + park-tags follow-ons), this backlog file revision bump (v33 → v34), `PR_Verification_Status.md` (new PR18 section + aggregate refresh), `V5_Implementation_PR18_Locale_CRUD_Bundle_Closing_Handoff_v1.md`. **5-file ceiling status:** 4 substantive code files (well under ceiling); the 4 bookkeeping files are mandatory per Rules #11/#12/#13 + the per-implementation-PR cadence. **Item E (retire legacy `LOCALES` enum cards on `/locales`)** stays separate per handoff §3.5 — hard-coupled to `routes/coaching.py:22` literal + `routes/references.py:5` import + `templates/references/exercises.html:56` cosmetic badge; needs its own focused refactor PR. **Park-specific tag taxonomy** is flagged in the closing handoff §6 as a known follow-up — `EQUIPMENT_CATEGORIES` is gym-centric, so outdoor_park shared profiles render irrelevant gym checkboxes; defer the category-aware tag taxonomy until park usage exercises the seam. Per Andy 2026-05-16: PR18 + browser-confirm delete UX pick. Per `V5_Implementation_PR18_Locale_CRUD_Bundle_Closing_Handoff_v1.md`.)
- v33 — 2026-05-16 (**Layer 4 single-session synthesizer (D-63) prompt body shipped — third of 5 in the Layer 4 prompt-body arc; coupled `Layer4_Spec.md` §10.4 amendment retiring rest-shape repurposing for sport-unavailable case** (Andy 2026-05-16 architectural call — convention "prompt-body sessions don't touch spec" broken intentionally per Andy's option-2 pick to fold the §10.4 fix into this session rather than spin a separate spec amendment session). **`aidstation-sources/prompts/Layer4_SingleSession_v1.md` — 589 lines, 14 H2 sections + 13 H3+ file-structure subsections.** Source decisions D1–D12 captured in the file header: D1 tool-use (`record_single_session`); D2 extended thinking ON ~3500 tokens (mid-defensive between seam-reviewer's 2000 + per-phase's 5000); D3 full payloads verbatim (1 + 2A + 2D + 3A + request — ~3500 input budget fits comfortably without trimming); D4 hybrid prior-session-window rendering (summary stats: ACWR / 7-day load / last-hard-session date+sport / intensity-distribution + last 7 days verbatim as a date-sport-duration-intensity-completion table); D5 closed-set 3-flag D-63-only enum: `intensity_modulated` (cross-phase D-63-only per §8.6) + `technique_emphasis` + `discipline_specific_intensity` — phase-tied flags (`long_slow_distance`, `overreach_test`, `weak_link_targeted`, `race_pace_specific`) excluded since D-63 has no phase context; D6 judgment + anchor signals + must-explain in `session_notes` when modulating (anchor signals: ACWR 7d/28d thresholds, last-hard-session lookback windows, same-sport vs cross-sport amplification, 7-day-vs-28-day load delta); **D7 pre-LLM precondition (γ) — sport-unavailable becomes a §4.4 input validation raise + D-63 §6.3 caller-side handling; the rest-shape `kind='rest'` + `rest_reason='athlete_unavailable'` repurposing per the previous §10.4 contract is retired this session.** Rationale: hijacking the rest-shape for system-error semantics conflates coaching artifacts (rest days affect training summaries, weekly load calcs, ACWR, calendar rendering) with system semantics; D-63 caller pre-checks sport availability and returns the unavailable response directly to the frontend without invoking Layer 4. The LLM is never invoked on impossible requests. D8 hybrid RuleFailure retry context (`rule_name + severity + detail + affected_session_id + suggested_constraint`); D9 tight schema length caps (240 chars `session_notes` / 200 chars `coaching_intent` / 120 chars `load_prescription` / 240 chars `instructions`) matched to the 1500 `max_tokens` budget; D10 verbatim-respect-on-`notes_for_synthesizer` unless safety blocker + overreach warning + three-tier interaction with D6 (Tier 1 full-honor / Tier 2 honor-with-warning / Tier 3 modulate-with-explanation — anchor thresholds in §9.1); D11 unified prompt with conditional equipment block (Mustache-style `{{#request.locale_slug}}…{{/}}` branch at the equipment-injection slot — same coaching logic for both locale + quick_equipment paths; only the equipment input shape differs); D12 file at `aidstation-sources/prompts/Layer4_SingleSession_v1.md` per `prompts/` subdir convention. **Paired spec amendments this session (`Layer4_Spec.md` + `OnDemand_Workout_D63_Design_v1.md`):** Layer 4 §3.5 (new `request_sport_unavailable_at_locale` error code added to `Layer4InputError` bullet); §4.4 (new precondition row "Sport equipment-resolvable at request locale or via `quick_equipment`" — caller pre-check expected, Layer 4 raises defensively if missed); §10.4 (sport-unavailable row 1 rewritten — rest-shape repurposing retired; pre-LLM raise path documented); §13.4 TS-33 (expected output updated to "D-63 caller pre-check catches; Layer 4 not invoked; defensive raise path on bypass"); D-63 §6.3 (wording updated from "Synthesis returns an error session" to "D-63 caller pre-checks before invoking Layer 4; returns unavailable response directly; rest-shape reserved for genuine coaching-chosen rest days"); D-63 §9 scenario 5 (updated to match). Companion contract sections honored (`Layer4_Spec.md`): §3.3 (call signature), §3.5 (errors — including new code this session), §4.4 (input validation — including new row this session), §5.3 (Pattern B algorithm), §5.4 (validator scope: minimal — duration/intensity/equipment/injury; no weekly-volume/ACWR/phase-shape checks for single-session), §5.5 (capped retry, cap=2), §7.2/§7.3/§7.4 (`PlanSession` discriminated union + `CardioBlock` + `StrengthExercise`), §8.6 (`intensity_modulated` D-63-only cross-phase flag), §8.7 (call-level observations — `intensity_modulated` orchestrator-side bubble; `opportunity` LLM-emitted exception), §10.4 (single-session edge cases — sport-unavailable row amended this session), §11.1 / §11.2 / §11.3 (latency / token / cost budgets — ~3s / ~3500 input + ~800 output / ~$0.02 typical). Token + cost per invocation: ~3500 input + ~3500 extended thinking + ~800 output = ~$0.075/invocation worst-case (matches §11.3 ~$0.02 typical headline assuming reasoning depth below worst-case). 15 PSS-prefix v1 test scenarios in §12 mapping to `Layer4_Spec.md` §13.4 + §13.6 + §13.7. **D-row status flips this revision:** None. **No new D-rows added** — prompt-body sessions are spec-track follow-on. **Files touched (1 substantive prompt body + 2 substantive spec amendments + 2 bookkeeping + handoff = 6 total):** new `aidstation-sources/prompts/Layer4_SingleSession_v1.md` (the single-session prompt body), `Layer4_Spec.md` (§3.5/§4.4/§10.4/§13.4 surgical amendments), `OnDemand_Workout_D63_Design_v1.md` (§6.3/§9 surgical amendments), `CLAUDE.md` (last-shipped-narrative bump from "2 of 5 prompts shipped" → "3 of 5 prompts shipped + coupled §10.4 spec amendment" + Layer 4 row status notes the spec amendments + authoritative-current-files adds the SingleSession file + Project_Backlog v32 → v33 + Next-forward-move rewritten to flag 2 remaining prompt bodies queued + Layer 4 implementation track note that D-63 LLM-call layer can now land), this backlog file revision bump (v32 → v33), single-session closing handoff. 5-file ceiling broken intentionally (6 files; Andy approved option-2 fold-in pre-draft). Per Andy 2026-05-16: γ pick for D7 — pre-LLM precondition path; rest-shape stays reserved for genuine coaching-chosen rest days; D-63 caller owns the unavailable response shape + frontend affordances ([Pick another location] / [Pick another sport]). Per `V5_Design_Layer4_Prompts_SingleSession_Closing_Handoff_v1.md`.)
- v32 — 2026-05-16 (**Layer 4 per-phase synthesizer prompt body shipped — second of 5 in the Layer 4 prompt-body arc; cadence switched from end-of-arc to per-prompt this session (Andy pick).** Catches up the predecessor seam-reviewer prompt body (`aidstation-sources/prompts/Layer4_SeamReviewer_v1.md` shipped 2026-05-16 via PR #59 — first of 5; bump was previously deferred per the v1-commit-deferral rule carried from the spec arc; now retroactively folded into this revision so the file-revision chain is coherent). **`aidstation-sources/prompts/Layer4_PerPhase_v1.md` — 764 lines, 14 H2 sections + 23 H3+ subsections.** Source decisions D1–D10 captured in the file header: D1 tool-use (`record_phase_sessions`); D2 extended thinking ON ~5000 tokens (max-defensive); D3 hybrid input format (small payloads full, large payloads — 2C equipment view + 2B terrain — trimmed per-locale per-format slice); D4 hybrid prior-phase rendering (weekly rollup + last-week sessions full); D5 closed-set coaching-flag enum union per §§8.2–8.6 (6 LLM-emitted flags: `technique_emphasis`, `long_slow_distance`, `weak_link_targeted`, `overreach_test`, `discipline_specific_intensity`, `race_pace_specific`); D6 deload cadence anchor table per mode (every 4th/3rd/5th wk standard/compressed/extended; custom = judgment) with override-with-rationale; D7 Taper-length anchor table per race format (1–2wk sub-marathon / 2–3wk marathon-class & half-IM-class / 3wk full-IM / 3wk multi-day ultra & stage race & expedition AR 48–72h / 3–4wk expedition AR >72h / 1wk open-ended) with override; D8 hybrid RuleFailure retry context (rule_name + severity + detail + affected_session_ids + suggested_constraint); D9 schema-enforced length caps (600 chars `session_notes` / 240 chars `coaching_intent` / 600 chars `phase_synthesis_notes` / 240 chars per opportunity entry); D10 file at `aidstation-sources/prompts/Layer4_PerPhase_v1.md` per the `prompts/` subdir convention from `Layer4_SeamReviewer_v1.md` §6.2. Companion contract sections (`Layer4_Spec.md`): §5.2 step 3 (call site), §5.4 (deterministic validator the synthesizer's output must clear), §5.5 (capped retry semantics), §6.1 (phase boundary helper), §6.2 (seam re-prompt path), §6.5 (`start_phase != 'Base'` handling), §7.2/§7.3/§7.4 (`PlanSession` discriminated union + sub-blocks), §7.6 (`PhaseSpec` + `SynthesisMetadata` orchestrator-filled metadata), §8 (closed-set flag taxonomy), §11.2 (token budget — confirmed). Token + cost accounting per phase: ~8000 input + ~5000 extended thinking + ~3000 output = ~$0.144 per phase; for 4-phase `plan_create` ~$0.58 synth + ~$0.18 seam = ~$0.76 total (matches §11.3 headline $0.50–1.10). 15 PPS-prefix v1 test scenarios in §12 mapping to `Layer4_Spec.md` §13.2 plan_create scenarios + §13.6 coaching-flag emit scenarios. **D-row status flips this revision:** None. **No new D-rows added** — prompt-body sessions are spec-track follow-on, not backlog D-rows. **Files touched (1 substantive + 2 bookkeeping = 3 + handoff = 4):** new `aidstation-sources/prompts/Layer4_PerPhase_v1.md` (the per-phase synth prompt body), `CLAUDE.md` (last-shipped-narrative bump from "Layer 4 spec v1" → "Layer 4 prompt-body arc — 2 of 5 prompts shipped (seam-reviewer + per-phase)" + authoritative-current-files adds the `prompts/` subdir entry + Project_Backlog v31 → v32 + Next-forward-move rewritten to flag 3 remaining prompt bodies queued under per-prompt cadence), this backlog file revision bump (v31 → v32), per-phase closing handoff. `Layer4_Spec.md` untouched — prompt bodies are downstream consumers; §12.2 forward-pointer remains accurate as a 5-prompt-arc reference (now 2 of 5 done). `Layer4_SeamReviewer_v1.md` untouched. Per Andy 2026-05-16: per-prompt cadence — PR after each prompt body, not end-of-arc batch (the end-of-arc PR alternative from `Layer4_SeamReviewer_v1.md` §5.4 is explicitly retired by this cadence switch). Per `V5_Design_Layer4_Prompts_PerPhase_Closing_Handoff_v1.md`.)
- v31 — 2026-05-16 (**Layer 4 spec v1 — 4-session design arc + 2 mid-arc calibration commits + close-out pass.** First v2-layer-spec completion since Layer 3A/3B; Layer 4 is the plan-generation layer. Arc covered: session 1 (§§1–3 + §7 payload schema including `RaceWeekBrief` + `RacePlan` + 7 design decisions including 4-entry-point split + discriminated-union session shape); session 2 (§§4–6 + Decision 8 seam-reviewer authority = β propose-patch); session 3 (§§8–9 coaching flag rules + caching/determinism + 2 calibration rounds resolving 10 policy-default items: Peak intensity 70/20/10; 12-week open-ended horizon; Taper hard bounds removed (synthesizer-picked within proportion); ACWR `volume_ramp_aggressive` threshold raised 1.15 → 1.25; `long_slow_distance` + `recovery_day_after_long` + `recovery_week` flags added; §6.4 4th `shape_override` trigger + escalation table for 4 `Layer4ShapeInfeasibleError` classes); session 4 (§§10/11/13 — edge cases with concrete detection algorithms for the 4 `Layer4ShapeInfeasibleError` classes; performance budget with latency + token + cost + cache hit-rate + cumulative ceilings; 84 numbered test scenarios + 5 CI smoke tests); close-out pass (§12 open items / forward references with 6 subsections + §14 stub re-labeled as intentional deferral to fresh-eyes follow-on session per §12.6). **`Layer4_Spec.md` 1667 lines, 90 H2+ headings, 84 TS rows.** Contract sections §§1–13 complete; §14 retrospective deferred to fresh-eyes session before Layer 4 implementation. **D-row status flips this revision:** D-63 + D-64 implementation gates lifted (both were "🟡 implementation pending — gates on Layer 4 spec landing"; now "🟡 implementation pending — Layer 4 spec v1 landed; ready for implementation"). **No new D-rows added** — five prompt-body design sessions queued per stop-and-ask trigger #2 (per-phase synthesizer + per-tier T1/T2 + single-session + seam-reviewer + race-week-brief) but these are spec-track follow-on sessions, not backlog D-rows. **Files touched (3 substantive + 1 bookkeeping = 4):** `Layer4_Spec.md` (the 4-session arc product), `CLAUDE.md` (last-shipped-narrative bump + Layer-pipeline-table Layer 4 status → SPEC v1 DONE + authoritative-current-files Layer4_Spec.md added + Project_Backlog v30 → v31 + Next-forward-move rewritten with §14 retro + 5 prompt-body sessions + Layer 4 implementation + Layer 4.5 + Layer 5 candidates), this backlog file revision bump (v30 → v31), session-4 closing handoff with §10 close-out addendum. Per Andy 2026-05-16: ship the v1 spec arc as a PR + merge now per "push to production as we go" rule; §14 retro lands before implementation. Per `V5_Design_Layer4_Spec_Session4_Closing_Handoff_v1.md`.)
- v30 — 2026-05-16 (PR17 — DATABASE.md deep rewrite + 3 code residuals PR13 missed. Closes out the last remaining SQLite-cleanup carry-forward — the root `DATABASE.md` deep-section rewrite tracked from PR14 §5.1 Option C2 / PR15 §5.1 Option C / PR16 §5.1 Option C. **Files touched (1 doc + 3 code + 3 bookkeeping = 7 substantive):** root `DATABASE.md` deep rewrite — drops the 4 `[STALE — SQLite path retired PR13]` blockquotes PR14 added (Migration philosophy, Init and seed flow, Backend-portable upserts, Postgres datetime vs SQLite TEXT) and rewrites each section PG-only (Migration philosophy: single `_PG_MIGRATIONS` list, dual-list rules dropped, "don't write SQLite-only syntax" subsection deleted; Init and seed flow: just `init_postgres()` + five-phase shape; Backend-portable upserts → "UPSERT patterns" with PG-only forms table; Postgres datetime vs SQLite TEXT → "Datetime columns in templates" with just the `|string` wrap explanation); top-of-file marker rewritten to drop the "rewrite owed but not blocking" framing; surgical PG-only edits on 5 inline spots (Multi-user scoping NULL framing line 233, users `created_at` TEXT-on-SQLite template-slicing note line 308, athlete_profile `_is_postgres()` branching reference line 343, body_metrics "INSERT OR REPLACE on SQLite (branched in)" line 483, `athlete.py` UPSERT description). Code residuals PR13 missed (discovered during the DATABASE.md cleanup): `routes/body.py` — top-level `_IS_PG = bool(os.environ.get('DATABASE_URL'))` constant + the live `if _IS_PG: ON CONFLICT ... else: INSERT OR REPLACE ...` branch in `_save()` (the else branch was dead code since `get_db()` raises if `DATABASE_URL` is unset post-PR13) — stripped completely, leaving just the PG `ON CONFLICT` path + unused `import os` removed; `routes/conditions.py:170` — 4-line stale comment explaining why `ON CONFLICT DO NOTHING` is preferred over the SQLite-only `INSERT OR IGNORE` (code was already PG-correct; just the comment was stale) — removed; `routes/locales.py:341` — 2-line stale comment about `CURRENT_TIMESTAMP` being portable vs `datetime('now')` being SQLite-only (code was already PG-correct) — removed. **D-rows touched:** None. The DATABASE.md deep-section rewrite + 3 code residuals were tracked as deferred cleanup carry-forwards across PR13/PR14/PR15/PR16 handoff §5.3 lists; not D-rows of their own. **Verification:** `grep -rn "_IS_PG\|_is_postgres\|INSERT OR IGNORE\|INSERT OR REPLACE\|datetime('now')\|SQLITE_SCHEMA\|_SQLITE_MIGRATIONS\|init_sqlite\|sqlite_path" --include="*.py" .` → 0 hits (codebase fully SQLite-free); `grep -n "STALE" DATABASE.md` → 1 hit (the audit-trail mention in the top-of-file marker, which is the appropriate historical reference); `python3 -m py_compile routes/body.py routes/conditions.py routes/locales.py` → all clean. SQLite cleanup story closed for real (D-54 ✅ Resolved code-side by PR13 + doc-side fully by PR14 + PR16 + PR17). Net file count: 7 substantive files (1 doc rewrite + 3 code residuals + 3 doc bookkeeping). 5-file ceiling broken intentionally per Andy's "Include all 3" pick. Per `V5_Implementation_PR17_DATABASE_md_Deep_Rewrite_Closing_Handoff_v1.md`.)
- v29 — 2026-05-16 (PR16 — Control_Spec v7 → v8 SQLite cleanup. New `Control_Spec_v8.md` with a "What changed in v8 vs v7" section retroactively flagging v6's "removed during Phase 5" SQLite-coupling narrative as superseded by PR13's standalone D-54 closure; TrueNAS D-65 ✅ Resolved noted; §9 doc-map staleness flagged with Rule #12 resolve-to-highest-N reminder + enumerated stale cross-refs (Onboarding v4→v5, Integration v2→v5, Catalog plan v2→v3, Backlog v11→v29, Layer3_3B_Spec ⏳→✅, design-wave specs uncovered); full §9 sync deferred to next architectural revision. §§1–8 + §§10–11 body byte-identical to v7. No code; no D-row status flips. Per `V5_Implementation_PR16_Control_Spec_v8_Closing_Handoff_v1.md`)
- v28 — 2026-05-16 (PR15 — D-61 profile-tab schedule edit follow-on. Extracted §G form into `templates/onboarding/_schedule_form.html` shared partial; `templates/onboarding/schedule.html` slimmed to a thin wrapper; `templates/profile/edit.html` gains a Schedule tab + pane; `routes/profile.py` GET pre-populates schedule context + new `save_schedule()` POST handler at `/profile/schedule` lazy-imports `_parse_schedule_form` from `routes.onboarding`. D-61 status flipped 🟢 §G + onboarding integration shipped → 🟢 §G + onboarding + profile-tab editing shipped; 🟡 JIT swap pending Layer 4. Per `V5_Implementation_PR15_D61_Profile_Tab_Edit_Closing_Handoff_v1.md`)
- v27 — 2026-05-16 (PR14 — Doc sweep. `Catalog_Migration_Plan_v2.md` → `_v3.md` (SQLite retirement decoupled from §1/§5); `Athlete_Data_Integration_Spec_v4.md` → `_v5.md` (§2.5 SQLite freeze flagged Retired); `aidstation-sources/DATABASE.md` collapsed to thin redirect; root `DATABASE.md` PR13 marker strengthened with 4 inline [STALE] flags. No code; no D-row status flips. Per `V5_Implementation_PR14_Doc_Sweep_Closing_Handoff_v1.md`)
- v26 — 2026-05-16 (PR13 — SQLite + TrueNAS retirement. Strips `SQLITE_SCHEMA` / `_SQLITE_MIGRATIONS` / `init_sqlite` / `sqlite_path` / `_is_postgres()` runtime guards (13 across 6 route files) / `now_sql` ternaries; deletes `Dockerfile` / `docker-compose.yml` / `deploy/truenas_setup.sh` / `deploy/update.sh`. PG-only via `DATABASE_URL`. D-54 ✅ Resolved; D-65 added ✅ Resolved (TrueNAS retire). Stack cleanup triggered by Andy's "we keep working on it, why?" question after PR12 added 5 cols to SQLITE_SCHEMA. Per `V5_Implementation_PR13_Closing_Handoff_v1.md`)
- v25 — 2026-05-16 (D-50 row status flip catching up PR12 + D-61 status flip: PR12 shipped §G per-day-windows form + onboarding-step integration; D-61 status flipped 🟡 Implementation pending → 🟢 §G + onboarding integration shipped; 🟡 JIT swap pending Layer 4. Per `V5_Implementation_PR12_Closing_Handoff_v1.md`. No new D-row work — pure status tracking + D-61 §G execution)
- v24 — 2026-05-15 (Plan-execution design pass — two new D-rows added: D-63 on-demand workout + D-64 plan refresh tiers. Both design complete; implementation gates on Layer 4 spec landing. No code shipped this revision; pure design addition. Per `V5_Design_D63_D64_Closing_Handoff_v1.md`)
- v23 — 2026-05-15 (D-50 row status flip catching up PR11 + D-59/D-60 status flip: D-50 status cell now reads 🟢 PR1–PR11 shipped; D3b shipped PR11 closing the v5 §J locale work — FK fix on `locale_equipment_overrides` / `locale_toggle_overrides` + D-60 inherit/override UI for the seven shared-profile categories + D-59 §6 manual→Mapbox upgrade + D-59 §7 on-demand refresh + `MANUAL_CATEGORIES` realignment to D-60 §3's 10-category taxonomy. PG-only. D-59 + D-60 statuses flipped 🟢 fully shipped. PR12+ carve-outs: §J.3 toggle UI, D-60 dispute / submit-as-correction / sharing opt-out / sharing-consent disclosure. Per `V5_Implementation_PR11_Closing_Handoff_v1.md`. No new D-row work — pure status tracking + D3b execution)
- v22 — 2026-05-15 (D-50 row status flip catching up PR10 + D-59 status flip: D-50 status cell read 🟢 PR1–PR10 shipped 2026-05-14/15; 🟢 D3a locale-creation + Mapbox chain detection shipped PR10 — `mapbox_client.py` wrapping the Mapbox Geocoding API → mid-session migrated to Search Box API for POI/brand search; `routes/locales.py` extended with `/locales/new` search + manual fallback + disclosure ack + `/locales/<slug>/nearby` proximity picker; 3 templates new/edited; chain detection consumed `chain_registry.detect_chain()` (PR2-shipped); D-59 status flipped 🟡 Implementation pending → 🟢 D3a shipped (D3b pending); no new D-row work — pure status tracking + D3a execution; pre-existing `locale_equipment_overrides` FK bug surfaced and flagged for D3b)
- v21 — 2026-05-15 (D-50 row status flip catching up PR9: D-50 status cell now reads 🟢 PR1 + PR2 + PR3 + PR4 + PR5 + PR6 + PR7 + PR8 + PR9 shipped 2026-05-14/15 (commits `3628ca6`, `686bb40`, `b819f0a`, `f4d2e75` PR4-merge, `34637d2` PR5-merge, `2c8d01f` PR6-merge, `df17f08` PR7-merge, `43ccedf` PR8-merge, `7cb786a` PR9-merge); 🟢 Option E shipped — PR9 wires the v5 §A.2.4 14-day connect-provider nudge as a Vercel-Cron-hosted daily scanner (`GET /cron/nudges/connect_provider_14d` token-gated via `Authorization: Bearer $CRON_SECRET`; INSERT … ON CONFLICT DO NOTHING over users >= 14 days old with zero `provider_auth.status='active'` rows + no existing `connect_provider_14d` row) + a dismissable in-app banner (`templates/_account_nudges.html` partial slotted into `templates/base.html` + `active_nudges` context processor reading `routes.nudges.get_active_nudges`) + `POST /nudges/<id>/dismiss` writing `dismissed_at` + `vercel.json` `crons` entry on `0 14 * * *` (daily 14:00 UTC). Deep-link target is PR5's `/onboarding/connect`. PG-only; SQLite dev returns no nudges (`account_nudges` is in `_PG_MIGRATIONS` only) per `V5_Implementation_PR9_Closing_Handoff_v1.md`. No new D-row work this revision — pure status tracking + E execution)
- v20 — 2026-05-15 (D-50 row status flip catching up PR8: D-50 status cell now reads 🟢 PR1 + PR2 + PR3 + PR4 + PR5 + PR6 + PR7 + PR8 shipped 2026-05-14/15 (commits `3628ca6`, `686bb40`, `b819f0a`, `f4d2e75` PR4-merge, `34637d2` PR5-merge, `2c8d01f` PR6-merge, `df17f08` PR7-merge, `43ccedf` PR8-merge); 🟢 frontend D1 (Step-2 connect) + D2a (read-side prefill UX) + D2b (write-side prefill UX) shipped — PR8 wires `[Use provider value]` / `[Keep current]` POST handlers at `/onboarding/prefill/<field>/use-provider` and `/keep-current`, refactors `routes/profile.py:_record_self_report_provenance` for the `'provider_*'` → `'manual_override'` source-flip per v5 §A.2.3, and ships the inline "Use {provider} value instead" clear-override link beneath `'manual_override'` badges per v5 §A.2.6 per `V5_Implementation_PR8_Closing_Handoff_v1.md`. Field-name validation is registry-driven (app-level guard inside `_record_self_report_provenance` + per-route `_lookup_field` 404 — `KNOWN_PROFILE_FIELDS` stays the single source of truth; no schema CHECK migration). No new D-row work this revision — pure status tracking)
- v19 — 2026-05-14 (D-50 row status flip catching up PR7: D-50 status cell now reads 🟢 PR1 + PR2 + PR3 + PR4 + PR5 + PR6 + PR7 shipped 2026-05-14 (commits `3628ca6`, `686bb40`, `b819f0a`, `f4d2e75` PR4-merge, `34637d2` PR5-merge, `2c8d01f` PR6-merge, `<PR7-merge-pending>`); 🟢 frontend Option D1 (Step-2 connect screen) shipped PR5; 🟢 Option D2a (read-side prefill UX) shipped PR7 — `KNOWN_PROFILE_FIELDS` registry (`routes/profile_fields.py`) closing Open Item #17 + provider extractors (`routes/profile_extractors.py`, 2 real HRmax-from-`cardio_log` + 8 stubs) + `/onboarding/prefill` GET route + comparison-page template + `_POST_STEP2_CONTINUE_TARGET` flip from `/profile?tab=athlete` to `/onboarding/prefill`; 🟡 D2b write-side button wiring + `manual_override` flip + clear-override popover deferred to PR8+. PR7 ships read-only with stub `[Use provider]` / `[Keep current]` controls per the 5-file ceiling per `V5_Implementation_PR7_Closing_Handoff_v1.md`. No new D-row work this revision — pure status tracking)
- v18 — 2026-05-14 (D-50 row status flip catching up PR5 + PR6: D-50 status cell now reads 🟢 PR1 + PR2 + PR3 + PR4 + PR5 shipped 2026-05-14 (commits `3628ca6`, `686bb40`, `b819f0a`, `f4d2e75` PR4-merge, `34637d2` PR5-merge); 🟢 PR6 shipped 2026-05-14 on branch `claude/v5-closing-handoff-zg5i0`; 🟢 frontend Option D1 (Step-2 connect screen) shipped PR5; 🟡 D2 prefill UX (registry + comparison UI) deferred to PR7 — PR6 lays the column foundation. PR5 ships `/onboarding/connect` (Step-2 connect screen reusing PR4's `CONNECTION_PROVIDERS` + `load_connections` registry; new-user post-register redirect points there) + drive-by CSP nonce fix on PR4's tab-activation script per `V5_Implementation_PR5_Closing_Handoff_v1.md`. PR6 (D-51 column foundation) adds 5 new `athlete_profile` columns — `body_weight_kg`, `hrmax_bpm`, `lactate_threshold_hr_bpm`, `vo2max`, `cycling_ftp_w` (v5 §A.2.1 prefill-eligible §F performance baselines + §A body weight) — plus manual self-report form fields under `/profile?tab=profile` + `_record_self_report_provenance` writes to `athlete_profile_field_provenance` (PG-only) closing Open Item #17's blocking dependency (no storage home for prefill-eligible fields). PR6 also bundles G — `coros_ingest._ingest_activity` ON CONFLICT cleanup against PR3's `cardio_log_coros_label_uidx` partial UNIQUE — per `V5_Implementation_PR6_Closing_Handoff_v1.md`. No new D-row work this revision — pure status tracking)
- v17 — 2026-05-14 (D-50 row status flip catching up PR2 + PR3 + PR4: D-50 status cell now reads 🟢 PR1 + PR2 + PR3 shipped 2026-05-14 (commits `3628ca6`, `686bb40`, `b819f0a`); 🟡 frontend (Option B partial — Account Config 1 Connections tab shipped PR4; full prefill UX D2 + step-2 connect D1 still pending). PR3 added Polar OAuth + webhook + ingestion + three `cardio_log` partial-UNIQUE indexes per `V5_Implementation_PR3_Closing_Handoff_v1.md`. PR4 ships the COROS+Polar management screen as a Connections tab on `/profile` + `provider_auth.disconnect` helper + `?<provider>_connected=1` passive-prefill prompt, bundled with this v16→v17 bump per PR3 §5.4 mechanical instructions. No new D-row work this revision — pure status tracking)
- v16 — 2026-05-14 (D-50 row status flip + D-59/60/61 schema PR2 reference: D-50 flips 🟡 Partial → 🟢 PR1 shipped 2026-05-14 (commit `3628ca6`) with COROS as first real OAuth + provider_auth helper + D-58 schema; 🟡 follow-on PRs pending per `V5_Implementation_PR1_Closing_Handoff_v1.md` §5.1. PR2 D-59/60/61 schema shipped same-day on branch `claude/review-v5-handoff-t7KBL` per `V5_Implementation_PR2_Closing_Handoff_v1.md`. No new D-row work this revision — pure status tracking)
- v15 — 2026-05-14 (D-50 Phase 1 review pass: `Athlete_Data_Integration_Spec` bumped to v4 with §2.5 carve-out — D-50 SQLite block ratified retroactively as a one-time exception; freeze remains in force for all subsequent tables. D-54 row updated to reflect the carve-out. New D-62 row added for the `webhook_events` retention prune called out in Integration v3 §4.2 and the D-50 closing handoff §8. No new D-row work this revision — pure ratification + tracking)
- v14 — 2026-05-14 (D-50 Phase 1 schema applied to `init_db.py` — `provider_auth`, `webhook_events`, four `polar_*`, `wahoo_plans`, three `coros_*` tables plus seven `cardio_log`/`training_log` foreign-id columns. SQLite + Postgres migration lists both updated; idempotent re-run verified locally against SQLite. D-50 status flipped Deferred → 🟡 **Partial — schema shipped; per-provider route wiring pending**. First production catchup work under Andy's "push to production as we go" rule — onboarding rebuild deferred to the D-58–D-61 design wave per next session)
- v13 — 2026-05-14 (Layer3_3B_Spec.md shipped same session as the reconciliation pass; new Rule #13 added to CLAUDE.md — every closing handoff names CLAUDE.md as the first re-read)
- v12 — 2026-05-14 (reconciliation pass: D-50 + D-55 reframed to drop "separate repo" / "Vercel app" framing — single repo; D-55 paused — Garmin API access temporarily closed by Garmin; v1+v2 selective-rebuild + strangler-fig operating context captured in CLAUDE.md)
- v11 — 2026-05-13 (L3-Spec-Trio feedback round 2: Onboarding v4 + Integration v2 + Catalog Migration v2 shipped this round; D-55 reframed Garmin "migrate" → "build"; new D-57 through D-61 capture research re-eval cadence + four deferred architectural design tracks from feedback)
- v10 — 2026-05-13 (post-L3-Discovery + L3-Spec-Trio: Layer 3 framing reconciled to 4-node structure; integration architecture locked; catalog reconciliation Option A target state captured in new `Catalog_Migration_Plan.md`; `Athlete_Data_Integration_Spec.md` v1 shipped; `Athlete_Onboarding_Data_Spec_v3.md` shipped with §H.3 Non-Event Goal Type amendment; D-48 through D-56 added for integration/migration/onboarding follow-ups)
**Purpose:** Single rolling tracker for cross-layer deferred work — drift findings, design decisions parked for later, cleanup tasks, items surfaced in one layer's work that need fixing in another. Updated at every layer/node boundary. Final-cleanup batches read this doc at the end of each layer phase.
**Categories:** Blocker / Deferred / Cleanup. Items can promote to Blocker but never silently demote.
**Source of initial findings:** `Layer0_Deployed_Schema_and_Drift_Report.md` v2 + per-session surface area as Layer 2 design progressed.

---

## Categorization rule

**Blocker** — must fix before continuing. Triggers:
- Current or imminent node would consume wrong/incomplete data because of it
- Causes silent wrong results downstream
- Blocks data the next node must produce

**Deferred** — documentation only OR doesn't affect current/imminent nodes OR has a query-layer workaround. Goes to final cleanup.

**Cleanup** — known spec/doc drift that has no functional impact. Pure documentation work. Lowest priority within the final cleanup batch.

---

## Status legend

- 🔴 Blocker — fix immediately
- 🟡 Deferred — verify on each node-boundary; promote if scope intersects new node
- 🟢 Cleanup — pure doc/cosmetic, final-batch only
- ✅ Resolved
- ⚪ Wont-Fix (intentional, with reason)

---

## Open items

| # | Finding | Severity | Status | Affects nodes | Notes / workaround |
|---|---|---|---|---|---|
| D-01 | `exercises` §4.12 spec block fully stale — 7 missing cols, wrong names on 4, contradictory UNIQUE | High | 🟡 Deferred | None directly; Batch B/C patches already document deployed shape | Batch B + Batch C patches are the authoritative reference. Final cleanup folds them into v4. |
| D-02 | `disciplines` §4.3 — 4 cols missing from spec, 3 renames, 2 spec-only numerics not built | High | 🟡 Deferred | 2A consumer (discipline classification), 2D (injury risk) | Deployed columns are correct and consumable as-is. Spec rewrite is documentation. |
| D-03 | `phase_load_allocation` — `is_conditional BOOLEAN` and `vertical_gain_notes TEXT` defined in spec, NOT in deployed | High (spec-ahead) | 🟡 Decision locked, code change deferred (FC-1a, 2026-05-11) | 2A (discipline classification), 2D (injury risk), 4 (plan-gen) | **Decision: BUILD** (retain both in v4 §4.5; Andy confirmed 2026-05-11). Implementation lands in ETL extractor before v20 re-run — parser additions per spec §4.5 derivation rules (`is_conditional` from Role/Notes markers; `vertical_gain_notes` from sport-conditional Notes parse). v4 spec footnote required: "scheduled for v20 ETL re-run; not yet deployed — do not query as available until v20 ships." Code-side TODO for CC. |
| D-04 | `phase_load_allocation` — column-name `phase_` prefix in spec, no prefix in deployed | Med | ✅ Resolved (FC-1a, 2026-05-11) | All | Idempotent rename migration confirmed deployed state matches target (8 unprefixed band columns). Spec patch in `Layer0_ETL_Spec_v3_Patch_Batch_D` §4.x. SQL: `migrate_phase_load_allocation_rename_phase_prefix_v1.sql` (no-op verify; columns already correct). |
| D-05 | **`phase_load_allocation` ETL not filtering aggregator rows.** 33 WEEKLY TOTAL TARGET rows present alongside 162 discipline rows. **Verified 2026-05-10:** all 33 sports affected, including AR. | High | 🟡 **Partial — cleanup done, ETL code patch + standing rule retirement pending** | 2A, 2D, 2E, 4 | **2026-05-11 (FC-1a):** Cleanup SQL ran (`cleanup_phase_load_allocation_aggregators_v1.sql`) — all 33 aggregator rows were already in `superseded_at IS NOT NULL` state at pre-flight (prior unrecorded cleanup); current `phase_load_allocation` active row count clean. **STILL PENDING:** (a) ETL extractor code patch per `FC1a_Closing_Handoff_v1.md` §5 — apply via CC; (b) Control_Spec §8.2 standing rule retirement after next clean ETL run confirms no regression. Defensive filter MUST remain in query-layer nodes until both (a) and (b) land. |
| D-06 | `phase_load_weekly_totals` — `hours_low/high` renamed to `weekly_low_hours/high_hours` | Low | ✅ Resolved (FC-1a, 2026-05-11) | 2E (nutrition uses hours), 4 (plan-gen) | Deployed columns renamed to match spec §4.5.1 (`hours_low`, `hours_high`). SQL: `migrate_phase_load_weekly_totals_rename_hours_cols_v1.sql`. |
| D-07 | `phase_load_weekly_totals` — 16 rows short. **Verified 2026-05-10:** 4 missing sports are Off-Road / Adventure Multisport (Non-Nav), Open Water Marathon Swimming (10km / Olympic Distance), Open Water Marathon Swimming (25km / Ultra Distance), Swimrun. AR has all 4 phase rows ✓ | Med | ✅ Resolved with decisions (FC-1a, 2026-05-11) — **CC implementation pending** | 2E, 4 | xlsx inspection revealed **three distinct parser failure modes**: (1) multi-sub-format hours per phase (Off-Road, Swimrun); (2) km-based volume not hrs (Open Water 10km, 25km); (3) percentage-cut TAPER (Swimrun). **Decisions locked (Andy 2026-05-11):** 7.1 = Option A (collapse multi-sub-format to min low / max high range); 7.2 = Option B (convert km→hrs at parse time at ~20 min/km marathon swim pace); 7.3 = Option A (derive TAPER hours = PEAK midpoint × (1 − taper_pct_midpoint)). Full parser fix spec in `FC1a_Closing_Handoff_v1.md` §5.2. CC task — not FC-1a or FC-1b. |
| D-08 | `sport_discipline_map` — 3 rows missing in deployed (73 source vs 70 deployed). **Verified 2026-05-10:** 3 missing rows are Long Distance / Endurance Cycling (-2: 5 in xlsx, 3 deployed) and Triathlon (-1: 5 in xlsx, 4 deployed). AR has all 15 disciplines ✓ | Med | ✅ Resolved with explanation (FC-1a, 2026-05-11) | 2A | **Not a parser bug.** Source xlsx has rows that share `(sport, discipline_id)` within a sport — UNIQUE constraint correctly dedups at load. Long Distance / Endurance Cycling: D-005 appears 3× (Road / Gravel / TT sub-formats), D-006 appears 2× (XC / Enduro) — legitimate sub-format expansion; collapse accepted (Andy 2026-05-11). Triathlon: D-002 Road Running appears 2× — one detailed row (sub-distance breakdown) + one brief stale row — **xlsx source fix required:** delete brief row at xlsx idx=18 in "Sport × Discipline Map" sheet; keep idx=17. Sub-format-specific plan-gen scales via `phase_load_allocation` sub-format-keyed rows (D-17 standing rule), not via the discipline map. |
| D-09 | `sport_discipline_bridge` §4.11 — deployed has 4 denormalized cols + different UNIQUE; spec describes 4 cols only | Med | 🟡 Deferred | 2C (uses bridge for discipline→exercise_db_sport resolution) | 2C already designed against deployed shape. Spec rewrite is documentation. |
| D-10 | `terrain_types` v2 §4.12.4 — deployed has 7 enrichment cols + second UNIQUE; spec describes 2 cols only | High (doc) | 🟡 Deferred | 2B (terrain mapping) | 2B already designed against deployed shape. Provenance unclear — check populate scripts during final cleanup. |
| D-11 | `terrain_gap_rules` — not in spec v3 §4 at all (only in unmerged corrections doc, schema wrong there too) | High (doc) | 🟡 Deferred | 2B (terrain gap resolution) | Deployed schema is authoritative. v4 needs full new §4.x. |
| D-12 | `sport_name_aliases` — schema not documented in §4 (only mentioned in §6.2 run order) | Med (doc) | ✅ Resolved (FC-1a, 2026-05-11) | 2C, 2A (via bridge) | Schema block drafted in `Layer0_ETL_Spec_v3_Patch_Batch_D` §4.x. UNIQUE constraint assumption flagged for verification before v4 lock — verification query in the patch doc. |
| D-13 | `discipline_technique_foci` — Batch B patch says `source_exercise_id` singular; deployed is `source_exercise_ids[]` array. Patch also missing `audit_log` col | Low | ✅ Resolved (FC-1a, 2026-05-11) | 4 (plan-gen consumes foci) | Correction drafted in `Layer0_ETL_Spec_v3_Patch_Batch_B_Correction` — `source_exercise_ids TEXT[]` + `audit_log TEXT`. Documentation-only; deployed shape already matches corrected spec. |
| D-14 | `cross_sport_properties` §4.8 — deployed has 4 extra cols (`source_evidence`, `notes`, `source_text`, `confidence`). `source_text` is suspicious — duplicates `source_evidence` semantically | Low | ✅ Resolved (FC-1a, 2026-05-11) | None currently | Investigation 2026-05-11: confirmed source_text content identical to source_evidence on single deployed row. Column dropped via `migrate_cross_sport_properties_drop_source_text_v1.sql`. Spec catches up in Batch D v2 — cross_sport_properties now has 3 extra cols (source_evidence, notes, confidence). |
| D-15 | `discipline_substitutes` §4.9 — UNIQUE in deployed includes `substitute_name`; spec doesn't. Loose constraint allows duplicate (target, substitute) with different names | Low | ✅ Resolved with explanation (FC-1a, 2026-05-11) | 2D? (substitution logic), 4 (plan-gen) | Investigation 2026-05-11: 2 conflict rows found, both **deliberately-authored sub-format variants with distinct Fidelity ratings**. D-008b→D-007 (Packrafting): whitewater 0.6 vs flat-water 0.4. D-023→D-001 (Trail Running): sustained downhill 0.85 vs rolling 0.3. Tightening would destroy real coaching signal. **Decision: leave deployed UNIQUE loose** (Option D, Andy 2026-05-11). Spec catches up to code in Batch D v2 §4.9 — `UNIQUE (target_id, substitute_id, substitute_name, etl_version)`; substitute_name acts as variant key. Layer 2D substitution logic should query all variants and pick by fidelity given athlete context (locale terrain, equipment, etc.). |
| D-16 | `primary_muscles` / `secondary_muscles` — Open Item R now resolved; spec v3 §4.12 doesn't list them | Low | ✅ Resolved (FC-1a, 2026-05-11) | None (data is fine; doc gap) | Type confirmed `TEXT[]` per Drift Report §1. ETL transform documented: `string_to_array(value, ', ')`. Folds into `Layer0_ETL_Spec_v3_Patch_Batch_B_Correction` (replacement rows for Batch B patch §4.12 "Columns added" table). |
| D-17 | **Sport naming convention mismatch between Sheet 3 and Sheet 5.** Surfaced 2026-05-10 during D-07 verification. `sport_discipline_map` and `sport_discipline_bridge` use top-level sport names ("Triathlon", "Skimo", "Long Distance / Endurance Cycling", "Canoe / Kayak Marathon", "Open Water Marathon Swimming"). `phase_load_allocation` and `phase_load_weekly_totals` use sub-format expansions ("Triathlon (Standard / Olympic)", "Skimo (Sprint)", etc.) — 1:N relationship. AR uses the same name in both tables ✓ | Med | 🟡 Deferred (AR-safe) — **design requirement, not cleanup** | Layer 1 race-goal capture, 2A | **Not an FC-1 task.** This is a forward design item. Resolution path: athlete's race goal (race distance + event format) drives sub-format selection. The mapping logic belongs in Layer 1 onboarding or 2A discipline classification, not in an ETL fix. Owner: whoever drafts the Layer 1 race-goal spec. **D-08 closure (2026-05-11) confirmed plan-gen scales correctly under this naming convention** — phase_load_allocation has sub-format-specific rows for all multi-format sports. |
| D-21 | **`health_condition_categories` column name uncertainty.** v3 spec §4.14 / v2 §4.12.2 defines `category_name`; v3 §6.2 validation note references `system_category`. Drift report originally flagged this table as "no drift" but didn't reconcile the column name. Surfaced 2026-05-10 during 2D design. | Low | ✅ Resolved (FC-4b, 2026-05-13) | 2D, 2E | **2026-05-13: D-21 closed.** Neon `information_schema.columns` retry returned 6 columns + 2 constraints. **Deployed column is `category_name`** (matching v3 §4.14 / v2 §4.12.2); the v3 §6.2 validation `system_category` reference was the stale half of the split — corrected to `.category_name` in `Layer0_ETL_Spec_v7.md` §6.2. Full schema block now in v7 §4.14. **Consumer impact: none.** `system_category` continues as the Python dataclass field name on `HealthConditionRecord` in Layer 2D / 2E (independent of the SQL column name); the dataclass field is what 2D and 2E actually read. Layer 2D row 2D-5 and Layer 2E row 2E-16 open-items can resolve on next 2D / 2E touch (no spec bump required just for this; piggyback with D-47). |
| D-22 | **`exercises.movement_components TEXT[]` deployed.** Structured movement-constraint tokens (subset of Onboarding §B.3 11-token enum) replace heuristic keyword-match against `injury_flags_text` for Layer 2D set-intersect. **Population: 159 active rows.** | High | ✅ Resolved (FC-1b, 2026-05-12) | 2D | **2026-05-12: D-22 closed.** 159/159 rows populated via `migrate_exercises_add_movement_components_v1.sql` (57 Pass 1 + 102 Pass 2). All 6 verification checks passed; GIN index `idx_exercises_movement_components` deployed. Full baseline + house rules in `D22_Curation_Reference_v2.md` (Rules 1–11 Pass 1 + Calibrations 12–15 Pass 2). EX024/EX119 consistency precedent applied. Generator: `etl/sources/generate_movement_components_migration.py`. `injury_flags_text` retained as reference data; `movement_components` is now source of truth for 2D. Open follow-ups: D-37 (source-data hygiene), D-38 (wrist deviation token gap). |
| D-23 | **`disciplines.body_parts_at_risk TEXT[]` deployed.** Structured body-part tokens enable direct set-intersect against athlete `Injury Record.body_part` (canonical 51-token vocabulary), replacing the heuristic `BODY_PART_KEYWORDS` map in Layer2D_Spec §5.5. **Population: 31 disciplines.** | High | ✅ Resolved (FC-1b, 2026-05-12) | 2D | **2026-05-12: D-23 closed.** 31/31 rows populated via `migrate_disciplines_add_body_parts_at_risk_v1.sql` (172 total token references across the baseline; 28 of 51 canonical body parts used in discipline-level risk patterns). All 6 verification checks passed; GIN index `idx_disciplines_body_parts_at_risk` deployed. Locked baseline + house rules in `D23_Curation_Reference_v1.md` (Rules 1–7 + Shoulder/Rotator-cuff normalization calibration). Generator: `etl/sources/generate_body_parts_at_risk_migration.py`. Per Andy 2026-05-12: Head/Eye/perineum permanently skipped (not body-part-relevant at this layer); Collarbone added to canonical body parts vocabulary. `common_injury_patterns` retained as reference data; `body_parts_at_risk` is now source of truth for 2D. **Layer2D_Spec §5.5 decision point B locked.** Follow-up: D-39 (Vocabulary_Audit v3 rewrite). |
| D-24 | **Medications × exercise interaction surface** — deferred to future work (not v1). | Low | 🟡 Deferred | Future | Risk space exists (beta blockers, anticoagulants, etc.) but v1 keeps it out of 2D scope. Revisit post-cohort feedback. Related: D-33 (beta blocker × Cardiac inference). |
| D-25 | **§I v3 polish candidates** — 10 onboarding refinement items parked during §I structured rewrite. | Low | 🟢 Cleanup | Layer 1 v3 | See `Section_I_Audit`. v3 onboarding pass, not now. Not blocking 2E. |
| D-26 | **`supplement_vocabulary` Layer 0 table implemented in FC-1.** | High | ✅ Resolved (FC-1, 2026-05-11) | 2E | Table deployed to Neon dev with 25 seed entries per `Supplement_Vocabulary_Spec`. Schema: 8-value `category` enum + 4-value `evidence_quality` enum + structured `contraindications TEXT[]` for 2E coaching-flag cross-ref against §B. 2E supplement FK reads now valid. ETL tag: `supp_vocab.v1.FC1`. Migration: `etl/sources/migrate_supplement_vocabulary.sql` (already in `main` from IJKL-drift commit 50785df; deployed this session). |
| D-27 | **Plan Management spec not yet written.** 2E names contracts for `HeatAcclimState`, `expected_race_temp_c`, and `current_phase` source-of-truth that Plan Management must honor. Plan Management subsystem also handles weight staleness advisories and adherence-drop logic (see `Adherence_Drop_Spec_v2`). | High | 🟡 Deferred | 2E, future Layer 3/4 | Spec lands post-Layer-3 design. 2E ships with documented contract; Plan Management implementation comes later. Named contract is the protection. |
| D-28 | **FFM (`ffm_kg`) field not captured in onboarding.** Cunningham BMR formula requires FFM; falls back to Mifflin-St Jeor when absent. Onboarding §A (demographics) vs §F (performance testing) home — Andy decision pending. | Med | 🟡 Deferred | 2E | 2E auto-switches BMR formula based on availability (no spec change needed). Onboarding v3 work. |
| D-29 | **`race_fueling_bands` Layer 0 table** — promotion candidate for §5.4.2 5-tier duration band × 7-column matrix currently in code. Sibling: **`dietary_pattern_adjustments`** table (vegan B12/iron/EPA, low-FODMAP race adj, etc., currently in §5.6 logic). | Low | 🟡 Deferred (post-v1) | 2E | Hand-curated bands ship in v1 code; promote when curation pressure rises. Splits curation from algorithm. Mirrors D-22 / D-23 promotion pattern. |
| D-30 | **`sport_endurance_modifier` Layer 0 table** — promotion candidate for §5.4.3 sport modifier (6×3 matrix) currently in code. Sport profile classification currently hand-coded. | Low | 🟡 Deferred (post-v1) | 2E | Same pattern as D-29. Promoting both formalizes the curation pipeline. |
| D-31 | **`sport_mets_table` (Compendium-based)** — promotion candidate enabling a v3 MET-based activity multiplier path more precise than v1 phase × volume lookup in §5.2.2. | Low | 🟡 Deferred (post-v1) | 2E | Multiplier-based fallback used today. METs path is a v3 refinement. |
| D-32 | **HRT × BMR research** — current spec produces miscalibrated BMR for HRT athletes. Coaching flag surfaces the limitation; doesn't fix it. | Low | 🟡 Deferred | 2E | v2 candidate. Research-bound. |
| D-33 | **Beta blocker × Cardiac inference** — should beta blocker presence imply a Cardiac condition for HITL purposes, or require explicit condition record? v1 stays conservative. | Low | 🟡 Deferred | 2E | Defer until first-cohort data informs. Related to D-24 (broader medications surface). |
| D-34 | **Pregnancy status capture** — §B has no explicit pregnancy status field. 2E currently reads `is_pregnant` via free text / HRT class proxy. Either add to §B or treat as Health Condition record (Endocrine/Metabolic). | Med | 🟡 Deferred | 2E, Layer 1 | Onboarding v3 work. 2E has HITL gates dependent on this signal (stimulant × pregnancy, contra supp × pregnancy). |
| D-35 | **Per-discipline GI-risk classification** — currently sport-level via §5.4.3 sport modifier. Discipline-level would tighten race fueling format choice. | Low | 🟡 Deferred (post-v1) | 2E | Future FC. |
| D-36 | **§L.2 Role on Team enum** — deleted from `Athlete_Link_Entity_v2` during V2 spec design (Captain / Navigator / Pacer / Specialist taxonomy was replaced by Discipline Focus on Team). Iceboxed for post-launch consideration. AR Navigator role specifically has distinct training implications (navigation prep, mental load) that may warrant revisit if post-launch evidence of need emerges. | Low | ⚪ Wont-Fix (v1) | §L, future team-training spec | Per Andy 2026-05-11: do not track for v1. Revisit post-launch only if cohort feedback shows the role taxonomy meaningfully improves prescriptions for AR / multi-discipline teams. Functional substitute today: Discipline Focus on Team (L.2) captures which legs an athlete owns, which is the operational signal. |
| D-37 | **`exercises.injury_flags_text` source-data hygiene.** Audit reveals flag text contains content that doesn't describe athlete movement-constraint risk: equipment critiques ("machine poorly aligned"), physiological state ("Cardiac — HR spikes"), cognitive notes ("Cognitive — discipline to walk"), surface-tissue signals ("Skin — friction burns", "Blister — sock interface"), recovery-state notes ("sore in the morning"). These don't fit the B.3 11-token movement constraint enum but **carry real signal** (per Andy: skin/blister affect multiple movements) and **must not be dropped**. Surfaced 2026-05-11 during D-22 Pass 1 sampling. | Med | 🟡 Deferred (post-FC-1) | 2D, Layer 4 | Two-part scope: (1) classify the non-movement flag categories present in `injury_flags_text` (cardiac, cognitive, surface-tissue, recovery-state, equipment-criticism); (2) design and populate appropriate structured Layer 0 columns (`physiological_flags TEXT[]`, `surface_tissue_flags TEXT[]`, etc.) — exact schema TBD. **Coordinates with D-22 (movement_components curation)**: cleaner source means simpler downstream classification. **Out of scope:** the equipment-criticism category specifically can probably just be dropped — per Andy, equipment-quality calls belong to the athlete, not the plan-gen system.  |
| D-38 | **Wrist deviation under load** — canonical token gap. The 11 `movement_components` tokens cover wrist *extension* (WristExt) but not wrist *deviation* (ulnar/radial). Surfaced twice in D-22 Pass 2: EX126 Freestyle Pull (ulnar deviation stress at hand entry) and EX235 Tricep Pushdown/Extension (wrist deviation if grip angle wrong). Both force-mapped to `Pain above specific joint angle` (Angle), analogous to Pass 1's Rule 11 lateral-flexion-→-Instab force-mapping. | Low | 🟢 Cleanup | 2D, future v2 vocabulary | Two data points across 159 rows is below threshold for adding a 12th canonical token. Track for Layer 2 v2 vocabulary review. Current force-mapping to Angle documented in `D22_Pass2_Done_MigSQL_D23_Kickoff_Handoff_v1.md` §3 Calibration D-precedent / §2 Aerobic-Endurance + Strength sections. |
| D-39 | **`Vocabulary_Audit` v3 rewrite** — formal canonical body parts list update. Two cleanup items folded together: (1) `Collarbone` added to Section 1 Shoulder region per D-23 curation (2026-05-12); (2) "Total: 41 canonical body parts" header is stale — enumerated tables sum to 51 with Collarbone (50 before). Both surfaced during D-23 work. | Low | ✅ Resolved (FC-2, 2026-05-12) | Layer 1 v3, future vocab work | Vocabulary_Audit_v3.md shipped FC-2: Collarbone added to Section 1 Shoulder canonical list with D-23 attribution; total recount to 51 with per-subsection breakdown (3+5+11+5+6+3+7+5+4+2). "What changed in v3 vs v2" header documents the closure. Section 1 now matches deployed `body_parts_at_risk` vocabulary. Layer2D_Spec_v1 §5.4/§5.5 references updated from v2 → v3. |
| D-40 | **`terrain_gap_rules` schema dump.** Drift report §2.12 documents 12-column deployed table with 12 active rows but does not enumerate columns. ETL_Spec_v3_Corrections_2ABC_v2 §4.10a proposal had 5 cols, doesn't match deployed. Layer0_ETL_Spec_v4 §4.17 contains a placeholder schema block with a `\d layer0.terrain_gap_rules` action note. | Low | ✅ Resolved (FC-3, 2026-05-13) | 2B (terrain gap resolution) | **2026-05-13: D-40 closed.** Neon `\d layer0.terrain_gap_rules` enumerated 12 functional columns: `target_terrain_id`, `target_terrain_name`, `proxy_terrain_id` (nullable), `proxy_terrain_name` (nullable), `gap_severity`, `adaptation_weeks_low/high`, `proxy_fidelity`, `proxy_methods TEXT[]`, `uncoverable_stimulus TEXT[]`, `prescription_note`, `audit_log`. UNIQUE: `(target_terrain_id, proxy_terrain_id, etl_version)`. Nullable `proxy_terrain_id` permits multiple "uncoverable" rows per target. Layer0_ETL_Spec_v5 §4.17 replaces placeholder with full schema block. |
| D-41 | **`terrain_types` 7 enrichment columns enumeration.** Drift report §2.10 names them (`terrain_id`, `category`, `requires_elevation`, `technical_surface`, `environment`, `simulatable`, `simulation_note`) + secondary UNIQUE on `(terrain_id, etl_version)` but v3/v4 vocabulary §4.14 still references the 2-col v2 spec. 16 superseded rows out of 31 indicate hand-curation provenance worth investigating during the same schema-dump session. | Low | ✅ Resolved (FC-4a, 2026-05-13) | 2B (terrain mapping) | **2026-05-13: D-41 closed.** `information_schema.columns` retry returned 9 functional columns + 2 UNIQUE constraints. **Drift report correction:** `simulatable` is `TEXT`, not `BOOLEAN` as §2.10 claimed (permits 'yes'/'no'/'partial'/'conditional'/etc.). `terrain_id` is nullable; UNIQUE on `(terrain_id, etl_version)` permits multiple `terrain_id IS NULL` rows per `etl_version` — consistent with 16/31 hand-curation history. Layer0_ETL_Spec_v6 §4.14 replaces the 2-bullet drift note with full schema block. |
| D-42 | **`cross_sport_properties` deployed-columns reconciliation.** Drift report §2.7 says v3 baseline 6 cols + 4 extras (later 3 after D-14 drop). Batch D v2 §3 "Replacement column list" shows DIFFERENT functional columns (`property_type`, `value`, `unit`, `applies_to_sports`, `applies_to_disciplines` instead of v3's `description`, `scope`, `ranking_text`, `estimated_values`). v4 §4.8 took the drift report as authoritative + retained v3 baseline cols + 3 extras. | Low | ✅ Resolved (FC-3, 2026-05-13) | None currently | **2026-05-13: D-42 closed.** Neon `\d layer0.cross_sport_properties` confirmed v4 column list correct (v3 baseline 6 + `source_evidence` + `notes` + `confidence`). Batch D v2 §3's proposed alternate columns (`property_type`, `value`, `unit`, `applies_to_sports`, `applies_to_disciplines`) confirmed not deployed — drift report was authoritative. **Type correction:** `confidence` is `TEXT`, not `NUMERIC` as v4 spec had it. Column carries qualitative labels (e.g. "High", "Medium", "Low"), not numeric scores. Layer0_ETL_Spec_v5 §4.8 corrected. |
| D-43 | **Batch A and Batch C patch documents not in project knowledge.** Both referenced in Control_Spec_v1 §9 doc map and Batch D v2 §1 companion-docs line, but absent from project files at FC-2 draft time. Their substance was reconstructed from drift report v2 for v4. | Low | 🟢 Cleanup | None | If Batch A/C documents are recovered (likely in repo `etl/patches/` or similar), archive in project knowledge for audit history. No new spec change expected — drift report has the same content table-by-table. |
| D-44 | **`sport_name_aliases` UNIQUE deployed-vs-spec divergence.** v4 §4.16 specified UNIQUE on 2 cols `(exercise_db_sport, etl_version)` and claimed "one-to-one enforced inversely — an exercise_db_sport at a given etl_version maps to exactly one framework_sport." Surfaced during FC-3 pre-v4-lock verification. | Med | ✅ Resolved (FC-3, 2026-05-13) | §4.11 `sport_discipline_bridge` derivation | **2026-05-13: D-44 opened and closed.** Neon `pg_constraint` query revealed deployed UNIQUE is 3 cols `(exercise_db_sport, framework_sport, etl_version)`. Per §6.6, deployed wins. Layer0_ETL_Spec_v5 §4.16 corrected; "one-to-one inverse" claim retracted. **Consumer impact:** §4.11 bridge derivation must treat the alias join as one-to-many. If any active `exercise_db_sport` maps to multiple `framework_sport` values, the bridge build will multiply rows. Follow-up D-46 below. |
| D-45 | **`Layer0_ETL_Spec` §5.2 signatures diverged from per-layer specs.** v4 §5.2 had 2A/2B/2E as 1–2-param stubs vs 4–8 in per-layer spec; FC-2 "corrections" for 2C/2D had wrong shape (2C used flat athlete-equipment vs locale-keyed; 2D pre-partitioned status externally vs internal §5.1 partition). Surfaced during FC-3 §5 narrative refresh. | Med | ✅ Resolved (FC-3, 2026-05-13) | All Layer 2 query implementation | **2026-05-13: D-45 opened and closed.** Layer0_ETL_Spec_v5 §5.2 rewritten to mirror each per-layer spec verbatim with "Mirror of LayerXX_Spec §3" attribution. Formalizes §6.6 spec-of-spec rule at signature level. Prevents FC-2-style misreading from recurring. v5 §5.2 includes a reconciliation log showing each diff. |
| D-46 | **`sport_discipline_bridge` row-multiplication audit.** Follow-up from D-44. If any active `exercise_db_sport` in `layer0.sport_name_aliases` maps to multiple `framework_sport` values at the same `etl_version`, the §4.11 bridge derivation will produce multiplied rows (one per alias mapping). Need to determine whether this is intentional curation (e.g., a sport like "Trail Running" legitimately belongs to multiple framework sports) or accidental dict duplication in `sport_name_aliases.py`. | Med | ✅ Resolved (FC-4a, 2026-05-13) | §4.11 bridge derivation, 2A consumption | **2026-05-13: D-46 closed.** Active-row COUNT returned 21 multi-mapped `exercise_db_sport` values. Pattern confirmed **intentional** for framework sub-format splitting (Triathlon → 5 sub-formats; Swimming → 11 contexts; SkiMo/Rowing/XC Skiing → sub-format variants; General Conditioning → 38 broadly-applicable framework sports). No `sport_name_aliases.py` tightening required. Multi-mapping table documented in Layer0_ETL_Spec_v6 §4.16. Downstream consequence: bridge produces multiplied rows for the same `(exercise_id, discipline_id)` pair when a discipline is shared across multi-mapped framework sports — consumer dedup requirement now documented in §4.11. Layer 2D §5.2 already dedups by `exercise_id` (functional behavior correct); rationale-comment update for that spec tracked separately as D-47. |
| D-47 | **Layer 2D §5.2 SQL rationale comment incomplete.** Comment cites multi-discipline path as the reason for post-query dedup by `exercise_id` but does not cite the framework-mapping multiplication path documented in Layer0_ETL_Spec_v6 §4.11 (multi-mapped `exercise_db_sport` produces multiple bridge rows for the same `(exercise_id, discipline_id)` pair). Functional behavior is correct (dedup-by-exercise_id handles both paths); only the explanatory comment is incomplete. Generalizes: any new consumer joining through `sport_discipline_bridge` needs to know about both paths. | Low | 🟢 Cleanup | Layer 2D, future consumers of `sport_discipline_bridge` | At next Layer 2D revision (or any FC pass that touches Layer2D_Spec), update §5.2 rationale comment to cite both paths. Suggested wording: "An exercise may appear in the join output multiple times for two reasons: (1) it maps to multiple included disciplines, and (2) its `exercise_db_sport` maps to multiple `framework_sport` values whose disciplines overlap (`Layer0_ETL_Spec` §4.11 multiplication property). Post-query dedup by `exercise_id` handles both; track `discipline_ids[]` per exercise for risk attribution." |
| D-48 | **Per-provider data-shape filling for Strava/Whoop integration tables.** Schemas not yet specified at `Athlete_Data_Integration_Spec` v1. Strava activity data may flow into `cardio_log` only (with `cardio_log.strava_activity_id`) if shape fits; Whoop recovery/strain/sleep may need per-provider tables analogous to Polar. Track as integration deploys per provider. | Low | 🟡 Deferred | `Athlete_Data_Integration_Spec` §5 | Not blocking Layer 3 spec writing — Strava/Whoop can be "TBD" in integration spec at first. Pin down when those providers move from stub to live ingestion. |
| D-49 | **3C conflict-rule enumeration revisit.** L3-Discovery enumerated 7 initial cross-node conflict rules for 3C (discipline × no equipment, discipline × terrain gap, etc.). If real testing surfaces patterns that don't fit a deterministic shape, revisit 3C as an LLM finishing step. Default for v1: query/rules only. | Low | 🟡 Deferred | Layer 3C | Revisit post-deployment. Watch during 3A/3B/3D testing for whether conflict-detection coverage is meaningful with rules-only. |
| D-50 | **Phase 1 integration deployment** — schema migration + app code promotion (this repo, root) for `provider_auth`, `webhook_events`, `polar_*`, `wahoo_*`, `coros_*`, plus new columns on `cardio_log` and `training_log` per `Athlete_Data_Integration_Spec` §4–§6. **Schema shipped 2026-05-14** (v14): all 10 tables + 7 ALTER columns + 6 indexes appended to both `_SQLITE_MIGRATIONS` and `_PG_MIGRATIONS`. **PR1 (commit `3628ca6`):** `provider_auth.py` helper + COROS OAuth + `coros_ingest.py` + D-58 schema (`athlete_profile_field_provenance`, `account_nudges`, `disclosure_acknowledgments`). **PR2 (commit `686bb40`):** D-59/60/61 schema (`daily_availability_windows`, `gym_profiles`, `locale_equipment_overrides`, `locale_toggle_overrides`, 13 `locale_profiles` columns, `chain_registry.py`). **PR3 (merge `b819f0a`):** Polar OAuth + webhook + transaction-fetch ingestion (`routes/polar.py`, `routes/polar_ingest.py`) + three `cardio_log` partial-UNIQUE indexes (`polar_exercise_uidx`, `coros_label_uidx`, `wahoo_workout_uidx`) closing the PR1 §6 race window. **PR4 (merge `f4d2e75`):** Account Config 1 Connections management tab on `/profile` (status / connect / disconnect / re-auth / `?<provider>_connected=1` passive-prefill prompt per v5 §A.2.7) + `provider_auth.disconnect` helper. **PR5 (merge `34637d2`):** v5 Step-2 onboarding connect screen at `/onboarding/connect` (reuses PR4's `CONNECTION_PROVIDERS` + `load_connections` registry; new-user post-register redirect points there) + drive-by CSP nonce fix on PR4's tab-activation script. **PR6 (merge `2c8d01f`):** Layer-1 athlete-profile column foundation closing Open Item #17's blocking dependency — `body_weight_kg`, `hrmax_bpm`, `lactate_threshold_hr_bpm`, `vo2max`, `cycling_ftp_w` added to `athlete_profile` (v5 §A.2.1 prefill-eligible §F + §A fields) + manual self-report form fields under `/profile?tab=profile` + `_record_self_report_provenance` writes to `athlete_profile_field_provenance` (PG-only, `source='self_report'`; D2 PRs add the `manual_override` flip when prefill ships). Bundles G — `coros_ingest._ingest_activity` ON CONFLICT cleanup against PR3's `cardio_log_coros_label_uidx`. **PR7 (merge `df17f08`):** D2a read-side prefill UX — `KNOWN_PROFILE_FIELDS` registry (`routes/profile_fields.py`, closes Open Item #17) + provider extractors (`routes/profile_extractors.py`, 2 real HRmax-from-`cardio_log` 90-day-window aggregates + 8 stubs for fields no provider currently ingests) + `/onboarding/prefill` GET route + comparison-page template + Step-2 Continue→`/onboarding/prefill` flip (Skip preserved at `/profile?tab=athlete`). **PR8 (merge `43ccedf`):** D2b write-side prefill UX — POST handlers at `/onboarding/prefill/<field>/use-provider` (writes winning candidate's value + `source='provider_<slug>'`) and `/keep-current` (writes `source='manual_override'`); refactor of `routes/profile.py:_record_self_report_provenance` to flip `'provider_*'` → `'manual_override'` when the athlete types over a provider-sourced value (v5 §A.2.3 stickiness); inline "Use {provider} value instead" clear-override link beneath `'manual_override'` badges (v5 §A.2.6 clear path). App-level field-name validation via `_lookup_field` 404 + `PREFILL_ELIGIBLE_FIELDS` guard — no schema CHECK migration. **PR9 (merge `7cb786a`):** Option E — 14-day connect-provider nudge (v5 §A.2.4). New blueprint `routes/nudges.py` with `GET /cron/nudges/connect_provider_14d` (token-gated daily scanner — `Authorization: Bearer $CRON_SECRET`; single `INSERT … SELECT … FROM users WHERE created_at <= NOW() - INTERVAL '14 days' AND NOT EXISTS active provider_auth AND NOT EXISTS prior nudge … ON CONFLICT DO NOTHING RETURNING id` — idempotent) and `POST /nudges/<int:nudge_id>/dismiss` (writes `dismissed_at`, scoped to current user). New partial `templates/_account_nudges.html` slotted into `templates/base.html` between flashes and `{% block content %}`. New context processor `_inject_active_nudges` in `app.py` exposes `active_nudges` from `routes.nudges.get_active_nudges` on every request. `vercel.json` gains a `crons` entry at `0 14 * * *` (daily 14:00 UTC, Vercel-Hobby-tier-safe daily granularity). Deep-link target is PR5's `/onboarding/connect`. PG-only — `account_nudges` is in `_PG_MIGRATIONS` only; SQLite dev returns empty `active_nudges` and the scanner returns `{inserted: 0}`. **PR10 (merge `0b7d4b6` — PR42 + same-day addenda PRs #43–#45):** Option D3a — Mapbox-anchored locale creation + chain detection (v5 §A.2 + D-59 §3/§4/§5/§6/§8). New `mapbox_client.py` module wrapping the Mapbox Geocoding API (`/geocoding/v5/mapbox.places/{q}.json`) with two surfaces: `search_places(query)` (autocomplete; `autocomplete=true, types=poi,address, limit=5`) and `search_nearby(query, lng, lat, radius_km=42.2)` (proximity-bbox over the 42.2 km marathon-radius). Token via `MAPBOX_PUBLIC_TOKEN` env var (D-59 §3.2); 1-retry on 5xx with 1s backoff (D-59 §3.4 row 3); typed exception hierarchy `MapboxError` → `MapboxTokenMissing` / `MapboxAPIError` / `MapboxNoResults` for inline render in routes. `routes/locales.py` extended: `GET /locales/new` (search form + disclosure-gated when `disclosure_acknowledgments` lacks `mapbox_geocoding_consent v1`); `POST /locales/new` (INSERT with `chain_registry.detect_chain()` lookup; chain-hit → redirect to nearby picker, no chain → category derived from Mapbox `properties.category` hint per D-59 §4.2 step 3); `POST /locales/new/manual` (D-59 §6 manual fallback — `manual_entry=TRUE`, no coords, no chain); `POST /locales/new/acknowledge` (writes consent row into existing `disclosure_acknowledgments` table — no schema change); `GET/POST /locales/<slug>/nearby` (D-59 §5 — re-fetches Mapbox, filters same-chain via `detect_chain()`, excludes anchor by mapbox_id, INSERT one row per opt-in checkbox). Existing `edit_profile(<locale>)` extended to accept athlete-created slugs (existing-row check) alongside legacy `home/hotel/partner/airport` enums. `templates/locales/new.html` (disclosure card + search + results picker + manual fallback under one template); `templates/locales/nearby.html` (checkbox picker for same-chain Mapbox results); `templates/locales/list.html` updated to render athlete-created rows alongside the 4 legacy enums + "+ New locale" button. `chain_registry.py` consumed unchanged (PR2-shipped). `init_db.py` unchanged — `disclosure_acknowledgments` (PR1 D-58 batch) + `locale_profiles` Mapbox/chain columns (PR2 D-59 batch) already exist. No JS — every action is a real form POST per the no-JS-required posture maintained since PR5. PG-only disclosure check; SQLite dev short-circuits to "acked" so local probes don't gate. **PR11 (merge `5629a25`):** Option D3b — closes the v5 §J locale work on top of D3a. Four pieces: (1) **`locale_equipment_overrides` / `locale_toggle_overrides` FK correction** (PR2-batch bug fix). Original CREATE TABLE used `locale_id INTEGER NOT NULL REFERENCES locale_profiles(id)` but `locale_profiles`' PK is composite `(user_id, locale)` — there is no `id` column. PG rejected the inline FK; the migration runner's try/except swallowed the error so the tables never actually got created in production. PR11 rewrites the CREATE TABLE statements with `locale TEXT NOT NULL` + composite `FOREIGN KEY (user_id, locale) REFERENCES locale_profiles(user_id, locale) ON DELETE CASCADE` + UNIQUE on (`user_id`, `locale`, `equipment_tag`, `action`) — plus an idempotent in-place ALTER guard for any partial-shape table from a prior boot. (2) **D-60 §4.2/§4.3/§4.4 inherit/override UI for the seven shared-profile categories** (commercial_chain_gym, independent_gym, hotel_gym, climbing_gym_chain, climbing_gym_indie, pool_indoor, pool_outdoor). `routes/locales.py` gains `_find_gym_profile` / `_shared_equipment_set` / `_load_overrides` / `_effective_equipment` / `_save_overrides` / `_create_gym_profile` / `_link_gym_profile` / `_touch_gym_profile_confirmation` helpers + `_edit_shared_locale` branch from the existing `edit_profile`. First athlete at a `mapbox_id` builds the `gym_profiles` row from their submitted equipment list (`mode='shared_build'`); subsequent athletes inherit the shared profile and write per-athlete deltas to `locale_equipment_overrides` (`action ∈ ('add','remove')`) with effective view = `(shared ∪ adds) ∖ removes`. `templates/locales/form.html` rewritten with three modes (legacy / shared_build / shared_inherit) showing `+ override` / `– override` / `shared` per-tag badges in inherit mode. Legacy enums (home/hotel/partner/airport) + manual-entry rows + no-shared-profile categories (home_gym / outdoor_park / other_residence) fall through to the existing per-athlete `locale_equipment` flow unchanged. (3) **D-59 §6 manual→Mapbox upgrade** via `/locales/new?upgrade=<slug>` — `_save_mapbox_anchored` taught about the `upgrade_slug` form field; on POST UPDATEs the existing row in place (slug + equipment + overrides preserved) and flips `manual_entry=FALSE`. `templates/locales/new.html` threads `upgrade_slug` through search + result forms + disclosure ack; "Look up on map" affordance on `templates/locales/form.html` for `manual_entry=TRUE` rows. (4) **D-59 §7 on-demand refresh** via `POST /locales/<slug>/refresh` — re-fetches Mapbox by stored name + proximity, matches by `mapbox_id`. Silent UPDATE of `place_payload` + `place_fetched_at` when name + chain unchanged; renders `templates/locales/refresh_confirm.html` confirmation prompt when chain or name changed (Yes applies the change, No keeps current). `templates/locales/list.html` adds a ⟳ Refresh button per athlete-created Mapbox-anchored row. `MANUAL_CATEGORIES` realigned to D-60 §3's full 10-category taxonomy (`commercial_chain_gym`, `independent_gym`, `hotel_gym`, `climbing_gym_chain`, `climbing_gym_indie`, `pool_indoor`, `pool_outdoor`, `home_gym`, `outdoor_park`, `other_residence`). PG-only — D-60 helpers gate on `database._is_postgres()`; SQLite dev falls through to the legacy `locale_equipment` flow for every locale. Defers: §J.3 sport-specific gear toggle UI (Andy's call — these aren't rigidly tied to location, want a separate form), D-60 dispute flow (§4.6), D-60 submit-as-correction (§4.5), account-level + per-locale sharing opt-out (§4.7), gym-profile sharing-consent disclosure (§4.7). **PR12 (this revision — merge pending):** Option A1 — D-61 §G + onboarding-step integration. New `/onboarding/schedule` route (Step 3b) slotted between `/onboarding/prefill` and `/profile?tab=athlete`. Per-day windows persist to the `daily_availability_windows` table (PG-only — table is in `_PG_MIGRATIONS` only per Integration v4 §2.5; SQLite dev silently skips the per-day-window upsert) via two new helpers on `athlete.py` (`get_daily_availability_windows`, `upsert_daily_availability_windows`). 5 new orthogonal-capacity columns added to `athlete_profile` (`long_session_available BOOLEAN`, `long_session_days TEXT`, `long_session_max_hr SMALLINT`, `doubles_feasible TEXT`, `preferred_rest_days TEXT`) on both cold-start schemas + the redundant CREATEs in the migration lists + `_PG_MIGRATIONS` hot ALTERs — full PR6 prefill-column pattern. New template `templates/onboarding/schedule.html` (7 day-rows with `enabled` + earliest-start + duration + optional second window gated on Doubles Feasible; long-session day-picker cross-validated subset-of-enabled at POST; preferred rest day multi-select; derived weekly-hours total displayed via small client script — form is no-JS-submittable, JS is purely cosmetic). `_POST_STEP3_TARGET` flipped to `/onboarding/schedule`; prefill template's Continue copy flipped "Continue to profile" → "Continue to schedule". JIT session-card swap UI explicitly out of scope (gates on Layer 4 spec) per PR11 §5.1 Option A1. Per-day-windows surfacing on `/profile?tab=athlete` for post-onboarding edits deferred to a follow-on PR (5-file ceiling). **PR13 (this revision — merge pending):** SQLite + TrueNAS retirement. Not a D-50 feature advance — a stack-cleanup PR triggered by Andy's 2026-05-16 question "we discussed stopping sqlite development a while back, but we keep working on it, why?" Strips `database.py`/`init_db.py`/`app.py`/`routes/status.py` to PG-only; removes 13 `_is_postgres()` runtime guards across 6 route files; deletes `Dockerfile`/`docker-compose.yml`/`deploy/truenas_setup.sh`/`deploy/update.sh`. Doc updates: `aidstation-sources/CLAUDE.md` Stack + Operating context PG-only, D-54 → ✅; root `DEV_SETUP.md` PG-only setup; root `DATABASE.md` overview/architecture rewritten + deep sections marked historical; root `HANDOFF.md` prefaced with stale-context marker. D-54 status flipped 🟡 → ✅ Resolved; new D-65 added ✅ Resolved (TrueNAS retire). See D-54 / D-65 rows below and `V5_Implementation_PR13_Closing_Handoff_v1.md`. **PR14 (this revision — doc sweep, no D-row status flips):** PR13-deferred doc-cleanup follow-on. `Catalog_Migration_Plan_v2.md` → `_v3.md` (SQLite retirement decoupled from §1 + §5 decision #5 ✅ Resolved); `Athlete_Data_Integration_Spec_v4.md` → `_v5.md` (§2.5 SQLite freeze flagged Retired with historical context preserved); `aidstation-sources/DATABASE.md` (stale duplicate of root) collapsed to thin redirect; root `DATABASE.md` PR13 marker strengthened with inline `[STALE — SQLite path retired PR13]` flags on the four historical SQLite subsections; CLAUDE.md pointers bumped (Integration v5, Catalog plan v3, backlog v27, last-shipped narrative). No code changes; no D-row status flips. See `V5_Implementation_PR14_Doc_Sweep_Closing_Handoff_v1.md`. | Med | 🟢 PR1–PR11 shipped 2026-05-14/15 (commits `3628ca6`, `686bb40`, `b819f0a`, `f4d2e75` PR4-merge, `34637d2` PR5-merge, `2c8d01f` PR6-merge, `df17f08` PR7-merge, `43ccedf` PR8-merge, `7cb786a` PR9-merge, `0b7d4b6` PR10-merge — PR42 plus same-day addenda PRs #43–#45, `5629a25` PR11-merge, `3232c68` PR12+PR13 combined merge (PR #48), `35958e1` PR13 workflow-cleanup follow-on merge (PR #49)); 🟢 frontend D1 (Step-2 connect) + D2a (read-side prefill UX) + D2b (write-side prefill UX + manual_override stickiness + clear-override path) + E (14-day connect-provider nudge background job + dismissable in-app banner) + D3a (Mapbox-anchored locale creation + chain detection + manual fallback + nearby same-chain picker + disclosure ack) + D3b (D-60 shared-`gym_profiles` inherit/override UI + D-59 §6 manual→Mapbox upgrade + D-59 §7 on-demand refresh + PR2-batch FK fix) + D-61 Option A1 (§G per-day-windows onboarding form + Step-3b routing + 5 athlete_profile orthogonal-capacity columns + `daily_availability_windows` read/write helpers) + PR13 stack cleanup (SQLite path retired + TrueNAS Docker artifacts deleted; PG-only via `DATABASE_URL`) shipped; 🟡 D-61 JIT swap session-card UI (gates on Layer 4) + F refresh-on-401 + H more providers + D2c bulk apply + telemetry on nudge (display/dismiss/act-on rates) + §J.3 sport-specific gear toggle UI + D-60 dispute/submit-as-correction/opt-out/sharing-disclosure pending | Production (Vercel `aidstation-pro.vercel.app` + TrueNAS Docker), AIDSTATION Wave-1 integration | Independent track. Lives in this repo (root `init_db.py`, `mapbox_client.py`, `routes/`, `app.py`, `templates/profile/`, `templates/onboarding/`, `templates/locales/`, `templates/_account_nudges.html`, `vercel.json`). See `V5_Implementation_PR12_Closing_Handoff_v1.md` §5.1 for the PR13+ candidate menu carrying forward: **Layer 4 plan-gen spec draft** (now the gate for the D-61 JIT swap session-card UI, D-63 on-demand workout, D-64 plan refresh tiers, and the rest of the plan-execution surface — substantial multi-session work; spec-first); **D-61 follow-on** surface per-day windows on `/profile?tab=athlete` for post-onboarding edits (small partial-template wire-up; doesn't need Layer 4); **Option D-60 closeout** dispute flow (§4.6) + submit-as-correction (§4.5) + account-level + per-locale sharing opt-out (§4.7) + gym-profile sharing-consent disclosure (§4.7) — premature at N=1 athlete; lands when cohort grows; **Option §J.3 toggles UI** sport-specific gear readiness toggles on a separate form (Andy's framing — these are athlete-attribute-like, not gym-attribute-like); **Option F** Polar refresh-on-401 watch item; **Option H** provider blueprint roster expansion; **Option D2c** bulk "Apply all" affordance + tolerance-based re-prefill (v5 §A.2.7); **Option E-telemetry** display / dismiss / act-on rates on the connect-provider banner (Open Item #18 from `Athlete_Onboarding_Data_Spec_v5.md`). Recommended sequence: Layer 4 spec draft next (it's the gate for D-63, D-64, and D-61's JIT swap — substantial spec work); D-61 follow-on profile-tab editing in parallel (small, no Layer 4 dependency); D-60 closeout + §J.3 toggles UI when cohort grows past Andy; F as a watch item; H as opportunistic per-provider PRs; D2c + E-telemetry picked up after first real production traffic. Pre-deploy verification (PR1 §5.0 + PR3 §5.0 + PR5 §5.0 + PR6 §5.0 + PR7 §5.0 + PR8 §5.0 + PR9 §5.0): all carry-forward; see PR9 §5.0 + PR10 §5.0 for the merged list. **PR10-specific pre-deploy:** (a) generate Mapbox public token (`pk.*`) at mapbox.com → set `MAPBOX_PUBLIC_TOKEN` env var on Vercel + TrueNAS `.env`; (b) browse `/locales/new` while logged in: expect disclosure card on first visit with Acknowledge button + manual entry link; (c) click Acknowledge → `psql`: `SELECT * FROM disclosure_acknowledgments WHERE disclosure_id='mapbox_geocoding_consent'` shows row; (d) search e.g. "Planet Fitness Minneapolis" → expect ≤5 result cards each with a name input + Save button + correct hidden mapbox_id/lat/lng/text/place_name fields; (e) Save → row in `locale_profiles` with `chain_id='planet_fitness'`, `chain_name='Planet Fitness'`, `category='commercial_chain_gym'`, `mapbox_id`, `lat`, `lng`, `place_payload` JSON populated; (f) chain-hit → expect redirect to `/locales/<slug>/nearby` with same-chain Mapbox results; opt-in checkboxes → POST inserts each as a new locale row; (g) non-chain place (e.g. a hotel) → no nearby redirect; row has `chain_id IS NULL`, `category` either `independent_gym` or NULL based on Mapbox category hint; (h) `/locales/new?manual=1` → manual form; Save → row with `manual_entry=TRUE`, `mapbox_id IS NULL`, `lat IS NULL`, `lng IS NULL`; (i) `/locales` list shows the 4 legacy enums plus athlete-created locales with chain/category badges; Edit on athlete-created row works (the existing-row gate accepts non-enum slugs); (j) revoke `MAPBOX_PUBLIC_TOKEN` on Vercel → `/locales/new` search shows "Place lookup is not configured on the server" inline; manual fallback still works. **PR11-specific pre-deploy:** (a) inspect `locale_equipment_overrides` + `locale_toggle_overrides` shape post-deploy: `\d locale_equipment_overrides` in `psql` should show `locale TEXT NOT NULL` column (not `locale_id INTEGER`) and a `FOREIGN KEY (user_id, locale) REFERENCES locale_profiles(user_id, locale)` constraint — confirms the FK fix landed; (b) create or edit an athlete-created Mapbox-anchored locale (e.g. an Anytime Fitness from PR10) — expect three new affordances: ⟳ Refresh button on `/locales`, "Look up on map" button on `/locales/<slug>/edit` for manual rows, and an inherit/build UI on the edit screen for shared-profile categories; (c) first-time edit on the chain-anchored locale renders "First athlete at this gym — build the equipment profile" alert; check several items + Save; `psql`: `SELECT * FROM gym_profiles WHERE mapbox_id=<the row's mapbox_id>` shows a row with `equipment` as a JSON array of checked tags, `created_by_user_id=<uid>`, `contribution_count=1`; `locale_profiles.gym_profile_id` for the edited row now points at the new id; (d) re-edit the same locale → form renders "Inherited from the shared profile" alert with last_confirmed_at + contribution_count; uncheck one inherited item and check a new one + Save; `psql`: `SELECT * FROM locale_equipment_overrides WHERE user_id=<uid> AND locale=<slug>` shows two rows — one with `action='remove'` for the unchecked tag, one with `action='add'` for the new tag; (e) on a `manual_entry=TRUE` locale, click "Look up on map" → goes to `/locales/new?upgrade=<slug>`; search + Save this → `psql`: same row UPDATEd with `manual_entry=false`, `mapbox_id` populated, `lat`/`lng` populated, slug unchanged; chain hit redirects to `/locales/<slug>/nearby` as in PR10; (f) on a Mapbox-anchored locale, click ⟳ Refresh on the list view → if nothing changed, info flash "Refreshed … — no changes detected" + back to list; if name/chain changed (rare unless Mapbox data shifted), renders the confirmation prompt showing old vs. new values + Yes/No buttons; (g) regression: PR10 §5.0 steps for legacy locales (home/hotel/partner/airport) + non-shared athlete-created locales (home_gym / outdoor_park / other_residence categories) — edit form still renders the simple `locale_equipment` checkboxes (no inherit alert, no shared-tag badges); (h) regression: nudges + provider connections + prefill flows continue to work end-to-end on `/profile?tab=athlete` and `/onboarding/prefill`. |
| D-51 | **Layer 1 §A-§L field-by-field inventory against `public.*` existing tables** — what's already there, what needs new columns/tables, what's onboarding-only. **Scoped larger than originally estimated** after L3-Spec-Trio inventory revealed substantial gaps: most of §C (Years of Training, Secondary Sports, Discipline Weighting, Peak Volume, Pack Load History, Previous Coaching), all of §E benchmarks, most of §F testing baselines, §G schedule, §H multi-event substructure, most of §I lifestyle fields, §L Athlete Network all need new storage. Conditions-log covers §B injuries but not chronic health conditions. | High | 🟡 Deferred | Layer 1 v4+ (onboarding tables); blocks full `q_layer1_payload` implementation | See `Athlete_Data_Integration_Spec` §7.6 for full gap summary. Likely 2-3 sessions to design the new tables/columns. Coordinates with D-52 (catalog migration) since some Layer 1 fields reference catalog data. |
| D-52 | **Catalog Migration Plan: Vercel app reads migrate from `public.*` to `layer0.*`.** Confirmed Option A 2026-05-13. Affects `exercise_inventory`, `equipment_items`, `exercise_equipment`, possibly `training_modalities`. Per-user FK references (training_log.exercise_id, locale_equipment.equipment_id, etc.) must transition. Non-trivial multi-phase migration. | High | 🟡 Deferred | Vercel app, all Layer 2 catalog reads (post-migration) | Spec doc: `Catalog_Migration_Plan.md` v1 shipped 2026-05-13. Strategy-level; per-phase execution detail to be added as each phase approaches. 5 phases — Verification + mapping → Alias tables + sync ETL → Route-by-route migration → Per-user FK migration → Drop `public.*` catalogs. Sub-items D-52.1 through D-52.7 inside the plan. Promote to top-level D-NN rows when Phase 1 starts. |
| D-53 | **Heat acclim state derivation logic.** Confirmed 2026-05-13: not stored as a profile field. Derived at read time from `public.conditions_log.temp_f` history + future integration-sourced ambient temp + §J locale climate context. Owned by Layer 2E consumer or plan-gen. | Med | 🟡 Deferred | 2E, Plan Management | Replace Control_Spec §3 line for "heat_acclim_state per event" implementation. Derivation rules: rolling window over conditions_log (e.g., last 14 days of training in temp_f > 25°C); confidence band per coverage; fallback to "unknown" when data sparse. Design when 2E gets next revision or when integration ambient temp data lands. |
| D-54 | **SQLite backend deprecation.** Confirmed 2026-05-13: dual-backend pattern in `init_db.py` collapses to PG-only. Originally queued as Catalog Migration Plan Phase 5; pulled forward to PR13 on 2026-05-16 after Andy flagged that the dual-maintenance pattern was bleeding into every session despite his earlier "stop" call. | Med | ✅ Resolved 2026-05-16 (PR13) | Vercel app `init_db.py`, `database.py` adapter, every route consuming `_is_postgres()` | **Resolution (PR13):** `database.py` stripped to PG-only — drops `sqlite3` import, `_is_postgres()`, `sqlite_path()`, `_on_vercel()`; `get_db()` raises `RuntimeError` if `DATABASE_URL` is unset. `init_db.py` lost `SQLITE_SCHEMA`, `_SQLITE_MIGRATIONS`, `init_sqlite()`, and 5 sqlite_master-driven migration helpers; two seed helpers lost their `is_postgres=False` branches. `app.py` always runs `init_postgres()`. 13 `_is_postgres()` runtime guards stripped across 6 route files. `now_sql = 'NOW()' if database._is_postgres() else "datetime('now')"` patterns inlined to `NOW()`. Catalog Migration Plan §5 footnote owed: Phase 5's "drop SQLite path" deliverable already shipped — Phase 5 collapses to "drop the `public.*` legacy catalogs" only. Athlete_Data_Integration_Spec_v4 §2.5 SQLite freeze retired by definition; the freeze was forward-looking guidance that no longer applies. See `V5_Implementation_PR13_Closing_Handoff_v1.md` §2. |
| D-55 | **Garmin built onto `provider_auth`** (reframed v11; **paused v12**). Confirmed 2026-05-13: the Garmin connector never functioned in production — there is no live data and no Garmin-connected users. Plan: drop legacy `garmin_auth` table as cleanup; build Garmin onto unified `provider_auth` shape with new `session_blob TEXT` column for `garth` session JSON. **Paused 2026-05-14:** Garmin has temporarily closed new API access. We cannot build a working Garmin integration until access reopens. Until then, this row is "design decision locked, implementation paused." | Med | ⏸ Paused (was Deferred) | `routes/garmin.py`, `garmin_connect.py` (this repo, root); `PROVIDERS_SCHEMA.md` §5.1, §7 reconciled in same v12 pass to align with drop-and-rebuild plan | Build order per `Athlete_Data_Integration_Spec_v3` §2.3 (build-from-scratch, not migration; **deferred until Garmin reopens**): (1) deploy `provider_auth` with `session_blob`; (2) drop `garmin_auth` from `init_db.py` and from the deployed DB; (3) build/refactor `routes/garmin.py` and `garmin_connect.py` against `provider_auth` from the start; (4) wire `garth` session capture/refresh against `provider_auth.session_blob`. Coordinate with D-50 (Phase 1 integration deployment). D-50 itself is NOT paused — `provider_auth` ships for the other providers (Polar, Wahoo, COROS, etc.) independent of Garmin. |
| D-56 | **`cardio_log` schema additions for AIDSTATION needs.** AIDSTATION-pipeline consumers want `is_race BOOLEAN` (to filter "Recent Race Results" per §C onboarding spec), `start_time TEXT` (Night Running detection per §D.1), and possibly more discovered during 3A spec writing. | Low | 🟡 Deferred | `cardio_log` schema, 3A consumer | Surfaced during `Athlete_Data_Integration_Spec` §7 field-mapping draft. Capture additional needs as 3A and 3B specs are written. Single migration batch when accumulated. |
| D-57 | **Periodic research re-evaluation cadence per science-based area.** AIDSTATION cites research across multiple domains (periodization, strength programming, nutrition + fueling, hydration + electrolytes, recovery + sleep, HRV-guided training, injury risk, ACWR thresholds, environmental physiology, supplementation, gut training, climbing prerequisites, etc.). Research evolves at different rates per domain (supplementation literature shifts quickly; fundamental periodization shifts slowly). Need a defined cadence for re-reading the literature in each domain, updating Layer 0 reference data (sport rule sets, exercise database notes, nutrition baselines) where evidence has changed, and flagging atrophied or superseded citations. | Med | 🟡 Deferred | Layer 0 reference data quality; coaching credibility | Design work: enumerate the science domains AIDSTATION cites; for each, define a review cadence (e.g., supplementation = annual; periodization fundamentals = every 3 years; ACWR = annual until consensus stabilizes), an owner (or rotation), and a tracking mechanism (issue tracker entries with due dates; review-log entries on each spec/data asset). Output: `Research_Review_Cadence.md` and tracker scaffold. Triggers Layer 0 ETL re-runs when domain content changes materially. |
| D-58 | **Account/integration connections precede onboarding data entry.** OAuth-first flow: athlete connects providers before manual onboarding data entry; onboarding fields prefill from integration data where available; self-report becomes fallback. | High | ✅ Design complete (2026-05-14); 🟡 Implementation pending | Onboarding flow (§A flow); Athlete_Data_Integration_Spec §8 (two-regime consumer model unchanged); Layer 1 sourcing; **D-50 wiring unblocked** | **Design shipped 2026-05-14:** `Onboarding_D58_Design_v1.md` (9 decisions; new tables `athlete_profile_field_provenance` + `account_nudges`; provider-prefill mechanics with most-recent-wins + edit-in-place + manual_override stickiness; 14-day soft nudge; per-field opt-in re-onboarding prompt). **Consolidated into `Athlete_Onboarding_Data_Spec_v5.md`** §A flow framing + §A.2 prefill mechanics + Account Config 1 reframe + Account Config 3 new disclosure_ids. **Implementation:** v5 onboarding implementation PR + D-50 wiring resumption (now unblocked). PR1 of D-50 wiring scope updated: `provider_auth.py` helper + first real OAuth flow (COROS recommended) + COROS webhook recording + D-58 connect-step frontend integration. |
| D-59 | **Location profiles via place lookup + chain-membership lookup.** Onboarding §J Locale Profile creation flow: place lookup + chain detection + manual fallback + privacy disclosure. **Provider choice changed during design:** Mapbox (not Google) for cost predictability per Andy's call. | Med | ✅ Design complete (2026-05-14); 🟢 Implementation fully shipped — D3a (PR10) + D3b §6 manual→Mapbox upgrade + §7 on-demand refresh (PR11) | Onboarding §J.1; `locale_profiles` schema; `chain_registry.py` module (PR2); `mapbox_client.py` (PR10); `routes/locales.py` (PR10 + PR11) | **Design shipped 2026-05-14:** `Onboarding_D59_Design_v1.md` (8 decisions; Mapbox Geocoding API; hybrid registry + API chain detection; 42.2 km / 26.2 mi proximity-and-surfacing radius; on-demand refresh; manual address fallback as first-class path; one-time privacy disclosure). **Consolidated into `Athlete_Onboarding_Data_Spec_v5.md`** §J.1 + §J.1.1–§J.1.5. **Implementation:** PR2 shipped `chain_registry.py` + 9 `locale_profiles` ALTER columns (`mapbox_id`, `lat`, `lng`, `chain_id`, `chain_name`, `category`, `manual_entry`, `place_payload`, `place_fetched_at` + `locale_name`). PR10 shipped `mapbox_client.py` (Search Box API forward + proximity bbox calc, post-mid-session-fix) + `routes/locales.py` extensions for `/locales/new` (Mapbox search + chain detect + INSERT), `/locales/new/manual` (manual fallback per §6), `/locales/new/acknowledge` (§8 disclosure ack into existing `disclosure_acknowledgments` table — no schema change), `/locales/<slug>/nearby` (§5 proximity picker — same-chain filter + opt-in INSERT) + 3 templates (`new.html`, `nearby.html`, updated `list.html`). PR11 shipped §6 manual→Mapbox upgrade (`/locales/new?upgrade=<slug>` UPDATE-in-place flow — preserves slug + equipment + override rows) and §7 on-demand refresh (`POST /locales/<slug>/refresh` — silent when name/chain unchanged, `templates/locales/refresh_confirm.html` confirmation prompt otherwise) + ⟳ Refresh button per athlete-created Mapbox-anchored row on the list view. **D-59 fully shipped.** |
| D-60 | **Per-locale gear readiness via shared gym profiles + per-athlete overrides.** **Premise reframed during design:** Andy rejected category-default-manifest inference (no inferences). Replaced with per-physical-address shared gym profiles (crowd-sourced default-on) + per-athlete add/remove overrides + 10-category locale taxonomy. | High | ✅ Design complete (2026-05-14); 🟢 Core inherit/override UI shipped (PR11); 🟡 dispute / submit-as-correction / sharing opt-out / sharing-consent disclosure / §J.3 toggle UI deferred — premature at N=1 athlete | Onboarding §J.2 + §J.3; `gym_profiles` table (shared across users; PR2 schema); `locale_equipment_overrides` + `locale_toggle_overrides` tables (PR2 schema, FK fixed in PR11); `locale_profiles.gym_profile_id` FK (PR2 ALTER); inherit/override UI (PR11); `MANUAL_CATEGORIES` (PR11, 10-category aligned) | **Design shipped 2026-05-14:** `Onboarding_D60_Design_v1.md` (9 decisions; per-mapbox_id shared profiles; default-on crowd-sourcing with account-level + per-locale opt-out; last-writer-wins + disputed-flag conflict resolution; submit-as-correction promotion path; 10-category enum). **Consolidated into `Athlete_Onboarding_Data_Spec_v5.md`** §J.2 + §J.3 (shared-profile model) + §J.1.3 (10-category taxonomy) + §A.1 gym-profile sharing consent disclosure. **Implementation:** PR2 shipped the schema (tables + 13 `locale_profiles` ALTER columns + `chain_registry.py`) but PR2's CREATE TABLE for the override tables used a bogus `REFERENCES locale_profiles(id)` FK against a composite-PK target — the inline FK silently failed on every fresh boot, so the tables never actually got created in production. PR11 corrects the FK shape (composite FK + `locale TEXT` + idempotent in-place ALTER guards) and ships the §4.2/§4.3/§4.4 inherit/override UI on `templates/locales/form.html` with three modes (legacy / shared_build / shared_inherit) — first athlete at a `mapbox_id` builds the `gym_profiles` row; subsequent athletes inherit + write per-athlete deltas to `locale_equipment_overrides` (effective view = `(shared ∪ adds) ∖ removes`). PG-only — D-60 helpers gate on `database._is_postgres()`; SQLite dev falls through to the legacy `locale_equipment` flow. `MANUAL_CATEGORIES` realigned to D-60 §3's full 10-category taxonomy. **Still pending (PR12+, all deferred at N=1 athlete):** D-60 §4.5 submit-as-correction (promote per-athlete overrides → shared profile); D-60 §4.6 dispute flow (flip equipment items to disputed status; surface "disputed by N athletes" affordance); D-60 §4.7 account-level + per-locale sharing opt-out (`gym_profiles.private=TRUE` path + `locale_profiles.sharing_opt_out` UX); D-60 §4.7 gym-profile sharing-consent disclosure (separate disclosure_id from `mapbox_geocoding_consent`); §J.3 sport-specific gear toggle UI (Andy 2026-05-15: "they aren't as rigidly tied to a location — should live in a different form"). |
| D-61 | **Per-day schedule windows + session→locale assignment.** §G rewritten with per-day windows (enabled / start / duration + optional second window when Doubles Feasible); §J.5 deleted entirely. Session-to-locale assignment runs through a deterministic resolver at plan-gen time; athletes can JIT-swap from session card. | Med | ✅ Design complete (2026-05-14); 🟢 §G + onboarding integration shipped (PR12, 2026-05-16); 🟢 profile-tab schedule edit surface shipped (PR15, 2026-05-16); 🟡 JIT swap session-card UI + `resolve_locale()` helper pending Layer 4 spec | Onboarding §G (rewrite) + §J.5 (delete); new `daily_availability_windows` table (PG-only); `locale_profiles.preferred` column; 5 new `athlete_profile` columns; Layer 4 plan-gen consumer contract; session-card UX | **Design shipped 2026-05-14:** `Onboarding_D61_Design_v1.md` (9 decisions; per-day windows shape; Long Session + Doubles as orthogonal Tier 1 toggles; Preferred Rest Day demoted Tier 1→2; §J.5 deletion; anchor→proximity-cluster→equipment-qualify→preferred-flag→closest-by-distance resolver; JIT swap UX). **Consolidated into `Athlete_Onboarding_Data_Spec_v5.md`** §G full rewrite + §J.1 preferred flag + §J.5 deletion + §G.4 session-to-locale resolver. **Implementation:** PR2 shipped the `daily_availability_windows` table + `locale_profiles.preferred` ALTER (schema-only). **PR12 (2026-05-16, merge pending) — Option A1:** §G frontend + onboarding-step integration. New `routes/onboarding.py` step `/onboarding/schedule` (GET renders 7-row daily-windows form + orthogonal toggle sections; POST validates, persists, redirects to `/profile?tab=athlete`). New template `templates/onboarding/schedule.html` (per-day rows with `enabled` checkbox + earliest-start + duration + optional second window gated on Doubles Feasible; long-session day-picker subset-of-enabled cross-validation; preferred rest day multi-select; derived weekly-hours total displayed via small client script). Two helpers added to `athlete.py`: `get_daily_availability_windows` reads the 7 day rows back (silently degrades to all-disabled on SQLite where the table doesn't exist), `upsert_daily_availability_windows` DELETE-then-INSERT replaces the user's window set (PG-only via `database._is_postgres()` guard, matching PR8's provenance-write pattern). 5 new `athlete_profile` columns added to `_PG_MIGRATIONS` + both cold-start SQLite/PG schemas + the redundant CREATE TABLEs in the migration lists (full PR6 prefill-cols pattern): `long_session_available BOOLEAN`, `long_session_days TEXT` (comma-separated day tokens sun..sat), `long_session_max_hr SMALLINT` (2/3/4/5/6/8 — 8 represents "8+"), `doubles_feasible TEXT` ('regularly'/'occasionally'/'no'), `preferred_rest_days TEXT` (CSV). Prefill template's Continue button copy flipped "Continue to profile" → "Continue to schedule"; `_POST_STEP3_TARGET` flipped to `/onboarding/schedule`. Athletes who connected zero providers still hit the schedule step (the prefill-empty-state pass-through path lands on `/onboarding/schedule` like everyone else). **Still pending (gates on Layer 4 spec):** `resolve_locale(user_id, session_date, required_equipment) → (locale_id, status)` helper (anchor→proximity→qualifying→preferred→closest); session-card JIT swap modal; "Other locales here" affordance; plan-summary review surface; stale-assignment surfacing on §K-overlay/locale-edit re-runs. **Deferred from PR12 (5-file ceiling discipline):** surfacing per-day windows on `/profile?tab=athlete` for post-onboarding edits — athletes currently re-edit by hitting `/onboarding/schedule` directly; lands as a thin partial-template wire-up in a follow-on PR. **PR15 (2026-05-16, Option B from PR14 §5.1):** Profile-tab schedule edit surface shipped. Extract §G form from `templates/onboarding/schedule.html` into a shared partial `templates/onboarding/_schedule_form.html`; `schedule.html` slimmed 293 → 53 lines and includes the partial inside its `<form action="/onboarding/schedule">` wrapper (regression-clean — onboarding flow unchanged). `templates/profile/edit.html` gains a Schedule tab + tab-pane wrapping `<form action="/profile/schedule">` + `{% include 'onboarding/_schedule_form.html' %}` + Save-schedule button. `routes/profile.py` `edit()` GET pre-populates `days`, `long_session_days`, `preferred_rest_days`, `doubles_feasible`, `long_session_available`, `long_session_max_hr`, `day_tokens`, `day_labels` identically to `routes/onboarding.py:schedule` GET (importing `DAY_TOKENS`/`DAY_LABELS`/`DOUBLES_FEASIBLE_CHOICES`/`LONG_SESSION_MAX_HR_CHOICES`/`get_daily_availability_windows`/`upsert_daily_availability_windows` from `athlete`). New `save_schedule()` POST handler at `/profile/schedule` lazy-imports `_parse_schedule_form` from `routes.onboarding` (the cycle would otherwise close — onboarding already imports `CONNECTION_PROVIDERS` + `load_connections` from profile), round-trips identically to `onboarding.schedule_save` differing only in success redirect (`/profile/?tab=schedule` vs `/profile?tab=athlete`). Tiny local `_split_csv_day_tokens` helper mirrors `onboarding._split_csv_days` for the GET path. Tab-activation JavaScript in `templates/profile/edit.html` honors `?tab=schedule` automatically (no JS change needed — the existing `data-bs-target` lookup handles the new pane). **Still pending (gates on Layer 4 spec):** `resolve_locale(user_id, session_date, required_equipment) → (locale_id, status)` helper; session-card JIT swap modal; "Other locales here" affordance; plan-summary review surface; stale-assignment surfacing. |
| D-62 | **`webhook_events` retention prune.** Append-only audit/dedup table grows monotonically as provider webhooks arrive. `Athlete_Data_Integration_Spec_v4` §4.2 recommends a daily cron pruning `processed_at IS NOT NULL AND received_at < NOW() - INTERVAL '90 days'`. Surfaced in `D50_Phase1_Schema_Closing_Handoff_v1.md` §8 ("What might be missing") and re-flagged in `D50_Phase1_Schema_Review_v1.md` Finding 5. **Unresolved rows** (`processed_at IS NULL` after dispatch retries exhausted) are out of scope for the prune — they need their own dead-letter handling design. | Low | 🟡 Deferred | `webhook_events` table; D-50 wiring PR (or a follow-on ops PR); Vercel Cron | Design work: (a) host on Vercel Cron (the PR9 `vercel.json` `crons` array is the natural home — TrueNAS path retired in PR13); (b) decide whether to soft-delete (move to `webhook_events_archive`) or hard-delete — recommend hard-delete given `raw_payload` already stored on the dispatched-side row in per-provider tables; (c) decide separately what to do with `processed_at IS NULL` rows older than N days — likely a dead-letter row or operator alert. Cheap to land alongside the D-50 wiring; don't let it slip. |
| D-63 | **On-demand workout — "build me a workout right now."** Athlete-initiated single-session synthesis off-plan. Athlete supplies sport/duration/intensity/location; system reads Layer 1 profile (constraints) + Layer 3A state (current fitness/fatigue) + D-60 effective-equipment view for the picked location; Layer 4 single-session synthesis returns a structured session matching planned-session output schema. Logged sessions feed Layer 3A. Optional T1 plan-check hook (D-64) post-completion. Enterprise feature — multi-athlete; profile-level constraints (no per-request injury hard-coding). | High | ✅ Design complete (2026-05-15); 🟡 Implementation pending — **Layer 4 spec v1 landed 2026-05-16 (gates lifted); ready for implementation** | Layer 4 plan-gen (single-session synthesis path); Layer 1 athlete profile (read-only); D-60 effective-equipment view (read-only); `cardio_log` + `training_log` schema extensions (`is_ad_hoc` BOOLEAN + `ad_hoc_request_payload` JSONB + `ad_hoc_suggestion_id` BIGINT); new `ad_hoc_workout_suggestions` table; D-64 T1 hook post-completion; new athlete UX (form + result view) | **Design shipped 2026-05-15:** `OnDemand_Workout_D63_Design_v1.md` (13 decisions; sport/duration/intensity/location inputs; D-60 equipment view; profile-level constraints; single-sport v1; structured session output matching Layer 4; lifecycle: suggested → logged/discarded/regenerated; T1 hook closes the off-plan-execution → plan-invalidation loop). **Implementation gate lifted 2026-05-16** — Layer 4 spec v1 landed (`Layer4_Spec.md` §§1–13; §14 retro deferred per §12.6); `llm_layer4_single_session_synthesize` entry point + Pattern B + `SingleSessionRequest` consumption + ad-hoc session shape all spec'd. The D-63 prompt body is one of five queued prompt-body design sessions (stop-and-ask trigger #2). Pre-prompt-body implementation could land the deterministic-validator harness + `ad_hoc_workout_suggestions` schema + UX scaffold + storage handoff against the Layer 4 contract; LLM call wires in once the single-session synthesizer prompt body lands. Sibling design: `Plan_Refresh_D64_Design_v1.md` — D-63's T1 hook fires a D-64 T1 with auto-filled NL context. Cross-spec dependency: `plan_refresh_log.triggered_by_ad_hoc_id` column lands on D-64 schema. |
| D-64 | **Plan refresh tiers — T1/T2/T3 athlete-initiated plan refresh.** Athlete-initiated button on `/dashboard` + training plan view + optional natural-language context textarea. T1 = next 2 days rolling (Layer 3A + Layer 4); T2 = next 7 days rolling (3A + 3B + Layer 4); T3 = next 28 days rolling OR initial plan generation when none exists (full 2A/B/C/D/E + 3A/B/C/D + Layer 4). NL context parsed by LLM intent classifier; parsed intent may ADD upstream layer re-runs (e.g., "I tweaked my ankle" → 2D re-runs). Atomic versioning: per-day `plan_version_id` pointer; mid-cascade failure → previous version retained; revert path reuses pointer flip. Soft frequency caps with override (T1 3/24h; T2 1/48h; T3 1/7d). | High | ✅ Design complete (2026-05-15); 🟡 Implementation pending — **Layer 4 spec v1 landed 2026-05-16 (gates lifted); ready for implementation** | Layer 4 plan-gen (consumer); Layer 3A/3B/2A-2E (cascade re-runs); Layer 1 (read-only); new `plan_refresh_log` table (telemetry); plan-version table (now spec'd in Layer4_Spec.md §7.11); per-day `plan_version_id` pointer model on plan-session table (now spec'd in Layer4_Spec.md §7.11 + §7.12); NL intent parser (LLM call — prompt body still deferred per stop-and-ask trigger #2); athlete UX (button + modal + diff view) | **Design shipped 2026-05-15:** `Plan_Refresh_D64_Design_v1.md` (12 decisions; rolling-window tier scopes; free-text NL + LLM parser → upstream layer triggers; default cascades per tier; both /dashboard and training-plan-view button surfaces; soft caps with override; atomic versioning; concurrency block; failure handling; diff visibility; `plan_refresh_log` schema). **Implementation gate lifted 2026-05-16** — Layer 4 spec v1 landed (`Layer4_Spec.md` §§1–13). `llm_layer4_plan_refresh` entry point + Pattern A (T3 cross-phase) + Pattern B (T1/T2/T3 intra-phase) + `plan_versions` table schema (§7.11) + per-day version pointer (§7.12) + capped retry semantics (§5.5) + parsed_intent consumption + atomic-write boundary (D-64 §6.2 honored) all spec'd. The T1/T2 + T3 + intent-parser prompt bodies are three of five queued prompt-body design sessions (stop-and-ask trigger #2). Pre-prompt-body implementation could land the `plan_versions` table + per-day pointer resolver + frequency-cap orchestrator + diff renderer scaffold + `plan_refresh_log` schema against the Layer 4 contract; LLM call wires in once the prompt bodies land. NL intent parser prompt body still deferred to its own spec session — D-64 defines I/O contract (`IntentParserInput` / `ParsedIntent` dataclasses) which Layer 4 consumes via §3.2 `parsed_intent` parameter. **Cross-spec dependency:** `plan_refresh_log.triggered_by_ad_hoc_id BIGINT` column lands here for D-63 T1 hook telemetry correlation. **Frequency cap thresholds are guesses** at N=1 athlete; tune post-deploy. **Differentiator surface:** this is the athlete-explicit counterpart to Control_Spec §4 partial-update model; together they realize Differentiator #1 (Plan iteration as situations change). |

| D-65 | **TrueNAS Docker deployment retirement.** The repo carried a TrueNAS-via-Docker deployment path (`Dockerfile`, `docker-compose.yml` with `watchtower` auto-pull, `deploy/truenas_setup.sh`, `deploy/update.sh`) from the v1 maintenance era. Never used in practice; the operational deploy target has been Vercel only. Carrying it added noise to docs (CLAUDE.md "Stack" line, root DATABASE.md/HANDOFF.md sections) and kept the dual-deploy framing alive in every session. Andy 2026-05-16: "we are not using it / TrueNAS deployment at all anymore." | Low | ✅ Resolved 2026-05-16 (PR13) | Root `Dockerfile`, `docker-compose.yml`, `deploy/truenas_setup.sh`, `deploy/update.sh`; doc references in `aidstation-sources/CLAUDE.md`, root `DATABASE.md`, root `HANDOFF.md` | **Resolution (PR13):** All four artifacts deleted. `deploy/get_garmin_tokens.py` retained (Garmin OAuth helper, not TrueNAS-specific). Docs updated to mention "Vercel is now the only deploy target." Bundled with the D-54 SQLite retirement since both stemmed from the same "dual-everything maintenance" pattern. See `V5_Implementation_PR13_Closing_Handoff_v1.md` §2. |
| D-66 | **Race-event data model.** Layer 4 race-week-brief synthesis (§7.13 RaceWeekBrief + §7.14 RacePlan) references inputs the v1 app has no UI or table backing for: target race format (single_day / expedition_ar / stage_race / multi_day_ultra), race rules summary, distance/elevation, and the route-locale graph (start → transition areas → aid stations → drop bag points → bivvy points → finish). Today's v1 schema has only `athlete.target_event_name` + `athlete.target_event_date` text columns. Surfaced 2026-05-17 during Layer 4 Step 3 PR-C planning; should have been caught during the original Layer 4 / locale-system design but wasn't. The §14 retrospective's "what might be missing" list flagged joint-sessions + post-race surface + brief-diff renderer but missed the route-locale gap. Affects `kit_manifest_inputs_incomplete` validator rule — decorative until D-66 ships. | Med | 🟡 Deferred (design wave) | Layer 1 §H race info (extends); Layer 2C (extends with route-locale category); Layer 4 race-week-brief consumer (§3.4 + §7.13 + §7.14 inputs); onboarding §A target-race step (extends); profile/race-event edit UI (new) | **Scope to design:** new `race_events` table (id, athlete_id, name, date, race_format enum {single_day, expedition_ar, stage_race, multi_day_ultra}, distance_km NUMERIC, total_elevation_gain_m NUMERIC, race_rules_summary TEXT, mandatory_gear_text TEXT, etl_version_set JSONB); new `race_route_locales` table (id, race_event_id, locale_id FK to `locale_profiles`, role enum {start, transition_area, aid_station, drop_bag_point, bivvy, finish, other}, sequence_idx INT, mile_marker NUMERIC NULL, notes TEXT); onboarding step prompt asking for target race + route locales; profile UI to add/remove/reorder route locales + edit roles; per-route-locale `equipment_overrides` enabled (closes the rule-12 always-warn case for multi-day events). **Defer trigger:** lands when Step 4e race-week-brief implementation approaches per `Layer4_Spec.md` §14.3.4 — or earlier if Andy's Pocket Gopher Extreme 2026 race-week (July 17–19) pulls it forward. **Cross-layer scope** (Layer 1 + Layer 2C + Layer 4 + onboarding UI + profile UI). Triggered stop-and-ask #11. |
| D-67 | **Per-date athlete restrictions.** Athletes today can't tell the synthesizer "today I'm only at home" or "today no MTB" or "today indoor only" or "today ≤30min only" — the only locale signal is the trip-environment-type field on `plan_travel.locale` (positive case: "I'm at this locale for this window") with no negative-case per-date filter surface. Surfaced 2026-05-17 during PR-C planning together with D-66. Should have been caught during the original locale-cluster design or §G availability-windows design but wasn't. Affects 4 Layer 4 validator rules — all forward-compatible-but-decorative until D-67 ships: `session_locale_not_in_cluster_*` (6c — locale-lock branch), `discipline_excluded_*` (D-67 extension — discipline exclusions branch), `daily_window_fit_*` (D-67 extension — max_total_minutes branch), `indoor_only_violation_*` (NEW rule — indoor-only locale + discipline check). | Med | 🟡 Deferred (design wave) | Onboarding §G + profile schedule tab (extends — per-date restrictions UI); new `daily_restrictions` table; Layer 4 validator (4 rule branches active when restrictions present); 2C locale-category metadata (extends — needs indoor/outdoor classification per `MANUAL_CATEGORIES` bucket); 2A discipline-classification metadata (extends — needs indoor/outdoor classification per discipline) | **Scope to design:** new `daily_restrictions` table (id, athlete_id, date DATE, locale_lock TEXT NULL — locale_id, discipline_exclusions TEXT[] NULL — discipline_ids, indoor_only BOOLEAN DEFAULT FALSE, max_total_minutes INT NULL, created_at TIMESTAMPTZ, UNIQUE(athlete_id, date)); locale-category indoor/outdoor classification on Layer 0 `MANUAL_CATEGORIES` (home_gym = indoor; hotel_gym = indoor; commercial_chain_gym = indoor; independent_gym = indoor; climbing_gym_chain = indoor; climbing_gym_indie = indoor; pool_indoor = indoor; pool_outdoor = outdoor; outdoor_park = outdoor; other_residence = indoor); discipline indoor/outdoor classification (likely in Layer 0 `disciplines` table — extend `discipline_attributes` JSONB or new `is_indoor BOOLEAN` column; outdoor disciplines: MTB-outdoor, trail running, outdoor rock climbing, packrafting, paddle racing, ski mountaineering, road cycling, road running, hiking, etc.); onboarding/profile UI — per-date restrictions form (schedule tab gains a per-date overlay; session-card override modal for last-minute "actually home only" picks); D-64 T1 plan refresh hook fires on restriction change for impacted future dates. **Single D-row covers 4 restriction types** (locale_lock, discipline_exclusions, indoor_only, max_total_minutes) since they share one UX surface and one Layer 4 consumption shape (`PerDateRestriction` dataclass per the PR-D context schema sketch). **Defer trigger:** lands when athlete-2 onboarding pushes a real travel-week regression OR concurrent with D-66 design wave. **Cross-layer scope** (Layer 0 + Layer 2A + Layer 2C + Layer 4 + onboarding/profile UI + D-64 invalidation). Triggered stop-and-ask #11. |
| D-68 | **Default equipment profile per locale category — hotel-gym / unknown-equipment fallback.** When an athlete travels to a hotel (or any locale without populated `equipment_overrides`), the Layer 2C effective equipment view is empty, the synthesizer can't propose workable exercises, and rule `equipment_unavailable_*` fires aggressively or `Layer4ShapeInfeasibleError` raises. Today the only fix is the athlete logging hotel gym equipment before workout generation — but athletes rarely do that proactively. Andy 2026-05-17 pick: **hybrid (a + prompt)** — Layer 0 ships category-default equipment profiles (curated per `MANUAL_CATEGORIES` bucket — "what a typical hotel gym has"; "what a typical commercial chain gym has"); Layer 2C falls back to the category default when no overrides present; orchestrator surfaces a nudge prompt on the affected session card ("Plan assumed your hotel has treadmill + dumbbells + cable. Tap to confirm or edit") that fires T1 refresh on edit. **Critical invariant (Andy 2026-05-17):** category defaults NEVER write to `locale_equipment_overrides` or persist on the locale profile — they're transient resolution-only. Athlete confirmation via the nudge prompt is the only mechanism by which defaults become real profile data. | Med | 🟡 Deferred (design wave) | Layer 0 reference data (new `locale_category_default_equipment` table — curated; LLM-generated-then-human-reviewed-then-locked per Layer 0 convention); Layer 2C resolution algorithm (fallback order: overrides → category default → empty + `data_gap`); Layer 4 synthesis (consumes the fallback equipment view; no code change beyond reading from a richer 2C output); orchestrator nudge prompt (new UX — session-card affordance + T1 refresh wiring); D-58 nudge framework (extends — share the `account_nudges` infrastructure?) | **Scope to design:** new Layer 0 table `locale_category_default_equipment` (category TEXT FK to `MANUAL_CATEGORIES` enum, equipment_canonical_name TEXT FK to `layer0.equipment_items.canonical_name`, confidence enum {high, medium, low}, etl_version_set JSONB, source_evidence TEXT, UNIQUE(category, equipment_canonical_name)); curation pass per the 8 `MANUAL_CATEGORIES` buckets (hotel_gym, commercial_chain_gym, home_gym, independent_gym, climbing_gym_chain, climbing_gym_indie, pool_indoor, other_residence — outdoor_park gets a sparse default or no default since it varies wildly) — LLM drafts initial set, Andy reviews + locks; Layer 2C resolution update (3-tier fallback: overrides → category default → empty + `data_gap` observation); orchestrator nudge UX on session-card render when session's equipment came from a category default — banner + "Confirm or edit" button + T1 refresh wiring on submit; **3 sub-decisions deferred to the design wave:** (a) source authority for the defaults (LLM-only / LLM + HITL like Layer 0 sport rule sets / curated table) — Andy lean: LLM + HITL; (b) fallback gracefulness when a category default exists but is wrong for this specific locale (low-confidence flag? always emit `data_gap` even on category-default hit? user-suppressible "stop asking" toggle?); (c) prompt-UX policy (always-prompt-on-fallback / prompt-on-first-use-of-default-per-locale-per-athlete / no-prompt + passive data collection from logged sessions). **Defer trigger:** lands when v5 onboarding implementation approaches OR when athlete-2 hotel-week regression surfaces (whichever is earlier — Andy's own travel cadence may pull it forward). **Cross-layer scope** (Layer 0 + Layer 2C + Layer 4 + orchestrator UX + D-58 nudge framework). Triggered stop-and-ask #11. |

| D-69 | **Injury-accommodation modality foundation.** Pre-amendment Layer 2D verdict model produced binary EXCLUDE / DOWNGRADE / CLEAN — captured the substitution path (EXCLUDE → 2C Tier 2/3) but didn't structurally represent the broader evidence-based accommodation modality space (volume reduction, intensity reduction, tempo modification, loading-type change, frequency reduction, range-of-motion restriction). When Andy framed the 4-step injury-accommodation lifecycle ("the injury is logged → injury is interpreted → exercises are interpreted [swap OR volume OR intensity OR other] → accommodations are made"), the audit surfaced: 2D's DOWNGRADE verdict said "downgrade this" with no structured quantification; Layer 4 spec referenced `Layer3APayload.current_state.active_injuries` field that never existed in 3A §7 (canonical injury source was always 2D per §3.2 line 29; lines 753/898/1360 were drafting errors). v1 closed enum specified per evidence-based literature scan (Soligard 2016 IOC consensus + Cook & Purdam 2014 + Alfredson 1998 + Beyer 2015 + Rio 2015 + Silbernagel 2007 + Manca 2017 + Hendy & Lamon 2017 + ACSM Guidelines 11ed FITT-VP). Six modalities: `volume_reduction`, `intensity_reduction`, `tempo_modification`, `loading_type_change`, `frequency_reduction`, `exercise_substitution` (the last for unified-enum completeness — flows through existing EXCLUDE + 2C Tier 2/3 path; not directly 2D-emitted). | Med | ✅ Resolved 2026-05-17 | `Layer2D_Spec.md` §5.3.4 + §5.3.5 + §5.3.6 + §7 (new AccommodationModality typed union + ExerciseRisk extension + verdict rename); `Layer2C_Spec.md` §5.6 + §7 (ResolvedExercise.accommodations pass-through); `Layer4_Spec.md` §3.2 + §5.4 (injury_violation_* wording fix + NEW injury_accommodation_violation_* rule + modality framework summary); `Project_Backlog_v42` D-row tracking | **Resolution:** Path II — research-grounded single amendment session (Andy 2026-05-17 pick over Path I = full Layer 0 reference-data wave OR Path III = open-ended `modality_type: str` deferral). Spec the 6-modality typed union + per-injury-type-and-severity decision matrix in 2D §5.3.6; propagate accommodations list through 2C ResolvedExercise; add modality-aware validator rule in Layer 4 §5.4. 3 spec amendments + 3 bookkeeping files. **Phase-dependent contraindications** documented: acute reactive tendinopathy MUST use `tempo_modification.tempo_pattern == 'isometric_only'` (eccentric/energy-storage CONTRAINDICATED per Cook & Purdam 2014); bone stress — `intensity_reduction` alone INSUFFICIENT (must pair with `frequency_reduction(factor=0.0)` OR route through EXCLUDE); post-surgical first 6wk — prefer `loading_type_change(to_type='unilateral_contralateral')` over reduced-load bilateral (cross-education sparing per Manca + Hendy & Lamon). v1 EXPLICITLY does NOT support: ROM modality (D-70 deferred — condition-specific parameterization); phase sequencing (D-71 deferred — current-time only, T1 refresh + 3A re-assessment provides implicit progression); per-exercise tailoring beyond (injury_type, severity); muscle-group-level interpretation (body_parts vocab sufficient for Andy's wrist case + most v1 injuries); discipline-specific modality tailoring. **Cross-layer scope** (Layer 2D + Layer 2C + Layer 4 + onboarding-side InjuryRecord consumer chain). Triggered stop-and-ask #11. |
| D-70 | **Range-of-motion restriction accommodation modality.** Acute joint injuries (post-op ACL, shoulder impingement, ankle sprain mid-recovery, etc.) commonly call for partial-ROM training — restrict the exercise to the pain-free arc while maintaining other prescription parameters. Excluded from the D-69 v1 closed modality enum because parameterization is condition-specific (post-ACL "0–90° flexion" vs shoulder impingement "<90° abduction" vs generalized "pain-free range") and no clean cross-condition parameter shape exists per the D-69 literature scan. v1 callers receive `intensity_reduction` + `volume_reduction` as the closest available approximation; ROM-specific guidance must currently go in `instructions` or `rationale` free text. Tracked for v2 add. | Low | 🟡 Deferred (v2) | `Layer2D_Spec.md` §7 (add `RangeOfMotionRestrictionModality` to AccommodationModality typed union); `Layer4_Spec.md` §5.4 (add ROM enforcement branch to `injury_accommodation_violation_*` rule); athlete-side capture (InjuryRecord may need optional `rom_restriction: dict` field carrying per-injury pain-free arc) | **Scope to design:** add `RangeOfMotionRestrictionModality(modality_type='range_of_motion_restriction', mode ∈ {pain_free, partial_specified}, max_flexion_deg: int \| None, max_extension_deg: int \| None, rom_fraction: float \| None ∈ [0.25, 1.0], rationale: str, evidence_basis: list[str])` to 2D §7 typed union. Add v1 default table entries for joint sprain × Acute (was just volume+intensity) and post-surgical first-6wk (currently loading_type_change only). Add Layer 4 validator branch for ROM enforcement (parses StrengthExercise.tempo or instructions for ROM cues; validates within tolerance). **Defer trigger:** lands when first real ROM-restriction case surfaces (Andy's wrist is body-part-level not ROM-level; post-op or shoulder impingement cases would force the question). v2 may also need per-condition parameter tables (post-ACL has well-published protocols; other conditions vary). **Cross-layer scope** (Layer 2D + Layer 4 + athlete onboarding extension). Tracked as D-69 follow-on. |
| D-71 | **Phase-sequencing for tendinopathy progression (Cook & Purdam staged loading).** v1 accommodation modality recommendation emits CURRENT-TIME only — for chronic tendinopathy, this means the athlete gets the modality matching their current `severity` (Recovering → HSR; Chronic-Managed → HSR maintenance). Cook & Purdam clinical framework calls for staged progression: isometric (Stage 1, ~weeks 0-4 for in-season analgesia) → HSR/isotonic (Stage 2, ~weeks 4-12 for capacity build) → energy-storage / plyometric (Stage 3, ~weeks 12-16 for return-to-sport) → full load (Stage 4, monitor pain ≤5/10 VAS). v1 captures this PROGRESSION implicitly via T1 refresh + 3A re-assessment: when athlete's recovery moves to a new severity tier (Recovering → Chronic-Managed → Resolved), 2D re-runs against updated severity and yields next-stage modality picks. v2 may instead emit modality SEQUENCE — {phase: isometric, weeks: 2-4} → {phase: HSR, weeks: 4-12} → {phase: energy-storage, weeks: 12-16} — letting Layer 4 synthesizer see the planned progression and structure the plan accordingly. | Low | 🟡 Deferred (v2) | `Layer2D_Spec.md` §5.3.6 + §7 (extend AccommodationModality with optional `phase_sequence: list[ModalityPhase]` or wrap modalities in a sequencing container); `Layer3_3A_Spec.md` (may need explicit recovery-phase tracking beyond severity); Layer 4 synthesizer prompt body for tendinopathy-progression awareness; D-64 plan refresh tier policy for tendinopathy-progression triggers | **Scope to design:** decide whether to extend AccommodationModality (per-modality phase sequencing) or introduce a higher-level container (`AccommodationProgram` wrapping a sequence). Spec the phase enum (`isometric` \| `isotonic_hsr` \| `energy_storage` \| `return_to_sport`). Define triggers for stage transition — symptom-based (athlete logs pain reduction) vs time-based (weeks from injury date) vs hybrid. Source: Cook & Purdam 2014 (BJSM) staged progression model; supplement with Rio 2015 (in-season isometric analgesia timing) + Beyer 2015 (HSR cadence). **Defer trigger:** lands when first chronic-tendinopathy case with multi-stage progression needs surface (no current v1 case requires it; Andy's wrist is treated as Chronic-Managed steady state rather than progressing through Cook & Purdam stages). v1 implicit-via-refresh mechanism is sufficient for cases where injury phase progression maps cleanly to severity tier changes. **Cross-layer scope** (Layer 2D + Layer 3A + Layer 4 + D-64 refresh policy). Tracked as D-69 follow-on. |

**Numbering note:** D-18, D-19, D-20 were never assigned (artifact of an aborted earlier numbering plan during the 2026-05-10 / 2026-05-11 session sequence). IDs in this table are stable references from per-layer specs; do not renumber.

---

## Spec doc backfill — completed 2026-05-10

| Item | Status | Notes |
|---|---|---|
| `Layer2A_Spec.md` | ✅ Backfilled | Source: `Layer1_2B_Done_2C_Kickoff_Handoff.md` §"Node 2A". Depth standard matches `Layer2C_Spec.md`. |
| `Layer2B_Spec.md` | ✅ Backfilled | Source: same handoff doc, §"Node 2B". Depth standard matches `Layer2C_Spec.md`. |
| `Layer2C_Spec.md` | ✅ Drafted earlier this session | First per-node consolidated spec; established depth standard. |
| `Control_Spec.md` | ✅ Drafted | Architecture overview; lives above per-node specs. |
| `Project_Backlog.md` | ✅ Renamed from `Layer0_Drift_Backlog.md` | Scope broadened from Layer 0 drift to cross-layer deferred work. |
| Memory rules added | ✅ | Memory edits #4, #5, #6 enforce per-node spec doc rule, backlog rule, control spec rule going forward. |

## Going-forward rule

Every layer/sublayer designed from this point on gets its own `LayerNX_Spec` at the `Layer2C_Spec` depth standard. Handoff docs are session bookkeeping; specs are source of truth. New spec → update `Control_Spec` §9 doc map.

---

## Verification checks — all resolved

**Run:** 2026-05-10 via `verify_drift_specifics.sql`. Output: `drift_verification.txt`.

All three checks (A, B, C) confirmed: D-05, D-07, D-08 are AR-safe and stay deferred. D-05 elevated to "standing rule" status — every Layer 2 query node spec MUST include the defensive aggregator filter. D-17 added as new finding from cross-referencing Check B and Check C outputs.

Verification SQL committed to project for reproducibility (Andy's note: matches prior introspection-script pattern).

---

## Final cleanup batch — status

### Session FC-1a: ETL bug fixes — ✅ CLOSED 2026-05-11

**All 11 items resolved or partial:**
- ~~D-04~~ — ✅ Resolved
- ~~D-05~~ — 🟡 Partial (cleanup done; ETL code patch + standing rule retirement deferred to CC)
- ~~D-06~~ — ✅ Resolved
- ~~D-07~~ — ✅ Resolved with decisions (CC parser fix pending; specs in `FC1a_Closing_Handoff_v1.md` §5.2)
- ~~D-08~~ — ✅ Resolved with explanation (no bug; xlsx source fix queued for Triathlon D-002 stale row)
- ~~D-12~~ — ✅ Resolved (Batch D patch)
- ~~D-13~~ — ✅ Resolved (Batch B Correction)
- ~~D-14~~ — ✅ Resolved (source_text dropped; spec catches up in Batch D v2)
- ~~D-15~~ — ✅ Resolved with explanation (loose UNIQUE preserved as variant key; spec catches up in Batch D v2)
- ~~D-16~~ — ✅ Resolved (TEXT[] confirmed; Batch B Correction)
- D-03 — Decision locked: BUILD (CC implementation deferred to v20 ETL re-run)

### Session FC-1b: column promotions — ✅ CLOSED 2026-05-12

**All FC-1b blockers resolved this session:**
- ~~D-22~~ — ✅ Resolved 2026-05-12. `exercises.movement_components TEXT[]` deployed; 159/159 rows populated; GIN index in place.
- ~~D-23~~ — ✅ Resolved 2026-05-12. `disciplines.body_parts_at_risk TEXT[]` deployed; 31/31 rows populated; GIN index in place. Vocabulary amendment: Collarbone added to canonical body parts (tracked as D-39 for formal v3 rewrite).
- ~~D-26~~ — ✅ Resolved 2026-05-11. `supplement_vocabulary` table deployed to Neon dev.

**Layer 2D implementation now unblocked.** Both source-of-truth columns (`movement_components`, `body_parts_at_risk`) live with GIN indexes; the set-intersect paths in Layer2D_Spec §5.4.1 (movement components) and §5.5 (body parts) can ship without the keyword-map fallbacks. Decision Points 2D-1 (body-part keyword map → Path B) and the movement-component path are both locked to structured columns.

### Session FC-2: Spec v4 rewrite — ✅ CLOSED 2026-05-12

**All FC-2 items resolved:**
- ~~D-39~~ — ✅ Resolved 2026-05-12. `Vocabulary_Audit_v3.md` shipped: Collarbone added, total recount to 51.
- ✅ `Layer0_ETL_Spec_v4.md` shipped: 4 new tables added (§4.15 discipline_technique_foci with B-Correction; §4.16 sport_name_aliases; §4.17 terrain_gap_rules placeholder; §4.18 supplement_vocabulary placeholder). All §4 existing tables reconciled to deployed shape (D-02 col renames; D-04 phase_ prefix drop; D-06/D-07 hours rename + parser-fix decisions; D-09 sport_discipline_bridge denormalized cols + corrected UNIQUE; D-14 cross_sport_properties extras; D-15 substitute_name variant key; D-16 muscles TEXT[]; D-01 exercises full reconciliation). New columns documented: `movement_components` on exercises (D-22); `body_parts_at_risk` on disciplines (D-23). §6.6 process note added ("Code is authoritative; spec catches up"). §7 open items refactored to point at this backlog. §8 consumer table corrected per `ETL_Spec_v3_Corrections_2ABC_v2`. §5.2 2D/2C function signatures corrected per Layer2D_Spec_v1 + corrections doc.
- ✅ `Layer2D_Spec_v1.md` shipped: §5.3.3 rewritten to use `movement_components` set-intersect (D-22 closed); §5.4 `discipline_risk()` rewritten to use `body_parts_at_risk` set-intersect (D-23 closed); §5.5 Decision Point B locked + deployed (Path B); §6 drift items D-22, D-23 marked Resolved; §12 open items 2D-1, 2D-2, 2D-6 marked Closed; new item 2D-11 added for D-38 sub-threshold wrist deviation. `MOVEMENT_CONSTRAINT_KEYWORDS` and `BODY_PART_KEYWORDS` demoted to historical reference (not runtime).
- **New cleanup items surfaced:** D-40 (terrain_gap_rules schema dump), D-41 (terrain_types 7 enrichment cols enumeration), D-42 (cross_sport_properties columns reconciliation), D-43 (Batch A/C patch docs not in project knowledge).

**Layer 2 family is now substantively spec-complete through 2D.** Remaining open Layer 0 work: D-03/D-07 ETL parser fixes pending v20 re-run (CC task); D-40/D-41/D-42 schema-dump documentation cleanup; D-43 patch-doc archive. None block Layer 3 design or query-layer implementation.

### Session FC-3: Spec v5 schema corrections + §5 rewrite — ✅ CLOSED 2026-05-13

**All in-scope FC-3 items resolved:**
- ~~D-40~~ — ✅ Resolved 2026-05-13. `terrain_gap_rules` schema enumerated; v5 §4.17 replaces placeholder.
- ~~D-42~~ — ✅ Resolved 2026-05-13. `cross_sport_properties` confirmed; `confidence` type corrected `NUMERIC` → `TEXT` in v5 §4.8. Batch D v2 §3 alternate columns confirmed not deployed.
- ~~D-44~~ — ✅ Opened and closed 2026-05-13. `sport_name_aliases` UNIQUE corrected from 2-col to 3-col `(exercise_db_sport, framework_sport, etl_version)` in v5 §4.16. "One-to-one inverse" claim retracted.
- ~~D-45~~ — ✅ Opened and closed 2026-05-13. `Layer0_ETL_Spec` §5.2 rewritten to mirror per-layer 2A–2E spec signatures verbatim. Formalizes §6.6 spec-of-spec rule at signature level.
- ✅ `Layer0_ETL_Spec_v5.md` shipped: schema corrections (§4.8, §4.16, §4.17), §5 query-layer narrative rewrite (§5.1 primitives note, §5.2 full rewrite with attribution, §5.3 canonical payload updated for `movement_components` + `common_injury_patterns` + `body_parts_at_risk` surfacing + `HealthConditionRecord` field alignment, §5.4 D-15 variant-key annotation).

**Carried forward:**
- **D-41** — `terrain_types` schema dump retry. Neon `\d` query failed in FC-3 on a client-side quoting artifact. Retry uses `information_schema.columns` form. One query + one str_replace edit.
- **D-46** — `sport_discipline_bridge` row-multiplication audit. Follow-up from D-44 closure. Single COUNT query to determine whether any active alias multi-mappings exist.

**Layer 0 spec is now self-consistent against deployed schema** with one verified-pending gap (D-41 `terrain_types`) and one consumer-side audit (D-46 bridge). The §5 query-layer narrative now reflects deployed reality and per-layer authoritative signatures. Layer 0 work has no remaining blockers for Layer 3 design or query-layer implementation.

### Session FC-4a: Spec v6 — D-41 terrain_types + D-46 multi-mapping audit — ✅ CLOSED 2026-05-13

**All in-scope FC-4a items resolved:**
- ~~D-41~~ — ✅ Resolved 2026-05-13. `terrain_types` 9-column schema enumerated; drift report's `simulatable BOOLEAN` claim corrected to `TEXT`; nullable `terrain_id` with NULL-distinct UNIQUE documented. Layer0_ETL_Spec_v6 §4.14 replaces 2-bullet drift note.
- ~~D-46~~ — ✅ Resolved 2026-05-13. 21 multi-mapped `exercise_db_sport` values confirmed intentional for framework sub-format splitting. No `sport_name_aliases.py` tightening. Multi-mapping table documented in v6 §4.16. Bridge multiplication property documented in v6 §4.11 with consumer dedup pattern.
- **New cleanup item D-47** — Layer 2D §5.2 rationale-comment update (functional behavior already correct; comment incomplete). Tracked for next Layer 2D revision.
- ✅ `Layer0_ETL_Spec_v6.md` shipped: §4.14 terrain_types schema enumerated; §4.16 multi-mapping audit findings + table; §4.11 bridge multiplication property + consumer dedup pattern + reference SQL.

**Layer 0 spec is now fully self-consistent against deployed schema** for every Neon-enumerated table. Remaining uncertainty is in `health_condition_categories` column name (D-21, separate session). All other tables either match spec exactly or have FC-1/FC-3/FC-4a corrections committed.

### Session FC-4b: Spec v7 — D-21 health_condition_categories column-name reconciliation — ✅ CLOSED 2026-05-13

**All in-scope FC-4b items resolved:**
- ~~D-21~~ — ✅ Resolved 2026-05-13. Neon `information_schema.columns` returned 6 columns + 2 constraints. **Deployed column is `category_name`**; v3 §6.2 `system_category` reference was the stale half of the split. Layer0_ETL_Spec_v7 §4.14 documents full schema block (6 cols, `UNIQUE (category_name, etl_version)`); §6.2 validation line corrected from `.system_category` → `.category_name`. **No consumer change required** — `system_category` is the Python dataclass field name on `HealthConditionRecord`, independent of the SQL column name.
- ✅ `Layer0_ETL_Spec_v7.md` shipped: §4.14 health_condition_categories schema block replaces D-21 deferral bullet; §6.2 column reference corrected; v6 → v7 changelog block added.
- ✅ Layer 0 spec is now **fully self-consistent against deployed Neon schema** across every enumerated table.

**Carried forward (not FC-4b scope):**
- **D-47** — Layer 2D §5.2 rationale-comment update. Fold into next Layer 2D revision; Layer 2D row 2D-5 and Layer 2E row 2E-16 D-21 housekeeping rows can be cleared in the same pass.
- **D-03 / D-07** — ETL parser fixes pending v20 re-run (CC tasks).

### Session L3-Discovery: Layer 3 framing reconciliation + integration architecture — ✅ CLOSED 2026-05-13

**Discovery session — no spec files modified. All decisions captured in `L3_Discovery_Closing_Handoff_v1.md` for execution in L3-Spec-Trio.**

**Decisions made:**
- **Layer 3 reshape: 6 sub-prompts → 4 nodes.** Race analysis dropped (owned by 2A+2B). Fitness capacity + training history merged into 3A. Injury/risk dropped (owned by 2D). Goal alignment + timeline viability merged into 3B. Cross-node conflict detection → 3C (new). HITL aggregation gate → 3D (new). Layer 3.5 collapses into 3D's gate output.
- **Integration architecture: single Neon DB, two schemas, no bridge.** `layer0.*` for platform reference; `public.*` for app tables. Layer 1 as conceptual aggregation, not a separate schema. (Catalog drift — the assumption that catalogs were unified at `layer0.*` — surfaced in L3-Spec-Trio inventory and reframed to Option A target state; see L3-Spec-Trio block below.)
- **§H.3 amendment for Layer 1 v3:** add `Non-Event Goal Type` enum to close the goal-type gap.
- **Integration deployment timing:** deploy after 3A spec lands (revised to "after `Athlete_Data_Integration_Spec` v1 lands" in L3-Spec-Trio).

**Carried forward to L3-Spec-Trio:**
- Draft `Athlete_Data_Integration_Spec.md` v1
- Draft `Athlete_Onboarding_Data_Spec_v3.md` (§H.3 amendment)
- Draft `Control_Spec_v6.md` (C-1 through C-8 surgical edits)
- Draft `Project_Backlog_v10.md` (this file)
- L3-Spec-Trio also added Catalog_Migration_Plan.md as a new doc after catalog drift discovered

### Session L3-Spec-Trio: Integration spec + onboarding v3 + Control_Spec v6 + Backlog v10 + Catalog Migration Plan — ✅ CLOSED 2026-05-13

**Execution session — 4 spec files shipped (one more than originally scoped; Catalog Migration Plan added after schema inventory revealed the parallel-catalogs drift):**

- ✅ `Athlete_Data_Integration_Spec.md` v1 shipped: consumer-side spec for AIDSTATION pipeline access to integration data. Locks single-Neon / two-schema architecture, Layer 1 as conceptual aggregation, Garmin onto `provider_auth`, PG-only. 12 sections. Includes §7 field mapping for Layer 1 §C/§D/§E/§F sourcing (which exposed D-51 as larger than originally scoped — see §7.6 of integration spec).
- ✅ `Athlete_Onboarding_Data_Spec_v3.md` shipped: v2 + §H.3 Non-Event Goal Type field. Backwards-compatible single-purpose amendment. v3.0 header with changed-section.
- ✅ `Catalog_Migration_Plan.md` v1 shipped: strategy spec for app catalog migration from `public.*` to `layer0.*`. 5 phases. Owns D-52.
- ✅ `Control_Spec_v6.md` shipped: C-1 through C-8 surgical edits per L3-Discovery handoff §6.2, plus catalog reconciliation language reflecting Option A target state, plus §8.3 cross-cutting spec clarification.

**Drift surfaced this session (then closed):**
- L3-Spec-Trio pre-step inventory revealed that the app reads catalogs (`exercise_inventory`, `equipment_items`, `exercise_equipment`) exclusively from `public.*`, not `layer0.*` as L3-Discovery had assumed. Resolved by adopting Option A target state (app migrates to read `layer0.*`) and creating new `Catalog_Migration_Plan.md` (D-52) to own the migration.

**New decisions captured this session:**
- **Catalog migration: Option A** — app reads migrate to `layer0.*`. D-52.
- **Heat acclim state: derived, not stored.** From `conditions_log` + integration ambient data + locale climate. D-53.
- **SQLite backend deprecation** as part of catalog migration. D-54.
- **Garmin migration onto `provider_auth`** — drop legacy `garmin_auth`. D-55.
- **`cardio_log` schema additions** for AIDSTATION needs (is_race, start_time). D-56.

**New backlog rows added:** D-48 through D-56 (see open items table above).

### Session L3-Spec-Trio Round 2: Onboarding v4 + Integration v2 + Catalog Migration v2 + Backlog v11 — ✅ CLOSED 2026-05-13

**Feedback-absorption session — 4 spec files shipped + Layer 3 spec writing begins same session (Layer3_3A_Spec.md):**

- ✅ `Athlete_Onboarding_Data_Spec_v4.md` shipped: pregnancy intentionally not captured (disclosure only, never a field); shooting/fencing technical readiness removed entirely (§D.7 retains Rock Climbing + Abseiling only; §J.3 drops Fencing setup + Shooting setup gear toggles). Scope note added that AIDSTATION does not program shooting or fencing technique. Modern pentathletes and biathletes still supported — endurance + strength + recovery only; skill-discipline training is athlete's responsibility.
- ✅ `Athlete_Data_Integration_Spec_v2.md` shipped: Garmin reframed — not live, build from scratch (§2.3 rewritten; D-55 reframed; Wave-1 paragraph corrected; §5.4 rewritten). New §2.7 athlete-integration-data retention rule: retain until BOTH (newer record of same type exists) AND (90+ days elapsed). Per-table "same type" definitions table. No-user-preservation note (1–2 test accounts only) carried throughout.
- ✅ `Catalog_Migration_Plan_v2.md` shipped: §4 Phase 1 step 3 expanded into explicit fuzzy-match + HITL workflow (exact → fuzzy with top-N candidates → human review queue → confirmed alias or gap-list entry). §4 Phase 4 adds "wipe pattern" as viable alternative to dual-write dance given 1–2 test accounts.
- ✅ `Project_Backlog_v11.md` shipped (this file): D-55 reframed Garmin "migrate" → "build"; new D-57 (research re-eval cadence), D-58 (account-first onboarding flow), D-59 (Google Maps Places + chain lookup), D-60 (gear from proximity), D-61 (session availability untied from locale).

**Feedback items absorbed:**
- Onboarding architectural items deferred to dedicated design sessions (D-58, D-59, D-60, D-61). Out of scope for the surgical v4 pass.
- Surgical onboarding items landed in v4 (pregnancy disclosure framing; shooting/fencing scope trim).
- Integration v2 absorbs the Garmin reality and the new retention rule.
- Catalog Migration v2 absorbs the aliases workflow refinement.

**Forward move same session:**
- Layer3_3A_Spec.md drafted against Onboarding v4 / Integration v2 / Control_Spec v6 input contracts.

### Session Reconciliation v12 — single-repo correction + Garmin pause — ✅ CLOSED 2026-05-14

**Trigger:** handoff between Claude.ai chat (spec/planning track) and Claude Code (this repo). Survey revealed the prior chat had been operating under a "Vercel-app is a separate repo" misconception. There is no separate repo — the v1 Flask AIDSTATION app lives at the root of this repo alongside `aidstation-sources/`.

**Operating framing locked (per Andy 2026-05-14):**
- **Selective rebuild (Q1=c).** Keep provider integration layer + auth + DB scaffolding from v1. Replace coaching + plan-gen with the Layer 0–5 LLM pipeline being designed in `aidstation-sources/`. Revisit v1 strength/UI surface later.
- **Garmin path (Q2 = Integration v2/v3 plan).** Drop `garmin_auth` and rebuild on `provider_auth` + `session_blob` once Garmin API access reopens. PROVIDERS_SCHEMA.md reconciled in this v12 pass to match (previous "garmin_auth stays as-is" language reversed).
- **Strangler-fig sequencing (Q3=i).** v1 is "live" but has no users. Ship v2 modules directly into the running v1 app one at a time; no parallel staging environment needed.

**Files touched this pass (4):**
- ✅ `Project_Backlog_v12.md` (this file): D-50 + D-55 reframed to drop separate-repo framing; D-55 status → ⏸ Paused (Garmin API closed); this session block added.
- ✅ `Athlete_Data_Integration_Spec_v3.md` shipped: cross-ref to `PROVIDERS_SCHEMA.md` corrected (same repo); Garmin API closure noted; D-55 dependency reframed.
- ✅ `CLAUDE.md` edited in place: §Stack rewritten to reflect v1+v2 selective-rebuild picture; new §Operating context section captures strangler-fig + Andy's "push to production as we go" rule; D-50 line in §Current state corrected.
- ✅ `PROVIDERS_SCHEMA.md` (root) edited in place: §5.1 + §7 garmin_auth statements reconciled — drop garmin_auth on Garmin reopen, build onto provider_auth with `session_blob`.

**Skipped intentionally:**
- `Catalog_Migration_Plan_v2.md` — "Vercel-app schema source of truth" phrasing is descriptive of where the app runs in prod, not a separate-repo implication. No change.
- Handoff files (`L3_Discovery_Closing_Handoff_v1.md`, `L3_Spec_Trio_R2_Closing_Handoff_v1.md`) — point-in-time records of what was understood when. Not rewritten.

**Forward move:** L3-3B plan-mode scoping in a fresh session, then implementation begins per Andy's "push to production as we go" rule.

### Session 3B-Spec — Layer3_3B_Spec.md + Rule #13 — ✅ CLOSED 2026-05-14

**Continuation of the reconciliation session that produced Backlog v12.** Same chat. After reconciliation closed (4 files + commit), Andy approved the proposed scope for `Layer3_3B_Spec.md` and the spec was written same session under "push to production as we go" rule. 5-file ceiling held loosely (substantive work this chat = Control_Spec_v7 stub + reconciliation pass + 3B spec; close-out files are book-keeping).

**Files shipped:**

| File | Lines | Notes |
|---|---|---|
| `Layer3_3B_Spec.md` | 508 | Second of four Layer 3 node specs. 14-section depth standard matching `Layer3_3A_Spec.md`. Resolves L3-Discovery §5.2's two open questions: HITL trigger threshold (§6.1 four-row table) and periodization-shape vocabulary (§6.2 `standard`/`compressed`/`extended`/`custom` + `start_phase`). Race-date-in-past as fatal `Layer3BInputError`. Two-mode discriminator (event vs no-event) as first-class branches. 8 test scenarios including Andy's actual PGX 2026 case as TS-1. |
| `CLAUDE.md` (edit in place) | +6 lines | New Rule #13: every closing handoff names CLAUDE.md as the first re-read. Backstop against operating-context drift between sessions. |
| `Project_Backlog_v13.md` (this file) | — | Session block added; D-rows unchanged. |
| `handoffs/3B_Spec_Closing_Handoff_v1.md` | — | Closing handoff covering reconciliation + 3B work, mechanically-applicable next-session instructions for 3C+3D, forward pointers, gut check. |

**Decisions confirmed in-chat before writing:**
- A: HITL trigger threshold (§6.1) — four-row table
- B: Periodization-shape vocabulary — enum + start_phase
- C: No-event mode handling (§6.6) — heuristics in system prompt
- D: Race-date-in-past — fatal validation error

**Forward move (next session):** combined `Layer3_3C_Spec.md` + `Layer3_3D_Spec.md` (per Round 2 handoff §6 — smaller surfaces, query/rules nodes, candidate for one session). After 3D ships: `Control_Spec_v8` doc-map sync batching all four Layer 3 node spec entries (Round 2 §5 Option A path now reachable).

**After Layer 3 specs complete:** per Andy's "push to production as we go" rule (CLAUDE.md §Operating context), proceed to building actual code — Layer 0–3 implementation against shipped specs while Layer 4 spec drafts in parallel. Strangler-fig sequencing into v1 Flask app at repo root.

### Session D-50-Phase-1-Schema — `init_db.py` migrations shipped — ✅ CLOSED 2026-05-14

**First production catchup session under Andy's "push to production as we go" rule.** Selected D-50 over onboarding rebuild after surfacing that `Athlete_Onboarding_Data_Spec_v4.md` §"What changed in v4 vs v3" itself flags §A flow / §B locales / §G schedule / §J equipment as out of scope for v4 surgical pass pending the D-58–D-61 design wave. v4-vs-v3 deltas (pregnancy / fencing / shooting) were already absent from v1 code — nothing surgical to patch. Onboarding deferred to the design wave; D-50 chosen as the genuinely-ready catchup target.

**Files shipped:**

| File | Change | Notes |
|---|---|---|
| `init_db.py` | +~170 lines | New tables appended to both `_SQLITE_MIGRATIONS` and `_PG_MIGRATIONS` lists, per established post-deploy pattern (the SCHEMA strings are frozen; recent tables `password_resets` / `admin_audit` / `api_tokens` / `wellness_self_report` set the precedent). SQLite uses `INTEGER PRIMARY KEY AUTOINCREMENT` / `TEXT DEFAULT (datetime('now'))` / `INTEGER` for BIGINT and BOOLEAN; PG uses `SERIAL` / `TIMESTAMP DEFAULT NOW()` / `BIGINT` / `BOOLEAN`. Partial indexes (`WHERE status IN (...)` and `WHERE processed_at IS NULL`) work in both backends. |
| `DATABASE.md` | +~85 lines | New `### Provider integrations` section between Garmin and Shared catalogs, documenting all 10 tables + the foreign-id ALTER columns + Garmin-paused context. |
| `Project_Backlog_v14.md` | new file | This file. D-50 row updated to partial-shipped status; session block. |
| `handoffs/D50_Phase1_Schema_Closing_Handoff_v1.md` | new file | Session-close mechanic. |

**Files explicitly NOT touched:**
- `PROVIDERS_SCHEMA.md` — v12 reconciliation already added `session_blob` notes (§5.1 + §7); no further edit needed at the schema-only stage.
- `Athlete_Data_Integration_Spec_v3.md` — spec is the input contract; not edited from implementation.
- `CLAUDE.md` — operating context unchanged this session.
- `Control_Spec_v7.md` — doc-map sync is bundled with Layer 3C/3D ship per 3B handoff §6.2.

**Tables landed:**

| Table | UNIQUE / index | Source spec |
|---|---|---|
| `provider_auth` | UNIQUE `(user_id, provider)`; partial idx on `status` | Integration v3 §4.1 |
| `webhook_events` | lookup idx `(provider, provider_user_id, entity_id, event_type)`; partial idx pending on `processed_at IS NULL` | Integration v3 §4.2 |
| `polar_sleep` | UNIQUE `(user_id, date)` | §5.1 |
| `polar_nightly_recharge` | UNIQUE `(user_id, date)` | §5.1 |
| `polar_cardio_load` | UNIQUE `(user_id, date)` | §5.1 |
| `polar_continuous_hr_samples` | UNIQUE `(user_id, timestamp_ms)`; user-time idx | §5.1 |
| `wahoo_plans` | plan_item idx | §5.2 |
| `coros_daily_summary` | UNIQUE `(user_id, happen_day)` | §5.3 |
| `coros_hrv_samples` | UNIQUE `(user_id, timestamp_s)` | §5.3 |
| `coros_plans` | plan_item idx | §5.3 |

**ALTER columns landed:**
- `cardio_log` — `polar_exercise_id`, `wahoo_workout_id`, `coros_label_id`, `rwgps_trip_id`
- `training_log` — `polar_exercise_id`, `wahoo_workout_id`, `coros_label_id`
- `strava_activity_id` on `cardio_log` deferred per spec §6 until Strava integration design lands (D-48)

**Verification (Rule #10):**
- Ran `init_sqlite()` against `/tmp/test_d50.db` from a fresh delete. All 10 D-50 tables created; all 7 ALTER columns present; all 6 named D-50 indexes present (including the 2 partial indexes). Re-run with the same file present completed without errors — migration loop's try/except + `IF NOT EXISTS` make every statement idempotent.
- PG path not exercised locally (would need a Neon connection). Migration syntax mirrors the SQLite block's structure with the standard PG type swaps; the existing PG migration list precedent (e.g., `wellness_self_report` deployed via the same pattern) is the strongest evidence the new block will apply on `init_postgres()` cold-start.

**Decisions made this session (no spec edits required):**
- **SQLite + PG both target.** Per Integration v3 §2.5 PG is the long-term target and D-54 schedules SQLite collapse during Catalog Migration Phase 5, but until then the dual-backend pattern stays — matches the precedent set by `wellness_self_report` and friends. SQLite gets the new tables so local dev stays functional.
- **No `garmin_*` per-provider table.** Per Integration v3 §5.4 build-from-scratch on `provider_auth` with `session_blob`. D-55 paused; existing `garmin_auth` left in place untouched.
- **No `strava_activity_id` on `cardio_log` yet.** Spec §6 defers until Strava integration design (D-48). Re-add when D-48 lands.

**Forward move (next session, per Andy's preference):** **D-58–D-61 onboarding architectural design wave.** Single batched design session per the 3B handoff §7 forward pointer ("worth scoping as a single design wave rather than four isolated sessions, since the items interact heavily — account-first flow feeds Google Maps location flow feeds gear-from-proximity feeds session-unbinding"). Output: `Onboarding_Design_Wave_v1.md` (or equivalent — name TBD) covering D-58 OAuth-first flow, D-59 Google Maps Places + chain lookup, D-60 gear from proximity, D-61 session time tied to plan. After the design wave: `Athlete_Onboarding_Data_Spec_v5.md` rewriting §A / §B / §G / §J / §K + Account Config 1 against the wave's decisions.

**D-50 follow-on (parallel track, after design wave or before — independent):** per-provider route wiring. Strangler-fig roll-in pattern:
1. Build a small `provider_auth.py` helper module (UPSERT by `(user_id, provider)`, status transitions, token refresh helpers).
2. Migrate COROS (`routes/coros.py`) and RWGPS (`routes/ride_with_gps.py`) — the two shipped providers — to write through the new helper. Keep behavior identical; only the storage path changes.
3. Wire the stubbed providers (Polar, Wahoo, Strava, Whoop, Zwift, TrainingPeaks) onto `provider_auth` as their real OAuth flows ship.
4. Build webhook handlers to write into `webhook_events` and dispatch by `provider_user_id` → `user_id` mapping via `provider_auth`.
5. (D-55, paused) Rebuild Garmin onto `provider_auth.session_blob` when Garmin API access reopens.



**Recommended path:** start fresh chats for Layer 3 spec writing. Per L3-Discovery handoff §8: likely one session per node (3A, 3B, combined 3C+3D). Layer 3.5 does not get its own spec — folded into 3D's section in Layer 3 spec writing.

**Prerequisites that landed in L3-Spec-Trio (no longer blockers):**
- ✅ Layer 1 v3 with Non-Event Goal Type field (3B input contract)
- ✅ Integration spec v1 (3A input contract for integration data)
- ✅ Control_Spec v6 (4-node framing in §2)

**Open items for the spec writer:**
- 3A spec writer should explicitly leave open: how to weight self-report vs integration data when they conflict; confidence-tag thresholds.
- 3B spec writer must wait for Layer 1 v3 to be in project knowledge before writing input contract. Done — v3 shipped this session.
- 3C spec writer enumerates the 7 initial conflict rules (per L3-Discovery handoff §5.3) plus any patterns surfaced during 3A/3B testing.
- 3D spec writer formalizes the resolution gate semantics including blocker-severity escalation.

**Independent track (not blocking Layer 3):**
- D-50 Phase 1 integration deployment — schema migration + Vercel app code promotion. Deploy whenever after `Athlete_Data_Integration_Spec` v1 lands (now).
- D-51 Layer 1 field inventory — needed before full `q_layer1_payload` implementation but not before Layer 3 spec writing.
- D-52 Catalog Migration Plan Phase 1 — verification + mapping. Independent of pipeline work.

---

## Process notes

1. At every Layer 2 node-boundary (end of 2C consolidation, end of 2D design, end of 2E design):
   - Re-read this backlog
   - Flag any 🟡 Deferred item that the just-finished node interacted with — does it need to promote to 🔴 Blocker before the next node starts?
   - Add new findings from the just-finished node, categorized fresh

2. The "Blocker" status is sticky-up but not sticky-down. Once promoted to blocker, stays blocker until resolved. A blocker is resolved only by a fix, not by deciding to ignore it.

3. If a "Cleanup" item turns out to have functional impact mid-flight, it gets recategorized 🟡 or 🔴 with a note. No reverse path — pure documentation issues that surface as blockers were never just docs.

4. Drift findings DO carry over to the v20 xlsx work — see the existing drift report §4 and §5 for the v20-specific items. v20 xlsx is its own batch, not in this backlog's scope.

---

## Open items NOT in this backlog (out of scope)

- v20 xlsx authoring (separate batch)
- Spec v4 narrative sections beyond §4 schema blocks (§3 parsing rules, §5 query layer, §8 consumer table) — fold into FC-2 if quick, otherwise own session
- Hand-curated table protection rules (terrain_types enrichment, terrain_gap_rules, discipline_technique_foci) — needs a "v20 ETL doesn't touch these" rule. Lives with v20 batch.
- Layer 3 design — separate workstream
- **LEA / RED-S surveillance** — bone health, energy availability tracking. Out of v1 scope per 2026-05-11 decision during 2E design. v3+ candidate. Status: ⚪ Wont-Fix for v1.
